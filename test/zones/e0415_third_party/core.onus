module core
import vendor

pub fn pay(pence: Int where it >= 0) -> Int may alloc
  claims vendor.Settled
{
  return vendor.charge(pence: pence)
}

path paying
  entry pay
  require { vendor.Settled }
