module e0415_policy
import std.io
import lib.noisy

pub fn run(files: io.Files) -> Unit may io.file
  claims noisy.Quiet
{
  return noisy.leaf(files: files)
}

policy own_word_only
  forbid assume outside { self }

path p
  entry run
  require { noisy.Quiet }
  policy own_word_only
