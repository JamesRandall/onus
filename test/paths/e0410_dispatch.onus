module e0410_dispatch

interface Show[T] {
  fn show(x: T) -> Text
}

impl Show[Int] {
  fn show(x: Int) -> Text {
    return "int"
  }
}

fn describe[T: Show](x: T) -> Text {
  return Show.show(x: x)
}

pub fn run() -> Text {
  return describe(x: 1)
}

path p
  entry run
