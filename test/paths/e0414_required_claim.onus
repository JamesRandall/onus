module e0414_required_claim
import std.io

claim Quiet "Writes nothing."

fn leaf(files: io.Files) -> Unit may io.file {
  return Unit
}

pub fn run(files: io.Files) -> Unit may io.file {
  return leaf(files: files)
}

path p
  entry run
  require { Quiet }
