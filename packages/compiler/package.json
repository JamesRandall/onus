{
  "name": "@onus/compiler",
  "version": "0.0.0",
  "private": true,
  "description": "The Onus compiler library and CLI.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "bin": {
    "onus": "./dist/cli/main.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "tsc -p tsconfig.test.json && vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@noble/hashes": "^2.0.0",
    "@onus/runtime": "workspace:*"
  },
  "devDependencies": {
    "fast-check": "^4.0.0"
  }
}
