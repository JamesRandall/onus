module elided_body

pub fn f(x: Int) -> Int { ... }

fn g(x: Int) -> Int where it >= 0 may alloc
  requires x > 0
  ensures result == x
{ ... }

impl Ord[Int] {
  fn less(a: Int, b: Int) -> Bool { ... }
}
