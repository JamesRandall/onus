module e0204_not_propagated
import std.io

claim Quiet "Writes nothing."

fn leaf(files: io.Files) -> Unit may io.file {
  return Unit
}

fn mid(files: io.Files) -> Unit may io.file
  claims Quiet
{
  return leaf(files: files)
}
