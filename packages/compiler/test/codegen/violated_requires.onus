module violated_requires
import std.io

fn half(n: Int) -> Int
  requires n > 0
{
  return n / 2
}

pub fn main(args: List[Text]) -> Result[Unit, io.Error] ! panic {
  let x: Int = half(n: List.len(xs: args) - 5)
  return Ok(value: Unit)
}
