// Generated by onus from module consteval. Do not edit.
import * as $rt from "/Users/jamesrandall/code/onus/packages/runtime/dist/index.js";
import * as $context from "./context.js";
import * as $report from "./report.js";
import * as $defs from "./defs.js";
import * as $std_map from "./std/map.js";
import * as $std_list from "./std/list.js";
import * as $parser from "./parser.js";
import * as $values from "./values.js";
import * as $types from "./types.js";
import * as $evaluator from "./evaluator.js";
import * as $printer from "./printer.js";
import * as $effectset from "./effectset.js";
import * as $std_text from "./std/text.js";

const $ob1 = { kind: "overflow", text: "-1 within Int", at: "/Users/jamesrandall/code/onus/self/consteval.onus:61:20", def: "or_neg" };
const $ob2 = { kind: "overflow", text: "-1 within Int", at: "/Users/jamesrandall/code/onus/self/consteval.onus:414:20", def: "res_fn" };
const $ob3 = { kind: "overflow", text: "-1 within Int", at: "/Users/jamesrandall/code/onus/self/consteval.onus:419:19", def: "res_fn" };
const $ob4 = { kind: "overflow", text: "-1 within Int", at: "/Users/jamesrandall/code/onus/self/consteval.onus:594:10", def: "sig_or_empty" };
const $ob5 = { kind: "overflow", text: "-1 within Int", at: "/Users/jamesrandall/code/onus/self/consteval.onus:727:20", def: "const_text_literal" };
const $ob6 = { kind: "overflow", text: "i + 1 within Int", at: "/Users/jamesrandall/code/onus/self/consteval.onus:741:9", def: "const_text_literal" };
const $ob7 = { kind: "overflow", text: "positional + 1 within Int", at: "/Users/jamesrandall/code/onus/self/consteval.onus:757:22", def: "const_text_literal" };
const $ob8 = { kind: "overflow", text: "literal.start + 1 within Int", at: "/Users/jamesrandall/code/onus/self/consteval.onus:795:21", def: "grapheme_span" };
const $ob9 = { kind: "overflow", text: "literal.end - 1 within Int", at: "/Users/jamesrandall/code/onus/self/consteval.onus:796:19", def: "grapheme_span" };
const $ob10 = { kind: "overflow", text: "i + 1 within Int", at: "/Users/jamesrandall/code/onus/self/consteval.onus:821:20", def: "grapheme_span" };
const $ob11 = { kind: "overflow", text: "i + 1 within Int", at: "/Users/jamesrandall/code/onus/self/consteval.onus:822:56", def: "grapheme_span" };
const $ob12 = { kind: "overflow", text: "i + width within Int", at: "/Users/jamesrandall/code/onus/self/consteval.onus:829:9", def: "grapheme_span" };
const $ob13 = { kind: "overflow", text: "unit + Text.count(t: seg) within Int", at: "/Users/jamesrandall/code/onus/self/consteval.onus:843:16", def: "grapheme_span" };
const $ob14 = { kind: "overflow", text: "g + 1 within Int", at: "/Users/jamesrandall/code/onus/self/consteval.onus:844:13", def: "grapheme_span" };
const $ob15 = { kind: "overflow", text: "literal.start + 1 within Int", at: "/Users/jamesrandall/code/onus/self/consteval.onus:848:19", def: "grapheme_span" };
const $ob16 = { kind: "overflow", text: "literal.end - 1 within Int", at: "/Users/jamesrandall/code/onus/self/consteval.onus:852:15", def: "grapheme_span" };
const $ob17 = { kind: "overflow", text: "unit + width within Int", at: "/Users/jamesrandall/code/onus/self/consteval.onus:857:23", def: "grapheme_span" };
const $ob18 = { kind: "overflow", text: "base + from within Int", at: "/Users/jamesrandall/code/onus/self/consteval.onus:862:28", def: "grapheme_span" };
const $ob19 = { kind: "overflow", text: "base + to within Int", at: "/Users/jamesrandall/code/onus/self/consteval.onus:862:46", def: "grapheme_span" };
export function skip($args) {
  return undefined;
}

export function rep({ cp, ctx, code, at, detail }) {
  const [, ctx$1] = $context.report({ ctx: ctx, d: $report.diagnostic({ code: code, file: cp.current_file, span: at, def_name: cp.current_def, detail: detail }) });
  ctx = ctx$1;
  return [undefined, ctx];
  return [undefined, ctx];
}

export function key({ cp, tag, span }) {
  return $defs.node_key({ file: cp.current_file, tag: tag, span: span });
}

export function or_neg({ o }) {
  const $m1 = o;
  $m1$match: {
    if ($m1.tag === "Some") {
      const value = $m1.value;
      return value;
      break $m1$match;
    }
    if ($m1.tag === "None") {
      return $rt.int.neg(1, $ob1);
      break $m1$match;
    }
    $rt.unreachable();
  }
}

export function def_of_key({ ctx, k }) {
  return or_neg({ o: $std_map.find({ d: ctx.def_of, key: k }) });
}

export function copy_env({ env }) {
  let out = $std_map.dict({  });
  for (const k of $std_map.keys({ d: env })) {
    const $m2 = $std_map.find({ d: env, key: k });
    $m2$match: {
      if ($m2.tag === "Some") {
        const value = $m2.value;
        const [, out$2] = $std_map.set({ d: out, key: k, value: value });
        out = out$2;
        break $m2$match;
      }
      if ($m2.tag === "None") {
        skip({  });
        break $m2$match;
      }
      $rt.unreachable();
    }
  }
  return out;
}

export function item_name({ item }) {
  const $m3 = item;
  $m3$match: {
    if ($m3.tag === "FnItem") {
      const decl = $m3.decl;
      return decl.name.text;
      break $m3$match;
    }
    if ($m3.tag === "TypeAliasItem") {
      const decl = $m3.decl;
      return decl.name.text;
      break $m3$match;
    }
    if ($m3.tag === "IntrinsicTypeItem") {
      const decl = $m3.decl;
      return decl.name.text;
      break $m3$match;
    }
    if ($m3.tag === "ConstItem") {
      const decl = $m3.decl;
      return decl.name.text;
      break $m3$match;
    }
    if ($m3.tag === "RecordItem") {
      const decl = $m3.decl;
      return decl.name.text;
      break $m3$match;
    }
    if ($m3.tag === "UnionItem") {
      const decl = $m3.decl;
      return decl.name.text;
      break $m3$match;
    }
    if ($m3.tag === "InterfaceItem") {
      const decl = $m3.decl;
      return decl.name.text;
      break $m3$match;
    }
    if ($m3.tag === "ImplItem") {
      const decl = $m3.decl;
      return decl.iface.text;
      break $m3$match;
    }
    if ($m3.tag === "ClaimItem") {
      const decl = $m3.decl;
      return decl.name.text;
      break $m3$match;
    }
    if ($m3.tag === "CapabilityItem") {
      const decl = $m3.decl;
      return decl.name.text;
      break $m3$match;
    }
    if ($m3.tag === "PathItem") {
      const decl = $m3.decl;
      return decl.name.text;
      break $m3$match;
    }
    if ($m3.tag === "PolicyItem") {
      const decl = $m3.decl;
      return decl.name.text;
      break $m3$match;
    }
    if ($m3.tag === "ExampleItem") {
      const decl = $m3.decl;
      return decl.name.text;
      break $m3$match;
    }
    if ($m3.tag === "PropertyItem") {
      const decl = $m3.decl;
      return decl.name.text;
      break $m3$match;
    }
    $rt.unreachable();
  }
}

export function calls_in_type({ t, out }) {
  const $m4 = t;
  $m4$match: {
    if ($m4.tag === "FnType") {
      const params = $m4.params;
      const ret = $m4.ret;
      const [, out$3] = calls_in_params({ ps: params, out: out });
      out = out$3;
      const [, out$4] = calls_in_type({ t: ret, out: out });
      out = out$4;
      break $m4$match;
    }
    if ($m4.tag === "NamedType") {
      const args = $m4.args;
      const where_ = $m4.where_;
      for (const a of args) {
        const $m5 = a;
        $m5$match: {
          if ($m5.tag === "TypeArgType") {
            const ty = $m5.ty;
            const [, out$5] = calls_in_type({ t: ty, out: out });
            out = out$5;
            break $m5$match;
          }
          if ($m5.tag === "TypeArgConst") {
            const expr = $m5.expr;
            const [, out$6] = calls_in_expr({ e: expr, out: out });
            out = out$6;
            break $m5$match;
          }
          $rt.unreachable();
        }
      }
      const $m6 = where_;
      $m6$match: {
        if ($m6.tag === "Some") {
          const value = $m6.value;
          const [, out$7] = calls_in_expr({ e: value, out: out });
          out = out$7;
          break $m6$match;
        }
        if ($m6.tag === "None") {
          skip({  });
          break $m6$match;
        }
        $rt.unreachable();
      }
      break $m4$match;
    }
    $rt.unreachable();
  }
  return [undefined, out];
  return [undefined, out];
}

export function calls_in_params({ ps, out }) {
  for (const p of ps) {
    const [, out$8] = calls_in_type({ t: p.ty, out: out });
    out = out$8;
  }
  return [undefined, out];
  return [undefined, out];
}

export function calls_in_targs({ ts, out }) {
  for (const a of ts) {
    const $m7 = a;
    $m7$match: {
      if ($m7.tag === "TypeArgType") {
        const ty = $m7.ty;
        const [, out$9] = calls_in_type({ t: ty, out: out });
        out = out$9;
        break $m7$match;
      }
      if ($m7.tag === "TypeArgConst") {
        const expr = $m7.expr;
        const [, out$10] = calls_in_expr({ e: expr, out: out });
        out = out$10;
        break $m7$match;
      }
      $rt.unreachable();
    }
  }
  return [undefined, out];
  return [undefined, out];
}

export function calls_in_block({ b, out }) {
  for (const s of b.stmts) {
    const [, out$11] = calls_in_stmt({ s: s, out: out });
    out = out$11;
  }
  return [undefined, out];
  return [undefined, out];
}

export function calls_in_stmt({ s, out }) {
  const $m8 = s;
  $m8$match: {
    if ($m8.tag === "Let") {
      const ty = $m8.ty;
      const value = $m8.value;
      const [, out$12] = calls_in_type({ t: ty, out: out });
      out = out$12;
      const [, out$13] = calls_in_expr({ e: value, out: out });
      out = out$13;
      break $m8$match;
    }
    if ($m8.tag === "Var") {
      const ty = $m8.ty;
      const value = $m8.value;
      const [, out$14] = calls_in_type({ t: ty, out: out });
      out = out$14;
      const [, out$15] = calls_in_expr({ e: value, out: out });
      out = out$15;
      break $m8$match;
    }
    if ($m8.tag === "Assign") {
      const value = $m8.value;
      const [, out$16] = calls_in_expr({ e: value, out: out });
      out = out$16;
      break $m8$match;
    }
    if ($m8.tag === "Return") {
      const value = $m8.value;
      const [, out$17] = calls_in_expr({ e: value, out: out });
      out = out$17;
      break $m8$match;
    }
    if ($m8.tag === "If") {
      const cond = $m8.cond;
      const then_block = $m8.then_block;
      const else_block = $m8.else_block;
      const [, out$18] = calls_in_expr({ e: cond, out: out });
      out = out$18;
      const [, out$19] = calls_in_block({ b: then_block, out: out });
      out = out$19;
      const $m9 = else_block;
      $m9$match: {
        if ($m9.tag === "Some") {
          const value = $m9.value;
          const [, out$20] = calls_in_block({ b: value, out: out });
          out = out$20;
          break $m9$match;
        }
        if ($m9.tag === "None") {
          skip({  });
          break $m9$match;
        }
        $rt.unreachable();
      }
      break $m8$match;
    }
    if ($m8.tag === "Match") {
      const scrutinee = $m8.scrutinee;
      const arms = $m8.arms;
      const [, out$21] = calls_in_expr({ e: scrutinee, out: out });
      out = out$21;
      for (const a of arms) {
        const [, out$22] = calls_in_pattern({ p: a.pattern, out: out });
        out = out$22;
        const $m10 = a.guard;
        $m10$match: {
          if ($m10.tag === "Some") {
            const value = $m10.value;
            const [, out$23] = calls_in_expr({ e: value, out: out });
            out = out$23;
            break $m10$match;
          }
          if ($m10.tag === "None") {
            skip({  });
            break $m10$match;
          }
          $rt.unreachable();
        }
        const $m11 = a.body;
        $m11$match: {
          if ($m11.tag === "ArmBlock") {
            const block = $m11.block;
            const [, out$24] = calls_in_block({ b: block, out: out });
            out = out$24;
            break $m11$match;
          }
          if ($m11.tag === "ArmStmt") {
            const stmt = $m11.stmt;
            const [, out$25] = calls_in_stmt({ s: stmt, out: out });
            out = out$25;
            break $m11$match;
          }
          $rt.unreachable();
        }
      }
      break $m8$match;
    }
    if ($m8.tag === "Loop") {
      const cond = $m8.cond;
      const clauses = $m8.clauses;
      const body = $m8.body;
      const [, out$26] = calls_in_expr({ e: cond, out: out });
      out = out$26;
      for (const c of clauses) {
        const [, out$27] = calls_in_expr({ e: c.expr, out: out });
        out = out$27;
      }
      const [, out$28] = calls_in_block({ b: body, out: out });
      out = out$28;
      break $m8$match;
    }
    if ($m8.tag === "For") {
      const ty = $m8.ty;
      const domain = $m8.domain;
      const body = $m8.body;
      const [, out$29] = calls_in_type({ t: ty, out: out });
      out = out$29;
      const $m12 = domain;
      $m12$match: {
        if ($m12.tag === "RangeDomain") {
          const lo = $m12.lo;
          const hi = $m12.hi;
          const [, out$30] = calls_in_expr({ e: lo, out: out });
          out = out$30;
          const [, out$31] = calls_in_expr({ e: hi, out: out });
          out = out$31;
          break $m12$match;
        }
        if ($m12.tag === "InDomain") {
          const expr = $m12.expr;
          const [, out$32] = calls_in_expr({ e: expr, out: out });
          out = out$32;
          break $m12$match;
        }
        $rt.unreachable();
      }
      const [, out$33] = calls_in_block({ b: body, out: out });
      out = out$33;
      break $m8$match;
    }
    if ($m8.tag === "Assume") {
      const verify_block = $m8.verify_block;
      const $m13 = verify_block;
      $m13$match: {
        if ($m13.tag === "Some") {
          const value = $m13.value;
          const [, out$34] = calls_in_params({ ps: value.params, out: out });
          out = out$34;
          const [, out$35] = calls_in_block({ b: value.body, out: out });
          out = out$35;
          break $m13$match;
        }
        if ($m13.tag === "None") {
          skip({  });
          break $m13$match;
        }
        $rt.unreachable();
      }
      break $m8$match;
    }
    if ($m8.tag === "ExprStmt") {
      const expr = $m8.expr;
      const [, out$36] = calls_in_expr({ e: expr, out: out });
      out = out$36;
      break $m8$match;
    }
    $rt.unreachable();
  }
  return [undefined, out];
  return [undefined, out];
}

export function calls_in_pattern({ p, out }) {
  const $m14 = p;
  $m14$match: {
    if ($m14.tag === "LitPat") {
      const literal = $m14.literal;
      const [, out$37] = calls_in_expr({ e: literal, out: out });
      out = out$37;
      break $m14$match;
    }
    if (true) {
      skip({  });
      break $m14$match;
    }
    $rt.unreachable();
  }
  return [undefined, out];
  return [undefined, out];
}

export function calls_in_expr({ e, out }) {
  const $m15 = e;
  $m15$match: {
    if ($m15.tag === "Ctor") {
      const args = $m15.args;
      const fields = $m15.fields;
      const $m16 = args;
      $m16$match: {
        if ($m16.tag === "Some") {
          const value = $m16.value;
          for (const a of value) {
            const [, out$38] = calls_in_expr({ e: a.value, out: out });
            out = out$38;
          }
          break $m16$match;
        }
        if ($m16.tag === "None") {
          skip({  });
          break $m16$match;
        }
        $rt.unreachable();
      }
      const $m17 = fields;
      $m17$match: {
        if ($m17.tag === "Some") {
          const value = $m17.value;
          for (const f of value) {
            const [, out$39] = calls_in_expr({ e: f.value, out: out });
            out = out$39;
          }
          break $m17$match;
        }
        if ($m17.tag === "None") {
          skip({  });
          break $m17$match;
        }
        $rt.unreachable();
      }
      break $m15$match;
    }
    if ($m15.tag === "RecordUpdate") {
      const base = $m15.base;
      const fields = $m15.fields;
      const [, out$40] = calls_in_expr({ e: base, out: out });
      out = out$40;
      for (const f of fields) {
        const [, out$41] = calls_in_expr({ e: f.value, out: out });
        out = out$41;
      }
      break $m15$match;
    }
    if ($m15.tag === "ListLit") {
      const elems = $m15.elems;
      for (const x of elems) {
        const [, out$42] = calls_in_expr({ e: x, out: out });
        out = out$42;
      }
      break $m15$match;
    }
    if ($m15.tag === "Try") {
      const expr = $m15.expr;
      const else_ = $m15.else_;
      const [, out$43] = calls_in_expr({ e: expr, out: out });
      out = out$43;
      const $m18 = else_;
      $m18$match: {
        if ($m18.tag === "Some") {
          const value = $m18.value;
          const [, out$44] = calls_in_expr({ e: value.expr, out: out });
          out = out$44;
          break $m18$match;
        }
        if ($m18.tag === "None") {
          skip({  });
          break $m18$match;
        }
        $rt.unreachable();
      }
      break $m15$match;
    }
    if ($m15.tag === "Recover") {
      const body = $m15.body;
      const [, out$45] = calls_in_block({ b: body, out: out });
      out = out$45;
      break $m15$match;
    }
    if ($m15.tag === "Quantifier") {
      const ty = $m15.ty;
      const domain = $m15.domain;
      const where_ = $m15.where_;
      const body = $m15.body;
      const [, out$46] = calls_in_type({ t: ty, out: out });
      out = out$46;
      const $m19 = domain;
      $m19$match: {
        if ($m19.tag === "Some") {
          const value = $m19.value;
          const $m20 = value;
          $m20$match: {
            if ($m20.tag === "RangeDomain") {
              const lo = $m20.lo;
              const hi = $m20.hi;
              const [, out$47] = calls_in_expr({ e: lo, out: out });
              out = out$47;
              const [, out$48] = calls_in_expr({ e: hi, out: out });
              out = out$48;
              break $m20$match;
            }
            if ($m20.tag === "InDomain") {
              const expr = $m20.expr;
              const [, out$49] = calls_in_expr({ e: expr, out: out });
              out = out$49;
              break $m20$match;
            }
            $rt.unreachable();
          }
          break $m19$match;
        }
        if ($m19.tag === "None") {
          skip({  });
          break $m19$match;
        }
        $rt.unreachable();
      }
      const $m21 = where_;
      $m21$match: {
        if ($m21.tag === "Some") {
          const value = $m21.value;
          const [, out$50] = calls_in_expr({ e: value, out: out });
          out = out$50;
          break $m21$match;
        }
        if ($m21.tag === "None") {
          skip({  });
          break $m21$match;
        }
        $rt.unreachable();
      }
      const [, out$51] = calls_in_expr({ e: body, out: out });
      out = out$51;
      break $m15$match;
    }
    if ($m15.tag === "Closure") {
      const params = $m15.params;
      const ret = $m15.ret;
      const body = $m15.body;
      const [, out$52] = calls_in_params({ ps: params, out: out });
      out = out$52;
      const [, out$53] = calls_in_type({ t: ret, out: out });
      out = out$53;
      const [, out$54] = calls_in_block({ b: body, out: out });
      out = out$54;
      break $m15$match;
    }
    if ($m15.tag === "Fake") {
      const fields = $m15.fields;
      for (const f of fields) {
        const [, out$55] = calls_in_expr({ e: f.value, out: out });
        out = out$55;
      }
      break $m15$match;
    }
    if ($m15.tag === "FieldAccess") {
      const object = $m15.object;
      const [, out$56] = calls_in_expr({ e: object, out: out });
      out = out$56;
      break $m15$match;
    }
    if ($m15.tag === "Call") {
      const callee = $m15.callee;
      const targs = $m15.targs;
      const args = $m15.args;
      const [, out$57] = $std_list.push({ b: out, x: e });
      out = out$57;
      const [, out$58] = calls_in_expr({ e: callee, out: out });
      out = out$58;
      const $m22 = targs;
      $m22$match: {
        if ($m22.tag === "Some") {
          const value = $m22.value;
          const [, out$59] = calls_in_targs({ ts: value, out: out });
          out = out$59;
          break $m22$match;
        }
        if ($m22.tag === "None") {
          skip({  });
          break $m22$match;
        }
        $rt.unreachable();
      }
      for (const a of args) {
        const [, out$60] = calls_in_expr({ e: a.value, out: out });
        out = out$60;
      }
      break $m15$match;
    }
    if ($m15.tag === "Unary") {
      const operand = $m15.operand;
      const [, out$61] = calls_in_expr({ e: operand, out: out });
      out = out$61;
      break $m15$match;
    }
    if ($m15.tag === "Binary") {
      const left = $m15.left;
      const right = $m15.right;
      const [, out$62] = calls_in_expr({ e: left, out: out });
      out = out$62;
      const [, out$63] = calls_in_expr({ e: right, out: out });
      out = out$63;
      break $m15$match;
    }
    if ($m15.tag === "And") {
      const operands = $m15.operands;
      for (const o of operands) {
        const [, out$64] = calls_in_expr({ e: o, out: out });
        out = out$64;
      }
      break $m15$match;
    }
    if ($m15.tag === "Or") {
      const operands = $m15.operands;
      for (const o of operands) {
        const [, out$65] = calls_in_expr({ e: o, out: out });
        out = out$65;
      }
      break $m15$match;
    }
    if ($m15.tag === "Is") {
      const expr = $m15.expr;
      const pattern = $m15.pattern;
      const [, out$66] = calls_in_expr({ e: expr, out: out });
      out = out$66;
      const [, out$67] = calls_in_pattern({ p: pattern, out: out });
      out = out$67;
      break $m15$match;
    }
    if (true) {
      skip({  });
      break $m15$match;
    }
    $rt.unreachable();
  }
  return [undefined, out];
  return [undefined, out];
}

export function calls_in_tparams({ ps, out }) {
  for (const p of ps) {
    const $m23 = p;
    $m23$match: {
      if ($m23.tag === "ConstParam") {
        const ty = $m23.ty;
        const [, out$68] = calls_in_type({ t: ty, out: out });
        out = out$68;
        break $m23$match;
      }
      if (true) {
        skip({  });
        break $m23$match;
      }
      $rt.unreachable();
    }
  }
  return [undefined, out];
  return [undefined, out];
}

export function calls_in_fn({ f, out }) {
  const [, out$69] = calls_in_tparams({ ps: f.tparams, out: out });
  out = out$69;
  const [, out$70] = calls_in_params({ ps: f.params, out: out });
  out = out$70;
  const [, out$71] = calls_in_type({ t: f.ret, out: out });
  out = out$71;
  for (const c of f.contracts) {
    const [, out$72] = calls_in_expr({ e: c.expr, out: out });
    out = out$72;
  }
  const $m24 = f.body;
  $m24$match: {
    if ($m24.tag === "Some") {
      const value = $m24.value;
      const [, out$73] = calls_in_block({ b: value, out: out });
      out = out$73;
      break $m24$match;
    }
    if ($m24.tag === "None") {
      skip({  });
      break $m24$match;
    }
    $rt.unreachable();
  }
  return [undefined, out];
  return [undefined, out];
}

export function calls_in_iface_item({ member, out }) {
  const $m25 = member;
  $m25$match: {
    if ($m25.tag === "IfaceFnItem") {
      const decl = $m25.decl;
      const [, out$74] = calls_in_params({ ps: decl.params, out: out });
      out = out$74;
      const [, out$75] = calls_in_type({ t: decl.ret, out: out });
      out = out$75;
      for (const c of decl.contracts) {
        const [, out$76] = calls_in_expr({ e: c.expr, out: out });
        out = out$76;
      }
      break $m25$match;
    }
    if ($m25.tag === "LawItem") {
      const decl = $m25.decl;
      const [, out$77] = calls_in_params({ ps: decl.params, out: out });
      out = out$77;
      const [, out$78] = calls_in_block({ b: decl.body, out: out });
      out = out$78;
      break $m25$match;
    }
    $rt.unreachable();
  }
  return [undefined, out];
  return [undefined, out];
}

export function calls_in_item({ item }) {
  let out = $std_list.builder({  });
  const $m26 = item;
  $m26$match: {
    if ($m26.tag === "FnItem") {
      const decl = $m26.decl;
      const [, out$79] = calls_in_fn({ f: decl, out: out });
      out = out$79;
      break $m26$match;
    }
    if ($m26.tag === "TypeAliasItem") {
      const decl = $m26.decl;
      const [, out$80] = calls_in_type({ t: decl.ty, out: out });
      out = out$80;
      break $m26$match;
    }
    if ($m26.tag === "IntrinsicTypeItem") {
      const decl = $m26.decl;
      const [, out$81] = calls_in_tparams({ ps: decl.tparams, out: out });
      out = out$81;
      break $m26$match;
    }
    if ($m26.tag === "ConstItem") {
      const decl = $m26.decl;
      const [, out$82] = calls_in_type({ t: decl.ty, out: out });
      out = out$82;
      const [, out$83] = calls_in_expr({ e: decl.value, out: out });
      out = out$83;
      break $m26$match;
    }
    if ($m26.tag === "RecordItem") {
      const decl = $m26.decl;
      const [, out$84] = calls_in_tparams({ ps: decl.tparams, out: out });
      out = out$84;
      for (const f of decl.fields) {
        const [, out$85] = calls_in_type({ t: f.ty, out: out });
        out = out$85;
      }
      break $m26$match;
    }
    if ($m26.tag === "UnionItem") {
      const decl = $m26.decl;
      const [, out$86] = calls_in_tparams({ ps: decl.tparams, out: out });
      out = out$86;
      for (const v of decl.variants) {
        for (const f of v.fields) {
          const [, out$87] = calls_in_type({ t: f.ty, out: out });
          out = out$87;
        }
      }
      break $m26$match;
    }
    if ($m26.tag === "InterfaceItem") {
      const decl = $m26.decl;
      for (const member of decl.items) {
        const [, out$88] = calls_in_iface_item({ member: member, out: out });
        out = out$88;
      }
      break $m26$match;
    }
    if ($m26.tag === "ImplItem") {
      const decl = $m26.decl;
      const [, out$89] = calls_in_type({ t: decl.target, out: out });
      out = out$89;
      for (const f of decl.fns) {
        const [, out$90] = calls_in_fn({ f: f, out: out });
        out = out$90;
      }
      break $m26$match;
    }
    if ($m26.tag === "CapabilityItem") {
      const decl = $m26.decl;
      const [, out$91] = calls_in_tparams({ ps: decl.tparams, out: out });
      out = out$91;
      for (const g of decl.grant_list) {
        const $m27 = g.when_cond;
        $m27$match: {
          if ($m27.tag === "Some") {
            const value = $m27.value;
            const [, out$92] = calls_in_expr({ e: value, out: out });
            out = out$92;
            break $m27$match;
          }
          if ($m27.tag === "None") {
            skip({  });
            break $m27$match;
          }
          $rt.unreachable();
        }
      }
      break $m26$match;
    }
    if ($m26.tag === "ExampleItem") {
      const decl = $m26.decl;
      const [, out$93] = calls_in_block({ b: decl.body, out: out });
      out = out$93;
      break $m26$match;
    }
    if ($m26.tag === "PropertyItem") {
      const decl = $m26.decl;
      const [, out$94] = calls_in_params({ ps: decl.params, out: out });
      out = out$94;
      const [, out$95] = calls_in_block({ b: decl.body, out: out });
      out = out$95;
      break $m26$match;
    }
    if (true) {
      skip({  });
      break $m26$match;
    }
    $rt.unreachable();
  }
  return $std_list.finish({ b: out });
}

export function res_fn({ ctx, k }) {
  const $m28 = $std_map.find({ d: ctx.refs, key: k });
  $m28$match: {
    if ($m28.tag === "None") {
      return $rt.int.neg(1, $ob2);
      break $m28$match;
    }
    if ($m28.tag === "Some") {
      const value = $m28.value;
      const $m29 = value;
      $m29$match: {
        if ($m29.tag === "DefRes") {
          const def = $m29.def;
          return def;
          break $m29$match;
        }
        if ($m29.tag === "CompanionRes") {
          const owner = $m29.owner;
          const fn_def = $m29.fn_def;
          return fn_def;
          break $m29$match;
        }
        if (true) {
          return $rt.int.neg(1, $ob3);
          break $m29$match;
        }
        $rt.unreachable();
      }
      break $m28$match;
    }
    $rt.unreachable();
  }
}

export function proved_clauses({ sig }) {
  let out = $std_list.builder({  });
  for (const c of sig.contracts) {
    if (c.clause === "requires" && c.is_proved) {
      const [, out$96] = $std_list.push({ b: out, x: c });
      out = out$96;
    }
  }
  return $std_list.finish({ b: out });
}

export function find_arg({ args, name }) {
  for (const a of args) {
    if (a.name.text === name) {
      return { tag: "Some", value: a };
    }
  }
  return { tag: "None" };
}

export function record_proved({ ctx, call_key, clause_key }) {
  let current = [];
  const $m32 = $std_map.find({ d: ctx.proved_at_check_time, key: call_key });
  $m32$match: {
    if ($m32.tag === "Some") {
      const value = $m32.value;
      current = value;
      break $m32$match;
    }
    if ($m32.tag === "None") {
      current = [];
      break $m32$match;
    }
    $rt.unreachable();
  }
  let table = ctx.proved_at_check_time;
  const [, table$97] = $std_map.set({ d: table, key: call_key, value: $std_list.append({ xs: current, x: clause_key }) });
  table = table$97;
  return [undefined, ctx];
  return [undefined, ctx];
}

export function check_call({ cp, ev, ctx, e }) {
  const $m33 = e;
  $m33$match: {
    if ($m33.tag === "Call") {
      const callee = $m33.callee;
      const targs = $m33.targs;
      const args = $m33.args;
      const span = $m33.span;
      const fn_id = res_fn({ ctx: ctx, k: key({ cp: cp, tag: $defs.tag_expr, span: $parser.span_of_expr({ e: callee }) }) });
      if (fn_id < 0) {
        return [undefined, ev, ctx];
      }
      const fn_def = $context.get_def({ ctx: ctx, id: fn_id });
      if (!$rt.eq(fn_def.kind, { tag: "Fn" })) {
        return [undefined, ev, ctx];
      }
      let sig_opt = $std_map.find({ d: ctx.signatures, key: fn_id });
      if (sig_opt.tag === "None") {
        return [undefined, ev, ctx];
      }
      const sig = sig_or_empty({ o: sig_opt });
      const clauses = proved_clauses({ sig: sig });
      if ($std_list.len({ xs: clauses }) === 0) {
        return [undefined, ev, ctx];
      }
      let env = $std_map.dict({  });
      const call_key = key({ cp: cp, tag: $defs.tag_expr, span: span });
      let inst = [];
      const $m35 = $std_map.find({ d: ctx.instantiations, key: call_key });
      $m35$match: {
        if ($m35.tag === "Some") {
          const value = $m35.value;
          inst = value;
          break $m35$match;
        }
        if ($m35.tag === "None") {
          inst = [];
          break $m35$match;
        }
        $rt.unreachable();
      }
      const $hi36 = $std_list.len({ xs: sig.tparams });
      for (let i = 0; i < $hi36; i++) {
        const p = $std_list.get({ xs: sig.tparams, i: i });
        if (p.tag === "ConstP") {
          let bound = { tag: "None" };
          if (i < $std_list.len({ xs: inst })) {
            const a = $std_list.get({ xs: inst, i: i });
            if (a.tag === "ConstA") {
              bound = $values.of_const({ c: $types.arg_const({ a: a }) });
            }
          }
          const $m38 = bound;
          $m38$match: {
            if ($m38.tag === "Some") {
              const value = $m38.value;
              const [, env$98] = $std_map.set({ d: env, key: $types.tparam_def({ p: p }), value: value });
              env = env$98;
              break $m38$match;
            }
            if ($m38.tag === "None") {
              return [undefined, ev, ctx];
              break $m38$match;
            }
            $rt.unreachable();
          }
        }
      }
      const $hi39 = $std_list.len({ xs: sig.params });
      for (let i = 0; i < $hi39; i++) {
        const p = $std_list.get({ xs: sig.params, i: i });
        if (i >= $std_list.len({ xs: sig.param_defs })) {
          return [undefined, ev, ctx];
        }
        const $m40 = find_arg({ args: args, name: p.name });
        $m40$match: {
          if ($m40.tag === "None") {
            return [undefined, ev, ctx];
            break $m40$match;
          }
          if ($m40.tag === "Some") {
            const value = $m40.value;
            const [, ev$99] = $evaluator.reset({ ev: ev });
            ev = ev$99;
            const [$r41, ev$100, ctx$100] = $evaluator.eval_expr({ fuel: $evaluator.max_depth, ev: ev, ctx: ctx, e: value.value, env: $std_map.dict({  }) });
            ev = ev$100;
            ctx = ctx$100;
            const r = $r41;
            if (r.tag === "Ok") {
              const [, env$101] = $std_map.set({ d: env, key: $std_list.get({ xs: sig.param_defs, i: i }), value: ok_or_unit({ r: r }) });
              env = env$101;
            }
            break $m40$match;
          }
          $rt.unreachable();
        }
      }
      const saved_file = ev.file;
      ev = { ...ev, file: fn_def.file };
      for (const clause of clauses) {
        const [, ev$102] = $evaluator.reset({ ev: ev });
        ev = ev$102;
        const [$r44, ev$103, ctx$103] = $evaluator.eval_expr({ fuel: $evaluator.max_depth, ev: ev, ctx: ctx, e: clause.expr, env: copy_env({ env: env }) });
        ev = ev$103;
        ctx = ctx$103;
        const $m43 = $r44;
        $m43$match: {
          if ($m43.tag === "Err") {
            const error = $m43.error;
            const $m45 = error;
            $m45$match: {
              if ($m45.tag === "PanicF") {
                const detail = $m45.detail;
                const [, ctx$104] = rep({ cp: cp, ctx: ctx, code: "E0701", at: span, detail: "evaluating `requires proved " + $printer.print_expr({ e: clause.expr }) + "` of `" + fn_def.name + "`: " + detail });
                ctx = ctx$104;
                break $m45$match;
              }
              if ($m45.tag === "BudgetF") {
                const fn_name = $m45.fn_name;
                const [, ctx$105] = rep({ cp: cp, ctx: ctx, code: "E0501", at: span, detail: "evaluating `requires proved " + $printer.print_expr({ e: clause.expr }) + "` of `" + fn_def.name + "` exceeded the check-time step budget in `" + fn_name + "`" });
                ctx = ctx$105;
                break $m45$match;
              }
              if (true) {
                skip({  });
                break $m45$match;
              }
              $rt.unreachable();
            }
            break $m43$match;
          }
          if ($m43.tag === "Ok") {
            const value = $m43.value;
            if (value.tag === "BoolV" && $values.bool_of({ c: value })) {
              const [, ctx$106] = record_proved({ ctx: ctx, call_key: call_key, clause_key: $defs.node_key({ file: fn_def.file, tag: $defs.tag_sig, span: clause.span }) });
              ctx = ctx$106;
            } else {
              const [, ctx$107] = library_check_failed({ cp: cp, ev: ev, ctx: ctx, at: span, fn_def: fn_def, clause: clause, targs: targs });
              ctx = ctx$107;
              ev = { ...ev, file: saved_file };
              return [undefined, ev, ctx];
            }
            break $m43$match;
          }
          $rt.unreachable();
        }
      }
      ev = { ...ev, file: saved_file };
      break $m33$match;
    }
    if (true) {
      skip({  });
      break $m33$match;
    }
    $rt.unreachable();
  }
  return [undefined, ev, ctx];
  return [undefined, ev, ctx];
}

export function sig_or_empty({ o }) {
  const $m48 = o;
  $m48$match: {
    if ($m48.tag === "Some") {
      const value = $m48.value;
      return value;
      break $m48$match;
    }
    if ($m48.tag === "None") {
      return { def: $rt.int.neg(1, $ob4), tparams: [], params: [], param_defs: [], ret: { tag: "ErrorT" }, effects: effectset_empty({  }), contracts: [], is_const_fn: false, is_intrinsic: false };
      break $m48$match;
    }
    $rt.unreachable();
  }
}

export function effectset_empty($args) {
  return $effectset.empty({  });
}

export function library_check_failed({ cp, ev, ctx, at, fn_def, clause, targs }) {
  let span = at;
  let message = "`requires proved " + $printer.print_expr({ e: clause.expr }) + "` of `" + fn_def.name + "` is false";
  const $m51 = ev.last_const_error;
  $m51$match: {
    if ($m51.tag === "Some") {
      const value = $m51.value;
      if (value.tag === "RecordV") {
        const fs = $values.fields_of({ c: value });
        const msg = $values.field_of({ fields: fs, name: "message" });
        if (msg.tag === "Some" && value_or_unit({ o: msg }).tag === "TextV") {
          message = $values.text_of({ c: value_or_unit({ o: msg }) });
        }
        const offset = $values.field_of({ fields: fs, name: "offset" });
        const literal = const_text_literal({ ctx: ctx, fn_def: fn_def, targs: targs });
        if (offset.tag === "Some" && literal.tag === "Some") {
          const ov = value_or_unit({ o: offset });
          if (ov.tag === "IntV" && $values.int_of({ c: ov }) >= 0) {
            span = grapheme_span({ ctx: ctx, file: cp.current_file, literal: span_or_zero({ o: literal }), grapheme_index: $values.int_of({ c: ov }) });
          }
        }
      }
      break $m51$match;
    }
    if ($m51.tag === "None") {
      skip({  });
      break $m51$match;
    }
    $rt.unreachable();
  }
  const [, ctx$108] = $context.report({ ctx: ctx, d: { code: "E0700", file: cp.current_file, span: span, def_name: cp.current_def, context: [message, "in the `requires proved` clause of `" + $context.qualified_name({ ctx: ctx, id: fn_def.id }) + "`"], obligation: { tag: "Some", value: { kind: "requires", text: $printer.print_expr({ e: clause.expr }), status: "failed", has_counterexample: false, counterexample: [] } }, repairs: [] } });
  ctx = ctx$108;
  return [undefined, ctx];
  return [undefined, ctx];
}

export function ok_or_unit({ r }) {
  const $m55 = r;
  $m55$match: {
    if ($m55.tag === "Ok") {
      const value = $m55.value;
      return value;
      break $m55$match;
    }
    if ($m55.tag === "Err") {
      return { tag: "UnitV" };
      break $m55$match;
    }
    $rt.unreachable();
  }
}

export function ident_text({ o }) {
  const $m57 = o;
  $m57$match: {
    if ($m57.tag === "Some") {
      const value = $m57.value;
      return value.text;
      break $m57$match;
    }
    if ($m57.tag === "None") {
      return "";
      break $m57$match;
    }
    $rt.unreachable();
  }
}

export function value_or_unit({ o }) {
  const $m58 = o;
  $m58$match: {
    if ($m58.tag === "Some") {
      const value = $m58.value;
      return value;
      break $m58$match;
    }
    if ($m58.tag === "None") {
      return { tag: "UnitV" };
      break $m58$match;
    }
    $rt.unreachable();
  }
}

export function span_or_zero({ o }) {
  const $m60 = o;
  $m60$match: {
    if ($m60.tag === "Some") {
      const value = $m60.value;
      return value;
      break $m60$match;
    }
    if ($m60.tag === "None") {
      return { start: 0, end: 0 };
      break $m60$match;
    }
    $rt.unreachable();
  }
}

export function type_arg_label({ a }) {
  const $m62 = a;
  $m62$match: {
    if ($m62.tag === "TypeArgType") {
      const label = $m62.label;
      return label;
      break $m62$match;
    }
    if ($m62.tag === "TypeArgConst") {
      const label = $m62.label;
      return label;
      break $m62$match;
    }
    $rt.unreachable();
  }
}

export function text_literal_span({ a }) {
  const $m63 = a;
  $m63$match: {
    if ($m63.tag === "TypeArgConst") {
      const expr = $m63.expr;
      if (expr.tag === "TextLit") {
        return { tag: "Some", value: $parser.span_of_expr({ e: expr }) };
      }
      return { tag: "None" };
      break $m63$match;
    }
    if (true) {
      return { tag: "None" };
      break $m63$match;
    }
    $rt.unreachable();
  }
}

export function const_text_literal({ ctx, fn_def, targs }) {
  let tps = [];
  const $m67 = $std_map.find({ d: ctx.type_params, key: fn_def.id });
  $m67$match: {
    if ($m67.tag === "Some") {
      const value = $m67.value;
      tps = value;
      break $m67$match;
    }
    if ($m67.tag === "None") {
      tps = [];
      break $m67$match;
    }
    $rt.unreachable();
  }
  let index = $rt.int.neg(1, $ob5);
  let pname = "";
  let i = 0;
  for (const p of tps) {
    if (index < 0) {
      const $m68 = p;
      $m68$match: {
        if ($m68.tag === "ConstP") {
          const def = $m68.def;
          const ty = $m68.ty;
          if ($types.prim_name({ t: $types.strip({ t: ty }) }) === "Text") {
            index = i;
            pname = $context.get_def({ ctx: ctx, id: def }).name;
          }
          break $m68$match;
        }
        if (true) {
          skip({  });
          break $m68$match;
        }
        $rt.unreachable();
      }
    }
    i = $rt.int.add(i, 1, $ob6);
  }
  if (index < 0) {
    return { tag: "None" };
  }
  const $m70 = targs;
  $m70$match: {
    if ($m70.tag === "None") {
      return { tag: "None" };
      break $m70$match;
    }
    if ($m70.tag === "Some") {
      const value = $m70.value;
      let positional = 0;
      for (const a of value) {
        let matches = false;
        const label = type_arg_label({ a: a });
        if (label.tag === "Some") {
          matches = ident_text({ o: label }) === pname;
        } else {
          matches = positional === index;
          positional = $rt.int.add(positional, 1, $ob7);
        }
        if (matches) {
          return text_literal_span({ a: a });
        }
      }
      return { tag: "None" };
      break $m70$match;
    }
    $rt.unreachable();
  }
}

export function decoded_escape({ e }) {
  if (e === 110) {
    return 10;
  }
  if (e === 116) {
    return 9;
  }
  if (e === 114) {
    return 13;
  }
  if (e === 48) {
    return 0;
  }
  return e;
}

export function grapheme_span({ ctx, file, literal, grapheme_index }) {
  if (file < 0 || file >= $context.file_count({ ctx: ctx })) {
    return literal;
  }
  const cps = $context.file_at({ ctx: ctx, id: file }).cps;
  const n = $std_list.len({ xs: cps });
  let from_i = $rt.int.add(literal.start, 1, $ob8);
  let to_i = $rt.int.sub(literal.end, 1, $ob9);
  if (from_i < 0) {
    from_i = 0;
  }
  if (from_i > n) {
    from_i = n;
  }
  if (to_i > n) {
    to_i = n;
  }
  if (to_i < from_i) {
    to_i = from_i;
  }
  const src = $std_list.slice({ xs: cps, from: from_i, to: to_i });
  const m = $std_list.len({ xs: src });
  let starts = $std_list.builder({  });
  let value_cps = $std_list.builder({  });
  let i = 0;
  while (i < m) {
    const c = $std_list.get({ xs: src, i: i });
    let decoded = c;
    let width = 1;
    if (c === 92 && $rt.int.add(i, 1, $ob10) < m) {
      decoded = decoded_escape({ e: $std_list.get({ xs: src, i: $rt.int.add(i, 1, $ob11) }) });
      width = 2;
    }
    const [, starts$109] = $std_list.push({ b: starts, x: i });
    starts = starts$109;
    if (decoded >= 0 && decoded <= 1114111) {
      const [, value_cps$110] = $std_list.push({ b: value_cps, x: decoded });
      value_cps = value_cps$110;
    }
    i = $rt.int.add(i, width, $ob12);
  }
  const start_list = $std_list.finish({ b: starts });
  const value_text = $std_text.of_code_points({ cps: $std_list.finish({ b: value_cps }) });
  let unit = 0;
  let g = 0;
  let width = 1;
  let found = false;
  for (const seg of $std_text.graphemes({ t: value_text })) {
    if (!found) {
      if (g === grapheme_index) {
        width = $std_text.count({ t: seg });
        found = true;
      } else {
        unit = $rt.int.add(unit, $std_text.count({ t: seg }), $ob13);
        g = $rt.int.add(g, 1, $ob14);
      }
    }
  }
  const base = $rt.int.add(literal.start, 1, $ob15);
  if (!found || unit < 0 || unit >= $std_list.len({ xs: start_list })) {
    let close = literal.start;
    if (literal.end > 0) {
      close = $rt.int.sub(literal.end, 1, $ob16);
    }
    return { start: close, end: literal.end };
  }
  const from = $std_list.get({ xs: start_list, i: unit });
  const to_index = $rt.int.add(unit, width, $ob17);
  let to = m;
  if (to_index >= 0 && to_index < $std_list.len({ xs: start_list })) {
    to = $std_list.get({ xs: start_list, i: to_index });
  }
  return { start: $rt.int.add(base, from, $ob18), end: $rt.int.add(base, to, $ob19) };
}

export function set_example({ ctx, def, status }) {
  let table = ctx.examples;
  const [, table$111] = $std_map.set({ d: table, key: def, value: status });
  table = table$111;
  return [undefined, ctx];
  return [undefined, ctx];
}

export function example_failure({ cp, ctx, decl, def, f }) {
  const $m76 = f;
  $m76$match: {
    if ($m76.tag === "NotConstF") {
      const [, ctx$112] = set_example({ ctx: ctx, def: def, status: "deferred" });
      ctx = ctx$112;
      break $m76$match;
    }
    if ($m76.tag === "PanicF") {
      const detail = $m76.detail;
      const span = $m76.span;
      const [, ctx$113] = rep({ cp: cp, ctx: ctx, code: "E0701", at: $evaluator.span_or({ o: span, dflt: decl.name.span }), detail: "example `" + decl.name.text + "`: " + detail });
      ctx = ctx$113;
      const [, ctx$114] = set_example({ ctx: ctx, def: def, status: "failed" });
      ctx = ctx$114;
      break $m76$match;
    }
    if ($m76.tag === "BudgetF") {
      const fn_name = $m76.fn_name;
      const [, ctx$115] = rep({ cp: cp, ctx: ctx, code: "E0501", at: decl.name.span, detail: "example `" + decl.name.text + "` exceeded the check-time step budget in `" + fn_name + "`" });
      ctx = ctx$115;
      const [, ctx$116] = set_example({ ctx: ctx, def: def, status: "failed" });
      ctx = ctx$116;
      break $m76$match;
    }
    if ($m76.tag === "ReturnF") {
      const [, ctx$117] = set_example({ ctx: ctx, def: def, status: "deferred" });
      ctx = ctx$117;
      break $m76$match;
    }
    $rt.unreachable();
  }
  return [undefined, ctx];
  return [undefined, ctx];
}

export function example({ cp, ev, ctx, decl }) {
  const def = def_of_key({ ctx: ctx, k: key({ cp: cp, tag: $defs.tag_item, span: decl.span }) });
  if (def < 0) {
    return [undefined, ev, ctx];
  }
  const env = $std_map.dict({  });
  const [, ev$118] = $evaluator.reset({ ev: ev });
  ev = ev$118;
  for (const s of decl.body.stmts) {
    const $m77 = s;
    $m77$match: {
      if ($m77.tag === "ExprStmt") {
        const expr = $m77.expr;
        const span = $m77.span;
        const [$r79, ev$119, ctx$119] = $evaluator.eval_expr({ fuel: $evaluator.max_depth, ev: ev, ctx: ctx, e: expr, env: env });
        ev = ev$119;
        ctx = ctx$119;
        const $m78 = $r79;
        $m78$match: {
          if ($m78.tag === "Ok") {
            const value = $m78.value;
            if (!(value.tag === "BoolV") || !$values.bool_of({ c: value })) {
              const [, ctx$120] = rep({ cp: cp, ctx: ctx, code: "E0702", at: span, detail: "`" + $printer.print_expr({ e: expr }) + "` is false" });
              ctx = ctx$120;
              const [, ctx$121] = set_example({ ctx: ctx, def: def, status: "failed" });
              ctx = ctx$121;
              return [undefined, ev, ctx];
            }
            break $m78$match;
          }
          if ($m78.tag === "Err") {
            const error = $m78.error;
            const [, ctx$122] = example_failure({ cp: cp, ctx: ctx, decl: decl, def: def, f: error });
            ctx = ctx$122;
            return [undefined, ev, ctx];
            break $m78$match;
          }
          $rt.unreachable();
        }
        break $m77$match;
      }
      if (true) {
        const [$r81, ev$123, ctx$123] = $evaluator.exec_stmt({ fuel: $evaluator.max_depth, ev: ev, ctx: ctx, s: s, env: env });
        ev = ev$123;
        ctx = ctx$123;
        const $m80 = $r81;
        $m80$match: {
          if ($m80.tag === "Ok") {
            skip({  });
            break $m80$match;
          }
          if ($m80.tag === "Err") {
            const error = $m80.error;
            const [, ctx$124] = example_failure({ cp: cp, ctx: ctx, decl: decl, def: def, f: error });
            ctx = ctx$124;
            return [undefined, ev, ctx];
            break $m80$match;
          }
          $rt.unreachable();
        }
        break $m77$match;
      }
      $rt.unreachable();
    }
  }
  const [, ctx$125] = set_example({ ctx: ctx, def: def, status: "passed" });
  ctx = ctx$125;
  return [undefined, ev, ctx];
  return [undefined, ev, ctx];
}

export function consteval_pass({ ctx }) {
  let ev = $evaluator.new_ev({ file: 0 });
  const $hi82 = ctx.module_count;
  for (let id = 0; id < $hi82; id++) {
    const $m83 = $std_map.find({ d: ctx.modules, key: id });
    $m83$match: {
      if ($m83.tag === "Some") {
        const value = $m83.value;
        const cp0 = { current_def: { tag: "None" }, current_file: value.file };
        ev = { ...ev, file: value.file };
        for (const item of value.tree.items) {
          const cp = { ...cp0, current_def: { tag: "Some", value: item_name({ item: item }) } };
          const $m89 = item;
          $m89$match: {
            if ($m89.tag === "ConstItem") {
              const decl = $m89.decl;
              const [$r90, ev$126, ctx$126] = $evaluator.const_of({ fuel: $evaluator.max_depth, ev: ev, ctx: ctx, def: def_of_key({ ctx: ctx, k: key({ cp: cp, tag: $defs.tag_item, span: decl.span }) }) });
              ev = ev$126;
              ctx = ctx$126;
              const cv = $r90;
              break $m89$match;
            }
            if ($m89.tag === "ExampleItem") {
              const decl = $m89.decl;
              const [, ev$127, ctx$127] = example({ cp: cp, ev: ev, ctx: ctx, decl: decl });
              ev = ev$127;
              ctx = ctx$127;
              break $m89$match;
            }
            if (true) {
              skip({  });
              break $m89$match;
            }
            $rt.unreachable();
          }
          for (const c of calls_in_item({ item: item })) {
            const [, ev$128, ctx$128] = check_call({ cp: cp, ev: ev, ctx: ctx, e: c });
            ev = ev$128;
            ctx = ctx$128;
          }
        }
        break $m83$match;
      }
      if ($m83.tag === "None") {
        skip({  });
        break $m83$match;
      }
      $rt.unreachable();
    }
  }
  return [undefined, ctx];
  return [undefined, ctx];
}

