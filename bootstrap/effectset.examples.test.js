// Generated by onus: tests of module effectset.
import * as $rt from "/Users/jamesrandall/code/onus/packages/runtime/dist/index.js";
import * as $self from "./effectset.js";
import * as $std_list from "./std/list.js";

import { test, expect, afterAll } from 'vitest';
import * as fc from 'fast-check';
test("sets", () => {
  const a = $self.of_list({ effects: [{ tag: "PrimEffect", name: "alloc" }, { tag: "PrimEffect", name: "alloc" }, { tag: "PrimEffect", name: "mutate" }] });
  expect($self.size({ s: a }) === 2).toBe(true);
  expect($self.has({ s: a, e: { tag: "PrimEffect", name: "mutate" } })).toBe(true);
  expect(!$self.has({ s: a, e: { tag: "PrimEffect", name: "panic" } })).toBe(true);
  expect($self.equals({ a: a, b: $self.of_list({ effects: [{ tag: "PrimEffect", name: "mutate" }, { tag: "PrimEffect", name: "alloc" }] }) })).toBe(true);
  expect($std_list.len({ xs: $self.minus({ a: a, b: $self.of_list({ effects: [{ tag: "PrimEffect", name: "alloc" }] }) }) }) === 1).toBe(true);
});
afterAll(() => $rt.coverage.flush());
