// Generated by onus from module paths. Do not edit.
import * as $rt from "./onus-runtime/index.js";
import * as $context from "./context.js";
import * as $report from "./report.js";
import * as $std_map from "./std/map.js";
import * as $std_list from "./std/list.js";
import * as $std_text from "./std/text.js";
import * as $defs from "./defs.js";
import * as $effectset from "./effectset.js";
import * as $resolve from "./resolve.js";
import * as $claimcheck from "./claimcheck.js";
import * as $ledger from "./ledger.js";
import * as $std_int from "./std/int.js";
import * as $typecheck from "./typecheck.js";
import * as $types from "./types.js";
import * as $walk from "./walk.js";

const $ob1 = { kind: "overflow", text: "-1 within Int", at: "self/paths.onus:62:20", def: "or_neg" };
const $ob2 = { kind: "overflow", text: "pc.errors + 1 within Int", at: "self/paths.onus:79:26", def: "rep" };
const $ob3 = { kind: "overflow", text: "-1 within Int", at: "self/paths.onus:93:20", def: "ref_def" };
const $ob4 = { kind: "overflow", text: "-1 within Int", at: "self/paths.onus:97:19", def: "ref_def" };
const $ob5 = { kind: "overflow", text: "pc.errors + 1 within Int", at: "self/paths.onus:259:28", def: "check_path" };
const $ob6 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/paths.onus:561:12", def: "reach" };
const $ob7 = { kind: "overflow", text: "head + 1 within Int", at: "self/paths.onus:563:12", def: "reach" };
const $ob8 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/paths.onus:768:11", def: "sealed_walk" };
const $ob9 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/paths.onus:779:23", def: "sealed_walk" };
const $ob10 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/paths.onus:782:11", def: "sealed_walk" };
const $ob11 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/paths.onus:789:11", def: "sealed_walk" };
const $ob12 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/paths.onus:796:11", def: "sealed_walk" };
const $ob13 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/paths.onus:804:25", def: "sealed_walk" };
const $ob14 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/paths.onus:806:23", def: "sealed_walk" };
const $ob15 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/paths.onus:827:13", def: "sealed_args" };
const $ob16 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/paths.onus:850:12", def: "uncovered" };
const $ob17 = { kind: "overflow", text: "head + 1 within Int", at: "self/paths.onus:852:12", def: "uncovered" };
export const assumption_max_age_days = 7;

export function skip($args) {
  return undefined;
}

export function or_neg({ o }) {
  const $m1 = o;
  $m1$match: {
    if ($m1.tag === "Some") {
      const value = $m1.value;
      return value;
      break $m1$match;
    }
    if ($m1.tag === "None") {
      return $rt.int.neg(1, $ob1);
      break $m1$match;
    }
    $rt.unreachable();
  }
}

export function ids_or({ o }) {
  const $m2 = o;
  $m2$match: {
    if ($m2.tag === "Some") {
      const value = $m2.value;
      return value;
      break $m2$match;
    }
    if ($m2.tag === "None") {
      return [];
      break $m2$match;
    }
    $rt.unreachable();
  }
}

export function rep({ pc, ctx, code, at, detail, def_name }) {
  pc = { ...pc, errors: $rt.int.add(pc.errors, 1, $ob2) };
  const [, ctx$1] = $context.report({ ctx: ctx, d: $report.diagnostic({ code: code, file: pc.m.file, span: at, def_name: def_name, detail: detail }) });
  ctx = ctx$1;
  return [undefined, pc, ctx];
  return [undefined, pc, ctx];
}

export function path_name({ pc }) {
  return { tag: "Some", value: pc.decl.name.text };
}

export function ref_def({ ctx, k }) {
  const $m5 = $std_map.find({ d: ctx.refs, key: k });
  $m5$match: {
    if ($m5.tag === "None") {
      return $rt.int.neg(1, $ob3);
      break $m5$match;
    }
    if ($m5.tag === "Some") {
      const value = $m5.value;
      const $m6 = value;
      $m6$match: {
        if ($m6.tag === "DefRes") {
          const def = $m6.def;
          return def;
          break $m6$match;
        }
        if (true) {
          return $rt.int.neg(1, $ob4);
          break $m6$match;
        }
        $rt.unreachable();
      }
      break $m5$match;
    }
    $rt.unreachable();
  }
}

export function qname_text({ q }) {
  let parts = $std_list.builder({  });
  for (const s of q.segments) {
    const [, parts$2] = $std_list.push({ b: parts, x: s.text });
    parts = parts$2;
  }
  return $std_text.join({ parts: $std_list.finish({ b: parts }), sep: "." });
}

export function module_name({ ctx, mod }) {
  const $m7 = $std_map.find({ d: ctx.modules, key: mod });
  $m7$match: {
    if ($m7.tag === "Some") {
      const value = $m7.value;
      return value.name;
      break $m7$match;
    }
    if ($m7.tag === "None") {
      return "";
      break $m7$match;
    }
    $rt.unreachable();
  }
}

export function paths_pass({ ctx }) {
  const $hi8 = ctx.module_count;
  for (let mid = 0; mid < $hi8; mid++) {
    const $m9 = $std_map.find({ d: ctx.modules, key: mid });
    $m9$match: {
      if ($m9.tag === "None") {
        skip({  });
        break $m9$match;
      }
      if ($m9.tag === "Some") {
        const value = $m9.value;
        for (const item of value.tree.items) {
          const $m10 = item;
          $m10$match: {
            if ($m10.tag === "PathItem") {
              const decl = $m10.decl;
              let pc = { m: value, decl: decl, errors: 0 };
              const [, pc$3, ctx$3] = check_path({ pc: pc, ctx: ctx });
              pc = pc$3;
              ctx = ctx$3;
              break $m10$match;
            }
            if (true) {
              skip({  });
              break $m10$match;
            }
            $rt.unreachable();
          }
        }
        break $m9$match;
      }
      $rt.unreachable();
    }
  }
  return [undefined, ctx];
  return [undefined, ctx];
}

export function effects_of_refs({ ctx, file, refs }) {
  let out = $std_list.builder({  });
  for (const r of refs) {
    const $m12 = $std_map.find({ d: ctx.refs, key: $defs.node_key({ file: file, tag: $defs.tag_sig, span: r.span }) });
    $m12$match: {
      if ($m12.tag === "None") {
        skip({  });
        break $m12$match;
      }
      if ($m12.tag === "Some") {
        const value = $m12.value;
        const $m13 = value;
        $m13$match: {
          if ($m13.tag === "EffectRes") {
            const effect = $m13.effect;
            const [, out$4] = $std_list.push({ b: out, x: effect });
            out = out$4;
            break $m13$match;
          }
          if (true) {
            skip({  });
            break $m13$match;
          }
          $rt.unreachable();
        }
        break $m12$match;
      }
      $rt.unreachable();
    }
  }
  return $effectset.of_list({ effects: $std_list.finish({ b: out }) });
}

export function check_path({ pc, ctx }) {
  const file = pc.m.file;
  const decl = pc.decl;
  const path_key = $defs.node_key({ file: file, tag: $defs.tag_item, span: decl.span });
  const path_def = or_neg({ o: $std_map.find({ d: ctx.def_of, key: path_key }) });
  const entry_fn = ref_def({ ctx: ctx, k: path_key });
  if (path_def < 0 || entry_fn < 0) {
    return [undefined, pc, ctx];
  }
  let has_bound = false;
  let bound = $effectset.empty({  });
  let forbidden = $effectset.empty({  });
  let forbid_claims = $std_list.builder({  });
  let required = [];
  let pol = { tag: "None" };
  let verified_only = { tag: "None" };
  let bound_span = decl.span;
  let forbid_span = decl.span;
  let require_span = decl.span;
  for (const c of decl.clauses) {
    const $m16 = c;
    $m16$match: {
      if ($m16.tag === "PathEffects") {
        const effects = $m16.effects;
        const span = $m16.span;
        if (!has_bound) {
          bound_span = span;
        }
        has_bound = true;
        bound = $effectset.join_sets({ a: bound, b: effects_of_refs({ ctx: ctx, file: file, refs: effects }) });
        break $m16$match;
      }
      if ($m16.tag === "PathForbid") {
        const effects = $m16.effects;
        const span = $m16.span;
        if ($effectset.size({ s: forbidden }) === 0 && $std_list.built({ b: forbid_claims }) === 0) {
          forbid_span = span;
        }
        forbidden = $effectset.join_sets({ a: forbidden, b: effects_of_refs({ ctx: ctx, file: file, refs: effects }) });
        for (const r of effects) {
          const d = ref_def({ ctx: ctx, k: $defs.node_key({ file: file, tag: $defs.tag_sig, span: r.span }) });
          if (d >= 0 && $rt.eq($context.get_def({ ctx: ctx, id: d }).kind, { tag: "Claim" })) {
            const [, forbid_claims$5] = $std_list.push({ b: forbid_claims, x: d });
            forbid_claims = forbid_claims$5;
          }
        }
        break $m16$match;
      }
      if ($m16.tag === "PathRequire") {
        const span = $m16.span;
        if ($std_list.len({ xs: required }) === 0) {
          require_span = span;
        }
        required = [...required, ...ids_or({ o: $std_map.find({ d: ctx.claim_lists, key: $defs.node_key({ file: file, tag: $defs.tag_sig, span: span }) }) })];
        break $m16$match;
      }
      if ($m16.tag === "PathPolicy") {
        const name = $m16.name;
        const except_list = $m16.except_list;
        const span = $m16.span;
        if (name.text === $resolve.builtin_policy) {
          verified_only = { tag: "Some", value: span };
        } else {
          const d = ref_def({ ctx: ctx, k: $defs.node_key({ file: file, tag: $defs.tag_sig, span: span }) });
          if (d >= 0) {
            let names = $std_list.builder({  });
            for (const q of except_list) {
              const [, names$6] = $std_list.push({ b: names, x: qname_text({ q: q }) });
              names = names$6;
            }
            pol = { tag: "Some", value: { def: d, except_list: $std_list.finish({ b: names }), span: span } };
          }
        }
        break $m16$match;
      }
      $rt.unreachable();
    }
  }
  const forbid_claim_list = $std_list.finish({ b: forbid_claims });
  if (has_bound) {
    for (const e of $effectset.values({ s: forbidden })) {
      if ($effectset.has({ s: bound, e: e })) {
        const [, pc$7, ctx$7] = rep({ pc: pc, ctx: ctx, code: "E0411", at: decl.span, detail: "`" + $claimcheck.effect_name({ ctx: ctx, e: e }) + "` is allowed by `effects <=` and listed under `forbid`", def_name: path_name({ pc: pc }) });
        pc = pc$7;
        ctx = ctx$7;
      }
    }
  }
  const r = reach({ ctx: ctx, entry_fn: entry_fn });
  for (const u of r.unresolvable) {
    const [, ctx$8] = $context.report({ ctx: ctx, d: $report.diagnostic({ code: "E0410", file: u.at_file, span: u.at_span, def_name: { tag: "Some", value: $context.get_def({ ctx: ctx, id: u.fn_def }).name }, detail: "on path `" + decl.name.text + "`: " + u.reason + ", so the reachable set cannot be closed" }) });
    ctx = ctx$8;
    pc = { ...pc, errors: $rt.int.add(pc.errors, 1, $ob5) };
  }
  let actual = $effectset.empty({  });
  for (const fn_def of r.reachable) {
    const effects = concrete_effects({ ctx: ctx, fn_def: fn_def });
    actual = $effectset.join_sets({ a: actual, b: effects });
    let outside = $std_list.builder({  });
    let bad = $std_list.builder({  });
    for (const e of $effectset.values({ s: effects })) {
      if (has_bound && !$effectset.has({ s: bound, e: e })) {
        const [, outside$9] = $std_list.push({ b: outside, x: $claimcheck.effect_name({ ctx: ctx, e: e }) });
        outside = outside$9;
      }
      if ($effectset.has({ s: forbidden, e: e })) {
        const [, bad$10] = $std_list.push({ b: bad, x: $claimcheck.effect_name({ ctx: ctx, e: e }) });
        bad = bad$10;
      }
    }
    const outside_list = $std_list.finish({ b: outside });
    if ($std_list.len({ xs: outside_list }) > 0) {
      const [, pc$11, ctx$11] = rep({ pc: pc, ctx: ctx, code: "E0412", at: bound_span, detail: "`" + $context.qualified_name({ ctx: ctx, id: fn_def }) + "` is reachable and has effect" + plural({ n: $std_list.len({ xs: outside_list }) }) + " { " + $std_text.join({ parts: outside_list, sep: ", " }) + " } outside the bound", def_name: path_name({ pc: pc }) });
      pc = pc$11;
      ctx = ctx$11;
    }
    const bad_list = $std_list.finish({ b: bad });
    if ($std_list.len({ xs: bad_list }) > 0) {
      const [, pc$12, ctx$12] = rep({ pc: pc, ctx: ctx, code: "E0413", at: forbid_span, detail: "`" + $context.qualified_name({ ctx: ctx, id: fn_def }) + "` is reachable and has forbidden effect" + plural({ n: $std_list.len({ xs: bad_list }) }) + " { " + $std_text.join({ parts: bad_list, sep: ", " }) + " }", def_name: path_name({ pc: pc }) });
      pc = pc$12;
      ctx = ctx$12;
    }
    for (const cl of forbid_claim_list) {
      if ($claimcheck.carries({ ctx: ctx, fn_def: fn_def, claim_def: cl })) {
        const [, pc$13, ctx$13] = rep({ pc: pc, ctx: ctx, code: "E0413", at: forbid_span, detail: "`" + $context.qualified_name({ ctx: ctx, id: fn_def }) + "` is reachable and carries forbidden claim `" + claim_display({ ctx: ctx, claim_def: cl }) + "`", def_name: path_name({ pc: pc }) });
        pc = pc$13;
        ctx = ctx$13;
      }
    }
  }
  let satisfied = true;
  for (const cl of required) {
    const asserted = $claimcheck.tier_of({ ctx: ctx, claim_def: cl }) === "asserted";
    let candidates = r.reachable;
    if (asserted) {
      candidates = uncovered({ ctx: ctx, entry_fn: entry_fn, claim_def: cl });
    }
    for (const fn_def of candidates) {
      let applies = true;
      if (asserted && !$claimcheck.observable({ effects: $claimcheck.effects_of_fn({ ctx: ctx, def: fn_def }) })) {
        applies = false;
      }
      if (applies && !$claimcheck.carries({ ctx: ctx, fn_def: fn_def, claim_def: cl })) {
        satisfied = false;
        let how = "";
        if (asserted) {
          how = " with observable effects";
        }
        const [, pc$14, ctx$14] = rep({ pc: pc, ctx: ctx, code: "E0414", at: require_span, detail: "`" + $context.qualified_name({ ctx: ctx, id: fn_def }) + "` is reachable" + how + " and does not carry required claim `" + $context.get_def({ ctx: ctx, id: cl }).name + "`", def_name: path_name({ pc: pc }) });
        pc = pc$14;
        ctx = ctx$14;
      }
    }
  }
  let assume_sites = $std_list.builder({  });
  for (const fn_def of r.reachable) {
    for (const a of $claimcheck.assumes_of({ ctx: ctx, fn_def: fn_def })) {
      const owner = $context.get_def({ ctx: ctx, id: fn_def });
      let permitted_by = "";
      const $m23 = pol;
      $m23$match: {
        if ($m23.tag === "Some") {
          const value = $m23.value;
          if (in_policy_scope({ ctx: ctx, pc: pc, policy_def: value.def, mod: owner.mod })) {
            permitted_by = "scope";
          } else {
            if ($std_list.contains({ xs: value.except_list, x: $context.qualified_name({ ctx: ctx, id: fn_def }) })) {
              permitted_by = "except";
            } else {
              const [, pc$15, ctx$15] = rep({ pc: pc, ctx: ctx, code: "E0415", at: value.span, detail: "`" + $context.qualified_name({ ctx: ctx, id: fn_def }) + "` assumes `" + $context.get_def({ ctx: ctx, id: a.claim_def }).name + "` (\"" + a.justification + "\") outside the policy's scope and is not listed under `except`", def_name: path_name({ pc: pc }) });
              pc = pc$15;
              ctx = ctx$15;
            }
          }
          break $m23$match;
        }
        if ($m23.tag === "None") {
          skip({  });
          break $m23$match;
        }
        $rt.unreachable();
      }
      const $m24 = verified_only;
      $m24$match: {
        if ($m24.tag === "Some") {
          const value = $m24.value;
          if ($ledger.is_verified({ ctx: ctx, key: a.key })) {
            skip({  });
          } else {
            let why = "with no `verify` block";
            if (a.verify_key.tag === "Some") {
              why = "without a passing verification in the last " + $std_int.to_text({ x: assumption_max_age_days }) + " days";
            }
            const [, pc$16, ctx$16] = rep({ pc: pc, ctx: ctx, code: "E0416", at: value, detail: "`" + $context.qualified_name({ ctx: ctx, id: fn_def }) + "` assumes `" + $context.get_def({ ctx: ctx, id: a.claim_def }).name + "` " + why + "; `policy " + $resolve.builtin_policy + "` requires one (§20.3)", def_name: path_name({ pc: pc }) });
            pc = pc$16;
            ctx = ctx$16;
          }
          break $m24$match;
        }
        if ($m24.tag === "None") {
          skip({  });
          break $m24$match;
        }
        $rt.unreachable();
      }
      const [, assume_sites$17] = $std_list.push({ b: assume_sites, x: { claim_def: a.claim_def, fn_def: fn_def, justification: a.justification, node: a.node, permitted_by: permitted_by, verify_key: a.verify_key, verify_block: a.verify_block, key: a.key } });
      assume_sites = assume_sites$17;
    }
  }
  let policy_def = { tag: "None" };
  const $m27 = pol;
  $m27$match: {
    if ($m27.tag === "Some") {
      const value = $m27.value;
      policy_def = { tag: "Some", value: value.def };
      break $m27$match;
    }
    if ($m27.tag === "None") {
      skip({  });
      break $m27$match;
    }
    $rt.unreachable();
  }
  let analyses = ctx.analyses;
  const [, analyses$18] = $std_map.set({ d: analyses, key: path_def, value: { def: path_def, mod: pc.m.id, entry_fn: entry_fn, reachable: r.reachable, has_bound: has_bound, bound: bound, forbidden: forbidden, forbid_claims: forbid_claim_list, actual: actual, required: required, satisfied: satisfied, policy_def: policy_def, assume_sites: $std_list.finish({ b: assume_sites }), unresolvable: r.unresolvable, capabilities: r.capabilities, edges: r.edges, gates: gates({ ctx: ctx, reachable: r.reachable }), recovers: r.recovers, ok: pc.errors === 0 } });
  analyses = analyses$18;
  return [undefined, pc, ctx];
  return [undefined, pc, ctx];
}

export function plural({ n }) {
  if (n === 1) {
    return "";
  }
  return "s";
}

export function concrete_effects({ ctx, fn_def }) {
  let out = $std_list.builder({  });
  for (const e of $effectset.values({ s: $claimcheck.effects_of_fn({ ctx: ctx, def: fn_def }) })) {
    const $m30 = e;
    $m30$match: {
      if ($m30.tag === "ParamEffect") {
        skip({  });
        break $m30$match;
      }
      if (true) {
        const [, out$19] = $std_list.push({ b: out, x: e });
        out = out$19;
        break $m30$match;
      }
      $rt.unreachable();
    }
  }
  return $effectset.of_list({ effects: $std_list.finish({ b: out }) });
}

export function claim_display({ ctx, claim_def }) {
  const d = $context.get_def({ ctx: ctx, id: claim_def });
  if (module_name({ ctx: ctx, mod: d.mod }) === "std.host") {
    return "host." + d.name;
  }
  return $context.qualified_name({ ctx: ctx, id: claim_def });
}

export function capability_text({ ctx, t }) {
  const text = $typecheck.show({ ctx: ctx, t: t });
  const $m31 = t;
  $m31$match: {
    if ($m31.tag === "Capability") {
      const def = $m31.def;
      const name = module_name({ ctx: ctx, mod: $context.get_def({ ctx: ctx, id: def }).mod });
      let alias = "";
      for (const seg of $std_text.split({ t: name, sep: "." })) {
        alias = seg;
      }
      return alias + "." + text;
      break $m31$match;
    }
    if (true) {
      return text;
      break $m31$match;
    }
    $rt.unreachable();
  }
}

export function capability_in({ t }) {
  const $m32 = t;
  $m32$match: {
    if ($m32.tag === "None") {
      return { tag: "None" };
      break $m32$match;
    }
    if ($m32.tag === "Some") {
      const value = $m32.value;
      const s = $types.strip({ t: value });
      if (s.tag === "Capability") {
        return { tag: "Some", value: s };
      }
      const $m35 = s;
      $m35$match: {
        if ($m35.tag === "UnionT") {
          const args = $m35.args;
          for (const a of args) {
            const $m36 = a;
            $m36$match: {
              if ($m36.tag === "TypeA") {
                const ty = $m36.ty;
                const inner = $types.strip({ t: ty });
                if (inner.tag === "Capability") {
                  return { tag: "Some", value: inner };
                }
                break $m36$match;
              }
              if (true) {
                skip({  });
                break $m36$match;
              }
              $rt.unreachable();
            }
          }
          return { tag: "None" };
          break $m35$match;
        }
        if (true) {
          return { tag: "None" };
          break $m35$match;
        }
        $rt.unreachable();
      }
      break $m32$match;
    }
    $rt.unreachable();
  }
}

export function fn_body({ ctx, fn_def }) {
  const $m40 = $context.get_def({ ctx: ctx, id: fn_def }).node;
  $m40$match: {
    if ($m40.tag === "FnNode") {
      const decl = $m40.decl;
      return decl.body;
      break $m40$match;
    }
    if (true) {
      return { tag: "None" };
      break $m40$match;
    }
    $rt.unreachable();
  }
}

export function reach({ ctx, entry_fn }) {
  let reachable = $std_list.builder({  });
  let seen = $std_map.dict({  });
  let unresolvable = $std_list.builder({  });
  let capabilities = $std_list.builder({  });
  let edges = $std_list.builder({  });
  let recovers = $std_list.builder({  });
  let queue = $std_list.builder({  });
  const [, queue$20] = $std_list.push({ b: queue, x: entry_fn });
  queue = queue$20;
  let head = 0;
  let fuel = 10000000;
  while (head < $std_list.built({ b: queue }) && fuel > 0) {
    fuel = $rt.int.sub(fuel, 1, $ob6);
    const cur = $std_list.at({ b: queue, i: head });
    head = $rt.int.add(head, 1, $ob7);
    if (!$std_map.contains({ d: seen, key: cur })) {
      const [, seen$21] = $std_map.set({ d: seen, key: cur, value: true });
      seen = seen$21;
      const [, reachable$22] = $std_list.push({ b: reachable, x: cur });
      reachable = reachable$22;
      const file = $context.get_def({ ctx: ctx, id: cur }).file;
      const $m43 = fn_body({ ctx: ctx, fn_def: cur });
      $m43$match: {
        if ($m43.tag === "None") {
          skip({  });
          break $m43$match;
        }
        if ($m43.tag === "Some") {
          const value = $m43.value;
          for (const n of $walk.nodes_in_block({ b: value, into_verify: false })) {
            const $m44 = n;
            $m44$match: {
              if ($m44.tag === "NExpr") {
                const e = $m44.e;
                const $m45 = e;
                $m45$match: {
                  if ($m45.tag === "Recover") {
                    const span = $m45.span;
                    const [, recovers$23] = $std_list.push({ b: recovers, x: { fn_def: cur, at: $defs.node_key({ file: file, tag: $defs.tag_expr, span: span }), at_file: file, at_span: span } });
                    recovers = recovers$23;
                    break $m45$match;
                  }
                  if ($m45.tag === "Call") {
                    const span = $m45.span;
                    const at = $defs.node_key({ file: file, tag: $defs.tag_expr, span: span });
                    const $m47 = $claimcheck.callee_of({ ctx: ctx, file: file, e: e });
                    $m47$match: {
                      if ($m47.tag === "FnCallee") {
                        const def = $m47.def;
                        const [, queue$24] = $std_list.push({ b: queue, x: def });
                        queue = queue$24;
                        const [, edges$25, capabilities$25] = push_edge({ ctx: ctx, fn_def: cur, def: def, at: at, file: file, span: span, edges: edges, capabilities: capabilities });
                        edges = edges$25;
                        capabilities = capabilities$25;
                        break $m47$match;
                      }
                      if ($m47.tag === "ImplCallee") {
                        const def = $m47.def;
                        const [, queue$26] = $std_list.push({ b: queue, x: def });
                        queue = queue$26;
                        const [, edges$27, capabilities$27] = push_edge({ ctx: ctx, fn_def: cur, def: def, at: at, file: file, span: span, edges: edges, capabilities: capabilities });
                        edges = edges$27;
                        capabilities = capabilities$27;
                        break $m47$match;
                      }
                      if ($m47.tag === "DispatchCallee") {
                        const fn_def = $m47.fn_def;
                        const [, unresolvable$28] = $std_list.push({ b: unresolvable, x: { fn_def: cur, at: at, at_file: file, at_span: span, reason: "`" + $context.get_def({ ctx: ctx, id: fn_def }).name + "` is dispatched on a type parameter" } });
                        unresolvable = unresolvable$28;
                        break $m47$match;
                      }
                      if ($m47.tag === "ValueCallee") {
                        const [, unresolvable$29] = $std_list.push({ b: unresolvable, x: { fn_def: cur, at: at, at_file: file, at_span: span, reason: "this call goes through a function value whose provenance is not tracked in v0" } });
                        unresolvable = unresolvable$29;
                        break $m47$match;
                      }
                      if ($m47.tag === "CtorCallee") {
                        skip({  });
                        break $m47$match;
                      }
                      $rt.unreachable();
                    }
                    break $m45$match;
                  }
                  if (true) {
                    skip({  });
                    break $m45$match;
                  }
                  $rt.unreachable();
                }
                break $m44$match;
              }
              if (true) {
                skip({  });
                break $m44$match;
              }
              $rt.unreachable();
            }
          }
          break $m43$match;
        }
        $rt.unreachable();
      }
    }
  }
  return { reachable: $std_list.finish({ b: reachable }), unresolvable: $std_list.finish({ b: unresolvable }), capabilities: $std_list.finish({ b: capabilities }), edges: $std_list.finish({ b: edges }), recovers: $std_list.finish({ b: recovers }) };
}

export function push_edge({ ctx, fn_def, def, at, file, span, edges, capabilities }) {
  const [, edges$30] = $std_list.push({ b: edges, x: { from: fn_def, to: def, at: at, at_file: file, at_span: span, effects: $claimcheck.callee_effects({ ctx: ctx, call_key: at, callee: def }) } });
  edges = edges$30;
  const $m52 = capability_in({ t: $std_map.find({ d: ctx.expr_types, key: at }) });
  $m52$match: {
    if ($m52.tag === "Some") {
      const value = $m52.value;
      const [, capabilities$31] = $std_list.push({ b: capabilities, x: { fn_def: fn_def, at: at, at_file: file, at_span: span, type_text: capability_text({ ctx: ctx, t: value }), callee: def } });
      capabilities = capabilities$31;
      break $m52$match;
    }
    if ($m52.tag === "None") {
      skip({  });
      break $m52$match;
    }
    $rt.unreachable();
  }
  return [undefined, edges, capabilities];
  return [undefined, edges, capabilities];
}

export function gates({ ctx, reachable }) {
  let producers = $std_map.dict({  });
  let producer_order = $std_list.builder({  });
  let guarded = $std_map.dict({  });
  for (const fn_def of reachable) {
    const $m54 = $std_map.find({ d: ctx.signatures, key: fn_def });
    $m54$match: {
      if ($m54.tag === "None") {
        skip({  });
        break $m54$match;
      }
      if ($m54.tag === "Some") {
        const value = $m54.value;
        for (const p of value.params) {
          for (const e of sealed_records_in({ fuel: 1000, ctx: ctx, t: p.ty })) {
            const [, guarded$32] = $std_map.set({ d: guarded, key: e, value: $std_list.append({ xs: ids_or({ o: $std_map.find({ d: guarded, key: e }) }), x: fn_def }) });
            guarded = guarded$32;
          }
        }
        for (const e of sealed_records_in({ fuel: 1000, ctx: ctx, t: value.ret })) {
          if (!$std_map.contains({ d: producers, key: e })) {
            const [, producer_order$33] = $std_list.push({ b: producer_order, x: e });
            producer_order = producer_order$33;
          }
          const [, producers$34] = $std_map.set({ d: producers, key: e, value: $std_list.append({ xs: ids_or({ o: $std_map.find({ d: producers, key: e }) }), x: fn_def }) });
          producers = producers$34;
        }
        break $m54$match;
      }
      $rt.unreachable();
    }
  }
  let out = $std_list.builder({  });
  for (const evidence of $std_list.finish({ b: producer_order })) {
    const ps = ids_or({ o: $std_map.find({ d: producers, key: evidence }) });
    let gs = $std_list.builder({  });
    for (const g of ids_or({ o: $std_map.find({ d: guarded, key: evidence }) })) {
      if (!$std_list.contains({ xs: ps, x: g })) {
        const [, gs$35] = $std_list.push({ b: gs, x: g });
        gs = gs$35;
      }
    }
    if ($std_list.built({ b: gs }) > 0) {
      const [, out$36] = $std_list.push({ b: out, x: { evidence: evidence, producers: ps, guarded: $std_list.finish({ b: gs }) } });
      out = out$36;
    }
  }
  return $std_list.finish({ b: out });
}

export function sealed_records_in({ fuel, ctx, t }) {
  let out = $std_list.builder({  });
  let seen = $std_map.dict({  });
  const [, seen$37, out$37] = sealed_walk({ fuel: fuel, ctx: ctx, t: t, seen: seen, out: out });
  seen = seen$37;
  out = out$37;
  return $std_list.finish({ b: out });
}

export function sealed_walk({ fuel, ctx, t, seen, out }) {
  if (fuel === 0) {
    return [undefined, seen, out];
  }
  const $m56 = t;
  $m56$match: {
    if ($m56.tag === "Refined") {
      const base = $m56.base;
      const [, seen$38, out$38] = sealed_walk({ fuel: $rt.int.sub(fuel, 1, $ob8), ctx: ctx, t: base, seen: seen, out: out });
      seen = seen$38;
      out = out$38;
      break $m56$match;
    }
    if ($m56.tag === "RecordT") {
      const def = $m56.def;
      const args = $m56.args;
      if ($context.get_def({ ctx: ctx, id: def }).is_sealed && !$std_map.contains({ d: seen, key: def })) {
        const [, seen$39] = $std_map.set({ d: seen, key: def, value: true });
        seen = seen$39;
        const [, out$40] = $std_list.push({ b: out, x: def });
        out = out$40;
      }
      const [, seen$41, out$41] = sealed_args({ fuel: $rt.int.sub(fuel, 1, $ob9), ctx: ctx, args: args, seen: seen, out: out });
      seen = seen$41;
      out = out$41;
      break $m56$match;
    }
    if ($m56.tag === "UnionT") {
      const args = $m56.args;
      const [, seen$42, out$42] = sealed_args({ fuel: $rt.int.sub(fuel, 1, $ob10), ctx: ctx, args: args, seen: seen, out: out });
      seen = seen$42;
      out = out$42;
      break $m56$match;
    }
    if ($m56.tag === "Opaque") {
      const args = $m56.args;
      const [, seen$43, out$43] = sealed_args({ fuel: $rt.int.sub(fuel, 1, $ob11), ctx: ctx, args: args, seen: seen, out: out });
      seen = seen$43;
      out = out$43;
      break $m56$match;
    }
    if ($m56.tag === "Capability") {
      const args = $m56.args;
      const [, seen$44, out$44] = sealed_args({ fuel: $rt.int.sub(fuel, 1, $ob12), ctx: ctx, args: args, seen: seen, out: out });
      seen = seen$44;
      out = out$44;
      break $m56$match;
    }
    if ($m56.tag === "FnT") {
      const params = $m56.params;
      const ret = $m56.ret;
      for (const p of params) {
        const [, seen$45, out$45] = sealed_walk({ fuel: $rt.int.sub(fuel, 1, $ob13), ctx: ctx, t: p.ty, seen: seen, out: out });
        seen = seen$45;
        out = out$45;
      }
      const [, seen$46, out$46] = sealed_walk({ fuel: $rt.int.sub(fuel, 1, $ob14), ctx: ctx, t: ret, seen: seen, out: out });
      seen = seen$46;
      out = out$46;
      break $m56$match;
    }
    if (true) {
      skip({  });
      break $m56$match;
    }
    $rt.unreachable();
  }
  return [undefined, seen, out];
  return [undefined, seen, out];
}

export function sealed_args({ fuel, ctx, args, seen, out }) {
  if (fuel === 0) {
    return [undefined, seen, out];
  }
  for (const a of args) {
    const $m57 = a;
    $m57$match: {
      if ($m57.tag === "TypeA") {
        const ty = $m57.ty;
        const [, seen$47, out$47] = sealed_walk({ fuel: $rt.int.sub(fuel, 1, $ob15), ctx: ctx, t: ty, seen: seen, out: out });
        seen = seen$47;
        out = out$47;
        break $m57$match;
      }
      if (true) {
        skip({  });
        break $m57$match;
      }
      $rt.unreachable();
    }
  }
  return [undefined, seen, out];
  return [undefined, seen, out];
}

export function uncovered({ ctx, entry_fn, claim_def }) {
  let out = $std_list.builder({  });
  let seen = $std_map.dict({  });
  let queue = $std_list.builder({  });
  const [, queue$48] = $std_list.push({ b: queue, x: entry_fn });
  queue = queue$48;
  let head = 0;
  let fuel = 10000000;
  while (head < $std_list.built({ b: queue }) && fuel > 0) {
    fuel = $rt.int.sub(fuel, 1, $ob16);
    const fn_def = $std_list.at({ b: queue, i: head });
    head = $rt.int.add(head, 1, $ob17);
    if (!$std_map.contains({ d: seen, key: fn_def })) {
      const [, seen$49] = $std_map.set({ d: seen, key: fn_def, value: true });
      seen = seen$49;
      const [, out$50] = $std_list.push({ b: out, x: fn_def });
      out = out$50;
      let assumed = false;
      for (const a of $claimcheck.assumes_of({ ctx: ctx, fn_def: fn_def })) {
        if (a.claim_def === claim_def) {
          assumed = true;
        }
      }
      if (!assumed) {
        const file = $context.get_def({ ctx: ctx, id: fn_def }).file;
        const $m59 = fn_body({ ctx: ctx, fn_def: fn_def });
        $m59$match: {
          if ($m59.tag === "None") {
            skip({  });
            break $m59$match;
          }
          if ($m59.tag === "Some") {
            const value = $m59.value;
            for (const n of $walk.nodes_in_block({ b: value, into_verify: false })) {
              const $m60 = n;
              $m60$match: {
                if ($m60.tag === "NExpr") {
                  const e = $m60.e;
                  if (e.tag === "Call") {
                    const $m61 = $claimcheck.callee_of({ ctx: ctx, file: file, e: e });
                    $m61$match: {
                      if ($m61.tag === "FnCallee") {
                        const def = $m61.def;
                        const [, queue$51] = $std_list.push({ b: queue, x: def });
                        queue = queue$51;
                        break $m61$match;
                      }
                      if ($m61.tag === "ImplCallee") {
                        const def = $m61.def;
                        const [, queue$52] = $std_list.push({ b: queue, x: def });
                        queue = queue$52;
                        break $m61$match;
                      }
                      if (true) {
                        skip({  });
                        break $m61$match;
                      }
                      $rt.unreachable();
                    }
                  }
                  break $m60$match;
                }
                if (true) {
                  skip({  });
                  break $m60$match;
                }
                $rt.unreachable();
              }
            }
            break $m59$match;
          }
          $rt.unreachable();
        }
      }
    }
  }
  return $std_list.finish({ b: out });
}

export function in_policy_scope({ ctx, pc, policy_def, mod }) {
  const $m62 = $context.get_def({ ctx: ctx, id: policy_def }).node;
  $m62$match: {
    if ($m62.tag === "PolicyNode") {
      const decl = $m62.decl;
      const name = module_name({ ctx: ctx, mod: mod });
      for (const scope of decl.outside_list) {
        const $m63 = scope.name;
        $m63$match: {
          if ($m63.tag === "None") {
            if (mod === pc.m.id) {
              return true;
            }
            break $m63$match;
          }
          if ($m63.tag === "Some") {
            const value = $m63.value;
            const prefix = qname_text({ q: value });
            if (scope.glob) {
              if (name === prefix || $std_text.starts_with({ t: name, prefix: prefix + "." })) {
                return true;
              }
            } else {
              if (name === prefix) {
                return true;
              }
            }
            break $m63$match;
          }
          $rt.unreachable();
        }
      }
      return false;
      break $m62$match;
    }
    if (true) {
      return false;
      break $m62$match;
    }
    $rt.unreachable();
  }
}

