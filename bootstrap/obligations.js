// Generated by onus from module obligations. Do not edit.
import * as $rt from "/Users/jamesrandall/code/onus/packages/runtime/dist/index.js";
export function kind_text({ k }) {
  const $m1 = k;
  $m1$match: {
    if ($m1.tag === "Requires") {
      return "requires";
      break $m1$match;
    }
    if ($m1.tag === "Ensures") {
      return "ensures";
      break $m1$match;
    }
    if ($m1.tag === "Refinement") {
      return "refinement";
      break $m1$match;
    }
    if ($m1.tag === "InvariantEntry") {
      return "invariant-entry";
      break $m1$match;
    }
    if ($m1.tag === "InvariantStep") {
      return "invariant-step";
      break $m1$match;
    }
    if ($m1.tag === "Decreases") {
      return "decreases";
      break $m1$match;
    }
    if ($m1.tag === "Index") {
      return "index";
      break $m1$match;
    }
    if ($m1.tag === "Law") {
      return "law";
      break $m1$match;
    }
    if ($m1.tag === "Property") {
      return "property";
      break $m1$match;
    }
    if ($m1.tag === "Overflow") {
      return "overflow";
      break $m1$match;
    }
    if ($m1.tag === "Representation") {
      return "representation";
      break $m1$match;
    }
    if ($m1.tag === "Assertion") {
      return "assertion";
      break $m1$match;
    }
    if ($m1.tag === "ConstCheck") {
      return "const-check";
      break $m1$match;
    }
    $rt.unreachable();
  }
}

export function status_text({ s }) {
  const $m2 = s;
  $m2$match: {
    if ($m2.tag === "Pending") {
      return "pending";
      break $m2$match;
    }
    if ($m2.tag === "Proved") {
      return "proved";
      break $m2$match;
    }
    if ($m2.tag === "Checked") {
      return "checked";
      break $m2$match;
    }
    if ($m2.tag === "Assumed") {
      return "assumed";
      break $m2$match;
    }
    if ($m2.tag === "Failed") {
      return "failed";
      break $m2$match;
    }
    $rt.unreachable();
  }
}

