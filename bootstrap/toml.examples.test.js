// Generated by onus: tests of module toml.
import * as $rt from "./onus-runtime/index.js";
import * as $self from "./toml.js";

import { test, expect, afterAll } from 'vitest';
import * as fc from 'fast-check';
test("parses_the_manifest_shape", () => {
  expect($self.parse({ text: "[zones]\n\"app.core.*\" = \"critical\"\ndefault = \"draft\"\n\n[zones.exceptions]\n\"app.core.checkout\" = { third_party_assumes = [\"vendor.payments.charge\"] }\n" }).tag === "Ok").toBe(true);
  expect($self.parse({ text: "[zones\n" }).tag === "Err").toBe(true);
  expect($self.parse({ text: "x = 3\n" }).tag === "Err").toBe(true);
  expect($self.parse({ text: "# only a comment\n" }).tag === "Ok").toBe(true);
});
afterAll(() => $rt.coverage.flush());
