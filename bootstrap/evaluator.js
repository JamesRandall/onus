// Generated by onus from module evaluator. Do not edit.
import * as $rt from "./onus-runtime/index.js";
import * as $std_map from "./std/map.js";
import * as $defs from "./defs.js";
import * as $std_list from "./std/list.js";
import * as $std_int from "./std/int.js";
import * as $context from "./context.js";
import * as $report from "./report.js";
import * as $parser from "./parser.js";
import * as $values from "./values.js";
import * as $types from "./types.js";
import * as $std_float from "./std/float.js";
import * as $effectset from "./effectset.js";
import * as $printer from "./printer.js";
import * as $std_text from "./std/text.js";
import * as $typecheck from "./typecheck.js";
import * as $std_bytes from "./std/bytes.js";
import * as $std_bool from "./std/bool.js";

const $ob1 = { kind: "overflow", text: "n - 1 within Int", at: "self/evaluator.onus:83:37", def: "top_name" };
const $ob2 = { kind: "overflow", text: "n - 1 within Int", at: "self/evaluator.onus:91:37", def: "top_ret" };
const $ob3 = { kind: "overflow", text: "ev.steps + 1 within Int", at: "self/evaluator.onus:95:25", def: "step" };
const $ob4 = { kind: "overflow", text: "-1 within Int", at: "self/evaluator.onus:143:20", def: "or_neg" };
const $ob5 = { kind: "overflow", text: "-1 within Int", at: "self/evaluator.onus:156:20", def: "res_def" };
const $ob6 = { kind: "overflow", text: "-1 within Int", at: "self/evaluator.onus:161:19", def: "res_def" };
const $ob7 = { kind: "overflow", text: "-1 within Int", at: "self/evaluator.onus:171:20", def: "std_type" };
const $ob8 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/evaluator.onus:316:13", def: "const_of" };
const $ob9 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/evaluator.onus:369:11", def: "eval_expr" };
const $ob10 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/evaluator.onus:385:11", def: "eval_expr" };
const $ob11 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/evaluator.onus:393:13", def: "eval_expr" };
const $ob12 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/evaluator.onus:405:15", def: "eval_expr" };
const $ob13 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/evaluator.onus:420:32", def: "eval_expr" };
const $ob14 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/evaluator.onus:426:11", def: "eval_expr" };
const $ob15 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/evaluator.onus:436:11", def: "eval_expr" };
const $ob16 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/evaluator.onus:450:11", def: "eval_expr" };
const $ob17 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/evaluator.onus:457:11", def: "eval_expr" };
const $ob18 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/evaluator.onus:465:13", def: "eval_expr" };
const $ob19 = { kind: "overflow", text: "0 - values.int_of(c: v) within Int", at: "self/evaluator.onus:478:55", def: "eval_expr" };
const $ob20 = { kind: "overflow", text: "0 - values.int_of(c: v) within Int", at: "self/evaluator.onus:484:60", def: "eval_expr" };
const $ob21 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/evaluator.onus:489:11", def: "eval_expr" };
const $ob22 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/evaluator.onus:498:15", def: "eval_expr" };
const $ob23 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/evaluator.onus:516:15", def: "eval_expr" };
const $ob24 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/evaluator.onus:533:13", def: "eval_expr" };
const $ob25 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/evaluator.onus:540:13", def: "eval_expr" };
const $ob26 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/evaluator.onus:571:28", def: "name_value" };
const $ob27 = { kind: "overflow", text: "-1 within Int", at: "self/evaluator.onus:621:27", def: "ctor" };
const $ob28 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/evaluator.onus:633:38", def: "ctor" };
const $ob29 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/evaluator.onus:644:38", def: "ctor" };
const $ob30 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/evaluator.onus:683:13", def: "try_expr" };
const $ob31 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/evaluator.onus:728:15", def: "try_expr" };
const $ob32 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/evaluator.onus:779:13", def: "quantifier" };
const $ob33 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/evaluator.onus:794:17", def: "quantifier" };
const $ob34 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/evaluator.onus:808:17", def: "quantifier" };
const $ob35 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/evaluator.onus:846:13", def: "domain_values" };
const $ob36 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/evaluator.onus:853:13", def: "domain_values" };
const $ob37 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/evaluator.onus:874:13", def: "domain_values" };
const $ob38 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/evaluator.onus:925:19", def: "field_access" };
const $ob39 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/evaluator.onus:947:13", def: "field_access" };
const $ob40 = { kind: "overflow", text: "a + b within Int", at: "self/evaluator.onus:1002:7", def: "int_arith" };
const $ob41 = { kind: "overflow", text: "a - b within Int", at: "self/evaluator.onus:1009:7", def: "int_arith" };
const $ob42 = { kind: "overflow", text: "a * b within Int", at: "self/evaluator.onus:1016:7", def: "int_arith" };
const $ob43 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/evaluator.onus:1099:13", def: "binary" };
const $ob44 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/evaluator.onus:1106:13", def: "binary" };
const $ob45 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/evaluator.onus:1197:36", def: "call" };
const $ob46 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/evaluator.onus:1226:13", def: "call" };
const $ob47 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/evaluator.onus:1320:11", def: "call_fn" };
const $ob48 = { kind: "overflow", text: "n - 1 within Int", at: "self/evaluator.onus:1331:67", def: "call_fn" };
const $ob49 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/evaluator.onus:1367:15", def: "call_body" };
const $ob50 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/evaluator.onus:1402:11", def: "call_body" };
const $ob51 = { kind: "overflow", text: "ms * 1000000 within Int", at: "self/evaluator.onus:2040:7", def: "call_impl" };
const $ob52 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/evaluator.onus:2064:25", def: "exec_block" };
const $ob53 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/evaluator.onus:2086:13", def: "exec_stmt" };
const $ob54 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/evaluator.onus:2100:13", def: "exec_stmt" };
const $ob55 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/evaluator.onus:2114:13", def: "exec_stmt" };
const $ob56 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/evaluator.onus:2127:13", def: "exec_stmt" };
const $ob57 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/evaluator.onus:2137:13", def: "exec_stmt" };
const $ob58 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/evaluator.onus:2147:28", def: "exec_stmt" };
const $ob59 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/evaluator.onus:2151:30", def: "exec_stmt" };
const $ob60 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/evaluator.onus:2159:13", def: "exec_stmt" };
const $ob61 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/evaluator.onus:2168:15", def: "exec_stmt" };
const $ob62 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/evaluator.onus:2179:19", def: "exec_stmt" };
const $ob63 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/evaluator.onus:2197:32", def: "exec_stmt" };
const $ob64 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/evaluator.onus:2200:31", def: "exec_stmt" };
const $ob65 = { kind: "overflow", text: "guard - 1 within Int", at: "self/evaluator.onus:2221:15", def: "exec_stmt" };
const $ob66 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/evaluator.onus:2224:15", def: "exec_stmt" };
const $ob67 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/evaluator.onus:2238:28", def: "exec_stmt" };
const $ob68 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/evaluator.onus:2250:15", def: "exec_stmt" };
const $ob69 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/evaluator.onus:2257:15", def: "exec_stmt" };
const $ob70 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/evaluator.onus:2272:32", def: "exec_stmt" };
const $ob71 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/evaluator.onus:2278:15", def: "exec_stmt" };
const $ob72 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/evaluator.onus:2290:30", def: "exec_stmt" };
const $ob73 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/evaluator.onus:2298:13", def: "exec_stmt" };
const $ob74 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/evaluator.onus:2353:13", def: "match_pattern" };
const $ob75 = { kind: "overflow", text: "i + 1 within Int", at: "self/evaluator.onus:2399:19", def: "match_pattern" };
export const default_budget = 1000000;

export const max_depth = 100000;

export function new_ev({ file }) {
  return { steps: 0, budget: default_budget, frames: [], last_const_error: { tag: "None" }, file: file, busy: $std_map.dict({  }) };
}

export function reset({ ev }) {
  ev = { ...ev, steps: 0, last_const_error: { tag: "None" }, frames: [] };
  return [undefined, ev];
  return [undefined, ev];
}

export function skip($args) {
  return undefined;
}

export function key({ ev, tag, span }) {
  return $defs.node_key({ file: ev.file, tag: tag, span: span });
}

export function top_name({ ev }) {
  const n = $std_list.len({ xs: ev.frames });
  if (n === 0) {
    return "constant expression";
  }
  return $std_list.get({ xs: ev.frames, i: $rt.int.sub(n, 1, $ob1) }).name;
}

export function top_ret({ ev }) {
  const n = $std_list.len({ xs: ev.frames });
  if (n === 0) {
    return { tag: "None" };
  }
  return $std_list.get({ xs: ev.frames, i: $rt.int.sub(n, 1, $ob2) }).ret;
}

export function step({ ev }) {
  ev = { ...ev, steps: $rt.int.add(ev.steps, 1, $ob3) };
  if (ev.steps > ev.budget) {
    return [{ tag: "Err", error: { tag: "BudgetF", fn_name: top_name({ ev: ev }) } }, ev];
  }
  return [{ tag: "Ok", value: undefined }, ev];
}

export function not_const({ reason, span }) {
  return { tag: "NotConstF", reason: reason, span: { tag: "Some", value: span } };
}

export function panic_at({ detail, span }) {
  return { tag: "PanicF", detail: detail, span: { tag: "Some", value: span } };
}

export const max_safe = 9007199254740991;

export function safe_int({ n, span }) {
  if (n > max_safe || n < 0 - max_safe) {
    return { tag: "Err", error: panic_at({ detail: $std_int.to_text({ x: n }) + " is outside the runtime's safe integer range (±2^53)", span: span }) };
  }
  return { tag: "Ok", value: n };
}

export function copy_env({ env }) {
  let out = $std_map.dict({  });
  for (const k of $std_map.keys({ d: env })) {
    const $m16 = $std_map.find({ d: env, key: k });
    $m16$match: {
      if ($m16.tag === "Some") {
        const value = $m16.value;
        const [, out$1] = $std_map.set({ d: out, key: k, value: value });
        out = out$1;
        break $m16$match;
      }
      if ($m16.tag === "None") {
        skip({  });
        break $m16$match;
      }
      $rt.unreachable();
    }
  }
  return out;
}

export function env_set({ env, k, v }) {
  let d = env;
  const [, d$2] = $std_map.set({ d: d, key: k, value: v });
  d = d$2;
  return undefined;
}

export function or_neg({ o }) {
  const $m17 = o;
  $m17$match: {
    if ($m17.tag === "Some") {
      const value = $m17.value;
      return value;
      break $m17$match;
    }
    if ($m17.tag === "None") {
      return $rt.int.neg(1, $ob4);
      break $m17$match;
    }
    $rt.unreachable();
  }
}

export function def_of_key({ ctx, k }) {
  return or_neg({ o: $std_map.find({ d: ctx.def_of, key: k }) });
}

export function res_at({ ctx, k }) {
  return $std_map.find({ d: ctx.refs, key: k });
}

export function res_def({ res }) {
  const $m18 = res;
  $m18$match: {
    if ($m18.tag === "None") {
      return $rt.int.neg(1, $ob5);
      break $m18$match;
    }
    if ($m18.tag === "Some") {
      const value = $m18.value;
      const $m19 = value;
      $m19$match: {
        if ($m19.tag === "DefRes") {
          const def = $m19.def;
          return def;
          break $m19$match;
        }
        if ($m19.tag === "CompanionRes") {
          const owner = $m19.owner;
          const fn_def = $m19.fn_def;
          return fn_def;
          break $m19$match;
        }
        if (true) {
          return $rt.int.neg(1, $ob6);
          break $m19$match;
        }
        $rt.unreachable();
      }
      break $m18$match;
    }
    $rt.unreachable();
  }
}

export function std_type({ ctx, module_name, type_name }) {
  const $m20 = $std_map.find({ d: ctx.by_name, key: module_name });
  $m20$match: {
    if ($m20.tag === "None") {
      return [$rt.int.neg(1, $ob7), ctx];
      break $m20$match;
    }
    if ($m20.tag === "Some") {
      const value = $m20.value;
      const [$r21, ctx$3] = $context.members_of({ ctx: ctx, module: value });
      ctx = ctx$3;
      return [or_neg({ o: $std_map.find({ d: $r21.types, key: type_name }) }), ctx];
      break $m20$match;
    }
    $rt.unreachable();
  }
}

export function owner_name({ ctx, o }) {
  const $m22 = o;
  $m22$match: {
    if ($m22.tag === "PrimOwner") {
      const name = $m22.name;
      return name;
      break $m22$match;
    }
    if ($m22.tag === "DefOwner") {
      const def = $m22.def;
      return $context.get_def({ ctx: ctx, id: def }).name;
      break $m22$match;
    }
    $rt.unreachable();
  }
}

export function variant_named({ ctx, module_name, union_name, variant_name, fields }) {
  const [$r23, ctx$4] = std_type({ ctx: ctx, module_name: module_name, type_name: union_name });
  ctx = ctx$4;
  const union_def = $r23;
  if (union_def >= 0) {
    const $m24 = $std_map.find({ d: ctx.variants, key: union_def });
    $m24$match: {
      if ($m24.tag === "Some") {
        const value = $m24.value;
        for (const v of value) {
          if ($context.get_def({ ctx: ctx, id: v }).name === variant_name) {
            return [{ tag: "Ok", value: { tag: "VariantV", def: v, fields: fields } }, ctx];
          }
        }
        break $m24$match;
      }
      if ($m24.tag === "None") {
        skip({  });
        break $m24$match;
      }
      $rt.unreachable();
    }
  }
  return [{ tag: "Err", error: { tag: "NotConstF", reason: union_name + "." + variant_name + " is not available", span: { tag: "None" } } }, ctx];
}

export function option_value({ ctx, o }) {
  const $m30 = o;
  $m30$match: {
    if ($m30.tag === "Some") {
      const value = $m30.value;
      const [$r32, ctx$5] = variant_named({ ctx: ctx, module_name: "std.option", union_name: "Option", variant_name: "Some", fields: [{ name: "value", value: value }] });
      ctx = ctx$5;
      return [$r32, ctx];
      break $m30$match;
    }
    if ($m30.tag === "None") {
      const [$r33, ctx$6] = variant_named({ ctx: ctx, module_name: "std.option", union_name: "Option", variant_name: "None", fields: [] });
      ctx = ctx$6;
      return [$r33, ctx];
      break $m30$match;
    }
    $rt.unreachable();
  }
}

export function report_failure({ ctx, file, def_name, what, f, at }) {
  const $m34 = f;
  $m34$match: {
    if ($m34.tag === "NotConstF") {
      const reason = $m34.reason;
      const span = $m34.span;
      const [, ctx$7] = $context.report({ ctx: ctx, d: $report.diagnostic({ code: "E0701", file: file, span: span_or({ o: span, dflt: at }), def_name: { tag: "Some", value: def_name }, detail: what + " is not a constant expression: " + reason }) });
      ctx = ctx$7;
      break $m34$match;
    }
    if ($m34.tag === "PanicF") {
      const detail = $m34.detail;
      const span = $m34.span;
      const [, ctx$8] = $context.report({ ctx: ctx, d: $report.diagnostic({ code: "E0701", file: file, span: span_or({ o: span, dflt: at }), def_name: { tag: "Some", value: def_name }, detail: what + ": " + detail }) });
      ctx = ctx$8;
      break $m34$match;
    }
    if ($m34.tag === "BudgetF") {
      const fn_name = $m34.fn_name;
      const [, ctx$9] = $context.report({ ctx: ctx, d: $report.diagnostic({ code: "E0501", file: file, span: at, def_name: { tag: "Some", value: def_name }, detail: "evaluating " + what + " exceeded the check-time step budget in `" + fn_name + "`" }) });
      ctx = ctx$9;
      break $m34$match;
    }
    if ($m34.tag === "ReturnF") {
      const [, ctx$10] = $context.report({ ctx: ctx, d: $report.diagnostic({ code: "E0701", file: file, span: at, def_name: { tag: "Some", value: def_name }, detail: what + " is not a constant expression: a `return` outside a function" }) });
      ctx = ctx$10;
      break $m34$match;
    }
    $rt.unreachable();
  }
  return [undefined, ctx];
  return [undefined, ctx];
}

export function span_or({ o, dflt }) {
  const $m39 = o;
  $m39$match: {
    if ($m39.tag === "Some") {
      const value = $m39.value;
      return value;
      break $m39$match;
    }
    if ($m39.tag === "None") {
      return dflt;
      break $m39$match;
    }
    $rt.unreachable();
  }
}

export function const_of({ fuel, ev, ctx, def }) {
  if (fuel === 0) {
    return [{ tag: "None" }, ev, ctx];
  }
  const $m41 = $std_map.find({ d: ctx.const_values, key: def });
  $m41$match: {
    if ($m41.tag === "Some") {
      const value = $m41.value;
      return [{ tag: "Some", value: value }, ev, ctx];
      break $m41$match;
    }
    if ($m41.tag === "None") {
      skip({  });
      break $m41$match;
    }
    $rt.unreachable();
  }
  if ($std_map.contains({ d: ev.busy, key: def })) {
    return [{ tag: "None" }, ev, ctx];
  }
  let busy = ev.busy;
  const [, busy$11] = $std_map.set({ d: busy, key: def, value: true });
  busy = busy$11;
  const d = $context.get_def({ ctx: ctx, id: def });
  let out = { tag: "None" };
  const $m45 = d.node;
  $m45$match: {
    if ($m45.tag === "ConstNode") {
      const decl = $m45.decl;
      const saved_file = ev.file;
      const [, ev$12] = reset({ ev: ev });
      ev = ev$12;
      ev = { ...ev, file: d.file };
      const [$r48, ev$13, ctx$13] = eval_expr({ fuel: $rt.int.sub(fuel, 1, $ob8), ev: ev, ctx: ctx, e: decl.value, env: $std_map.dict({  }) });
      ev = ev$13;
      ctx = ctx$13;
      const $m47 = $r48;
      $m47$match: {
        if ($m47.tag === "Ok") {
          const value = $m47.value;
          out = { tag: "Some", value: value };
          break $m47$match;
        }
        if ($m47.tag === "Err") {
          const error = $m47.error;
          const [, ctx$14] = report_failure({ ctx: ctx, file: d.file, def_name: d.name, what: "`" + d.name + "`", f: error, at: $parser.span_of_expr({ e: decl.value }) });
          ctx = ctx$14;
          break $m47$match;
        }
        $rt.unreachable();
      }
      ev = { ...ev, file: saved_file };
      break $m45$match;
    }
    if (true) {
      out = { tag: "None" };
      break $m45$match;
    }
    $rt.unreachable();
  }
  const [, busy$15] = $std_map.remove({ d: busy, key: def });
  busy = busy$15;
  const $m52 = out;
  $m52$match: {
    if ($m52.tag === "Some") {
      const value = $m52.value;
      let table = ctx.const_values;
      const [, table$16] = $std_map.set({ d: table, key: def, value: value });
      table = table$16;
      break $m52$match;
    }
    if ($m52.tag === "None") {
      skip({  });
      break $m52$match;
    }
    $rt.unreachable();
  }
  return [out, ev, ctx];
}

export function eval_expr({ fuel, ev, ctx, e, env }) {
  try {
    if (fuel === 0) {
      return [{ tag: "Err", error: { tag: "BudgetF", fn_name: top_name({ ev: ev }) } }, ev, ctx];
    }
    const [$r55, ev$17] = step({ ev: ev });
    ev = ev$17;
    $rt.unwrap($r55);
    const $m56 = e;
    $m56$match: {
      if ($m56.tag === "IntLit") {
        const value = $m56.value;
        const span = $m56.span;
        return [{ tag: "Ok", value: { tag: "IntV", v: $rt.unwrap(safe_int({ n: value, span: span })) } }, ev, ctx];
        break $m56$match;
      }
      if ($m56.tag === "FloatLit") {
        const value = $m56.value;
        return [{ tag: "Ok", value: { tag: "FloatV", v: value } }, ev, ctx];
        break $m56$match;
      }
      if ($m56.tag === "TextLit") {
        const value = $m56.value;
        return [{ tag: "Ok", value: { tag: "TextV", v: value } }, ev, ctx];
        break $m56$match;
      }
      if ($m56.tag === "BoolLit") {
        const value = $m56.value;
        return [{ tag: "Ok", value: { tag: "BoolV", v: value } }, ev, ctx];
        break $m56$match;
      }
      if ($m56.tag === "DurationLit") {
        const nanos = $m56.nanos;
        const span = $m56.span;
        return [{ tag: "Ok", value: { tag: "DurationV", v: $rt.unwrap(safe_int({ n: nanos, span: span })) } }, ev, ctx];
        break $m56$match;
      }
      if ($m56.tag === "Name") {
        const [$r67, ev$18, ctx$18] = name_value({ fuel: $rt.int.sub(fuel, 1, $ob9), ev: ev, ctx: ctx, e: e, env: env });
        ev = ev$18;
        ctx = ctx$18;
        return [$r67, ev, ctx];
        break $m56$match;
      }
      if ($m56.tag === "It") {
        const span = $m56.span;
        return [{ tag: "Err", error: not_const({ reason: "contract keywords have no value at check time", span: span }) }, ev, ctx];
        break $m56$match;
      }
      if ($m56.tag === "ResultRef") {
        const span = $m56.span;
        return [{ tag: "Err", error: not_const({ reason: "contract keywords have no value at check time", span: span }) }, ev, ctx];
        break $m56$match;
      }
      if ($m56.tag === "Old") {
        const span = $m56.span;
        return [{ tag: "Err", error: not_const({ reason: "contract keywords have no value at check time", span: span }) }, ev, ctx];
        break $m56$match;
      }
      if ($m56.tag === "Ctor") {
        const [$r71, ev$19, ctx$19] = ctor({ fuel: $rt.int.sub(fuel, 1, $ob10), ev: ev, ctx: ctx, e: e, env: env });
        ev = ev$19;
        ctx = ctx$19;
        return [$r71, ev, ctx];
        break $m56$match;
      }
      if ($m56.tag === "RecordUpdate") {
        const base = $m56.base;
        const fields = $m56.fields;
        const span = $m56.span;
        const [$r72, ev$20, ctx$20] = eval_expr({ fuel: $rt.int.sub(fuel, 1, $ob11), ev: ev, ctx: ctx, e: base, env: env });
        ev = ev$20;
        ctx = ctx$20;
        const bv = $rt.unwrap($r72);
        if (!(bv.tag === "RecordV")) {
          return [{ tag: "Err", error: not_const({ reason: "`with` on a non-record", span: span }) }, ev, ctx];
        }
        let fs = $values.fields_of({ c: bv });
        for (const f of fields) {
          const [$r74, ev$21, ctx$21] = eval_expr({ fuel: $rt.int.sub(fuel, 1, $ob12), ev: ev, ctx: ctx, e: f.value, env: env });
          ev = ev$21;
          ctx = ctx$21;
          const fv = $rt.unwrap($r74);
          fs = $values.with_field({ fields: fs, name: f.name.text, value: fv });
        }
        return [{ tag: "Ok", value: { tag: "RecordV", def: $values.def_of({ c: bv }), fields: fs } }, ev, ctx];
        break $m56$match;
      }
      if ($m56.tag === "ListLit") {
        const elems = $m56.elems;
        let items = $std_list.builder({  });
        for (const x of elems) {
          const [$r77, ev$22, ctx$22] = eval_expr({ fuel: $rt.int.sub(fuel, 1, $ob13), ev: ev, ctx: ctx, e: x, env: env });
          ev = ev$22;
          ctx = ctx$22;
          const [, items$23] = $std_list.push({ b: items, x: $rt.unwrap($r77) });
          items = items$23;
        }
        return [{ tag: "Ok", value: { tag: "ListV", items: $std_list.finish({ b: items }) } }, ev, ctx];
        break $m56$match;
      }
      if ($m56.tag === "Try") {
        const [$r80, ev$24, ctx$24] = try_expr({ fuel: $rt.int.sub(fuel, 1, $ob14), ev: ev, ctx: ctx, e: e, env: env });
        ev = ev$24;
        ctx = ctx$24;
        return [$r80, ev, ctx];
        break $m56$match;
      }
      if ($m56.tag === "Recover") {
        const span = $m56.span;
        return [{ tag: "Err", error: not_const({ reason: "`recover` is not evaluated at check time", span: span }) }, ev, ctx];
        break $m56$match;
      }
      if ($m56.tag === "Quantifier") {
        const [$r82, ev$25, ctx$25] = quantifier({ fuel: $rt.int.sub(fuel, 1, $ob15), ev: ev, ctx: ctx, e: e, env: env });
        ev = ev$25;
        ctx = ctx$25;
        return [$r82, ev, ctx];
        break $m56$match;
      }
      if ($m56.tag === "Closure") {
        const span = $m56.span;
        return [{ tag: "Err", error: not_const({ reason: "closures are not values at check time", span: span }) }, ev, ctx];
        break $m56$match;
      }
      if ($m56.tag === "Fake") {
        const span = $m56.span;
        return [{ tag: "Err", error: not_const({ reason: "`fake` capabilities exist only in generated tests", span: span }) }, ev, ctx];
        break $m56$match;
      }
      if ($m56.tag === "Hole") {
        const span = $m56.span;
        return [{ tag: "Err", error: not_const({ reason: "a hole has no value", span: span }) }, ev, ctx];
        break $m56$match;
      }
      if ($m56.tag === "FieldAccess") {
        const [$r86, ev$26, ctx$26] = field_access({ fuel: $rt.int.sub(fuel, 1, $ob16), ev: ev, ctx: ctx, e: e, env: env });
        ev = ev$26;
        ctx = ctx$26;
        return [$r86, ev, ctx];
        break $m56$match;
      }
      if ($m56.tag === "Call") {
        const [$r87, ev$27, ctx$27] = call({ fuel: $rt.int.sub(fuel, 1, $ob17), ev: ev, ctx: ctx, e: e, env: env });
        ev = ev$27;
        ctx = ctx$27;
        return [$r87, ev, ctx];
        break $m56$match;
      }
      if ($m56.tag === "Unary") {
        const op = $m56.op;
        const operand = $m56.operand;
        const span = $m56.span;
        const [$r88, ev$28, ctx$28] = eval_expr({ fuel: $rt.int.sub(fuel, 1, $ob18), ev: ev, ctx: ctx, e: operand, env: env });
        ev = ev$28;
        ctx = ctx$28;
        const v = $rt.unwrap($r88);
        if (op === "not") {
          if (!(v.tag === "BoolV")) {
            return [{ tag: "Err", error: panic_at({ detail: "`not` on a non-Bool", span: span }) }, ev, ctx];
          }
          return [{ tag: "Ok", value: { tag: "BoolV", v: !$values.bool_of({ c: v }) } }, ev, ctx];
        }
        if (v.tag === "IntV") {
          return [{ tag: "Ok", value: { tag: "IntV", v: $rt.unwrap(safe_int({ n: $rt.int.sub(0, $values.int_of({ c: v }), $ob19), span: span })) } }, ev, ctx];
        }
        if (v.tag === "FloatV") {
          return [{ tag: "Ok", value: { tag: "FloatV", v: 0 - $values.float_of({ c: v }) } }, ev, ctx];
        }
        if (v.tag === "DurationV") {
          return [{ tag: "Ok", value: { tag: "DurationV", v: $rt.unwrap(safe_int({ n: $rt.int.sub(0, $values.int_of({ c: v }), $ob20), span: span })) } }, ev, ctx];
        }
        return [{ tag: "Err", error: panic_at({ detail: "unary `-` on a non-number", span: span }) }, ev, ctx];
        break $m56$match;
      }
      if ($m56.tag === "Binary") {
        const [$r99, ev$29, ctx$29] = binary({ fuel: $rt.int.sub(fuel, 1, $ob21), ev: ev, ctx: ctx, e: e, env: env });
        ev = ev$29;
        ctx = ctx$29;
        return [$r99, ev, ctx];
        break $m56$match;
      }
      if ($m56.tag === "And") {
        const operands = $m56.operands;
        for (const o of operands) {
          const [$r100, ev$30, ctx$30] = eval_expr({ fuel: $rt.int.sub(fuel, 1, $ob22), ev: ev, ctx: ctx, e: o, env: env });
          ev = ev$30;
          ctx = ctx$30;
          const v = $rt.unwrap($r100);
          if (!(v.tag === "BoolV")) {
            return [{ tag: "Err", error: panic_at({ detail: "`and` on a non-Bool", span: $parser.span_of_expr({ e: o }) }) }, ev, ctx];
          }
          if (!$values.bool_of({ c: v })) {
            return [{ tag: "Ok", value: { tag: "BoolV", v: false } }, ev, ctx];
          }
        }
        return [{ tag: "Ok", value: { tag: "BoolV", v: true } }, ev, ctx];
        break $m56$match;
      }
      if ($m56.tag === "Or") {
        const operands = $m56.operands;
        for (const o of operands) {
          const [$r106, ev$31, ctx$31] = eval_expr({ fuel: $rt.int.sub(fuel, 1, $ob23), ev: ev, ctx: ctx, e: o, env: env });
          ev = ev$31;
          ctx = ctx$31;
          const v = $rt.unwrap($r106);
          if (!(v.tag === "BoolV")) {
            return [{ tag: "Err", error: panic_at({ detail: "`or` on a non-Bool", span: $parser.span_of_expr({ e: o }) }) }, ev, ctx];
          }
          if ($values.bool_of({ c: v })) {
            return [{ tag: "Ok", value: { tag: "BoolV", v: true } }, ev, ctx];
          }
        }
        return [{ tag: "Ok", value: { tag: "BoolV", v: false } }, ev, ctx];
        break $m56$match;
      }
      if ($m56.tag === "Is") {
        const expr = $m56.expr;
        const pattern = $m56.pattern;
        const [$r112, ev$32, ctx$32] = eval_expr({ fuel: $rt.int.sub(fuel, 1, $ob24), ev: ev, ctx: ctx, e: expr, env: env });
        ev = ev$32;
        ctx = ctx$32;
        const v = $rt.unwrap($r112);
        const [$r113, ev$33, ctx$33] = match_pattern({ fuel: $rt.int.sub(fuel, 1, $ob25), ev: ev, ctx: ctx, p: pattern, v: v, env: copy_env({ env: env }) });
        ev = ev$33;
        ctx = ctx$33;
        const matched = $rt.unwrap($r113);
        return [{ tag: "Ok", value: { tag: "BoolV", v: matched } }, ev, ctx];
        break $m56$match;
      }
      $rt.unreachable();
    }
  } catch ($e) {
    if ($e instanceof $rt.EarlyReturn) return [$e.value, ev, ctx];
    throw $e;
  }
}

export function name_value({ fuel, ev, ctx, e, env }) {
  if (fuel === 0) {
    return [{ tag: "Err", error: { tag: "BudgetF", fn_name: top_name({ ev: ev }) } }, ev, ctx];
  }
  const $m118 = e;
  $m118$match: {
    if ($m118.tag === "Name") {
      const name = $m118.name;
      const span = $m118.span;
      const def = res_def({ res: res_at({ ctx: ctx, k: key({ ev: ev, tag: $defs.tag_expr, span: span }) }) });
      if (def < 0) {
        return [{ tag: "Err", error: not_const({ reason: "`" + name.text + "` is unresolved", span: span }) }, ev, ctx];
      }
      const d = $context.get_def({ ctx: ctx, id: def });
      if ($rt.eq(d.kind, { tag: "Const" })) {
        const [$r122, ev$34, ctx$34] = const_of({ fuel: $rt.int.sub(fuel, 1, $ob26), ev: ev, ctx: ctx, def: def });
        ev = ev$34;
        ctx = ctx$34;
        const $m121 = $r122;
        $m121$match: {
          if ($m121.tag === "Some") {
            const value = $m121.value;
            return [{ tag: "Ok", value: value }, ev, ctx];
            break $m121$match;
          }
          if ($m121.tag === "None") {
            return [{ tag: "Err", error: not_const({ reason: "`" + d.name + "` is not a constant", span: span }) }, ev, ctx];
            break $m121$match;
          }
          $rt.unreachable();
        }
      }
      if ($rt.eq(d.kind, { tag: "Fn" })) {
        return [{ tag: "Err", error: not_const({ reason: "`" + d.name + "` is a function value", span: span }) }, ev, ctx];
      }
      const $m127 = $std_map.find({ d: env, key: def });
      $m127$match: {
        if ($m127.tag === "Some") {
          const value = $m127.value;
          return [{ tag: "Ok", value: value }, ev, ctx];
          break $m127$match;
        }
        if ($m127.tag === "None") {
          return [{ tag: "Err", error: not_const({ reason: "`" + d.name + "` is not known at check time", span: span }) }, ev, ctx];
          break $m127$match;
        }
        $rt.unreachable();
      }
      break $m118$match;
    }
    if (true) {
      return [{ tag: "Err", error: { tag: "NotConstF", reason: "not a name", span: { tag: "None" } } }, ev, ctx];
      break $m118$match;
    }
    $rt.unreachable();
  }
}

export function ctor_target({ ev, ctx, span }) {
  const $m133 = res_at({ ctx: ctx, k: key({ ev: ev, tag: $defs.tag_expr, span: span }) });
  $m133$match: {
    if ($m133.tag === "None") {
      return { tag: "Err", error: not_const({ reason: "unresolved constructor", span: span }) };
      break $m133$match;
    }
    if ($m133.tag === "Some") {
      const value = $m133.value;
      const $m135 = value;
      $m135$match: {
        if ($m135.tag === "UnitRes") {
          return { tag: "Ok", value: { tag: "UnitTarget" } };
          break $m135$match;
        }
        if ($m135.tag === "TypeValueRes") {
          const owner = $m135.owner;
          return { tag: "Ok", value: { tag: "InfoTarget", owner: owner } };
          break $m135$match;
        }
        if ($m135.tag === "DefRes") {
          const def = $m135.def;
          return { tag: "Ok", value: { tag: "DefTarget", def: def } };
          break $m135$match;
        }
        if (true) {
          return { tag: "Err", error: not_const({ reason: "unresolved constructor", span: span }) };
          break $m135$match;
        }
        $rt.unreachable();
      }
      break $m133$match;
    }
    $rt.unreachable();
  }
}

export function ctor({ fuel, ev, ctx, e, env }) {
  try {
    if (fuel === 0) {
      return [{ tag: "Err", error: { tag: "BudgetF", fn_name: top_name({ ev: ev }) } }, ev, ctx];
    }
    const $m145 = e;
    $m145$match: {
      if ($m145.tag === "Ctor") {
        const args = $m145.args;
        const fields = $m145.fields;
        const span = $m145.span;
        const target = $rt.unwrap(ctor_target({ ev: ev, ctx: ctx, span: span }));
        let target_def = $rt.int.neg(1, $ob27);
        const $m146 = target;
        $m146$match: {
          if ($m146.tag === "UnitTarget") {
            return [{ tag: "Ok", value: { tag: "UnitV" } }, ev, ctx];
            break $m146$match;
          }
          if ($m146.tag === "InfoTarget") {
            const owner = $m146.owner;
            return [{ tag: "Ok", value: { tag: "TypeInfoV", owner: owner } }, ev, ctx];
            break $m146$match;
          }
          if ($m146.tag === "DefTarget") {
            const def = $m146.def;
            target_def = def;
            break $m146$match;
          }
          $rt.unreachable();
        }
        let fs = [];
        const $m151 = args;
        $m151$match: {
          if ($m151.tag === "Some") {
            const value = $m151.value;
            for (const a of value) {
              const [$r152, ev$35, ctx$35] = eval_expr({ fuel: $rt.int.sub(fuel, 1, $ob28), ev: ev, ctx: ctx, e: a.value, env: env });
              ev = ev$35;
              ctx = ctx$35;
              fs = $values.with_field({ fields: fs, name: a.name.text, value: $rt.unwrap($r152) });
            }
            break $m151$match;
          }
          if ($m151.tag === "None") {
            skip({  });
            break $m151$match;
          }
          $rt.unreachable();
        }
        const $m153 = fields;
        $m153$match: {
          if ($m153.tag === "Some") {
            const value = $m153.value;
            for (const f of value) {
              const [$r154, ev$36, ctx$36] = eval_expr({ fuel: $rt.int.sub(fuel, 1, $ob29), ev: ev, ctx: ctx, e: f.value, env: env });
              ev = ev$36;
              ctx = ctx$36;
              fs = $values.with_field({ fields: fs, name: f.name.text, value: $rt.unwrap($r154) });
            }
            break $m153$match;
          }
          if ($m153.tag === "None") {
            skip({  });
            break $m153$match;
          }
          $rt.unreachable();
        }
        const d = $context.get_def({ ctx: ctx, id: target_def });
        if ($rt.eq(d.kind, { tag: "Variant" })) {
          return [{ tag: "Ok", value: { tag: "VariantV", def: target_def, fields: fs } }, ev, ctx];
        }
        const rv = { tag: "RecordV", def: target_def, fields: fs };
        const [$r159, ctx$37] = std_type({ ctx: ctx, module_name: "std.check", type_name: "ConstError" });
        ctx = ctx$37;
        if (target_def === $r159) {
          ev = { ...ev, last_const_error: { tag: "Some", value: rv } };
        }
        return [{ tag: "Ok", value: rv }, ev, ctx];
        break $m145$match;
      }
      if (true) {
        return [{ tag: "Err", error: { tag: "NotConstF", reason: "not a constructor", span: { tag: "None" } } }, ev, ctx];
        break $m145$match;
      }
      $rt.unreachable();
    }
  } catch ($e) {
    if ($e instanceof $rt.EarlyReturn) return [$e.value, ev, ctx];
    throw $e;
  }
}

export function value_or_unit({ o }) {
  const $m166 = o;
  $m166$match: {
    if ($m166.tag === "Some") {
      const value = $m166.value;
      return value;
      break $m166$match;
    }
    if ($m166.tag === "None") {
      return { tag: "UnitV" };
      break $m166$match;
    }
    $rt.unreachable();
  }
}

export function try_expr({ fuel, ev, ctx, e, env }) {
  try {
    if (fuel === 0) {
      return [{ tag: "Err", error: { tag: "BudgetF", fn_name: top_name({ ev: ev }) } }, ev, ctx];
    }
    const $m170 = e;
    $m170$match: {
      if ($m170.tag === "Try") {
        const expr = $m170.expr;
        const else_ = $m170.else_;
        const span = $m170.span;
        const [$r171, ev$38, ctx$38] = eval_expr({ fuel: $rt.int.sub(fuel, 1, $ob30), ev: ev, ctx: ctx, e: expr, env: env });
        ev = ev$38;
        ctx = ctx$38;
        const v = $rt.unwrap($r171);
        if (!(v.tag === "VariantV")) {
          return [{ tag: "Err", error: not_const({ reason: "`try` on a non-union", span: span }) }, ev, ctx];
        }
        const vname = $context.get_def({ ctx: ctx, id: $values.def_of({ c: v }) }).name;
        if (vname === "Ok" || vname === "Some") {
          const inner_value = $values.field_of({ fields: $values.fields_of({ c: v }), name: "value" });
          if (inner_value.tag === "None") {
            return [{ tag: "Err", error: not_const({ reason: "malformed Ok/Some", span: span }) }, ev, ctx];
          }
          return [{ tag: "Ok", value: value_or_unit({ o: inner_value }) }, ev, ctx];
        }
        let ret = { tag: "ErrorT" };
        const $m176 = top_ret({ ev: ev });
        $m176$match: {
          if ($m176.tag === "Some") {
            const value = $m176.value;
            ret = $types.strip({ t: value });
            break $m176$match;
          }
          if ($m176.tag === "None") {
            ret = { tag: "ErrorT" };
            break $m176$match;
          }
          $rt.unreachable();
        }
        if (!(ret.tag === "UnionT")) {
          return [{ tag: "Err", error: not_const({ reason: "`try` outside a fallible function", span: span }) }, ev, ctx];
        }
        const [$r179, ctx$39] = std_type({ ctx: ctx, module_name: "std.option", type_name: "Option" });
        ctx = ctx$39;
        const returns_option = $types.def_of({ t: ret }) === $r179;
        const $m180 = else_;
        $m180$match: {
          if ($m180.tag === "None") {
            return [{ tag: "Err", error: { tag: "ReturnF", value: v } }, ev, ctx];
            break $m180$match;
          }
          if ($m180.tag === "Some") {
            const value = $m180.value;
            let err_value = { tag: "UnitV" };
            if (vname === "Err") {
              err_value = value_or_unit({ o: $values.field_of({ fields: $values.fields_of({ c: v }), name: "error" }) });
            }
            const inner = copy_env({ env: env });
            const else_def = def_of_key({ ctx: ctx, k: key({ ev: ev, tag: $defs.tag_misc, span: value.span }) });
            if (else_def >= 0) {
              env_set({ env: inner, k: else_def, v: err_value });
            }
            const [$r184, ev$40, ctx$40] = eval_expr({ fuel: $rt.int.sub(fuel, 1, $ob31), ev: ev, ctx: ctx, e: value.expr, env: inner });
            ev = ev$40;
            ctx = ctx$40;
            const converted = $rt.unwrap($r184);
            if (returns_option) {
              const [$r185, ctx$41] = variant_named({ ctx: ctx, module_name: "std.option", union_name: "Option", variant_name: "None", fields: [] });
              ctx = ctx$41;
              const none = $rt.unwrap($r185);
              return [{ tag: "Err", error: { tag: "ReturnF", value: none } }, ev, ctx];
            }
            const [$r189, ctx$42] = variant_named({ ctx: ctx, module_name: "std.results", union_name: "Result", variant_name: "Err", fields: [{ name: "error", value: converted }] });
            ctx = ctx$42;
            const err = $rt.unwrap($r189);
            return [{ tag: "Err", error: { tag: "ReturnF", value: err } }, ev, ctx];
            break $m180$match;
          }
          $rt.unreachable();
        }
        break $m170$match;
      }
      if (true) {
        return [{ tag: "Err", error: { tag: "NotConstF", reason: "not a try", span: { tag: "None" } } }, ev, ctx];
        break $m170$match;
      }
      $rt.unreachable();
    }
  } catch ($e) {
    if ($e instanceof $rt.EarlyReturn) return [$e.value, ev, ctx];
    throw $e;
  }
}

export function quantifier({ fuel, ev, ctx, e, env }) {
  try {
    if (fuel === 0) {
      return [{ tag: "Err", error: { tag: "BudgetF", fn_name: top_name({ ev: ev }) } }, ev, ctx];
    }
    const $m197 = e;
    $m197$match: {
      if ($m197.tag === "Quantifier") {
        const quant = $m197.quant;
        const domain = $m197.domain;
        const where_ = $m197.where_;
        const body = $m197.body;
        const span = $m197.span;
        const binder = def_of_key({ ctx: ctx, k: key({ ev: ev, tag: $defs.tag_expr, span: span }) });
        if (binder < 0) {
          return [{ tag: "Err", error: not_const({ reason: "unresolved binder", span: span }) }, ev, ctx];
        }
        let items = [];
        const $m199 = domain;
        $m199$match: {
          if ($m199.tag === "None") {
            items = [{ tag: "BoolV", v: true }, { tag: "BoolV", v: false }];
            break $m199$match;
          }
          if ($m199.tag === "Some") {
            const value = $m199.value;
            const [$r202, ev$43, ctx$43] = domain_values({ fuel: $rt.int.sub(fuel, 1, $ob32), ev: ev, ctx: ctx, d: value, env: env, at: span });
            ev = ev$43;
            ctx = ctx$43;
            items = $rt.unwrap($r202);
            break $m199$match;
          }
          $rt.unreachable();
        }
        const inner = copy_env({ env: env });
        for (const x of items) {
          const [$r203, ev$44] = step({ ev: ev });
          ev = ev$44;
          $rt.unwrap($r203);
          env_set({ env: inner, k: binder, v: x });
          let keep = true;
          const $m204 = where_;
          $m204$match: {
            if ($m204.tag === "Some") {
              const value = $m204.value;
              const [$r205, ev$45, ctx$45] = eval_expr({ fuel: $rt.int.sub(fuel, 1, $ob33), ev: ev, ctx: ctx, e: value, env: inner });
              ev = ev$45;
              ctx = ctx$45;
              const w = $rt.unwrap($r205);
              if (!(w.tag === "BoolV")) {
                return [{ tag: "Err", error: panic_at({ detail: "`where` must be Bool", span: span }) }, ev, ctx];
              }
              keep = $values.bool_of({ c: w });
              break $m204$match;
            }
            if ($m204.tag === "None") {
              keep = true;
              break $m204$match;
            }
            $rt.unreachable();
          }
          if (keep) {
            const [$r207, ev$46, ctx$46] = eval_expr({ fuel: $rt.int.sub(fuel, 1, $ob34), ev: ev, ctx: ctx, e: body, env: inner });
            ev = ev$46;
            ctx = ctx$46;
            const b = $rt.unwrap($r207);
            if (!(b.tag === "BoolV")) {
              return [{ tag: "Err", error: panic_at({ detail: "quantifier body must be Bool", span: span }) }, ev, ctx];
            }
            if (quant === "forall" && !$values.bool_of({ c: b })) {
              return [{ tag: "Ok", value: { tag: "BoolV", v: false } }, ev, ctx];
            }
            if (quant === "exists" && $values.bool_of({ c: b })) {
              return [{ tag: "Ok", value: { tag: "BoolV", v: true } }, ev, ctx];
            }
          }
        }
        return [{ tag: "Ok", value: { tag: "BoolV", v: quant === "forall" } }, ev, ctx];
        break $m197$match;
      }
      if (true) {
        return [{ tag: "Err", error: { tag: "NotConstF", reason: "not a quantifier", span: { tag: "None" } } }, ev, ctx];
        break $m197$match;
      }
      $rt.unreachable();
    }
  } catch ($e) {
    if ($e instanceof $rt.EarlyReturn) return [$e.value, ev, ctx];
    throw $e;
  }
}

export function domain_values({ fuel, ev, ctx, d, env, at }) {
  try {
    if (fuel === 0) {
      return [{ tag: "Err", error: { tag: "BudgetF", fn_name: top_name({ ev: ev }) } }, ev, ctx];
    }
    const $m220 = d;
    $m220$match: {
      if ($m220.tag === "RangeDomain") {
        const lo = $m220.lo;
        const hi = $m220.hi;
        const [$r221, ev$47, ctx$47] = eval_expr({ fuel: $rt.int.sub(fuel, 1, $ob35), ev: ev, ctx: ctx, e: lo, env: env });
        ev = ev$47;
        ctx = ctx$47;
        const lv = $rt.unwrap($r221);
        const [$r222, ev$48, ctx$48] = eval_expr({ fuel: $rt.int.sub(fuel, 1, $ob36), ev: ev, ctx: ctx, e: hi, env: env });
        ev = ev$48;
        ctx = ctx$48;
        const hv = $rt.unwrap($r222);
        if (!(lv.tag === "IntV") || !(hv.tag === "IntV")) {
          return [{ tag: "Err", error: panic_at({ detail: "range bounds must be Int", span: at }) }, ev, ctx];
        }
        let out = $std_list.builder({  });
        const lo_i = $values.int_of({ c: lv });
        const hi_i = $values.int_of({ c: hv });
        if (lo_i < hi_i) {
          for (let i = lo_i; i < hi_i; i++) {
            const [, out$49] = $std_list.push({ b: out, x: { tag: "IntV", v: i } });
            out = out$49;
          }
        }
        return [{ tag: "Ok", value: $std_list.finish({ b: out }) }, ev, ctx];
        break $m220$match;
      }
      if ($m220.tag === "InDomain") {
        const expr = $m220.expr;
        const [$r226, ev$50, ctx$50] = eval_expr({ fuel: $rt.int.sub(fuel, 1, $ob37), ev: ev, ctx: ctx, e: expr, env: env });
        ev = ev$50;
        ctx = ctx$50;
        const dv = $rt.unwrap($r226);
        if (dv.tag === "ListV") {
          return [{ tag: "Ok", value: $values.items_of({ c: dv }) }, ev, ctx];
        }
        if (dv.tag === "VariantV") {
          const vname = $context.get_def({ ctx: ctx, id: $values.def_of({ c: dv }) }).name;
          if (vname === "Ok" || vname === "Some") {
            const $m228 = $values.field_of({ fields: $values.fields_of({ c: dv }), name: "value" });
            $m228$match: {
              if ($m228.tag === "Some") {
                const value = $m228.value;
                if (value.tag === "ListV") {
                  return [{ tag: "Ok", value: $values.items_of({ c: value }) }, ev, ctx];
                }
                return [{ tag: "Err", error: not_const({ reason: "quantifier domain is not a list", span: at }) }, ev, ctx];
                break $m228$match;
              }
              if ($m228.tag === "None") {
                return [{ tag: "Err", error: not_const({ reason: "quantifier domain is not a list", span: at }) }, ev, ctx];
                break $m228$match;
              }
              $rt.unreachable();
            }
          }
          return [{ tag: "Ok", value: [] }, ev, ctx];
        }
        return [{ tag: "Err", error: not_const({ reason: "quantifier domain is not a list", span: at }) }, ev, ctx];
        break $m220$match;
      }
      $rt.unreachable();
    }
  } catch ($e) {
    if ($e instanceof $rt.EarlyReturn) return [$e.value, ev, ctx];
    throw $e;
  }
}

export function field_access({ fuel, ev, ctx, e, env }) {
  try {
    if (fuel === 0) {
      return [{ tag: "Err", error: { tag: "BudgetF", fn_name: top_name({ ev: ev }) } }, ev, ctx];
    }
    const $m236 = e;
    $m236$match: {
      if ($m236.tag === "FieldAccess") {
        const object = $m236.object;
        const name = $m236.name;
        const span = $m236.span;
        const res = res_at({ ctx: ctx, k: key({ ev: ev, tag: $defs.tag_expr, span: span }) });
        if (res.tag === "Some") {
          const def = res_def({ res: res });
          if (def >= 0 && res_is_def({ res: res })) {
            const d = $context.get_def({ ctx: ctx, id: def });
            if ($rt.eq(d.kind, { tag: "Const" })) {
              const [$r238, ev$51, ctx$51] = const_of({ fuel: $rt.int.sub(fuel, 1, $ob38), ev: ev, ctx: ctx, def: def });
              ev = ev$51;
              ctx = ctx$51;
              const cv = $r238;
              if (cv.tag === "None") {
                return [{ tag: "Err", error: not_const({ reason: "`" + d.name + "` is not a constant", span: span }) }, ev, ctx];
              }
              return [{ tag: "Ok", value: value_or_unit({ o: cv }) }, ev, ctx];
            }
            return [{ tag: "Err", error: not_const({ reason: "a function is not a value at check time", span: span }) }, ev, ctx];
          }
          if (res_is_unit({ res: res })) {
            return [{ tag: "Ok", value: { tag: "UnitV" } }, ev, ctx];
          }
          const $m244 = res_owner({ res: res });
          $m244$match: {
            if ($m244.tag === "Some") {
              const value = $m244.value;
              return [{ tag: "Ok", value: { tag: "TypeInfoV", owner: value } }, ev, ctx];
              break $m244$match;
            }
            if ($m244.tag === "None") {
              return [{ tag: "Err", error: not_const({ reason: "a function is not a value at check time", span: span }) }, ev, ctx];
              break $m244$match;
            }
            $rt.unreachable();
          }
        }
        const [$r248, ev$52, ctx$52] = eval_expr({ fuel: $rt.int.sub(fuel, 1, $ob39), ev: ev, ctx: ctx, e: object, env: env });
        ev = ev$52;
        ctx = ctx$52;
        const obj = $rt.unwrap($r248);
        if (!(obj.tag === "RecordV")) {
          return [{ tag: "Err", error: not_const({ reason: "field access on a non-record", span: span }) }, ev, ctx];
        }
        const $m250 = $values.field_of({ fields: $values.fields_of({ c: obj }), name: name.text });
        $m250$match: {
          if ($m250.tag === "Some") {
            const value = $m250.value;
            return [{ tag: "Ok", value: value }, ev, ctx];
            break $m250$match;
          }
          if ($m250.tag === "None") {
            return [{ tag: "Err", error: not_const({ reason: "no field `" + name.text + "`", span: span }) }, ev, ctx];
            break $m250$match;
          }
          $rt.unreachable();
        }
        break $m236$match;
      }
      if (true) {
        return [{ tag: "Err", error: { tag: "NotConstF", reason: "not a field access", span: { tag: "None" } } }, ev, ctx];
        break $m236$match;
      }
      $rt.unreachable();
    }
  } catch ($e) {
    if ($e instanceof $rt.EarlyReturn) return [$e.value, ev, ctx];
    throw $e;
  }
}

export function res_is_def({ res }) {
  const $m256 = res;
  $m256$match: {
    if ($m256.tag === "Some") {
      const value = $m256.value;
      return value.tag === "DefRes";
      break $m256$match;
    }
    if ($m256.tag === "None") {
      return false;
      break $m256$match;
    }
    $rt.unreachable();
  }
}

export function res_is_unit({ res }) {
  const $m257 = res;
  $m257$match: {
    if ($m257.tag === "Some") {
      const value = $m257.value;
      return value.tag === "UnitRes";
      break $m257$match;
    }
    if ($m257.tag === "None") {
      return false;
      break $m257$match;
    }
    $rt.unreachable();
  }
}

export function res_owner({ res }) {
  const $m258 = res;
  $m258$match: {
    if ($m258.tag === "None") {
      return { tag: "None" };
      break $m258$match;
    }
    if ($m258.tag === "Some") {
      const value = $m258.value;
      const $m260 = value;
      $m260$match: {
        if ($m260.tag === "TypeValueRes") {
          const owner = $m260.owner;
          return { tag: "Some", value: owner };
          break $m260$match;
        }
        if (true) {
          return { tag: "None" };
          break $m260$match;
        }
        $rt.unreachable();
      }
      break $m258$match;
    }
    $rt.unreachable();
  }
}

export function overflow({ a, b, op, span }) {
  let n = $std_float.of({ x: a }) + $std_float.of({ x: b });
  if (op === "-") {
    n = $std_float.of({ x: a }) - $std_float.of({ x: b });
  }
  if (op === "*") {
    n = $std_float.of({ x: a }) * $std_float.of({ x: b });
  }
  return panic_at({ detail: $std_float.to_text({ x: n }) + " is outside the runtime's safe integer range (±2^53)", span: span });
}

export function int_arith({ op, a, b, span }) {
  if (op === "+") {
    const $m263 = $rt.recover(() => {
  return $rt.int.add(a, b, $ob40);
});
    $m263$match: {
      if ($m263.tag === "Ok") {
        const value = $m263.value;
        return { tag: "Ok", value: value };
        break $m263$match;
      }
      if ($m263.tag === "Err") {
        const error = $m263.error;
        return { tag: "Err", error: overflow({ a: a, b: b, op: op, span: span }) };
        break $m263$match;
      }
      $rt.unreachable();
    }
  }
  if (op === "-") {
    const $m266 = $rt.recover(() => {
  return $rt.int.sub(a, b, $ob41);
});
    $m266$match: {
      if ($m266.tag === "Ok") {
        const value = $m266.value;
        return { tag: "Ok", value: value };
        break $m266$match;
      }
      if ($m266.tag === "Err") {
        const error = $m266.error;
        return { tag: "Err", error: overflow({ a: a, b: b, op: op, span: span }) };
        break $m266$match;
      }
      $rt.unreachable();
    }
  }
  if (op === "*") {
    const $m269 = $rt.recover(() => {
  return $rt.int.mul(a, b, $ob42);
});
    $m269$match: {
      if ($m269.tag === "Ok") {
        const value = $m269.value;
        return { tag: "Ok", value: value };
        break $m269$match;
      }
      if ($m269.tag === "Err") {
        const error = $m269.error;
        return { tag: "Err", error: overflow({ a: a, b: b, op: op, span: span }) };
        break $m269$match;
      }
      $rt.unreachable();
    }
  }
  if (op === "/") {
    if (b === 0) {
      return { tag: "Err", error: panic_at({ detail: "division by zero", span: span }) };
    }
    return { tag: "Ok", value: Math.trunc(a / b) };
  }
  if (b === 0) {
    return { tag: "Err", error: panic_at({ detail: "remainder by zero", span: span }) };
  }
  return { tag: "Ok", value: a % b };
}

export function int_compare({ op, a, b }) {
  if (op === "<") {
    return { tag: "Some", value: a < b };
  }
  if (op === "<=") {
    return { tag: "Some", value: a <= b };
  }
  if (op === ">") {
    return { tag: "Some", value: a > b };
  }
  if (op === ">=") {
    return { tag: "Some", value: a >= b };
  }
  return { tag: "None" };
}

export function float_binary({ op, a, b }) {
  if (op === "+") {
    return { tag: "Some", value: { tag: "FloatV", v: a + b } };
  }
  if (op === "-") {
    return { tag: "Some", value: { tag: "FloatV", v: a - b } };
  }
  if (op === "*") {
    return { tag: "Some", value: { tag: "FloatV", v: a * b } };
  }
  if (op === "/") {
    return { tag: "Some", value: { tag: "FloatV", v: a / b } };
  }
  if (op === "%") {
    return { tag: "Some", value: { tag: "FloatV", v: a % b } };
  }
  if (op === "<") {
    return { tag: "Some", value: { tag: "BoolV", v: a < b } };
  }
  if (op === "<=") {
    return { tag: "Some", value: { tag: "BoolV", v: a <= b } };
  }
  if (op === ">") {
    return { tag: "Some", value: { tag: "BoolV", v: a > b } };
  }
  if (op === ">=") {
    return { tag: "Some", value: { tag: "BoolV", v: a >= b } };
  }
  return { tag: "None" };
}

export function is_arith({ op }) {
  return op === "+" || op === "-" || op === "*" || op === "/" || op === "%";
}

export function binary({ fuel, ev, ctx, e, env }) {
  try {
    if (fuel === 0) {
      return [{ tag: "Err", error: { tag: "BudgetF", fn_name: top_name({ ev: ev }) } }, ev, ctx];
    }
    const $m302 = e;
    $m302$match: {
      if ($m302.tag === "Binary") {
        const op = $m302.op;
        const left = $m302.left;
        const right = $m302.right;
        const span = $m302.span;
        const [$r303, ev$53, ctx$53] = eval_expr({ fuel: $rt.int.sub(fuel, 1, $ob43), ev: ev, ctx: ctx, e: left, env: env });
        ev = ev$53;
        ctx = ctx$53;
        const l = $rt.unwrap($r303);
        const [$r304, ev$54, ctx$54] = eval_expr({ fuel: $rt.int.sub(fuel, 1, $ob44), ev: ev, ctx: ctx, e: right, env: env });
        ev = ev$54;
        ctx = ctx$54;
        const r = $rt.unwrap($r304);
        if (op === "==") {
          return [{ tag: "Ok", value: { tag: "BoolV", v: $values.equals({ a: l, b: r }) } }, ev, ctx];
        }
        if (op === "!=") {
          return [{ tag: "Ok", value: { tag: "BoolV", v: !$values.equals({ a: l, b: r }) } }, ev, ctx];
        }
        if (op === "implies") {
          if (!(l.tag === "BoolV") || !(r.tag === "BoolV")) {
            return [{ tag: "Err", error: panic_at({ detail: "`implies` on non-Bool", span: span }) }, ev, ctx];
          }
          return [{ tag: "Ok", value: { tag: "BoolV", v: !$values.bool_of({ c: l }) || $values.bool_of({ c: r }) } }, ev, ctx];
        }
        if (op === "++") {
          if (l.tag === "TextV" && r.tag === "TextV") {
            return [{ tag: "Ok", value: { tag: "TextV", v: $values.text_of({ c: l }) + $values.text_of({ c: r }) } }, ev, ctx];
          }
          if (l.tag === "ListV" && r.tag === "ListV") {
            return [{ tag: "Ok", value: { tag: "ListV", items: [...$values.items_of({ c: l }), ...$values.items_of({ c: r })] } }, ev, ctx];
          }
          return [{ tag: "Err", error: panic_at({ detail: "`++` on mismatched operands", span: span }) }, ev, ctx];
        }
        const same_int = l.tag === "IntV" && r.tag === "IntV" || l.tag === "DurationV" && r.tag === "DurationV";
        if (same_int) {
          const a = $values.int_of({ c: l });
          const b = $values.int_of({ c: r });
          if (is_arith({ op: op })) {
            const n = $rt.unwrap(int_arith({ op: op, a: a, b: b, span: span }));
            if (l.tag === "DurationV") {
              return [{ tag: "Ok", value: { tag: "DurationV", v: n } }, ev, ctx];
            }
            return [{ tag: "Ok", value: { tag: "IntV", v: n } }, ev, ctx];
          }
          const $m321 = int_compare({ op: op, a: a, b: b });
          $m321$match: {
            if ($m321.tag === "Some") {
              const value = $m321.value;
              return [{ tag: "Ok", value: { tag: "BoolV", v: value } }, ev, ctx];
              break $m321$match;
            }
            if ($m321.tag === "None") {
              skip({  });
              break $m321$match;
            }
            $rt.unreachable();
          }
        }
        if (l.tag === "FloatV" && r.tag === "FloatV") {
          const $m324 = float_binary({ op: op, a: $values.float_of({ c: l }), b: $values.float_of({ c: r }) });
          $m324$match: {
            if ($m324.tag === "Some") {
              const value = $m324.value;
              return [{ tag: "Ok", value: value }, ev, ctx];
              break $m324$match;
            }
            if ($m324.tag === "None") {
              skip({  });
              break $m324$match;
            }
            $rt.unreachable();
          }
        }
        return [{ tag: "Err", error: panic_at({ detail: "`" + op + "` on mismatched operands", span: span }) }, ev, ctx];
        break $m302$match;
      }
      if (true) {
        return [{ tag: "Err", error: { tag: "NotConstF", reason: "not a binary expression", span: { tag: "None" } } }, ev, ctx];
        break $m302$match;
      }
      $rt.unreachable();
    }
  } catch ($e) {
    if ($e instanceof $rt.EarlyReturn) return [$e.value, ev, ctx];
    throw $e;
  }
}

export function call({ fuel, ev, ctx, e, env }) {
  try {
    if (fuel === 0) {
      return [{ tag: "Err", error: { tag: "BudgetF", fn_name: top_name({ ev: ev }) } }, ev, ctx];
    }
    const $m332 = e;
    $m332$match: {
      if ($m332.tag === "Call") {
        const callee = $m332.callee;
        const args = $m332.args;
        const span = $m332.span;
        const callee_span = $parser.span_of_expr({ e: callee });
        const res = res_at({ ctx: ctx, k: key({ ev: ev, tag: $defs.tag_expr, span: callee_span }) });
        const fn_id = res_def({ res: res });
        if (fn_id < 0) {
          return [{ tag: "Err", error: not_const({ reason: "only named functions are called at check time", span: callee_span }) }, ev, ctx];
        }
        const fn_def = $context.get_def({ ctx: ctx, id: fn_id });
        if (!$rt.eq(fn_def.kind, { tag: "Fn" })) {
          return [{ tag: "Err", error: not_const({ reason: "`" + fn_def.name + "` is not a function", span: callee_span }) }, ev, ctx];
        }
        const sig = $rt.unwrap(sig_of({ ctx: ctx, d: fn_def }));
        let arg_values = $std_map.dict({  });
        let inout_vars = $std_map.dict({  });
        for (const a of args) {
          const [$r336, ev$55, ctx$55] = eval_expr({ fuel: $rt.int.sub(fuel, 1, $ob45), ev: ev, ctx: ctx, e: a.value, env: env });
          ev = ev$55;
          ctx = ctx$55;
          const [, arg_values$56] = $std_map.set({ d: arg_values, key: a.name.text, value: $rt.unwrap($r336) });
          arg_values = arg_values$56;
          if (a.is_inout && a.value.tag === "Name") {
            const target = res_def({ res: res_at({ ctx: ctx, k: key({ ev: ev, tag: $defs.tag_expr, span: $parser.span_of_expr({ e: a.value }) }) }) });
            if (target >= 0) {
              const [, inout_vars$57] = $std_map.set({ d: inout_vars, key: a.name.text, value: target });
              inout_vars = inout_vars$57;
            }
          }
        }
        let subst = $std_map.dict({  });
        let targs = [];
        const $m337 = $std_map.find({ d: ctx.instantiations, key: key({ ev: ev, tag: $defs.tag_expr, span: span }) });
        $m337$match: {
          if ($m337.tag === "Some") {
            const value = $m337.value;
            targs = value;
            break $m337$match;
          }
          if ($m337.tag === "None") {
            targs = [];
            break $m337$match;
          }
          $rt.unreachable();
        }
        const $hi338 = $std_list.len({ xs: sig.tparams });
        for (let i = 0; i < $hi338; i++) {
          if (i < $std_list.len({ xs: targs })) {
            const [, subst$58] = $std_map.set({ d: subst, key: $types.tparam_def({ p: $std_list.get({ xs: sig.tparams, i: i }) }), value: $std_list.get({ xs: targs, i: i }) });
            subst = subst$58;
          }
        }
        const [$r340, ev$59, ctx$59] = call_fn({ fuel: $rt.int.sub(fuel, 1, $ob46), ev: ev, ctx: ctx, d: fn_def, sig: sig, args: arg_values, subst: subst, span: { tag: "Some", value: span } });
        ev = ev$59;
        ctx = ctx$59;
        const r = $rt.unwrap($r340);
        for (const name of $std_map.keys({ d: inout_vars })) {
          const $m341 = $std_map.find({ d: r.inouts, key: name });
          $m341$match: {
            if ($m341.tag === "Some") {
              const value = $m341.value;
              env_set({ env: env, k: or_neg({ o: $std_map.find({ d: inout_vars, key: name }) }), v: value });
              break $m341$match;
            }
            if ($m341.tag === "None") {
              skip({  });
              break $m341$match;
            }
            $rt.unreachable();
          }
        }
        return [{ tag: "Ok", value: r.value }, ev, ctx];
        break $m332$match;
      }
      if (true) {
        return [{ tag: "Err", error: { tag: "NotConstF", reason: "not a call", span: { tag: "None" } } }, ev, ctx];
        break $m332$match;
      }
      $rt.unreachable();
    }
  } catch ($e) {
    if ($e instanceof $rt.EarlyReturn) return [$e.value, ev, ctx];
    throw $e;
  }
}

export function sig_of({ ctx, d }) {
  const $m346 = $std_map.find({ d: ctx.signatures, key: d.id });
  $m346$match: {
    if ($m346.tag === "Some") {
      const value = $m346.value;
      return { tag: "Ok", value: value };
      break $m346$match;
    }
    if ($m346.tag === "None") {
      return { tag: "Err", error: { tag: "NotConstF", reason: "`" + d.name + "` has no signature", span: { tag: "None" } } };
      break $m346$match;
    }
    $rt.unreachable();
  }
}

export function call_fn({ fuel, ev, ctx, d, sig, args, subst, span }) {
  try {
    if (fuel === 0) {
      return [{ tag: "Err", error: { tag: "BudgetF", fn_name: top_name({ ev: ev }) } }, ev, ctx];
    }
    const [$r353, ev$60] = step({ ev: ev });
    ev = ev$60;
    $rt.unwrap($r353);
    for (const eff of $effectset.values({ s: sig.effects })) {
      if (!$rt.eq(eff, { tag: "PrimEffect", name: "alloc" })) {
        return [{ tag: "Err", error: { tag: "NotConstF", reason: "`" + d.name + "` has effects and cannot run at check time", span: span } }, ev, ctx];
      }
    }
    let env = $std_map.dict({  });
    for (const p of sig.tparams) {
      if (p.tag === "ConstP") {
        const pdef = $types.tparam_def({ p: p });
        let bound = { tag: "None" };
        const $m358 = $std_map.find({ d: subst, key: pdef });
        $m358$match: {
          if ($m358.tag === "Some") {
            const value = $m358.value;
            if (value.tag === "ConstA") {
              bound = $values.of_const({ c: $types.arg_const({ a: value }) });
            }
            break $m358$match;
          }
          if ($m358.tag === "None") {
            bound = { tag: "None" };
            break $m358$match;
          }
          $rt.unreachable();
        }
        const $m360 = bound;
        $m360$match: {
          if ($m360.tag === "Some") {
            const value = $m360.value;
            const [, env$61] = $std_map.set({ d: env, key: pdef, value: value });
            env = env$61;
            break $m360$match;
          }
          if ($m360.tag === "None") {
            return [{ tag: "Err", error: { tag: "NotConstF", reason: "type index `" + $context.get_def({ ctx: ctx, id: pdef }).name + "` of `" + d.name + "` is not known at check time", span: span } }, ev, ctx];
            break $m360$match;
          }
          $rt.unreachable();
        }
      }
    }
    const $hi363 = $std_list.len({ xs: sig.params });
    for (let i = 0; i < $hi363; i++) {
      const p = $std_list.get({ xs: sig.params, i: i });
      if (i >= $std_list.len({ xs: sig.param_defs })) {
        return [{ tag: "Err", error: { tag: "NotConstF", reason: "missing argument `" + p.name + "`", span: span } }, ev, ctx];
      }
      const $m366 = $std_map.find({ d: args, key: p.name });
      $m366$match: {
        if ($m366.tag === "Some") {
          const value = $m366.value;
          const [, env$62] = $std_map.set({ d: env, key: $std_list.get({ xs: sig.param_defs, i: i }), value: value });
          env = env$62;
          break $m366$match;
        }
        if ($m366.tag === "None") {
          return [{ tag: "Err", error: { tag: "NotConstF", reason: "missing argument `" + p.name + "`", span: span } }, ev, ctx];
          break $m366$match;
        }
        $rt.unreachable();
      }
    }
    const saved_file = ev.file;
    ev = { ...ev, frames: $std_list.append({ xs: ev.frames, x: { name: d.name, ret: { tag: "Some", value: sig.ret } } }), file: d.file };
    const [$r372, ev$63, ctx$63] = call_body({ fuel: $rt.int.sub(fuel, 1, $ob47), ev: ev, ctx: ctx, d: d, sig: sig, env: env, subst: subst, span: span });
    ev = ev$63;
    ctx = ctx$63;
    const r = $r372;
    const n = $std_list.len({ xs: ev.frames });
    if (n > 0) {
      ev = { ...ev, frames: $std_list.slice({ xs: ev.frames, from: 0, to: $rt.int.sub(n, 1, $ob48) }) };
    }
    ev = { ...ev, file: saved_file };
    return [r, ev, ctx];
  } catch ($e) {
    if ($e instanceof $rt.EarlyReturn) return [$e.value, ev, ctx];
    throw $e;
  }
}

export function block_or_empty({ o }) {
  const $m375 = o;
  $m375$match: {
    if ($m375.tag === "Some") {
      const value = $m375.value;
      return value;
      break $m375$match;
    }
    if ($m375.tag === "None") {
      return { stmts: [], elided: false, span: { start: 0, end: 0 } };
      break $m375$match;
    }
    $rt.unreachable();
  }
}

export function body_of({ d }) {
  const $m378 = d.node;
  $m378$match: {
    if ($m378.tag === "FnNode") {
      const decl = $m378.decl;
      return decl.body;
      break $m378$match;
    }
    if (true) {
      return { tag: "None" };
      break $m378$match;
    }
    $rt.unreachable();
  }
}

export function call_body({ fuel, ev, ctx, d, sig, env, subst, span }) {
  try {
    if (fuel === 0) {
      return [{ tag: "Err", error: { tag: "BudgetF", fn_name: top_name({ ev: ev }) } }, ev, ctx];
    }
    for (const c of sig.contracts) {
      if (c.clause === "requires") {
        const [$r382, ev$64, ctx$64] = eval_expr({ fuel: $rt.int.sub(fuel, 1, $ob49), ev: ev, ctx: ctx, e: c.expr, env: env });
        ev = ev$64;
        ctx = ctx$64;
        const ok = $rt.unwrap($r382);
        if (!(ok.tag === "BoolV") || !$values.bool_of({ c: ok })) {
          return [{ tag: "Err", error: { tag: "PanicF", detail: "`requires " + $printer.print_expr({ e: c.expr }) + "` of `" + d.name + "` does not hold", span: span } }, ev, ctx];
        }
      }
    }
    if (sig.is_intrinsic) {
      const [$r385, ctx$65] = intrinsic({ ctx: ctx, d: d, sig: sig, env: env, subst: subst, span: span });
      ctx = ctx$65;
      const v = $rt.unwrap($r385);
      return [{ tag: "Ok", value: { value: v, inouts: $std_map.dict({  }) } }, ev, ctx];
    }
    const body = body_of({ d: d });
    if (body.tag === "None") {
      return [{ tag: "Err", error: { tag: "NotConstF", reason: "`" + d.name + "` has no body", span: span } }, ev, ctx];
    }
    let out = { tag: "UnitV" };
    const [$r392, ev$66, ctx$66] = exec_block({ fuel: $rt.int.sub(fuel, 1, $ob50), ev: ev, ctx: ctx, b: block_or_empty({ o: body }), env: env });
    ev = ev$66;
    ctx = ctx$66;
    const $m391 = $r392;
    $m391$match: {
      if ($m391.tag === "Ok") {
        out = { tag: "UnitV" };
        break $m391$match;
      }
      if ($m391.tag === "Err") {
        const error = $m391.error;
        const $m394 = error;
        $m394$match: {
          if ($m394.tag === "ReturnF") {
            const value = $m394.value;
            out = value;
            break $m394$match;
          }
          if (true) {
            return [{ tag: "Err", error: error }, ev, ctx];
            break $m394$match;
          }
          $rt.unreachable();
        }
        break $m391$match;
      }
      $rt.unreachable();
    }
    let inouts = $std_map.dict({  });
    const $hi396 = $std_list.len({ xs: sig.params });
    for (let i = 0; i < $hi396; i++) {
      const p = $std_list.get({ xs: sig.params, i: i });
      if (p.is_inout && i < $std_list.len({ xs: sig.param_defs })) {
        const $m397 = $std_map.find({ d: env, key: $std_list.get({ xs: sig.param_defs, i: i }) });
        $m397$match: {
          if ($m397.tag === "Some") {
            const value = $m397.value;
            const [, inouts$67] = $std_map.set({ d: inouts, key: p.name, value: value });
            inouts = inouts$67;
            break $m397$match;
          }
          if ($m397.tag === "None") {
            skip({  });
            break $m397$match;
          }
          $rt.unreachable();
        }
      }
    }
    return [{ tag: "Ok", value: { value: out, inouts: inouts } }, ev, ctx];
  } catch ($e) {
    if ($e instanceof $rt.EarlyReturn) return [$e.value, ev, ctx];
    throw $e;
  }
}

export const implemented = ["std.int.to_text", "std.int.floor", "std.int.parse", "std.float.of", "std.float.classify", "std.float.to_text", "std.float.parse", "std.bool.to_text", "std.text.len", "std.text.graphemes", "std.text.bytes", "std.text.starts_with", "std.text.lower", "std.text.trim", "std.text.count", "std.text.code_points", "std.text.of_code_points", "std.text.of_code_point", "std.text.slice", "std.text.index_of", "std.text.contains", "std.text.ends_with", "std.text.split", "std.text.join", "std.text.repeat", "std.text.replace", "std.text.compare", "std.text.upper", "std.list.len", "std.list.get", "std.list.replicate", "std.list.append", "std.list.slice", "std.bytes.len", "std.bytes.get", "std.duration.nanos", "std.duration.of_millis"];

export function has_impl({ qualified }) {
  return $std_list.contains({ xs: implemented, x: qualified }) || $std_text.starts_with({ t: qualified, prefix: "std.typeinfo." });
}

export function intrinsic({ ctx, d, sig, env, subst, span }) {
  const qualified = $context.qualified_name({ ctx: ctx, id: d.id });
  if (!sig.is_const_fn || !has_impl({ qualified: qualified })) {
    return [{ tag: "Err", error: { tag: "NotConstF", reason: "`" + qualified + "` is not evaluated at check time", span: span } }, ctx];
  }
  let args = $std_list.builder({  });
  for (const pd of sig.param_defs) {
    const $m402 = $std_map.find({ d: env, key: pd });
    $m402$match: {
      if ($m402.tag === "Some") {
        const value = $m402.value;
        const [, args$68] = $std_list.push({ b: args, x: value });
        args = args$68;
        break $m402$match;
      }
      if ($m402.tag === "None") {
        const [, args$69] = $std_list.push({ b: args, x: { tag: "UnitV" } });
        args = args$69;
        break $m402$match;
      }
      $rt.unreachable();
    }
  }
  const arg_list = $std_list.finish({ b: args });
  if ($std_text.starts_with({ t: qualified, prefix: "std.typeinfo." })) {
    const [$r404, ctx$70] = typeinfo_intrinsic({ ctx: ctx, qualified: qualified, args: arg_list, span: span });
    ctx = ctx$70;
    return [$r404, ctx];
  }
  const [$r405, ctx$71] = call_impl({ ctx: ctx, qualified: qualified, args: arg_list, span: span });
  ctx = ctx$71;
  return [$r405, ctx];
}

export function typeinfo_intrinsic({ ctx, qualified, args, span }) {
  if ($std_list.len({ xs: args }) === 0 || !($std_list.get({ xs: args, i: 0 }).tag === "TypeInfoV")) {
    return [{ tag: "Err", error: { tag: "NotConstF", reason: "TypeInfo intrinsic without a type", span: span } }, ctx];
  }
  const owner = $values.owner_of({ c: $std_list.get({ xs: args, i: 0 }) });
  if (qualified === "std.typeinfo.name") {
    return [{ tag: "Ok", value: { tag: "TextV", v: owner_name({ ctx: ctx, o: owner }) } }, ctx];
  }
  if (qualified === "std.typeinfo.fields") {
    const [$r410, ctx$72] = std_type({ ctx: ctx, module_name: "std.typeinfo", type_name: "Field" });
    ctx = ctx$72;
    const field_def = $r410;
    if (field_def < 0) {
      return [{ tag: "Err", error: { tag: "NotConstF", reason: "std.typeinfo.Field is not available", span: span } }, ctx];
    }
    let items = $std_list.builder({  });
    const $m413 = owner;
    $m413$match: {
      if ($m413.tag === "DefOwner") {
        const def = $m413.def;
        const $m414 = $std_map.find({ d: ctx.fields, key: def });
        $m414$match: {
          if ($m414.tag === "Some") {
            const value = $m414.value;
            for (const f of value) {
              const [, items$73] = $std_list.push({ b: items, x: { tag: "RecordV", def: field_def, fields: [{ name: "name", value: { tag: "TextV", v: f.name } }, { name: "type_name", value: { tag: "TextV", v: $typecheck.show({ ctx: ctx, t: f.ty }) } }] } });
              items = items$73;
            }
            break $m414$match;
          }
          if ($m414.tag === "None") {
            skip({  });
            break $m414$match;
          }
          $rt.unreachable();
        }
        break $m413$match;
      }
      if (true) {
        skip({  });
        break $m413$match;
      }
      $rt.unreachable();
    }
    return [{ tag: "Ok", value: { tag: "ListV", items: $std_list.finish({ b: items }) } }, ctx];
  }
  return [{ tag: "Err", error: { tag: "NotConstF", reason: "`" + qualified + "` is not evaluated at check time", span: span } }, ctx];
}

export function conversion({ what, span }) {
  return { tag: "NotConstF", reason: "expected " + what, span: span };
}

export function arg_at({ args, i, span }) {
  if (i < 0 || i >= $std_list.len({ xs: args })) {
    return { tag: "Err", error: conversion({ what: "an argument", span: span }) };
  }
  return { tag: "Ok", value: $std_list.get({ xs: args, i: i }) };
}

export function arg_text({ args, i, span }) {
  try {
    const v = $rt.unwrap(arg_at({ args: args, i: i, span: span }));
    if (!(v.tag === "TextV")) {
      return { tag: "Err", error: conversion({ what: "Text", span: span }) };
    }
    return { tag: "Ok", value: $values.text_of({ c: v }) };
  } catch ($e) {
    if ($e instanceof $rt.EarlyReturn) return $e.value;
    throw $e;
  }
}

export function arg_int({ args, i, span }) {
  try {
    const v = $rt.unwrap(arg_at({ args: args, i: i, span: span }));
    if (!(v.tag === "IntV") && !(v.tag === "DurationV")) {
      return { tag: "Err", error: conversion({ what: "a number", span: span }) };
    }
    return { tag: "Ok", value: $values.int_of({ c: v }) };
  } catch ($e) {
    if ($e instanceof $rt.EarlyReturn) return $e.value;
    throw $e;
  }
}

export function arg_float({ args, i, span }) {
  try {
    const v = $rt.unwrap(arg_at({ args: args, i: i, span: span }));
    if (!(v.tag === "FloatV")) {
      return { tag: "Err", error: conversion({ what: "a number", span: span }) };
    }
    return { tag: "Ok", value: $values.float_of({ c: v }) };
  } catch ($e) {
    if ($e instanceof $rt.EarlyReturn) return $e.value;
    throw $e;
  }
}

export function arg_bool({ args, i, span }) {
  try {
    const v = $rt.unwrap(arg_at({ args: args, i: i, span: span }));
    if (!(v.tag === "BoolV")) {
      return { tag: "Err", error: conversion({ what: "Bool", span: span }) };
    }
    return { tag: "Ok", value: $values.bool_of({ c: v }) };
  } catch ($e) {
    if ($e instanceof $rt.EarlyReturn) return $e.value;
    throw $e;
  }
}

export function arg_list({ args, i, span }) {
  try {
    const v = $rt.unwrap(arg_at({ args: args, i: i, span: span }));
    if (!(v.tag === "ListV")) {
      return { tag: "Err", error: conversion({ what: "a List", span: span }) };
    }
    return { tag: "Ok", value: $values.items_of({ c: v }) };
  } catch ($e) {
    if ($e instanceof $rt.EarlyReturn) return $e.value;
    throw $e;
  }
}

export function arg_bytes({ args, i, span }) {
  try {
    const v = $rt.unwrap(arg_at({ args: args, i: i, span: span }));
    if (!(v.tag === "BytesV")) {
      return { tag: "Err", error: conversion({ what: "Bytes", span: span }) };
    }
    return { tag: "Ok", value: $values.bytes_of({ c: v }) };
  } catch ($e) {
    if ($e instanceof $rt.EarlyReturn) return $e.value;
    throw $e;
  }
}

export function texts_of({ items, span }) {
  let out = $std_list.builder({  });
  for (const x of items) {
    if (!(x.tag === "TextV")) {
      return { tag: "Err", error: conversion({ what: "Text", span: span }) };
    }
    const [, out$74] = $std_list.push({ b: out, x: $values.text_of({ c: x }) });
    out = out$74;
  }
  return { tag: "Ok", value: $std_list.finish({ b: out }) };
}

export function text_list({ items }) {
  let out = $std_list.builder({  });
  for (const t of items) {
    const [, out$75] = $std_list.push({ b: out, x: { tag: "TextV", v: t } });
    out = out$75;
  }
  return { tag: "ListV", items: $std_list.finish({ b: out }) };
}

export function int_list({ items }) {
  let out = $std_list.builder({  });
  for (const n of items) {
    const [, out$76] = $std_list.push({ b: out, x: { tag: "IntV", v: n } });
    out = out$76;
  }
  return { tag: "ListV", items: $std_list.finish({ b: out }) };
}

export function code_points_of({ items, span }) {
  let out = $std_list.builder({  });
  for (const x of items) {
    if (!(x.tag === "IntV")) {
      return { tag: "Err", error: conversion({ what: "a number", span: span }) };
    }
    const cp = $values.int_of({ c: x });
    if (cp < 0 || cp > 1114111) {
      return { tag: "Err", error: { tag: "PanicF", detail: "refinement `0 <= it and it <= 1114111` of std.text.of_code_points: " + $std_int.to_text({ x: cp }) + " is not a code point", span: span } };
    }
    const [, out$77] = $std_list.push({ b: out, x: cp });
    out = out$77;
  }
  return { tag: "Ok", value: $std_list.finish({ b: out }) };
}

export function bytes_value({ b }) {
  let out = $std_list.builder({  });
  const $hi449 = $std_bytes.len({ b: b });
  for (let i = 0; i < $hi449; i++) {
    const [, out$78] = $std_list.push({ b: out, x: $std_bytes.get({ b: b, i: i }) });
    out = out$78;
  }
  return { tag: "BytesV", bytes: $std_list.finish({ b: out }) };
}

export function precondition({ what, span }) {
  return { tag: "PanicF", detail: what, span: span };
}

export function call_impl({ ctx, qualified, args, span }) {
  try {
    if (qualified === "std.int.to_text") {
      return [{ tag: "Ok", value: { tag: "TextV", v: $std_int.to_text({ x: $rt.unwrap(arg_int({ args: args, i: 0, span: span })) }) } }, ctx];
    }
    if (qualified === "std.int.floor") {
      const $m454 = $std_int.floor({ x: $rt.unwrap(arg_float({ args: args, i: 0, span: span })) });
      $m454$match: {
        if ($m454.tag === "Ok") {
          const value = $m454.value;
          const [$r457, ctx$79] = variant_named({ ctx: ctx, module_name: "std.results", union_name: "Result", variant_name: "Ok", fields: [{ name: "value", value: { tag: "IntV", v: value } }] });
          ctx = ctx$79;
          return [$r457, ctx];
          break $m454$match;
        }
        if ($m454.tag === "Err") {
          const error = $m454.error;
          let vname = "OutOfRange";
          if (error.tag === "NotFinite") {
            vname = "NotFinite";
          }
          const [$r458, ctx$80] = variant_named({ ctx: ctx, module_name: "std.int", union_name: "RangeError", variant_name: vname, fields: [] });
          ctx = ctx$80;
          const err = $rt.unwrap($r458);
          const [$r460, ctx$81] = variant_named({ ctx: ctx, module_name: "std.results", union_name: "Result", variant_name: "Err", fields: [{ name: "error", value: err }] });
          ctx = ctx$81;
          return [$r460, ctx];
          break $m454$match;
        }
        $rt.unreachable();
      }
    }
    if (qualified === "std.int.parse") {
      const $m461 = $std_int.parse({ t: $rt.unwrap(arg_text({ args: args, i: 0, span: span })) });
      $m461$match: {
        if ($m461.tag === "Some") {
          const value = $m461.value;
          const [$r464, ctx$82] = option_value({ ctx: ctx, o: { tag: "Some", value: { tag: "IntV", v: value } } });
          ctx = ctx$82;
          return [$r464, ctx];
          break $m461$match;
        }
        if ($m461.tag === "None") {
          const [$r466, ctx$83] = option_value({ ctx: ctx, o: { tag: "None" } });
          ctx = ctx$83;
          return [$r466, ctx];
          break $m461$match;
        }
        $rt.unreachable();
      }
    }
    if (qualified === "std.float.of") {
      return [{ tag: "Ok", value: { tag: "FloatV", v: $std_float.of({ x: $rt.unwrap(arg_int({ args: args, i: 0, span: span })) }) } }, ctx];
    }
    if (qualified === "std.float.classify") {
      let vname = "Finite";
      const $m469 = $std_float.classify({ x: $rt.unwrap(arg_float({ args: args, i: 0, span: span })) });
      $m469$match: {
        if ($m469.tag === "Finite") {
          vname = "Finite";
          break $m469$match;
        }
        if ($m469.tag === "Infinite") {
          vname = "Infinite";
          break $m469$match;
        }
        if ($m469.tag === "NotANumber") {
          vname = "NotANumber";
          break $m469$match;
        }
        $rt.unreachable();
      }
      const [$r470, ctx$84] = variant_named({ ctx: ctx, module_name: "std.float", union_name: "Class", variant_name: vname, fields: [] });
      ctx = ctx$84;
      return [$r470, ctx];
    }
    if (qualified === "std.float.to_text") {
      return [{ tag: "Ok", value: { tag: "TextV", v: $std_float.to_text({ x: $rt.unwrap(arg_float({ args: args, i: 0, span: span })) }) } }, ctx];
    }
    if (qualified === "std.float.parse") {
      const $m473 = $std_float.parse({ t: $rt.unwrap(arg_text({ args: args, i: 0, span: span })) });
      $m473$match: {
        if ($m473.tag === "Some") {
          const value = $m473.value;
          const [$r476, ctx$85] = option_value({ ctx: ctx, o: { tag: "Some", value: { tag: "FloatV", v: value } } });
          ctx = ctx$85;
          return [$r476, ctx];
          break $m473$match;
        }
        if ($m473.tag === "None") {
          const [$r478, ctx$86] = option_value({ ctx: ctx, o: { tag: "None" } });
          ctx = ctx$86;
          return [$r478, ctx];
          break $m473$match;
        }
        $rt.unreachable();
      }
    }
    if (qualified === "std.bool.to_text") {
      return [{ tag: "Ok", value: { tag: "TextV", v: $std_bool.to_text({ b: $rt.unwrap(arg_bool({ args: args, i: 0, span: span })) }) } }, ctx];
    }
    if (qualified === "std.text.len") {
      return [{ tag: "Ok", value: { tag: "IntV", v: $std_text.len({ t: $rt.unwrap(arg_text({ args: args, i: 0, span: span })) }) } }, ctx];
    }
    if (qualified === "std.text.graphemes") {
      return [{ tag: "Ok", value: text_list({ items: $std_text.graphemes({ t: $rt.unwrap(arg_text({ args: args, i: 0, span: span })) }) }) }, ctx];
    }
    if (qualified === "std.text.bytes") {
      return [{ tag: "Ok", value: bytes_value({ b: $std_text.bytes({ t: $rt.unwrap(arg_text({ args: args, i: 0, span: span })) }) }) }, ctx];
    }
    if (qualified === "std.text.starts_with") {
      return [{ tag: "Ok", value: { tag: "BoolV", v: $std_text.starts_with({ t: $rt.unwrap(arg_text({ args: args, i: 0, span: span })), prefix: $rt.unwrap(arg_text({ args: args, i: 1, span: span })) }) } }, ctx];
    }
    if (qualified === "std.text.lower") {
      return [{ tag: "Ok", value: { tag: "TextV", v: $std_text.lower({ t: $rt.unwrap(arg_text({ args: args, i: 0, span: span })) }) } }, ctx];
    }
    if (qualified === "std.text.trim") {
      return [{ tag: "Ok", value: { tag: "TextV", v: $std_text.trim({ t: $rt.unwrap(arg_text({ args: args, i: 0, span: span })) }) } }, ctx];
    }
    if (qualified === "std.text.count") {
      return [{ tag: "Ok", value: { tag: "IntV", v: $std_text.count({ t: $rt.unwrap(arg_text({ args: args, i: 0, span: span })) }) } }, ctx];
    }
    if (qualified === "std.text.code_points") {
      return [{ tag: "Ok", value: int_list({ items: $std_text.code_points({ t: $rt.unwrap(arg_text({ args: args, i: 0, span: span })) }) }) }, ctx];
    }
    if (qualified === "std.text.of_code_points") {
      return [{ tag: "Ok", value: { tag: "TextV", v: $std_text.of_code_points({ cps: $rt.unwrap(code_points_of({ items: $rt.unwrap(arg_list({ args: args, i: 0, span: span })), span: span })) }) } }, ctx];
    }
    if (qualified === "std.text.of_code_point") {
      const cp = $rt.unwrap(arg_int({ args: args, i: 0, span: span }));
      if (cp < 0 || cp > 1114111) {
        return [{ tag: "Err", error: precondition({ what: "refinement `0 <= it and it <= 1114111` of std.text.of_code_point: " + $std_int.to_text({ x: cp }) + " is not a code point", span: span }) }, ctx];
      }
      return [{ tag: "Ok", value: { tag: "TextV", v: $std_text.of_code_point({ cp: cp }) } }, ctx];
    }
    if (qualified === "std.text.slice") {
      const t = $rt.unwrap(arg_text({ args: args, i: 0, span: span }));
      const from = $rt.unwrap(arg_int({ args: args, i: 1, span: span }));
      const to = $rt.unwrap(arg_int({ args: args, i: 2, span: span }));
      if (from < 0 || to < from || to > $std_text.count({ t: t })) {
        return [{ tag: "Err", error: precondition({ what: "refinement of std.text.slice: " + $std_int.to_text({ x: from }) + " ..< " + $std_int.to_text({ x: to }) + " is outside the text", span: span }) }, ctx];
      }
      return [{ tag: "Ok", value: { tag: "TextV", v: $std_text.slice({ t: t, from: from, to: to }) } }, ctx];
    }
    if (qualified === "std.text.index_of") {
      const t = $rt.unwrap(arg_text({ args: args, i: 0, span: span }));
      const needle = $rt.unwrap(arg_text({ args: args, i: 1, span: span }));
      const from = $rt.unwrap(arg_int({ args: args, i: 2, span: span }));
      if (from < 0 || from > $std_text.count({ t: t })) {
        return [{ tag: "Err", error: precondition({ what: "refinement of std.text.index_of: " + $std_int.to_text({ x: from }) + " is outside the text", span: span }) }, ctx];
      }
      const $m503 = $std_text.index_of({ t: t, needle: needle, from: from });
      $m503$match: {
        if ($m503.tag === "Some") {
          const value = $m503.value;
          const [$r506, ctx$87] = option_value({ ctx: ctx, o: { tag: "Some", value: { tag: "IntV", v: value } } });
          ctx = ctx$87;
          return [$r506, ctx];
          break $m503$match;
        }
        if ($m503.tag === "None") {
          const [$r508, ctx$88] = option_value({ ctx: ctx, o: { tag: "None" } });
          ctx = ctx$88;
          return [$r508, ctx];
          break $m503$match;
        }
        $rt.unreachable();
      }
    }
    if (qualified === "std.text.contains") {
      return [{ tag: "Ok", value: { tag: "BoolV", v: $std_text.contains({ t: $rt.unwrap(arg_text({ args: args, i: 0, span: span })), needle: $rt.unwrap(arg_text({ args: args, i: 1, span: span })) }) } }, ctx];
    }
    if (qualified === "std.text.ends_with") {
      return [{ tag: "Ok", value: { tag: "BoolV", v: $std_text.ends_with({ t: $rt.unwrap(arg_text({ args: args, i: 0, span: span })), suffix: $rt.unwrap(arg_text({ args: args, i: 1, span: span })) }) } }, ctx];
    }
    if (qualified === "std.text.split") {
      return [{ tag: "Ok", value: text_list({ items: $std_text.split({ t: $rt.unwrap(arg_text({ args: args, i: 0, span: span })), sep: $rt.unwrap(arg_text({ args: args, i: 1, span: span })) }) }) }, ctx];
    }
    if (qualified === "std.text.join") {
      return [{ tag: "Ok", value: { tag: "TextV", v: $std_text.join({ parts: $rt.unwrap(texts_of({ items: $rt.unwrap(arg_list({ args: args, i: 0, span: span })), span: span })), sep: $rt.unwrap(arg_text({ args: args, i: 1, span: span })) }) } }, ctx];
    }
    if (qualified === "std.text.repeat") {
      const n = $rt.unwrap(arg_int({ args: args, i: 1, span: span }));
      if (n < 0) {
        return [{ tag: "Err", error: precondition({ what: "refinement `it >= 0` of std.text.repeat: " + $std_int.to_text({ x: n }), span: span }) }, ctx];
      }
      return [{ tag: "Ok", value: { tag: "TextV", v: $std_text.repeat({ t: $rt.unwrap(arg_text({ args: args, i: 0, span: span })), n: n }) } }, ctx];
    }
    if (qualified === "std.text.replace") {
      return [{ tag: "Ok", value: { tag: "TextV", v: $std_text.replace({ t: $rt.unwrap(arg_text({ args: args, i: 0, span: span })), from: $rt.unwrap(arg_text({ args: args, i: 1, span: span })), to: $rt.unwrap(arg_text({ args: args, i: 2, span: span })) }) } }, ctx];
    }
    if (qualified === "std.text.compare") {
      return [{ tag: "Ok", value: { tag: "IntV", v: $std_text.compare({ a: $rt.unwrap(arg_text({ args: args, i: 0, span: span })), b: $rt.unwrap(arg_text({ args: args, i: 1, span: span })) }) } }, ctx];
    }
    if (qualified === "std.text.upper") {
      return [{ tag: "Ok", value: { tag: "TextV", v: $std_text.upper({ t: $rt.unwrap(arg_text({ args: args, i: 0, span: span })) }) } }, ctx];
    }
    if (qualified === "std.list.len") {
      return [{ tag: "Ok", value: { tag: "IntV", v: $std_list.len({ xs: $rt.unwrap(arg_list({ args: args, i: 0, span: span })) }) } }, ctx];
    }
    if (qualified === "std.list.get") {
      const xs = $rt.unwrap(arg_list({ args: args, i: 0, span: span }));
      const i = $rt.unwrap(arg_int({ args: args, i: 1, span: span }));
      if (i < 0 || i >= $std_list.len({ xs: xs })) {
        return [{ tag: "Err", error: precondition({ what: "requires `0 <= i and i < len(xs: xs)` of get: index " + $std_int.to_text({ x: i }) + " is outside 0 ..< " + $std_int.to_text({ x: $std_list.len({ xs: xs }) }), span: span }) }, ctx];
      }
      return [{ tag: "Ok", value: $std_list.get({ xs: xs, i: i }) }, ctx];
    }
    if (qualified === "std.list.replicate") {
      const n = $rt.unwrap(arg_int({ args: args, i: 1, span: span }));
      if (n < 0) {
        return [{ tag: "Err", error: precondition({ what: "requires `count >= 0` of replicate: count " + $std_int.to_text({ x: n }) + " is negative", span: span }) }, ctx];
      }
      return [{ tag: "Ok", value: { tag: "ListV", items: $std_list.replicate({ value: $rt.unwrap(arg_at({ args: args, i: 0, span: span })), count: n }) } }, ctx];
    }
    if (qualified === "std.list.append") {
      return [{ tag: "Ok", value: { tag: "ListV", items: $std_list.append({ xs: $rt.unwrap(arg_list({ args: args, i: 0, span: span })), x: $rt.unwrap(arg_at({ args: args, i: 1, span: span })) }) } }, ctx];
    }
    if (qualified === "std.list.slice") {
      const xs = $rt.unwrap(arg_list({ args: args, i: 0, span: span }));
      const from = $rt.unwrap(arg_int({ args: args, i: 1, span: span }));
      const to = $rt.unwrap(arg_int({ args: args, i: 2, span: span }));
      if (from < 0 || to < from || to > $std_list.len({ xs: xs })) {
        return [{ tag: "Err", error: precondition({ what: "requires `0 <= from and from <= to and to <= len(xs: xs)` of slice: " + $std_int.to_text({ x: from }) + " ..< " + $std_int.to_text({ x: to }) + " is outside 0 ..< " + $std_int.to_text({ x: $std_list.len({ xs: xs }) }), span: span }) }, ctx];
      }
      return [{ tag: "Ok", value: { tag: "ListV", items: $std_list.slice({ xs: xs, from: from, to: to }) } }, ctx];
    }
    if (qualified === "std.bytes.len") {
      return [{ tag: "Ok", value: { tag: "IntV", v: $std_list.len({ xs: $rt.unwrap(arg_bytes({ args: args, i: 0, span: span })) }) } }, ctx];
    }
    if (qualified === "std.bytes.get") {
      const bs = $rt.unwrap(arg_bytes({ args: args, i: 0, span: span }));
      const i = $rt.unwrap(arg_int({ args: args, i: 1, span: span }));
      if (i < 0 || i >= $std_list.len({ xs: bs })) {
        return [{ tag: "Err", error: precondition({ what: "requires `0 <= i and i < len(b: b)` of get: index " + $std_int.to_text({ x: i }) + " is outside 0 ..< " + $std_int.to_text({ x: $std_list.len({ xs: bs }) }), span: span }) }, ctx];
      }
      return [{ tag: "Ok", value: { tag: "IntV", v: $std_list.get({ xs: bs, i: i }) } }, ctx];
    }
    if (qualified === "std.duration.nanos") {
      return [{ tag: "Ok", value: { tag: "IntV", v: $rt.unwrap(arg_int({ args: args, i: 0, span: span })) } }, ctx];
    }
    if (qualified === "std.duration.of_millis") {
      const ms = $rt.unwrap(arg_int({ args: args, i: 0, span: span }));
      if (ms < 0) {
        return [{ tag: "Err", error: precondition({ what: "refinement `it >= 0` of std.duration.of_millis: " + $std_int.to_text({ x: ms }), span: span }) }, ctx];
      }
      const $m545 = $rt.recover(() => {
  return $rt.int.mul(ms, 1000000, $ob51);
});
      $m545$match: {
        if ($m545.tag === "Ok") {
          const value = $m545.value;
          return [{ tag: "Ok", value: { tag: "DurationV", v: value } }, ctx];
          break $m545$match;
        }
        if ($m545.tag === "Err") {
          const error = $m545.error;
          return [{ tag: "Err", error: precondition({ what: "duration out of range", span: span }) }, ctx];
          break $m545$match;
        }
        $rt.unreachable();
      }
    }
    return [{ tag: "Err", error: { tag: "NotConstF", reason: "`" + qualified + "` is not evaluated at check time", span: span } }, ctx];
  } catch ($e) {
    if ($e instanceof $rt.EarlyReturn) return [$e.value, ctx];
    throw $e;
  }
}

export function exec_block({ fuel, ev, ctx, b, env }) {
  try {
    if (fuel === 0) {
      return [{ tag: "Err", error: { tag: "BudgetF", fn_name: top_name({ ev: ev }) } }, ev, ctx];
    }
    for (const s of b.stmts) {
      const [$r553, ev$89, ctx$89] = exec_stmt({ fuel: $rt.int.sub(fuel, 1, $ob52), ev: ev, ctx: ctx, s: s, env: env });
      ev = ev$89;
      ctx = ctx$89;
      $rt.unwrap($r553);
    }
    return [{ tag: "Ok", value: undefined }, ev, ctx];
  } catch ($e) {
    if ($e instanceof $rt.EarlyReturn) return [$e.value, ev, ctx];
    throw $e;
  }
}

export function exec_stmt({ fuel, ev, ctx, s, env }) {
  try {
    if (fuel === 0) {
      return [{ tag: "Err", error: { tag: "BudgetF", fn_name: top_name({ ev: ev }) } }, ev, ctx];
    }
    const [$r557, ev$90] = step({ ev: ev });
    ev = ev$90;
    $rt.unwrap($r557);
    const $m558 = s;
    $m558$match: {
      if ($m558.tag === "Let") {
        const value = $m558.value;
        const span = $m558.span;
        const def = def_of_key({ ctx: ctx, k: key({ ev: ev, tag: $defs.tag_stmt, span: span }) });
        const [$r559, ev$91, ctx$91] = eval_expr({ fuel: $rt.int.sub(fuel, 1, $ob53), ev: ev, ctx: ctx, e: value, env: env });
        ev = ev$91;
        ctx = ctx$91;
        const v = $rt.unwrap($r559);
        if (def >= 0) {
          env_set({ env: env, k: def, v: v });
        }
        return [{ tag: "Ok", value: undefined }, ev, ctx];
        break $m558$match;
      }
      if ($m558.tag === "Var") {
        const value = $m558.value;
        const span = $m558.span;
        const def = def_of_key({ ctx: ctx, k: key({ ev: ev, tag: $defs.tag_stmt, span: span }) });
        const [$r561, ev$92, ctx$92] = eval_expr({ fuel: $rt.int.sub(fuel, 1, $ob54), ev: ev, ctx: ctx, e: value, env: env });
        ev = ev$92;
        ctx = ctx$92;
        const v = $rt.unwrap($r561);
        if (def >= 0) {
          env_set({ env: env, k: def, v: v });
        }
        return [{ tag: "Ok", value: undefined }, ev, ctx];
        break $m558$match;
      }
      if ($m558.tag === "Assign") {
        const value = $m558.value;
        const span = $m558.span;
        const def = res_def({ res: res_at({ ctx: ctx, k: key({ ev: ev, tag: $defs.tag_stmt, span: span }) }) });
        const [$r563, ev$93, ctx$93] = eval_expr({ fuel: $rt.int.sub(fuel, 1, $ob55), ev: ev, ctx: ctx, e: value, env: env });
        ev = ev$93;
        ctx = ctx$93;
        const v = $rt.unwrap($r563);
        if (def >= 0) {
          env_set({ env: env, k: def, v: v });
        }
        return [{ tag: "Ok", value: undefined }, ev, ctx];
        break $m558$match;
      }
      if ($m558.tag === "Return") {
        const value = $m558.value;
        const [$r565, ev$94, ctx$94] = eval_expr({ fuel: $rt.int.sub(fuel, 1, $ob56), ev: ev, ctx: ctx, e: value, env: env });
        ev = ev$94;
        ctx = ctx$94;
        const v = $rt.unwrap($r565);
        return [{ tag: "Err", error: { tag: "ReturnF", value: v } }, ev, ctx];
        break $m558$match;
      }
      if ($m558.tag === "If") {
        const cond = $m558.cond;
        const then_block = $m558.then_block;
        const else_block = $m558.else_block;
        const [$r568, ev$95, ctx$95] = eval_expr({ fuel: $rt.int.sub(fuel, 1, $ob57), ev: ev, ctx: ctx, e: cond, env: env });
        ev = ev$95;
        ctx = ctx$95;
        const c = $rt.unwrap($r568);
        if (!(c.tag === "BoolV")) {
          return [{ tag: "Err", error: panic_at({ detail: "`if` on a non-Bool", span: $parser.span_of_expr({ e: cond }) }) }, ev, ctx];
        }
        if ($values.bool_of({ c: c })) {
          const [$r570, ev$96, ctx$96] = exec_block({ fuel: $rt.int.sub(fuel, 1, $ob58), ev: ev, ctx: ctx, b: then_block, env: env });
          ev = ev$96;
          ctx = ctx$96;
          $rt.unwrap($r570);
        } else {
          const $m571 = else_block;
          $m571$match: {
            if ($m571.tag === "Some") {
              const value = $m571.value;
              const [$r572, ev$97, ctx$97] = exec_block({ fuel: $rt.int.sub(fuel, 1, $ob59), ev: ev, ctx: ctx, b: value, env: env });
              ev = ev$97;
              ctx = ctx$97;
              $rt.unwrap($r572);
              break $m571$match;
            }
            if ($m571.tag === "None") {
              skip({  });
              break $m571$match;
            }
            $rt.unreachable();
          }
        }
        return [{ tag: "Ok", value: undefined }, ev, ctx];
        break $m558$match;
      }
      if ($m558.tag === "Match") {
        const scrutinee = $m558.scrutinee;
        const arms = $m558.arms;
        const span = $m558.span;
        const [$r574, ev$98, ctx$98] = eval_expr({ fuel: $rt.int.sub(fuel, 1, $ob60), ev: ev, ctx: ctx, e: scrutinee, env: env });
        ev = ev$98;
        ctx = ctx$98;
        const v = $rt.unwrap($r574);
        for (const arm of arms) {
          const arm_env = copy_env({ env: env });
          const [$r575, ev$99, ctx$99] = match_pattern({ fuel: $rt.int.sub(fuel, 1, $ob61), ev: ev, ctx: ctx, p: arm.pattern, v: v, env: arm_env });
          ev = ev$99;
          ctx = ctx$99;
          let applies = $rt.unwrap($r575);
          if (applies) {
            const $m576 = arm.guard;
            $m576$match: {
              if ($m576.tag === "Some") {
                const value = $m576.value;
                const [$r577, ev$100, ctx$100] = eval_expr({ fuel: $rt.int.sub(fuel, 1, $ob62), ev: ev, ctx: ctx, e: value, env: arm_env });
                ev = ev$100;
                ctx = ctx$100;
                const g = $rt.unwrap($r577);
                if (!(g.tag === "BoolV")) {
                  return [{ tag: "Err", error: panic_at({ detail: "guard on a non-Bool", span: $parser.span_of_expr({ e: value }) }) }, ev, ctx];
                }
                applies = $values.bool_of({ c: g });
                break $m576$match;
              }
              if ($m576.tag === "None") {
                skip({  });
                break $m576$match;
              }
              $rt.unreachable();
            }
          }
          if (applies) {
            const $m579 = arm.body;
            $m579$match: {
              if ($m579.tag === "ArmBlock") {
                const block = $m579.block;
                const [$r580, ev$101, ctx$101] = exec_block({ fuel: $rt.int.sub(fuel, 1, $ob63), ev: ev, ctx: ctx, b: block, env: arm_env });
                ev = ev$101;
                ctx = ctx$101;
                $rt.unwrap($r580);
                break $m579$match;
              }
              if ($m579.tag === "ArmStmt") {
                const stmt = $m579.stmt;
                const [$r581, ev$102, ctx$102] = exec_stmt({ fuel: $rt.int.sub(fuel, 1, $ob64), ev: ev, ctx: ctx, s: stmt, env: arm_env });
                ev = ev$102;
                ctx = ctx$102;
                $rt.unwrap($r581);
                break $m579$match;
              }
              $rt.unreachable();
            }
            for (const k of $std_map.keys({ d: arm_env })) {
              if ($std_map.contains({ d: env, key: k })) {
                const $m582 = $std_map.find({ d: arm_env, key: k });
                $m582$match: {
                  if ($m582.tag === "Some") {
                    const value = $m582.value;
                    env_set({ env: env, k: k, v: value });
                    break $m582$match;
                  }
                  if ($m582.tag === "None") {
                    skip({  });
                    break $m582$match;
                  }
                  $rt.unreachable();
                }
              }
            }
            return [{ tag: "Ok", value: undefined }, ev, ctx];
          }
        }
        return [{ tag: "Err", error: panic_at({ detail: "no match arm applied", span: span }) }, ev, ctx];
        break $m558$match;
      }
      if ($m558.tag === "Loop") {
        const cond = $m558.cond;
        const body = $m558.body;
        let guard = ev.budget;
        while (guard > 0) {
          guard = $rt.int.sub(guard, 1, $ob65);
          const [$r586, ev$103] = step({ ev: ev });
          ev = ev$103;
          $rt.unwrap($r586);
          const [$r587, ev$104, ctx$104] = eval_expr({ fuel: $rt.int.sub(fuel, 1, $ob66), ev: ev, ctx: ctx, e: cond, env: env });
          ev = ev$104;
          ctx = ctx$104;
          const c = $rt.unwrap($r587);
          if (!(c.tag === "BoolV")) {
            return [{ tag: "Err", error: panic_at({ detail: "`loop while` on a non-Bool", span: $parser.span_of_expr({ e: cond }) }) }, ev, ctx];
          }
          if (!$values.bool_of({ c: c })) {
            return [{ tag: "Ok", value: undefined }, ev, ctx];
          }
          const [$r590, ev$105, ctx$105] = exec_block({ fuel: $rt.int.sub(fuel, 1, $ob67), ev: ev, ctx: ctx, b: body, env: env });
          ev = ev$105;
          ctx = ctx$105;
          $rt.unwrap($r590);
        }
        return [{ tag: "Err", error: { tag: "BudgetF", fn_name: top_name({ ev: ev }) } }, ev, ctx];
        break $m558$match;
      }
      if ($m558.tag === "For") {
        const domain = $m558.domain;
        const body = $m558.body;
        const span = $m558.span;
        const def = def_of_key({ ctx: ctx, k: key({ ev: ev, tag: $defs.tag_stmt, span: span }) });
        if (def < 0) {
          return [{ tag: "Ok", value: undefined }, ev, ctx];
        }
        const $m594 = domain;
        $m594$match: {
          if ($m594.tag === "RangeDomain") {
            const lo = $m594.lo;
            const hi = $m594.hi;
            const [$r595, ev$106, ctx$106] = eval_expr({ fuel: $rt.int.sub(fuel, 1, $ob68), ev: ev, ctx: ctx, e: lo, env: env });
            ev = ev$106;
            ctx = ctx$106;
            const lv = $rt.unwrap($r595);
            const [$r596, ev$107, ctx$107] = eval_expr({ fuel: $rt.int.sub(fuel, 1, $ob69), ev: ev, ctx: ctx, e: hi, env: env });
            ev = ev$107;
            ctx = ctx$107;
            const hv = $rt.unwrap($r596);
            if (!(lv.tag === "IntV") || !(hv.tag === "IntV")) {
              return [{ tag: "Err", error: panic_at({ detail: "range bounds must be Int", span: span }) }, ev, ctx];
            }
            const lo_i = $values.int_of({ c: lv });
            const hi_i = $values.int_of({ c: hv });
            if (lo_i < hi_i) {
              for (let i = lo_i; i < hi_i; i++) {
                const [$r598, ev$108] = step({ ev: ev });
                ev = ev$108;
                $rt.unwrap($r598);
                env_set({ env: env, k: def, v: { tag: "IntV", v: i } });
                const [$r600, ev$109, ctx$109] = exec_block({ fuel: $rt.int.sub(fuel, 1, $ob70), ev: ev, ctx: ctx, b: body, env: env });
                ev = ev$109;
                ctx = ctx$109;
                $rt.unwrap($r600);
              }
            }
            break $m594$match;
          }
          if ($m594.tag === "InDomain") {
            const expr = $m594.expr;
            const [$r601, ev$110, ctx$110] = eval_expr({ fuel: $rt.int.sub(fuel, 1, $ob71), ev: ev, ctx: ctx, e: expr, env: env });
            ev = ev$110;
            ctx = ctx$110;
            const dv = $rt.unwrap($r601);
            if (!(dv.tag === "ListV")) {
              return [{ tag: "Err", error: not_const({ reason: "`for` over a non-list", span: span }) }, ev, ctx];
            }
            for (const x of $values.items_of({ c: dv })) {
              const [$r603, ev$111] = step({ ev: ev });
              ev = ev$111;
              $rt.unwrap($r603);
              env_set({ env: env, k: def, v: x });
              const [$r604, ev$112, ctx$112] = exec_block({ fuel: $rt.int.sub(fuel, 1, $ob72), ev: ev, ctx: ctx, b: body, env: env });
              ev = ev$112;
              ctx = ctx$112;
              $rt.unwrap($r604);
            }
            break $m594$match;
          }
          $rt.unreachable();
        }
        return [{ tag: "Ok", value: undefined }, ev, ctx];
        break $m558$match;
      }
      if ($m558.tag === "Assume") {
        return [{ tag: "Ok", value: undefined }, ev, ctx];
        break $m558$match;
      }
      if ($m558.tag === "ExprStmt") {
        const expr = $m558.expr;
        const [$r607, ev$113, ctx$113] = eval_expr({ fuel: $rt.int.sub(fuel, 1, $ob73), ev: ev, ctx: ctx, e: expr, env: env });
        ev = ev$113;
        ctx = ctx$113;
        const v = $rt.unwrap($r607);
        return [{ tag: "Ok", value: undefined }, ev, ctx];
        break $m558$match;
      }
      $rt.unreachable();
    }
  } catch ($e) {
    if ($e instanceof $rt.EarlyReturn) return [$e.value, ev, ctx];
    throw $e;
  }
}

export function pat_field_tag({ pf }) {
  const $m609 = pf;
  $m609$match: {
    if ($m609.tag === "PatFieldName") {
      return 0;
      break $m609$match;
    }
    if ($m609.tag === "PatFieldSkip") {
      return 1;
      break $m609$match;
    }
    if ($m609.tag === "PatFieldRest") {
      return 2;
      break $m609$match;
    }
    $rt.unreachable();
  }
}

export function pat_field_span({ pf }) {
  const $m610 = pf;
  $m610$match: {
    if ($m610.tag === "PatFieldName") {
      const span = $m610.span;
      return span;
      break $m610$match;
    }
    if ($m610.tag === "PatFieldSkip") {
      const span = $m610.span;
      return span;
      break $m610$match;
    }
    if ($m610.tag === "PatFieldRest") {
      const span = $m610.span;
      return span;
      break $m610$match;
    }
    $rt.unreachable();
  }
}

export function field_infos_or_empty({ o }) {
  const $m611 = o;
  $m611$match: {
    if ($m611.tag === "Some") {
      const value = $m611.value;
      return value;
      break $m611$match;
    }
    if ($m611.tag === "None") {
      return [];
      break $m611$match;
    }
    $rt.unreachable();
  }
}

export function match_pattern({ fuel, ev, ctx, p, v, env }) {
  try {
    if (fuel === 0) {
      return [{ tag: "Err", error: { tag: "BudgetF", fn_name: top_name({ ev: ev }) } }, ev, ctx];
    }
    const $m614 = p;
    $m614$match: {
      if ($m614.tag === "WildcardPat") {
        return [{ tag: "Ok", value: true }, ev, ctx];
        break $m614$match;
      }
      if ($m614.tag === "BindPat") {
        const span = $m614.span;
        const def = def_of_key({ ctx: ctx, k: key({ ev: ev, tag: $defs.tag_pattern, span: span }) });
        if (def >= 0) {
          env_set({ env: env, k: def, v: v });
        }
        return [{ tag: "Ok", value: true }, ev, ctx];
        break $m614$match;
      }
      if ($m614.tag === "LitPat") {
        const literal = $m614.literal;
        const [$r617, ev$114, ctx$114] = eval_expr({ fuel: $rt.int.sub(fuel, 1, $ob74), ev: ev, ctx: ctx, e: literal, env: env });
        ev = ev$114;
        ctx = ctx$114;
        const lv = $rt.unwrap($r617);
        return [{ tag: "Ok", value: $values.equals({ a: lv, b: v }) }, ev, ctx];
        break $m614$match;
      }
      if ($m614.tag === "VariantPat") {
        const fields = $m614.fields;
        const span = $m614.span;
        const vdef = res_def({ res: res_at({ ctx: ctx, k: key({ ev: ev, tag: $defs.tag_pattern, span: span }) }) });
        if (vdef < 0) {
          return [{ tag: "Ok", value: false }, ev, ctx];
        }
        if (!(v.tag === "VariantV") || $values.def_of({ c: v }) !== vdef) {
          return [{ tag: "Ok", value: false }, ev, ctx];
        }
        const $m621 = fields;
        $m621$match: {
          if ($m621.tag === "None") {
            return [{ tag: "Ok", value: true }, ev, ctx];
            break $m621$match;
          }
          if ($m621.tag === "Some") {
            const value = $m621.value;
            const infos = field_infos_or_empty({ o: $std_map.find({ d: ctx.fields, key: vdef }) });
            let i = 0;
            let stop = false;
            for (const pf of value) {
              if (!stop) {
                if (pat_field_tag({ pf: pf }) === 2) {
                  stop = true;
                } else {
                  if (i >= $std_list.len({ xs: infos })) {
                    return [{ tag: "Ok", value: false }, ev, ctx];
                  } else {
                    const f = $std_list.get({ xs: infos, i: i });
                    if (pat_field_tag({ pf: pf }) === 0) {
                      const def = def_of_key({ ctx: ctx, k: key({ ev: ev, tag: $defs.tag_pattern, span: pat_field_span({ pf: pf }) }) });
                      const fv = $values.field_of({ fields: $values.fields_of({ c: v }), name: f.name });
                      if (def >= 0 && fv.tag === "Some") {
                        env_set({ env: env, k: def, v: value_or_unit({ o: fv }) });
                      }
                    }
                    i = $rt.int.add(i, 1, $ob75);
                  }
                }
              }
            }
            return [{ tag: "Ok", value: true }, ev, ctx];
            break $m621$match;
          }
          $rt.unreachable();
        }
        break $m614$match;
      }
      $rt.unreachable();
    }
  } catch ($e) {
    if ($e instanceof $rt.EarlyReturn) return [$e.value, ev, ctx];
    throw $e;
  }
}

