// Generated by onus from module json. Do not edit.
import * as $rt from "/Users/jamesrandall/code/onus/packages/runtime/dist/index.js";
import * as $std_int from "./std/int.js";
import * as $std_list from "./std/list.js";
import * as $std_text from "./std/text.js";
import * as $std_bool from "./std/bool.js";

const $ob1 = { kind: "overflow", text: "-1 within Int", at: "/Users/jamesrandall/code/onus/self/json.onus:225:10", def: "peek" };
const $ob2 = { kind: "overflow", text: "p.i + 1 within Int", at: "/Users/jamesrandall/code/onus/self/json.onus:229:19", def: "advance" };
const $ob3 = { kind: "overflow", text: "List.len(xs: p.cps) + 1 within Int", at: "/Users/jamesrandall/code/onus/self/json.onus:242:34", def: "skip_ws" };
const $ob4 = { kind: "overflow", text: "steps - 1 within Int", at: "/Users/jamesrandall/code/onus/self/json.onus:247:13", def: "skip_ws" };
const $ob5 = { kind: "overflow", text: "-1 within Int", at: "/Users/jamesrandall/code/onus/self/json.onus:271:10", def: "hex_value" };
const $ob6 = { kind: "overflow", text: "-1 within Int", at: "/Users/jamesrandall/code/onus/self/json.onus:280:14", def: "hex4" };
const $ob7 = { kind: "overflow", text: "v * 16 within Int", at: "/Users/jamesrandall/code/onus/self/json.onus:282:9", def: "hex4" };
const $ob8 = { kind: "overflow", text: "v * 16 + d within Int", at: "/Users/jamesrandall/code/onus/self/json.onus:282:9", def: "hex4" };
const $ob9 = { kind: "overflow", text: "List.len(xs: p.cps) + 1 within Int", at: "/Users/jamesrandall/code/onus/self/json.onus:295:34", def: "parse_string" };
const $ob10 = { kind: "overflow", text: "steps - 1 within Int", at: "/Users/jamesrandall/code/onus/self/json.onus:300:13", def: "parse_string" };
const $ob11 = { kind: "overflow", text: "-1 within Int", at: "/Users/jamesrandall/code/onus/self/json.onus:302:13", def: "parse_string" };
const $ob12 = { kind: "overflow", text: "List.len(xs: p.cps) + 1 within Int", at: "/Users/jamesrandall/code/onus/self/json.onus:370:34", def: "digits" };
const $ob13 = { kind: "overflow", text: "steps - 1 within Int", at: "/Users/jamesrandall/code/onus/self/json.onus:375:13", def: "digits" };
const $ob14 = { kind: "overflow", text: "n + 1 within Int", at: "/Users/jamesrandall/code/onus/self/json.onus:377:9", def: "digits" };
const $ob15 = { kind: "overflow", text: "List.len(xs: p.cps) + 1 within Int", at: "/Users/jamesrandall/code/onus/self/json.onus:437:34", def: "parse_array" };
const $ob16 = { kind: "overflow", text: "steps - 1 within Int", at: "/Users/jamesrandall/code/onus/self/json.onus:442:13", def: "parse_array" };
const $ob17 = { kind: "overflow", text: "fuel - 1 within Int", at: "/Users/jamesrandall/code/onus/self/json.onus:443:29", def: "parse_array" };
const $ob18 = { kind: "overflow", text: "List.len(xs: p.cps) + 1 within Int", at: "/Users/jamesrandall/code/onus/self/json.onus:473:34", def: "parse_object" };
const $ob19 = { kind: "overflow", text: "steps - 1 within Int", at: "/Users/jamesrandall/code/onus/self/json.onus:478:13", def: "parse_object" };
const $ob20 = { kind: "overflow", text: "fuel - 1 within Int", at: "/Users/jamesrandall/code/onus/self/json.onus:489:29", def: "parse_object" };
const $ob21 = { kind: "overflow", text: "fuel - 1 within Int", at: "/Users/jamesrandall/code/onus/self/json.onus:515:31", def: "parse_value" };
const $ob22 = { kind: "overflow", text: "fuel - 1 within Int", at: "/Users/jamesrandall/code/onus/self/json.onus:518:30", def: "parse_value" };
export function int({ v }) {
  return { tag: "JNumber", text: $std_int.to_text({ x: v }) };
}

export function text({ t }) {
  return { tag: "JString", value: t };
}

export function bool({ b }) {
  return { tag: "JBool", value: b };
}

export function field({ key, value }) {
  return { key: key, value: value };
}

export function texts({ ts }) {
  let out = $std_list.builder({  });
  for (const t of ts) {
    const [, out$1] = $std_list.push({ b: out, x: { tag: "JString", value: t } });
    out = out$1;
  }
  return { tag: "JArray", items: $std_list.finish({ b: out }) };
}

export function text_or_null({ o }) {
  const $m7 = o;
  $m7$match: {
    if ($m7.tag === "Some") {
      const value = $m7.value;
      return { tag: "JString", value: value };
      break $m7$match;
    }
    if ($m7.tag === "None") {
      return { tag: "JNull" };
      break $m7$match;
    }
    $rt.unreachable();
  }
}

export function hex_digit({ n }) {
  if (n === 0) {
    return "0";
  }
  if (n === 1) {
    return "1";
  }
  if (n === 2) {
    return "2";
  }
  if (n === 3) {
    return "3";
  }
  if (n === 4) {
    return "4";
  }
  if (n === 5) {
    return "5";
  }
  if (n === 6) {
    return "6";
  }
  if (n === 7) {
    return "7";
  }
  if (n === 8) {
    return "8";
  }
  if (n === 9) {
    return "9";
  }
  if (n === 10) {
    return "a";
  }
  if (n === 11) {
    return "b";
  }
  if (n === 12) {
    return "c";
  }
  if (n === 13) {
    return "d";
  }
  if (n === 14) {
    return "e";
  }
  return "f";
}

export function quote({ t }) {
  let out = $std_list.builder({  });
  const [, out$2] = $std_list.push({ b: out, x: "\"" });
  out = out$2;
  for (const c of $std_text.code_points({ t: t })) {
    if (c === 34) {
      const [, out$3] = $std_list.push({ b: out, x: "\\\"" });
      out = out$3;
    } else {
      if (c === 92) {
        const [, out$4] = $std_list.push({ b: out, x: "\\\\" });
        out = out$4;
      } else {
        if (c === 10) {
          const [, out$5] = $std_list.push({ b: out, x: "\\n" });
          out = out$5;
        } else {
          if (c === 13) {
            const [, out$6] = $std_list.push({ b: out, x: "\\r" });
            out = out$6;
          } else {
            if (c === 9) {
              const [, out$7] = $std_list.push({ b: out, x: "\\t" });
              out = out$7;
            } else {
              if (c === 8) {
                const [, out$8] = $std_list.push({ b: out, x: "\\b" });
                out = out$8;
              } else {
                if (c === 12) {
                  const [, out$9] = $std_list.push({ b: out, x: "\\f" });
                  out = out$9;
                } else {
                  if (c < 32) {
                    const [, out$10] = $std_list.push({ b: out, x: "\\u00" + hex_digit({ n: Math.trunc(c / 16) }) + hex_digit({ n: c % 16 }) });
                    out = out$10;
                  } else {
                    const [, out$11] = $std_list.push({ b: out, x: $std_text.of_code_point({ cp: c }) });
                    out = out$11;
                  }
                }
              }
            }
          }
        }
      }
    }
  }
  const [, out$12] = $std_list.push({ b: out, x: "\"" });
  out = out$12;
  return $std_text.join({ parts: $std_list.finish({ b: out }), sep: "" });
}

export function compact({ j }) {
  const $m10 = j;
  $m10$match: {
    if ($m10.tag === "JNull") {
      return "null";
      break $m10$match;
    }
    if ($m10.tag === "JBool") {
      const value = $m10.value;
      return $std_bool.to_text({ b: value });
      break $m10$match;
    }
    if ($m10.tag === "JNumber") {
      const text = $m10.text;
      return text;
      break $m10$match;
    }
    if ($m10.tag === "JString") {
      const value = $m10.value;
      return quote({ t: value });
      break $m10$match;
    }
    if ($m10.tag === "JArray") {
      const items = $m10.items;
      let parts = $std_list.builder({  });
      for (const x of items) {
        const [, parts$13] = $std_list.push({ b: parts, x: compact({ j: x }) });
        parts = parts$13;
      }
      return "[" + $std_text.join({ parts: $std_list.finish({ b: parts }), sep: "," }) + "]";
      break $m10$match;
    }
    if ($m10.tag === "JObject") {
      const fields = $m10.fields;
      let parts = $std_list.builder({  });
      for (const f of fields) {
        const [, parts$14] = $std_list.push({ b: parts, x: quote({ t: f.key }) + ":" + compact({ j: f.value }) });
        parts = parts$14;
      }
      return "{" + $std_text.join({ parts: $std_list.finish({ b: parts }), sep: "," }) + "}";
      break $m10$match;
    }
    $rt.unreachable();
  }
}

export function pretty({ j }) {
  return pretty_at({ j: j, indent: "" });
}

export function pretty_at({ j, indent }) {
  const $m11 = j;
  $m11$match: {
    if ($m11.tag === "JArray") {
      const items = $m11.items;
      if ($std_list.len({ xs: items }) === 0) {
        return "[]";
      }
      const inner = indent + "  ";
      let parts = $std_list.builder({  });
      for (const x of items) {
        const [, parts$15] = $std_list.push({ b: parts, x: inner + pretty_at({ j: x, indent: inner }) });
        parts = parts$15;
      }
      return "[\n" + $std_text.join({ parts: $std_list.finish({ b: parts }), sep: ",\n" }) + "\n" + indent + "]";
      break $m11$match;
    }
    if ($m11.tag === "JObject") {
      const fields = $m11.fields;
      if ($std_list.len({ xs: fields }) === 0) {
        return "{}";
      }
      const inner = indent + "  ";
      let parts = $std_list.builder({  });
      for (const f of fields) {
        const [, parts$16] = $std_list.push({ b: parts, x: inner + quote({ t: f.key }) + ": " + pretty_at({ j: f.value, indent: inner }) });
        parts = parts$16;
      }
      return "{\n" + $std_text.join({ parts: $std_list.finish({ b: parts }), sep: ",\n" }) + "\n" + indent + "}";
      break $m11$match;
    }
    if (true) {
      return compact({ j: j });
      break $m11$match;
    }
    $rt.unreachable();
  }
}

export const parse_depth = 100000;

export function at_end({ p }) {
  return p.i >= $std_list.len({ xs: p.cps });
}

export function peek({ p }) {
  if (p.i < $std_list.len({ xs: p.cps })) {
    return $std_list.get({ xs: p.cps, i: p.i });
  }
  return $rt.int.neg(1, $ob1);
}

export function advance({ p }) {
  p = { ...p, i: $rt.int.add(p.i, 1, $ob2) };
  return [undefined, p];
  return [undefined, p];
}

export function is_ws({ c }) {
  return c === 32 || c === 9 || c === 10 || c === 13;
}

export function is_digit({ c }) {
  return c >= 48 && c <= 57;
}

export function skip_ws({ p }) {
  let steps = $rt.int.add($std_list.len({ xs: p.cps }), 1, $ob3);
  while (steps > 0 && is_ws({ c: peek({ p: p }) })) {
    steps = $rt.int.sub(steps, 1, $ob4);
    const [, p$17] = advance({ p: p });
    p = p$17;
  }
  return [undefined, p];
  return [undefined, p];
}

export function clamp({ c }) {
  if (c < 0 || c > 1114111) {
    return 0;
  }
  return c;
}

export function hex_value({ c }) {
  if (c >= 48 && c <= 57) {
    return c - 48;
  }
  if (c >= 97 && c <= 102) {
    return c - 87;
  }
  if (c >= 65 && c <= 70) {
    return c - 55;
  }
  return $rt.int.neg(1, $ob5);
}

export function hex4({ p }) {
  let v = 0;
  for (let k = 0; k < 4; k++) {
    const d = hex_value({ c: peek({ p: p }) });
    if (d < 0) {
      return [$rt.int.neg(1, $ob6), p];
    }
    v = $rt.int.add($rt.int.mul(v, 16, $ob7), d, $ob8);
    const [, p$18] = advance({ p: p });
    p = p$18;
  }
  return [v, p];
}

export function parse_string({ p }) {
  if (peek({ p: p }) !== 34) {
    return [{ tag: "None" }, p];
  }
  const [, p$19] = advance({ p: p });
  p = p$19;
  let out = $std_list.builder({  });
  let steps = $rt.int.add($std_list.len({ xs: p.cps }), 1, $ob9);
  while (steps > 0) {
    steps = $rt.int.sub(steps, 1, $ob10);
    const c = peek({ p: p });
    if (c === $rt.int.neg(1, $ob11)) {
      return [{ tag: "None" }, p];
    }
    if (c === 34) {
      const [, p$20] = advance({ p: p });
      p = p$20;
      return [{ tag: "Some", value: $std_text.of_code_points({ cps: $std_list.finish({ b: out }) }) }, p];
    }
    if (c === 92) {
      const [, p$21] = advance({ p: p });
      p = p$21;
      const e = peek({ p: p });
      const [, p$22] = advance({ p: p });
      p = p$22;
      if (e === 34 || e === 92 || e === 47) {
        const [, out$23] = $std_list.push({ b: out, x: clamp({ c: e }) });
        out = out$23;
      } else {
        if (e === 98) {
          const [, out$24] = $std_list.push({ b: out, x: 8 });
          out = out$24;
        } else {
          if (e === 102) {
            const [, out$25] = $std_list.push({ b: out, x: 12 });
            out = out$25;
          } else {
            if (e === 110) {
              const [, out$26] = $std_list.push({ b: out, x: 10 });
              out = out$26;
            } else {
              if (e === 114) {
                const [, out$27] = $std_list.push({ b: out, x: 13 });
                out = out$27;
              } else {
                if (e === 116) {
                  const [, out$28] = $std_list.push({ b: out, x: 9 });
                  out = out$28;
                } else {
                  if (e === 117) {
                    const [$r18, p$29] = hex4({ p: p });
                    p = p$29;
                    let unit = $r18;
                    if (unit < 0) {
                      return [{ tag: "None" }, p];
                    }
                    if (unit >= 55296 && unit <= 56319 && peek({ p: p }) === 92) {
                      let q = p;
                      const [, q$30] = advance({ p: q });
                      q = q$30;
                      if (peek({ p: q }) === 117) {
                        const [, q$31] = advance({ p: q });
                        q = q$31;
                        const [$r20, q$32] = hex4({ p: q });
                        q = q$32;
                        const low = $r20;
                        if (low >= 56320 && low <= 57343) {
                          unit = 65536 + (unit - 55296) * 1024 + (low - 56320);
                          p = q;
                        }
                      }
                    }
                    const [, out$33] = $std_list.push({ b: out, x: clamp({ c: unit }) });
                    out = out$33;
                  } else {
                    return [{ tag: "None" }, p];
                  }
                }
              }
            }
          }
        }
      }
    } else {
      const [, out$34] = $std_list.push({ b: out, x: clamp({ c: c }) });
      out = out$34;
      const [, p$35] = advance({ p: p });
      p = p$35;
    }
  }
  return [{ tag: "None" }, p];
}

export function digits({ p }) {
  let n = 0;
  let steps = $rt.int.add($std_list.len({ xs: p.cps }), 1, $ob12);
  while (steps > 0 && is_digit({ c: peek({ p: p }) })) {
    steps = $rt.int.sub(steps, 1, $ob13);
    const [, p$36] = advance({ p: p });
    p = p$36;
    n = $rt.int.add(n, 1, $ob14);
  }
  return [n, p];
}

export function parse_number({ p }) {
  const start = p.i;
  if (peek({ p: p }) === 45) {
    const [, p$37] = advance({ p: p });
    p = p$37;
  }
  const [$r24, p$38] = digits({ p: p });
  p = p$38;
  if ($r24 === 0) {
    return [{ tag: "None" }, p];
  }
  if (peek({ p: p }) === 46) {
    const [, p$39] = advance({ p: p });
    p = p$39;
    const [$r26, p$40] = digits({ p: p });
    p = p$40;
    if ($r26 === 0) {
      return [{ tag: "None" }, p];
    }
  }
  if (peek({ p: p }) === 101 || peek({ p: p }) === 69) {
    const [, p$41] = advance({ p: p });
    p = p$41;
    if (peek({ p: p }) === 43 || peek({ p: p }) === 45) {
      const [, p$42] = advance({ p: p });
      p = p$42;
    }
    const [$r28, p$43] = digits({ p: p });
    p = p$43;
    if ($r28 === 0) {
      return [{ tag: "None" }, p];
    }
  }
  let out = $std_list.builder({  });
  const $hi30 = p.i;
  for (let k = start; k < $hi30; k++) {
    if (k >= 0 && k < $std_list.len({ xs: p.cps })) {
      const [, out$44] = $std_list.push({ b: out, x: clamp({ c: $std_list.get({ xs: p.cps, i: k }) }) });
      out = out$44;
    }
  }
  return [{ tag: "Some", value: { tag: "JNumber", text: $std_text.of_code_points({ cps: $std_list.finish({ b: out }) }) } }, p];
}

export function parse_word({ p, word, v }) {
  for (const c of $std_text.code_points({ t: word })) {
    if (peek({ p: p }) !== c) {
      return [{ tag: "None" }, p];
    }
    const [, p$45] = advance({ p: p });
    p = p$45;
  }
  return [{ tag: "Some", value: v }, p];
}

export function parse_array({ fuel, p }) {
  if (fuel === 0) {
    return [{ tag: "None" }, p];
  }
  const [, p$46] = advance({ p: p });
  p = p$46;
  let items = $std_list.builder({  });
  const [, p$47] = skip_ws({ p: p });
  p = p$47;
  if (peek({ p: p }) === 93) {
    const [, p$48] = advance({ p: p });
    p = p$48;
    return [{ tag: "Some", value: { tag: "JArray", items: [] } }, p];
  }
  let steps = $rt.int.add($std_list.len({ xs: p.cps }), 1, $ob15);
  while (steps > 0) {
    steps = $rt.int.sub(steps, 1, $ob16);
    const [$r40, p$49] = parse_value({ fuel: $rt.int.sub(fuel, 1, $ob17), p: p });
    p = p$49;
    const $m39 = $r40;
    $m39$match: {
      if ($m39.tag === "Some") {
        const value = $m39.value;
        const [, items$50] = $std_list.push({ b: items, x: value });
        items = items$50;
        break $m39$match;
      }
      if ($m39.tag === "None") {
        return [{ tag: "None" }, p];
        break $m39$match;
      }
      $rt.unreachable();
    }
    const [, p$51] = skip_ws({ p: p });
    p = p$51;
    const c = peek({ p: p });
    if (c === 93) {
      const [, p$52] = advance({ p: p });
      p = p$52;
      return [{ tag: "Some", value: { tag: "JArray", items: $std_list.finish({ b: items }) } }, p];
    }
    if (c !== 44) {
      return [{ tag: "None" }, p];
    }
    const [, p$53] = advance({ p: p });
    p = p$53;
  }
  return [{ tag: "None" }, p];
}

export function parse_object({ fuel, p }) {
  if (fuel === 0) {
    return [{ tag: "None" }, p];
  }
  const [, p$54] = advance({ p: p });
  p = p$54;
  let fields = $std_list.builder({  });
  const [, p$55] = skip_ws({ p: p });
  p = p$55;
  if (peek({ p: p }) === 125) {
    const [, p$56] = advance({ p: p });
    p = p$56;
    return [{ tag: "Some", value: { tag: "JObject", fields: [] } }, p];
  }
  let steps = $rt.int.add($std_list.len({ xs: p.cps }), 1, $ob18);
  while (steps > 0) {
    steps = $rt.int.sub(steps, 1, $ob19);
    const [, p$57] = skip_ws({ p: p });
    p = p$57;
    let key = "";
    const [$r51, p$58] = parse_string({ p: p });
    p = p$58;
    const $m50 = $r51;
    $m50$match: {
      if ($m50.tag === "Some") {
        const value = $m50.value;
        key = value;
        break $m50$match;
      }
      if ($m50.tag === "None") {
        return [{ tag: "None" }, p];
        break $m50$match;
      }
      $rt.unreachable();
    }
    const [, p$59] = skip_ws({ p: p });
    p = p$59;
    if (peek({ p: p }) !== 58) {
      return [{ tag: "None" }, p];
    }
    const [, p$60] = advance({ p: p });
    p = p$60;
    const [$r55, p$61] = parse_value({ fuel: $rt.int.sub(fuel, 1, $ob20), p: p });
    p = p$61;
    const $m54 = $r55;
    $m54$match: {
      if ($m54.tag === "Some") {
        const value = $m54.value;
        const [, fields$62] = $std_list.push({ b: fields, x: { key: key, value: value } });
        fields = fields$62;
        break $m54$match;
      }
      if ($m54.tag === "None") {
        return [{ tag: "None" }, p];
        break $m54$match;
      }
      $rt.unreachable();
    }
    const [, p$63] = skip_ws({ p: p });
    p = p$63;
    const c = peek({ p: p });
    if (c === 125) {
      const [, p$64] = advance({ p: p });
      p = p$64;
      return [{ tag: "Some", value: { tag: "JObject", fields: $std_list.finish({ b: fields }) } }, p];
    }
    if (c !== 44) {
      return [{ tag: "None" }, p];
    }
    const [, p$65] = advance({ p: p });
    p = p$65;
  }
  return [{ tag: "None" }, p];
}

export function parse_value({ fuel, p }) {
  if (fuel === 0) {
    return [{ tag: "None" }, p];
  }
  const [, p$66] = skip_ws({ p: p });
  p = p$66;
  const c = peek({ p: p });
  if (c === 123) {
    const [$r63, p$67] = parse_object({ fuel: $rt.int.sub(fuel, 1, $ob21), p: p });
    p = p$67;
    return [$r63, p];
  }
  if (c === 91) {
    const [$r64, p$68] = parse_array({ fuel: $rt.int.sub(fuel, 1, $ob22), p: p });
    p = p$68;
    return [$r64, p];
  }
  if (c === 34) {
    const [$r66, p$69] = parse_string({ p: p });
    p = p$69;
    const $m65 = $r66;
    $m65$match: {
      if ($m65.tag === "Some") {
        const value = $m65.value;
        return [{ tag: "Some", value: { tag: "JString", value: value } }, p];
        break $m65$match;
      }
      if ($m65.tag === "None") {
        return [{ tag: "None" }, p];
        break $m65$match;
      }
      $rt.unreachable();
    }
  }
  if (c === 116) {
    const [$r71, p$70] = parse_word({ p: p, word: "true", v: { tag: "JBool", value: true } });
    p = p$70;
    return [$r71, p];
  }
  if (c === 102) {
    const [$r73, p$71] = parse_word({ p: p, word: "false", v: { tag: "JBool", value: false } });
    p = p$71;
    return [$r73, p];
  }
  if (c === 110) {
    const [$r75, p$72] = parse_word({ p: p, word: "null", v: { tag: "JNull" } });
    p = p$72;
    return [$r75, p];
  }
  if (c === 45 || is_digit({ c: c })) {
    const [$r76, p$73] = parse_number({ p: p });
    p = p$73;
    return [$r76, p];
  }
  return [{ tag: "None" }, p];
}

export function parse({ t }) {
  let p = { cps: $std_text.code_points({ t: t }), i: 0 };
  const [$r79, p$74] = parse_value({ fuel: parse_depth, p: p });
  p = p$74;
  const v = $r79;
  const [, p$75] = skip_ws({ p: p });
  p = p$75;
  if (!at_end({ p: p })) {
    return { tag: "None" };
  }
  return v;
}

export function at({ j, key }) {
  const $m81 = j;
  $m81$match: {
    if ($m81.tag === "JObject") {
      const fields = $m81.fields;
      for (const f of fields) {
        if (f.key === key) {
          return f.value;
        }
      }
      return { tag: "JNull" };
      break $m81$match;
    }
    if (true) {
      return { tag: "JNull" };
      break $m81$match;
    }
    $rt.unreachable();
  }
}

export function has({ j, key }) {
  const $m84 = j;
  $m84$match: {
    if ($m84.tag === "JObject") {
      const fields = $m84.fields;
      for (const f of fields) {
        if (f.key === key) {
          return true;
        }
      }
      return false;
      break $m84$match;
    }
    if (true) {
      return false;
      break $m84$match;
    }
    $rt.unreachable();
  }
}

export function is_null({ j }) {
  return j.tag === "JNull";
}

export function str_of({ j }) {
  const $m85 = j;
  $m85$match: {
    if ($m85.tag === "JString") {
      const value = $m85.value;
      return value;
      break $m85$match;
    }
    if (true) {
      return "";
      break $m85$match;
    }
    $rt.unreachable();
  }
}

export function str_at({ j, key }) {
  return str_of({ j: at({ j: j, key: key }) });
}

export function list_of({ j }) {
  const $m86 = j;
  $m86$match: {
    if ($m86.tag === "JArray") {
      const items = $m86.items;
      return items;
      break $m86$match;
    }
    if (true) {
      return [];
      break $m86$match;
    }
    $rt.unreachable();
  }
}

export function list_at({ j, key }) {
  return list_of({ j: at({ j: j, key: key }) });
}

export function texts_of({ j }) {
  let out = $std_list.builder({  });
  for (const x of list_of({ j: j })) {
    const [, out$76] = $std_list.push({ b: out, x: str_of({ j: x }) });
    out = out$76;
  }
  return $std_list.finish({ b: out });
}

export function texts_at({ j, key }) {
  return texts_of({ j: at({ j: j, key: key }) });
}

export function bool_at({ j, key }) {
  const $m87 = at({ j: j, key: key });
  $m87$match: {
    if ($m87.tag === "JBool") {
      const value = $m87.value;
      return value;
      break $m87$match;
    }
    if (true) {
      return false;
      break $m87$match;
    }
    $rt.unreachable();
  }
}

export function int_at({ j, key }) {
  const $m88 = at({ j: j, key: key });
  $m88$match: {
    if ($m88.tag === "JNumber") {
      const text = $m88.text;
      const $m89 = $std_int.parse({ t: text });
      $m89$match: {
        if ($m89.tag === "Some") {
          const value = $m89.value;
          return value;
          break $m89$match;
        }
        if ($m89.tag === "None") {
          return 0;
          break $m89$match;
        }
        $rt.unreachable();
      }
      break $m88$match;
    }
    if (true) {
      return 0;
      break $m88$match;
    }
    $rt.unreachable();
  }
}

