// Generated by onus from module parser. Do not edit.
import * as $rt from "/Users/jamesrandall/code/onus/packages/runtime/dist/index.js";
import * as $std_list from "./std/list.js";
import * as $tokens from "./tokens.js";
import * as $std_text from "./std/text.js";

const $ob1 = { kind: "overflow", text: "-1 within Int", at: "/Users/jamesrandall/code/onus/self/parser.onus:265:23", def: "source_text" };
const $ob2 = { kind: "overflow", text: "List.len(xs: segments) - 1 within Int", at: "/Users/jamesrandall/code/onus/self/parser.onus:550:51", def: "qname_of" };
export const r_block = 50;

export const r_stmts = 48;

export const r_stmt = 46;

export const r_if = 45;

export const r_domain = 44;

export const r_expr = 40;

export const r_or = 38;

export const r_and = 36;

export const r_not = 34;

export const r_cmp = 32;

export const r_add = 30;

export const r_pattern = 29;

export const r_mul = 28;

export const r_variant_pattern = 27;

export const r_unary = 26;

export const r_postfix = 24;

export const r_primary = 22;

export const r_ctor_rest = 20;

export const r_call_args = 18;

export const r_field_inits = 17;

export const r_type_args = 43;

export const r_type_arg = 42;

export const r_type = 14;

export const r_binder_type = 15;

export const r_param_list = 8;

export const r_param = 6;

export const r_claim_or = 12;

export const r_claim_and = 11;

export const r_claim_not = 10;

export const r_claim_atom = 9;

export function parse({ toks, source_end }) {
  let p = { toks: toks, pos: 0, source_end: source_end, current_def: "", is_test_module: false, is_std_module: false, no_brace: false, assertion_block: false, diagnostics: [] };
  const [$r3, p$1] = parse_module({ p: p });
  p = p$1;
  const $m2 = $r3;
  $m2$match: {
    if ($m2.tag === "Ok") {
      const value = $m2.value;
      return { module: { tag: "Some", value: value }, diagnostics: p.diagnostics };
      break $m2$match;
    }
    if ($m2.tag === "Err") {
      const error = $m2.error;
      return { module: { tag: "None" }, diagnostics: p.diagnostics };
      break $m2$match;
    }
    $rt.unreachable();
  }
}

export function count({ p }) {
  return $std_list.len({ xs: p.toks });
}

export function peek({ p, n }) {
  let i = p.pos + n;
  if (i > $std_list.len({ xs: p.toks }) - 1) {
    i = $std_list.len({ xs: p.toks }) - 1;
  }
  return $std_list.get({ xs: p.toks, i: i });
}

export function kind_at({ p, n }) {
  return peek({ p: p, n: n }).kind;
}

export function at_punct({ p, want }) {
  return $tokens.is_punct({ kind: kind_at({ p: p, n: 0 }), want: want });
}

export function at_punct_n({ p, n, want }) {
  return $tokens.is_punct({ kind: kind_at({ p: p, n: n }), want: want });
}

export function at_word({ p, want }) {
  return $tokens.is_word({ kind: kind_at({ p: p, n: 0 }), want: want });
}

export function at_word_n({ p, n, want }) {
  return $tokens.is_word({ kind: kind_at({ p: p, n: n }), want: want });
}

export function at_name({ p, n }) {
  return $tokens.is_name_kind({ kind: kind_at({ p: p, n: n }) });
}

export function at_tname({ p, n }) {
  return kind_at({ p: p, n: n }).tag === "TName";
}

export function at_newline({ p }) {
  return kind_at({ p: p, n: 0 }).tag === "Newline";
}

export function at_eof({ p }) {
  return kind_at({ p: p, n: 0 }).tag === "Eof";
}

export function at_end({ p }) {
  return p.pos >= $std_list.len({ xs: p.toks }) - 1;
}

export function here({ p }) {
  return peek({ p: p, n: 0 }).start;
}

export function prev({ p }) {
  if (p.pos === 0) {
    return peek({ p: p, n: 0 });
  }
  return $std_list.get({ xs: p.toks, i: p.pos - 1 });
}

export function span_from({ p, start }) {
  const end = prev({ p: p }).end;
  if (end < start) {
    return { start: start, end: start };
  }
  return { start: start, end: end };
}

export function span_of({ t }) {
  return { start: t.start, end: t.end };
}

export function join_spans({ a, b }) {
  let start = a.start;
  if (b.start < start) {
    start = b.start;
  }
  let end = a.end;
  if (b.end > end) {
    end = b.end;
  }
  return { start: start, end: end };
}

export function ident_of({ t }) {
  const $m12 = t.kind;
  $m12$match: {
    if ($m12.tag === "Name") {
      const text = $m12.text;
      return { text: text, span: span_of({ t: t }) };
      break $m12$match;
    }
    if ($m12.tag === "TName") {
      const text = $m12.text;
      return { text: text, span: span_of({ t: t }) };
      break $m12$match;
    }
    if ($m12.tag === "Keyword") {
      const word = $m12.word;
      return { text: word, span: span_of({ t: t }) };
      break $m12$match;
    }
    if ($m12.tag === "Punct") {
      const text = $m12.text;
      return { text: text, span: span_of({ t: t }) };
      break $m12$match;
    }
    if (true) {
      return { text: "", span: span_of({ t: t }) };
      break $m12$match;
    }
    $rt.unreachable();
  }
}

export function text_value({ t }) {
  const $m18 = t.kind;
  $m18$match: {
    if ($m18.tag === "TextLit") {
      const value = $m18.value;
      return value;
      break $m18$match;
    }
    if (true) {
      return "";
      break $m18$match;
    }
    $rt.unreachable();
  }
}

export function advance({ p }) {
  const t = peek({ p: p, n: 0 });
  if (p.pos < $std_list.len({ xs: p.toks }) - 1) {
    p = { ...p, pos: p.pos + 1 };
  }
  return [t, p];
}

export function current_def_opt({ p }) {
  if (p.current_def === "") {
    return { tag: "None" };
  }
  return { tag: "Some", value: p.current_def };
}

export function source_text({ p, start, end }) {
  let out = $std_list.builder({  });
  let prev_end = $rt.int.neg(1, $ob1);
  for (const t of p.toks) {
    if (t.start >= start && t.end <= end && t.end > t.start) {
      if (prev_end >= 0 && t.start > prev_end) {
        const [, out$2] = $std_list.push({ b: out, x: " " });
        out = out$2;
      }
      const [, out$3] = $std_list.push({ b: out, x: token_text({ t: t }) });
      out = out$3;
      prev_end = t.end;
    }
  }
  return $std_text.join({ parts: $std_list.finish({ b: out }), sep: "" });
}

export function token_text({ t }) {
  const $m22 = t.kind;
  $m22$match: {
    if ($m22.tag === "IntLit") {
      const text = $m22.text;
      return text;
      break $m22$match;
    }
    if ($m22.tag === "TextLit") {
      const value = $m22.value;
      return "\"" + value + "\"";
      break $m22$match;
    }
    if (true) {
      return ident_of({ t: t }).text;
      break $m22$match;
    }
    $rt.unreachable();
  }
}

export function take({ p }) {
  if (p.pos < $std_list.len({ xs: p.toks }) - 1) {
    const t = peek({ p: p, n: 0 });
    p = { ...p, pos: p.pos + 1 };
    return [{ tag: "Ok", value: t }, p];
  }
  const [$r25, p$4] = fail({ p: p, expected: "a token" });
  p = p$4;
  return [{ tag: "Err", error: $r25 }, p];
}

export function flag({ b }) {
  if (b) {
    return 1;
  }
  return 0;
}

export function first_segment({ q }) {
  for (const s of q.segments) {
    return s.text;
  }
  return "";
}

export function report({ p, code, start, end, detail }) {
  p = { ...p, diagnostics: $std_list.append({ xs: p.diagnostics, x: { code: code, start: start, end: end, detail: detail, def_name: current_def_opt({ p: p }) } }) };
  return [undefined, p];
}

export function fail_at({ p, expected, t }) {
  const [, p$5] = report({ p: p, code: "E0003", start: t.start, end: t.end, detail: "expected " + expected + ", found " + $tokens.describe_kind({ kind: t.kind }) });
  p = p$5;
  return [{ tag: "Failed" }, p];
}

export function fail({ p, expected }) {
  const t = peek({ p: p, n: 0 });
  const [$r30, p$6] = fail_at({ p: p, expected: expected, t: t });
  p = p$6;
  return [$r30, p];
}

export function accept_punct({ p, want }) {
  if (at_punct({ p: p, want: want }) && p.pos < $std_list.len({ xs: p.toks }) - 1) {
    const [, p$7] = skip({ p: p });
    p = p$7;
    return [true, p];
  }
  return [false, p];
}

export function accept_word({ p, want }) {
  if (at_word({ p: p, want: want }) && p.pos < $std_list.len({ xs: p.toks }) - 1) {
    const [, p$8] = skip({ p: p });
    p = p$8;
    return [true, p];
  }
  return [false, p];
}

export function accept_newline({ p }) {
  if (at_newline({ p: p }) && p.pos < $std_list.len({ xs: p.toks }) - 1) {
    const [, p$9] = skip({ p: p });
    p = p$9;
    return [true, p];
  }
  return [false, p];
}

export function expect_punct({ p, want }) {
  if (at_punct({ p: p, want: want }) && p.pos < $std_list.len({ xs: p.toks }) - 1) {
    const [$r31, p$10] = advance({ p: p });
    p = p$10;
    const t = $r31;
    return [{ tag: "Ok", value: t }, p];
  }
  const [$r33, p$11] = fail({ p: p, expected: "`" + want + "`" });
  p = p$11;
  return [{ tag: "Err", error: $r33 }, p];
}

export function expect_word({ p, want }) {
  if (at_word({ p: p, want: want }) && p.pos < $std_list.len({ xs: p.toks }) - 1) {
    const [$r35, p$12] = advance({ p: p });
    p = p$12;
    const t = $r35;
    return [{ tag: "Ok", value: t }, p];
  }
  const [$r37, p$13] = fail({ p: p, expected: "`" + want + "`" });
  p = p$13;
  return [{ tag: "Err", error: $r37 }, p];
}

export function expect_newline({ p }) {
  if (at_newline({ p: p }) && p.pos < $std_list.len({ xs: p.toks }) - 1) {
    const [$r39, p$14] = advance({ p: p });
    p = p$14;
    const t = $r39;
    return [{ tag: "Ok", value: t }, p];
  }
  const [$r41, p$15] = fail({ p: p, expected: "newline" });
  p = p$15;
  return [{ tag: "Err", error: $r41 }, p];
}

export function expect_name({ p }) {
  if (at_name({ p: p, n: 0 }) && p.pos < $std_list.len({ xs: p.toks }) - 1) {
    const [$r43, p$16] = advance({ p: p });
    p = p$16;
    const t = $r43;
    return [{ tag: "Ok", value: ident_of({ t: t }) }, p];
  }
  const [$r45, p$17] = fail({ p: p, expected: "a name" });
  p = p$17;
  return [{ tag: "Err", error: $r45 }, p];
}

export function expect_tname({ p }) {
  if (at_tname({ p: p, n: 0 }) && p.pos < $std_list.len({ xs: p.toks }) - 1) {
    const [$r47, p$18] = advance({ p: p });
    p = p$18;
    const t = $r47;
    return [{ tag: "Ok", value: ident_of({ t: t }) }, p];
  }
  const [$r49, p$19] = fail({ p: p, expected: "a type name" });
  p = p$19;
  return [{ tag: "Err", error: $r49 }, p];
}

export function skip({ p }) {
  const [$r51, p$20] = advance({ p: p });
  p = p$20;
  const t = $r51;
  return [undefined, p];
}

export function skip_newline({ p }) {
  const [$r52, p$21] = accept_newline({ p: p });
  p = p$21;
  const seen = $r52;
  return [undefined, p];
}

export function need_punct({ p, want }) {
  try {
    const [$r53, p$22] = expect_punct({ p: p, want: want });
    p = p$22;
    const t = $rt.unwrap($r53);
    return [{ tag: "Ok", value: undefined }, p];
  } catch ($e) {
    if ($e instanceof $rt.EarlyReturn) return [$e.value, p];
    throw $e;
  }
}

export function need_word({ p, want }) {
  try {
    const [$r55, p$23] = expect_word({ p: p, want: want });
    p = p$23;
    const t = $rt.unwrap($r55);
    return [{ tag: "Ok", value: undefined }, p];
  } catch ($e) {
    if ($e instanceof $rt.EarlyReturn) return [$e.value, p];
    throw $e;
  }
}

export function need_newline({ p }) {
  try {
    const [$r57, p$24] = expect_newline({ p: p });
    p = p$24;
    const t = $rt.unwrap($r57);
    return [{ tag: "Ok", value: undefined }, p];
  } catch ($e) {
    if ($e instanceof $rt.EarlyReturn) return [$e.value, p];
    throw $e;
  }
}

export function terminator({ p, in_block }) {
  try {
    if (in_block && at_punct({ p: p, want: "}" }) || at_eof({ p: p })) {
      return [{ tag: "Ok", value: undefined }, p];
    }
    if (prev({ p: p }).kind.tag === "Newline") {
      return [{ tag: "Ok", value: undefined }, p];
    }
    const [$r61, p$25] = need_newline({ p: p });
    p = p$25;
    $rt.unwrap($r61);
    return [{ tag: "Ok", value: undefined }, p];
  } catch ($e) {
    if ($e instanceof $rt.EarlyReturn) return [$e.value, p];
    throw $e;
  }
}

export function recover_to_line_end({ p }) {
  const start = p.pos;
  const toks0 = p.toks;
  while (p.pos < $std_list.len({ xs: p.toks }) - 1 && !at_newline({ p: p }) && !at_punct({ p: p, want: "}" })) {
    const [, p$26] = skip({ p: p });
    p = p$26;
  }
  return [undefined, p];
}

export function recover_to_item({ p }) {
  const start = p.pos;
  const toks0 = p.toks;
  while (p.pos < $std_list.len({ xs: p.toks }) - 1) {
    if (at_newline({ p: p }) && starts_item({ kind: kind_at({ p: p, n: 1 }) })) {
      const [, p$27] = skip({ p: p });
      p = p$27;
      return [undefined, p];
    }
    const [, p$28] = skip({ p: p });
    p = p$28;
  }
  return [undefined, p];
}

export function starts_item({ kind }) {
  const $m65 = kind;
  $m65$match: {
    if ($m65.tag === "Keyword") {
      const word = $m65.word;
      return word === "pub" || word === "sealed" || word === "fn" || word === "const" || word === "type" || word === "record" || word === "union" || word === "interface" || word === "impl" || word === "claim" || word === "capability" || word === "path" || word === "policy" || word === "example" || word === "property";
      break $m65$match;
    }
    if (true) {
      return false;
      break $m65$match;
    }
    $rt.unreachable();
  }
}

export function qname_of({ segments }) {
  const first = $std_list.get({ xs: segments, i: 0 });
  const last = $std_list.get({ xs: segments, i: $rt.int.sub($std_list.len({ xs: segments }), 1, $ob2) });
  return { segments: segments, span: join_spans({ a: first.span, b: last.span }) };
}

export function module_name({ p }) {
  try {
    let segs = $std_list.builder({  });
    const [$r67, p$29] = expect_name({ p: p });
    p = p$29;
    const first = $rt.unwrap($r67);
    const [, segs$30] = $std_list.push({ b: segs, x: first });
    segs = segs$30;
    const start = p.pos;
    const toks0 = p.toks;
    while (at_punct({ p: p, want: "." }) && at_name({ p: p, n: 1 }) && p.pos < $std_list.len({ xs: p.toks }) - 2) {
      const [, p$31] = skip({ p: p });
      p = p$31;
      const [$r69, p$32] = expect_name({ p: p });
      p = p$32;
      const seg = $rt.unwrap($r69);
      const [, segs$33] = $std_list.push({ b: segs, x: seg });
      segs = segs$33;
    }
    return [{ tag: "Ok", value: qname_of({ segments: $std_list.finish({ b: segs }) }) }, p];
  } catch ($e) {
    if ($e instanceof $rt.EarlyReturn) return [$e.value, p];
    throw $e;
  }
}

export function dotted({ p }) {
  try {
    let segs = $std_list.builder({  });
    if (at_name({ p: p, n: 0 }) || at_tname({ p: p, n: 0 })) {
      const [$r71, p$34] = take({ p: p });
      p = p$34;
      const t = $rt.unwrap($r71);
      const [, segs$35] = $std_list.push({ b: segs, x: ident_of({ t: t }) });
      segs = segs$35;
    } else {
      const [$r72, p$36] = fail({ p: p, expected: "a name" });
      p = p$36;
      return [{ tag: "Err", error: $r72 }, p];
    }
    const start = p.pos;
    const toks0 = p.toks;
    while (at_punct({ p: p, want: "." }) && (at_name({ p: p, n: 1 }) || at_tname({ p: p, n: 1 })) && p.pos < $std_list.len({ xs: p.toks }) - 2) {
      const [, p$37] = skip({ p: p });
      p = p$37;
      const [$r75, p$38] = advance({ p: p });
      p = p$38;
      const t = $r75;
      const [, segs$39] = $std_list.push({ b: segs, x: ident_of({ t: t }) });
      segs = segs$39;
    }
    return [{ tag: "Ok", value: qname_of({ segments: $std_list.finish({ b: segs }) }) }, p];
  } catch ($e) {
    if ($e instanceof $rt.EarlyReturn) return [$e.value, p];
    throw $e;
  }
}

export function qtname_ahead({ p }) {
  if (at_tname({ p: p, n: 0 })) {
    return true;
  }
  if (!at_name({ p: p, n: 0 })) {
    return false;
  }
  if (at_punct_n({ p: p, n: 1, want: "." })) {
    if (at_tname({ p: p, n: 2 })) {
      return true;
    }
    if (at_name({ p: p, n: 2 }) && at_punct_n({ p: p, n: 3, want: "." }) && at_tname({ p: p, n: 4 })) {
      return true;
    }
  }
  return false;
}

export function qtname({ p }) {
  try {
    let segs = $std_list.builder({  });
    const start = p.pos;
    const toks0 = p.toks;
    while (at_name({ p: p, n: 0 }) && p.pos < $std_list.len({ xs: p.toks }) - 1) {
      const [$r78, p$40] = expect_name({ p: p });
      p = p$40;
      const seg = $rt.unwrap($r78);
      const [, segs$41] = $std_list.push({ b: segs, x: seg });
      segs = segs$41;
      const [$r79, p$42] = need_punct({ p: p, want: "." });
      p = p$42;
      $rt.unwrap($r79);
    }
    const [$r80, p$43] = expect_tname({ p: p });
    p = p$43;
    const last = $rt.unwrap($r80);
    const [, segs$44] = $std_list.push({ b: segs, x: last });
    segs = segs$44;
    return [{ tag: "Ok", value: qname_of({ segments: $std_list.finish({ b: segs }) }) }, p];
  } catch ($e) {
    if ($e instanceof $rt.EarlyReturn) return [$e.value, p];
    throw $e;
  }
}

export function parse_module({ p }) {
  try {
    let is_test = false;
    const [$r82, p$45] = accept_word({ p: p, want: "test" });
    p = p$45;
    if ($r82) {
      is_test = true;
    }
    p = { ...p, is_test_module: is_test };
    const [$r84, p$46] = need_word({ p: p, want: "module" });
    p = p$46;
    $rt.unwrap($r84);
    const [$r85, p$47] = module_name({ p: p });
    p = p$47;
    const name = $rt.unwrap($r85);
    p = { ...p, is_std_module: first_segment({ q: name }) === "std" };
    const [$r87, p$48] = need_newline({ p: p });
    p = p$48;
    $rt.unwrap($r87);
    let imports = $std_list.builder({  });
    const toks0 = p.toks;
    const start0 = p.pos;
    while (at_word({ p: p, want: "import" }) && p.pos < $std_list.len({ xs: p.toks }) - 1) {
      const s = here({ p: p });
      const [, p$49] = skip({ p: p });
      p = p$49;
      const [$r89, p$50] = module_name({ p: p });
      p = p$50;
      const n = $rt.unwrap($r89);
      const [$r90, p$51] = need_newline({ p: p });
      p = p$51;
      $rt.unwrap($r90);
      const [, imports$52] = $std_list.push({ b: imports, x: { name: n, span: span_from({ p: p, start: s }) } });
      imports = imports$52;
    }
    let items = $std_list.builder({  });
    const start1 = p.pos;
    while (!at_eof({ p: p }) && p.pos < $std_list.len({ xs: p.toks }) - 1) {
      const before = p.pos;
      const [$r94, p$53] = parse_item({ p: p });
      p = p$53;
      const $m93 = $r94;
      $m93$match: {
        if ($m93.tag === "Ok") {
          const value = $m93.value;
          const [, items$54] = $std_list.push({ b: items, x: value });
          items = items$54;
          const [$r95, p$55] = terminator({ p: p, in_block: false });
          p = p$55;
          const term = $r95;
          if (term.tag === "Ok") {
            p = { ...p, current_def: "" };
          } else {
            if (p.pos === before) {
              const [, p$56] = skip({ p: p });
              p = p$56;
            }
            const [, p$57] = recover_to_item({ p: p });
            p = p$57;
          }
          break $m93$match;
        }
        if ($m93.tag === "Err") {
          const error = $m93.error;
          if (p.pos === before) {
            const [, p$58] = skip({ p: p });
            p = p$58;
          }
          const [, p$59] = recover_to_item({ p: p });
          p = p$59;
          break $m93$match;
        }
        $rt.unreachable();
      }
      p = { ...p, current_def: "", no_brace: false, assertion_block: false };
      if (p.pos === before) {
        const [, p$60] = skip({ p: p });
        p = p$60;
      }
    }
    return [{ tag: "Ok", value: { is_test: is_test, name: name, imports: $std_list.finish({ b: imports }), items: $std_list.finish({ b: items }), span: { start: 0, end: p.source_end } } }, p];
  } catch ($e) {
    if ($e instanceof $rt.EarlyReturn) return [$e.value, p];
    throw $e;
  }
}

export function visibility({ p }) {
  const [$r101, p$61] = accept_word({ p: p, want: "pub" });
  p = p$61;
  const is_pub = $r101;
  const [$r102, p$62] = accept_word({ p: p, want: "sealed" });
  p = p$62;
  const is_sealed = $r102;
  return [{ is_pub: is_pub, is_sealed: is_sealed }, p];
}

export function parse_item({ p }) {
  try {
    const start = here({ p: p });
    if (at_word({ p: p, want: "impl" })) {
      const [$r104, p$63] = parse_impl({ p: p, start: start });
      p = p$63;
      const d = $rt.unwrap($r104);
      return [{ tag: "Ok", value: { tag: "ImplItem", decl: d } }, p];
    }
    if (at_word({ p: p, want: "path" })) {
      const [$r107, p$64] = parse_path({ p: p, start: start });
      p = p$64;
      const d = $rt.unwrap($r107);
      return [{ tag: "Ok", value: { tag: "PathItem", decl: d } }, p];
    }
    if (at_word({ p: p, want: "policy" })) {
      const [$r110, p$65] = parse_policy({ p: p, start: start });
      p = p$65;
      const d = $rt.unwrap($r110);
      return [{ tag: "Ok", value: { tag: "PolicyItem", decl: d } }, p];
    }
    if (at_word({ p: p, want: "example" })) {
      const [$r113, p$66] = parse_example({ p: p, start: start });
      p = p$66;
      const d = $rt.unwrap($r113);
      return [{ tag: "Ok", value: { tag: "ExampleItem", decl: d } }, p];
    }
    if (at_word({ p: p, want: "property" })) {
      const [$r116, p$67] = parse_property({ p: p, start: start });
      p = p$67;
      const d = $rt.unwrap($r116);
      return [{ tag: "Ok", value: { tag: "PropertyItem", decl: d } }, p];
    }
    const [$r119, p$68] = visibility({ p: p });
    p = p$68;
    const vis = $r119;
    if (at_word({ p: p, want: "fn" })) {
      const [$r120, p$69] = parse_fn({ p: p, start: start, vis: vis, is_const: false, is_intrinsic: false });
      p = p$69;
      const d = $rt.unwrap($r120);
      return [{ tag: "Ok", value: { tag: "FnItem", decl: d } }, p];
    }
    if (at_word({ p: p, want: "const" })) {
      if (at_word_n({ p: p, n: 1, want: "fn" })) {
        const [, p$70] = skip({ p: p });
        p = p$70;
        const [$r123, p$71] = parse_fn({ p: p, start: start, vis: vis, is_const: true, is_intrinsic: false });
        p = p$71;
        const d = $rt.unwrap($r123);
        return [{ tag: "Ok", value: { tag: "FnItem", decl: d } }, p];
      }
      if (at_word_n({ p: p, n: 1, want: "intrinsic" }) && at_word_n({ p: p, n: 2, want: "fn" })) {
        const [, p$72] = skip({ p: p });
        p = p$72;
        const [, p$73] = skip({ p: p });
        p = p$73;
        const [$r126, p$74] = parse_fn({ p: p, start: start, vis: vis, is_const: true, is_intrinsic: true });
        p = p$74;
        const d = $rt.unwrap($r126);
        return [{ tag: "Ok", value: { tag: "FnItem", decl: d } }, p];
      }
      const [$r129, p$75] = parse_const({ p: p, start: start, vis: vis });
      p = p$75;
      const d = $rt.unwrap($r129);
      return [{ tag: "Ok", value: { tag: "ConstItem", decl: d } }, p];
    }
    if (at_word({ p: p, want: "intrinsic" })) {
      if (at_word_n({ p: p, n: 1, want: "fn" })) {
        const [, p$76] = skip({ p: p });
        p = p$76;
        const [$r132, p$77] = parse_fn({ p: p, start: start, vis: vis, is_const: false, is_intrinsic: true });
        p = p$77;
        const d = $rt.unwrap($r132);
        return [{ tag: "Ok", value: { tag: "FnItem", decl: d } }, p];
      }
      if (at_word_n({ p: p, n: 1, want: "type" })) {
        const [, p$78] = skip({ p: p });
        p = p$78;
        const [$r135, p$79] = parse_intrinsic_type({ p: p, start: start, vis: vis });
        p = p$79;
        const d = $rt.unwrap($r135);
        return [{ tag: "Ok", value: { tag: "IntrinsicTypeItem", decl: d } }, p];
      }
      const [, p$80] = skip({ p: p });
      p = p$80;
      const [$r138, p$81] = fail({ p: p, expected: "`fn` or `type` after `intrinsic`" });
      p = p$81;
      return [{ tag: "Err", error: $r138 }, p];
    }
    if (at_word({ p: p, want: "type" })) {
      const [$r140, p$82] = parse_type_alias({ p: p, start: start, vis: vis });
      p = p$82;
      const d = $rt.unwrap($r140);
      return [{ tag: "Ok", value: { tag: "TypeAliasItem", decl: d } }, p];
    }
    if (at_word({ p: p, want: "record" })) {
      const [$r143, p$83] = parse_record({ p: p, start: start, vis: vis });
      p = p$83;
      const d = $rt.unwrap($r143);
      return [{ tag: "Ok", value: { tag: "RecordItem", decl: d } }, p];
    }
    if (at_word({ p: p, want: "union" })) {
      const [$r146, p$84] = parse_union({ p: p, start: start, vis: vis });
      p = p$84;
      const d = $rt.unwrap($r146);
      return [{ tag: "Ok", value: { tag: "UnionItem", decl: d } }, p];
    }
    if (at_word({ p: p, want: "interface" })) {
      const [$r149, p$85] = parse_interface({ p: p, start: start, vis: vis });
      p = p$85;
      const d = $rt.unwrap($r149);
      return [{ tag: "Ok", value: { tag: "InterfaceItem", decl: d } }, p];
    }
    if (at_word({ p: p, want: "claim" })) {
      const [$r152, p$86] = parse_claim({ p: p, start: start, vis: vis });
      p = p$86;
      const d = $rt.unwrap($r152);
      return [{ tag: "Ok", value: { tag: "ClaimItem", decl: d } }, p];
    }
    if (at_word({ p: p, want: "capability" })) {
      const [$r155, p$87] = parse_capability({ p: p, start: start, vis: vis });
      p = p$87;
      const d = $rt.unwrap($r155);
      return [{ tag: "Ok", value: { tag: "CapabilityItem", decl: d } }, p];
    }
    const [$r158, p$88] = fail({ p: p, expected: "an item (fn, type, const, record, union, interface, impl, claim, capability, path, policy, example or property)" });
    p = p$88;
    return [{ tag: "Err", error: $r158 }, p];
  } catch ($e) {
    if ($e instanceof $rt.EarlyReturn) return [$e.value, p];
    throw $e;
  }
}

export function parse_fn({ p, start, vis, is_const, is_intrinsic }) {
  try {
    const [$r160, p$89] = expect_word({ p: p, want: "fn" });
    p = p$89;
    const fn_tok = $rt.unwrap($r160);
    const [$r161, p$90] = expect_name({ p: p });
    p = p$90;
    const name = $rt.unwrap($r161);
    p = { ...p, current_def: name.text };
    if (is_intrinsic && !p.is_std_module) {
      const [, p$91] = report({ p: p, code: "E0102", start: fn_tok.start, end: name.span.end, detail: "intrinsic declarations are reserved for `module std.…`" });
      p = p$91;
    }
    let tparams = [];
    if (at_punct({ p: p, want: "[" })) {
      const [$r163, p$92] = parse_tparams({ p: p });
      p = p$92;
      tparams = $rt.unwrap($r163);
    }
    const [$r164, p$93] = param_list({ p: p, rank: r_param_list });
    p = p$93;
    const params = $rt.unwrap($r164);
    const [$r165, p$94] = need_punct({ p: p, want: "->" });
    p = p$94;
    $rt.unwrap($r165);
    const saved = p.no_brace;
    p = { ...p, no_brace: true };
    const [$r167, p$95] = parse_type({ p: p, rank: r_type });
    p = p$95;
    const ret = $rt.unwrap($r167);
    p = { ...p, no_brace: saved };
    const [$r169, p$96] = effects_opt({ p: p });
    p = p$96;
    const effects = $rt.unwrap($r169);
    let claim_list = $std_list.builder({  });
    const [$r170, p$97] = accept_word({ p: p, want: "claims" });
    p = p$97;
    if ($r170) {
      const [$r171, p$98] = dotted({ p: p });
      p = p$98;
      const first = $rt.unwrap($r171);
      const [, claim_list$99] = $std_list.push({ b: claim_list, x: first });
      claim_list = claim_list$99;
      const toks0 = p.toks;
      const start0 = p.pos;
      while (at_punct({ p: p, want: "," }) && p.pos < $std_list.len({ xs: p.toks }) - 1) {
        const [, p$100] = skip({ p: p });
        p = p$100;
        const [$r173, p$101] = dotted({ p: p });
        p = p$101;
        const more = $rt.unwrap($r173);
        const [, claim_list$102] = $std_list.push({ b: claim_list, x: more });
        claim_list = claim_list$102;
      }
    }
    const [$r174, p$103] = parse_contracts({ p: p });
    p = p$103;
    const contracts = $rt.unwrap($r174);
    let body = { tag: "None" };
    if (is_intrinsic) {
      if (at_punct({ p: p, want: "{" })) {
        const [$r176, p$104] = fail({ p: p, expected: "a newline: an intrinsic function has no body" });
        p = p$104;
        return [{ tag: "Err", error: $r176 }, p];
      }
    } else {
      const [$r178, p$105] = parse_block({ p: p, rank: r_block, allow_elided: true });
      p = p$105;
      const b = $rt.unwrap($r178);
      body = { tag: "Some", value: b };
    }
    return [{ tag: "Ok", value: { vis: vis, is_const: is_const, is_intrinsic: is_intrinsic, name: name, tparams: tparams, params: params, ret: ret, effects: effects, claim_list: $std_list.finish({ b: claim_list }), contracts: contracts, body: body, span: span_from({ p: p, start: start }) } }, p];
  } catch ($e) {
    if ($e instanceof $rt.EarlyReturn) return [$e.value, p];
    throw $e;
  }
}

export function parse_intrinsic_type({ p, start, vis }) {
  try {
    const [$r182, p$106] = expect_word({ p: p, want: "type" });
    p = p$106;
    const type_tok = $rt.unwrap($r182);
    const [$r183, p$107] = expect_tname({ p: p });
    p = p$107;
    const name = $rt.unwrap($r183);
    p = { ...p, current_def: name.text };
    if (!p.is_std_module) {
      const [, p$108] = report({ p: p, code: "E0102", start: type_tok.start, end: name.span.end, detail: "intrinsic declarations are reserved for `module std.…`" });
      p = p$108;
    }
    let tparams = [];
    if (at_punct({ p: p, want: "[" })) {
      const [$r185, p$109] = parse_tparams({ p: p });
      p = p$109;
      tparams = $rt.unwrap($r185);
    }
    return [{ tag: "Ok", value: { vis: vis, name: name, tparams: tparams, span: span_from({ p: p, start: start }) } }, p];
  } catch ($e) {
    if ($e instanceof $rt.EarlyReturn) return [$e.value, p];
    throw $e;
  }
}

export function parse_contracts({ p }) {
  try {
    let out = $std_list.builder({  });
    const toks0 = p.toks;
    const start0 = p.pos;
    while ((at_word({ p: p, want: "requires" }) || at_word({ p: p, want: "ensures" }) || at_word({ p: p, want: "decreases" })) && p.pos < $std_list.len({ xs: p.toks }) - 1) {
      const s = here({ p: p });
      const [$r189, p$110] = advance({ p: p });
      p = p$110;
      const kw = $r189;
      const clause = ident_of({ t: kw }).text;
      let is_proved = false;
      if (clause !== "decreases") {
        const [$r190, p$111] = accept_word({ p: p, want: "proved" });
        p = p$111;
        is_proved = $r190;
      }
      const saved = p.no_brace;
      p = { ...p, no_brace: true };
      const [$r192, p$112] = parse_expr({ p: p, rank: r_expr });
      p = p$112;
      const e = $rt.unwrap($r192);
      p = { ...p, no_brace: saved };
      const [, out$113] = $std_list.push({ b: out, x: { clause: clause, is_proved: is_proved, expr: e, span: span_from({ p: p, start: s }) } });
      out = out$113;
    }
    return [{ tag: "Ok", value: $std_list.finish({ b: out }) }, p];
  } catch ($e) {
    if ($e instanceof $rt.EarlyReturn) return [$e.value, p];
    throw $e;
  }
}

export function effects_opt({ p }) {
  const [$r196, p$114] = accept_word({ p: p, want: "may" });
  p = p$114;
  if (!$r196) {
    return [{ tag: "Ok", value: [] }, p];
  }
  const [$r198, p$115] = effect_list({ p: p });
  p = p$115;
  return [$r198, p];
}

export function effect_list({ p }) {
  try {
    let out = $std_list.builder({  });
    const [$r199, p$116] = effect_ref({ p: p });
    p = p$116;
    const first = $rt.unwrap($r199);
    const [, out$117] = $std_list.push({ b: out, x: first });
    out = out$117;
    const toks0 = p.toks;
    const start0 = p.pos;
    while (at_punct({ p: p, want: "," }) && effect_follows({ p: p }) && p.pos < $std_list.len({ xs: p.toks }) - 1) {
      const [, p$118] = skip({ p: p });
      p = p$118;
      const [$r201, p$119] = effect_ref({ p: p });
      p = p$119;
      const more = $rt.unwrap($r201);
      const [, out$120] = $std_list.push({ b: out, x: more });
      out = out$120;
    }
    return [{ tag: "Ok", value: $std_list.finish({ b: out }) }, p];
  } catch ($e) {
    if ($e instanceof $rt.EarlyReturn) return [$e.value, p];
    throw $e;
  }
}

export function effect_follows({ p }) {
  return (at_name({ p: p, n: 1 }) || at_word_n({ p: p, n: 1, want: "recover" })) && !at_punct_n({ p: p, n: 2, want: ":" });
}

export function effect_ref({ p }) {
  try {
    const s = here({ p: p });
    const t = peek({ p: p, n: 0 });
    const [$r203, p$121] = accept_word({ p: p, want: "recover" });
    p = p$121;
    if ($r203) {
      const id = ident_of({ t: t });
      const name = { segments: [id], span: id.span };
      return [{ tag: "Ok", value: { name: name, span: span_from({ p: p, start: s }) } }, p];
    }
    const [$r207, p$122] = module_name({ p: p });
    p = p$122;
    const name = $rt.unwrap($r207);
    return [{ tag: "Ok", value: { name: name, span: span_from({ p: p, start: s }) } }, p];
  } catch ($e) {
    if ($e instanceof $rt.EarlyReturn) return [$e.value, p];
    throw $e;
  }
}

export function parse_tparams({ p }) {
  try {
    const [$r210, p$123] = need_punct({ p: p, want: "[" });
    p = p$123;
    $rt.unwrap($r210);
    let out = $std_list.builder({  });
    let more = true;
    const toks0 = p.toks;
    const start0 = p.pos;
    while (more && p.pos < $std_list.len({ xs: p.toks }) - 1) {
      const s = here({ p: p });
      if (at_tname({ p: p, n: 0 })) {
        const [$r212, p$124] = expect_tname({ p: p });
        p = p$124;
        const name = $rt.unwrap($r212);
        let bound = { tag: "None" };
        const [$r214, p$125] = accept_punct({ p: p, want: ":" });
        p = p$125;
        if ($r214) {
          const [$r215, p$126] = expect_tname({ p: p });
          p = p$126;
          const b = $rt.unwrap($r215);
          bound = { tag: "Some", value: b };
        }
        const [, out$127] = $std_list.push({ b: out, x: { tag: "TypeParam", name: name, bound: bound, span: span_from({ p: p, start: s }) } });
        out = out$127;
      } else {
        const [$r218, p$128] = accept_word({ p: p, want: "const" });
        p = p$128;
        if ($r218) {
          const [$r219, p$129] = expect_name({ p: p });
          p = p$129;
          const name = $rt.unwrap($r219);
          const [$r220, p$130] = need_punct({ p: p, want: ":" });
          p = p$130;
          $rt.unwrap($r220);
          const [$r221, p$131] = parse_type({ p: p, rank: r_type });
          p = p$131;
          const ty = $rt.unwrap($r221);
          const [, out$132] = $std_list.push({ b: out, x: { tag: "ConstParam", name: name, ty: ty, span: span_from({ p: p, start: s }) } });
          out = out$132;
        } else {
          if (at_name({ p: p, n: 0 })) {
            const [$r223, p$133] = expect_name({ p: p });
            p = p$133;
            const name = $rt.unwrap($r223);
            const [, out$134] = $std_list.push({ b: out, x: { tag: "EffectParam", name: name, span: span_from({ p: p, start: s }) } });
            out = out$134;
          } else {
            const [$r225, p$135] = fail({ p: p, expected: "a type parameter, `const` parameter or effect parameter" });
            p = p$135;
            return [{ tag: "Err", error: $r225 }, p];
          }
        }
      }
      const [$r227, p$136] = accept_punct({ p: p, want: "," });
      p = p$136;
      more = $r227;
    }
    const [$r228, p$137] = need_punct({ p: p, want: "]" });
    p = p$137;
    $rt.unwrap($r228);
    return [{ tag: "Ok", value: $std_list.finish({ b: out }) }, p];
  } catch ($e) {
    if ($e instanceof $rt.EarlyReturn) return [$e.value, p];
    throw $e;
  }
}

export function param_list({ p, rank }) {
  try {
    const [$r230, p$138] = need_punct({ p: p, want: "(" });
    p = p$138;
    $rt.unwrap($r230);
    let out = $std_list.builder({  });
    if (!at_punct({ p: p, want: ")" })) {
      let more = true;
      const toks0 = p.toks;
      const start0 = p.pos;
      while (more && p.pos < $std_list.len({ xs: p.toks }) - 1) {
        const [$r232, p$139] = parse_param({ p: p, rank: r_param });
        p = p$139;
        const param = $rt.unwrap($r232);
        const [, out$140] = $std_list.push({ b: out, x: param });
        out = out$140;
        const [$r233, p$141] = accept_punct({ p: p, want: "," });
        p = p$141;
        more = $r233;
      }
    }
    const [$r234, p$142] = need_punct({ p: p, want: ")" });
    p = p$142;
    $rt.unwrap($r234);
    return [{ tag: "Ok", value: $std_list.finish({ b: out }) }, p];
  } catch ($e) {
    if ($e instanceof $rt.EarlyReturn) return [$e.value, p];
    throw $e;
  }
}

export function parse_param({ p, rank }) {
  try {
    const s = here({ p: p });
    const [$r236, p$143] = expect_name({ p: p });
    p = p$143;
    const name = $rt.unwrap($r236);
    const [$r237, p$144] = need_punct({ p: p, want: ":" });
    p = p$144;
    $rt.unwrap($r237);
    const [$r238, p$145] = accept_word({ p: p, want: "inout" });
    p = p$145;
    const is_inout = $r238;
    const saved = p.no_brace;
    p = { ...p, no_brace: false };
    const [$r240, p$146] = parse_type({ p: p, rank: r_type });
    p = p$146;
    const ty = $rt.unwrap($r240);
    p = { ...p, no_brace: saved };
    return [{ tag: "Ok", value: { is_inout: is_inout, name: name, ty: ty, span: span_from({ p: p, start: s }) } }, p];
  } catch ($e) {
    if ($e instanceof $rt.EarlyReturn) return [$e.value, p];
    throw $e;
  }
}

export function parse_const({ p, start, vis }) {
  try {
    const [$r244, p$147] = need_word({ p: p, want: "const" });
    p = p$147;
    $rt.unwrap($r244);
    const [$r245, p$148] = expect_name({ p: p });
    p = p$148;
    const name = $rt.unwrap($r245);
    p = { ...p, current_def: name.text };
    const [$r247, p$149] = need_punct({ p: p, want: ":" });
    p = p$149;
    $rt.unwrap($r247);
    const [$r248, p$150] = parse_type({ p: p, rank: r_type });
    p = p$150;
    const ty = $rt.unwrap($r248);
    const [$r249, p$151] = need_punct({ p: p, want: "=" });
    p = p$151;
    $rt.unwrap($r249);
    const [$r250, p$152] = parse_expr({ p: p, rank: r_expr });
    p = p$152;
    const value = $rt.unwrap($r250);
    return [{ tag: "Ok", value: { vis: vis, name: name, ty: ty, value: value, span: span_from({ p: p, start: start }) } }, p];
  } catch ($e) {
    if ($e instanceof $rt.EarlyReturn) return [$e.value, p];
    throw $e;
  }
}

export function parse_type_alias({ p, start, vis }) {
  try {
    const [$r253, p$153] = need_word({ p: p, want: "type" });
    p = p$153;
    $rt.unwrap($r253);
    const [$r254, p$154] = expect_tname({ p: p });
    p = p$154;
    const name = $rt.unwrap($r254);
    p = { ...p, current_def: name.text };
    const [$r256, p$155] = need_punct({ p: p, want: "=" });
    p = p$155;
    $rt.unwrap($r256);
    const [$r257, p$156] = parse_type({ p: p, rank: r_type });
    p = p$156;
    const ty = $rt.unwrap($r257);
    return [{ tag: "Ok", value: { vis: vis, name: name, ty: ty, span: span_from({ p: p, start: start }) } }, p];
  } catch ($e) {
    if ($e instanceof $rt.EarlyReturn) return [$e.value, p];
    throw $e;
  }
}

export function parse_record({ p, start, vis }) {
  try {
    const [$r260, p$157] = need_word({ p: p, want: "record" });
    p = p$157;
    $rt.unwrap($r260);
    const [$r261, p$158] = expect_tname({ p: p });
    p = p$158;
    const name = $rt.unwrap($r261);
    p = { ...p, current_def: name.text };
    let tparams = [];
    if (at_punct({ p: p, want: "[" })) {
      const [$r263, p$159] = parse_tparams({ p: p });
      p = p$159;
      tparams = $rt.unwrap($r263);
    }
    const [$r264, p$160] = braced_fields({ p: p });
    p = p$160;
    const fields = $rt.unwrap($r264);
    return [{ tag: "Ok", value: { vis: vis, name: name, tparams: tparams, fields: fields, span: span_from({ p: p, start: start }) } }, p];
  } catch ($e) {
    if ($e instanceof $rt.EarlyReturn) return [$e.value, p];
    throw $e;
  }
}

export function parse_field({ p }) {
  try {
    const s = here({ p: p });
    const [$r267, p$161] = expect_name({ p: p });
    p = p$161;
    const name = $rt.unwrap($r267);
    const [$r268, p$162] = need_punct({ p: p, want: ":" });
    p = p$162;
    $rt.unwrap($r268);
    const [$r269, p$163] = parse_type({ p: p, rank: r_type });
    p = p$163;
    const ty = $rt.unwrap($r269);
    return [{ tag: "Ok", value: { name: name, ty: ty, span: span_from({ p: p, start: s }) } }, p];
  } catch ($e) {
    if ($e instanceof $rt.EarlyReturn) return [$e.value, p];
    throw $e;
  }
}

export function braced_fields({ p }) {
  try {
    const [$r272, p$164] = need_punct({ p: p, want: "{" });
    p = p$164;
    $rt.unwrap($r272);
    let out = $std_list.builder({  });
    const [$r273, p$165] = accept_newline({ p: p });
    p = p$165;
    if ($r273) {
      const toks0 = p.toks;
      const start0 = p.pos;
      while (!at_punct({ p: p, want: "}" }) && !at_eof({ p: p }) && p.pos < $std_list.len({ xs: p.toks }) - 1) {
        const before = p.pos;
        const [$r276, p$166] = parse_field({ p: p });
        p = p$166;
        const $m275 = $r276;
        $m275$match: {
          if ($m275.tag === "Ok") {
            const value = $m275.value;
            const [, out$167] = $std_list.push({ b: out, x: value });
            out = out$167;
            const [$r277, p$168] = terminator({ p: p, in_block: true });
            p = p$168;
            const term = $r277;
            if (term.tag === "Ok") {
              p = p;
            } else {
              const [, p$169] = recover_to_line_end({ p: p });
              p = p$169;
              const [, p$170] = skip_newline({ p: p });
              p = p$170;
            }
            break $m275$match;
          }
          if ($m275.tag === "Err") {
            const error = $m275.error;
            if (p.pos === before) {
              const [, p$171] = skip({ p: p });
              p = p$171;
            }
            const [, p$172] = recover_to_line_end({ p: p });
            p = p$172;
            const [, p$173] = skip_newline({ p: p });
            p = p$173;
            break $m275$match;
          }
          $rt.unreachable();
        }
        if (p.pos === before) {
          const [, p$174] = skip({ p: p });
          p = p$174;
        }
      }
      const [$r278, p$175] = need_punct({ p: p, want: "}" });
      p = p$175;
      $rt.unwrap($r278);
      return [{ tag: "Ok", value: $std_list.finish({ b: out }) }, p];
    }
    const [$r280, p$176] = accept_punct({ p: p, want: "}" });
    p = p$176;
    if ($r280) {
      return [{ tag: "Ok", value: $std_list.finish({ b: out }) }, p];
    }
    const [$r282, p$177] = parse_field({ p: p });
    p = p$177;
    const one = $rt.unwrap($r282);
    const [, out$178] = $std_list.push({ b: out, x: one });
    out = out$178;
    const [$r283, p$179] = need_punct({ p: p, want: "}" });
    p = p$179;
    $rt.unwrap($r283);
    return [{ tag: "Ok", value: $std_list.finish({ b: out }) }, p];
  } catch ($e) {
    if ($e instanceof $rt.EarlyReturn) return [$e.value, p];
    throw $e;
  }
}

export function parse_union({ p, start, vis }) {
  try {
    const [$r285, p$180] = need_word({ p: p, want: "union" });
    p = p$180;
    $rt.unwrap($r285);
    const [$r286, p$181] = expect_tname({ p: p });
    p = p$181;
    const name = $rt.unwrap($r286);
    p = { ...p, current_def: name.text };
    let tparams = [];
    if (at_punct({ p: p, want: "[" })) {
      const [$r288, p$182] = parse_tparams({ p: p });
      p = p$182;
      tparams = $rt.unwrap($r288);
    }
    const [$r289, p$183] = need_punct({ p: p, want: "=" });
    p = p$183;
    $rt.unwrap($r289);
    const [$r290, p$184] = need_newline({ p: p });
    p = p$184;
    $rt.unwrap($r290);
    let variants = $std_list.builder({  });
    let end = prev({ p: p }).end;
    const toks0 = p.toks;
    const start0 = p.pos;
    while (at_punct({ p: p, want: "|" }) && p.pos < $std_list.len({ xs: p.toks }) - 1) {
      const s = here({ p: p });
      const [, p$185] = skip({ p: p });
      p = p$185;
      const [$r292, p$186] = expect_tname({ p: p });
      p = p$186;
      const vname = $rt.unwrap($r292);
      let fields = $std_list.builder({  });
      const [$r293, p$187] = accept_word({ p: p, want: "of" });
      p = p$187;
      if ($r293) {
        const [$r294, p$188] = parse_field({ p: p });
        p = p$188;
        const first = $rt.unwrap($r294);
        const [, fields$189] = $std_list.push({ b: fields, x: first });
        fields = fields$189;
        const start1 = p.pos;
        while (at_punct({ p: p, want: "," }) && p.pos < $std_list.len({ xs: p.toks }) - 1) {
          const [, p$190] = skip({ p: p });
          p = p$190;
          const [$r296, p$191] = parse_field({ p: p });
          p = p$191;
          const more = $rt.unwrap($r296);
          const [, fields$192] = $std_list.push({ b: fields, x: more });
          fields = fields$192;
        }
      }
      const vspan = span_from({ p: p, start: s });
      end = vspan.end;
      const [, variants$193] = $std_list.push({ b: variants, x: { name: vname, fields: $std_list.finish({ b: fields }), span: vspan } });
      variants = variants$193;
      const [$r298, p$194] = need_newline({ p: p });
      p = p$194;
      $rt.unwrap($r298);
    }
    return [{ tag: "Ok", value: { vis: vis, name: name, tparams: tparams, variants: $std_list.finish({ b: variants }), span: { start: start, end: end } } }, p];
  } catch ($e) {
    if ($e instanceof $rt.EarlyReturn) return [$e.value, p];
    throw $e;
  }
}

export function parse_interface({ p, start, vis }) {
  try {
    const [$r302, p$195] = need_word({ p: p, want: "interface" });
    p = p$195;
    $rt.unwrap($r302);
    const [$r303, p$196] = expect_tname({ p: p });
    p = p$196;
    const name = $rt.unwrap($r303);
    p = { ...p, current_def: name.text };
    const [$r305, p$197] = need_punct({ p: p, want: "[" });
    p = p$197;
    $rt.unwrap($r305);
    const [$r306, p$198] = expect_tname({ p: p });
    p = p$198;
    const tparam = $rt.unwrap($r306);
    const [$r307, p$199] = need_punct({ p: p, want: "]" });
    p = p$199;
    $rt.unwrap($r307);
    const [$r308, p$200] = need_punct({ p: p, want: "{" });
    p = p$200;
    $rt.unwrap($r308);
    let items = $std_list.builder({  });
    const [$r309, p$201] = accept_newline({ p: p });
    p = p$201;
    if ($r309) {
      const toks0 = p.toks;
      const start0 = p.pos;
      while (!at_punct({ p: p, want: "}" }) && !at_eof({ p: p }) && p.pos < $std_list.len({ xs: p.toks }) - 1) {
        const before = p.pos;
        const [$r312, p$202] = iface_item({ p: p });
        p = p$202;
        const $m311 = $r312;
        $m311$match: {
          if ($m311.tag === "Ok") {
            const value = $m311.value;
            const [, items$203] = $std_list.push({ b: items, x: value });
            items = items$203;
            const [$r313, p$204] = terminator({ p: p, in_block: true });
            p = p$204;
            const term = $r313;
            if (term.tag === "Ok") {
              p = p;
            } else {
              const [, p$205] = recover_to_line_end({ p: p });
              p = p$205;
              const [, p$206] = skip_newline({ p: p });
              p = p$206;
            }
            break $m311$match;
          }
          if ($m311.tag === "Err") {
            const error = $m311.error;
            if (p.pos === before) {
              const [, p$207] = skip({ p: p });
              p = p$207;
            }
            const [, p$208] = recover_to_line_end({ p: p });
            p = p$208;
            const [, p$209] = skip_newline({ p: p });
            p = p$209;
            break $m311$match;
          }
          $rt.unreachable();
        }
        if (p.pos === before) {
          const [, p$210] = skip({ p: p });
          p = p$210;
        }
      }
      const [$r314, p$211] = need_punct({ p: p, want: "}" });
      p = p$211;
      $rt.unwrap($r314);
    } else {
      const [$r315, p$212] = accept_punct({ p: p, want: "}" });
      p = p$212;
      if (!$r315) {
        const [$r316, p$213] = iface_item({ p: p });
        p = p$213;
        const one = $rt.unwrap($r316);
        const [, items$214] = $std_list.push({ b: items, x: one });
        items = items$214;
        const [$r317, p$215] = need_punct({ p: p, want: "}" });
        p = p$215;
        $rt.unwrap($r317);
      }
    }
    return [{ tag: "Ok", value: { vis: vis, name: name, tparam: tparam, items: $std_list.finish({ b: items }), span: span_from({ p: p, start: start }) } }, p];
  } catch ($e) {
    if ($e instanceof $rt.EarlyReturn) return [$e.value, p];
    throw $e;
  }
}

export function iface_item({ p }) {
  try {
    const s = here({ p: p });
    const [$r320, p$216] = accept_word({ p: p, want: "law" });
    p = p$216;
    if ($r320) {
      const [$r321, p$217] = expect_name({ p: p });
      p = p$217;
      const name = $rt.unwrap($r321);
      const [$r322, p$218] = param_list({ p: p, rank: r_param_list });
      p = p$218;
      const params = $rt.unwrap($r322);
      const [$r323, p$219] = parse_assertion_block({ p: p, rank: r_block });
      p = p$219;
      const body = $rt.unwrap($r323);
      return [{ tag: "Ok", value: { tag: "LawItem", decl: { name: name, params: params, body: body, span: span_from({ p: p, start: s }) } } }, p];
    }
    const [$r327, p$220] = need_word({ p: p, want: "fn" });
    p = p$220;
    $rt.unwrap($r327);
    const [$r328, p$221] = expect_name({ p: p });
    p = p$221;
    const name = $rt.unwrap($r328);
    const [$r329, p$222] = param_list({ p: p, rank: r_param_list });
    p = p$222;
    const params = $rt.unwrap($r329);
    const [$r330, p$223] = need_punct({ p: p, want: "->" });
    p = p$223;
    $rt.unwrap($r330);
    const saved = p.no_brace;
    p = { ...p, no_brace: true };
    const [$r332, p$224] = parse_type({ p: p, rank: r_type });
    p = p$224;
    const ret = $rt.unwrap($r332);
    p = { ...p, no_brace: saved };
    const [$r334, p$225] = effects_opt({ p: p });
    p = p$225;
    const effects = $rt.unwrap($r334);
    const [$r335, p$226] = parse_contracts({ p: p });
    p = p$226;
    const contracts = $rt.unwrap($r335);
    return [{ tag: "Ok", value: { tag: "IfaceFnItem", decl: { name: name, params: params, ret: ret, effects: effects, contracts: contracts, span: span_from({ p: p, start: s }) } } }, p];
  } catch ($e) {
    if ($e instanceof $rt.EarlyReturn) return [$e.value, p];
    throw $e;
  }
}

export function parse_impl({ p, start }) {
  try {
    const [$r339, p$227] = need_word({ p: p, want: "impl" });
    p = p$227;
    $rt.unwrap($r339);
    const [$r340, p$228] = expect_tname({ p: p });
    p = p$228;
    const iface = $rt.unwrap($r340);
    const [$r341, p$229] = need_punct({ p: p, want: "[" });
    p = p$229;
    $rt.unwrap($r341);
    const target_start = here({ p: p });
    const [$r342, p$230] = parse_type({ p: p, rank: r_type });
    p = p$230;
    const target = $rt.unwrap($r342);
    const target_end = prev({ p: p }).end;
    const [$r343, p$231] = need_punct({ p: p, want: "]" });
    p = p$231;
    $rt.unwrap($r343);
    const impl_name = iface.text + "[" + source_text({ p: p, start: target_start, end: target_end }) + "]";
    p = { ...p, current_def: impl_name };
    const [$r345, p$232] = need_punct({ p: p, want: "{" });
    p = p$232;
    $rt.unwrap($r345);
    let fns = $std_list.builder({  });
    const [$r346, p$233] = accept_newline({ p: p });
    p = p$233;
    if ($r346) {
      const toks0 = p.toks;
      const start0 = p.pos;
      while (!at_punct({ p: p, want: "}" }) && !at_eof({ p: p }) && p.pos < $std_list.len({ xs: p.toks }) - 1) {
        const before = p.pos;
        const s = here({ p: p });
        const [$r348, p$234] = visibility({ p: p });
        p = p$234;
        const vis = $r348;
        const [$r350, p$235] = parse_fn({ p: p, start: s, vis: vis, is_const: false, is_intrinsic: false });
        p = p$235;
        const $m349 = $r350;
        $m349$match: {
          if ($m349.tag === "Ok") {
            const value = $m349.value;
            const [, fns$236] = $std_list.push({ b: fns, x: value });
            fns = fns$236;
            const [$r351, p$237] = terminator({ p: p, in_block: true });
            p = p$237;
            const term = $r351;
            if (term.tag === "Ok") {
              p = p;
            } else {
              const [, p$238] = recover_to_line_end({ p: p });
              p = p$238;
              const [, p$239] = skip_newline({ p: p });
              p = p$239;
            }
            break $m349$match;
          }
          if ($m349.tag === "Err") {
            const error = $m349.error;
            if (p.pos === before) {
              const [, p$240] = skip({ p: p });
              p = p$240;
            }
            const [, p$241] = recover_to_line_end({ p: p });
            p = p$241;
            const [, p$242] = skip_newline({ p: p });
            p = p$242;
            break $m349$match;
          }
          $rt.unreachable();
        }
        if (p.pos === before) {
          const [, p$243] = skip({ p: p });
          p = p$243;
        }
      }
      const [$r352, p$244] = need_punct({ p: p, want: "}" });
      p = p$244;
      $rt.unwrap($r352);
    } else {
      const [$r353, p$245] = accept_punct({ p: p, want: "}" });
      p = p$245;
      if (!$r353) {
        const s = here({ p: p });
        const [$r354, p$246] = visibility({ p: p });
        p = p$246;
        const vis = $r354;
        const [$r355, p$247] = parse_fn({ p: p, start: s, vis: vis, is_const: false, is_intrinsic: false });
        p = p$247;
        const one = $rt.unwrap($r355);
        const [, fns$248] = $std_list.push({ b: fns, x: one });
        fns = fns$248;
        const [$r356, p$249] = need_punct({ p: p, want: "}" });
        p = p$249;
        $rt.unwrap($r356);
      }
    }
    return [{ tag: "Ok", value: { iface: iface, target: target, fns: $std_list.finish({ b: fns }), span: span_from({ p: p, start: start }) } }, p];
  } catch ($e) {
    if ($e instanceof $rt.EarlyReturn) return [$e.value, p];
    throw $e;
  }
}

export function parse_claim({ p, start, vis }) {
  try {
    const [$r359, p$250] = need_word({ p: p, want: "claim" });
    p = p$250;
    $rt.unwrap($r359);
    let name = { text: "", span: { start: 0, end: 0 } };
    if (at_name({ p: p, n: 0 })) {
      const [$r362, p$251] = advance({ p: p });
      p = p$251;
      const t = $r362;
      name = ident_of({ t: t });
    } else {
      const [$r363, p$252] = expect_tname({ p: p });
      p = p$252;
      name = $rt.unwrap($r363);
    }
    p = { ...p, current_def: name.text };
    const [$r365, p$253] = accept_punct({ p: p, want: ":=" });
    p = p$253;
    if ($r365) {
      const [$r366, p$254] = claim_or({ p: p, rank: r_claim_or });
      p = p$254;
      const pred = $rt.unwrap($r366);
      return [{ tag: "Ok", value: { vis: vis, name: name, body: { tag: "Derived", pred: pred }, span: span_from({ p: p, start: start }) } }, p];
    }
    if (kind_at({ p: p, n: 0 }).tag === "TextLit") {
      const [$r370, p$255] = advance({ p: p });
      p = p$255;
      const t = $r370;
      return [{ tag: "Ok", value: { vis: vis, name: name, body: { tag: "Asserted", description: text_value({ t: t }) }, span: span_from({ p: p, start: start }) } }, p];
    }
    const [$r374, p$256] = fail({ p: p, expected: "`:=` or a description text" });
    p = p$256;
    return [{ tag: "Err", error: $r374 }, p];
  } catch ($e) {
    if ($e instanceof $rt.EarlyReturn) return [$e.value, p];
    throw $e;
  }
}

export function claim_or({ p, rank }) {
  try {
    const s = here({ p: p });
    const [$r376, p$257] = claim_and({ p: p, rank: r_claim_and });
    p = p$257;
    const first = $rt.unwrap($r376);
    if (!at_word({ p: p, want: "or" })) {
      return [{ tag: "Ok", value: first.pred }, p];
    }
    let operands = $std_list.builder({  });
    const [, operands$258] = $std_list.push({ b: operands, x: first.pred });
    operands = operands$258;
    let mixed = first.chained;
    const toks0 = p.toks;
    const start0 = p.pos;
    while (at_word({ p: p, want: "or" }) && p.pos < $std_list.len({ xs: p.toks }) - 1) {
      const [, p$259] = skip({ p: p });
      p = p$259;
      const [$r379, p$260] = claim_and({ p: p, rank: r_claim_and });
      p = p$260;
      const next = $rt.unwrap($r379);
      if (next.chained) {
        mixed = true;
      }
      const [, operands$261] = $std_list.push({ b: operands, x: next.pred });
      operands = operands$261;
    }
    const span = span_from({ p: p, start: s });
    if (mixed) {
      const [, p$262] = report({ p: p, code: "E0007", start: span.start, end: span.end, detail: "write `(a and b) or c` or `a and (b or c)`" });
      p = p$262;
    }
    return [{ tag: "Ok", value: { tag: "ClaimOr", operands: $std_list.finish({ b: operands }), span: span } }, p];
  } catch ($e) {
    if ($e instanceof $rt.EarlyReturn) return [$e.value, p];
    throw $e;
  }
}

export function claim_and({ p, rank }) {
  try {
    const s = here({ p: p });
    const [$r382, p$263] = claim_not({ p: p, rank: r_claim_not });
    p = p$263;
    const first = $rt.unwrap($r382);
    if (!at_word({ p: p, want: "and" })) {
      return [{ tag: "Ok", value: { pred: first, chained: false } }, p];
    }
    let operands = $std_list.builder({  });
    const [, operands$264] = $std_list.push({ b: operands, x: first });
    operands = operands$264;
    const toks0 = p.toks;
    const start0 = p.pos;
    while (at_word({ p: p, want: "and" }) && p.pos < $std_list.len({ xs: p.toks }) - 1) {
      const [, p$265] = skip({ p: p });
      p = p$265;
      const [$r386, p$266] = claim_not({ p: p, rank: r_claim_not });
      p = p$266;
      const next = $rt.unwrap($r386);
      const [, operands$267] = $std_list.push({ b: operands, x: next });
      operands = operands$267;
    }
    return [{ tag: "Ok", value: { pred: { tag: "ClaimAnd", operands: $std_list.finish({ b: operands }), span: span_from({ p: p, start: s }) }, chained: true } }, p];
  } catch ($e) {
    if ($e instanceof $rt.EarlyReturn) return [$e.value, p];
    throw $e;
  }
}

export function claim_not({ p, rank }) {
  try {
    const s = here({ p: p });
    const [$r390, p$268] = accept_word({ p: p, want: "not" });
    p = p$268;
    if ($r390) {
      const [$r391, p$269] = claim_atom({ p: p, rank: r_claim_atom });
      p = p$269;
      const operand = $rt.unwrap($r391);
      return [{ tag: "Ok", value: { tag: "ClaimNot", operand: operand, span: span_from({ p: p, start: s }) } }, p];
    }
    const [$r394, p$270] = claim_atom({ p: p, rank: r_claim_atom });
    p = p$270;
    return [$r394, p];
  } catch ($e) {
    if ($e instanceof $rt.EarlyReturn) return [$e.value, p];
    throw $e;
  }
}

export function claim_atom({ p, rank }) {
  try {
    const s = here({ p: p });
    const [$r395, p$271] = accept_punct({ p: p, want: "(" });
    p = p$271;
    if ($r395) {
      const [$r396, p$272] = claim_or({ p: p, rank: r_claim_or });
      p = p$272;
      const inner = $rt.unwrap($r396);
      const [$r397, p$273] = need_punct({ p: p, want: ")" });
      p = p$273;
      $rt.unwrap($r397);
      return [{ tag: "Ok", value: inner }, p];
    }
    const [$r399, p$274] = accept_word({ p: p, want: "effects" });
    p = p$274;
    if ($r399) {
      const [$r400, p$275] = need_punct({ p: p, want: "==" });
      p = p$275;
      $rt.unwrap($r400);
      const [$r401, p$276] = need_punct({ p: p, want: "{" });
      p = p$276;
      $rt.unwrap($r401);
      let effects = [];
      if (!at_punct({ p: p, want: "}" })) {
        const [$r402, p$277] = effect_list({ p: p });
        p = p$277;
        effects = $rt.unwrap($r402);
      }
      const [$r403, p$278] = need_punct({ p: p, want: "}" });
      p = p$278;
      $rt.unwrap($r403);
      return [{ tag: "Ok", value: { tag: "ClaimEffectsEq", effects: effects, span: span_from({ p: p, start: s }) } }, p];
    }
    if (at_name({ p: p, n: 0 }) || at_tname({ p: p, n: 0 })) {
      const [$r406, p$279] = dotted({ p: p });
      p = p$279;
      const name = $rt.unwrap($r406);
      return [{ tag: "Ok", value: { tag: "ClaimAtom", name: name, span: span_from({ p: p, start: s }) } }, p];
    }
    const [$r409, p$280] = fail({ p: p, expected: "an effect, a claim, `effects == { ... }` or `(`" });
    p = p$280;
    return [{ tag: "Err", error: $r409 }, p];
  } catch ($e) {
    if ($e instanceof $rt.EarlyReturn) return [$e.value, p];
    throw $e;
  }
}

export function parse_capability({ p, start, vis }) {
  try {
    const [$r411, p$281] = need_word({ p: p, want: "capability" });
    p = p$281;
    $rt.unwrap($r411);
    const [$r412, p$282] = expect_tname({ p: p });
    p = p$282;
    const name = $rt.unwrap($r412);
    p = { ...p, current_def: name.text };
    let tparams = [];
    if (at_punct({ p: p, want: "[" })) {
      const [$r414, p$283] = parse_tparams({ p: p });
      p = p$283;
      tparams = $rt.unwrap($r414);
    }
    const [$r415, p$284] = need_newline({ p: p });
    p = p$284;
    $rt.unwrap($r415);
    let grant_list = $std_list.builder({  });
    let end = name.span.end;
    const toks0 = p.toks;
    const start0 = p.pos;
    while (at_word({ p: p, want: "grants" }) && p.pos < $std_list.len({ xs: p.toks }) - 1) {
      const s = here({ p: p });
      const [, p$285] = skip({ p: p });
      p = p$285;
      const [$r417, p$286] = effect_ref({ p: p });
      p = p$286;
      const effect = $rt.unwrap($r417);
      let when_cond = { tag: "None" };
      const [$r419, p$287] = accept_word({ p: p, want: "when" });
      p = p$287;
      if ($r419) {
        const saved = p.no_brace;
        p = { ...p, no_brace: true };
        const [$r421, p$288] = parse_expr({ p: p, rank: r_expr });
        p = p$288;
        const e = $rt.unwrap($r421);
        p = { ...p, no_brace: saved };
        when_cond = { tag: "Some", value: e };
      }
      const gspan = span_from({ p: p, start: s });
      end = gspan.end;
      const [, grant_list$289] = $std_list.push({ b: grant_list, x: { effect: effect, when_cond: when_cond, span: gspan } });
      grant_list = grant_list$289;
      const [$r425, p$290] = need_newline({ p: p });
      p = p$290;
      $rt.unwrap($r425);
    }
    return [{ tag: "Ok", value: { vis: vis, name: name, tparams: tparams, grant_list: $std_list.finish({ b: grant_list }), span: { start: start, end: end } } }, p];
  } catch ($e) {
    if ($e instanceof $rt.EarlyReturn) return [$e.value, p];
    throw $e;
  }
}

export function parse_path({ p, start }) {
  try {
    const [$r429, p$291] = need_word({ p: p, want: "path" });
    p = p$291;
    $rt.unwrap($r429);
    const [$r430, p$292] = expect_name({ p: p });
    p = p$292;
    const name = $rt.unwrap($r430);
    p = { ...p, current_def: name.text };
    const [$r432, p$293] = need_newline({ p: p });
    p = p$293;
    $rt.unwrap($r432);
    const [$r433, p$294] = need_word({ p: p, want: "entry" });
    p = p$294;
    $rt.unwrap($r433);
    const [$r434, p$295] = expect_name({ p: p });
    p = p$295;
    const entry_fn = $rt.unwrap($r434);
    const [$r435, p$296] = need_newline({ p: p });
    p = p$296;
    $rt.unwrap($r435);
    let clauses = $std_list.builder({  });
    let end = entry_fn.span.end;
    let more = true;
    const toks0 = p.toks;
    const start0 = p.pos;
    while (more && p.pos < $std_list.len({ xs: p.toks }) - 1) {
      const s = here({ p: p });
      const [$r437, p$297] = accept_word({ p: p, want: "effects" });
      p = p$297;
      if ($r437) {
        const [$r438, p$298] = need_punct({ p: p, want: "<=" });
        p = p$298;
        $rt.unwrap($r438);
        const [$r439, p$299] = need_punct({ p: p, want: "{" });
        p = p$299;
        $rt.unwrap($r439);
        let effects = [];
        if (!at_punct({ p: p, want: "}" })) {
          const [$r440, p$300] = effect_list({ p: p });
          p = p$300;
          effects = $rt.unwrap($r440);
        }
        const [$r441, p$301] = need_punct({ p: p, want: "}" });
        p = p$301;
        $rt.unwrap($r441);
        const cspan = span_from({ p: p, start: s });
        end = cspan.end;
        const [, clauses$302] = $std_list.push({ b: clauses, x: { tag: "PathEffects", effects: effects, span: cspan } });
        clauses = clauses$302;
        const [$r443, p$303] = need_newline({ p: p });
        p = p$303;
        $rt.unwrap($r443);
      } else {
        const [$r444, p$304] = accept_word({ p: p, want: "forbid" });
        p = p$304;
        if ($r444) {
          const [$r445, p$305] = need_punct({ p: p, want: "{" });
          p = p$305;
          $rt.unwrap($r445);
          let effects = [];
          if (!at_punct({ p: p, want: "}" })) {
            const [$r446, p$306] = effect_list({ p: p });
            p = p$306;
            effects = $rt.unwrap($r446);
          }
          const [$r447, p$307] = need_punct({ p: p, want: "}" });
          p = p$307;
          $rt.unwrap($r447);
          const cspan = span_from({ p: p, start: s });
          end = cspan.end;
          const [, clauses$308] = $std_list.push({ b: clauses, x: { tag: "PathForbid", effects: effects, span: cspan } });
          clauses = clauses$308;
          const [$r449, p$309] = need_newline({ p: p });
          p = p$309;
          $rt.unwrap($r449);
        } else {
          const [$r450, p$310] = accept_word({ p: p, want: "require" });
          p = p$310;
          if ($r450) {
            const [$r451, p$311] = need_punct({ p: p, want: "{" });
            p = p$311;
            $rt.unwrap($r451);
            let claim_list = $std_list.builder({  });
            if (!at_punct({ p: p, want: "}" })) {
              const [$r452, p$312] = dotted({ p: p });
              p = p$312;
              const first = $rt.unwrap($r452);
              const [, claim_list$313] = $std_list.push({ b: claim_list, x: first });
              claim_list = claim_list$313;
              const start1 = p.pos;
              while (at_punct({ p: p, want: "," }) && p.pos < $std_list.len({ xs: p.toks }) - 1) {
                const [, p$314] = skip({ p: p });
                p = p$314;
                const [$r454, p$315] = dotted({ p: p });
                p = p$315;
                const next = $rt.unwrap($r454);
                const [, claim_list$316] = $std_list.push({ b: claim_list, x: next });
                claim_list = claim_list$316;
              }
            }
            const [$r455, p$317] = need_punct({ p: p, want: "}" });
            p = p$317;
            $rt.unwrap($r455);
            const cspan = span_from({ p: p, start: s });
            end = cspan.end;
            const [, clauses$318] = $std_list.push({ b: clauses, x: { tag: "PathRequire", claim_list: $std_list.finish({ b: claim_list }), span: cspan } });
            clauses = clauses$318;
            const [$r457, p$319] = need_newline({ p: p });
            p = p$319;
            $rt.unwrap($r457);
          } else {
            const [$r458, p$320] = accept_word({ p: p, want: "policy" });
            p = p$320;
            if ($r458) {
              const [$r459, p$321] = expect_name({ p: p });
              p = p$321;
              const pname = $rt.unwrap($r459);
              let except_list = $std_list.builder({  });
              const [$r460, p$322] = accept_word({ p: p, want: "except" });
              p = p$322;
              if ($r460) {
                const [$r461, p$323] = need_punct({ p: p, want: "{" });
                p = p$323;
                $rt.unwrap($r461);
                const [$r462, p$324] = dotted({ p: p });
                p = p$324;
                const first = $rt.unwrap($r462);
                const [, except_list$325] = $std_list.push({ b: except_list, x: first });
                except_list = except_list$325;
                const start2 = p.pos;
                while (at_punct({ p: p, want: "," }) && p.pos < $std_list.len({ xs: p.toks }) - 1) {
                  const [, p$326] = skip({ p: p });
                  p = p$326;
                  const [$r464, p$327] = dotted({ p: p });
                  p = p$327;
                  const next = $rt.unwrap($r464);
                  const [, except_list$328] = $std_list.push({ b: except_list, x: next });
                  except_list = except_list$328;
                }
                const [$r465, p$329] = need_punct({ p: p, want: "}" });
                p = p$329;
                $rt.unwrap($r465);
              }
              const cspan = span_from({ p: p, start: s });
              end = cspan.end;
              const [, clauses$330] = $std_list.push({ b: clauses, x: { tag: "PathPolicy", name: pname, except_list: $std_list.finish({ b: except_list }), span: cspan } });
              clauses = clauses$330;
              const [$r467, p$331] = need_newline({ p: p });
              p = p$331;
              $rt.unwrap($r467);
            } else {
              more = false;
            }
          }
        }
      }
    }
    return [{ tag: "Ok", value: { name: name, entry_fn: entry_fn, clauses: $std_list.finish({ b: clauses }), span: { start: start, end: end } } }, p];
  } catch ($e) {
    if ($e instanceof $rt.EarlyReturn) return [$e.value, p];
    throw $e;
  }
}

export function parse_policy({ p, start }) {
  try {
    const [$r471, p$332] = need_word({ p: p, want: "policy" });
    p = p$332;
    $rt.unwrap($r471);
    const [$r472, p$333] = expect_name({ p: p });
    p = p$333;
    const name = $rt.unwrap($r472);
    p = { ...p, current_def: name.text };
    const [$r474, p$334] = need_newline({ p: p });
    p = p$334;
    $rt.unwrap($r474);
    const [$r475, p$335] = need_word({ p: p, want: "forbid" });
    p = p$335;
    $rt.unwrap($r475);
    const [$r476, p$336] = need_word({ p: p, want: "assume" });
    p = p$336;
    $rt.unwrap($r476);
    const [$r477, p$337] = need_word({ p: p, want: "outside" });
    p = p$337;
    $rt.unwrap($r477);
    const [$r478, p$338] = need_punct({ p: p, want: "{" });
    p = p$338;
    $rt.unwrap($r478);
    let outside_list = $std_list.builder({  });
    let more = true;
    const toks0 = p.toks;
    const start0 = p.pos;
    while (more && p.pos < $std_list.len({ xs: p.toks }) - 1) {
      const s = here({ p: p });
      const [$r480, p$339] = accept_word({ p: p, want: "self" });
      p = p$339;
      if ($r480) {
        const [, outside_list$340] = $std_list.push({ b: outside_list, x: { name: { tag: "None" }, glob: false, span: span_from({ p: p, start: s }) } });
        outside_list = outside_list$340;
      } else {
        const [$r483, p$341] = module_name({ p: p });
        p = p$341;
        const n = $rt.unwrap($r483);
        let glob = false;
        if (at_punct({ p: p, want: "." }) && at_punct_n({ p: p, n: 1, want: "*" })) {
          const [, p$342] = skip({ p: p });
          p = p$342;
          const [, p$343] = skip({ p: p });
          p = p$343;
          glob = true;
        }
        const [, outside_list$344] = $std_list.push({ b: outside_list, x: { name: { tag: "Some", value: n }, glob: glob, span: span_from({ p: p, start: s }) } });
        outside_list = outside_list$344;
      }
      const [$r486, p$345] = accept_punct({ p: p, want: "," });
      p = p$345;
      more = $r486;
    }
    const [$r487, p$346] = need_punct({ p: p, want: "}" });
    p = p$346;
    $rt.unwrap($r487);
    return [{ tag: "Ok", value: { name: name, outside_list: $std_list.finish({ b: outside_list }), span: span_from({ p: p, start: start }) } }, p];
  } catch ($e) {
    if ($e instanceof $rt.EarlyReturn) return [$e.value, p];
    throw $e;
  }
}

export function parse_example({ p, start }) {
  try {
    const [$r490, p$347] = need_word({ p: p, want: "example" });
    p = p$347;
    $rt.unwrap($r490);
    const [$r491, p$348] = expect_name({ p: p });
    p = p$348;
    const name = $rt.unwrap($r491);
    p = { ...p, current_def: name.text };
    const [$r493, p$349] = parse_assertion_block({ p: p, rank: r_block });
    p = p$349;
    const body = $rt.unwrap($r493);
    return [{ tag: "Ok", value: { name: name, body: body, span: span_from({ p: p, start: start }) } }, p];
  } catch ($e) {
    if ($e instanceof $rt.EarlyReturn) return [$e.value, p];
    throw $e;
  }
}

export function parse_property({ p, start }) {
  try {
    const [$r496, p$350] = need_word({ p: p, want: "property" });
    p = p$350;
    $rt.unwrap($r496);
    const [$r497, p$351] = expect_name({ p: p });
    p = p$351;
    const name = $rt.unwrap($r497);
    p = { ...p, current_def: name.text };
    const [$r499, p$352] = param_list({ p: p, rank: r_param_list });
    p = p$352;
    const params = $rt.unwrap($r499);
    const [$r500, p$353] = parse_assertion_block({ p: p, rank: r_block });
    p = p$353;
    const body = $rt.unwrap($r500);
    return [{ tag: "Ok", value: { name: name, params: params, body: body, span: span_from({ p: p, start: start }) } }, p];
  } catch ($e) {
    if ($e instanceof $rt.EarlyReturn) return [$e.value, p];
    throw $e;
  }
}

export function parse_type({ p, rank }) {
  try {
    const s = here({ p: p });
    const [$r503, p$354] = accept_word({ p: p, want: "fn" });
    p = p$354;
    if ($r503) {
      const [$r504, p$355] = param_list({ p: p, rank: r_param_list });
      p = p$355;
      const params = $rt.unwrap($r504);
      const [$r505, p$356] = need_punct({ p: p, want: "->" });
      p = p$356;
      $rt.unwrap($r505);
      const [$r506, p$357] = parse_type({ p: p, rank: r_type });
      p = p$357;
      const ret = $rt.unwrap($r506);
      const [$r507, p$358] = effects_opt({ p: p });
      p = p$358;
      const effects = $rt.unwrap($r507);
      return [{ tag: "Ok", value: { tag: "FnType", params: params, ret: ret, effects: effects, span: span_from({ p: p, start: s }) } }, p];
    }
    if (!qtname_ahead({ p: p })) {
      const [$r510, p$359] = fail({ p: p, expected: "a type" });
      p = p$359;
      return [{ tag: "Err", error: $r510 }, p];
    }
    const [$r512, p$360] = qtname({ p: p });
    p = p$360;
    const name = $rt.unwrap($r512);
    let args = [];
    if (at_punct({ p: p, want: "[" })) {
      const [$r513, p$361] = type_args({ p: p, rank: r_type_args });
      p = p$361;
      args = $rt.unwrap($r513);
    }
    let where_ = { tag: "None" };
    const [$r515, p$362] = accept_word({ p: p, want: "where" });
    p = p$362;
    if ($r515) {
      const [$r516, p$363] = parse_expr({ p: p, rank: r_expr });
      p = p$363;
      const e = $rt.unwrap($r516);
      where_ = { tag: "Some", value: e };
    }
    return [{ tag: "Ok", value: { tag: "NamedType", name: name, args: args, where_: where_, span: span_from({ p: p, start: s }) } }, p];
  } catch ($e) {
    if ($e instanceof $rt.EarlyReturn) return [$e.value, p];
    throw $e;
  }
}

export function parse_binder_type({ p, rank }) {
  try {
    const s = here({ p: p });
    if (at_word({ p: p, want: "fn" })) {
      const [$r520, p$364] = parse_type({ p: p, rank: r_type });
      p = p$364;
      return [$r520, p];
    }
    if (!qtname_ahead({ p: p })) {
      const [$r521, p$365] = fail({ p: p, expected: "a type" });
      p = p$365;
      return [{ tag: "Err", error: $r521 }, p];
    }
    const [$r523, p$366] = qtname({ p: p });
    p = p$366;
    const name = $rt.unwrap($r523);
    let args = [];
    if (at_punct({ p: p, want: "[" })) {
      const [$r524, p$367] = type_args({ p: p, rank: r_type_args });
      p = p$367;
      args = $rt.unwrap($r524);
    }
    return [{ tag: "Ok", value: { tag: "NamedType", name: name, args: args, where_: { tag: "None" }, span: span_from({ p: p, start: s }) } }, p];
  } catch ($e) {
    if ($e instanceof $rt.EarlyReturn) return [$e.value, p];
    throw $e;
  }
}

export function type_args({ p, rank }) {
  try {
    const [$r528, p$368] = need_punct({ p: p, want: "[" });
    p = p$368;
    $rt.unwrap($r528);
    let out = $std_list.builder({  });
    const saved = p.no_brace;
    p = { ...p, no_brace: false };
    let more = true;
    const toks0 = p.toks;
    const start0 = p.pos;
    while (more && p.pos < $std_list.len({ xs: p.toks }) - 1) {
      const [$r531, p$369] = type_arg({ p: p, rank: r_type_arg });
      p = p$369;
      const a = $rt.unwrap($r531);
      const [, out$370] = $std_list.push({ b: out, x: a });
      out = out$370;
      const [$r532, p$371] = accept_punct({ p: p, want: "," });
      p = p$371;
      more = $r532;
    }
    p = { ...p, no_brace: saved };
    const [$r534, p$372] = need_punct({ p: p, want: "]" });
    p = p$372;
    $rt.unwrap($r534);
    return [{ tag: "Ok", value: $std_list.finish({ b: out }) }, p];
  } catch ($e) {
    if ($e instanceof $rt.EarlyReturn) return [$e.value, p];
    throw $e;
  }
}

export function type_arg({ p, rank }) {
  try {
    const s = here({ p: p });
    let label = { tag: "None" };
    if (at_name({ p: p, n: 0 }) && at_punct_n({ p: p, n: 1, want: ":" })) {
      const [$r537, p$373] = expect_name({ p: p });
      p = p$373;
      const l = $rt.unwrap($r537);
      const [, p$374] = skip({ p: p });
      p = p$374;
      label = { tag: "Some", value: l };
    }
    if (at_word({ p: p, want: "fn" }) || qtname_ahead({ p: p })) {
      const [$r539, p$375] = parse_type({ p: p, rank: r_type });
      p = p$375;
      const ty = $rt.unwrap($r539);
      return [{ tag: "Ok", value: { tag: "TypeArgType", label: label, ty: ty, span: span_from({ p: p, start: s }) } }, p];
    }
    const [$r542, p$376] = parse_expr({ p: p, rank: r_expr });
    p = p$376;
    const e = $rt.unwrap($r542);
    return [{ tag: "Ok", value: { tag: "TypeArgConst", label: label, expr: e, span: span_from({ p: p, start: s }) } }, p];
  } catch ($e) {
    if ($e instanceof $rt.EarlyReturn) return [$e.value, p];
    throw $e;
  }
}

export function parse_block({ p, rank, allow_elided }) {
  try {
    const s = here({ p: p });
    const saved_assert = p.assertion_block;
    const saved_brace = p.no_brace;
    p = { ...p, assertion_block: false, no_brace: false };
    const [$r546, p$377] = need_punct({ p: p, want: "{" });
    p = p$377;
    $rt.unwrap($r546);
    const [$r547, p$378] = accept_punct({ p: p, want: "..." });
    p = p$378;
    if (allow_elided && $r547) {
      const [$r548, p$379] = need_punct({ p: p, want: "}" });
      p = p$379;
      $rt.unwrap($r548);
      p = { ...p, assertion_block: saved_assert, no_brace: saved_brace };
      return [{ tag: "Ok", value: { stmts: [], elided: true, span: span_from({ p: p, start: s }) } }, p];
    }
    const [$r552, p$380] = braced_stmts({ p: p, rank: r_stmts });
    p = p$380;
    const stmts = $rt.unwrap($r552);
    p = { ...p, assertion_block: saved_assert, no_brace: saved_brace };
    return [{ tag: "Ok", value: { stmts: stmts, elided: false, span: span_from({ p: p, start: s }) } }, p];
  } catch ($e) {
    if ($e instanceof $rt.EarlyReturn) return [$e.value, p];
    throw $e;
  }
}

export function parse_assertion_block({ p, rank }) {
  try {
    const s = here({ p: p });
    const saved_assert = p.assertion_block;
    const saved_brace = p.no_brace;
    p = { ...p, assertion_block: true, no_brace: false };
    const [$r557, p$381] = need_punct({ p: p, want: "{" });
    p = p$381;
    $rt.unwrap($r557);
    const [$r558, p$382] = braced_stmts({ p: p, rank: r_stmts });
    p = p$382;
    const stmts = $rt.unwrap($r558);
    p = { ...p, assertion_block: saved_assert, no_brace: saved_brace };
    return [{ tag: "Ok", value: { stmts: stmts, elided: false, span: span_from({ p: p, start: s }) } }, p];
  } catch ($e) {
    if ($e instanceof $rt.EarlyReturn) return [$e.value, p];
    throw $e;
  }
}

export function braced_stmts({ p, rank }) {
  try {
    let out = $std_list.builder({  });
    const [$r562, p$383] = accept_newline({ p: p });
    p = p$383;
    if ($r562) {
      const toks0 = p.toks;
      const start0 = p.pos;
      while (!at_punct({ p: p, want: "}" }) && !at_eof({ p: p }) && p.pos < $std_list.len({ xs: p.toks }) - 1) {
        const before = p.pos;
        const [$r565, p$384] = parse_stmt({ p: p, rank: r_stmt });
        p = p$384;
        const $m564 = $r565;
        $m564$match: {
          if ($m564.tag === "Ok") {
            const value = $m564.value;
            const [, out$385] = $std_list.push({ b: out, x: value });
            out = out$385;
            const [$r566, p$386] = terminator({ p: p, in_block: true });
            p = p$386;
            const term = $r566;
            if (term.tag === "Ok") {
              p = p;
            } else {
              const [, p$387] = recover_to_line_end({ p: p });
              p = p$387;
              const [, p$388] = skip_newline({ p: p });
              p = p$388;
            }
            break $m564$match;
          }
          if ($m564.tag === "Err") {
            const error = $m564.error;
            if (p.pos === before) {
              const [, p$389] = skip({ p: p });
              p = p$389;
            }
            const [, p$390] = recover_to_line_end({ p: p });
            p = p$390;
            const [, p$391] = skip_newline({ p: p });
            p = p$391;
            break $m564$match;
          }
          $rt.unreachable();
        }
        if (p.pos === before) {
          const [, p$392] = skip({ p: p });
          p = p$392;
        }
      }
      const [$r567, p$393] = need_punct({ p: p, want: "}" });
      p = p$393;
      $rt.unwrap($r567);
      return [{ tag: "Ok", value: $std_list.finish({ b: out }) }, p];
    }
    const [$r569, p$394] = accept_punct({ p: p, want: "}" });
    p = p$394;
    if ($r569) {
      return [{ tag: "Ok", value: $std_list.finish({ b: out }) }, p];
    }
    const [$r571, p$395] = parse_stmt({ p: p, rank: r_stmt });
    p = p$395;
    const one = $rt.unwrap($r571);
    const [, out$396] = $std_list.push({ b: out, x: one });
    out = out$396;
    const [$r572, p$397] = need_punct({ p: p, want: "}" });
    p = p$397;
    $rt.unwrap($r572);
    return [{ tag: "Ok", value: $std_list.finish({ b: out }) }, p];
  } catch ($e) {
    if ($e instanceof $rt.EarlyReturn) return [$e.value, p];
    throw $e;
  }
}

export function parse_stmt({ p, rank }) {
  try {
    const s = here({ p: p });
    if (at_word({ p: p, want: "let" }) || at_word({ p: p, want: "var" })) {
      const [$r574, p$398] = advance({ p: p });
      p = p$398;
      const kw = $r574;
      const [$r575, p$399] = expect_name({ p: p });
      p = p$399;
      const name = $rt.unwrap($r575);
      const [$r576, p$400] = need_punct({ p: p, want: ":" });
      p = p$400;
      $rt.unwrap($r576);
      const [$r577, p$401] = parse_type({ p: p, rank: r_type });
      p = p$401;
      const ty = $rt.unwrap($r577);
      const [$r578, p$402] = need_punct({ p: p, want: "=" });
      p = p$402;
      $rt.unwrap($r578);
      const [$r579, p$403] = parse_expr({ p: p, rank: r_expr });
      p = p$403;
      const value = $rt.unwrap($r579);
      if (ident_of({ t: kw }).text === "let") {
        return [{ tag: "Ok", value: { tag: "Let", name: name, ty: ty, value: value, span: span_from({ p: p, start: s }) } }, p];
      }
      return [{ tag: "Ok", value: { tag: "Var", name: name, ty: ty, value: value, span: span_from({ p: p, start: s }) } }, p];
    }
    const [$r584, p$404] = accept_word({ p: p, want: "return" });
    p = p$404;
    if ($r584) {
      const [$r585, p$405] = parse_expr({ p: p, rank: r_expr });
      p = p$405;
      const value = $rt.unwrap($r585);
      return [{ tag: "Ok", value: { tag: "Return", value: value, span: span_from({ p: p, start: s }) } }, p];
    }
    if (at_word({ p: p, want: "if" })) {
      const [$r588, p$406] = parse_if({ p: p, rank: r_if });
      p = p$406;
      return [$r588, p];
    }
    const [$r589, p$407] = accept_word({ p: p, want: "match" });
    p = p$407;
    if ($r589) {
      const saved = p.no_brace;
      p = { ...p, no_brace: true };
      const [$r591, p$408] = parse_expr({ p: p, rank: r_expr });
      p = p$408;
      const scrutinee = $rt.unwrap($r591);
      p = { ...p, no_brace: saved };
      const [$r593, p$409] = need_word({ p: p, want: "with" });
      p = p$409;
      $rt.unwrap($r593);
      const [$r594, p$410] = need_newline({ p: p });
      p = p$410;
      $rt.unwrap($r594);
      let arms = $std_list.builder({  });
      let end = span_of_expr({ e: scrutinee }).end;
      let more = at_punct({ p: p, want: "|" });
      const toks0 = p.toks;
      const start0 = p.pos;
      while (more && p.pos < $std_list.len({ xs: p.toks }) - 1) {
        const as_ = here({ p: p });
        const [, p$411] = skip({ p: p });
        p = p$411;
        const [$r596, p$412] = parse_pattern({ p: p, rank: r_pattern });
        p = p$412;
        const pattern = $rt.unwrap($r596);
        let guard = { tag: "None" };
        const [$r598, p$413] = accept_word({ p: p, want: "when" });
        p = p$413;
        if ($r598) {
          const [$r599, p$414] = parse_expr({ p: p, rank: r_expr });
          p = p$414;
          const g = $rt.unwrap($r599);
          guard = { tag: "Some", value: g };
        }
        const [$r601, p$415] = need_punct({ p: p, want: "->" });
        p = p$415;
        $rt.unwrap($r601);
        let body = { tag: "ArmBlock", block: { stmts: [], elided: false, span: span_from({ p: p, start: as_ }) } };
        if (at_punct({ p: p, want: "{" })) {
          const [$r604, p$416] = parse_block({ p: p, rank: r_block, allow_elided: false });
          p = p$416;
          const b = $rt.unwrap($r604);
          body = { tag: "ArmBlock", block: b };
        } else {
          const [$r606, p$417] = parse_stmt({ p: p, rank: r_stmt });
          p = p$417;
          const st = $rt.unwrap($r606);
          body = { tag: "ArmStmt", stmt: st };
        }
        const aspan = span_from({ p: p, start: as_ });
        end = aspan.end;
        const [, arms$418] = $std_list.push({ b: arms, x: { pattern: pattern, guard: guard, body: body, span: aspan } });
        arms = arms$418;
        const [$r609, p$419] = terminator({ p: p, in_block: true });
        p = p$419;
        $rt.unwrap($r609);
        more = at_punct({ p: p, want: "|" }) && !at_punct({ p: p, want: "}" }) && !at_eof({ p: p });
      }
      return [{ tag: "Ok", value: { tag: "Match", scrutinee: scrutinee, arms: $std_list.finish({ b: arms }), span: { start: s, end: end } } }, p];
    }
    const [$r613, p$420] = accept_word({ p: p, want: "loop" });
    p = p$420;
    if ($r613) {
      const [$r614, p$421] = need_word({ p: p, want: "while" });
      p = p$421;
      $rt.unwrap($r614);
      const saved = p.no_brace;
      p = { ...p, no_brace: true };
      const [$r616, p$422] = parse_expr({ p: p, rank: r_expr });
      p = p$422;
      const cond = $rt.unwrap($r616);
      let clauses = $std_list.builder({  });
      const toks0 = p.toks;
      const start0 = p.pos;
      while ((at_word({ p: p, want: "invariant" }) || at_word({ p: p, want: "decreases" })) && p.pos < $std_list.len({ xs: p.toks }) - 1) {
        const cs = here({ p: p });
        const [$r618, p$423] = advance({ p: p });
        p = p$423;
        const kw = $r618;
        const [$r619, p$424] = parse_expr({ p: p, rank: r_expr });
        p = p$424;
        const e = $rt.unwrap($r619);
        const [, clauses$425] = $std_list.push({ b: clauses, x: { clause: ident_of({ t: kw }).text, expr: e, span: span_from({ p: p, start: cs }) } });
        clauses = clauses$425;
      }
      p = { ...p, no_brace: saved };
      const [$r622, p$426] = parse_block({ p: p, rank: r_block, allow_elided: false });
      p = p$426;
      const body = $rt.unwrap($r622);
      return [{ tag: "Ok", value: { tag: "Loop", cond: cond, clauses: $std_list.finish({ b: clauses }), body: body, span: span_from({ p: p, start: s }) } }, p];
    }
    const [$r625, p$427] = accept_word({ p: p, want: "for" });
    p = p$427;
    if ($r625) {
      const [$r626, p$428] = expect_name({ p: p });
      p = p$428;
      const name = $rt.unwrap($r626);
      const [$r627, p$429] = need_punct({ p: p, want: ":" });
      p = p$429;
      $rt.unwrap($r627);
      const [$r628, p$430] = parse_type({ p: p, rank: r_type });
      p = p$430;
      const ty = $rt.unwrap($r628);
      const [$r629, p$431] = need_word({ p: p, want: "in" });
      p = p$431;
      $rt.unwrap($r629);
      const saved = p.no_brace;
      p = { ...p, no_brace: true };
      const [$r631, p$432] = parse_domain({ p: p, rank: r_domain });
      p = p$432;
      const domain = $rt.unwrap($r631);
      p = { ...p, no_brace: saved };
      const [$r633, p$433] = parse_block({ p: p, rank: r_block, allow_elided: false });
      p = p$433;
      const body = $rt.unwrap($r633);
      return [{ tag: "Ok", value: { tag: "For", name: name, ty: ty, domain: domain, body: body, span: span_from({ p: p, start: s }) } }, p];
    }
    const [$r636, p$434] = accept_word({ p: p, want: "assume" });
    p = p$434;
    if ($r636) {
      const [$r637, p$435] = dotted({ p: p });
      p = p$435;
      const claim = $rt.unwrap($r637);
      let justification = "";
      if (kind_at({ p: p, n: 0 }).tag === "TextLit") {
        const [$r638, p$436] = advance({ p: p });
        p = p$436;
        const t = $r638;
        justification = text_value({ t: t });
      } else {
        const [$r639, p$437] = fail({ p: p, expected: "a text literal" });
        p = p$437;
        return [{ tag: "Err", error: $r639 }, p];
      }
      let verify_block = { tag: "None" };
      if (at_word({ p: p, want: "verify" })) {
        const vs = here({ p: p });
        const [, p$438] = skip({ p: p });
        p = p$438;
        const [$r642, p$439] = param_list({ p: p, rank: r_param_list });
        p = p$439;
        const params = $rt.unwrap($r642);
        const [$r643, p$440] = effects_opt({ p: p });
        p = p$440;
        const effects = $rt.unwrap($r643);
        const [$r644, p$441] = parse_assertion_block({ p: p, rank: r_block });
        p = p$441;
        const body = $rt.unwrap($r644);
        verify_block = { tag: "Some", value: { params: params, effects: effects, body: body, span: span_from({ p: p, start: vs }) } };
      }
      return [{ tag: "Ok", value: { tag: "Assume", claim: claim, justification: justification, verify_block: verify_block, span: span_from({ p: p, start: s }) } }, p];
    }
    const [$r649, p$442] = parse_expr({ p: p, rank: r_expr });
    p = p$442;
    const e = $rt.unwrap($r649);
    const $m650 = e;
    $m650$match: {
      if ($m650.tag === "Name") {
        const name = $m650.name;
        const span = $m650.span;
        const [$r651, p$443] = accept_punct({ p: p, want: "=" });
        p = p$443;
        if ($r651) {
          const [$r652, p$444] = parse_expr({ p: p, rank: r_expr });
          p = p$444;
          const value = $rt.unwrap($r652);
          return [{ tag: "Ok", value: { tag: "Assign", name: name, value: value, span: span_from({ p: p, start: s }) } }, p];
        }
        break $m650$match;
      }
      if (true) {
        p = p;
        break $m650$match;
      }
      $rt.unreachable();
    }
    if (!p.assertion_block && !is_call_for_effect({ e: e })) {
      const espan = span_of_expr({ e: e });
      const [, p$445] = report({ p: p, code: "E0002", start: espan.start, end: espan.end, detail: "a bare expression statement must be a call (or `try` of a call)" });
      p = p$445;
    }
    return [{ tag: "Ok", value: { tag: "ExprStmt", expr: e, span: span_from({ p: p, start: s }) } }, p];
  } catch ($e) {
    if ($e instanceof $rt.EarlyReturn) return [$e.value, p];
    throw $e;
  }
}

export function parse_if({ p, rank }) {
  try {
    const s = here({ p: p });
    const [$r657, p$446] = need_word({ p: p, want: "if" });
    p = p$446;
    $rt.unwrap($r657);
    const saved = p.no_brace;
    p = { ...p, no_brace: true };
    const [$r659, p$447] = parse_expr({ p: p, rank: r_expr });
    p = p$447;
    const cond = $rt.unwrap($r659);
    p = { ...p, no_brace: saved };
    const [$r661, p$448] = parse_block({ p: p, rank: r_block, allow_elided: false });
    p = p$448;
    const then_block = $rt.unwrap($r661);
    let else_block = { tag: "None" };
    const [$r663, p$449] = accept_word({ p: p, want: "else" });
    p = p$449;
    if ($r663) {
      if (at_word({ p: p, want: "if" })) {
        const [$r664, p$450] = parse_if({ p: p, rank: r_if });
        p = p$450;
        const nested = $rt.unwrap($r664);
        const nspan = span_of_stmt({ st: nested });
        else_block = { tag: "Some", value: { stmts: [nested], elided: false, span: nspan } };
      } else {
        const [$r667, p$451] = parse_block({ p: p, rank: r_block, allow_elided: false });
        p = p$451;
        const b = $rt.unwrap($r667);
        else_block = { tag: "Some", value: b };
      }
    }
    return [{ tag: "Ok", value: { tag: "If", cond: cond, then_block: then_block, else_block: else_block, span: span_from({ p: p, start: s }) } }, p];
  } catch ($e) {
    if ($e instanceof $rt.EarlyReturn) return [$e.value, p];
    throw $e;
  }
}

export function parse_domain({ p, rank }) {
  try {
    const s = here({ p: p });
    const [$r671, p$452] = parse_expr({ p: p, rank: r_expr });
    p = p$452;
    const first = $rt.unwrap($r671);
    const [$r672, p$453] = accept_punct({ p: p, want: "..<" });
    p = p$453;
    if ($r672) {
      const [$r673, p$454] = parse_expr({ p: p, rank: r_expr });
      p = p$454;
      const hi = $rt.unwrap($r673);
      return [{ tag: "Ok", value: { tag: "RangeDomain", lo: first, hi: hi, span: span_from({ p: p, start: s }) } }, p];
    }
    return [{ tag: "Ok", value: { tag: "InDomain", expr: first, span: span_from({ p: p, start: s }) } }, p];
  } catch ($e) {
    if ($e instanceof $rt.EarlyReturn) return [$e.value, p];
    throw $e;
  }
}

export function span_of_expr({ e }) {
  const $m678 = e;
  $m678$match: {
    if ($m678.tag === "IntLit") {
      const value = $m678.value;
      const text = $m678.text;
      const span = $m678.span;
      return span;
      break $m678$match;
    }
    if ($m678.tag === "FloatLit") {
      const value = $m678.value;
      const span = $m678.span;
      return span;
      break $m678$match;
    }
    if ($m678.tag === "TextLit") {
      const value = $m678.value;
      const span = $m678.span;
      return span;
      break $m678$match;
    }
    if ($m678.tag === "BoolLit") {
      const value = $m678.value;
      const span = $m678.span;
      return span;
      break $m678$match;
    }
    if ($m678.tag === "DurationLit") {
      const nanos = $m678.nanos;
      const span = $m678.span;
      return span;
      break $m678$match;
    }
    if ($m678.tag === "Name") {
      const name = $m678.name;
      const span = $m678.span;
      return span;
      break $m678$match;
    }
    if ($m678.tag === "It") {
      const span = $m678.span;
      return span;
      break $m678$match;
    }
    if ($m678.tag === "ResultRef") {
      const span = $m678.span;
      return span;
      break $m678$match;
    }
    if ($m678.tag === "Ctor") {
      const name = $m678.name;
      const args = $m678.args;
      const fields = $m678.fields;
      const span = $m678.span;
      return span;
      break $m678$match;
    }
    if ($m678.tag === "RecordUpdate") {
      const base = $m678.base;
      const fields = $m678.fields;
      const span = $m678.span;
      return span;
      break $m678$match;
    }
    if ($m678.tag === "ListLit") {
      const elems = $m678.elems;
      const span = $m678.span;
      return span;
      break $m678$match;
    }
    if ($m678.tag === "Try") {
      const expr = $m678.expr;
      const else_ = $m678.else_;
      const span = $m678.span;
      return span;
      break $m678$match;
    }
    if ($m678.tag === "Recover") {
      const body = $m678.body;
      const span = $m678.span;
      return span;
      break $m678$match;
    }
    if ($m678.tag === "Old") {
      const name = $m678.name;
      const span = $m678.span;
      return span;
      break $m678$match;
    }
    if ($m678.tag === "Quantifier") {
      const quant = $m678.quant;
      const name = $m678.name;
      const ty = $m678.ty;
      const domain = $m678.domain;
      const where_ = $m678.where_;
      const body = $m678.body;
      const span = $m678.span;
      return span;
      break $m678$match;
    }
    if ($m678.tag === "Closure") {
      const params = $m678.params;
      const ret = $m678.ret;
      const effects = $m678.effects;
      const body = $m678.body;
      const span = $m678.span;
      return span;
      break $m678$match;
    }
    if ($m678.tag === "Fake") {
      const capability = $m678.capability;
      const fields = $m678.fields;
      const span = $m678.span;
      return span;
      break $m678$match;
    }
    if ($m678.tag === "Hole") {
      const span = $m678.span;
      return span;
      break $m678$match;
    }
    if ($m678.tag === "FieldAccess") {
      const object = $m678.object;
      const name = $m678.name;
      const span = $m678.span;
      return span;
      break $m678$match;
    }
    if ($m678.tag === "Call") {
      const callee = $m678.callee;
      const targs = $m678.targs;
      const args = $m678.args;
      const span = $m678.span;
      return span;
      break $m678$match;
    }
    if ($m678.tag === "Unary") {
      const op = $m678.op;
      const operand = $m678.operand;
      const span = $m678.span;
      return span;
      break $m678$match;
    }
    if ($m678.tag === "Binary") {
      const op = $m678.op;
      const left = $m678.left;
      const right = $m678.right;
      const span = $m678.span;
      return span;
      break $m678$match;
    }
    if ($m678.tag === "And") {
      const operands = $m678.operands;
      const span = $m678.span;
      return span;
      break $m678$match;
    }
    if ($m678.tag === "Or") {
      const operands = $m678.operands;
      const span = $m678.span;
      return span;
      break $m678$match;
    }
    if ($m678.tag === "Is") {
      const expr = $m678.expr;
      const pattern = $m678.pattern;
      const span = $m678.span;
      return span;
      break $m678$match;
    }
    $rt.unreachable();
  }
}

export function span_of_stmt({ st }) {
  const $m679 = st;
  $m679$match: {
    if ($m679.tag === "Let") {
      const name = $m679.name;
      const ty = $m679.ty;
      const value = $m679.value;
      const span = $m679.span;
      return span;
      break $m679$match;
    }
    if ($m679.tag === "Var") {
      const name = $m679.name;
      const ty = $m679.ty;
      const value = $m679.value;
      const span = $m679.span;
      return span;
      break $m679$match;
    }
    if ($m679.tag === "Assign") {
      const name = $m679.name;
      const value = $m679.value;
      const span = $m679.span;
      return span;
      break $m679$match;
    }
    if ($m679.tag === "Return") {
      const value = $m679.value;
      const span = $m679.span;
      return span;
      break $m679$match;
    }
    if ($m679.tag === "If") {
      const cond = $m679.cond;
      const then_block = $m679.then_block;
      const else_block = $m679.else_block;
      const span = $m679.span;
      return span;
      break $m679$match;
    }
    if ($m679.tag === "Match") {
      const scrutinee = $m679.scrutinee;
      const arms = $m679.arms;
      const span = $m679.span;
      return span;
      break $m679$match;
    }
    if ($m679.tag === "Loop") {
      const cond = $m679.cond;
      const clauses = $m679.clauses;
      const body = $m679.body;
      const span = $m679.span;
      return span;
      break $m679$match;
    }
    if ($m679.tag === "For") {
      const name = $m679.name;
      const ty = $m679.ty;
      const domain = $m679.domain;
      const body = $m679.body;
      const span = $m679.span;
      return span;
      break $m679$match;
    }
    if ($m679.tag === "Assume") {
      const claim = $m679.claim;
      const justification = $m679.justification;
      const verify_block = $m679.verify_block;
      const span = $m679.span;
      return span;
      break $m679$match;
    }
    if ($m679.tag === "ExprStmt") {
      const expr = $m679.expr;
      const span = $m679.span;
      return span;
      break $m679$match;
    }
    $rt.unreachable();
  }
}

export function is_call_for_effect({ e }) {
  const $m680 = e;
  $m680$match: {
    if ($m680.tag === "Call") {
      const callee = $m680.callee;
      const targs = $m680.targs;
      const args = $m680.args;
      const span = $m680.span;
      return true;
      break $m680$match;
    }
    if ($m680.tag === "Try") {
      const expr = $m680.expr;
      const else_ = $m680.else_;
      const span = $m680.span;
      return is_call_for_effect_of_try({ e: expr });
      break $m680$match;
    }
    if (true) {
      return false;
      break $m680$match;
    }
    $rt.unreachable();
  }
}

export function is_call_for_effect_of_try({ e }) {
  const $m681 = e;
  $m681$match: {
    if ($m681.tag === "Call") {
      const callee = $m681.callee;
      const targs = $m681.targs;
      const args = $m681.args;
      const span = $m681.span;
      return true;
      break $m681$match;
    }
    if ($m681.tag === "Try") {
      const expr = $m681.expr;
      const else_ = $m681.else_;
      const span = $m681.span;
      return expr.tag === "Call";
      break $m681$match;
    }
    if (true) {
      return false;
      break $m681$match;
    }
    $rt.unreachable();
  }
}

export function parse_expr({ p, rank }) {
  try {
    const s = here({ p: p });
    const [$r682, p$455] = or_expr({ p: p, rank: r_or });
    p = p$455;
    const left = $rt.unwrap($r682);
    if (!at_word({ p: p, want: "implies" })) {
      return [{ tag: "Ok", value: left }, p];
    }
    const [, p$456] = skip({ p: p });
    p = p$456;
    const [$r684, p$457] = or_expr({ p: p, rank: r_or });
    p = p$457;
    const right = $rt.unwrap($r684);
    const span = span_from({ p: p, start: s });
    if (at_word({ p: p, want: "implies" })) {
      const t = peek({ p: p, n: 0 });
      const [, p$458] = report({ p: p, code: "E0011", start: t.start, end: t.end, detail: "parenthesise one side: `(a implies b) implies c` or `a implies (b implies c)`" });
      p = p$458;
      const [, p$459] = skip({ p: p });
      p = p$459;
      const [$r685, p$460] = or_expr({ p: p, rank: r_or });
      p = p$460;
      const extra = $rt.unwrap($r685);
    }
    return [{ tag: "Ok", value: { tag: "Binary", op: "implies", left: left, right: right, span: span } }, p];
  } catch ($e) {
    if ($e instanceof $rt.EarlyReturn) return [$e.value, p];
    throw $e;
  }
}

export function or_expr({ p, rank }) {
  try {
    const s = here({ p: p });
    const [$r688, p$461] = and_expr({ p: p, rank: r_and });
    p = p$461;
    const first = $rt.unwrap($r688);
    if (!at_word({ p: p, want: "or" })) {
      return [{ tag: "Ok", value: first.expr }, p];
    }
    let operands = $std_list.builder({  });
    const [, operands$462] = $std_list.push({ b: operands, x: first.expr });
    operands = operands$462;
    let mixed = first.chained;
    const toks0 = p.toks;
    const start0 = p.pos;
    while (at_word({ p: p, want: "or" }) && p.pos < $std_list.len({ xs: p.toks }) - 1) {
      const [, p$463] = skip({ p: p });
      p = p$463;
      const [$r691, p$464] = and_expr({ p: p, rank: r_and });
      p = p$464;
      const next = $rt.unwrap($r691);
      if (next.chained) {
        mixed = true;
      }
      const [, operands$465] = $std_list.push({ b: operands, x: next.expr });
      operands = operands$465;
    }
    const span = span_from({ p: p, start: s });
    if (mixed) {
      const [, p$466] = report({ p: p, code: "E0007", start: span.start, end: span.end, detail: "write `(a and b) or c` or `a and (b or c)`" });
      p = p$466;
    }
    return [{ tag: "Ok", value: { tag: "Or", operands: $std_list.finish({ b: operands }), span: span } }, p];
  } catch ($e) {
    if ($e instanceof $rt.EarlyReturn) return [$e.value, p];
    throw $e;
  }
}

export function and_expr({ p, rank }) {
  try {
    const s = here({ p: p });
    const [$r694, p$467] = not_expr({ p: p, rank: r_not });
    p = p$467;
    const first = $rt.unwrap($r694);
    if (!at_word({ p: p, want: "and" })) {
      return [{ tag: "Ok", value: { expr: first, chained: false } }, p];
    }
    let operands = $std_list.builder({  });
    const [, operands$468] = $std_list.push({ b: operands, x: first });
    operands = operands$468;
    const toks0 = p.toks;
    const start0 = p.pos;
    while (at_word({ p: p, want: "and" }) && p.pos < $std_list.len({ xs: p.toks }) - 1) {
      const [, p$469] = skip({ p: p });
      p = p$469;
      const [$r698, p$470] = not_expr({ p: p, rank: r_not });
      p = p$470;
      const next = $rt.unwrap($r698);
      const [, operands$471] = $std_list.push({ b: operands, x: next });
      operands = operands$471;
    }
    return [{ tag: "Ok", value: { expr: { tag: "And", operands: $std_list.finish({ b: operands }), span: span_from({ p: p, start: s }) }, chained: true } }, p];
  } catch ($e) {
    if ($e instanceof $rt.EarlyReturn) return [$e.value, p];
    throw $e;
  }
}

export function not_expr({ p, rank }) {
  try {
    const s = here({ p: p });
    const [$r702, p$472] = accept_word({ p: p, want: "not" });
    p = p$472;
    if ($r702) {
      const [$r703, p$473] = cmp_expr({ p: p, rank: r_cmp });
      p = p$473;
      const operand = $rt.unwrap($r703);
      return [{ tag: "Ok", value: { tag: "Unary", op: "not", operand: operand, span: span_from({ p: p, start: s }) } }, p];
    }
    const [$r706, p$474] = cmp_expr({ p: p, rank: r_cmp });
    p = p$474;
    return [$r706, p];
  } catch ($e) {
    if ($e instanceof $rt.EarlyReturn) return [$e.value, p];
    throw $e;
  }
}

export function is_cmp_op({ kind }) {
  const $m707 = kind;
  $m707$match: {
    if ($m707.tag === "Punct") {
      const text = $m707.text;
      return text === "==" || text === "!=" || text === "<" || text === "<=" || text === ">" || text === ">=";
      break $m707$match;
    }
    if (true) {
      return false;
      break $m707$match;
    }
    $rt.unreachable();
  }
}

export function is_add_op({ kind }) {
  const $m708 = kind;
  $m708$match: {
    if ($m708.tag === "Punct") {
      const text = $m708.text;
      return text === "+" || text === "-" || text === "++";
      break $m708$match;
    }
    if (true) {
      return false;
      break $m708$match;
    }
    $rt.unreachable();
  }
}

export function is_mul_op({ kind }) {
  const $m709 = kind;
  $m709$match: {
    if ($m709.tag === "Punct") {
      const text = $m709.text;
      return text === "*" || text === "/" || text === "%";
      break $m709$match;
    }
    if (true) {
      return false;
      break $m709$match;
    }
    $rt.unreachable();
  }
}

export function cmp_expr({ p, rank }) {
  try {
    const s = here({ p: p });
    const [$r710, p$475] = add_expr({ p: p, rank: r_add });
    p = p$475;
    const left = $rt.unwrap($r710);
    if (at_word({ p: p, want: "is" })) {
      const [, p$476] = skip({ p: p });
      p = p$476;
      const [$r711, p$477] = parse_pattern({ p: p, rank: r_pattern });
      p = p$477;
      const pattern = $rt.unwrap($r711);
      return [{ tag: "Ok", value: { tag: "Is", expr: left, pattern: pattern, span: span_from({ p: p, start: s }) } }, p];
    }
    if (!is_cmp_op({ kind: kind_at({ p: p, n: 0 }) })) {
      return [{ tag: "Ok", value: left }, p];
    }
    const [$r715, p$478] = advance({ p: p });
    p = p$478;
    const op_tok = $r715;
    const [$r716, p$479] = add_expr({ p: p, rank: r_add });
    p = p$479;
    const right = $rt.unwrap($r716);
    const span = span_from({ p: p, start: s });
    if (is_cmp_op({ kind: kind_at({ p: p, n: 0 }) })) {
      const t = peek({ p: p, n: 0 });
      const [, p$480] = report({ p: p, code: "E0006", start: t.start, end: t.end, detail: "write `a < b and b < c`" });
      p = p$480;
      const [, p$481] = skip({ p: p });
      p = p$481;
      const [$r717, p$482] = add_expr({ p: p, rank: r_add });
      p = p$482;
      const extra = $rt.unwrap($r717);
    }
    return [{ tag: "Ok", value: { tag: "Binary", op: ident_of({ t: op_tok }).text, left: left, right: right, span: span } }, p];
  } catch ($e) {
    if ($e instanceof $rt.EarlyReturn) return [$e.value, p];
    throw $e;
  }
}

export function add_expr({ p, rank }) {
  try {
    const s = here({ p: p });
    const [$r720, p$483] = mul_expr({ p: p, rank: r_mul });
    p = p$483;
    let left = $rt.unwrap($r720);
    const toks0 = p.toks;
    const start0 = p.pos;
    while (is_add_op({ kind: kind_at({ p: p, n: 0 }) }) && p.pos < $std_list.len({ xs: p.toks }) - 1) {
      const [$r722, p$484] = advance({ p: p });
      p = p$484;
      const op_tok = $r722;
      const [$r723, p$485] = mul_expr({ p: p, rank: r_mul });
      p = p$485;
      const right = $rt.unwrap($r723);
      left = { tag: "Binary", op: ident_of({ t: op_tok }).text, left: left, right: right, span: span_from({ p: p, start: s }) };
    }
    return [{ tag: "Ok", value: left }, p];
  } catch ($e) {
    if ($e instanceof $rt.EarlyReturn) return [$e.value, p];
    throw $e;
  }
}

export function mul_expr({ p, rank }) {
  try {
    const s = here({ p: p });
    const [$r726, p$486] = unary_expr({ p: p, rank: r_unary });
    p = p$486;
    let left = $rt.unwrap($r726);
    const toks0 = p.toks;
    const start0 = p.pos;
    while (is_mul_op({ kind: kind_at({ p: p, n: 0 }) }) && p.pos < $std_list.len({ xs: p.toks }) - 1) {
      const [$r728, p$487] = advance({ p: p });
      p = p$487;
      const op_tok = $r728;
      const [$r729, p$488] = unary_expr({ p: p, rank: r_unary });
      p = p$488;
      const right = $rt.unwrap($r729);
      left = { tag: "Binary", op: ident_of({ t: op_tok }).text, left: left, right: right, span: span_from({ p: p, start: s }) };
    }
    return [{ tag: "Ok", value: left }, p];
  } catch ($e) {
    if ($e instanceof $rt.EarlyReturn) return [$e.value, p];
    throw $e;
  }
}

export function unary_expr({ p, rank }) {
  try {
    const s = here({ p: p });
    const [$r732, p$489] = accept_punct({ p: p, want: "-" });
    p = p$489;
    if ($r732) {
      const [$r733, p$490] = postfix_expr({ p: p, rank: r_postfix });
      p = p$490;
      const operand = $rt.unwrap($r733);
      return [{ tag: "Ok", value: { tag: "Unary", op: "neg", operand: operand, span: span_from({ p: p, start: s }) } }, p];
    }
    const [$r736, p$491] = postfix_expr({ p: p, rank: r_postfix });
    p = p$491;
    return [$r736, p];
  } catch ($e) {
    if ($e instanceof $rt.EarlyReturn) return [$e.value, p];
    throw $e;
  }
}

export function name_chain({ e }) {
  const $m737 = e;
  $m737$match: {
    if ($m737.tag === "Name") {
      const name = $m737.name;
      const span = $m737.span;
      return { tag: "Some", value: [name] };
      break $m737$match;
    }
    if ($m737.tag === "FieldAccess") {
      const object = $m737.object;
      const name = $m737.name;
      const span = $m737.span;
      const $m739 = name_chain_of_object({ e: object });
      $m739$match: {
        if ($m739.tag === "Some") {
          const value = $m739.value;
          return { tag: "Some", value: $std_list.append({ xs: value, x: name }) };
          break $m739$match;
        }
        if ($m739.tag === "None") {
          return { tag: "None" };
          break $m739$match;
        }
        $rt.unreachable();
      }
      break $m737$match;
    }
    if (true) {
      return { tag: "None" };
      break $m737$match;
    }
    $rt.unreachable();
  }
}

export function name_chain_of_object({ e }) {
  const $m743 = e;
  $m743$match: {
    if ($m743.tag === "Name") {
      const name = $m743.name;
      const span = $m743.span;
      return { tag: "Some", value: [name] };
      break $m743$match;
    }
    if ($m743.tag === "FieldAccess") {
      const object = $m743.object;
      const name = $m743.name;
      const span = $m743.span;
      const $m745 = single_name({ e: object });
      $m745$match: {
        if ($m745.tag === "Some") {
          const value = $m745.value;
          return { tag: "Some", value: [value, name] };
          break $m745$match;
        }
        if ($m745.tag === "None") {
          return { tag: "None" };
          break $m745$match;
        }
        $rt.unreachable();
      }
      break $m743$match;
    }
    if (true) {
      return { tag: "None" };
      break $m743$match;
    }
    $rt.unreachable();
  }
}

export function single_name({ e }) {
  const $m749 = e;
  $m749$match: {
    if ($m749.tag === "Name") {
      const name = $m749.name;
      const span = $m749.span;
      return { tag: "Some", value: name };
      break $m749$match;
    }
    if (true) {
      return { tag: "None" };
      break $m749$match;
    }
    $rt.unreachable();
  }
}

export function postfix_expr({ p, rank }) {
  try {
    const s = here({ p: p });
    const [$r752, p$492] = primary({ p: p, rank: r_primary });
    p = p$492;
    let e = $rt.unwrap($r752);
    let more = true;
    const toks0 = p.toks;
    const start0 = p.pos;
    while (more && p.pos < $std_list.len({ xs: p.toks }) - 1) {
      if (at_punct({ p: p, want: "." })) {
        if (at_name({ p: p, n: 1 })) {
          const [, p$493] = skip({ p: p });
          p = p$493;
          const [$r754, p$494] = expect_name({ p: p });
          p = p$494;
          const name = $rt.unwrap($r754);
          e = { tag: "FieldAccess", object: e, name: name, span: span_from({ p: p, start: s }) };
        } else {
          if (at_tname({ p: p, n: 1 })) {
            const $m756 = name_chain({ e: e });
            $m756$match: {
              if ($m756.tag === "Some") {
                const value = $m756.value;
                const [, p$495] = skip({ p: p });
                p = p$495;
                const [$r757, p$496] = expect_tname({ p: p });
                p = p$496;
                const tn = $rt.unwrap($r757);
                const qn = qname_of({ segments: $std_list.append({ xs: value, x: tn }) });
                const [$r758, p$497] = ctor_rest({ p: p, rank: r_ctor_rest, start: s, name: qn });
                p = p$497;
                e = $rt.unwrap($r758);
                break $m756$match;
              }
              if ($m756.tag === "None") {
                const t = peek({ p: p, n: 1 });
                const [$r759, p$498] = fail_at({ p: p, expected: "a field name after `.`", t: t });
                p = p$498;
                return [{ tag: "Err", error: $r759 }, p];
                break $m756$match;
              }
              $rt.unreachable();
            }
          } else {
            const [, p$499] = skip({ p: p });
            p = p$499;
            const [$r761, p$500] = fail({ p: p, expected: "a field name after `.`" });
            p = p$500;
            return [{ tag: "Err", error: $r761 }, p];
          }
        }
      } else {
        if (at_punct({ p: p, want: "[" })) {
          const [$r763, p$501] = type_args({ p: p, rank: r_type_args });
          p = p$501;
          const targs = $rt.unwrap($r763);
          const [$r764, p$502] = call_args({ p: p, rank: r_call_args });
          p = p$502;
          const args = $rt.unwrap($r764);
          e = { tag: "Call", callee: e, targs: { tag: "Some", value: targs }, args: args, span: span_from({ p: p, start: s }) };
        } else {
          if (at_punct({ p: p, want: "(" })) {
            const [$r767, p$503] = call_args({ p: p, rank: r_call_args });
            p = p$503;
            const args = $rt.unwrap($r767);
            e = { tag: "Call", callee: e, targs: { tag: "None" }, args: args, span: span_from({ p: p, start: s }) };
          } else {
            more = false;
          }
        }
      }
    }
    return [{ tag: "Ok", value: e }, p];
  } catch ($e) {
    if ($e instanceof $rt.EarlyReturn) return [$e.value, p];
    throw $e;
  }
}

export function call_args({ p, rank }) {
  try {
    const [$r771, p$504] = need_punct({ p: p, want: "(" });
    p = p$504;
    $rt.unwrap($r771);
    let out = $std_list.builder({  });
    const saved = p.no_brace;
    p = { ...p, no_brace: false };
    if (!at_punct({ p: p, want: ")" })) {
      let more = true;
      const toks0 = p.toks;
      const start0 = p.pos;
      while (more && p.pos < $std_list.len({ xs: p.toks }) - 1) {
        const s = here({ p: p });
        const [$r774, p$505] = expect_name({ p: p });
        p = p$505;
        const name = $rt.unwrap($r774);
        const [$r775, p$506] = need_punct({ p: p, want: ":" });
        p = p$506;
        $rt.unwrap($r775);
        const [$r776, p$507] = accept_word({ p: p, want: "inout" });
        p = p$507;
        const is_inout = $r776;
        const [$r777, p$508] = parse_expr({ p: p, rank: r_expr });
        p = p$508;
        const value = $rt.unwrap($r777);
        const [, out$509] = $std_list.push({ b: out, x: { name: name, is_inout: is_inout, value: value, span: span_from({ p: p, start: s }) } });
        out = out$509;
        const [$r779, p$510] = accept_punct({ p: p, want: "," });
        p = p$510;
        more = $r779;
      }
    }
    p = { ...p, no_brace: saved };
    const [$r781, p$511] = need_punct({ p: p, want: ")" });
    p = p$511;
    $rt.unwrap($r781);
    return [{ tag: "Ok", value: $std_list.finish({ b: out }) }, p];
  } catch ($e) {
    if ($e instanceof $rt.EarlyReturn) return [$e.value, p];
    throw $e;
  }
}

export function ctor_rest({ p, rank, start, name }) {
  try {
    let args = { tag: "None" };
    if (at_punct({ p: p, want: "(" })) {
      const [$r784, p$512] = call_args({ p: p, rank: r_call_args });
      p = p$512;
      const a = $rt.unwrap($r784);
      args = { tag: "Some", value: a };
    }
    let fields = { tag: "None" };
    if (!p.no_brace && at_punct({ p: p, want: "{" })) {
      const [$r787, p$513] = field_inits({ p: p, rank: r_field_inits });
      p = p$513;
      const f = $rt.unwrap($r787);
      fields = { tag: "Some", value: f };
    }
    return [{ tag: "Ok", value: { tag: "Ctor", name: name, args: args, fields: fields, span: span_from({ p: p, start: start }) } }, p];
  } catch ($e) {
    if ($e instanceof $rt.EarlyReturn) return [$e.value, p];
    throw $e;
  }
}

export function field_inits({ p, rank }) {
  try {
    const [$r791, p$514] = need_punct({ p: p, want: "{" });
    p = p$514;
    $rt.unwrap($r791);
    let out = $std_list.builder({  });
    const saved = p.no_brace;
    p = { ...p, no_brace: false };
    const [, p$515] = skip_newline({ p: p });
    p = p$515;
    if (!at_punct({ p: p, want: "}" })) {
      let more = true;
      const toks0 = p.toks;
      const start0 = p.pos;
      while (more && p.pos < $std_list.len({ xs: p.toks }) - 1) {
        const [, p$516] = skip_newline({ p: p });
        p = p$516;
        const s = here({ p: p });
        const [$r794, p$517] = expect_name({ p: p });
        p = p$517;
        const name = $rt.unwrap($r794);
        const [$r795, p$518] = need_punct({ p: p, want: ":" });
        p = p$518;
        $rt.unwrap($r795);
        const [$r796, p$519] = parse_expr({ p: p, rank: r_expr });
        p = p$519;
        const value = $rt.unwrap($r796);
        const [, out$520] = $std_list.push({ b: out, x: { name: name, value: value, span: span_from({ p: p, start: s }) } });
        out = out$520;
        const [$r798, p$521] = accept_punct({ p: p, want: "," });
        p = p$521;
        more = $r798;
      }
      const [, p$522] = skip_newline({ p: p });
      p = p$522;
    }
    p = { ...p, no_brace: saved };
    const [$r800, p$523] = need_punct({ p: p, want: "}" });
    p = p$523;
    $rt.unwrap($r800);
    return [{ tag: "Ok", value: $std_list.finish({ b: out }) }, p];
  } catch ($e) {
    if ($e instanceof $rt.EarlyReturn) return [$e.value, p];
    throw $e;
  }
}

export function primary({ p, rank }) {
  try {
    const s = here({ p: p });
    const t = peek({ p: p, n: 0 });
    const $m802 = t.kind;
    $m802$match: {
      if ($m802.tag === "IntLit") {
        const value = $m802.value;
        const text = $m802.text;
        const [$r803, p$524] = take({ p: p });
        p = p$524;
        const taken = $rt.unwrap($r803);
        return [{ tag: "Ok", value: { tag: "IntLit", value: value, text: text, span: span_of({ t: t }) } }, p];
        break $m802$match;
      }
      if ($m802.tag === "FloatLit") {
        const value = $m802.value;
        const [$r806, p$525] = take({ p: p });
        p = p$525;
        const taken = $rt.unwrap($r806);
        return [{ tag: "Ok", value: { tag: "FloatLit", value: value, span: span_of({ t: t }) } }, p];
        break $m802$match;
      }
      if ($m802.tag === "TextLit") {
        const value = $m802.value;
        const [$r809, p$526] = take({ p: p });
        p = p$526;
        const taken = $rt.unwrap($r809);
        return [{ tag: "Ok", value: { tag: "TextLit", value: value, span: span_of({ t: t }) } }, p];
        break $m802$match;
      }
      if ($m802.tag === "DurationLit") {
        const nanos = $m802.nanos;
        const [$r812, p$527] = take({ p: p });
        p = p$527;
        const taken = $rt.unwrap($r812);
        return [{ tag: "Ok", value: { tag: "DurationLit", nanos: nanos, span: span_of({ t: t }) } }, p];
        break $m802$match;
      }
      if ($m802.tag === "TName") {
        const text = $m802.text;
        const [$r815, p$528] = expect_tname({ p: p });
        p = p$528;
        const tn = $rt.unwrap($r815);
        const [$r816, p$529] = ctor_rest({ p: p, rank: r_ctor_rest, start: s, name: qname_of({ segments: [tn] }) });
        p = p$529;
        return [$r816, p];
        break $m802$match;
      }
      if ($m802.tag === "Keyword") {
        const word = $m802.word;
        if (word === "true" || word === "false") {
          const [$r817, p$530] = need_word({ p: p, want: word });
          p = p$530;
          $rt.unwrap($r817);
          return [{ tag: "Ok", value: { tag: "BoolLit", value: word === "true", span: span_of({ t: t }) } }, p];
        }
        if (word === "it") {
          const [$r820, p$531] = need_word({ p: p, want: word });
          p = p$531;
          $rt.unwrap($r820);
          return [{ tag: "Ok", value: { tag: "It", span: span_of({ t: t }) } }, p];
        }
        if (word === "result") {
          const [$r823, p$532] = need_word({ p: p, want: word });
          p = p$532;
          $rt.unwrap($r823);
          return [{ tag: "Ok", value: { tag: "ResultRef", span: span_of({ t: t }) } }, p];
        }
        if (word === "try") {
          const [$r826, p$533] = need_word({ p: p, want: word });
          p = p$533;
          $rt.unwrap($r826);
          const [$r827, p$534] = parse_expr({ p: p, rank: r_expr });
          p = p$534;
          const expr = $rt.unwrap($r827);
          let else_ = { tag: "None" };
          if (at_word({ p: p, want: "else" })) {
            const es = here({ p: p });
            const [, p$535] = skip({ p: p });
            p = p$535;
            let name = { text: "_", span: span_of({ t: peek({ p: p, n: 0 }) }) };
            if (at_punct({ p: p, want: "_" })) {
              const [, p$536] = skip({ p: p });
              p = p$536;
            } else {
              const [$r830, p$537] = expect_name({ p: p });
              p = p$537;
              name = $rt.unwrap($r830);
            }
            const [$r831, p$538] = need_punct({ p: p, want: ":" });
            p = p$538;
            $rt.unwrap($r831);
            const [$r832, p$539] = parse_expr({ p: p, rank: r_expr });
            p = p$539;
            const eexpr = $rt.unwrap($r832);
            else_ = { tag: "Some", value: { name: name, expr: eexpr, span: span_from({ p: p, start: es }) } };
          }
          return [{ tag: "Ok", value: { tag: "Try", expr: expr, else_: else_, span: span_from({ p: p, start: s }) } }, p];
        }
        if (word === "recover") {
          const [$r837, p$540] = need_word({ p: p, want: word });
          p = p$540;
          $rt.unwrap($r837);
          const [$r838, p$541] = parse_assertion_block({ p: p, rank: r_block });
          p = p$541;
          const body = $rt.unwrap($r838);
          return [{ tag: "Ok", value: { tag: "Recover", body: body, span: span_from({ p: p, start: s }) } }, p];
        }
        if (word === "old") {
          const [$r841, p$542] = need_word({ p: p, want: word });
          p = p$542;
          $rt.unwrap($r841);
          const [$r842, p$543] = need_punct({ p: p, want: "(" });
          p = p$543;
          $rt.unwrap($r842);
          const [$r843, p$544] = expect_name({ p: p });
          p = p$544;
          const name = $rt.unwrap($r843);
          const [$r844, p$545] = need_punct({ p: p, want: ")" });
          p = p$545;
          $rt.unwrap($r844);
          return [{ tag: "Ok", value: { tag: "Old", name: name, span: span_from({ p: p, start: s }) } }, p];
        }
        if (word === "forall" || word === "exists") {
          const [$r847, p$546] = need_word({ p: p, want: word });
          p = p$546;
          $rt.unwrap($r847);
          const [$r848, p$547] = expect_name({ p: p });
          p = p$547;
          const name = $rt.unwrap($r848);
          const [$r849, p$548] = need_punct({ p: p, want: ":" });
          p = p$548;
          $rt.unwrap($r849);
          const [$r850, p$549] = parse_binder_type({ p: p, rank: r_binder_type });
          p = p$549;
          const ty = $rt.unwrap($r850);
          let domain = { tag: "None" };
          const [$r852, p$550] = accept_word({ p: p, want: "in" });
          p = p$550;
          if ($r852) {
            const [$r853, p$551] = parse_domain({ p: p, rank: r_domain });
            p = p$551;
            const d = $rt.unwrap($r853);
            domain = { tag: "Some", value: d };
          }
          let where_ = { tag: "None" };
          const [$r856, p$552] = accept_word({ p: p, want: "where" });
          p = p$552;
          if ($r856) {
            const [$r857, p$553] = parse_expr({ p: p, rank: r_expr });
            p = p$553;
            const w = $rt.unwrap($r857);
            where_ = { tag: "Some", value: w };
          }
          const [$r859, p$554] = need_punct({ p: p, want: ":" });
          p = p$554;
          $rt.unwrap($r859);
          const [$r860, p$555] = parse_expr({ p: p, rank: r_expr });
          p = p$555;
          const body = $rt.unwrap($r860);
          return [{ tag: "Ok", value: { tag: "Quantifier", quant: word, name: name, ty: ty, domain: domain, where_: where_, body: body, span: span_from({ p: p, start: s }) } }, p];
        }
        if (word === "fn") {
          const [$r863, p$556] = need_word({ p: p, want: word });
          p = p$556;
          $rt.unwrap($r863);
          const [$r864, p$557] = param_list({ p: p, rank: r_param_list });
          p = p$557;
          const params = $rt.unwrap($r864);
          const [$r865, p$558] = need_punct({ p: p, want: "->" });
          p = p$558;
          $rt.unwrap($r865);
          const saved = p.no_brace;
          p = { ...p, no_brace: true };
          const [$r867, p$559] = parse_type({ p: p, rank: r_type });
          p = p$559;
          const ret = $rt.unwrap($r867);
          p = { ...p, no_brace: saved };
          const [$r869, p$560] = effects_opt({ p: p });
          p = p$560;
          const effects = $rt.unwrap($r869);
          const [$r870, p$561] = parse_block({ p: p, rank: r_block, allow_elided: false });
          p = p$561;
          const body = $rt.unwrap($r870);
          return [{ tag: "Ok", value: { tag: "Closure", params: params, ret: ret, effects: effects, body: body, span: span_from({ p: p, start: s }) } }, p];
        }
        if (word === "fake") {
          const [$r873, p$562] = need_word({ p: p, want: word });
          p = p$562;
          $rt.unwrap($r873);
          if (!p.is_test_module) {
            const [, p$563] = report({ p: p, code: "E0012", start: t.start, end: t.end, detail: "`fake` constructs capabilities only inside a `test module`" });
            p = p$563;
          }
          const [$r874, p$564] = qtname({ p: p });
          p = p$564;
          const capability = $rt.unwrap($r874);
          const [$r875, p$565] = field_inits({ p: p, rank: r_field_inits });
          p = p$565;
          const fields = $rt.unwrap($r875);
          return [{ tag: "Ok", value: { tag: "Fake", capability: capability, fields: fields, span: span_from({ p: p, start: s }) } }, p];
        }
        if ($tokens.is_soft_keyword({ word: word })) {
          const [$r878, p$566] = expect_name({ p: p });
          p = p$566;
          const name = $rt.unwrap($r878);
          return [{ tag: "Ok", value: { tag: "Name", name: name, span: name.span } }, p];
        }
        const [$r881, p$567] = fail({ p: p, expected: "an expression" });
        p = p$567;
        return [{ tag: "Err", error: $r881 }, p];
        break $m802$match;
      }
      if ($m802.tag === "Punct") {
        const text = $m802.text;
        if (text === "{") {
          if (p.no_brace) {
            const [$r883, p$568] = fail({ p: p, expected: "an expression (parenthesise a record update in this position)" });
            p = p$568;
            return [{ tag: "Err", error: $r883 }, p];
          }
          const [$r885, p$569] = need_punct({ p: p, want: text });
          p = p$569;
          $rt.unwrap($r885);
          const [$r886, p$570] = parse_expr({ p: p, rank: r_expr });
          p = p$570;
          const base = $rt.unwrap($r886);
          const [$r887, p$571] = need_word({ p: p, want: "with" });
          p = p$571;
          $rt.unwrap($r887);
          let fields = $std_list.builder({  });
          let more = true;
          const toks0 = p.toks;
          const start0 = p.pos;
          while (more && p.pos < $std_list.len({ xs: p.toks }) - 1) {
            const [, p$572] = skip_newline({ p: p });
            p = p$572;
            const fs = here({ p: p });
            const [$r889, p$573] = expect_name({ p: p });
            p = p$573;
            const fname = $rt.unwrap($r889);
            const [$r890, p$574] = need_punct({ p: p, want: ":" });
            p = p$574;
            $rt.unwrap($r890);
            const [$r891, p$575] = parse_expr({ p: p, rank: r_expr });
            p = p$575;
            const value = $rt.unwrap($r891);
            const [, fields$576] = $std_list.push({ b: fields, x: { name: fname, value: value, span: span_from({ p: p, start: fs }) } });
            fields = fields$576;
            const [$r893, p$577] = accept_punct({ p: p, want: "," });
            p = p$577;
            more = $r893;
          }
          const [, p$578] = skip_newline({ p: p });
          p = p$578;
          const [$r894, p$579] = need_punct({ p: p, want: "}" });
          p = p$579;
          $rt.unwrap($r894);
          return [{ tag: "Ok", value: { tag: "RecordUpdate", base: base, fields: $std_list.finish({ b: fields }), span: span_from({ p: p, start: s }) } }, p];
        }
        if (text === "(") {
          const [$r897, p$580] = need_punct({ p: p, want: text });
          p = p$580;
          $rt.unwrap($r897);
          const saved = p.no_brace;
          p = { ...p, no_brace: false };
          const [$r899, p$581] = parse_expr({ p: p, rank: r_expr });
          p = p$581;
          const inner = $rt.unwrap($r899);
          p = { ...p, no_brace: saved };
          const [$r901, p$582] = need_punct({ p: p, want: ")" });
          p = p$582;
          $rt.unwrap($r901);
          return [{ tag: "Ok", value: inner }, p];
        }
        if (text === "[") {
          const [$r903, p$583] = need_punct({ p: p, want: text });
          p = p$583;
          $rt.unwrap($r903);
          let elems = $std_list.builder({  });
          const saved = p.no_brace;
          p = { ...p, no_brace: false };
          if (!at_punct({ p: p, want: "]" })) {
            let more = true;
            const toks0 = p.toks;
            const start0 = p.pos;
            while (more && p.pos < $std_list.len({ xs: p.toks }) - 1) {
              const [$r906, p$584] = parse_expr({ p: p, rank: r_expr });
              p = p$584;
              const e = $rt.unwrap($r906);
              const [, elems$585] = $std_list.push({ b: elems, x: e });
              elems = elems$585;
              const [$r907, p$586] = accept_punct({ p: p, want: "," });
              p = p$586;
              more = $r907;
            }
          }
          p = { ...p, no_brace: saved };
          const [$r909, p$587] = need_punct({ p: p, want: "]" });
          p = p$587;
          $rt.unwrap($r909);
          return [{ tag: "Ok", value: { tag: "ListLit", elems: $std_list.finish({ b: elems }), span: span_from({ p: p, start: s }) } }, p];
        }
        const [$r912, p$588] = fail({ p: p, expected: "an expression" });
        p = p$588;
        return [{ tag: "Err", error: $r912 }, p];
        break $m802$match;
      }
      if ($m802.tag === "Name") {
        const text = $m802.text;
        const [$r914, p$589] = expect_name({ p: p });
        p = p$589;
        const name = $rt.unwrap($r914);
        return [{ tag: "Ok", value: { tag: "Name", name: name, span: name.span } }, p];
        break $m802$match;
      }
      if (true) {
        const [$r917, p$590] = fail({ p: p, expected: "an expression" });
        p = p$590;
        return [{ tag: "Err", error: $r917 }, p];
        break $m802$match;
      }
      $rt.unreachable();
    }
  } catch ($e) {
    if ($e instanceof $rt.EarlyReturn) return [$e.value, p];
    throw $e;
  }
}

export function parse_pattern({ p, rank }) {
  try {
    const s = here({ p: p });
    const t = peek({ p: p, n: 0 });
    const [$r919, p$591] = accept_punct({ p: p, want: "_" });
    p = p$591;
    if ($r919) {
      return [{ tag: "Ok", value: { tag: "WildcardPat", span: span_of({ t: t }) } }, p];
    }
    if (starts_literal_pattern({ kind: t.kind })) {
      const [$r922, p$592] = unary_expr({ p: p, rank: r_unary });
      p = p$592;
      const lit = $rt.unwrap($r922);
      if (!is_literal({ e: lit })) {
        const [$r923, p$593] = fail_at({ p: p, expected: "a literal pattern", t: t });
        p = p$593;
        return [{ tag: "Err", error: $r923 }, p];
      }
      return [{ tag: "Ok", value: { tag: "LitPat", literal: lit, span: span_from({ p: p, start: s }) } }, p];
    }
    if (at_tname({ p: p, n: 0 })) {
      const [$r927, p$594] = variant_pattern({ p: p, rank: r_variant_pattern, start: s });
      p = p$594;
      return [$r927, p];
    }
    if (at_name({ p: p, n: 0 })) {
      if (!qtname_ahead({ p: p })) {
        const [$r928, p$595] = expect_name({ p: p });
        p = p$595;
        const name = $rt.unwrap($r928);
        return [{ tag: "Ok", value: { tag: "BindPat", name: name, span: name.span } }, p];
      }
      const [$r931, p$596] = variant_pattern({ p: p, rank: r_variant_pattern, start: s });
      p = p$596;
      return [$r931, p];
    }
    const [$r932, p$597] = fail({ p: p, expected: "a pattern" });
    p = p$597;
    return [{ tag: "Err", error: $r932 }, p];
  } catch ($e) {
    if ($e instanceof $rt.EarlyReturn) return [$e.value, p];
    throw $e;
  }
}

export function starts_literal_pattern({ kind }) {
  const $m934 = kind;
  $m934$match: {
    if ($m934.tag === "IntLit") {
      const value = $m934.value;
      const text = $m934.text;
      return true;
      break $m934$match;
    }
    if ($m934.tag === "FloatLit") {
      const value = $m934.value;
      return true;
      break $m934$match;
    }
    if ($m934.tag === "TextLit") {
      const value = $m934.value;
      return true;
      break $m934$match;
    }
    if ($m934.tag === "DurationLit") {
      const nanos = $m934.nanos;
      return true;
      break $m934$match;
    }
    if ($m934.tag === "Keyword") {
      const word = $m934.word;
      return word === "true" || word === "false";
      break $m934$match;
    }
    if ($m934.tag === "Punct") {
      const text = $m934.text;
      return text === "-";
      break $m934$match;
    }
    if (true) {
      return false;
      break $m934$match;
    }
    $rt.unreachable();
  }
}

export function is_literal({ e }) {
  const $m935 = e;
  $m935$match: {
    if ($m935.tag === "IntLit") {
      const value = $m935.value;
      const text = $m935.text;
      const span = $m935.span;
      return true;
      break $m935$match;
    }
    if ($m935.tag === "FloatLit") {
      const value = $m935.value;
      const span = $m935.span;
      return true;
      break $m935$match;
    }
    if ($m935.tag === "TextLit") {
      const value = $m935.value;
      const span = $m935.span;
      return true;
      break $m935$match;
    }
    if ($m935.tag === "BoolLit") {
      const value = $m935.value;
      const span = $m935.span;
      return true;
      break $m935$match;
    }
    if ($m935.tag === "DurationLit") {
      const nanos = $m935.nanos;
      const span = $m935.span;
      return true;
      break $m935$match;
    }
    if ($m935.tag === "Unary") {
      const op = $m935.op;
      const operand = $m935.operand;
      const span = $m935.span;
      return op === "neg" && (operand.tag === "IntLit" || operand.tag === "FloatLit" || operand.tag === "DurationLit");
      break $m935$match;
    }
    if (true) {
      return false;
      break $m935$match;
    }
    $rt.unreachable();
  }
}

export function variant_pattern({ p, rank, start }) {
  try {
    const [$r936, p$598] = qtname({ p: p });
    p = p$598;
    const name = $rt.unwrap($r936);
    let fields = { tag: "None" };
    const [$r938, p$599] = accept_punct({ p: p, want: "(" });
    p = p$599;
    if ($r938) {
      let out = $std_list.builder({  });
      let more = true;
      const toks0 = p.toks;
      const start0 = p.pos;
      while (more && p.pos < $std_list.len({ xs: p.toks }) - 1) {
        const fs = here({ p: p });
        const [$r940, p$600] = accept_punct({ p: p, want: "_" });
        p = p$600;
        if ($r940) {
          const [, out$601] = $std_list.push({ b: out, x: { tag: "PatFieldSkip", span: span_from({ p: p, start: fs }) } });
          out = out$601;
        } else {
          const [$r942, p$602] = accept_punct({ p: p, want: ".." });
          p = p$602;
          if ($r942) {
            const [, out$603] = $std_list.push({ b: out, x: { tag: "PatFieldRest", span: span_from({ p: p, start: fs }) } });
            out = out$603;
          } else {
            const [$r944, p$604] = expect_name({ p: p });
            p = p$604;
            const n = $rt.unwrap($r944);
            const [, out$605] = $std_list.push({ b: out, x: { tag: "PatFieldName", name: n, span: n.span } });
            out = out$605;
          }
        }
        const [$r946, p$606] = accept_punct({ p: p, want: "," });
        p = p$606;
        more = $r946;
      }
      const [$r947, p$607] = need_punct({ p: p, want: ")" });
      p = p$607;
      $rt.unwrap($r947);
      fields = { tag: "Some", value: $std_list.finish({ b: out }) };
    }
    return [{ tag: "Ok", value: { tag: "VariantPat", name: name, fields: fields, span: span_from({ p: p, start: start }) } }, p];
  } catch ($e) {
    if ($e instanceof $rt.EarlyReturn) return [$e.value, p];
    throw $e;
  }
}

