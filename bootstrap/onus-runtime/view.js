/**
 * `std.view`'s primitives (language spec §22.3): the intrinsics a module
 * declares reach the runtime under the module's own name, so `patch` and
 * `run` are exported here and implemented beside the capability in `dom.ts`.
 */
export { patch, run } from './dom.js';
//# sourceMappingURL=view.js.map