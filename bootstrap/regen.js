// Generated by onus from module regen. Do not edit.
import * as $rt from "./onus-runtime/index.js";
import * as $std_int from "./std/int.js";
import * as $std_text from "./std/text.js";
import * as $std_list from "./std/list.js";
import * as $json from "./json.js";
import * as $std_io from "./std/io.js";
import * as $std_http from "./std/http.js";
import * as $std_map from "./std/map.js";
import * as $context from "./context.js";
import * as $loader from "./loader.js";
import * as $loc from "./loc.js";
import * as $diagjson from "./diagjson.js";
import * as $printer from "./printer.js";
import * as $std_hash from "./std/hash.js";
import * as $check from "./check.js";
import * as $interface from "./interface.js";
import * as $build from "./build.js";
import * as $testcmd from "./testcmd.js";
import * as $idiff from "./idiff.js";
import * as $codes from "./codes.js";
import * as $lexer from "./lexer.js";
import * as $parser from "./parser.js";
import * as $tokens from "./tokens.js";
import * as $assumptions from "./assumptions.js";

const $ob1 = { kind: "overflow", text: "n - 1 within Int", at: "self/regen.onus:73:41", def: "trim_end" };
const $ob2 = { kind: "overflow", text: "n - 1 - i within Int", at: "self/regen.onus:73:41", def: "trim_end" };
const $ob3 = { kind: "overflow", text: "end - 1 within Int", at: "self/regen.onus:75:15", def: "trim_end" };
const $ob4 = { kind: "overflow", text: "i - 1 within Int", at: "self/regen.onus:118:73", def: "has_word" };
const $ob5 = { kind: "overflow", text: "i + w within Int", at: "self/regen.onus:119:36", def: "has_word" };
const $ob6 = { kind: "overflow", text: "i + w within Int", at: "self/regen.onus:120:55", def: "has_word" };
const $ob7 = { kind: "overflow", text: "at + w within Int", at: "self/regen.onus:134:16", def: "matches_at" };
const $ob8 = { kind: "overflow", text: "at + k within Int", at: "self/regen.onus:138:29", def: "matches_at" };
const $ob9 = { kind: "overflow", text: "n - 1 within Int", at: "self/regen.onus:470:45", def: "strip_quotes" };
const $ob10 = { kind: "overflow", text: "n - 1 within Int", at: "self/regen.onus:472:44", def: "strip_quotes" };
const $ob11 = { kind: "overflow", text: "k + 1 within Int", at: "self/regen.onus:498:13", def: "env_line" };
const $ob12 = { kind: "overflow", text: "Text.count(t: system) + Text.count(t: prompt) within Int", at: "self/regen.onus:580:11", def: "estimate" };
const $ob13 = { kind: "overflow", text: "Text.count(t: system) + Text.count(t: prompt) + Text.count(t: text) within Int", at: "self/regen.onus:580:11", def: "estimate" };
const $ob14 = { kind: "overflow", text: "Text.count(t: system) + Text.count(t: prompt) + Text.count(t: text) + 3 within Int", at: "self/regen.onus:580:11", def: "estimate" };
const $ob15 = { kind: "overflow", text: "n - 1 within Int", at: "self/regen.onus:591:30", def: "at_or_last" };
const $ob16 = { kind: "overflow", text: "Text.count(t: head) + 1 within Int", at: "self/regen.onus:606:49", def: "model_from_spec" };
const $ob17 = { kind: "overflow", text: "Text.count(t: text) + 3 within Int", at: "self/regen.onus:754:24", def: "generate" };
const $ob18 = { kind: "overflow", text: "json.int_at(j: usage, key: \"input_tokens\") + json.int_at(j: usage, key: \"output_tokens\") within Int", at: "self/regen.onus:759:16", def: "generate" };
const $ob19 = { kind: "overflow", text: "fence + 3 within Int", at: "self/regen.onus:941:16", def: "after_fence" };
const $ob20 = { kind: "overflow", text: "i + 4 within Int", at: "self/regen.onus:948:43", def: "after_fence" };
const $ob21 = { kind: "overflow", text: "i + 4 within Int", at: "self/regen.onus:949:9", def: "after_fence" };
const $ob22 = { kind: "overflow", text: "-1 within Int", at: "self/regen.onus:951:27", def: "after_fence" };
const $ob23 = { kind: "overflow", text: "last_newline + 1 within Int", at: "self/regen.onus:968:45", def: "after_fence" };
const $ob24 = { kind: "overflow", text: "Text.count(t: text) + 1 within Int", at: "self/regen.onus:1100:19", def: "apply_repairs" };
const $ob25 = { kind: "overflow", text: "applied + 1 within Int", at: "self/regen.onus:1110:17", def: "apply_repairs" };
const $ob26 = { kind: "overflow", text: "List.len(xs: parts) - 1 within Int", at: "self/regen.onus:1410:33", def: "last_segment" };
const $ob27 = { kind: "overflow", text: "n_history - 1 within Int", at: "self/regen.onus:1567:55", def: "assemble" };
const $ob28 = { kind: "overflow", text: "n_history - 1 within Int", at: "self/regen.onus:1627:14", def: "assemble" };
const $ob29 = { kind: "overflow", text: "i + 1 within Int", at: "self/regen.onus:1643:11", def: "assemble" };
const $ob30 = { kind: "overflow", text: "n_history - 1 within Int", at: "self/regen.onus:1661:55", def: "assemble" };
const $ob31 = { kind: "overflow", text: "line - 1 within Int", at: "self/regen.onus:2295:54", def: "syntax_notes" };
const $ob32 = { kind: "overflow", text: "scan + 1 within Int", at: "self/regen.onus:2300:18", def: "syntax_notes" };
const $ob33 = { kind: "overflow", text: "line - 2 within Int", at: "self/regen.onus:2320:16", def: "syntax_notes" };
const $ob34 = { kind: "overflow", text: "-1 within Int", at: "self/regen.onus:2390:20", def: "effects_of" };
const $ob35 = { kind: "overflow", text: "i - 1 within Int", at: "self/regen.onus:2393:78", def: "effects_of" };
const $ob36 = { kind: "overflow", text: "i + 3 within Int", at: "self/regen.onus:2394:28", def: "effects_of" };
const $ob37 = { kind: "overflow", text: "i + 3 within Int", at: "self/regen.onus:2394:83", def: "effects_of" };
const $ob38 = { kind: "overflow", text: "i + 3 within Int", at: "self/regen.onus:2396:17", def: "effects_of" };
const $ob39 = { kind: "overflow", text: "k + 1 within Int", at: "self/regen.onus:2411:15", def: "effects_of" };
const $ob40 = { kind: "overflow", text: "-1 within Int", at: "self/regen.onus:2457:20", def: "last_close" };
const $ob41 = { kind: "overflow", text: "n - 1 within Int", at: "self/regen.onus:2460:20", def: "last_close" };
const $ob42 = { kind: "overflow", text: "n - 1 - i within Int", at: "self/regen.onus:2460:20", def: "last_close" };
const $ob43 = { kind: "overflow", text: "close + 1 within Int", at: "self/regen.onus:2478:63", def: "proposal_between" };
const $ob44 = { kind: "overflow", text: "r.ladder + 1 within Int", at: "self/regen.onus:2622:24", def: "escalate" };
const $ob45 = { kind: "overflow", text: "r.escalation_steps + 1 within Int", at: "self/regen.onus:2622:56", def: "escalate" };
const $ob46 = { kind: "overflow", text: "guard - 1 within Int", at: "self/regen.onus:2630:13", def: "escalate" };
const $ob47 = { kind: "overflow", text: "r.checks + 1 within Int", at: "self/regen.onus:2726:24", def: "check_working" };
const $ob48 = { kind: "overflow", text: "r.calls + 1 within Int", at: "self/regen.onus:2758:23", def: "ask" };
const $ob49 = { kind: "overflow", text: "r.tokens + answer.tokens within Int", at: "self/regen.onus:2758:44", def: "ask" };
const $ob50 = { kind: "overflow", text: "n_history - 1 within Int", at: "self/regen.onus:2910:41", def: "conclude" };
const $ob51 = { kind: "overflow", text: "io.time(clock: clock) - t0 within Int", at: "self/regen.onus:3015:54", def: "ticket" };
const $ob52 = { kind: "overflow", text: "r.claim_edits + 1 within Int", at: "self/regen.onus:3092:31", def: "splice_found" };
const $ob53 = { kind: "overflow", text: "sp.count + 1 within Int", at: "self/regen.onus:3173:41", def: "splice_into" };
const $ob54 = { kind: "overflow", text: "repairs + applied.applied within Int", at: "self/regen.onus:3258:23", def: "repair_rounds" };
const $ob55 = { kind: "overflow", text: "r.mechanical + repairs within Int", at: "self/regen.onus:3277:28", def: "repair_rounds" };
const $ob56 = { kind: "overflow", text: "io.time(clock: clock) - r.started within Int", at: "self/regen.onus:3337:6", def: "iterate" };
const $ob57 = { kind: "overflow", text: "io.time(clock: clock) - t0 within Int", at: "self/regen.onus:3418:11", def: "iterate" };
const $ob58 = { kind: "overflow", text: "r.helper_attempts + 1 within Int", at: "self/regen.onus:3443:35", def: "iterate" };
const $ob59 = { kind: "overflow", text: "io.time(clock: clock) - t0 within Int", at: "self/regen.onus:3476:13", def: "iterate" };
const $ob60 = { kind: "overflow", text: "io.time(clock: clock) - t0 within Int", at: "self/regen.onus:3509:11", def: "iterate" };
const $ob61 = { kind: "overflow", text: "io.time(clock: clock) - t0 within Int", at: "self/regen.onus:3536:11", def: "iterate" };
const $ob62 = { kind: "overflow", text: "r.mechanical - mechanical_before within Int", at: "self/regen.onus:3564:22", def: "iterate" };
const $ob63 = { kind: "overflow", text: "io.time(clock: clock) - t0 within Int", at: "self/regen.onus:3578:11", def: "iterate" };
const $ob64 = { kind: "overflow", text: "List.len(xs: checked.json) + List.len(xs: failing) within Int", at: "self/regen.onus:3591:19", def: "iterate" };
const $ob65 = { kind: "overflow", text: "size + 1 within Int", at: "self/regen.onus:3607:30", def: "iterate" };
const $ob66 = { kind: "overflow", text: "n_history - 2 within Int", at: "self/regen.onus:3609:63", def: "iterate" };
const $ob67 = { kind: "overflow", text: "List.len(xs: prev.diagnostics) + List.len(xs: prev.failing) within Int", at: "self/regen.onus:3610:23", def: "iterate" };
const $ob68 = { kind: "overflow", text: "r.grew + 1 within Int", at: "self/regen.onus:3616:30", def: "iterate" };
const $ob69 = { kind: "overflow", text: "io.time(clock: clock) - t0 within Int", at: "self/regen.onus:3640:9", def: "iterate" };
const $ob70 = { kind: "overflow", text: "stray + 1 within Int", at: "self/regen.onus:3806:15", def: "run_task" };
const $ob71 = { kind: "overflow", text: "task.budget.iterations + 1 within Int", at: "self/regen.onus:3842:31", def: "run_task" };
export function skip($args) {
  return undefined;
}

export function or_empty({ o }) {
  const $m1 = o;
  $m1$match: {
    if ($m1.tag === "Some") {
      const value = $m1.value;
      return value;
      break $m1$match;
    }
    if ($m1.tag === "None") {
      return "";
      break $m1$match;
    }
    $rt.unreachable();
  }
}

export function text_or({ o, dflt }) {
  const $m2 = o;
  $m2$match: {
    if ($m2.tag === "Some") {
      const value = $m2.value;
      return value;
      break $m2$match;
    }
    if ($m2.tag === "None") {
      return dflt;
      break $m2$match;
    }
    $rt.unreachable();
  }
}

export function int_or({ o, dflt }) {
  const $m3 = o;
  $m3$match: {
    if ($m3.tag === "Some") {
      const value = $m3.value;
      return value;
      break $m3$match;
    }
    if ($m3.tag === "None") {
      return dflt;
      break $m3$match;
    }
    $rt.unreachable();
  }
}

export function plural({ n, word }) {
  if (n === 1) {
    return $std_int.to_text({ x: n }) + " " + word;
  }
  return $std_int.to_text({ x: n }) + " " + word + "s";
}

export function trim_end({ t }) {
  const cps = $std_text.code_points({ t: t });
  const n = $std_list.len({ xs: cps });
  let end = n;
  let scanning = true;
  for (let i = 0; i < n; i++) {
    if (scanning) {
      const c = $std_list.get({ xs: cps, i: $rt.int.sub($rt.int.sub(n, 1, $ob1), i, $ob2) });
      if (c === 32 || c === 9 || c === 10 || c === 13) {
        end = $rt.int.sub(end, 1, $ob3);
      } else {
        scanning = false;
      }
    }
  }
  return slice_within({ t: t, from: 0, to: end });
}

export function sorted_texts({ xs }) {
  let out = [];
  for (const x of xs) {
    let placed = false;
    let next_out = $std_list.builder({  });
    for (const y of out) {
      if (!placed && $std_text.compare({ a: x, b: y }) < 0) {
        const [, next_out$1] = $std_list.push({ b: next_out, x: x });
        next_out = next_out$1;
        placed = true;
      }
      const [, next_out$2] = $std_list.push({ b: next_out, x: y });
      next_out = next_out$2;
    }
    if (!placed) {
      const [, next_out$3] = $std_list.push({ b: next_out, x: x });
      next_out = next_out$3;
    }
    out = $std_list.finish({ b: next_out });
  }
  return out;
}

export function is_word_char({ c }) {
  return c >= 48 && c <= 57 || c >= 65 && c <= 90 || c >= 97 && c <= 122 || c === 95;
}

export function has_word({ t, word, after_only }) {
  const cps = $std_text.code_points({ t: t });
  const wcps = $std_text.code_points({ t: word });
  const n = $std_list.len({ xs: cps });
  const w = $std_list.len({ xs: wcps });
  let found = false;
  for (let i = 0; i < n; i++) {
    if (!found && matches_at({ cps: cps, at: i, word: wcps })) {
      let ok = i === 0 || !is_word_char({ c: $std_list.get({ xs: cps, i: $rt.int.sub(i, 1, $ob4) }) });
      if (ok && !after_only && $rt.int.add(i, w, $ob5) < n) {
        ok = !is_word_char({ c: $std_list.get({ xs: cps, i: $rt.int.add(i, w, $ob6) }) });
      }
      if (ok) {
        found = true;
      }
    }
  }
  return found;
}

export function matches_at({ cps, at, word }) {
  const n = $std_list.len({ xs: cps });
  const w = $std_list.len({ xs: word });
  if (at < 0 || $rt.int.add(at, w, $ob7) > n) {
    return false;
  }
  for (let k = 0; k < w; k++) {
    if ($std_list.get({ xs: cps, i: $rt.int.add(at, k, $ob8) }) !== $std_list.get({ xs: word, i: k })) {
      return false;
    }
  }
  return true;
}

export const default_iterations = 12;

export const default_tokens = 400000;

export const default_wall_ms = 900000;

export function is_string({ j }) {
  return j.tag === "JString";
}

export function keys_within({ j, allowed }) {
  const $m4 = j;
  $m4$match: {
    if ($m4.tag === "JObject") {
      const fields = $m4.fields;
      for (const f of fields) {
        if (!$std_list.contains({ xs: allowed, x: f.key })) {
          return { tag: "Some", value: f.key };
        }
      }
      return { tag: "None" };
      break $m4$match;
    }
    if (true) {
      return { tag: "None" };
      break $m4$match;
    }
    $rt.unreachable();
  }
}

export function positive_int({ j, key, dflt }) {
  if (!$json.has({ j: j, key: key })) {
    return { tag: "Ok", value: dflt };
  }
  const $m9 = $json.at({ j: j, key: key });
  $m9$match: {
    if ($m9.tag === "JNumber") {
      const text = $m9.text;
      const $m10 = $std_int.parse({ t: text });
      $m10$match: {
        if ($m10.tag === "Some") {
          const value = $m10.value;
          if (value < 1) {
            return { tag: "Err", error: "task: /budget/" + key + ": must be >= 1" };
          }
          return { tag: "Ok", value: value };
          break $m10$match;
        }
        if ($m10.tag === "None") {
          return { tag: "Err", error: "task: /budget/" + key + ": must be integer" };
          break $m10$match;
        }
        $rt.unreachable();
      }
      break $m9$match;
    }
    if (true) {
      return { tag: "Err", error: "task: /budget/" + key + ": must be integer" };
      break $m9$match;
    }
    $rt.unreachable();
  }
}

export function str_or_none({ j }) {
  const $m15 = j;
  $m15$match: {
    if ($m15.tag === "JString") {
      const value = $m15.value;
      return { tag: "Some", value: value };
      break $m15$match;
    }
    if (true) {
      return { tag: "None" };
      break $m15$match;
    }
    $rt.unreachable();
  }
}

export function parse_task({ j }) {
  try {
    if (!(j.tag === "JObject")) {
      return { tag: "Err", error: "task: must be object" };
    }
    const $m19 = keys_within({ j: j, allowed: ["id", "kind", "scope", "target", "counterexample", "budget", "context_policy", "origin", "description", "escalation"] });
    $m19$match: {
      if ($m19.tag === "Some") {
        const value = $m19.value;
        return { tag: "Err", error: "task: must NOT have additional properties (" + value + ")" };
        break $m19$match;
      }
      if ($m19.tag === "None") {
        skip({  });
        break $m19$match;
      }
      $rt.unreachable();
    }
    if (!is_string({ j: $json.at({ j: j, key: "id" }) }) || $json.str_at({ j: j, key: "id" }) === "") {
      return { tag: "Err", error: "task: must have required property 'id'" };
    }
    const kind = $json.str_at({ j: j, key: "kind" });
    if (kind !== "implement" && kind !== "repair" && kind !== "interface_change" && kind !== "regenerate" && kind !== "ticket") {
      return { tag: "Err", error: "task: unknown kind" };
    }
    if (!($json.at({ j: j, key: "scope" }).tag === "JArray")) {
      return { tag: "Err", error: "task: must have required property 'scope'" };
    }
    let scope = $std_list.builder({  });
    for (const s of $json.list_at({ j: j, key: "scope" })) {
      if (!is_string({ j: s }) || $json.str_of({ j: s }) === "") {
        return { tag: "Err", error: "task: /scope: every item must be a non-empty string" };
      }
      const [, scope$4] = $std_list.push({ b: scope, x: $json.str_of({ j: s }) });
      scope = scope$4;
    }
    if ($std_list.built({ b: scope }) === 0) {
      return { tag: "Err", error: "task: /scope: must NOT have fewer than 1 items" };
    }
    let target = { tag: "None" };
    if ($json.has({ j: j, key: "target" })) {
      const t = $json.at({ j: j, key: "target" });
      if (!(t.tag === "JObject")) {
        return { tag: "Err", error: "task: /target: must be object" };
      }
      const $m28 = keys_within({ j: t, allowed: ["def", "obligation"] });
      $m28$match: {
        if ($m28.tag === "Some") {
          const value = $m28.value;
          return { tag: "Err", error: "task: /target: must NOT have additional properties (" + value + ")" };
          break $m28$match;
        }
        if ($m28.tag === "None") {
          skip({  });
          break $m28$match;
        }
        $rt.unreachable();
      }
      if (!is_string({ j: $json.at({ j: t, key: "def" }) })) {
        return { tag: "Err", error: "task: /target: must have required property 'def'" };
      }
      if ($json.has({ j: t, key: "obligation" }) && !is_string({ j: $json.at({ j: t, key: "obligation" }) })) {
        return { tag: "Err", error: "task: /target/obligation: must be string" };
      }
      target = { tag: "Some", value: { def: $json.str_at({ j: t, key: "def" }), obligation: str_or_none({ j: $json.at({ j: t, key: "obligation" }) }) } };
    }
    if ((kind === "implement" || kind === "repair" || kind === "interface_change") && target.tag === "None") {
      return { tag: "Err", error: "task: a " + kind + " task needs a target" };
    }
    let counterexample = { tag: "None" };
    if ($json.has({ j: j, key: "counterexample" })) {
      const c = $json.at({ j: j, key: "counterexample" });
      if (!(c.tag === "JObject")) {
        return { tag: "Err", error: "task: /counterexample: must be object" };
      }
      counterexample = { tag: "Some", value: c };
    }
    let budget = { iterations: default_iterations, tokens: default_tokens, wall_ms: default_wall_ms };
    if ($json.has({ j: j, key: "budget" })) {
      const b = $json.at({ j: j, key: "budget" });
      if (!(b.tag === "JObject")) {
        return { tag: "Err", error: "task: /budget: must be object" };
      }
      const $m40 = keys_within({ j: b, allowed: ["iterations", "tokens", "wall_ms"] });
      $m40$match: {
        if ($m40.tag === "Some") {
          const value = $m40.value;
          return { tag: "Err", error: "task: /budget: must NOT have additional properties (" + value + ")" };
          break $m40$match;
        }
        if ($m40.tag === "None") {
          skip({  });
          break $m40$match;
        }
        $rt.unreachable();
      }
      const iterations = $rt.unwrap(positive_int({ j: b, key: "iterations", dflt: default_iterations }));
      const tokens = $rt.unwrap(positive_int({ j: b, key: "tokens", dflt: default_tokens }));
      const wall_ms = $rt.unwrap(positive_int({ j: b, key: "wall_ms", dflt: default_wall_ms }));
      budget = { iterations: iterations, tokens: tokens, wall_ms: wall_ms };
    }
    let context_policy = "module";
    if ($json.has({ j: j, key: "context_policy" })) {
      const p = $json.str_at({ j: j, key: "context_policy" });
      if (p !== "none" && p !== "module" && p !== "scope") {
        return { tag: "Err", error: "task: /context_policy: must be equal to one of the allowed values" };
      }
      context_policy = p;
    }
    let origin = { tag: "None" };
    if ($json.has({ j: j, key: "origin" })) {
      const o = $json.at({ j: j, key: "origin" });
      if (!(o.tag === "JObject") || !is_string({ j: $json.at({ j: o, key: "kind" }) }) || !is_string({ j: $json.at({ j: o, key: "ref" }) })) {
        return { tag: "Err", error: "task: /origin: must have required properties 'kind' and 'ref'" };
      }
      const $m46 = keys_within({ j: o, allowed: ["kind", "ref"] });
      $m46$match: {
        if ($m46.tag === "Some") {
          const value = $m46.value;
          return { tag: "Err", error: "task: /origin: must NOT have additional properties (" + value + ")" };
          break $m46$match;
        }
        if ($m46.tag === "None") {
          skip({  });
          break $m46$match;
        }
        $rt.unreachable();
      }
      origin = { tag: "Some", value: { kind: $json.str_at({ j: o, key: "kind" }), ref: $json.str_at({ j: o, key: "ref" }) } };
    }
    let description = { tag: "None" };
    if ($json.has({ j: j, key: "description" })) {
      if (!is_string({ j: $json.at({ j: j, key: "description" }) })) {
        return { tag: "Err", error: "task: /description: must be string" };
      }
      description = { tag: "Some", value: $json.str_at({ j: j, key: "description" }) };
    }
    let helpers = false;
    let alternate = { tag: "None" };
    if ($json.has({ j: j, key: "escalation" })) {
      const e = $json.at({ j: j, key: "escalation" });
      if (!(e.tag === "JObject")) {
        return { tag: "Err", error: "task: /escalation: must be object" };
      }
      const $m55 = keys_within({ j: e, allowed: ["helpers", "alternate_model"] });
      $m55$match: {
        if ($m55.tag === "Some") {
          const value = $m55.value;
          return { tag: "Err", error: "task: /escalation: must NOT have additional properties (" + value + ")" };
          break $m55$match;
        }
        if ($m55.tag === "None") {
          skip({  });
          break $m55$match;
        }
        $rt.unreachable();
      }
      if ($json.has({ j: e, key: "helpers" }) && !($json.at({ j: e, key: "helpers" }).tag === "JBool")) {
        return { tag: "Err", error: "task: /escalation/helpers: must be boolean" };
      }
      if ($json.has({ j: e, key: "alternate_model" }) && !is_string({ j: $json.at({ j: e, key: "alternate_model" }) })) {
        return { tag: "Err", error: "task: /escalation/alternate_model: must be string" };
      }
      helpers = $json.bool_at({ j: e, key: "helpers" });
      alternate = str_or_none({ j: $json.at({ j: e, key: "alternate_model" }) });
    }
    return { tag: "Ok", value: { id: $json.str_at({ j: j, key: "id" }), kind: kind, scope: $std_list.finish({ b: scope }), target: target, counterexample: counterexample, budget: budget, context_policy: context_policy, origin: origin, description: description, helpers: helpers, alternate_model: alternate } };
  } catch ($e) {
    if ($e instanceof $rt.EarlyReturn) return $e.value;
    throw $e;
  }
}

export function target_json({ t }) {
  const $m61 = t;
  $m61$match: {
    if ($m61.tag === "None") {
      return { tag: "JNull" };
      break $m61$match;
    }
    if ($m61.tag === "Some") {
      const value = $m61.value;
      return { tag: "JObject", fields: [$json.field({ key: "def", value: $json.text({ t: value.def }) }), $json.field({ key: "obligation", value: $json.text_or_null({ o: value.obligation }) })] };
      break $m61$match;
    }
    $rt.unreachable();
  }
}

export function origin_json({ o }) {
  const $m64 = o;
  $m64$match: {
    if ($m64.tag === "None") {
      return { tag: "JNull" };
      break $m64$match;
    }
    if ($m64.tag === "Some") {
      const value = $m64.value;
      return { tag: "JObject", fields: [$json.field({ key: "kind", value: $json.text({ t: value.kind }) }), $json.field({ key: "ref", value: $json.text({ t: value.ref }) })] };
      break $m64$match;
    }
    $rt.unreachable();
  }
}

export function json_or_null({ o }) {
  const $m67 = o;
  $m67$match: {
    if ($m67.tag === "None") {
      return { tag: "JNull" };
      break $m67$match;
    }
    if ($m67.tag === "Some") {
      const value = $m67.value;
      return value;
      break $m67$match;
    }
    $rt.unreachable();
  }
}

export function task_json({ t }) {
  return { tag: "JObject", fields: [$json.field({ key: "id", value: $json.text({ t: t.id }) }), $json.field({ key: "kind", value: $json.text({ t: t.kind }) }), $json.field({ key: "scope", value: $json.texts({ ts: t.scope }) }), $json.field({ key: "target", value: target_json({ t: t.target }) }), $json.field({ key: "counterexample", value: json_or_null({ o: t.counterexample }) }), $json.field({ key: "budget", value: { tag: "JObject", fields: [$json.field({ key: "iterations", value: $json.int({ v: t.budget.iterations }) }), $json.field({ key: "tokens", value: $json.int({ v: t.budget.tokens }) }), $json.field({ key: "wall_ms", value: $json.int({ v: t.budget.wall_ms }) })] } }), $json.field({ key: "context_policy", value: $json.text({ t: t.context_policy }) }), $json.field({ key: "origin", value: origin_json({ o: t.origin }) }), $json.field({ key: "description", value: $json.text_or_null({ o: t.description }) }), $json.field({ key: "escalation", value: { tag: "JObject", fields: [$json.field({ key: "helpers", value: $json.bool({ b: t.helpers }) }), $json.field({ key: "alternate_model", value: $json.text_or_null({ o: t.alternate_model }) })] } })] };
}

export function is_key_start({ c }) {
  return c >= 65 && c <= 90 || c >= 97 && c <= 122 || c === 95;
}

export function is_key_char({ c }) {
  return is_key_start({ c: c }) || c >= 48 && c <= 57;
}

export function strip_quotes({ v }) {
  const n = $std_text.count({ t: v });
  if (n >= 2) {
    const first = $std_text.slice({ t: v, from: 0, to: 1 });
    const last = $std_text.slice({ t: v, from: $rt.int.sub(n, 1, $ob9), to: n });
    if (first === "\"" && last === "\"" || first === "'" && last === "'") {
      return $std_text.slice({ t: v, from: 1, to: $rt.int.sub(n, 1, $ob10) });
    }
  }
  return v;
}

export function env_line({ raw }) {
  let line = $std_text.trim({ t: raw });
  if (line === "" || $std_text.starts_with({ t: line, prefix: "#" })) {
    return { tag: "None" };
  }
  if ($std_text.starts_with({ t: line, prefix: "export " })) {
    line = $std_text.trim({ t: slice_within({ t: line, from: 7, to: $std_text.count({ t: line }) }) });
  }
  const cps = $std_text.code_points({ t: line });
  const n = $std_list.len({ xs: cps });
  if (n === 0 || !is_key_start({ c: $std_list.get({ xs: cps, i: 0 }) })) {
    return { tag: "None" };
  }
  let i = 1;
  let scanning = true;
  for (let k = 1; k < n; k++) {
    if (scanning) {
      if (is_key_char({ c: $std_list.get({ xs: cps, i: k }) })) {
        i = $rt.int.add(k, 1, $ob11);
      } else {
        scanning = false;
      }
    }
  }
  const key = slice_within({ t: line, from: 0, to: i });
  const rest = $std_text.trim({ t: slice_within({ t: line, from: i, to: n }) });
  if (!$std_text.starts_with({ t: rest, prefix: "=" })) {
    return { tag: "None" };
  }
  const value_text = $std_text.trim({ t: slice_within({ t: rest, from: 1, to: $std_text.count({ t: rest }) }) });
  return { tag: "Some", value: { kind: key, ref: strip_quotes({ v: value_text }) } };
}

export function parse_env({ text }) {
  let out = $std_list.builder({  });
  for (const raw of $std_text.split({ t: $std_text.replace({ t: text, from: "\r\n", to: "\n" }), sep: "\n" })) {
    const $m77 = env_line({ raw: raw });
    $m77$match: {
      if ($m77.tag === "Some") {
        const value = $m77.value;
        const [, out$5] = $std_list.push({ b: out, x: value });
        out = out$5;
        break $m77$match;
      }
      if ($m77.tag === "None") {
        skip({  });
        break $m77$match;
      }
      $rt.unreachable();
    }
  }
  return $std_list.finish({ b: out });
}

export function env_value({ env, files, dirs, name }) {
  const $m78 = $std_io.get_env({ env: env, name: name });
  $m78$match: {
    if ($m78.tag === "Some") {
      const value = $m78.value;
      return { tag: "Some", value: value };
      break $m78$match;
    }
    if ($m78.tag === "None") {
      skip({  });
      break $m78$match;
    }
    $rt.unreachable();
  }
  for (const dir of dirs) {
    for (const file_name of [".env", ".env.local"]) {
      const $m80 = $std_io.read({ files: files, path: dir + "/" + file_name });
      $m80$match: {
        if ($m80.tag === "Err") {
          skip({  });
          break $m80$match;
        }
        if ($m80.tag === "Ok") {
          const value = $m80.value;
          for (const pair of parse_env({ text: value })) {
            if (pair.kind === name) {
              return { tag: "Some", value: pair.ref };
            }
          }
          break $m80$match;
        }
        $rt.unreachable();
      }
    }
  }
  return { tag: "None" };
}

export const request_timeout_ms = 180000;

export function model_name({ m }) {
  const $m83 = m;
  $m83$match: {
    if ($m83.tag === "Scripted") {
      return "scripted";
      break $m83$match;
    }
    if ($m83.tag === "ClaudeCode") {
      const model_id = $m83.model_id;
      const $m84 = model_id;
      $m84$match: {
        if ($m84.tag === "Some") {
          const value = $m84.value;
          return "claude-code:" + value;
          break $m84$match;
        }
        if ($m84.tag === "None") {
          return "claude-code";
          break $m84$match;
        }
        $rt.unreachable();
      }
      break $m83$match;
    }
    if ($m83.tag === "Anthropic") {
      const model_id = $m83.model_id;
      return "anthropic:" + model_id;
      break $m83$match;
    }
    if ($m83.tag === "OpenRouter") {
      const model_id = $m83.model_id;
      return "openrouter:" + model_id;
      break $m83$match;
    }
    $rt.unreachable();
  }
}

export function estimate({ system, prompt, text }) {
  return Math.trunc($rt.int.add($rt.int.add($rt.int.add($std_text.count({ t: system }), $std_text.count({ t: prompt }), $ob12), $std_text.count({ t: text }), $ob13), 3, $ob14) / 4);
}

export function at_or_last({ xs, i }) {
  const n = $std_list.len({ xs: xs });
  if (n === 0) {
    return "";
  }
  if (i >= 0 && i < n) {
    return $std_list.get({ xs: xs, i: i });
  }
  return $std_list.get({ xs: xs, i: $rt.int.sub(n, 1, $ob15) });
}

export function model_from_spec({ spec, env, files, env_dirs }) {
  const parts = $std_text.split({ t: spec, sep: ":" });
  const head = $std_list.get({ xs: parts, i: 0 });
  let arg = { tag: "None" };
  if ($std_list.len({ xs: parts }) > 1) {
    arg = { tag: "Some", value: $std_text.slice({ t: spec, from: $rt.int.add($std_text.count({ t: head }), 1, $ob16), to: $std_text.count({ t: spec }) }) };
  }
  if (head === "claude-code") {
    return { tag: "Ok", value: { tag: "ClaudeCode", model_id: arg } };
  }
  if (head === "anthropic") {
    const key = or_empty({ o: env_value({ env: env, files: files, dirs: env_dirs, name: "ANTHROPIC_API_KEY" }) });
    if (key === "") {
      return { tag: "Err", error: "the anthropic model needs ANTHROPIC_API_KEY" };
    }
    return { tag: "Ok", value: { tag: "Anthropic", key: key, model_id: text_or({ o: arg, dflt: "claude-fable-5-1" }) } };
  }
  if (head === "openrouter") {
    const key = or_empty({ o: env_value({ env: env, files: files, dirs: env_dirs, name: "OPENROUTER_API_KEY" }) });
    if (key === "") {
      return { tag: "Err", error: "the openrouter model needs OPENROUTER_API_KEY (in the environment or a .env.local at the project root)" };
    }
    const configured = text_or({ o: env_value({ env: env, files: files, dirs: env_dirs, name: "OPENROUTER_MODEL" }), dflt: "deepseek/deepseek-v4-flash" });
    return { tag: "Ok", value: { tag: "OpenRouter", key: key, model_id: text_or({ o: arg, dflt: configured }) } };
  }
  if (head === "scripted") {
    const $m95 = arg;
    $m95$match: {
      if ($m95.tag === "None") {
        return { tag: "Err", error: "scripted:<file.json> names a JSON array of answers" };
        break $m95$match;
      }
      if ($m95.tag === "Some") {
        const value = $m95.value;
        return scripted_from({ files: files, path: value });
        break $m95$match;
      }
      $rt.unreachable();
    }
  }
  return { tag: "Err", error: "unknown model spec " + spec + "; use claude-code[:<model>], anthropic[:<model>], openrouter[:<model>] or scripted:<file.json>" };
}

export function scripted_from({ files, path }) {
  const $m98 = $std_io.read({ files: files, path: path });
  $m98$match: {
    if ($m98.tag === "Err") {
      return { tag: "Err", error: "cannot read " + path };
      break $m98$match;
    }
    if ($m98.tag === "Ok") {
      const value = $m98.value;
      return scripted_of_text({ path: path, text: value });
      break $m98$match;
    }
    $rt.unreachable();
  }
}

export function scripted_of_text({ path, text }) {
  const $m100 = $json.parse({ t: text });
  $m100$match: {
    if ($m100.tag === "None") {
      return { tag: "Err", error: path + ": not a JSON array of strings" };
      break $m100$match;
    }
    if ($m100.tag === "Some") {
      const value = $m100.value;
      const $m102 = value;
      $m102$match: {
        if ($m102.tag === "JArray") {
          const items = $m102.items;
          let out = $std_list.builder({  });
          for (const x of items) {
            if (!is_string({ j: x })) {
              return { tag: "Err", error: path + ": not a JSON array of strings" };
            }
            const [, out$6] = $std_list.push({ b: out, x: $json.str_of({ j: x }) });
            out = out$6;
          }
          return { tag: "Ok", value: { tag: "Scripted", answers: $std_list.finish({ b: out }) } };
          break $m102$match;
        }
        if (true) {
          return { tag: "Err", error: path + ": not a JSON array of strings" };
          break $m102$match;
        }
        $rt.unreachable();
      }
      break $m100$match;
    }
    $rt.unreachable();
  }
}

export function generate({ m, process, net, call, system, prompt, max_tokens }) {
  try {
    const $m107 = m;
    $m107$match: {
      if ($m107.tag === "Scripted") {
        const answers = $m107.answers;
        const text = at_or_last({ xs: answers, i: call });
        return { tag: "Ok", value: { text: text, model: "scripted", tokens: estimate({ system: system, prompt: prompt, text: text }) } };
        break $m107$match;
      }
      if ($m107.tag === "ClaudeCode") {
        const model_id = $m107.model_id;
        let args = ["-c", "for v in $(env | sed -n 's/^\\(CLAUDE[A-Za-z0-9_]*\\)=.*/\\1/p'); do unset \"$v\"; done; exec claude \"$@\"", "sh", "-p", "--output-format", "text", "--system-prompt", system];
        const $m110 = model_id;
        $m110$match: {
          if ($m110.tag === "Some") {
            const value = $m110.value;
            args = [...args, ...["--model", value]];
            break $m110$match;
          }
          if ($m110.tag === "None") {
            skip({  });
            break $m110$match;
          }
          $rt.unreachable();
        }
        const $m111 = $std_io.run({ process: process, program: "sh", args: args, stdin: prompt, timeout_ms: 900000 });
        $m111$match: {
          if ($m111.tag === "Err") {
            const error = $m111.error;
            return { tag: "Err", error: "claude: " + io_error_text({ e: error }) };
            break $m111$match;
          }
          if ($m111.tag === "Ok") {
            const value = $m111.value;
            if (value.status !== 0) {
              return { tag: "Err", error: "claude exited " + $std_int.to_text({ x: value.status }) + ": " + value.stderr };
            }
            return { tag: "Ok", value: { text: value.stdout, model: model_name({ m: m }), tokens: estimate({ system: system, prompt: prompt, text: value.stdout }) } };
            break $m111$match;
          }
          $rt.unreachable();
        }
        break $m107$match;
      }
      if ($m107.tag === "Anthropic") {
        const key = $m107.key;
        const model_id = $m107.model_id;
        const body = { tag: "JObject", fields: [$json.field({ key: "model", value: $json.text({ t: model_id }) }), $json.field({ key: "max_tokens", value: $json.int({ v: max_tokens }) }), $json.field({ key: "system", value: $json.text({ t: system }) }), $json.field({ key: "messages", value: { tag: "JArray", items: [message({ role: "user", content: prompt })] } })] };
        const reply = $rt.unwrap(post({ net: net, provider: "anthropic", url: "https://api.anthropic.com/v1/messages", headers: ["content-type: application/json", "x-api-key: " + key, "anthropic-version: 2023-06-01"], body: body }));
        const text = content_text({ body: reply });
        const usage = $json.at({ j: reply, key: "usage" });
        let tokens = Math.trunc($rt.int.add($std_text.count({ t: text }), 3, $ob17) / 4);
        if ($json.at({ j: usage, key: "input_tokens" }).tag === "JNumber" && $json.at({ j: usage, key: "output_tokens" }).tag === "JNumber") {
          tokens = $rt.int.add($json.int_at({ j: usage, key: "input_tokens" }), $json.int_at({ j: usage, key: "output_tokens" }), $ob18);
        }
        return { tag: "Ok", value: { text: text, model: model_name({ m: m }), tokens: tokens } };
        break $m107$match;
      }
      if ($m107.tag === "OpenRouter") {
        const key = $m107.key;
        const model_id = $m107.model_id;
        const body = { tag: "JObject", fields: [$json.field({ key: "model", value: $json.text({ t: model_id }) }), $json.field({ key: "max_tokens", value: $json.int({ v: max_tokens }) }), $json.field({ key: "messages", value: { tag: "JArray", items: [message({ role: "system", content: system }), message({ role: "user", content: prompt })] } })] };
        const reply = $rt.unwrap(post({ net: net, provider: "openrouter", url: "https://openrouter.ai/api/v1/chat/completions", headers: ["content-type: application/json", "authorization: Bearer " + key, "x-title: onus loop"], body: body }));
        const err = $json.at({ j: reply, key: "error" });
        if (err.tag === "JObject" && $json.has({ j: err, key: "message" })) {
          return { tag: "Err", error: "openrouter: " + $json.str_at({ j: err, key: "message" }) };
        }
        const text = choice_text({ body: reply });
        const usage = $json.at({ j: reply, key: "usage" });
        let tokens = estimate({ system: system, prompt: prompt, text: text });
        if ($json.at({ j: usage, key: "total_tokens" }).tag === "JNumber") {
          tokens = $json.int_at({ j: usage, key: "total_tokens" });
        }
        return { tag: "Ok", value: { text: text, model: model_name({ m: m }), tokens: tokens } };
        break $m107$match;
      }
      $rt.unreachable();
    }
  } catch ($e) {
    if ($e instanceof $rt.EarlyReturn) return $e.value;
    throw $e;
  }
}

export function io_error_text({ e }) {
  const $m125 = e;
  $m125$match: {
    if ($m125.tag === "NotFound") {
      const path = $m125.path;
      return "not found: " + path;
      break $m125$match;
    }
    if ($m125.tag === "Denied") {
      const path = $m125.path;
      return "denied: " + path;
      break $m125$match;
    }
    if ($m125.tag === "Other") {
      const detail = $m125.detail;
      return detail;
      break $m125$match;
    }
    $rt.unreachable();
  }
}

export function message({ role, content }) {
  return { tag: "JObject", fields: [$json.field({ key: "role", value: $json.text({ t: role }) }), $json.field({ key: "content", value: $json.text({ t: content }) })] };
}

export function post({ net, provider, url, headers, body }) {
  const $m127 = $std_http.post({ net: net, url: url, headers: headers, body: $json.compact({ j: body }), timeout_ms: request_timeout_ms });
  $m127$match: {
    if ($m127.tag === "Err") {
      const error = $m127.error;
      return { tag: "Err", error: provider + ": " + io_error_text({ e: error }) };
      break $m127$match;
    }
    if ($m127.tag === "Ok") {
      const value = $m127.value;
      if (value.status < 200 || value.status >= 300) {
        return { tag: "Err", error: provider + ": HTTP " + $std_int.to_text({ x: value.status }) + ": " + value.body };
      }
      return parse_reply({ provider: provider, body: value.body });
      break $m127$match;
    }
    $rt.unreachable();
  }
}

export function parse_reply({ provider, body }) {
  const $m130 = $json.parse({ t: body });
  $m130$match: {
    if ($m130.tag === "None") {
      return { tag: "Err", error: provider + ": the response is not JSON" };
      break $m130$match;
    }
    if ($m130.tag === "Some") {
      const value = $m130.value;
      return { tag: "Ok", value: value };
      break $m130$match;
    }
    $rt.unreachable();
  }
}

export function content_text({ body }) {
  let parts = $std_list.builder({  });
  for (const block of $json.list_at({ j: body, key: "content" })) {
    if ($json.str_at({ j: block, key: "type" }) === "text" && is_string({ j: $json.at({ j: block, key: "text" }) })) {
      const [, parts$7] = $std_list.push({ b: parts, x: $json.str_at({ j: block, key: "text" }) });
      parts = parts$7;
    }
  }
  return $std_text.join({ parts: $std_list.finish({ b: parts }), sep: "" });
}

export function choice_text({ body }) {
  const choices = $json.list_at({ j: body, key: "choices" });
  if ($std_list.len({ xs: choices }) === 0) {
    return "";
  }
  const content = $json.at({ j: $json.at({ j: $std_list.get({ xs: choices, i: 0 }), key: "message" }), key: "content" });
  const $m133 = content;
  $m133$match: {
    if ($m133.tag === "JString") {
      const value = $m133.value;
      return value;
      break $m133$match;
    }
    if ($m133.tag === "JArray") {
      const items = $m133.items;
      let parts = $std_list.builder({  });
      for (const part of items) {
        const [, parts$8] = $std_list.push({ b: parts, x: $json.str_at({ j: part, key: "text" }) });
        parts = parts$8;
      }
      return $std_text.join({ parts: $std_list.finish({ b: parts }), sep: "" });
      break $m133$match;
    }
    if (true) {
      return "";
      break $m133$match;
    }
    $rt.unreachable();
  }
}

export function empty_table($args) {
  return $std_map.dict({  });
}

export function parse_module({ path, text }) {
  let ctx = $context.new_context({ root: { tag: "None" }, stdlib: { tag: "None" } });
  const [$r136, ctx$9] = $loader.add_file({ ctx: ctx, path: path, text: text });
  ctx = ctx$9;
  const added = $r136;
  let module = { tag: "None" };
  if (added.tag === "Some") {
    module = $loader.module_of_file({ ctx: ctx, file: 0 });
  }
  const diagnostics = $std_list.finish({ b: ctx.diagnostics });
  const tab = $loc.line_tables({ ctx: ctx });
  let out = $std_list.builder({  });
  for (const d of diagnostics) {
    const [, out$10] = $std_list.push({ b: out, x: $diagjson.to_json({ ctx: ctx, tab: tab, d: d }) });
    out = out$10;
  }
  return { module: module, diagnostics: diagnostics, json: $std_list.finish({ b: out }), text: text };
}

export function canonical_text({ path, text }) {
  let ctx = $context.new_context({ root: { tag: "None" }, stdlib: { tag: "None" } });
  const [$r141, ctx$11] = $loader.add_file({ ctx: ctx, path: path, text: text });
  ctx = ctx$11;
  const added = $r141;
  if (added.tag === "None") {
    return text;
  }
  const [, ctx$12] = $loader.canonical({ ctx: ctx, file: 0 });
  ctx = ctx$12;
  const $m142 = $std_map.find({ d: ctx.canonical, key: 0 });
  $m142$match: {
    if ($m142.tag === "Some") {
      const value = $m142.value;
      return value;
      break $m142$match;
    }
    if ($m142.tag === "None") {
      return text;
      break $m142$match;
    }
    $rt.unreachable();
  }
}

export function extract_code({ answer }) {
  const $m143 = $std_text.index_of({ t: answer, needle: "```", from: 0 });
  $m143$match: {
    if ($m143.tag === "None") {
      return $std_text.trim({ t: answer });
      break $m143$match;
    }
    if ($m143.tag === "Some") {
      const value = $m143.value;
      return after_fence({ answer: answer, fence: value });
      break $m143$match;
    }
    $rt.unreachable();
  }
}

export function after_fence({ answer, fence }) {
  const cps = $std_text.code_points({ t: answer });
  const n = $std_list.len({ xs: cps });
  let i = $rt.int.add(fence, 3, $ob19);
  if (i < 0) {
    i = 0;
  }
  if (i > n) {
    i = n;
  }
  if (slice_within({ t: answer, from: i, to: $rt.int.add(i, 4, $ob20) }) === "onus") {
    i = $rt.int.add(i, 4, $ob21);
  }
  let last_newline = $rt.int.neg(1, $ob22);
  let scanning = true;
  for (let k = 0; k < n; k++) {
    if (k >= i && scanning) {
      const c = $std_list.get({ xs: cps, i: k });
      if (c === 32 || c === 9 || c === 13 || c === 10) {
        if (c === 10) {
          last_newline = k;
        }
      } else {
        scanning = false;
      }
    }
  }
  if (last_newline < 0) {
    return $std_text.trim({ t: answer });
  }
  return fenced_body({ answer: answer, start: $rt.int.add(last_newline, 1, $ob23) });
}

export function fenced_body({ answer, start }) {
  if (start < 0 || start > $std_text.count({ t: answer })) {
    return $std_text.trim({ t: answer });
  }
  const $m144 = $std_text.index_of({ t: answer, needle: "```", from: start });
  $m144$match: {
    if ($m144.tag === "None") {
      return $std_text.trim({ t: answer });
      break $m144$match;
    }
    if ($m144.tag === "Some") {
      const value = $m144.value;
      return $std_text.trim({ t: slice_within({ t: answer, from: start, to: value }) });
      break $m144$match;
    }
    $rt.unreachable();
  }
}

export function parse_fragment({ text }) {
  return parse_module({ path: "model-output.onus", text: "module model_output\n\n" + text + "\n" });
}

export function fns_of({ m }) {
  let out = $std_list.builder({  });
  for (const item of m.items) {
    const $m145 = item;
    $m145$match: {
      if ($m145.tag === "FnItem") {
        const decl = $m145.decl;
        const [, out$13] = $std_list.push({ b: out, x: decl });
        out = out$13;
        break $m145$match;
      }
      if (true) {
        skip({  });
        break $m145$match;
      }
      $rt.unreachable();
    }
  }
  return $std_list.finish({ b: out });
}

export function find_fn({ m, name }) {
  for (const f of fns_of({ m: m })) {
    if (f.name.text === name) {
      return { tag: "Some", value: f };
    }
  }
  return { tag: "None" };
}

export function has_body({ f }) {
  const $m148 = f.body;
  $m148$match: {
    if ($m148.tag === "Some") {
      const value = $m148.value;
      return !value.elided;
      break $m148$match;
    }
    if ($m148.tag === "None") {
      return false;
      break $m148$match;
    }
    $rt.unreachable();
  }
}

export function elide({ f }) {
  const $m149 = f.body;
  $m149$match: {
    if ($m149.tag === "None") {
      return f;
      break $m149$match;
    }
    if ($m149.tag === "Some") {
      const value = $m149.value;
      return { ...f, body: { tag: "Some", value: { stmts: [], elided: true, span: value.span } } };
      break $m149$match;
    }
    $rt.unreachable();
  }
}

export function with_body({ f, body }) {
  return { ...f, body: { tag: "Some", value: body } };
}

export function item_text({ item }) {
  return $std_text.trim({ t: $printer.print_item({ item: item, tab: $std_map.empty({  }), elide: false }) });
}

export function signature_text({ f }) {
  return item_text({ item: { tag: "FnItem", decl: elide({ f: f }) } });
}

export function body_hash({ f }) {
  return $std_hash.blake3_hex({ t: $printer.print_item({ item: { tag: "FnItem", decl: f }, tab: $std_map.empty({  }), elide: false }) });
}

export function slice_within({ t, from, to }) {
  const n = $std_text.count({ t: t });
  let a = from;
  let b = to;
  if (a < 0) {
    a = 0;
  }
  if (a > n) {
    a = n;
  }
  if (b < a) {
    b = a;
  }
  if (b > n) {
    b = n;
  }
  return $std_text.slice({ t: t, from: a, to: b });
}

export function splice_item({ text, span, replacement }) {
  return slice_within({ t: text, from: 0, to: span.start }) + trim_end({ t: $printer.print_item({ item: replacement, tab: $std_map.empty({  }), elide: false }) }) + slice_within({ t: text, from: span.end, to: $std_text.count({ t: text }) });
}

export function sorted_edits({ edits }) {
  let out = [];
  for (const e of edits) {
    let placed = false;
    let next_out = $std_list.builder({  });
    for (const y of out) {
      if (!placed && e.start > y.start) {
        const [, next_out$14] = $std_list.push({ b: next_out, x: e });
        next_out = next_out$14;
        placed = true;
      }
      const [, next_out$15] = $std_list.push({ b: next_out, x: y });
      next_out = next_out$15;
    }
    if (!placed) {
      const [, next_out$16] = $std_list.push({ b: next_out, x: e });
      next_out = next_out$16;
    }
    out = $std_list.finish({ b: next_out });
  }
  return out;
}

export function apply_repairs({ text, edits }) {
  let out = text;
  let last = $rt.int.add($std_text.count({ t: text }), 1, $ob24);
  let applied = 0;
  for (const e of sorted_edits({ edits: edits })) {
    if (e.end <= last) {
      out = slice_within({ t: out, from: 0, to: e.start }) + e.with_text + slice_within({ t: out, from: e.end, to: $std_text.count({ t: out }) });
      last = e.start;
      applied = $rt.int.add(applied, 1, $ob25);
    }
  }
  return { text: out, applied: applied };
}

export function module_path({ root, name }) {
  const rel = $std_text.replace({ t: name, from: ".", to: "/" }) + ".onus";
  if (root === ".") {
    return rel;
  }
  return root + "/" + rel;
}

export function load_project({ files, root, scope }) {
  let paths = $std_map.dict({  });
  let texts = $std_map.dict({  });
  for (const name of scope) {
    const path = module_path({ root: root, name: name });
    const $m158 = $std_io.read({ files: files, path: path });
    $m158$match: {
      if ($m158.tag === "Err") {
        const error = $m158.error;
        return { tag: "Err", error: "cannot read module " + name + " at " + path + ": " + io_error_text({ e: error }) };
        break $m158$match;
      }
      if ($m158.tag === "Ok") {
        const value = $m158.value;
        const [, texts$17] = $std_map.set({ d: texts, key: path, value: value });
        texts = texts$17;
        break $m158$match;
      }
      $rt.unreachable();
    }
    const [, paths$18] = $std_map.set({ d: paths, key: name, value: path });
    paths = paths$18;
  }
  return { tag: "Ok", value: { root: root, scope: scope, paths: paths, texts: texts } };
}

export function text_of({ texts, fallback, path }) {
  const $m162 = $std_map.find({ d: texts, key: path });
  $m162$match: {
    if ($m162.tag === "Some") {
      const value = $m162.value;
      return value;
      break $m162$match;
    }
    if ($m162.tag === "None") {
      const $m163 = $std_map.find({ d: fallback, key: path });
      $m163$match: {
        if ($m163.tag === "Some") {
          const value = $m163.value;
          return value;
          break $m163$match;
        }
        if ($m163.tag === "None") {
          return "";
          break $m163$match;
        }
        $rt.unreachable();
      }
      break $m162$match;
    }
    $rt.unreachable();
  }
}

export function check_project({ files, console, process, project, tools, texts, run_tests, out_dir }) {
  try {
    let ctx = $context.new_context({ root: { tag: "Some", value: project.root }, stdlib: tools.stdlib });
    for (const name of project.scope) {
      const $m165 = $std_map.find({ d: project.paths, key: name });
      $m165$match: {
        if ($m165.tag === "Some") {
          const value = $m165.value;
          const [$r166, ctx$19] = $loader.add_file({ ctx: ctx, path: value, text: text_of({ texts: texts, fallback: project.texts, path: value }) });
          ctx = ctx$19;
          const added = $r166;
          break $m165$match;
        }
        if ($m165.tag === "None") {
          skip({  });
          break $m165$match;
        }
        $rt.unreachable();
      }
    }
    let budget = 2000;
    if (tools.budget_ms >= 0) {
      budget = tools.budget_ms;
    }
    const [, ctx$20] = $check.run_passes({ ctx: ctx, to: 11, files: files, console: console, process: process, vopts: { z3_path: { tag: "None" }, budget_ms: budget, cache_dir: { tag: "None" }, dump_dir: { tag: "None" } } });
    ctx = ctx$20;
    const diagnostics = $std_list.finish({ b: ctx.diagnostics });
    const tab = $loc.line_tables({ ctx: ctx });
    let json_out = $std_list.builder({  });
    let internal_error = false;
    for (const d of diagnostics) {
      const [, json_out$21] = $std_list.push({ b: json_out, x: $diagjson.to_json({ ctx: ctx, tab: tab, d: d }) });
      json_out = json_out$21;
      if (d.code === "E0999") {
        internal_error = true;
      }
    }
    let interfaces = $std_map.dict({  });
    let ledger = $std_list.builder({  });
    const clean = $context.clean({ ctx: ctx });
    if (clean) {
      const $hi171 = ctx.module_count;
      for (let mid = 0; mid < $hi171; mid++) {
        const $m172 = $std_map.find({ d: ctx.modules, key: mid });
        $m172$match: {
          if ($m172.tag === "Some") {
            const value = $m172.value;
            if (!value.is_std) {
              const doc = $interface.interface_of({ ctx: ctx, mod: mid });
              const [, interfaces$22] = $std_map.set({ d: interfaces, key: value.name, value: doc });
              interfaces = interfaces$22;
              for (const l of $json.list_at({ j: doc, key: "ledger" })) {
                const [, ledger$23] = $std_list.push({ b: ledger, x: { def: value.name + "." + $json.str_at({ j: l, key: "def" }), kind: $json.str_at({ j: l, key: "kind" }), text: $json.str_at({ j: l, key: "text" }), status: $json.str_at({ j: l, key: "status" }) } });
                ledger = ledger$23;
              }
            }
            break $m172$match;
          }
          if ($m172.tag === "None") {
            skip({  });
            break $m172$match;
          }
          $rt.unreachable();
        }
      }
    }
    let tests = { tag: "None" };
    if (clean && run_tests) {
      if (tools.bundled) {
        $rt.unwrap($build.write_bundled_runtime({ files: files, out_dir: out_dir }));
      }
      const built = $rt.unwrap($build.emit_all({ ctx: ctx, files: files, opts: { out_dir: out_dir, runtime: tools.runtime, verify_all: false, negate_guard: { tag: "None" } } }));
      let any_tests = false;
      for (const m of built.emitted) {
        if (m.tests.tag === "Some") {
          any_tests = true;
        }
      }
      if (any_tests) {
        tests = { tag: "Some", value: $testcmd.run_js_examples({ files: files, process: process, out_dir: out_dir, emitted: built.emitted, include_properties: true }) };
      } else {
        tests = { tag: "Some", value: $std_map.dict({  }) };
      }
    }
    return { tag: "Ok", value: { ctx: ctx, tab: tab, diagnostics: diagnostics, json: $std_list.finish({ b: json_out }), internal_error: internal_error, interfaces: interfaces, ledger: $std_list.finish({ b: ledger }), tests: tests } };
  } catch ($e) {
    if ($e instanceof $rt.EarlyReturn) return $e.value;
    throw $e;
  }
}

export function module_items({ c, name }) {
  const $m181 = $std_map.find({ d: c.ctx.by_name, key: name });
  $m181$match: {
    if ($m181.tag === "None") {
      return [];
      break $m181$match;
    }
    if ($m181.tag === "Some") {
      const value = $m181.value;
      return module_items_of({ c: c, mid: value });
      break $m181$match;
    }
    $rt.unreachable();
  }
}

export function module_items_of({ c, mid }) {
  const $m182 = $std_map.find({ d: c.ctx.modules, key: mid });
  $m182$match: {
    if ($m182.tag === "None") {
      return [];
      break $m182$match;
    }
    if ($m182.tag === "Some") {
      const value = $m182.value;
      return value.tree.items;
      break $m182$match;
    }
    $rt.unreachable();
  }
}

export function module_name_of({ c, mid }) {
  const $m183 = $std_map.find({ d: c.ctx.modules, key: mid });
  $m183$match: {
    if ($m183.tag === "None") {
      return "";
      break $m183$match;
    }
    if ($m183.tag === "Some") {
      const value = $m183.value;
      return value.name;
      break $m183$match;
    }
    $rt.unreachable();
  }
}

export function file_path_of({ c, file }) {
  if (file < 0 || file >= $std_list.built({ b: c.ctx.files })) {
    return "";
  }
  return $context.file_at({ ctx: c.ctx, id: file }).path;
}

export const system_prompt = "You write function bodies in Onus, a language in which a model writes bodies, a human owns the contracts, and the compiler is the only checker.\n\nRules:\n- Write only the bodies of the functions listed under \"Targets\". Return each target function complete, signature and body, inside one fenced code block marked onus, and nothing else.\n- Never change a signature, contract (requires, ensures, decreases), effect list (may ...), claim, type, example or property. Never add functions, types or imports.\n- Use only what the interfaces shown declare. Bodies of imports are not available and not needed.\n- Every obligation must be proved or checked: satisfy every ensures on every path, establish every requires at every call, keep loop invariants, and give every loop a decreases measure.\n- Prefer the simplest body that satisfies the contracts.\n\nOnus syntax notes:\n- Statements end at a newline. A block is { ... }. Comments start with --.\n- let x: T = e   var x: T = e   x = e   return e\n- if c { ... } else { ... }\n- match e { Variant(field: pattern) => expr, _ => expr }; union values are built as Variant(field: value); records as Name { field: value }; a record is copied with { r with field: value }\n- for i in lo ..< hi { ... } iterates a half-open range; loop while c decreases m { ... } is a while loop with a measure; both take invariant e clauses after the header.\n- Calls name every argument: f(a: 1, b: 2). Results are Ok(value: v) and Err(error: e); options Some(value: v) and None. try e unwraps a Result or Option or returns early.\n- Arithmetic on Int is checked; / and % are integer operations; comparison is == != < <= > >=; Bool uses and, or, not.\n- A closure is fn(x: T) -> U { ... }. Text concatenation is ++.";

export function target_names({ targets }) {
  let out = $std_map.dict({  });
  for (const t of targets) {
    const [, out$24] = $std_map.set({ d: out, key: t.module_name + "." + t.name, value: true });
    out = out$24;
  }
  return out;
}

export function is_target_name({ targets, name }) {
  for (const t of targets) {
    if (t.name === name) {
      return true;
    }
  }
  return false;
}

export function fenced({ texts }) {
  return "```onus\n" + $std_text.join({ parts: texts, sep: "\n\n" }) + "\n```";
}

export function relevant({ d, targets }) {
  const location = $json.at({ j: d, key: "location" });
  if ($json.str_at({ j: d, key: "code" }) === "E0115" && !$json.is_null({ j: $json.at({ j: location, key: "def" }) })) {
    return !is_target_name({ targets: targets, name: $json.str_at({ j: location, key: "def" }) });
  }
  return true;
}

export function diagnostic_summary({ d }) {
  let ob = "";
  if ($json.has({ j: d, key: "obligation" })) {
    const o = $json.at({ j: d, key: "obligation" });
    ob = " (" + $json.str_at({ j: o, key: "kind" }) + " " + $json.str_at({ j: o, key: "text" }) + ")";
  }
  return $json.str_at({ j: d, key: "code" }) + " " + $json.str_at({ j: d, key: "title" }) + ob;
}

export function last_segment({ name }) {
  const parts = $std_text.split({ t: name, sep: "." });
  return $std_list.get({ xs: parts, i: $rt.int.sub($std_list.len({ xs: parts }), 1, $ob26) });
}

export function mentions({ mention, module_name }) {
  if (module_name === "std.text") {
    return has_word({ t: mention, word: "Text", after_only: false });
  }
  if (module_name === "std.list") {
    return has_word({ t: mention, word: "List", after_only: false });
  }
  if (module_name === "std.grid") {
    return has_word({ t: mention, word: "Grid", after_only: false }) || has_word({ t: mention, word: "grid.", after_only: true });
  }
  if (module_name === "std.map") {
    return has_word({ t: mention, word: "Map", after_only: false }) || has_word({ t: mention, word: "map.", after_only: true });
  }
  if (module_name === "std.bytes") {
    return has_word({ t: mention, word: "Bytes", after_only: false });
  }
  if (module_name === "std.io") {
    return has_word({ t: mention, word: "io.", after_only: true });
  }
  if (module_name === "std.sql") {
    return has_word({ t: mention, word: "sql.", after_only: true });
  }
  if (module_name === "std.float") {
    return has_word({ t: mention, word: "Float", after_only: false });
  }
  if (module_name === "std.int") {
    return has_word({ t: mention, word: "Int", after_only: false });
  }
  return false;
}

export function assemble({ task, baseline, current, targets, ctx_policy, history, full_history, messages }) {
  let sections = $std_list.builder({  });
  const names = target_names({ targets: targets });
  let target_texts = $std_list.builder({  });
  for (const target of targets) {
    const [, target_texts$25] = $std_list.push({ b: target_texts, x: item_text({ item: { tag: "FnItem", decl: elide({ f: target.decl }) } }) });
    target_texts = target_texts$25;
    for (const item of module_items({ c: baseline, name: target.module_name })) {
      const $m185 = item;
      $m185$match: {
        if ($m185.tag === "ExampleItem") {
          const decl = $m185.decl;
          const text = item_text({ item: item });
          if (decl.name.text === target.name || $std_text.contains({ t: text, needle: target.name + "(" })) {
            const [, target_texts$26] = $std_list.push({ b: target_texts, x: text });
            target_texts = target_texts$26;
          }
          break $m185$match;
        }
        if ($m185.tag === "PropertyItem") {
          const decl = $m185.decl;
          const text = item_text({ item: item });
          if (decl.name.text === target.name || $std_text.contains({ t: text, needle: target.name + "(" })) {
            const [, target_texts$27] = $std_list.push({ b: target_texts, x: text });
            target_texts = target_texts$27;
          }
          break $m185$match;
        }
        if (true) {
          skip({  });
          break $m185$match;
        }
        $rt.unreachable();
      }
    }
  }
  const target_list = $std_list.finish({ b: target_texts });
  let task_line = task.kind;
  const $m186 = task.description;
  $m186$match: {
    if ($m186.tag === "Some") {
      const value = $m186.value;
      task_line = task_line + ": " + value;
      break $m186$match;
    }
    if ($m186.tag === "None") {
      skip({  });
      break $m186$match;
    }
    $rt.unreachable();
  }
  const [, sections$28] = $std_list.push({ b: sections, x: "## Task\n\n" + task_line + "\n\n## Targets\n\n" + fenced({ texts: target_list }) });
  sections = sections$28;
  let wanted = $std_list.builder({  });
  for (const name of task.scope) {
    const [, wanted$29] = $std_list.push({ b: wanted, x: name });
    wanted = wanted$29;
  }
  for (const name of task.scope) {
    for (const imported of imports_of({ c: current, name: name })) {
      const [, wanted$30] = $std_list.push({ b: wanted, x: imported });
      wanted = wanted$30;
    }
  }
  let seen = $std_map.dict({  });
  let iface_texts = $std_list.builder({  });
  for (const name of $std_list.finish({ b: wanted })) {
    if (!$std_map.contains({ d: seen, key: name })) {
      const [, seen$31] = $std_map.set({ d: seen, key: name, value: true });
      seen = seen$31;
      const $m187 = $std_map.find({ d: current.ctx.by_name, key: name });
      $m187$match: {
        if ($m187.tag === "None") {
          skip({  });
          break $m187$match;
        }
        if ($m187.tag === "Some") {
          const value = $m187.value;
          if (!is_std_module({ c: current, mid: value })) {
            const [, iface_texts$32] = $std_list.push({ b: iface_texts, x: $std_text.trim({ t: $interface.interface_text({ ctx: current.ctx, mod: value }) }) });
            iface_texts = iface_texts$32;
          }
          break $m187$match;
        }
        $rt.unreachable();
      }
    }
  }
  if ($std_list.built({ b: iface_texts }) > 0) {
    const [, sections$33] = $std_list.push({ b: sections, x: "## Interfaces\n\n" + fenced({ texts: $std_list.finish({ b: iface_texts }) }) });
    sections = sections$33;
  }
  if (ctx_policy !== "none") {
    let chosen = $std_map.dict({  });
    if (ctx_policy === "module") {
      for (const t of targets) {
        const [, chosen$34] = $std_map.set({ d: chosen, key: t.module_name, value: true });
        chosen = chosen$34;
      }
    } else {
      for (const name of task.scope) {
        const [, chosen$35] = $std_map.set({ d: chosen, key: name, value: true });
        chosen = chosen$35;
      }
    }
    let bodies = $std_list.builder({  });
    const $hi188 = current.ctx.module_count;
    for (let mid = 0; mid < $hi188; mid++) {
      const mname = module_name_of({ c: current, mid: mid });
      if ($std_map.contains({ d: chosen, key: mname })) {
        for (const f of fns_of_items({ items: module_items_of({ c: current, mid: mid }) })) {
          if (!$std_map.contains({ d: names, key: mname + "." + f.name.text }) && has_body({ f: f })) {
            const [, bodies$36] = $std_list.push({ b: bodies, x: item_text({ item: { tag: "FnItem", decl: f } }) });
            bodies = bodies$36;
          }
        }
      }
    }
    if ($std_list.built({ b: bodies }) > 0) {
      const [, sections$37] = $std_list.push({ b: sections, x: "## Other functions in scope (for reference)\n\n" + fenced({ texts: $std_list.finish({ b: bodies }) }) });
      sections = sections$37;
    }
  }
  const n_history = $std_list.len({ xs: history });
  if (n_history > 0) {
    const last = $std_list.get({ xs: history, i: $rt.int.sub(n_history, 1, $ob27) });
    let shown = $std_list.builder({  });
    for (const d of last.diagnostics) {
      if (relevant({ d: d, targets: targets })) {
        const [, shown$38] = $std_list.push({ b: shown, x: $json.compact({ j: d }) });
        shown = shown$38;
      }
    }
    if ($std_list.built({ b: shown }) > 0) {
      const [, sections$39] = $std_list.push({ b: sections, x: "## Diagnostics from the last check (" + $std_int.to_text({ x: $std_list.built({ b: shown }) }) + ")\n\n" + $std_text.join({ parts: $std_list.finish({ b: shown }), sep: "\n" }) });
      sections = sections$39;
    } else {
      if ($std_list.len({ xs: last.failing }) === 0) {
        const [, sections$40] = $std_list.push({ b: sections, x: "## Diagnostics from the last check\n\nnone" });
        sections = sections$40;
      }
    }
    if ($std_list.len({ xs: last.failing }) > 0) {
      let bodies = $std_list.builder({  });
      for (const name of last.failing) {
        const short = last_segment({ name: name });
        const $hi190 = baseline.ctx.module_count;
        for (let mid = 0; mid < $hi190; mid++) {
          const mname = module_name_of({ c: baseline, mid: mid });
          for (const item of module_items_of({ c: baseline, mid: mid })) {
            const $m191 = item;
            $m191$match: {
              if ($m191.tag === "ExampleItem") {
                const decl = $m191.decl;
                if (mname + "." + decl.name.text === name) {
                  const [, bodies$41] = $std_list.push({ b: bodies, x: item_text({ item: item }) });
                  bodies = bodies$41;
                }
                break $m191$match;
              }
              if ($m191.tag === "PropertyItem") {
                const decl = $m191.decl;
                if (mname + "." + decl.name.text === name) {
                  const [, bodies$42] = $std_list.push({ b: bodies, x: item_text({ item: item }) });
                  bodies = bodies$42;
                }
                break $m191$match;
              }
              if ($m191.tag === "ImplItem") {
                const decl = $m191.decl;
                if ($std_text.starts_with({ t: short, prefix: "law " })) {
                  const [, bodies$43] = $std_list.push({ b: bodies, x: item_text({ item: item }) });
                  bodies = bodies$43;
                }
                break $m191$match;
              }
              if (true) {
                skip({  });
                break $m191$match;
              }
              $rt.unreachable();
            }
          }
        }
      }
      const [, sections$44] = $std_list.push({ b: sections, x: "## Failing examples and properties (" + $std_int.to_text({ x: $std_list.len({ xs: last.failing }) }) + ")\n\n" + $std_text.join({ parts: last.failing, sep: "\n" }) + "\n\n" + fenced({ texts: $std_list.finish({ b: bodies }) }) });
      sections = sections$44;
    }
  }
  if (full_history && n_history > 1) {
    let earlier = $std_list.builder({  });
    let i = 0;
    for (const h of history) {
      if (i < $rt.int.sub(n_history, 1, $ob28)) {
        let parts = $std_list.builder({  });
        for (const d of h.diagnostics) {
          if (relevant({ d: d, targets: targets })) {
            const [, parts$45] = $std_list.push({ b: parts, x: diagnostic_summary({ d: d }) });
            parts = parts$45;
          }
        }
        let line = $std_text.join({ parts: $std_list.finish({ b: parts }), sep: "; " });
        if (line === "") {
          line = "no diagnostics";
        }
        if ($std_list.len({ xs: h.failing }) > 0) {
          line = line + "; failing: " + $std_text.join({ parts: h.failing, sep: ", " });
        }
        const [, earlier$46] = $std_list.push({ b: earlier, x: "iteration " + $std_int.to_text({ x: h.iteration }) + ": " + line });
        earlier = earlier$46;
      }
      i = $rt.int.add(i, 1, $ob29);
    }
    const [, sections$47] = $std_list.push({ b: sections, x: "## Earlier iterations\n\n" + $std_text.join({ parts: $std_list.finish({ b: earlier }), sep: "\n" }) });
    sections = sections$47;
  }
  let counterexamples = $std_list.builder({  });
  const $m192 = task.counterexample;
  $m192$match: {
    if ($m192.tag === "Some") {
      const value = $m192.value;
      const [, counterexamples$48] = $std_list.push({ b: counterexamples, x: "from " + origin_kind({ task: task }) + against_text({ task: task }) + ": " + $json.compact({ j: value }) });
      counterexamples = counterexamples$48;
      break $m192$match;
    }
    if ($m192.tag === "None") {
      skip({  });
      break $m192$match;
    }
    $rt.unreachable();
  }
  if (n_history > 0) {
    const last = $std_list.get({ xs: history, i: $rt.int.sub(n_history, 1, $ob30) });
    for (const d of last.diagnostics) {
      if ($json.has({ j: d, key: "obligation" })) {
        const o = $json.at({ j: d, key: "obligation" });
        if (!$json.is_null({ j: $json.at({ j: o, key: "counterexample" }) })) {
          let in_def = "?";
          const location = $json.at({ j: d, key: "location" });
          if (!$json.is_null({ j: $json.at({ j: location, key: "def" }) })) {
            in_def = $json.str_at({ j: location, key: "def" });
          }
          const [, counterexamples$49] = $std_list.push({ b: counterexamples, x: "against `" + $json.str_at({ j: o, key: "kind" }) + " " + $json.str_at({ j: o, key: "text" }) + "` in " + in_def + ": " + $json.compact({ j: $json.at({ j: o, key: "counterexample" }) }) });
          counterexamples = counterexamples$49;
        }
      }
    }
  }
  if ($std_list.built({ b: counterexamples }) > 0) {
    const [, sections$50] = $std_list.push({ b: sections, x: "## Counterexamples\n\n" + $std_text.join({ parts: $std_list.finish({ b: counterexamples }), sep: "\n" }) });
    sections = sections$50;
  }
  const mention = $std_text.join({ parts: target_list, sep: "\n" });
  let std_texts = $std_list.builder({  });
  for (const module_name of ["std.text", "std.list", "std.grid", "std.map", "std.bytes", "std.io", "std.sql", "std.float", "std.int"]) {
    if (mentions({ mention: mention, module_name: module_name })) {
      const $m193 = $std_map.find({ d: current.ctx.by_name, key: module_name });
      $m193$match: {
        if ($m193.tag === "Some") {
          const value = $m193.value;
          const [, std_texts$51] = $std_list.push({ b: std_texts, x: $std_text.trim({ t: $interface.interface_text({ ctx: current.ctx, mod: value }) }) });
          std_texts = std_texts$51;
          break $m193$match;
        }
        if ($m193.tag === "None") {
          skip({  });
          break $m193$match;
        }
        $rt.unreachable();
      }
    }
  }
  if ($std_list.built({ b: std_texts }) > 0) {
    const [, sections$52] = $std_list.push({ b: sections, x: "## Standard library\n\n" + fenced({ texts: $std_list.finish({ b: std_texts }) }) });
    sections = sections$52;
  }
  if ($std_list.len({ xs: messages }) > 0) {
    let notes = $std_list.builder({  });
    for (const m of messages) {
      const [, notes$53] = $std_list.push({ b: notes, x: "- " + m });
      notes = notes$53;
    }
    const [, sections$54] = $std_list.push({ b: sections, x: "## Notes from the loop\n\n" + $std_text.join({ parts: $std_list.finish({ b: notes }), sep: "\n" }) });
    sections = sections$54;
  }
  const prompt = $std_text.join({ parts: $std_list.finish({ b: sections }), sep: "\n\n" });
  return { system: system_prompt, prompt: prompt, prompt_hash: "b3:" + $std_hash.blake3_hex({ t: system_prompt + "\n" + prompt }) };
}

export function imports_of({ c, name }) {
  const $m195 = $std_map.find({ d: c.ctx.by_name, key: name });
  $m195$match: {
    if ($m195.tag === "None") {
      return [];
      break $m195$match;
    }
    if ($m195.tag === "Some") {
      const value = $m195.value;
      return imports_of_module({ c: c, mid: value });
      break $m195$match;
    }
    $rt.unreachable();
  }
}

export function imports_of_module({ c, mid }) {
  const $m196 = $std_map.find({ d: c.ctx.modules, key: mid });
  $m196$match: {
    if ($m196.tag === "None") {
      return [];
      break $m196$match;
    }
    if ($m196.tag === "Some") {
      const value = $m196.value;
      let out = $std_list.builder({  });
      for (const imp of value.imports) {
        const [, out$55] = $std_list.push({ b: out, x: module_name_of({ c: c, mid: imp.mod }) });
        out = out$55;
      }
      return $std_list.finish({ b: out });
      break $m196$match;
    }
    $rt.unreachable();
  }
}

export function origin_kind({ task }) {
  const $m197 = task.origin;
  $m197$match: {
    if ($m197.tag === "Some") {
      const value = $m197.value;
      return value.kind;
      break $m197$match;
    }
    if ($m197.tag === "None") {
      return "the task";
      break $m197$match;
    }
    $rt.unreachable();
  }
}

export function against_text({ task }) {
  const $m198 = task.target;
  $m198$match: {
    if ($m198.tag === "None") {
      return "";
      break $m198$match;
    }
    if ($m198.tag === "Some") {
      const value = $m198.value;
      return obligation_text({ o: value.obligation });
      break $m198$match;
    }
    $rt.unreachable();
  }
}

export function obligation_text({ o }) {
  const $m199 = o;
  $m199$match: {
    if ($m199.tag === "Some") {
      const value = $m199.value;
      return " against `" + value + "`";
      break $m199$match;
    }
    if ($m199.tag === "None") {
      return "";
      break $m199$match;
    }
    $rt.unreachable();
  }
}

export function is_std_module({ c, mid }) {
  const $m200 = $std_map.find({ d: c.ctx.modules, key: mid });
  $m200$match: {
    if ($m200.tag === "Some") {
      const value = $m200.value;
      return value.is_std;
      break $m200$match;
    }
    if ($m200.tag === "None") {
      return true;
      break $m200$match;
    }
    $rt.unreachable();
  }
}

export function fns_of_items({ items }) {
  let out = $std_list.builder({  });
  for (const item of items) {
    const $m201 = item;
    $m201$match: {
      if ($m201.tag === "FnItem") {
        const decl = $m201.decl;
        const [, out$56] = $std_list.push({ b: out, x: decl });
        out = out$56;
        break $m201$match;
      }
      if (true) {
        skip({  });
        break $m201$match;
      }
      $rt.unreachable();
    }
  }
  return $std_list.finish({ b: out });
}

export function sanitised({ id }) {
  let out = $std_list.builder({  });
  for (const c of $std_text.code_points({ t: id })) {
    if (is_word_char({ c: c }) || c === 46 || c === 45) {
      const [, out$57] = $std_list.push({ b: out, x: c });
      out = out$57;
    } else {
      const [, out$58] = $std_list.push({ b: out, x: 95 });
      out = out$58;
    }
  }
  return $std_text.of_code_points({ cps: $std_list.finish({ b: out }) });
}

export function change_dir({ root, task_id }) {
  return root + "/.onus/changes/" + sanitised({ id: task_id });
}

export function write_change({ files, root, task_id, change }) {
  try {
    const dir = change_dir({ root: root, task_id: task_id });
    $rt.unwrap($std_io.mkdir({ files: files, path: dir }));
    const path = dir + "/change.json";
    $rt.unwrap($build.write_file({ files: files, path: path, text: $json.pretty({ j: change }) + "\n" }));
    return { tag: "Ok", value: path };
  } catch ($e) {
    if ($e instanceof $rt.EarlyReturn) return $e.value;
    throw $e;
  }
}

export function proposal_json({ kind, def, current, proposed, counterexample, iterations, finding, rationale }) {
  let evidence = $std_list.builder({  });
  const [, evidence$59] = $std_list.push({ b: evidence, x: $json.field({ key: "counterexample", value: counterexample }) });
  evidence = evidence$59;
  const [, evidence$60] = $std_list.push({ b: evidence, x: $json.field({ key: "iterations", value: $json.int({ v: iterations }) }) });
  evidence = evidence$60;
  const $m203 = finding;
  $m203$match: {
    if ($m203.tag === "Some") {
      const value = $m203.value;
      const [, evidence$61] = $std_list.push({ b: evidence, x: $json.field({ key: "finding", value: $json.text({ t: value }) }) });
      evidence = evidence$61;
      break $m203$match;
    }
    if ($m203.tag === "None") {
      skip({  });
      break $m203$match;
    }
    $rt.unreachable();
  }
  return { tag: "JObject", fields: [$json.field({ key: "kind", value: $json.text({ t: kind }) }), $json.field({ key: "def", value: $json.text({ t: def }) }), $json.field({ key: "current", value: $json.text_or_null({ o: current }) }), $json.field({ key: "proposed", value: $json.text_or_null({ o: proposed }) }), $json.field({ key: "evidence", value: { tag: "JObject", fields: $std_list.finish({ b: evidence }) } }), $json.field({ key: "rationale", value: $json.text({ t: rationale }) })] };
}

export function outcome_signature({ diagnostics, failing }) {
  let parts = $std_list.builder({  });
  for (const d of diagnostics) {
    const location = $json.at({ j: d, key: "location" });
    let ob = "";
    if ($json.has({ j: d, key: "obligation" })) {
      const o = $json.at({ j: d, key: "obligation" });
      ob = "|" + $json.str_at({ j: o, key: "kind" }) + " " + $json.str_at({ j: o, key: "text" });
    }
    const [, parts$62] = $std_list.push({ b: parts, x: $json.str_at({ j: d, key: "code" }) + "@" + $json.str_at({ j: location, key: "def" }) + ":" + $json.compact({ j: $json.at({ j: location, key: "span" }) }) + ob });
    parts = parts$62;
  }
  let all = $std_list.builder({  });
  for (const p of sorted_texts({ xs: $std_list.finish({ b: parts }) })) {
    const [, all$63] = $std_list.push({ b: all, x: p });
    all = all$63;
  }
  for (const f of sorted_texts({ xs: failing })) {
    const [, all$64] = $std_list.push({ b: all, x: "test " + f });
    all = all$64;
  }
  return $std_text.join({ parts: $std_list.finish({ b: all }), sep: "\n" });
}

export function row_key({ r }) {
  return r.def + "|" + r.kind + "|" + r.text;
}

export function delta_json({ r, before, after }) {
  return { tag: "JObject", fields: [$json.field({ key: "def", value: $json.text({ t: r.def }) }), $json.field({ key: "kind", value: $json.text({ t: r.kind }) }), $json.field({ key: "text", value: $json.text({ t: r.text }) }), $json.field({ key: "before", value: $json.text_or_null({ o: before }) }), $json.field({ key: "after", value: $json.text_or_null({ o: after }) })] };
}

export function ledger_delta({ before, after }) {
  let a = $std_map.dict({  });
  for (const r of before.ledger) {
    const [, a$65] = $std_map.set({ d: a, key: row_key({ r: r }), value: r });
    a = a$65;
  }
  let b = $std_map.dict({  });
  for (const r of after.ledger) {
    const [, b$66] = $std_map.set({ d: b, key: row_key({ r: r }), value: r });
    b = b$66;
  }
  let out = $std_list.builder({  });
  for (const k of $std_map.keys({ d: b })) {
    const $m207 = $std_map.find({ d: b, key: k });
    $m207$match: {
      if ($m207.tag === "None") {
        skip({  });
        break $m207$match;
      }
      if ($m207.tag === "Some") {
        const value = $m207.value;
        const [, out$67] = delta_of({ a: a, r: value, out: out });
        out = out$67;
        break $m207$match;
      }
      $rt.unreachable();
    }
  }
  for (const k of $std_map.keys({ d: a })) {
    if (!$std_map.contains({ d: b, key: k })) {
      const $m208 = $std_map.find({ d: a, key: k });
      $m208$match: {
        if ($m208.tag === "None") {
          skip({  });
          break $m208$match;
        }
        if ($m208.tag === "Some") {
          const value = $m208.value;
          const [, out$68] = $std_list.push({ b: out, x: delta_json({ r: value, before: { tag: "Some", value: value.status }, after: { tag: "None" } }) });
          out = out$68;
          break $m208$match;
        }
        $rt.unreachable();
      }
    }
  }
  return $std_list.finish({ b: out });
}

export function delta_of({ a, r, out }) {
  const $m211 = $std_map.find({ d: a, key: row_key({ r: r }) });
  $m211$match: {
    if ($m211.tag === "None") {
      const [, out$69] = $std_list.push({ b: out, x: delta_json({ r: r, before: { tag: "None" }, after: { tag: "Some", value: r.status } }) });
      out = out$69;
      break $m211$match;
    }
    if ($m211.tag === "Some") {
      const value = $m211.value;
      if (value.status !== r.status) {
        const [, out$70] = $std_list.push({ b: out, x: delta_json({ r: r, before: { tag: "Some", value: value.status }, after: { tag: "Some", value: r.status } }) });
        out = out$70;
      }
      break $m211$match;
    }
    $rt.unreachable();
  }
  return [undefined, out];
  return [undefined, out];
}

export function interface_diffs({ scope, before, after }) {
  let out = $std_list.builder({  });
  for (const name of scope) {
    const $m216 = diff_pair({ a: $std_map.find({ d: before.interfaces, key: name }), b: $std_map.find({ d: after.interfaces, key: name }) });
    $m216$match: {
      if ($m216.tag === "Some") {
        const value = $m216.value;
        const [, out$71] = $std_list.push({ b: out, x: value });
        out = out$71;
        break $m216$match;
      }
      if ($m216.tag === "None") {
        skip({  });
        break $m216$match;
      }
      $rt.unreachable();
    }
  }
  return $std_list.finish({ b: out });
}

export function diff_pair({ a, b }) {
  const $m217 = a;
  $m217$match: {
    if ($m217.tag === "None") {
      return { tag: "None" };
      break $m217$match;
    }
    if ($m217.tag === "Some") {
      const value = $m217.value;
      return diff_with({ before_doc: value, b: b });
      break $m217$match;
    }
    $rt.unreachable();
  }
}

export function diff_with({ before_doc, b }) {
  const $m219 = b;
  $m219$match: {
    if ($m219.tag === "None") {
      return { tag: "None" };
      break $m219$match;
    }
    if ($m219.tag === "Some") {
      const value = $m219.value;
      const d = $idiff.interface_diff({ before: before_doc, after: value });
      if ($std_list.len({ xs: d.added }) > 0 || $std_list.len({ xs: d.removed }) > 0 || $std_list.len({ xs: d.changed }) > 0) {
        return { tag: "Some", value: $idiff.diff_json({ d: d }) };
      }
      return { tag: "None" };
      break $m219$match;
    }
    $rt.unreachable();
  }
}

export function finding_json({ f }) {
  return { tag: "JObject", fields: [$json.field({ key: "finding", value: $json.text({ t: f.finding }) }), $json.field({ key: "detail", value: $json.text({ t: f.detail }) })] };
}

export function def_or_question({ d }) {
  const location = $json.at({ j: d, key: "location" });
  if ($json.is_null({ j: $json.at({ j: location, key: "def" }) })) {
    return "?";
  }
  return $json.str_at({ j: location, key: "def" });
}

export function audit({ baseline, final }) {
  const $m224 = final;
  $m224$match: {
    if ($m224.tag === "None") {
      return [];
      break $m224$match;
    }
    if ($m224.tag === "Some") {
      const value = $m224.value;
      return audit_of({ baseline: baseline, final: value });
      break $m224$match;
    }
    $rt.unreachable();
  }
}

export function audit_of({ baseline, final }) {
  let out = $std_list.builder({  });
  if ($std_list.len({ xs: final.diagnostics }) > 0) {
    for (const d of final.json) {
      if ($json.str_at({ j: d, key: "code" }) === "E0702") {
        const [, out$72] = $std_list.push({ b: out, x: { finding: "example_failed", detail: $std_text.join({ parts: $json.texts_at({ j: d, key: "context" }), sep: "; " }) + " on the regenerated body of " + def_or_question({ d: d }) + ": something the old body did that no claim required" } });
        out = out$72;
      } else {
        let ob = "";
        if ($json.has({ j: d, key: "obligation" })) {
          const o = $json.at({ j: d, key: "obligation" });
          ob = " (" + $json.str_at({ j: o, key: "kind" }) + " " + $json.str_at({ j: o, key: "text" }) + ")";
        }
        const [, out$73] = $std_list.push({ b: out, x: { finding: "obligation_regressed", detail: $json.str_at({ j: d, key: "code" }) + " " + $json.str_at({ j: d, key: "title" }) + ob + " in " + def_or_question({ d: d }) + ": the interfaces were sufficient to check the old body but not to reconstruct it" } });
        out = out$73;
      }
    }
    return $std_list.finish({ b: out });
  }
  let after = $std_map.dict({  });
  for (const r of final.ledger) {
    const [, after$74] = $std_map.set({ d: after, key: row_key({ r: r }), value: r.status });
    after = after$74;
  }
  for (const r of baseline.ledger) {
    if (r.status === "proved" || r.status === "checked") {
      const now = or_empty({ o: $std_map.find({ d: after, key: row_key({ r: r }) }) });
      if (now === "failed" || now === "unprovable" || r.status === "proved" && now === "checked") {
        const [, out$75] = $std_list.push({ b: out, x: { finding: "obligation_regressed", detail: r.kind + " " + r.text + " in " + r.def + ": " + r.status + " on the old body, " + now + " on the regenerated one; usually a missing example or invariant" } });
        out = out$75;
      }
    }
  }
  const $m228 = baseline.tests;
  $m228$match: {
    if ($m228.tag === "None") {
      skip({  });
      break $m228$match;
    }
    if ($m228.tag === "Some") {
      const value = $m228.value;
      for (const f of test_regressions({ before: value, after: final.tests })) {
        const [, out$76] = $std_list.push({ b: out, x: f });
        out = out$76;
      }
      break $m228$match;
    }
    $rt.unreachable();
  }
  return $std_list.finish({ b: out });
}

export function test_regressions({ before, after }) {
  const $m229 = after;
  $m229$match: {
    if ($m229.tag === "None") {
      return [];
      break $m229$match;
    }
    if ($m229.tag === "Some") {
      const value = $m229.value;
      let out = $std_list.builder({  });
      for (const name of $std_map.keys({ d: before })) {
        if (test_passed({ tests: before, name: name }) && $std_map.contains({ d: value, key: name }) && !test_passed({ tests: value, name: name })) {
          const [, out$77] = $std_list.push({ b: out, x: { finding: "example_failed", detail: name + " passes on the old body and fails on the regenerated one: something the old body did that no claim required" } });
          out = out$77;
        }
      }
      return $std_list.finish({ b: out });
      break $m229$match;
    }
    $rt.unreachable();
  }
}

export function test_passed({ tests, name }) {
  const $m231 = $std_map.find({ d: tests, key: name });
  $m231$match: {
    if ($m231.tag === "Some") {
      const value = $m231.value;
      return value;
      break $m231$match;
    }
    if ($m231.tag === "None") {
      return false;
      break $m231$match;
    }
    $rt.unreachable();
  }
}

export function proposals_from_audit({ findings, target, iterations }) {
  let out = $std_list.builder({  });
  for (const f of findings) {
    let kind = "add_example";
    if (f.finding === "example_failed") {
      kind = "add_claim";
    }
    const [, out$78] = $std_list.push({ b: out, x: proposal_json({ kind: kind, def: target, current: { tag: "None" }, proposed: { tag: "None" }, counterexample: { tag: "JNull" }, iterations: iterations, finding: { tag: "Some", value: f.finding }, rationale: f.detail }) });
    out = out$78;
  }
  return $std_list.finish({ b: out });
}

export function target_body_spans({ c, targets }) {
  let out = $std_map.dict({  });
  const $hi236 = c.ctx.module_count;
  for (let mid = 0; mid < $hi236; mid++) {
    const $m237 = $std_map.find({ d: c.ctx.modules, key: mid });
    $m237$match: {
      if ($m237.tag === "None") {
        skip({  });
        break $m237$match;
      }
      if ($m237.tag === "Some") {
        const value = $m237.value;
        const [, out$79] = body_spans_of({ c: c, m: value, targets: targets, out: out });
        out = out$79;
        break $m237$match;
      }
      $rt.unreachable();
    }
  }
  return out;
}

export function body_spans_of({ c, m, targets, out }) {
  const path = file_path_of({ c: c, file: m.file });
  for (const f of fns_of({ m: m.tree })) {
    if (is_target({ targets: targets, module_name: m.name, name: f.name.text })) {
      const $m238 = f.body;
      $m238$match: {
        if ($m238.tag === "Some") {
          const value = $m238.value;
          const [, out$80] = $std_map.set({ d: out, key: path, value: $std_list.append({ xs: or_list({ o: $std_map.find({ d: out, key: path }) }), x: value.span }) });
          out = out$80;
          break $m238$match;
        }
        if ($m238.tag === "None") {
          skip({  });
          break $m238$match;
        }
        $rt.unreachable();
      }
    }
  }
  return [undefined, out];
  return [undefined, out];
}

export function is_target({ targets, module_name, name }) {
  for (const t of targets) {
    if (t.module_name === module_name && t.name === name) {
      return true;
    }
  }
  return false;
}

export function within({ spans, span }) {
  for (const s of spans) {
    if (s.start <= span.start && span.end <= s.end) {
      return true;
    }
  }
  return false;
}

export function info({ d }) {
  return { module_name: d.module_name, name: d.fn_decl.name.text, path: d.path, decl: elide({ f: d.fn_decl }) };
}

export function select_targets({ task, project, baseline, texts }) {
  let decls = $std_map.dict({  });
  for (const name of project.scope) {
    const path = or_empty({ o: $std_map.find({ d: project.paths, key: name }) });
    const text = text_of({ texts: texts, fallback: project.texts, path: path });
    const $m240 = parse_module({ path: path, text: text }).module;
    $m240$match: {
      if ($m240.tag === "None") {
        return { tag: "Err", error: path + " does not parse" };
        break $m240$match;
      }
      if ($m240.tag === "Some") {
        const value = $m240.value;
        for (const f of fns_of({ m: value })) {
          const [, decls$81] = $std_map.set({ d: decls, key: name + "." + f.name.text, value: { fn_decl: f, module_name: name, path: path } });
          decls = decls$81;
        }
        break $m240$match;
      }
      $rt.unreachable();
    }
  }
  if (task.kind === "implement" || task.kind === "repair") {
    let def = "";
    const $m243 = task.target;
    $m243$match: {
      if ($m243.tag === "Some") {
        const value = $m243.value;
        def = value.def;
        break $m243$match;
      }
      if ($m243.tag === "None") {
        skip({  });
        break $m243$match;
      }
      $rt.unreachable();
    }
    const $m244 = $std_map.find({ d: decls, key: def });
    $m244$match: {
      if ($m244.tag === "None") {
        return { tag: "Err", error: "target " + def + " is not a function in scope" };
        break $m244$match;
      }
      if ($m244.tag === "Some") {
        const value = $m244.value;
        if (task.kind === "repair" && !has_body({ f: value.fn_decl })) {
          return { tag: "Err", error: "target " + def + " has no body to repair" };
        }
        return { tag: "Ok", value: [info({ d: value })] };
        break $m244$match;
      }
      $rt.unreachable();
    }
  }
  let out = $std_list.builder({  });
  if (task.kind === "interface_change") {
    for (const d of $std_map.values({ d: decls })) {
      if (has_body({ f: d.fn_decl }) && named_in({ diagnostics: baseline.json, name: d.fn_decl.name.text, path: d.path })) {
        const [, out$82] = $std_list.push({ b: out, x: info({ d: d }) });
        out = out$82;
      }
    }
    return { tag: "Ok", value: $std_list.finish({ b: out }) };
  }
  if (task.kind === "regenerate") {
    for (const d of $std_map.values({ d: decls })) {
      if (has_body({ f: d.fn_decl })) {
        const [, out$83] = $std_list.push({ b: out, x: info({ d: d }) });
        out = out$83;
      }
    }
    return { tag: "Ok", value: $std_list.finish({ b: out }) };
  }
  return { tag: "Ok", value: [] };
}

export function named_in({ diagnostics, name, path }) {
  for (const d of diagnostics) {
    const location = $json.at({ j: d, key: "location" });
    if ($json.str_at({ j: location, key: "def" }) === name && $json.str_at({ j: location, key: "file" }) === path) {
      return true;
    }
  }
  return false;
}

export function syntax_notes({ fragment }) {
  const lines = $std_text.split({ t: fragment.text, sep: "\n" });
  let out = $std_list.builder({  });
  let seen_lines = $std_map.dict({  });
  for (const d of fragment.diagnostics) {
    if ($std_list.built({ b: out }) < 6) {
      const before = slice_within({ t: fragment.text, from: 0, to: d.span.start });
      const line = $std_list.len({ xs: $std_text.split({ t: before, sep: "\n" }) });
      if (!$std_map.contains({ d: seen_lines, key: line })) {
        const [, seen_lines$84] = $std_map.set({ d: seen_lines, key: line, value: true });
        seen_lines = seen_lines$84;
        let text = "";
        if (line >= 1 && line <= $std_list.len({ xs: lines })) {
          text = $std_text.trim({ t: $std_list.get({ xs: lines, i: $rt.int.sub(line, 1, $ob31) }) });
        }
        let line_start = 0;
        let scan = 0;
        for (const c of $std_text.code_points({ t: before })) {
          scan = $rt.int.add(scan, 1, $ob32);
          if (c === 10) {
            line_start = scan;
          }
        }
        if (line_start < 0) {
          line_start = 0;
        }
        const legal_list = legal_starts({ text: fragment.text, line_start: line_start });
        let context_text = "";
        if ($std_list.len({ xs: d.context }) > 0) {
          context_text = ": " + $std_text.join({ parts: d.context, sep: "; " });
        }
        let admits = "";
        if ($std_list.len({ xs: legal_list }) > 0) {
          admits = ". A line here may start with: " + $std_text.join({ parts: legal_list, sep: " " });
        }
        const [, out$85] = $std_list.push({ b: out, x: "line " + $std_int.to_text({ x: $rt.int.sub(line, 2, $ob33) }) + ": `" + text + "` — " + d.code + " " + $codes.title({ code: d.code }) + context_text + admits });
        out = out$85;
      }
    }
  }
  return $std_list.finish({ b: out });
}

export function legal_starts({ text, line_start }) {
  const prefix = slice_within({ t: text, from: 0, to: line_start });
  if ($std_text.count({ t: prefix }) > $lexer.max_source) {
    return [];
  }
  const lexed = $lexer.lex({ source: prefix });
  let legal = $std_list.builder({  });
  for (const k of $std_map.keys({ d: $parser.legal_tokens_at({ prefix: $parser.prefix_tokens({ toks: lexed.tokens }), offset: line_start }) })) {
    const name = $tokens.decoder_name_of({ kind: k });
    if (name !== "newline" && name !== "eof") {
      const [, legal$86] = $std_list.push({ b: legal, x: name });
      legal = legal$86;
    }
  }
  return sorted_texts({ xs: $std_list.finish({ b: legal }) });
}

export function best_bodies({ texts, targets }) {
  let fields = $std_list.builder({  });
  for (const target of targets) {
    const $m251 = body_text_of({ path: target.path, text: $std_map.find({ d: texts, key: target.path }), name: target.name });
    $m251$match: {
      if ($m251.tag === "Some") {
        const value = $m251.value;
        const [, fields$87] = $std_list.push({ b: fields, x: $json.field({ key: target.module_name + "." + target.name, value: $json.text({ t: value }) }) });
        fields = fields$87;
        break $m251$match;
      }
      if ($m251.tag === "None") {
        skip({  });
        break $m251$match;
      }
      $rt.unreachable();
    }
  }
  return { tag: "JObject", fields: $std_list.finish({ b: fields }) };
}

export function body_text_of({ path, text, name }) {
  const $m253 = text;
  $m253$match: {
    if ($m253.tag === "None") {
      return { tag: "None" };
      break $m253$match;
    }
    if ($m253.tag === "Some") {
      const value = $m253.value;
      return fn_text({ m: parse_module({ path: path, text: value }).module, name: name });
      break $m253$match;
    }
    $rt.unreachable();
  }
}

export function fn_text({ m, name }) {
  const $m255 = m;
  $m255$match: {
    if ($m255.tag === "None") {
      return { tag: "None" };
      break $m255$match;
    }
    if ($m255.tag === "Some") {
      const value = $m255.value;
      return fn_item_text({ f: find_fn({ m: value, name: name }) });
      break $m255$match;
    }
    $rt.unreachable();
  }
}

export function fn_item_text({ f }) {
  const $m257 = f;
  $m257$match: {
    if ($m257.tag === "None") {
      return { tag: "None" };
      break $m257$match;
    }
    if ($m257.tag === "Some") {
      const value = $m257.value;
      return { tag: "Some", value: item_text({ item: { tag: "FnItem", decl: value } }) };
      break $m257$match;
    }
    $rt.unreachable();
  }
}

export function effects_of({ sig }) {
  const cps = $std_text.code_points({ t: sig });
  const n = $std_list.len({ xs: cps });
  const may_word = $std_text.code_points({ t: "may" });
  let start = $rt.int.neg(1, $ob34);
  for (let i = 0; i < n; i++) {
    if (start < 0 && matches_at({ cps: cps, at: i, word: may_word })) {
      const left_ok = i === 0 || !is_word_char({ c: $std_list.get({ xs: cps, i: $rt.int.sub(i, 1, $ob35) }) });
      const right_ok = $rt.int.add(i, 3, $ob36) >= n || !is_word_char({ c: $std_list.get({ xs: cps, i: $rt.int.add(i, 3, $ob37) }) });
      if (left_ok && right_ok) {
        start = $rt.int.add(i, 3, $ob38);
      }
    }
  }
  if (start < 0) {
    return "";
  }
  let end = start;
  let scanning = true;
  for (let k = 0; k < n; k++) {
    if (k >= start && scanning) {
      const c = $std_list.get({ xs: cps, i: k });
      if (c === 10 || c === 123) {
        scanning = false;
      } else {
        end = $rt.int.add(k, 1, $ob39);
      }
    }
  }
  return $std_text.trim({ t: slice_within({ t: sig, from: start, to: end }) });
}

export function signature_proposal({ target, proposed, iteration }) {
  const a = signature_text({ f: target.decl });
  const b = signature_text({ f: proposed });
  let kind = "weaken_postcondition";
  if (effects_of({ sig: a }) !== effects_of({ sig: b })) {
    kind = "widen_effects";
  }
  return proposal_json({ kind: kind, def: target.module_name + "." + target.name, current: { tag: "Some", value: a }, proposed: { tag: "Some", value: b }, counterexample: { tag: "JNull" }, iterations: iteration, finding: { tag: "None" }, rationale: "the model changed the signature twice; the loop may not edit a claim, so the change is proposed instead" });
}

export function parse_proposal({ answer, fallback_def }) {
  const $m265 = $std_text.index_of({ t: answer, needle: "{", from: 0 });
  $m265$match: {
    if ($m265.tag === "None") {
      return { tag: "None" };
      break $m265$match;
    }
    if ($m265.tag === "Some") {
      const value = $m265.value;
      return proposal_between({ answer: answer, open: value, close: last_close({ answer: answer, from: value }), fallback_def: fallback_def });
      break $m265$match;
    }
    $rt.unreachable();
  }
}

export function last_close({ answer, from }) {
  const cps = $std_text.code_points({ t: answer });
  const n = $std_list.len({ xs: cps });
  let close = $rt.int.neg(1, $ob40);
  for (let i = 0; i < n; i++) {
    if (close < 0) {
      const k = $rt.int.sub($rt.int.sub(n, 1, $ob41), i, $ob42);
      if (k >= from && $std_list.get({ xs: cps, i: k }) === 125) {
        close = k;
      }
    }
  }
  return close;
}

export function proposal_between({ answer, open, close, fallback_def }) {
  if (close < open) {
    return { tag: "None" };
  }
  const $m268 = $json.parse({ t: slice_within({ t: answer, from: open, to: $rt.int.add(close, 1, $ob43) }) });
  $m268$match: {
    if ($m268.tag === "None") {
      return { tag: "None" };
      break $m268$match;
    }
    if ($m268.tag === "Some") {
      const value = $m268.value;
      return proposal_of_object({ parsed: value, answer: answer, fallback_def: fallback_def });
      break $m268$match;
    }
    $rt.unreachable();
  }
}

export function proposal_of_object({ parsed, answer, fallback_def }) {
  if (!(parsed.tag === "JObject")) {
    return { tag: "None" };
  }
  const kind_text = $json.str_at({ j: parsed, key: "kind" });
  let kind = "unsatisfiable";
  if ($std_list.contains({ xs: ["weaken_postcondition", "add_precondition", "widen_effects", "add_claim", "add_example", "new_helper", "unsatisfiable"], x: kind_text })) {
    kind = kind_text;
  }
  let def = fallback_def;
  if (is_string({ j: $json.at({ j: parsed, key: "def" }) })) {
    def = $json.str_at({ j: parsed, key: "def" });
  }
  let rationale = $std_text.trim({ t: answer });
  if (is_string({ j: $json.at({ j: parsed, key: "rationale" }) })) {
    rationale = $json.str_at({ j: parsed, key: "rationale" });
  }
  return { tag: "Some", value: proposal_json({ kind: kind, def: def, current: str_or_none({ j: $json.at({ j: parsed, key: "current" }) }), proposed: str_or_none({ j: $json.at({ j: parsed, key: "proposed" }) }), counterexample: { tag: "JNull" }, iterations: 1, finding: { tag: "None" }, rationale: rationale }) };
}

export function copy_texts({ d }) {
  let out = $std_map.dict({  });
  for (const k of $std_map.keys({ d: d })) {
    const [, out$88] = $std_map.set({ d: out, key: k, value: or_empty({ o: $std_map.find({ d: d, key: k }) }) });
    out = out$88;
  }
  return out;
}

export function failure({ error }) {
  return { status: "error", change: { tag: "None" }, error: { tag: "Some", value: error }, path: { tag: "None" } };
}

export function log({ console, line }) {
  $std_io.eprint({ console: console, text: line + "\n" });
  return undefined;
}

export function diagnostics_of({ c, fallback }) {
  const $m278 = c;
  $m278$match: {
    if ($m278.tag === "Some") {
      const value = $m278.value;
      return value.json;
      break $m278$match;
    }
    if ($m278.tag === "None") {
      return fallback.json;
      break $m278$match;
    }
    $rt.unreachable();
  }
}

export function count_of({ c, fallback }) {
  return $std_list.len({ xs: diagnostics_of({ c: c, fallback: fallback }) });
}

export function escalate({ r }) {
  r = { ...r, ladder: $rt.int.add(r.ladder, 1, $ob44), escalation_steps: $rt.int.add(r.escalation_steps, 1, $ob45) };
  let go = true;
  let answer = false;
  let guard = 8;
  while (go && guard > 0) {
    guard = $rt.int.sub(guard, 1, $ob46);
    if (r.ladder === 1) {
      r = { ...r, full_history: true, pending_escalation: { tag: "Some", value: "full diagnostic history" } };
      answer = true;
      go = false;
    } else {
      if (r.ladder === 2) {
        if (r.ctx_policy === "scope") {
          r = { ...r, ladder: r.ladder + 1 };
        } else {
          let next_policy = "scope";
          if (r.ctx_policy === "none") {
            next_policy = "module";
          }
          r = { ...r, ctx_policy: next_policy, pending_escalation: { tag: "Some", value: "context policy " + next_policy } };
          answer = true;
          go = false;
        }
      } else {
        if (r.ladder === 3 || r.ladder === 4) {
          r = { ...r, ladder: r.ladder + 1 };
        } else {
          r = { ...r, cause: { tag: "Some", value: "stall" } };
          answer = false;
          go = false;
        }
      }
    }
  }
  return [answer, r];
}

export function record_trace({ r, console, iteration, model, prompt_hash, before, diags, failing, repairs, tokens, ms, classification, escalation }) {
  let codes = $std_list.builder({  });
  for (const d of diags) {
    const [, codes$89] = $std_list.push({ b: codes, x: $json.str_at({ j: d, key: "code" }) });
    codes = codes$89;
  }
  const entry = { tag: "JObject", fields: [$json.field({ key: "iteration", value: $json.int({ v: iteration }) }), $json.field({ key: "model", value: $json.text({ t: model }) }), $json.field({ key: "prompt_hash", value: $json.text({ t: prompt_hash }) }), $json.field({ key: "diagnostics_before", value: $json.int({ v: before }) }), $json.field({ key: "diagnostics_after", value: $json.int({ v: $std_list.len({ xs: diags }) }) }), $json.field({ key: "codes_after", value: $json.texts({ ts: $std_list.finish({ b: codes }) }) }), $json.field({ key: "failing_tests", value: $json.texts({ ts: failing }) }), $json.field({ key: "mechanical_repairs", value: $json.int({ v: repairs }) }), $json.field({ key: "tokens", value: $json.int({ v: tokens }) }), $json.field({ key: "ms", value: $json.int({ v: ms }) }), $json.field({ key: "classification", value: $json.text({ t: classification }) }), $json.field({ key: "escalation", value: $json.text_or_null({ o: escalation }) })] };
  r = { ...r, trace: $std_list.append({ xs: r.trace, x: entry }) };
  let failing_text = "";
  if ($std_list.len({ xs: failing }) > 0) {
    failing_text = ", " + plural({ n: $std_list.len({ xs: failing }), word: "failing test" });
  }
  log({ console: console, line: "onus loop: iteration " + $std_int.to_text({ x: iteration }) + ": " + classification + " (" + plural({ n: $std_list.len({ xs: diags }), word: "diagnostic" }) + failing_text + ")" });
  return [undefined, r];
  return [undefined, r];
}

export function check_working({ r, files, console, process, tools, texts }) {
  r = { ...r, checks: $rt.int.add(r.checks, 1, $ob47) };
  return [check_project({ files: files, console: console, process: process, project: r.project, tools: tools, texts: texts, run_tests: true, out_dir: r.work_dir + "/check-" + $std_int.to_text({ x: r.checks }) }), r];
}

export function ask({ r, files, process, net, model, system, prompt, max_tokens }) {
  try {
    const answer = $rt.unwrap(generate({ m: model, process: process, net: net, call: r.calls, system: system, prompt: prompt, max_tokens: max_tokens }));
    r = { ...r, calls: $rt.int.add(r.calls, 1, $ob48), tokens: $rt.int.add(r.tokens, answer.tokens, $ob49), model_name: answer.model };
    return [{ tag: "Ok", value: answer }, r];
  } catch ($e) {
    if ($e instanceof $rt.EarlyReturn) return [$e.value, r];
    throw $e;
  }
}

export function take_messages({ r }) {
  const out = r.messages;
  r = { ...r, messages: [] };
  return [out, r];
}

export function push_message({ r, m }) {
  r = { ...r, messages: $std_list.append({ xs: r.messages, x: m }) };
  return [undefined, r];
  return [undefined, r];
}

export function push_proposal({ r, p }) {
  r = { ...r, proposals: $std_list.append({ xs: r.proposals, x: p }) };
  return [undefined, r];
  return [undefined, r];
}

export function push_history({ r, h }) {
  r = { ...r, history: $std_list.append({ xs: r.history, x: h }) };
  return [undefined, r];
  return [undefined, r];
}

export function stop({ r }) {
  r = { ...r, done: true };
  return [undefined, r];
  return [undefined, r];
}

export function body_diffs({ r }) {
  let out = $std_list.builder({  });
  for (const path of $std_map.keys({ d: r.working })) {
    const after = or_empty({ o: $std_map.find({ d: r.working, key: path }) });
    const before = or_empty({ o: $std_map.find({ d: r.texts0, key: path }) });
    if (before !== after) {
      let module_name = "";
      for (const name of $std_map.keys({ d: r.project.paths })) {
        if (module_name === "" && or_empty({ o: $std_map.find({ d: r.project.paths, key: name }) }) === path) {
          module_name = name;
        }
      }
      const [, out$90] = $std_list.push({ b: out, x: { tag: "JObject", fields: [$json.field({ key: "file", value: $json.text({ t: path }) }), $json.field({ key: "module", value: $json.text({ t: module_name }) }), $json.field({ key: "before", value: $json.text({ t: before }) }), $json.field({ key: "after", value: $json.text({ t: after }) })] } });
      out = out$90;
    }
  }
  return $std_list.finish({ b: out });
}

export function conclude({ r, files, status, final, now, write }) {
  try {
    let findings = [];
    if (r.task.kind === "regenerate") {
      findings = audit({ baseline: r.baseline, final: final });
      if ($std_list.len({ xs: findings }) > 0) {
        r = { ...r, proposals: [...r.proposals, ...proposals_from_audit({ findings: findings, target: $std_text.join({ parts: r.task.scope, sep: ", " }), iterations: $std_list.len({ xs: r.trace }) })] };
      }
    }
    const diffs = body_diffs({ r: r });
    let fields = $std_list.builder({  });
    const [, fields$91] = $std_list.push({ b: fields, x: $json.field({ key: "schema_version", value: $json.int({ v: 1 }) }) });
    fields = fields$91;
    const [, fields$92] = $std_list.push({ b: fields, x: $json.field({ key: "task", value: task_json({ t: r.task }) }) });
    fields = fields$92;
    const [, fields$93] = $std_list.push({ b: fields, x: $json.field({ key: "status", value: $json.text({ t: status }) }) });
    fields = fields$93;
    const $m301 = r.cause;
    $m301$match: {
      if ($m301.tag === "Some") {
        const value = $m301.value;
        const [, fields$94] = $std_list.push({ b: fields, x: $json.field({ key: "cause", value: $json.text({ t: value }) }) });
        fields = fields$94;
        break $m301$match;
      }
      if ($m301.tag === "None") {
        skip({  });
        break $m301$match;
      }
      $rt.unreachable();
    }
    const [, fields$95] = $std_list.push({ b: fields, x: $json.field({ key: "generated", value: { tag: "JObject", fields: [$json.field({ key: "at", value: $json.text({ t: $assumptions.iso_of_millis({ ms: now }) }) }), $json.field({ key: "model", value: $json.text({ t: r.model_name }) })] } }) });
    fields = fields$95;
    let iface = [];
    let delta = [];
    const $m303 = final;
    $m303$match: {
      if ($m303.tag === "Some") {
        const value = $m303.value;
        iface = interface_diffs({ scope: r.task.scope, before: r.baseline, after: value });
        delta = ledger_delta({ before: r.baseline, after: value });
        break $m303$match;
      }
      if ($m303.tag === "None") {
        skip({  });
        break $m303$match;
      }
      $rt.unreachable();
    }
    const [, fields$96] = $std_list.push({ b: fields, x: $json.field({ key: "interface_diff", value: { tag: "JArray", items: iface } }) });
    fields = fields$96;
    const [, fields$97] = $std_list.push({ b: fields, x: $json.field({ key: "ledger_delta", value: { tag: "JArray", items: delta } }) });
    fields = fields$97;
    let body_diff = [];
    if (status === "opened") {
      body_diff = diffs;
    }
    const [, fields$98] = $std_list.push({ b: fields, x: $json.field({ key: "body_diff", value: { tag: "JArray", items: body_diff } }) });
    fields = fields$98;
    const [, fields$99] = $std_list.push({ b: fields, x: $json.field({ key: "trace", value: { tag: "JArray", items: r.trace } }) });
    fields = fields$99;
    const [, fields$100] = $std_list.push({ b: fields, x: $json.field({ key: "metrics", value: { tag: "JObject", fields: [$json.field({ key: "iterations", value: $json.int({ v: $std_list.len({ xs: r.trace }) }) }), $json.field({ key: "mechanical_repairs", value: $json.int({ v: r.mechanical }) }), $json.field({ key: "escalation_steps", value: $json.int({ v: r.escalation_steps }) }), $json.field({ key: "proposals", value: $json.int({ v: $std_list.len({ xs: r.proposals }) }) }), $json.field({ key: "tokens", value: $json.int({ v: r.tokens }) })] } }) });
    fields = fields$100;
    const [, fields$101] = $std_list.push({ b: fields, x: $json.field({ key: "proposals", value: { tag: "JArray", items: r.proposals } }) });
    fields = fields$101;
    let audit_json = $std_list.builder({  });
    for (const f of findings) {
      const [, audit_json$102] = $std_list.push({ b: audit_json, x: finding_json({ f: f }) });
      audit_json = audit_json$102;
    }
    const [, fields$103] = $std_list.push({ b: fields, x: $json.field({ key: "audit", value: { tag: "JArray", items: $std_list.finish({ b: audit_json }) } }) });
    fields = fields$103;
    if (status === "blocked") {
      let last = [];
      const n_history = $std_list.len({ xs: r.history });
      if (n_history > 0) {
        last = $std_list.get({ xs: r.history, i: $rt.int.sub(n_history, 1, $ob50) }).diagnostics;
      } else {
        const $m311 = final;
        $m311$match: {
          if ($m311.tag === "Some") {
            const value = $m311.value;
            last = value.json;
            break $m311$match;
          }
          if ($m311.tag === "None") {
            skip({  });
            break $m311$match;
          }
          $rt.unreachable();
        }
      }
      const [, fields$104] = $std_list.push({ b: fields, x: $json.field({ key: "last_diagnostics", value: { tag: "JArray", items: last } }) });
      fields = fields$104;
      let best_json = { tag: "JNull" };
      const $m314 = r.best;
      $m314$match: {
        if ($m314.tag === "Some") {
          const value = $m314.value;
          best_json = { tag: "JObject", fields: [$json.field({ key: "iteration", value: $json.int({ v: value.iteration }) }), $json.field({ key: "diagnostics", value: $json.int({ v: value.diagnostics }) }), $json.field({ key: "bodies", value: best_bodies({ texts: value.texts, targets: r.targets }) })] };
          break $m314$match;
        }
        if ($m314.tag === "None") {
          skip({  });
          break $m314$match;
        }
        $rt.unreachable();
      }
      const [, fields$105] = $std_list.push({ b: fields, x: $json.field({ key: "best_attempt", value: best_json }) });
      fields = fields$105;
    }
    const change = { tag: "JObject", fields: $std_list.finish({ b: fields }) };
    let path = { tag: "None" };
    if (write) {
      if (status === "opened") {
        for (const d of diffs) {
          $rt.unwrap($build.write_file({ files: files, path: $json.str_at({ j: d, key: "file" }), text: $json.str_at({ j: d, key: "after" }) }));
        }
      }
      const written = $rt.unwrap(write_change({ files: files, root: r.project.root, task_id: r.task.id, change: change }));
      path = { tag: "Some", value: written };
    }
    let outcome_status = "blocked";
    if (status === "opened") {
      outcome_status = "change";
    }
    return [{ tag: "Ok", value: { status: outcome_status, change: { tag: "Some", value: change }, error: { tag: "None" }, path: path } }, r];
  } catch ($e) {
    if ($e instanceof $rt.EarlyReturn) return [$e.value, r];
    throw $e;
  }
}

export function ticket({ r, files, process, net, clock, model, write }) {
  const ctx_in = assemble({ task: r.task, baseline: r.baseline, current: r.baseline, targets: [], ctx_policy: r.task.context_policy, history: [], full_history: false, messages: ["This is a ticket. Do not write code. Answer with one JSON object describing the interface change it needs: {\"kind\": one of weaken_postcondition, add_precondition, widen_effects, add_claim, add_example, new_helper, unsatisfiable; \"def\": qualified name; \"current\": text or null; \"proposed\": text or null; \"rationale\": text}."] });
  const t0 = $std_io.time({ clock: clock });
  const [$r324, r$106] = ask({ r: r, files: files, process: process, net: net, model: model, system: ctx_in.system, prompt: ctx_in.prompt, max_tokens: 4096 });
  r = r$106;
  const $m323 = $r324;
  $m323$match: {
    if ($m323.tag === "Err") {
      const error = $m323.error;
      return [{ tag: "Ok", value: failure({ error: "model: " + error }) }, r];
      break $m323$match;
    }
    if ($m323.tag === "Ok") {
      const value = $m323.value;
      const [$r326, r$107] = note_proposal({ r: r, p: parse_proposal({ answer: value.text, fallback_def: fallback_def({ task: r.task }) }) });
      r = r$107;
      const classification = $r326;
      const n = $std_list.len({ xs: r.baseline.json });
      r = { ...r, trace: $std_list.append({ xs: r.trace, x: { tag: "JObject", fields: [$json.field({ key: "iteration", value: $json.int({ v: 1 }) }), $json.field({ key: "model", value: $json.text({ t: value.model }) }), $json.field({ key: "prompt_hash", value: $json.text({ t: ctx_in.prompt_hash }) }), $json.field({ key: "diagnostics_before", value: $json.int({ v: n }) }), $json.field({ key: "diagnostics_after", value: $json.int({ v: n }) }), $json.field({ key: "codes_after", value: { tag: "JArray", items: [] } }), $json.field({ key: "failing_tests", value: { tag: "JArray", items: [] } }), $json.field({ key: "mechanical_repairs", value: $json.int({ v: 0 }) }), $json.field({ key: "tokens", value: $json.int({ v: value.tokens }) }), $json.field({ key: "ms", value: $json.int({ v: $rt.int.sub($std_io.time({ clock: clock }), t0, $ob51) }) }), $json.field({ key: "classification", value: $json.text({ t: classification }) }), $json.field({ key: "escalation", value: { tag: "JNull" } })] } }) };
      const [$r333, r$108] = conclude({ r: r, files: files, status: "opened", final: { tag: "Some", value: r.baseline }, now: $std_io.time({ clock: clock }), write: write });
      r = r$108;
      return [$r333, r];
      break $m323$match;
    }
    $rt.unreachable();
  }
}

export function fallback_def({ task }) {
  const $m334 = task.target;
  $m334$match: {
    if ($m334.tag === "Some") {
      const value = $m334.value;
      return value.def;
      break $m334$match;
    }
    if ($m334.tag === "None") {
      if ($std_list.len({ xs: task.scope }) > 0) {
        return $std_list.get({ xs: task.scope, i: 0 });
      }
      return "";
      break $m334$match;
    }
    $rt.unreachable();
  }
}

export function note_proposal({ r, p }) {
  const $m335 = p;
  $m335$match: {
    if ($m335.tag === "Some") {
      const value = $m335.value;
      const [, r$109] = push_proposal({ r: r, p: value });
      r = r$109;
      return ["proposal", r];
      break $m335$match;
    }
    if ($m335.tag === "None") {
      return ["no proposal", r];
      break $m335$match;
    }
    $rt.unreachable();
  }
}

export function splice_answers({ r, returned, iteration }) {
  let sp = { texts: copy_texts({ d: r.working }), count: 0, repeated: false };
  for (const target of r.targets) {
    const $m337 = find_in({ fns: returned, name: target.name });
    $m337$match: {
      if ($m337.tag === "None") {
        const [, r$110] = push_message({ r: r, m: "The answer did not include `" + target.name + "`; return every target." });
        r = r$110;
        break $m337$match;
      }
      if ($m337.tag === "Some") {
        const value = $m337.value;
        const [, r$111, sp$111] = splice_found({ r: r, target: target, f: value, iteration: iteration, sp: sp });
        r = r$111;
        sp = sp$111;
        break $m337$match;
      }
      $rt.unreachable();
    }
  }
  return [sp, r];
}

export function splice_found({ r, target, f, iteration, sp }) {
  if (signature_text({ f: f }) !== signature_text({ f: target.decl })) {
    r = { ...r, claim_edits: $rt.int.add(r.claim_edits, 1, $ob52) };
    const [, r$112] = push_message({ r: r, m: "The signature of `" + target.name + "` was changed; only the body may change. Keep exactly:\n" + signature_text({ f: target.decl }) });
    r = r$112;
    if (r.claim_edits >= 2) {
      const [, r$113] = push_proposal({ r: r, p: signature_proposal({ target: target, proposed: f, iteration: iteration }) });
      r = r$113;
    }
    return [undefined, r, sp];
  }
  const $m339 = f.body;
  $m339$match: {
    if ($m339.tag === "None") {
      const [, r$114] = push_message({ r: r, m: "`" + target.name + "` came back without a body." });
      r = r$114;
      break $m339$match;
    }
    if ($m339.tag === "Some") {
      const value = $m339.value;
      if (value.elided) {
        const [, r$115] = push_message({ r: r, m: "`" + target.name + "` came back without a body." });
        r = r$115;
      } else {
        const [, r$116, sp$116] = splice_body({ r: r, target: target, body: value, sp: sp });
        r = r$116;
        sp = sp$116;
      }
      break $m339$match;
    }
    $rt.unreachable();
  }
  return [undefined, r, sp];
  return [undefined, r, sp];
}

export function splice_body({ r, target, body, sp }) {
  const text = or_empty({ o: $std_map.find({ d: sp.texts, key: target.path }) });
  const $m340 = parse_module({ path: target.path, text: text }).module;
  $m340$match: {
    if ($m340.tag === "None") {
      skip({  });
      break $m340$match;
    }
    if ($m340.tag === "Some") {
      const value = $m340.value;
      const [, r$117, sp$117] = splice_into({ r: r, target: target, body: body, cur: find_fn({ m: value, name: target.name }), text: text, sp: sp });
      r = r$117;
      sp = sp$117;
      break $m340$match;
    }
    $rt.unreachable();
  }
  return [undefined, r, sp];
  return [undefined, r, sp];
}

export function splice_into({ r, target, body, cur, text, sp }) {
  const $m341 = cur;
  $m341$match: {
    if ($m341.tag === "None") {
      skip({  });
      break $m341$match;
    }
    if ($m341.tag === "Some") {
      const value = $m341.value;
      const h = body_hash({ f: with_body({ f: target.decl, body: body }) });
      let hashes = or_dict({ o: $std_map.find({ d: r.seen_bodies, key: target.name }) });
      let repeated = sp.repeated;
      if ($std_map.contains({ d: hashes, key: h })) {
        repeated = true;
      }
      const [, hashes$118] = $std_map.set({ d: hashes, key: h, value: true });
      hashes = hashes$118;
      let seen_bodies = r.seen_bodies;
      const [, seen_bodies$119] = $std_map.set({ d: seen_bodies, key: target.name, value: hashes });
      seen_bodies = seen_bodies$119;
      r = { ...r, seen_bodies: seen_bodies };
      let texts = sp.texts;
      const [, texts$120] = $std_map.set({ d: texts, key: target.path, value: canonical_text({ path: target.path, text: splice_item({ text: text, span: value.span, replacement: { tag: "FnItem", decl: with_body({ f: value, body: body }) } }) }) });
      texts = texts$120;
      sp = { ...sp, texts: texts, count: $rt.int.add(sp.count, 1, $ob53), repeated: repeated };
      break $m341$match;
    }
    $rt.unreachable();
  }
  return [undefined, r, sp];
  return [undefined, r, sp];
}

export function or_dict({ o }) {
  const $m345 = o;
  $m345$match: {
    if ($m345.tag === "Some") {
      const value = $m345.value;
      return value;
      break $m345$match;
    }
    if ($m345.tag === "None") {
      return $std_map.dict({  });
      break $m345$match;
    }
    $rt.unreachable();
  }
}

export function find_in({ fns, name }) {
  for (const f of fns) {
    if (f.name.text === name) {
      return { tag: "Some", value: f };
    }
  }
  return { tag: "None" };
}

export function failing_of({ c }) {
  let out = $std_list.builder({  });
  const $m348 = c.tests;
  $m348$match: {
    if ($m348.tag === "None") {
      skip({  });
      break $m348$match;
    }
    if ($m348.tag === "Some") {
      const value = $m348.value;
      for (const name of $std_map.keys({ d: value })) {
        if (!test_passed({ tests: value, name: name })) {
          const [, out$121] = $std_list.push({ b: out, x: name });
          out = out$121;
        }
      }
      break $m348$match;
    }
    $rt.unreachable();
  }
  return $std_list.finish({ b: out });
}

export function repair_rounds({ r, files, console, process, tools, first }) {
  try {
    let current = first;
    let repairs = 0;
    let round = 0;
    let go = true;
    while (go && round < 3) {
      round = round + 1;
      if (current.internal_error || $std_list.len({ xs: current.diagnostics }) === 0) {
        go = false;
      } else {
        const spans = target_body_spans({ c: current, targets: r.targets });
        let changed = false;
        for (const path of $std_map.keys({ d: spans })) {
          const inside = or_list({ o: $std_map.find({ d: spans, key: path }) });
          const $m350 = $std_map.find({ d: r.working, key: path });
          $m350$match: {
            if ($m350.tag === "None") {
              skip({  });
              break $m350$match;
            }
            if ($m350.tag === "Some") {
              const value = $m350.value;
              const text = value;
              let edits = $std_list.builder({  });
              for (const d of current.diagnostics) {
                if (file_path_of({ c: current, file: d.file }) === path) {
                  for (const rep of d.repairs) {
                    if (rep.confidence === "high" && within({ spans: inside, span: rep.span })) {
                      const [, edits$122] = $std_list.push({ b: edits, x: { start: rep.span.start, end: rep.span.end, with_text: rep.with_text } });
                      edits = edits$122;
                    }
                  }
                }
              }
              const applied = apply_repairs({ text: text, edits: $std_list.finish({ b: edits }) });
              if (applied.applied > 0) {
                let working = r.working;
                const [, working$123] = $std_map.set({ d: working, key: path, value: canonical_text({ path: path, text: applied.text }) });
                working = working$123;
                r = { ...r, working: working };
                repairs = $rt.int.add(repairs, applied.applied, $ob54);
                changed = true;
              }
              break $m350$match;
            }
            $rt.unreachable();
          }
        }
        if (changed) {
          const [$r353, r$124] = check_working({ r: r, files: files, console: console, process: process, tools: tools, texts: r.working });
          r = r$124;
          current = $rt.unwrap($r353);
        } else {
          go = false;
        }
      }
    }
    r = { ...r, mechanical: $rt.int.add(r.mechanical, repairs, $ob55) };
    return [{ tag: "Ok", value: current }, r];
  } catch ($e) {
    if ($e instanceof $rt.EarlyReturn) return [$e.value, r];
    throw $e;
  }
}

export function or_list({ o }) {
  const $m356 = o;
  $m356$match: {
    if ($m356.tag === "Some") {
      const value = $m356.value;
      return value;
      break $m356$match;
    }
    if ($m356.tag === "None") {
      return [];
      break $m356$match;
    }
    $rt.unreachable();
  }
}

export function set_best({ r, iteration, size }) {
  let better = true;
  const $m357 = r.best;
  $m357$match: {
    if ($m357.tag === "Some") {
      const value = $m357.value;
      better = size < value.diagnostics;
      break $m357$match;
    }
    if ($m357.tag === "None") {
      skip({  });
      break $m357$match;
    }
    $rt.unreachable();
  }
  if (better) {
    r = { ...r, best: { tag: "Some", value: { iteration: iteration, diagnostics: size, texts: copy_texts({ d: r.working }) } } };
  }
  return [undefined, r];
  return [undefined, r];
}

export function conflicts_of({ c, targets }) {
  let out = $std_list.builder({  });
  for (const d of c.diagnostics) {
    const $m361 = d.obligation;
    $m361$match: {
      if ($m361.tag === "Some") {
        const value = $m361.value;
        if (value.has_counterexample && is_target_name({ targets: targets, name: or_empty({ o: d.def_name }) })) {
          const [, out$125] = $std_list.push({ b: out, x: d });
          out = out$125;
        }
        break $m361$match;
      }
      if ($m361.tag === "None") {
        skip({  });
        break $m361$match;
      }
      $rt.unreachable();
    }
  }
  return $std_list.finish({ b: out });
}

export function counterexample_json({ c, d }) {
  const j = $diagjson.to_json({ ctx: c.ctx, tab: c.tab, d: d });
  return $json.at({ j: $json.at({ j: j, key: "obligation" }), key: "counterexample" });
}

export function iterate({ r, files, console, process, net, clock, model, tools, iteration }) {
  try {
    if ($rt.int.sub($std_io.time({ clock: clock }), r.started, $ob56) > r.task.budget.wall_ms || r.tokens > r.task.budget.tokens) {
      r = { ...r, cause: { tag: "Some", value: "budget" } };
      const [, r$126] = stop({ r: r });
      r = r$126;
      return [{ tag: "Ok", value: { tag: "None" } }, r];
    }
    let current_check = r.baseline;
    const $m366 = r.current;
    $m366$match: {
      if ($m366.tag === "Some") {
        const value = $m366.value;
        current_check = value;
        break $m366$match;
      }
      if ($m366.tag === "None") {
        skip({  });
        break $m366$match;
      }
      $rt.unreachable();
    }
    const [$r367, r$127] = take_messages({ r: r });
    r = r$127;
    const ctx_in = assemble({ task: r.task, baseline: r.baseline, current: current_check, targets: r.targets, ctx_policy: r.ctx_policy, history: r.history, full_history: r.full_history, messages: $r367 });
    const t0 = $std_io.time({ clock: clock });
    let answer = { text: "", model: "", tokens: 0 };
    const [$r370, r$128] = ask({ r: r, files: files, process: process, net: net, model: model, system: ctx_in.system, prompt: ctx_in.prompt, max_tokens: 8192 });
    r = r$128;
    const $m369 = $r370;
    $m369$match: {
      if ($m369.tag === "Err") {
        const error = $m369.error;
        return [{ tag: "Ok", value: { tag: "Some", value: "model: " + error } }, r];
        break $m369$match;
      }
      if ($m369.tag === "Ok") {
        const value = $m369.value;
        answer = value;
        break $m369$match;
      }
      $rt.unreachable();
    }
    $rt.unwrap($build.write_file({ files: files, path: r.work_dir + "/prompt-" + $std_int.to_text({ x: iteration }) + ".md", text: ctx_in.system + "\n\n---\n\n" + ctx_in.prompt + "\n" }));
    $rt.unwrap($build.write_file({ files: files, path: r.work_dir + "/answer-" + $std_int.to_text({ x: iteration }) + ".md", text: answer.text }));
    const escalation = r.pending_escalation;
    r = { ...r, pending_escalation: { tag: "None" } };
    const before = count_of({ c: r.current, fallback: r.baseline });
    const fragment = parse_fragment({ text: extract_code({ answer: answer.text }) });
    if (fragment.module.tag === "None" || $std_list.len({ xs: fragment.diagnostics }) > 0) {
      const [, r$129] = push_history({ r: r, h: { iteration: iteration, diagnostics: fragment.json, failing: [] } });
      r = r$129;
      const sig = outcome_signature({ diagnostics: fragment.json, failing: [] });
      const stalled = $std_map.contains({ d: r.seen, key: sig });
      let seen = r.seen;
      const [, seen$130] = $std_map.set({ d: seen, key: sig, value: true });
      seen = seen$130;
      r = { ...r, seen: seen };
      const [, r$131] = push_message({ r: r, m: "The previous answer did not parse as Onus; the diagnostics above are about that answer, not the file. The offending lines of that answer were:" });
      r = r$131;
      for (const note of syntax_notes({ fragment: fragment })) {
        const [, r$132] = push_message({ r: r, m: note });
        r = r$132;
      }
      let classification = "progress";
      if (stalled) {
        classification = "stall";
      }
      const [, r$133] = record_trace({ r: r, console: console, iteration: iteration, model: answer.model, prompt_hash: ctx_in.prompt_hash, before: before, diags: fragment.json, failing: [], repairs: 0, tokens: answer.tokens, ms: $rt.int.sub($std_io.time({ clock: clock }), t0, $ob57), classification: classification, escalation: escalation });
      r = r$133;
      let $sc378 = stalled;
      if ($sc378) {
        const [$r377, r$134] = escalate({ r: r });
        r = r$134;
        $sc378 = !$r377;
      }
      if ($sc378) {
        const [, r$135] = stop({ r: r });
        r = r$135;
      }
      return [{ tag: "Ok", value: { tag: "None" } }, r];
    }
    let returned = [];
    let item_count = 0;
    const $m381 = fragment.module;
    $m381$match: {
      if ($m381.tag === "Some") {
        const value = $m381.value;
        returned = fns_of({ m: value });
        item_count = $std_list.len({ xs: value.items });
        break $m381$match;
      }
      if ($m381.tag === "None") {
        skip({  });
        break $m381$match;
      }
      $rt.unreachable();
    }
    let extra = $std_list.builder({  });
    for (const f of returned) {
      if (!is_target_name({ targets: r.targets, name: f.name.text })) {
        const [, extra$136] = $std_list.push({ b: extra, x: f });
        extra = extra$136;
      }
    }
    const extras = $std_list.finish({ b: extra });
    if ($std_list.len({ xs: extras }) > 0) {
      r = { ...r, helper_attempts: $rt.int.add(r.helper_attempts, 1, $ob58) };
      if (r.helper_attempts >= 2 || item_count !== $std_list.len({ xs: returned })) {
        let first_module = "";
        if ($std_list.len({ xs: r.targets }) > 0) {
          first_module = $std_list.get({ xs: r.targets, i: 0 }).module_name;
        }
        for (const f of extras) {
          const [, r$137] = push_proposal({ r: r, p: proposal_json({ kind: "new_helper", def: first_module + "." + f.name.text, current: { tag: "None" }, proposed: { tag: "Some", value: signature_text({ f: f }) }, counterexample: { tag: "JNull" }, iterations: iteration, finding: { tag: "None" }, rationale: "the model needed a helper it may not introduce (loop spec §4.1 step 3 is off)" }) });
          r = r$137;
        }
        r = { ...r, cause: { tag: "Some", value: "out_of_scope" } };
        const [, r$138] = record_trace({ r: r, console: console, iteration: iteration, model: answer.model, prompt_hash: ctx_in.prompt_hash, before: before, diags: diagnostics_of({ c: r.current, fallback: r.baseline }), failing: [], repairs: 0, tokens: answer.tokens, ms: $rt.int.sub($std_io.time({ clock: clock }), t0, $ob59), classification: "unusable", escalation: escalation });
        r = r$138;
        const [, r$139] = stop({ r: r });
        r = r$139;
        return [{ tag: "Ok", value: { tag: "None" } }, r];
      }
      let names = $std_list.builder({  });
      for (const f of extras) {
        const [, names$140] = $std_list.push({ b: names, x: "`" + f.name.text + "`" });
        names = names$140;
      }
      const [, r$141] = push_message({ r: r, m: "The answer added " + $std_text.join({ parts: $std_list.finish({ b: names }), sep: ", " }) + "; the loop may not add functions. Return only the targets." });
      r = r$141;
    }
    const [$r391, r$142] = splice_answers({ r: r, returned: returned, iteration: iteration });
    r = r$142;
    const spliced = $r391;
    if (r.claim_edits >= 2) {
      r = { ...r, cause: { tag: "Some", value: "out_of_scope" } };
      const [, r$143] = record_trace({ r: r, console: console, iteration: iteration, model: answer.model, prompt_hash: ctx_in.prompt_hash, before: before, diags: diagnostics_of({ c: r.current, fallback: r.baseline }), failing: [], repairs: 0, tokens: answer.tokens, ms: $rt.int.sub($std_io.time({ clock: clock }), t0, $ob60), classification: "unusable", escalation: escalation });
      r = r$143;
      const [, r$144] = stop({ r: r });
      r = r$144;
      return [{ tag: "Ok", value: { tag: "None" } }, r];
    }
    if (spliced.count === 0) {
      const [, r$145] = push_history({ r: r, h: { iteration: iteration, diagnostics: diagnostics_of({ c: r.current, fallback: r.baseline }), failing: [] } });
      r = r$145;
      const [, r$146] = record_trace({ r: r, console: console, iteration: iteration, model: answer.model, prompt_hash: ctx_in.prompt_hash, before: before, diags: diagnostics_of({ c: r.current, fallback: r.baseline }), failing: [], repairs: 0, tokens: answer.tokens, ms: $rt.int.sub($std_io.time({ clock: clock }), t0, $ob61), classification: "unusable", escalation: escalation });
      r = r$146;
      const [$r397, r$147] = escalate({ r: r });
      r = r$147;
      if (!$r397) {
        const [, r$148] = stop({ r: r });
        r = r$148;
      }
      return [{ tag: "Ok", value: { tag: "None" } }, r];
    }
    r = { ...r, working: spliced.texts };
    const [$r401, r$149] = check_working({ r: r, files: files, console: console, process: process, tools: tools, texts: r.working });
    r = r$149;
    const first = $rt.unwrap($r401);
    const mechanical_before = r.mechanical;
    const [$r402, r$150] = repair_rounds({ r: r, files: files, console: console, process: process, tools: tools, first: first });
    r = r$150;
    const checked = $rt.unwrap($r402);
    const repairs = $rt.int.sub(r.mechanical, mechanical_before, $ob62);
    if (checked.internal_error) {
      r = { ...r, cause: { tag: "Some", value: "compiler_bug" }, current: { tag: "Some", value: checked } };
      const [, r$151] = record_trace({ r: r, console: console, iteration: iteration, model: answer.model, prompt_hash: ctx_in.prompt_hash, before: before, diags: checked.json, failing: [], repairs: repairs, tokens: answer.tokens, ms: $rt.int.sub($std_io.time({ clock: clock }), t0, $ob63), classification: "unusable", escalation: escalation });
      r = r$151;
      const [, r$152] = stop({ r: r });
      r = r$152;
      return [{ tag: "Ok", value: { tag: "None" } }, r];
    }
    r = { ...r, current: { tag: "Some", value: checked } };
    const failing = failing_of({ c: checked });
    const [, r$153] = push_history({ r: r, h: { iteration: iteration, diagnostics: checked.json, failing: failing } });
    r = r$153;
    const size = $rt.int.add($std_list.len({ xs: checked.json }), $std_list.len({ xs: failing }), $ob64);
    const [, r$154] = set_best({ r: r, iteration: iteration, size: size });
    r = r$154;
    const sig = outcome_signature({ diagnostics: checked.json, failing: failing });
    const conflicts = conflicts_of({ c: checked, targets: r.targets });
    let classification = "progress";
    if ($std_list.len({ xs: checked.diagnostics }) === 0 && $std_list.len({ xs: failing }) === 0) {
      classification = "green";
    } else {
      if ($std_list.len({ xs: conflicts }) > 0 && spliced.repeated) {
        classification = "contract conflict";
      } else {
        if ($std_map.contains({ d: r.seen, key: sig })) {
          classification = "stall";
        } else {
          const n_history = $std_list.len({ xs: r.history });
          let prev_size = $rt.int.add(size, 1, $ob65);
          if (n_history >= 2) {
            const prev = $std_list.get({ xs: r.history, i: $rt.int.sub(n_history, 2, $ob66) });
            prev_size = $rt.int.add($std_list.len({ xs: prev.diagnostics }), $std_list.len({ xs: prev.failing }), $ob67);
          }
          if (size < prev_size) {
            classification = "progress";
            r = { ...r, grew: 0 };
          } else {
            r = { ...r, grew: $rt.int.add(r.grew, 1, $ob68) };
            if (r.grew >= 2) {
              classification = "stall";
            } else {
              classification = "grew";
            }
          }
        }
      }
    }
    let seen = r.seen;
    const [, seen$155] = $std_map.set({ d: seen, key: sig, value: true });
    seen = seen$155;
    r = { ...r, seen: seen };
    const [, r$156] = record_trace({ r: r, console: console, iteration: iteration, model: answer.model, prompt_hash: ctx_in.prompt_hash, before: before, diags: checked.json, failing: failing, repairs: repairs, tokens: answer.tokens, ms: $rt.int.sub($std_io.time({ clock: clock }), t0, $ob69), classification: classification, escalation: escalation });
    r = r$156;
    if (classification === "green") {
      r = { ...r, green: true };
      const [, r$157] = stop({ r: r });
      r = r$157;
      return [{ tag: "Ok", value: { tag: "None" } }, r];
    }
    if (classification === "contract conflict") {
      for (const d of conflicts) {
        const $m417 = d.obligation;
        $m417$match: {
          if ($m417.tag === "None") {
            skip({  });
            break $m417$match;
          }
          if ($m417.tag === "Some") {
            const value = $m417.value;
            const ob = value;
            let kind = "unsatisfiable";
            if (ob.kind === "ensures") {
              kind = "weaken_postcondition";
            }
            if (ob.kind === "requires") {
              kind = "add_precondition";
            }
            let def = or_empty({ o: d.def_name });
            for (const t of r.targets) {
              if (t.name === or_empty({ o: d.def_name })) {
                def = t.module_name + "." + t.name;
              }
            }
            const [, r$158] = push_proposal({ r: r, p: proposal_json({ kind: kind, def: def, current: { tag: "Some", value: ob.kind + " " + ob.text }, proposed: { tag: "None" }, counterexample: counterexample_json({ c: checked, d: d }), iterations: iteration, finding: { tag: "None" }, rationale: "the counterexample satisfies every precondition and violates the clause, and the model proposed the same body twice: the contract is likely wrong" }) });
            r = r$158;
            break $m417$match;
          }
          $rt.unreachable();
        }
      }
      r = { ...r, cause: { tag: "Some", value: "contract_conflict" } };
      const [, r$159] = stop({ r: r });
      r = r$159;
      return [{ tag: "Ok", value: { tag: "None" } }, r];
    }
    let $sc426 = classification === "stall";
    if ($sc426) {
      const [$r425, r$160] = escalate({ r: r });
      r = r$160;
      $sc426 = !$r425;
    }
    if ($sc426) {
      const [, r$161] = stop({ r: r });
      r = r$161;
    }
    return [{ tag: "Ok", value: { tag: "None" } }, r];
  } catch ($e) {
    if ($e instanceof $rt.EarlyReturn) return [$e.value, r];
    throw $e;
  }
}

export function run_task({ task, files, console, process, net, clock, opts }) {
  try {
    const started = $std_io.time({ clock: clock });
    let project = { root: opts.root, scope: [], paths: $std_map.dict({  }), texts: $std_map.dict({  }) };
    const $m430 = load_project({ files: files, root: opts.root, scope: task.scope });
    $m430$match: {
      if ($m430.tag === "Err") {
        const error = $m430.error;
        return { tag: "Ok", value: failure({ error: error }) };
        break $m430$match;
      }
      if ($m430.tag === "Ok") {
        const value = $m430.value;
        project = value;
        break $m430$match;
      }
      $rt.unreachable();
    }
    const work_dir = change_dir({ root: opts.root, task_id: task.id }) + "/work";
    $rt.unwrap($std_io.remove_all({ files: files, path: work_dir }));
    $rt.unwrap($std_io.mkdir({ files: files, path: work_dir }));
    let texts0 = $std_map.dict({  });
    for (const path of $std_map.keys({ d: project.texts })) {
      const [, texts0$162] = $std_map.set({ d: texts0, key: path, value: canonical_text({ path: path, text: or_empty({ o: $std_map.find({ d: project.texts, key: path }) }) }) });
      texts0 = texts0$162;
    }
    let r = { task: task, project: project, work_dir: work_dir, started: started, texts0: texts0, working: copy_texts({ d: texts0 }), baseline: $rt.unwrap(check_project({ files: files, console: console, process: process, project: project, tools: opts.tools, texts: texts0, run_tests: true, out_dir: work_dir + "/check-1" })), targets: [], trace: [], history: [], proposals: [], messages: [], tokens: 0, mechanical: 0, escalation_steps: 0, cause: { tag: "None" }, model_name: model_name({ m: opts.model }), ctx_policy: task.context_policy, full_history: false, ladder: 0, pending_escalation: { tag: "None" }, current: { tag: "None" }, grew: 0, seen: $std_map.dict({  }), seen_bodies: $std_map.dict({  }), claim_edits: 0, helper_attempts: 0, best: { tag: "None" }, checks: 1, calls: 0, done: false, green: false };
    if (r.baseline.internal_error) {
      return { tag: "Ok", value: failure({ error: "the compiler reported E0999 on the baseline; this is filed against the compiler, not the task" }) };
    }
    const $m438 = select_targets({ task: task, project: project, baseline: r.baseline, texts: texts0 });
    $m438$match: {
      if ($m438.tag === "Err") {
        const error = $m438.error;
        return { tag: "Ok", value: failure({ error: error }) };
        break $m438$match;
      }
      if ($m438.tag === "Ok") {
        const value = $m438.value;
        r = { ...r, targets: value };
        break $m438$match;
      }
      $rt.unreachable();
    }
    if (task.kind === "regenerate") {
      r = { ...r, ctx_policy: "none" };
    }
    if (task.kind === "ticket") {
      const [$r442, r$163] = ticket({ r: r, files: files, process: process, net: net, clock: clock, model: opts.model, write: opts.write });
      r = r$163;
      return $r442;
    }
    let stray = 0;
    for (const d of r.baseline.json) {
      const location = $json.at({ j: d, key: "location" });
      if (!is_target_at({ targets: r.targets, name: $json.str_at({ j: location, key: "def" }), path: $json.str_at({ j: location, key: "file" }) })) {
        stray = $rt.int.add(stray, 1, $ob70);
      }
    }
    if (stray > 0 && task.kind !== "interface_change") {
      r = { ...r, cause: { tag: "Some", value: "baseline" } };
      log({ console: console, line: "onus loop: the baseline has " + $std_int.to_text({ x: stray }) + " diagnostic(s) outside the targets" });
      const [$r446, r$164] = conclude({ r: r, files: files, status: "blocked", final: { tag: "Some", value: r.baseline }, now: $std_io.time({ clock: clock }), write: opts.write });
      r = r$164;
      return $r446;
    }
    if ($std_list.len({ xs: r.targets }) === 0) {
      const [$r448, r$165] = conclude({ r: r, files: files, status: "opened", final: { tag: "Some", value: r.baseline }, now: $std_io.time({ clock: clock }), write: opts.write });
      r = r$165;
      return $r448;
    }
    if (task.kind === "implement" || task.kind === "regenerate") {
      for (const target of r.targets) {
        const [, r$166] = elide_target({ r: r, target: target });
        r = r$166;
      }
    }
    const $hi449 = $rt.int.add(task.budget.iterations, 1, $ob71);
    for (let iteration = 1; iteration < $hi449; iteration++) {
      if (!r.done) {
        const [$r451, r$167] = iterate({ r: r, files: files, console: console, process: process, net: net, clock: clock, model: opts.model, tools: opts.tools, iteration: iteration });
        r = r$167;
        const $m450 = $r451;
        $m450$match: {
          if ($m450.tag === "Err") {
            const error = $m450.error;
            return { tag: "Err", error: error };
            break $m450$match;
          }
          if ($m450.tag === "Ok") {
            const value = $m450.value;
            if (value.tag === "Some") {
              return { tag: "Ok", value: failure({ error: or_empty({ o: value }) }) };
            }
            break $m450$match;
          }
          $rt.unreachable();
        }
      }
    }
    if (r.green) {
      const [$r454, r$168] = conclude({ r: r, files: files, status: "opened", final: r.current, now: $std_io.time({ clock: clock }), write: opts.write });
      r = r$168;
      return $r454;
    }
    if (r.cause.tag === "None") {
      r = { ...r, cause: { tag: "Some", value: "budget" } };
    }
    const [$r457, r$169] = conclude({ r: r, files: files, status: "blocked", final: r.current, now: $std_io.time({ clock: clock }), write: opts.write });
    r = r$169;
    return $r457;
  } catch ($e) {
    if ($e instanceof $rt.EarlyReturn) return $e.value;
    throw $e;
  }
}

export function elide_target({ r, target }) {
  const $m458 = $std_map.find({ d: r.working, key: target.path });
  $m458$match: {
    if ($m458.tag === "None") {
      skip({  });
      break $m458$match;
    }
    if ($m458.tag === "Some") {
      const value = $m458.value;
      const [, r$170] = elide_in({ r: r, target: target, text: value });
      r = r$170;
      break $m458$match;
    }
    $rt.unreachable();
  }
  return [undefined, r];
  return [undefined, r];
}

export function elide_in({ r, target, text }) {
  const $m459 = parse_module({ path: target.path, text: text }).module;
  $m459$match: {
    if ($m459.tag === "None") {
      skip({  });
      break $m459$match;
    }
    if ($m459.tag === "Some") {
      const value = $m459.value;
      const [, r$171] = elide_fn({ r: r, target: target, text: text, f: find_fn({ m: value, name: target.name }) });
      r = r$171;
      break $m459$match;
    }
    $rt.unreachable();
  }
  return [undefined, r];
  return [undefined, r];
}

export function elide_fn({ r, target, text, f }) {
  const $m460 = f;
  $m460$match: {
    if ($m460.tag === "None") {
      skip({  });
      break $m460$match;
    }
    if ($m460.tag === "Some") {
      const value = $m460.value;
      if (has_body({ f: value })) {
        let working = r.working;
        const [, working$172] = $std_map.set({ d: working, key: target.path, value: canonical_text({ path: target.path, text: splice_item({ text: text, span: value.span, replacement: { tag: "FnItem", decl: elide({ f: value }) } }) }) });
        working = working$172;
        r = { ...r, working: working };
      }
      break $m460$match;
    }
    $rt.unreachable();
  }
  return [undefined, r];
  return [undefined, r];
}

export function is_target_at({ targets, name, path }) {
  for (const t of targets) {
    if (t.name === name && t.path === path) {
      return true;
    }
  }
  return false;
}

