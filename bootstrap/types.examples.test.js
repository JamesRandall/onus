// Generated by onus: tests of module types.
import * as $rt from "./onus-runtime/index.js";
import * as $self from "./types.js";

import { test, expect, afterAll } from 'vitest';
import * as fc from 'fast-check';
test("equality", () => {
  expect($self.same_base({ a: $self.int_t, b: { tag: "Refined", base: $self.int_t, pred: 7, alias: { tag: "None" } } })).toBe(true);
  expect(!$self.same_base({ a: $self.int_t, b: $self.text_t })).toBe(true);
  expect($self.assignable({ actual: { tag: "ErrorT" }, expected: $self.bool_t })).toBe(true);
  expect($self.const_equals({ a: { tag: "IntV", v: 3 }, b: { tag: "IntV", v: 3 } })).toBe(true);
  expect(!$self.const_equals({ a: { tag: "IntV", v: 3 }, b: { tag: "TextV", v: "3" } })).toBe(true);
  expect($self.is_numeric({ t: { tag: "Refined", base: $self.duration_t, pred: 1, alias: { tag: "None" } } })).toBe(true);
});
afterAll(() => $rt.coverage.flush());
