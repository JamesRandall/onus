// Generated by onus from module typecheck. Do not edit.
import * as $rt from "/Users/jamesrandall/code/onus/packages/runtime/dist/index.js";
import * as $std_map from "./std/map.js";
import * as $basic from "./basic.js";
import * as $std_list from "./std/list.js";
import * as $context from "./context.js";
import * as $report from "./report.js";
import * as $defs from "./defs.js";
import * as $std_int from "./std/int.js";
import * as $std_float from "./std/float.js";
import * as $std_bool from "./std/bool.js";
import * as $std_text from "./std/text.js";
import * as $effectset from "./effectset.js";
import * as $types from "./types.js";
import * as $comments from "./comments.js";
import * as $parser from "./parser.js";
import * as $walk from "./walk.js";

const $ob1 = { kind: "overflow", text: "n - 1 within Int", at: "/Users/jamesrandall/code/onus/self/typecheck.onus:254:71", def: "pop_fn" };
const $ob2 = { kind: "overflow", text: "n - 1 within Int", at: "/Users/jamesrandall/code/onus/self/typecheck.onus:264:51", def: "fn_top" };
const $ob3 = { kind: "overflow", text: "-1 within Int", at: "/Users/jamesrandall/code/onus/self/typecheck.onus:324:39", def: "tparam_or" };
const $ob4 = { kind: "overflow", text: "-1 within Int", at: "/Users/jamesrandall/code/onus/self/typecheck.onus:346:20", def: "or_neg" };
const $ob5 = { kind: "overflow", text: "-1 within Int", at: "/Users/jamesrandall/code/onus/self/typecheck.onus:370:20", def: "res_def" };
const $ob6 = { kind: "overflow", text: "-1 within Int", at: "/Users/jamesrandall/code/onus/self/typecheck.onus:376:19", def: "res_def" };
const $ob7 = { kind: "overflow", text: "-1 within Int", at: "/Users/jamesrandall/code/onus/self/typecheck.onus:382:20", def: "res_tag" };
const $ob8 = { kind: "overflow", text: "-1 within Int", at: "/Users/jamesrandall/code/onus/self/typecheck.onus:398:20", def: "res_iface" };
const $ob9 = { kind: "overflow", text: "-1 within Int", at: "/Users/jamesrandall/code/onus/self/typecheck.onus:402:19", def: "res_iface" };
const $ob10 = { kind: "overflow", text: "-1 within Int", at: "/Users/jamesrandall/code/onus/self/typecheck.onus:413:20", def: "std_type" };
const $ob11 = { kind: "overflow", text: "fuel - 1 within Int", at: "/Users/jamesrandall/code/onus/self/typecheck.onus:565:46", def: "type_of" };
const $ob12 = { kind: "overflow", text: "fuel - 1 within Int", at: "/Users/jamesrandall/code/onus/self/typecheck.onus:591:29", def: "compute_type" };
const $ob13 = { kind: "overflow", text: "fuel - 1 within Int", at: "/Users/jamesrandall/code/onus/self/typecheck.onus:597:26", def: "compute_type" };
const $ob14 = { kind: "overflow", text: "fuel - 1 within Int", at: "/Users/jamesrandall/code/onus/self/typecheck.onus:606:13", def: "compute_type" };
const $ob15 = { kind: "overflow", text: "fuel - 1 within Int", at: "/Users/jamesrandall/code/onus/self/typecheck.onus:688:31", def: "named_base" };
const $ob16 = { kind: "overflow", text: "fuel - 1 within Int", at: "/Users/jamesrandall/code/onus/self/typecheck.onus:703:32", def: "named_base" };
const $ob17 = { kind: "overflow", text: "fuel - 1 within Int", at: "/Users/jamesrandall/code/onus/self/typecheck.onus:771:47", def: "alias_type" };
const $ob18 = { kind: "overflow", text: "fuel - 1 within Int", at: "/Users/jamesrandall/code/onus/self/typecheck.onus:829:41", def: "type_params_of" };
const $ob19 = { kind: "overflow", text: "-1 within Int", at: "/Users/jamesrandall/code/onus/self/typecheck.onus:849:10", def: "iface_param" };
const $ob20 = { kind: "overflow", text: "-1 within Int", at: "/Users/jamesrandall/code/onus/self/typecheck.onus:854:20", def: "tparam_tag" };
const $ob21 = { kind: "overflow", text: "fuel - 1 within Int", at: "/Users/jamesrandall/code/onus/self/typecheck.onus:943:39", def: "type_arg_of" };
const $ob22 = { kind: "overflow", text: "fuel - 1 within Int", at: "/Users/jamesrandall/code/onus/self/typecheck.onus:1019:11", def: "instantiate" };
const $ob23 = { kind: "overflow", text: "-1 within Int", at: "/Users/jamesrandall/code/onus/self/typecheck.onus:1031:68", def: "instantiate" };
const $ob24 = { kind: "overflow", text: "fuel - 1 within Int", at: "/Users/jamesrandall/code/onus/self/typecheck.onus:1042:17", def: "instantiate" };
const $ob25 = { kind: "overflow", text: "fuel - 1 within Int", at: "/Users/jamesrandall/code/onus/self/typecheck.onus:1054:19", def: "instantiate" };
const $ob26 = { kind: "overflow", text: "positional + 1 within Int", at: "/Users/jamesrandall/code/onus/self/typecheck.onus:1097:22", def: "instantiate" };
const $ob27 = { kind: "overflow", text: "fuel - 1 within Int", at: "/Users/jamesrandall/code/onus/self/typecheck.onus:1100:17", def: "instantiate" };
const $ob28 = { kind: "overflow", text: "positional + 1 within Int", at: "/Users/jamesrandall/code/onus/self/typecheck.onus:1109:22", def: "instantiate" };
const $ob29 = { kind: "overflow", text: "i + 1 within Int", at: "/Users/jamesrandall/code/onus/self/typecheck.onus:1892:20", def: "check_impl" };
const $ob30 = { kind: "overflow", text: "i + 1 within Int", at: "/Users/jamesrandall/code/onus/self/typecheck.onus:2367:9", def: "coverage" };
const $ob31 = { kind: "overflow", text: "i + 1 within Int", at: "/Users/jamesrandall/code/onus/self/typecheck.onus:2695:28", def: "check_pattern" };
const $ob32 = { kind: "overflow", text: "i + 1 within Int", at: "/Users/jamesrandall/code/onus/self/typecheck.onus:2708:21", def: "check_pattern" };
const $ob33 = { kind: "overflow", text: "n - 1 within Int", at: "/Users/jamesrandall/code/onus/self/typecheck.onus:2830:39", def: "it_top" };
const $ob34 = { kind: "overflow", text: "-1 within Int", at: "/Users/jamesrandall/code/onus/self/typecheck.onus:3577:18", def: "check_inout_arg" };
const $ob35 = { kind: "overflow", text: "n - 1 within Int", at: "/Users/jamesrandall/code/onus/self/typecheck.onus:4151:15", def: "recover_expr" };
const $ob36 = { kind: "overflow", text: "i + 1 within Int", at: "/Users/jamesrandall/code/onus/self/typecheck.onus:4163:11", def: "recover_expr" };
const $ob37 = { kind: "overflow", text: "fn_frame(ck: ck) + 1 within Int", at: "/Users/jamesrandall/code/onus/self/typecheck.onus:4329:22", def: "closure" };
const $ob38 = { kind: "overflow", text: "-1 within Int", at: "/Users/jamesrandall/code/onus/self/typecheck.onus:4456:25", def: "resolved_value_type" };
const $ob39 = { kind: "overflow", text: "positional + 1 within Int", at: "/Users/jamesrandall/code/onus/self/typecheck.onus:4629:24", def: "call_signature" };
const $ob40 = { kind: "overflow", text: "n - 1 within Int", at: "/Users/jamesrandall/code/onus/self/typecheck.onus:4828:73", def: "process_pending" };
const $ob41 = { kind: "overflow", text: "steps - 1 within Int", at: "/Users/jamesrandall/code/onus/self/typecheck.onus:5096:13", def: "reaches" };
const $ob42 = { kind: "overflow", text: "List.len(xs: queue) - 1 within Int", at: "/Users/jamesrandall/code/onus/self/typecheck.onus:5097:44", def: "reaches" };
const $ob43 = { kind: "overflow", text: "List.len(xs: queue) - 1 within Int", at: "/Users/jamesrandall/code/onus/self/typecheck.onus:5098:48", def: "reaches" };
export const max_type_depth = 100000;

export function new_checker($args) {
  return { current_file: 0, current_def: { tag: "None" }, current_module: { tag: "None" }, it_stack: [], fn_stack: [], type_memo: $std_map.dict({  }), env: $basic.new_env({  }), busy: $std_map.dict({  }), impls: $std_map.dict({  }), pending: $std_list.builder({  }), processed: 0 };
}

export function skip($args) {
  return undefined;
}

export function rep({ ck, ctx, code, at, detail }) {
  const [, ctx$1] = $context.report({ ctx: ctx, d: $report.diagnostic({ code: code, file: ck.current_file, span: at, def_name: ck.current_def, detail: detail }) });
  ctx = ctx$1;
  return [undefined, ctx];
  return [undefined, ctx];
}

export function key({ ck, tag, span }) {
  return $defs.node_key({ file: ck.current_file, tag: tag, span: span });
}

export function def_name({ ctx, id }) {
  return $context.get_def({ ctx: ctx, id: id }).name;
}

export function const_to_text({ ctx, c }) {
  const $m4 = c;
  $m4$match: {
    if ($m4.tag === "IntV") {
      const v = $m4.v;
      return $std_int.to_text({ x: v });
      break $m4$match;
    }
    if ($m4.tag === "FloatV") {
      const v = $m4.v;
      return $std_float.to_text({ x: v });
      break $m4$match;
    }
    if ($m4.tag === "BoolV") {
      const v = $m4.v;
      return $std_bool.to_text({ b: v });
      break $m4$match;
    }
    if ($m4.tag === "TextV") {
      const v = $m4.v;
      return printer_quote({ t: v });
      break $m4$match;
    }
    if ($m4.tag === "DurationV") {
      const v = $m4.v;
      return $std_int.to_text({ x: v }) + "ns";
      break $m4$match;
    }
    if ($m4.tag === "UnitV") {
      return "Unit";
      break $m4$match;
    }
    if ($m4.tag === "VariantV") {
      const def = $m4.def;
      return def_name({ ctx: ctx, id: def });
      break $m4$match;
    }
    if ($m4.tag === "SymV") {
      const def = $m4.def;
      return def_name({ ctx: ctx, id: def });
      break $m4$match;
    }
    if ($m4.tag === "ErrorV") {
      return "<error>";
      break $m4$match;
    }
    $rt.unreachable();
  }
}

export function printer_quote({ t }) {
  return "\"" + $std_text.replace({ t: $std_text.replace({ t: t, from: "\\", to: "\\\\" }), from: "\"", to: "\\\"" }) + "\"";
}

export function effects_to_text({ ctx, es }) {
  let names = $std_list.builder({  });
  for (const e of $effectset.values({ s: es })) {
    const $m5 = e;
    $m5$match: {
      if ($m5.tag === "PrimEffect") {
        const name = $m5.name;
        const [, names$2] = $std_list.push({ b: names, x: name });
        names = names$2;
        break $m5$match;
      }
      if ($m5.tag === "ResourceEffect") {
        const mod = $m5.mod;
        const name = $m5.name;
        const [, names$3] = $std_list.push({ b: names, x: name });
        names = names$3;
        break $m5$match;
      }
      if ($m5.tag === "ParamEffect") {
        const def = $m5.def;
        const [, names$4] = $std_list.push({ b: names, x: def_name({ ctx: ctx, id: def }) });
        names = names$4;
        break $m5$match;
      }
      $rt.unreachable();
    }
  }
  return $std_text.join({ parts: sort_texts({ xs: $std_list.finish({ b: names }) }), sep: ", " });
}

export function sort_texts({ xs }) {
  let out = [];
  for (const x of xs) {
    let inserted = false;
    let b = $std_list.builder({  });
    for (const y of out) {
      if (!inserted && $std_text.compare({ a: x, b: y }) < 0) {
        const [, b$5] = $std_list.push({ b: b, x: x });
        b = b$5;
        inserted = true;
      }
      const [, b$6] = $std_list.push({ b: b, x: y });
      b = b$6;
    }
    if (!inserted) {
      const [, b$7] = $std_list.push({ b: b, x: x });
      b = b$7;
    }
    out = $std_list.finish({ b: b });
  }
  return out;
}

export function show({ ctx, t }) {
  const $m6 = t;
  $m6$match: {
    if ($m6.tag === "Prim") {
      const name = $m6.name;
      return name;
      break $m6$match;
    }
    if ($m6.tag === "Refined") {
      const base = $m6.base;
      const alias = $m6.alias;
      const $m7 = alias;
      $m7$match: {
        if ($m7.tag === "Some") {
          const value = $m7.value;
          return def_name({ ctx: ctx, id: value });
          break $m7$match;
        }
        if ($m7.tag === "None") {
          return show({ ctx: ctx, t: base }) + " where …";
          break $m7$match;
        }
        $rt.unreachable();
      }
      break $m6$match;
    }
    if ($m6.tag === "RecordT") {
      const def = $m6.def;
      const args = $m6.args;
      return def_name({ ctx: ctx, id: def }) + show_args({ ctx: ctx, args: args });
      break $m6$match;
    }
    if ($m6.tag === "UnionT") {
      const def = $m6.def;
      const args = $m6.args;
      return def_name({ ctx: ctx, id: def }) + show_args({ ctx: ctx, args: args });
      break $m6$match;
    }
    if ($m6.tag === "Opaque") {
      const def = $m6.def;
      const args = $m6.args;
      return def_name({ ctx: ctx, id: def }) + show_args({ ctx: ctx, args: args });
      break $m6$match;
    }
    if ($m6.tag === "Capability") {
      const def = $m6.def;
      const args = $m6.args;
      const restrictions = $m6.restrictions;
      let parts = $std_list.builder({  });
      for (const a of args) {
        const [, parts$8] = $std_list.push({ b: parts, x: show_arg({ ctx: ctx, a: a }) });
        parts = parts$8;
      }
      for (const r of restrictions) {
        const [, parts$9] = $std_list.push({ b: parts, x: r.label + ": " + const_to_text({ ctx: ctx, c: r.value }) });
        parts = parts$9;
      }
      if ($std_list.built({ b: parts }) === 0) {
        return def_name({ ctx: ctx, id: def });
      }
      return def_name({ ctx: ctx, id: def }) + "[" + $std_text.join({ parts: $std_list.finish({ b: parts }), sep: ", " }) + "]";
      break $m6$match;
    }
    if ($m6.tag === "FnT") {
      const params = $m6.params;
      const ret = $m6.ret;
      const effects = $m6.effects;
      let ps = $std_list.builder({  });
      for (const p of params) {
        let head = p.name + ": ";
        if (p.is_inout) {
          head = head + "inout ";
        }
        const [, ps$10] = $std_list.push({ b: ps, x: head + show({ ctx: ctx, t: p.ty }) });
        ps = ps$10;
      }
      let eff = "";
      if ($effectset.size({ s: effects }) > 0) {
        eff = " may " + effects_to_text({ ctx: ctx, es: effects });
      }
      return "fn(" + $std_text.join({ parts: $std_list.finish({ b: ps }), sep: ", " }) + ") -> " + show({ ctx: ctx, t: ret }) + eff;
      break $m6$match;
    }
    if ($m6.tag === "ParamT") {
      const def = $m6.def;
      return def_name({ ctx: ctx, id: def });
      break $m6$match;
    }
    if ($m6.tag === "TypeInfoT") {
      return "TypeInfo";
      break $m6$match;
    }
    if ($m6.tag === "SpecT") {
      return "Spec";
      break $m6$match;
    }
    if ($m6.tag === "ErrorT") {
      return "<error>";
      break $m6$match;
    }
    $rt.unreachable();
  }
}

export function show_arg({ ctx, a }) {
  const $m8 = a;
  $m8$match: {
    if ($m8.tag === "TypeA") {
      const ty = $m8.ty;
      return show({ ctx: ctx, t: ty });
      break $m8$match;
    }
    if ($m8.tag === "ConstA") {
      const value = $m8.value;
      return const_to_text({ ctx: ctx, c: value });
      break $m8$match;
    }
    if ($m8.tag === "EffectsA") {
      const effects = $m8.effects;
      return "! " + effects_to_text({ ctx: ctx, es: effects });
      break $m8$match;
    }
    $rt.unreachable();
  }
}

export function show_args({ ctx, args }) {
  if ($std_list.len({ xs: args }) === 0) {
    return "";
  }
  let parts = $std_list.builder({  });
  for (const a of args) {
    const [, parts$11] = $std_list.push({ b: parts, x: show_arg({ ctx: ctx, a: a }) });
    parts = parts$11;
  }
  return "[" + $std_text.join({ parts: $std_list.finish({ b: parts }), sep: ", " }) + "]";
}

export function plural({ n }) {
  if (n === 1) {
    return $std_int.to_text({ x: n }) + " type argument";
  }
  return $std_int.to_text({ x: n }) + " type arguments";
}

export function push_fn({ ck, f }) {
  ck = { ...ck, fn_stack: $std_list.append({ xs: ck.fn_stack, x: f }) };
  return [undefined, ck];
  return [undefined, ck];
}

export function pop_fn({ ck }) {
  const n = $std_list.len({ xs: ck.fn_stack });
  if (n > 0) {
    ck = { ...ck, fn_stack: $std_list.slice({ xs: ck.fn_stack, from: 0, to: $rt.int.sub(n, 1, $ob1) }) };
  }
  return [undefined, ck];
  return [undefined, ck];
}

export function fn_top({ ck }) {
  const n = $std_list.len({ xs: ck.fn_stack });
  if (n === 0) {
    return { tag: "None" };
  }
  return { tag: "Some", value: $std_list.get({ xs: ck.fn_stack, i: $rt.int.sub(n, 1, $ob2) }) };
}

export function fn_ret({ ck }) {
  const $m13 = fn_top({ ck: ck });
  $m13$match: {
    if ($m13.tag === "Some") {
      const value = $m13.value;
      return value.ret;
      break $m13$match;
    }
    if ($m13.tag === "None") {
      return { tag: "None" };
      break $m13$match;
    }
    $rt.unreachable();
  }
}

export function fn_frame({ ck }) {
  const $m15 = fn_top({ ck: ck });
  $m15$match: {
    if ($m15.tag === "Some") {
      const value = $m15.value;
      return value.frame;
      break $m15$match;
    }
    if ($m15.tag === "None") {
      return 0;
      break $m15$match;
    }
    $rt.unreachable();
  }
}

export function fn_flag({ ck, which }) {
  const $m16 = fn_top({ ck: ck });
  $m16$match: {
    if ($m16.tag === "Some") {
      const value = $m16.value;
      if (which === 0) {
        return value.assertion;
      }
      if (which === 1) {
        return value.in_recover;
      }
      return value.is_verify;
      break $m16$match;
    }
    if ($m16.tag === "None") {
      return false;
      break $m16$match;
    }
    $rt.unreachable();
  }
}

export function plain_fn({ ret, frame }) {
  return { ret: ret, assertion: false, in_recover: false, frame: frame, is_verify: false };
}

export function or_error({ o }) {
  const $m18 = o;
  $m18$match: {
    if ($m18.tag === "Some") {
      const value = $m18.value;
      return value;
      break $m18$match;
    }
    if ($m18.tag === "None") {
      return { tag: "ErrorT" };
      break $m18$match;
    }
    $rt.unreachable();
  }
}

export function arg_or({ o }) {
  const $m20 = o;
  $m20$match: {
    if ($m20.tag === "Some") {
      const value = $m20.value;
      return value;
      break $m20$match;
    }
    if ($m20.tag === "None") {
      return { tag: "TypeA", ty: { tag: "ErrorT" } };
      break $m20$match;
    }
    $rt.unreachable();
  }
}

export function const_or({ o }) {
  const $m23 = o;
  $m23$match: {
    if ($m23.tag === "Some") {
      const value = $m23.value;
      return value;
      break $m23$match;
    }
    if ($m23.tag === "None") {
      return { tag: "ErrorV" };
      break $m23$match;
    }
    $rt.unreachable();
  }
}

export function ident_or({ o }) {
  const $m25 = o;
  $m25$match: {
    if ($m25.tag === "Some") {
      const value = $m25.value;
      return value;
      break $m25$match;
    }
    if ($m25.tag === "None") {
      return { text: "", span: { start: 0, end: 0 } };
      break $m25$match;
    }
    $rt.unreachable();
  }
}

export function tparam_or({ o }) {
  const $m28 = o;
  $m28$match: {
    if ($m28.tag === "Some") {
      const value = $m28.value;
      return value;
      break $m28$match;
    }
    if ($m28.tag === "None") {
      return { tag: "EffectP", def: $rt.int.neg(1, $ob3) };
      break $m28$match;
    }
    $rt.unreachable();
  }
}

export function tparam_bound({ p }) {
  const $m30 = p;
  $m30$match: {
    if ($m30.tag === "TypeP") {
      const bound = $m30.bound;
      return bound;
      break $m30$match;
    }
    if (true) {
      return { tag: "None" };
      break $m30$match;
    }
    $rt.unreachable();
  }
}

export function else_or({ o }) {
  const $m32 = o;
  $m32$match: {
    if ($m32.tag === "Some") {
      const value = $m32.value;
      return value;
      break $m32$match;
    }
    if ($m32.tag === "None") {
      return { name: { text: "_", span: { start: 0, end: 0 } }, expr: { tag: "IntLit", value: 0, text: "0", span: { start: 0, end: 0 } }, span: { start: 0, end: 0 } };
      break $m32$match;
    }
    $rt.unreachable();
  }
}

export function or_neg({ o }) {
  const $m39 = o;
  $m39$match: {
    if ($m39.tag === "Some") {
      const value = $m39.value;
      return value;
      break $m39$match;
    }
    if ($m39.tag === "None") {
      return $rt.int.neg(1, $ob4);
      break $m39$match;
    }
    $rt.unreachable();
  }
}

export function decl_type({ ctx, def }) {
  return or_error({ o: $std_map.find({ d: ctx.decl_types, key: def }) });
}

export function set_decl_type({ ctx, def, t }) {
  let table = ctx.decl_types;
  const [, table$12] = $std_map.set({ d: table, key: def, value: t });
  table = table$12;
  return [undefined, ctx];
  return [undefined, ctx];
}

export function def_of_key({ ctx, k }) {
  return or_neg({ o: $std_map.find({ d: ctx.def_of, key: k }) });
}

export function ref_at({ ctx, k }) {
  return $std_map.find({ d: ctx.refs, key: k });
}

export function res_def({ res }) {
  const $m40 = res;
  $m40$match: {
    if ($m40.tag === "None") {
      return $rt.int.neg(1, $ob5);
      break $m40$match;
    }
    if ($m40.tag === "Some") {
      const value = $m40.value;
      const $m41 = value;
      $m41$match: {
        if ($m41.tag === "DefRes") {
          const def = $m41.def;
          return def;
          break $m41$match;
        }
        if ($m41.tag === "CompanionRes") {
          const owner = $m41.owner;
          const fn_def = $m41.fn_def;
          return fn_def;
          break $m41$match;
        }
        if ($m41.tag === "IfaceFnRes") {
          const iface = $m41.iface;
          const fn_def = $m41.fn_def;
          return fn_def;
          break $m41$match;
        }
        if (true) {
          return $rt.int.neg(1, $ob6);
          break $m41$match;
        }
        $rt.unreachable();
      }
      break $m40$match;
    }
    $rt.unreachable();
  }
}

export function res_tag({ res }) {
  const $m42 = res;
  $m42$match: {
    if ($m42.tag === "None") {
      return $rt.int.neg(1, $ob7);
      break $m42$match;
    }
    if ($m42.tag === "Some") {
      const value = $m42.value;
      const $m43 = value;
      $m43$match: {
        if ($m43.tag === "DefRes") {
          const def = $m43.def;
          return 0;
          break $m43$match;
        }
        if ($m43.tag === "PrimRes") {
          const name = $m43.name;
          return 1;
          break $m43$match;
        }
        if ($m43.tag === "UnitRes") {
          return 2;
          break $m43$match;
        }
        if ($m43.tag === "ModuleRes") {
          const mod = $m43.mod;
          return 3;
          break $m43$match;
        }
        if ($m43.tag === "TypeValueRes") {
          const owner = $m43.owner;
          return 4;
          break $m43$match;
        }
        if ($m43.tag === "CompanionRes") {
          const owner = $m43.owner;
          const fn_def = $m43.fn_def;
          return 5;
          break $m43$match;
        }
        if ($m43.tag === "IfaceFnRes") {
          const iface = $m43.iface;
          const fn_def = $m43.fn_def;
          return 6;
          break $m43$match;
        }
        if ($m43.tag === "EffectRes") {
          const effect = $m43.effect;
          return 7;
          break $m43$match;
        }
        $rt.unreachable();
      }
      break $m42$match;
    }
    $rt.unreachable();
  }
}

export function res_iface({ res }) {
  const $m44 = res;
  $m44$match: {
    if ($m44.tag === "None") {
      return $rt.int.neg(1, $ob8);
      break $m44$match;
    }
    if ($m44.tag === "Some") {
      const value = $m44.value;
      const $m45 = value;
      $m45$match: {
        if ($m45.tag === "IfaceFnRes") {
          const iface = $m45.iface;
          const fn_def = $m45.fn_def;
          return iface;
          break $m45$match;
        }
        if (true) {
          return $rt.int.neg(1, $ob9);
          break $m45$match;
        }
        $rt.unreachable();
      }
      break $m44$match;
    }
    $rt.unreachable();
  }
}

export function std_type({ ctx, module_name, type_name }) {
  const $m46 = $std_map.find({ d: ctx.by_name, key: module_name });
  $m46$match: {
    if ($m46.tag === "None") {
      return [$rt.int.neg(1, $ob10), ctx];
      break $m46$match;
    }
    if ($m46.tag === "Some") {
      const value = $m46.value;
      const [$r47, ctx$13] = $context.members_of({ ctx: ctx, module: value });
      ctx = ctx$13;
      return [or_neg({ o: $std_map.find({ d: $r47.types, key: type_name }) }), ctx];
      break $m46$match;
    }
    $rt.unreachable();
  }
}

export function list_of({ ctx, elem }) {
  const [$r48, ctx$14] = std_type({ ctx: ctx, module_name: "std.list", type_name: "List" });
  ctx = ctx$14;
  const def = $r48;
  if (def < 0) {
    return [{ tag: "ErrorT" }, ctx];
  }
  return [{ tag: "Opaque", def: def, args: [{ tag: "TypeA", ty: elem }] }, ctx];
}

export function result_of({ ctx, value, error }) {
  const [$r52, ctx$15] = std_type({ ctx: ctx, module_name: "std.results", type_name: "Result" });
  ctx = ctx$15;
  const def = $r52;
  if (def < 0) {
    return [{ tag: "ErrorT" }, ctx];
  }
  return [{ tag: "UnionT", def: def, args: [{ tag: "TypeA", ty: value }, { tag: "TypeA", ty: error }] }, ctx];
}

export function panicked_type({ ctx }) {
  const [$r57, ctx$16] = std_type({ ctx: ctx, module_name: "std.results", type_name: "Panicked" });
  ctx = ctx$16;
  const def = $r57;
  if (def < 0) {
    return [{ tag: "ErrorT" }, ctx];
  }
  return [{ tag: "RecordT", def: def, args: [] }, ctx];
}

export function is_list_type({ ctx, t }) {
  const [$r60, ctx$17] = std_type({ ctx: ctx, module_name: "std.list", type_name: "List" });
  ctx = ctx$17;
  return [t.tag === "Opaque" && $types.def_of({ t: t }) === $r60, ctx];
}

export function first_type_arg({ t }) {
  const args = $types.args_of({ t: t });
  if ($std_list.len({ xs: args }) === 0) {
    return { tag: "None" };
  }
  const a = $std_list.get({ xs: args, i: 0 });
  if (a.tag === "TypeA") {
    return { tag: "Some", value: $types.arg_type({ a: a }) };
  }
  return { tag: "None" };
}

export function second_type_arg({ t }) {
  const args = $types.args_of({ t: t });
  if ($std_list.len({ xs: args }) < 2) {
    return { tag: "None" };
  }
  const a = $std_list.get({ xs: args, i: 1 });
  if (a.tag === "TypeA") {
    return { tag: "Some", value: $types.arg_type({ a: a }) };
  }
  return { tag: "None" };
}

export function fallible_value_or({ o, dflt }) {
  const $m67 = o;
  $m67$match: {
    if ($m67.tag === "Some") {
      const value = $m67.value;
      return value.value;
      break $m67$match;
    }
    if ($m67.tag === "None") {
      return dflt;
      break $m67$match;
    }
    $rt.unreachable();
  }
}

export function unwrap_fallible({ ctx, t }) {
  const s = $types.strip({ t: t });
  if (!(s.tag === "UnionT")) {
    return [{ tag: "None" }, ctx];
  }
  const a0 = first_type_arg({ t: s });
  if (a0.tag === "None") {
    return [{ tag: "None" }, ctx];
  }
  const [$r70, ctx$18] = std_type({ ctx: ctx, module_name: "std.results", type_name: "Result" });
  ctx = ctx$18;
  if ($types.def_of({ t: s }) === $r70) {
    const a1 = second_type_arg({ t: s });
    if (a1.tag === "None") {
      return [{ tag: "None" }, ctx];
    }
    return [{ tag: "Some", value: { value: or_error({ o: a0 }), error: or_error({ o: a1 }), is_option: false } }, ctx];
  }
  const [$r74, ctx$19] = std_type({ ctx: ctx, module_name: "std.option", type_name: "Option" });
  ctx = ctx$19;
  if ($types.def_of({ t: s }) === $r74) {
    return [{ tag: "Some", value: { value: or_error({ o: a0 }), error: $types.unit_t, is_option: true } }, ctx];
  }
  return [{ tag: "None" }, ctx];
}

export function effect_set_of({ ctx, file, refs }) {
  let out = $std_list.builder({  });
  for (const r of refs) {
    const $m78 = $std_map.find({ d: ctx.refs, key: $defs.node_key({ file: file, tag: $defs.tag_sig, span: r.span }) });
    $m78$match: {
      if ($m78.tag === "Some") {
        const value = $m78.value;
        const $m79 = value;
        $m79$match: {
          if ($m79.tag === "EffectRes") {
            const effect = $m79.effect;
            const [, out$20] = $std_list.push({ b: out, x: effect });
            out = out$20;
            break $m79$match;
          }
          if (true) {
            skip({  });
            break $m79$match;
          }
          $rt.unreachable();
        }
        break $m78$match;
      }
      if ($m78.tag === "None") {
        skip({  });
        break $m78$match;
      }
      $rt.unreachable();
    }
  }
  return $effectset.of_list({ effects: $std_list.finish({ b: out }) });
}

export function type_of({ fuel, ck, ctx, t }) {
  if (fuel === 0) {
    return [{ tag: "ErrorT" }, ck, ctx];
  }
  const k = key({ ck: ck, tag: $defs.tag_type, span: $comments.type_span({ t: t }) });
  const $m81 = $std_map.find({ d: ck.type_memo, key: k });
  $m81$match: {
    if ($m81.tag === "Some") {
      const value = $m81.value;
      return [value, ck, ctx];
      break $m81$match;
    }
    if ($m81.tag === "None") {
      skip({  });
      break $m81$match;
    }
    $rt.unreachable();
  }
  const [$r82, ck$21, ctx$21] = compute_type({ fuel: $rt.int.sub(fuel, 1, $ob11), ck: ck, ctx: ctx, t: t });
  ck = ck$21;
  ctx = ctx$21;
  const res_t = $r82;
  let memo = ck.type_memo;
  const [, memo$22] = $std_map.set({ d: memo, key: k, value: res_t });
  memo = memo$22;
  return [res_t, ck, ctx];
}

export function compute_type({ fuel, ck, ctx, t }) {
  if (fuel === 0) {
    return [{ tag: "ErrorT" }, ck, ctx];
  }
  const $m84 = t;
  $m84$match: {
    if ($m84.tag === "FnType") {
      const params = $m84.params;
      const ret = $m84.ret;
      const effects = $m84.effects;
      const span = $m84.span;
      let ps = $std_list.builder({  });
      for (const p of params) {
        const [$r85, ck$23, ctx$23] = type_of({ fuel: $rt.int.sub(fuel, 1, $ob12), ck: ck, ctx: ctx, t: p.ty });
        ck = ck$23;
        ctx = ctx$23;
        const [, ps$24] = $std_list.push({ b: ps, x: { name: p.name.text, is_inout: p.is_inout, ty: $r85 } });
        ps = ps$24;
      }
      const [$r87, ck$25, ctx$25] = type_of({ fuel: $rt.int.sub(fuel, 1, $ob13), ck: ck, ctx: ctx, t: ret });
      ck = ck$25;
      ctx = ctx$25;
      return [{ tag: "FnT", params: $std_list.finish({ b: ps }), ret: $r87, effects: effect_set_of({ ctx: ctx, file: ck.current_file, refs: effects }) }, ck, ctx];
      break $m84$match;
    }
    if ($m84.tag === "NamedType") {
      const name = $m84.name;
      const args = $m84.args;
      const where_ = $m84.where_;
      const span = $m84.span;
      let base = { tag: "ErrorT" };
      const $m90 = ref_at({ ctx: ctx, k: key({ ck: ck, tag: $defs.tag_type, span: span }) });
      $m90$match: {
        if ($m90.tag === "None") {
          base = { tag: "ErrorT" };
          break $m90$match;
        }
        if ($m90.tag === "Some") {
          const value = $m90.value;
          const [$r92, ck$26, ctx$26] = named_base({ fuel: $rt.int.sub(fuel, 1, $ob14), ck: ck, ctx: ctx, res: value, q: name, args: args, at: span });
          ck = ck$26;
          ctx = ctx$26;
          base = $r92;
          break $m90$match;
        }
        $rt.unreachable();
      }
      const $m93 = where_;
      $m93$match: {
        if ($m93.tag === "None") {
          return [base, ck, ctx];
          break $m93$match;
        }
        if ($m93.tag === "Some") {
          const value = $m93.value;
          let q = ck.pending;
          let wheres = ctx.where_exprs;
          const [, wheres$27] = $std_map.set({ d: wheres, key: key({ ck: ck, tag: $defs.tag_expr, span: $parser.span_of_expr({ e: value }) }), value: value });
          wheres = wheres$27;
          const [, q$28] = $std_list.push({ b: q, x: { file: ck.current_file, def_name: ck.current_def, pred: value, base: base, fn_ctx: fn_top({ ck: ck }) } });
          q = q$28;
          return [{ tag: "Refined", base: base, pred: key({ ck: ck, tag: $defs.tag_expr, span: $parser.span_of_expr({ e: value }) }), alias: { tag: "None" } }, ck, ctx];
          break $m93$match;
        }
        $rt.unreachable();
      }
      break $m84$match;
    }
    $rt.unreachable();
  }
}

export function named_base({ fuel, ck, ctx, res, q, args, at }) {
  if (fuel === 0) {
    return [{ tag: "ErrorT" }, ck, ctx];
  }
  const $m98 = res;
  $m98$match: {
    if ($m98.tag === "PrimRes") {
      const name = $m98.name;
      if ($std_list.len({ xs: args }) > 0) {
        const [, ctx$29] = rep({ ck: ck, ctx: ctx, code: "E0324", at: at, detail: "`" + name + "` takes no type arguments" });
        ctx = ctx$29;
      }
      if (name === "TypeInfo") {
        return [{ tag: "TypeInfoT" }, ck, ctx];
      }
      if (name === "Spec") {
        return [{ tag: "SpecT" }, ck, ctx];
      }
      return [{ tag: "Prim", name: name }, ck, ctx];
      break $m98$match;
    }
    if ($m98.tag === "DefRes") {
      const def = $m98.def;
      const d = $context.get_def({ ctx: ctx, id: def });
      if ($rt.eq(d.kind, { tag: "Alias" })) {
        if ($std_list.len({ xs: args }) > 0) {
          const [, ctx$30] = rep({ ck: ck, ctx: ctx, code: "E0324", at: at, detail: "`" + d.name + "` is a type alias and takes no type arguments" });
          ctx = ctx$30;
        }
        const [$r103, ck$31, ctx$31] = alias_type({ fuel: $rt.int.sub(fuel, 1, $ob15), ck: ck, ctx: ctx, d: d });
        ck = ck$31;
        ctx = ctx$31;
        return [$r103, ck, ctx];
      }
      if ($rt.eq(d.kind, { tag: "TypeParam" })) {
        if ($std_list.len({ xs: args }) > 0) {
          const [, ctx$32] = rep({ ck: ck, ctx: ctx, code: "E0324", at: at, detail: "type parameter `" + d.name + "` takes no type arguments" });
          ctx = ctx$32;
        }
        return [{ tag: "ParamT", def: def }, ck, ctx];
      }
      if ($rt.eq(d.kind, { tag: "Record" }) || $rt.eq(d.kind, { tag: "Union" }) || $rt.eq(d.kind, { tag: "IntrinsicType" }) || $rt.eq(d.kind, { tag: "Capability" })) {
        const [$r110, ck$33, ctx$33] = instantiate({ fuel: $rt.int.sub(fuel, 1, $ob16), ck: ck, ctx: ctx, d: d, args: args, at: at });
        ck = ck$33;
        ctx = ctx$33;
        return [$r110, ck, ctx];
      }
      if ($rt.eq(d.kind, { tag: "Interface" })) {
        const [, ctx$34] = rep({ ck: ck, ctx: ctx, code: "E0106", at: q.span, detail: "`" + d.name + "` is an interface; write it as a bound (`[T: " + d.name + "]`), not a type" });
        ctx = ctx$34;
        return [{ tag: "ErrorT" }, ck, ctx];
      }
      if ($rt.eq(d.kind, { tag: "Variant" })) {
        const [, ctx$35] = rep({ ck: ck, ctx: ctx, code: "E0106", at: q.span, detail: "`" + d.name + "` is a variant, not a type" });
        ctx = ctx$35;
        return [{ tag: "ErrorT" }, ck, ctx];
      }
      return [{ tag: "ErrorT" }, ck, ctx];
      break $m98$match;
    }
    if (true) {
      return [{ tag: "ErrorT" }, ck, ctx];
      break $m98$match;
    }
    $rt.unreachable();
  }
}

export function with_alias({ t, alias }) {
  const $m117 = t;
  $m117$match: {
    if ($m117.tag === "Refined") {
      const base = $m117.base;
      const pred = $m117.pred;
      return { tag: "Refined", base: base, pred: pred, alias: { tag: "Some", value: alias } };
      break $m117$match;
    }
    if (true) {
      return t;
      break $m117$match;
    }
    $rt.unreachable();
  }
}

export function alias_type({ fuel, ck, ctx, d }) {
  if (fuel === 0) {
    return [{ tag: "ErrorT" }, ck, ctx];
  }
  const $m121 = $std_map.find({ d: ctx.type_aliases, key: d.id });
  $m121$match: {
    if ($m121.tag === "Some") {
      const value = $m121.value;
      return [value, ck, ctx];
      break $m121$match;
    }
    if ($m121.tag === "None") {
      skip({  });
      break $m121$match;
    }
    $rt.unreachable();
  }
  const saved_file = ck.current_file;
  ck = { ...ck, current_file: d.file };
  if ($std_map.contains({ d: ck.busy, key: d.id })) {
    const [, ctx$36] = rep({ ck: ck, ctx: ctx, code: "E0106", at: d.span, detail: "type alias `" + d.name + "` refers to itself" });
    ctx = ctx$36;
    ck = { ...ck, current_file: saved_file };
    return [{ tag: "ErrorT" }, ck, ctx];
  }
  let busy = ck.busy;
  const [, busy$37] = $std_map.set({ d: busy, key: d.id, value: true });
  busy = busy$37;
  let t = { tag: "ErrorT" };
  const $m126 = d.node;
  $m126$match: {
    if ($m126.tag === "AliasNode") {
      const decl = $m126.decl;
      const [$r127, ck$38, ctx$38] = type_of({ fuel: $rt.int.sub(fuel, 1, $ob17), ck: ck, ctx: ctx, t: decl.ty });
      ck = ck$38;
      ctx = ctx$38;
      t = $r127;
      break $m126$match;
    }
    if (true) {
      t = { tag: "ErrorT" };
      break $m126$match;
    }
    $rt.unreachable();
  }
  t = with_alias({ t: t, alias: d.id });
  const [, busy$39] = $std_map.remove({ d: busy, key: d.id });
  busy = busy$39;
  let table = ctx.type_aliases;
  const [, table$40] = $std_map.set({ d: table, key: d.id, value: t });
  table = table$40;
  ck = { ...ck, current_file: saved_file };
  return [t, ck, ctx];
}

export function tparams_of_node({ node }) {
  const $m130 = node;
  $m130$match: {
    if ($m130.tag === "FnNode") {
      const decl = $m130.decl;
      return decl.tparams;
      break $m130$match;
    }
    if ($m130.tag === "RecordNode") {
      const decl = $m130.decl;
      return decl.tparams;
      break $m130$match;
    }
    if ($m130.tag === "UnionNode") {
      const decl = $m130.decl;
      return decl.tparams;
      break $m130$match;
    }
    if ($m130.tag === "IntrinsicTypeNode") {
      const decl = $m130.decl;
      return decl.tparams;
      break $m130$match;
    }
    if ($m130.tag === "CapabilityNode") {
      const decl = $m130.decl;
      return decl.tparams;
      break $m130$match;
    }
    if (true) {
      return [];
      break $m130$match;
    }
    $rt.unreachable();
  }
}

export function tparam_span({ p }) {
  const $m131 = p;
  $m131$match: {
    if ($m131.tag === "TypeParam") {
      const span = $m131.span;
      return span;
      break $m131$match;
    }
    if ($m131.tag === "ConstParam") {
      const span = $m131.span;
      return span;
      break $m131$match;
    }
    if ($m131.tag === "EffectParam") {
      const span = $m131.span;
      return span;
      break $m131$match;
    }
    $rt.unreachable();
  }
}

export function type_params_of({ fuel, ck, ctx, d }) {
  if (fuel === 0) {
    return [[], ck, ctx];
  }
  const $m132 = $std_map.find({ d: ctx.type_params, key: d.id });
  $m132$match: {
    if ($m132.tag === "Some") {
      const value = $m132.value;
      return [value, ck, ctx];
      break $m132$match;
    }
    if ($m132.tag === "None") {
      skip({  });
      break $m132$match;
    }
    $rt.unreachable();
  }
  let table = ctx.type_params;
  const [, table$41] = $std_map.set({ d: table, key: d.id, value: [] });
  table = table$41;
  const saved_file = ck.current_file;
  ck = { ...ck, current_file: d.file };
  let out = $std_list.builder({  });
  for (const p of tparams_of_node({ node: d.node })) {
    const pd = def_of_key({ ctx: ctx, k: key({ ck: ck, tag: $defs.tag_sig, span: tparam_span({ p: p }) }) });
    const $m134 = p;
    $m134$match: {
      if ($m134.tag === "TypeParam") {
        const name = $m134.name;
        const bound = $m134.bound;
        const span = $m134.span;
        const b = res_def({ res: ref_at({ ctx: ctx, k: key({ ck: ck, tag: $defs.tag_sig, span: span }) }) });
        let bound_def = { tag: "None" };
        if (b >= 0) {
          bound_def = { tag: "Some", value: b };
        }
        const [, out$42] = $std_list.push({ b: out, x: { tag: "TypeP", def: pd, bound: bound_def } });
        out = out$42;
        break $m134$match;
      }
      if ($m134.tag === "ConstParam") {
        const name = $m134.name;
        const ty = $m134.ty;
        const span = $m134.span;
        const [$r138, ck$43, ctx$43] = type_of({ fuel: $rt.int.sub(fuel, 1, $ob18), ck: ck, ctx: ctx, t: ty });
        ck = ck$43;
        ctx = ctx$43;
        const t = $r138;
        const [, ctx$44] = set_decl_type({ ctx: ctx, def: pd, t: t });
        ctx = ctx$44;
        const [, out$45] = $std_list.push({ b: out, x: { tag: "ConstP", def: pd, ty: t } });
        out = out$45;
        break $m134$match;
      }
      if ($m134.tag === "EffectParam") {
        const name = $m134.name;
        const span = $m134.span;
        const [, out$46] = $std_list.push({ b: out, x: { tag: "EffectP", def: pd } });
        out = out$46;
        break $m134$match;
      }
      $rt.unreachable();
    }
  }
  const res_t = $std_list.finish({ b: out });
  const [, table$47] = $std_map.set({ d: table, key: d.id, value: res_t });
  table = table$47;
  ck = { ...ck, current_file: saved_file };
  return [res_t, ck, ctx];
}

export function iface_param({ ctx, iface }) {
  const $hi142 = $context.def_count({ ctx: ctx });
  for (let i = 0; i < $hi142; i++) {
    const d = $context.get_def({ ctx: ctx, id: i });
    if ($rt.eq(d.kind, { tag: "TypeParam" }) && $rt.eq(d.parent, { tag: "Some", value: iface })) {
      return i;
    }
  }
  return $rt.int.neg(1, $ob19);
}

export function tparam_tag({ p }) {
  const $m145 = p;
  $m145$match: {
    if ($m145.tag === "None") {
      return $rt.int.neg(1, $ob20);
      break $m145$match;
    }
    if ($m145.tag === "Some") {
      const value = $m145.value;
      const $m146 = value;
      $m146$match: {
        if ($m146.tag === "TypeP") {
          return 0;
          break $m146$match;
        }
        if ($m146.tag === "ConstP") {
          return 1;
          break $m146$match;
        }
        if ($m146.tag === "EffectP") {
          return 2;
          break $m146$match;
        }
        $rt.unreachable();
      }
      break $m145$match;
    }
    $rt.unreachable();
  }
}

export function tparam_name({ ctx, p }) {
  const $m147 = p;
  $m147$match: {
    if ($m147.tag === "None") {
      return "";
      break $m147$match;
    }
    if ($m147.tag === "Some") {
      const value = $m147.value;
      return def_name({ ctx: ctx, id: $types.tparam_def({ p: value }) });
      break $m147$match;
    }
    $rt.unreachable();
  }
}

export function tparam_type({ p }) {
  const $m148 = p;
  $m148$match: {
    if ($m148.tag === "None") {
      return { tag: "ErrorT" };
      break $m148$match;
    }
    if ($m148.tag === "Some") {
      const value = $m148.value;
      const $m150 = value;
      $m150$match: {
        if ($m150.tag === "ConstP") {
          const ty = $m150.ty;
          return ty;
          break $m150$match;
        }
        if (true) {
          return { tag: "ErrorT" };
          break $m150$match;
        }
        $rt.unreachable();
      }
      break $m148$match;
    }
    $rt.unreachable();
  }
}

export function type_arg_label({ a }) {
  const $m152 = a;
  $m152$match: {
    if ($m152.tag === "TypeArgType") {
      const label = $m152.label;
      return label;
      break $m152$match;
    }
    if ($m152.tag === "TypeArgConst") {
      const label = $m152.label;
      return label;
      break $m152$match;
    }
    $rt.unreachable();
  }
}

export function type_arg_span({ a }) {
  const $m153 = a;
  $m153$match: {
    if ($m153.tag === "TypeArgType") {
      const span = $m153.span;
      return span;
      break $m153$match;
    }
    if ($m153.tag === "TypeArgConst") {
      const span = $m153.span;
      return span;
      break $m153$match;
    }
    $rt.unreachable();
  }
}

export function eval_const({ ck, ctx, e }) {
  return $basic.eval_({ fuel: $basic.max_depth, ctx: ctx, env: ck.env, e: e, file: ck.current_file });
}

export function const_type({ ck, ctx, v }) {
  const $m154 = v;
  $m154$match: {
    if ($m154.tag === "IntV") {
      return { tag: "Some", value: $types.int_t };
      break $m154$match;
    }
    if ($m154.tag === "FloatV") {
      return { tag: "Some", value: $types.float_t };
      break $m154$match;
    }
    if ($m154.tag === "BoolV") {
      return { tag: "Some", value: $types.bool_t };
      break $m154$match;
    }
    if ($m154.tag === "TextV") {
      return { tag: "Some", value: $types.text_t };
      break $m154$match;
    }
    if ($m154.tag === "DurationV") {
      return { tag: "Some", value: $types.duration_t };
      break $m154$match;
    }
    if ($m154.tag === "UnitV") {
      return { tag: "Some", value: $types.unit_t };
      break $m154$match;
    }
    if ($m154.tag === "VariantV") {
      const def = $m154.def;
      const $m161 = $context.get_def({ ctx: ctx, id: def }).parent;
      $m161$match: {
        if ($m161.tag === "None") {
          return { tag: "None" };
          break $m161$match;
        }
        if ($m161.tag === "Some") {
          const value = $m161.value;
          return { tag: "Some", value: { tag: "UnionT", def: value, args: [] } };
          break $m161$match;
        }
        $rt.unreachable();
      }
      break $m154$match;
    }
    if ($m154.tag === "SymV") {
      const def = $m154.def;
      return $std_map.find({ d: ctx.decl_types, key: def });
      break $m154$match;
    }
    if ($m154.tag === "ErrorV") {
      return { tag: "None" };
      break $m154$match;
    }
    $rt.unreachable();
  }
}

export function type_arg_of({ fuel, ck, ctx, a, param }) {
  if (fuel === 0) {
    return [{ tag: "None" }, ck, ctx];
  }
  const $m167 = a;
  $m167$match: {
    if ($m167.tag === "TypeArgType") {
      const label = $m167.label;
      const ty = $m167.ty;
      const span = $m167.span;
      if (ty.tag === "NamedType") {
        const v = res_def({ res: ref_at({ ctx: ctx, k: key({ ck: ck, tag: $defs.tag_type, span: $comments.type_span({ t: ty }) }) }) });
        if (v >= 0 && $rt.eq($context.get_def({ ctx: ctx, id: v }).kind, { tag: "Variant" })) {
          return [{ tag: "Some", value: { tag: "ConstA", value: { tag: "VariantV", def: v } } }, ck, ctx];
        }
      }
      const [$r172, ck$48, ctx$48] = type_of({ fuel: $rt.int.sub(fuel, 1, $ob21), ck: ck, ctx: ctx, t: ty });
      ck = ck$48;
      ctx = ctx$48;
      const t = $r172;
      if (tparam_tag({ p: param }) === 1) {
        const [, ctx$49] = rep({ ck: ck, ctx: ctx, code: "E0324", at: span, detail: "`" + tparam_name({ ctx: ctx, p: param }) + "` is a const parameter; a type was given" });
        ctx = ctx$49;
        return [{ tag: "None" }, ck, ctx];
      }
      return [{ tag: "Some", value: { tag: "TypeA", ty: t } }, ck, ctx];
      break $m167$match;
    }
    if ($m167.tag === "TypeArgConst") {
      const label = $m167.label;
      const expr = $m167.expr;
      const span = $m167.span;
      const $m176 = eval_const({ ck: ck, ctx: ctx, e: expr });
      $m176$match: {
        if ($m176.tag === "None") {
          const [, ctx$50] = rep({ ck: ck, ctx: ctx, code: "E0337", at: span, detail: "a type index must be a literal, a `const`, or a parameter" });
          ctx = ctx$50;
          return [{ tag: "Some", value: { tag: "ConstA", value: { tag: "ErrorV" } } }, ck, ctx];
          break $m176$match;
        }
        if ($m176.tag === "Some") {
          const value = $m176.value;
          if (tparam_tag({ p: param }) === 0) {
            const [, ctx$51] = rep({ ck: ck, ctx: ctx, code: "E0324", at: span, detail: "`" + tparam_name({ ctx: ctx, p: param }) + "` is a type parameter; a value was given" });
            ctx = ctx$51;
            return [{ tag: "None" }, ck, ctx];
          }
          if (tparam_tag({ p: param }) === 1) {
            const vt = const_type({ ck: ck, ctx: ctx, v: value });
            if (vt.tag === "Some" && !$types.assignable({ actual: or_error({ o: vt }), expected: tparam_type({ p: param }) })) {
              const [, ctx$52] = rep({ ck: ck, ctx: ctx, code: "E0321", at: span, detail: "index `" + tparam_name({ ctx: ctx, p: param }) + "` has type " + show({ ctx: ctx, t: tparam_type({ p: param }) }) + ", but this value has type " + show({ ctx: ctx, t: or_error({ o: vt }) }) });
              ctx = ctx$52;
            }
          }
          return [{ tag: "Some", value: { tag: "ConstA", value: value } }, ck, ctx];
          break $m176$match;
        }
        $rt.unreachable();
      }
      break $m167$match;
    }
    $rt.unreachable();
  }
}

export function instantiate({ fuel, ck, ctx, d, args, at }) {
  if (fuel === 0) {
    return [{ tag: "ErrorT" }, ck, ctx];
  }
  const [$r184, ck$53, ctx$53] = type_params_of({ fuel: $rt.int.sub(fuel, 1, $ob22), ck: ck, ctx: ctx, d: d });
  ck = ck$53;
  ctx = ctx$53;
  const tparams = $r184;
  const n = $std_list.len({ xs: tparams });
  let out = $std_map.dict({  });
  let restrictions = $std_list.builder({  });
  let positional = 0;
  for (const a of args) {
    const $m185 = type_arg_label({ a: a });
    $m185$match: {
      if ($m185.tag === "Some") {
        const value = $m185.value;
        let idx = $rt.int.neg(1, $ob23);
        for (let i = 0; i < n; i++) {
          if (idx < 0 && def_name({ ctx: ctx, id: $types.tparam_def({ p: $std_list.get({ xs: tparams, i: i }) }) }) === value.text) {
            idx = i;
          }
        }
        if (idx >= 0) {
          const [$r187, ck$54, ctx$54] = type_arg_of({ fuel: $rt.int.sub(fuel, 1, $ob24), ck: ck, ctx: ctx, a: a, param: { tag: "Some", value: $std_list.get({ xs: tparams, i: idx }) } });
          ck = ck$54;
          ctx = ctx$54;
          const v = $r187;
          if (v.tag === "Some") {
            const [, out$55] = $std_map.set({ d: out, key: idx, value: arg_or({ o: v }) });
            out = out$55;
          }
        } else {
          if ($rt.eq(d.kind, { tag: "Capability" })) {
            const [$r190, ck$56, ctx$56] = type_arg_of({ fuel: $rt.int.sub(fuel, 1, $ob25), ck: ck, ctx: ctx, a: a, param: { tag: "None" } });
            ck = ck$56;
            ctx = ctx$56;
            const v = $r190;
            if (v.tag === "Some") {
              const va = arg_or({ o: v });
              if (va.tag === "ConstA") {
                const [, restrictions$57] = $std_list.push({ b: restrictions, x: { label: value.text, value: $types.arg_const({ a: va }) } });
                restrictions = restrictions$57;
              } else {
                const [, ctx$58] = rep({ ck: ck, ctx: ctx, code: "E0324", at: type_arg_span({ a: a }), detail: "a capability restriction must be a constant value" });
                ctx = ctx$58;
              }
            }
          } else {
            const [, ctx$59] = rep({ ck: ck, ctx: ctx, code: "E0324", at: type_arg_span({ a: a }), detail: "`" + d.name + "` has no type parameter `" + value.text + "`" });
            ctx = ctx$59;
          }
        }
        break $m185$match;
      }
      if ($m185.tag === "None") {
        if (positional >= n) {
          const [, ctx$60] = rep({ ck: ck, ctx: ctx, code: "E0324", at: type_arg_span({ a: a }), detail: "`" + d.name + "` takes " + plural({ n: n }) });
          ctx = ctx$60;
          positional = $rt.int.add(positional, 1, $ob26);
        } else {
          const [$r193, ck$61, ctx$61] = type_arg_of({ fuel: $rt.int.sub(fuel, 1, $ob27), ck: ck, ctx: ctx, a: a, param: { tag: "Some", value: $std_list.get({ xs: tparams, i: positional }) } });
          ck = ck$61;
          ctx = ctx$61;
          const v = $r193;
          if (v.tag === "Some") {
            const [, out$62] = $std_map.set({ d: out, key: positional, value: arg_or({ o: v }) });
            out = out$62;
          }
          positional = $rt.int.add(positional, 1, $ob28);
        }
        break $m185$match;
      }
      $rt.unreachable();
    }
  }
  let filled = $std_list.builder({  });
  let missing = false;
  for (let i = 0; i < n; i++) {
    const $m194 = $std_map.find({ d: out, key: i });
    $m194$match: {
      if ($m194.tag === "Some") {
        const value = $m194.value;
        const [, filled$63] = $std_list.push({ b: filled, x: value });
        filled = filled$63;
        break $m194$match;
      }
      if ($m194.tag === "None") {
        missing = true;
        if (tparam_tag({ p: { tag: "Some", value: $std_list.get({ xs: tparams, i: i }) } }) === 0) {
          const [, filled$64] = $std_list.push({ b: filled, x: { tag: "TypeA", ty: { tag: "ErrorT" } } });
          filled = filled$64;
        } else {
          const [, filled$65] = $std_list.push({ b: filled, x: { tag: "ConstA", value: { tag: "UnitV" } } });
          filled = filled$65;
        }
        break $m194$match;
      }
      $rt.unreachable();
    }
  }
  if (missing && $std_list.len({ xs: args }) > 0) {
    const [, ctx$66] = rep({ ck: ck, ctx: ctx, code: "E0324", at: at, detail: "`" + d.name + "` takes " + plural({ n: n }) });
    ctx = ctx$66;
  } else {
    if (missing) {
      const [, ctx$67] = rep({ ck: ck, ctx: ctx, code: "E0324", at: at, detail: "`" + d.name + "` needs " + plural({ n: n }) });
      ctx = ctx$67;
    }
  }
  const fa = $std_list.finish({ b: filled });
  if ($rt.eq(d.kind, { tag: "Record" })) {
    return [{ tag: "RecordT", def: d.id, args: fa }, ck, ctx];
  }
  if ($rt.eq(d.kind, { tag: "Union" })) {
    return [{ tag: "UnionT", def: d.id, args: fa }, ck, ctx];
  }
  if ($rt.eq(d.kind, { tag: "Capability" })) {
    return [{ tag: "Capability", def: d.id, args: fa, restrictions: $std_list.finish({ b: restrictions }) }, ck, ctx];
  }
  return [{ tag: "Opaque", def: d.id, args: fa }, ck, ctx];
}

export function fields_of_node({ node }) {
  const $m207 = node;
  $m207$match: {
    if ($m207.tag === "RecordNode") {
      const decl = $m207.decl;
      return decl.fields;
      break $m207$match;
    }
    if ($m207.tag === "VariantNode") {
      const decl = $m207.decl;
      return decl.fields;
      break $m207$match;
    }
    if (true) {
      return [];
      break $m207$match;
    }
    $rt.unreachable();
  }
}

export function fields_of({ ck, ctx, d }) {
  const $m208 = $std_map.find({ d: ctx.fields, key: d.id });
  $m208$match: {
    if ($m208.tag === "Some") {
      const value = $m208.value;
      return [value, ck, ctx];
      break $m208$match;
    }
    if ($m208.tag === "None") {
      skip({  });
      break $m208$match;
    }
    $rt.unreachable();
  }
  let table = ctx.fields;
  const [, table$68] = $std_map.set({ d: table, key: d.id, value: [] });
  table = table$68;
  const saved_file = ck.current_file;
  ck = { ...ck, current_file: d.file };
  let out = $std_list.builder({  });
  for (const f of fields_of_node({ node: d.node })) {
    const fd = def_of_key({ ctx: ctx, k: key({ ck: ck, tag: $defs.tag_sig, span: f.span }) });
    const [$r210, ck$69, ctx$69] = type_of({ fuel: max_type_depth, ck: ck, ctx: ctx, t: f.ty });
    ck = ck$69;
    ctx = ctx$69;
    const t = $r210;
    const [, ctx$70] = set_decl_type({ ctx: ctx, def: fd, t: t });
    ctx = ctx$70;
    const [, out$71] = $std_list.push({ b: out, x: { def: fd, name: f.name.text, ty: t } });
    out = out$71;
  }
  const res_t = $std_list.finish({ b: out });
  const [, table$72] = $std_map.set({ d: table, key: d.id, value: res_t });
  table = table$72;
  ck = { ...ck, current_file: saved_file };
  return [res_t, ck, ctx];
}

export function variants_of({ ck, ctx, d }) {
  const $m213 = $std_map.find({ d: ctx.variants, key: d.id });
  $m213$match: {
    if ($m213.tag === "Some") {
      const value = $m213.value;
      return [value, ctx];
      break $m213$match;
    }
    if ($m213.tag === "None") {
      skip({  });
      break $m213$match;
    }
    $rt.unreachable();
  }
  let out = $std_list.builder({  });
  const $m214 = d.node;
  $m214$match: {
    if ($m214.tag === "UnionNode") {
      const decl = $m214.decl;
      for (const v of decl.variants) {
        const [, out$73] = $std_list.push({ b: out, x: def_of_key({ ctx: ctx, k: $defs.node_key({ file: d.file, tag: $defs.tag_sig, span: v.span }) }) });
        out = out$73;
      }
      break $m214$match;
    }
    if (true) {
      skip({  });
      break $m214$match;
    }
    $rt.unreachable();
  }
  const res_t = $std_list.finish({ b: out });
  let table = ctx.variants;
  const [, table$74] = $std_map.set({ d: table, key: d.id, value: res_t });
  table = table$74;
  return [res_t, ctx];
}

export function sig_parts({ node }) {
  const $m215 = node;
  $m215$match: {
    if ($m215.tag === "FnNode") {
      const decl = $m215.decl;
      return { params: decl.params, ret: { tag: "Some", value: decl.ret }, effects: decl.effects, contracts: decl.contracts, is_const: decl.is_const, is_intrinsic: decl.is_intrinsic, has_tparams: true };
      break $m215$match;
    }
    if ($m215.tag === "IfaceFnNode") {
      const decl = $m215.decl;
      return { params: decl.params, ret: { tag: "Some", value: decl.ret }, effects: decl.effects, contracts: decl.contracts, is_const: false, is_intrinsic: false, has_tparams: false };
      break $m215$match;
    }
    if ($m215.tag === "LawNode") {
      const decl = $m215.decl;
      return { params: decl.params, ret: { tag: "None" }, effects: [], contracts: [], is_const: false, is_intrinsic: false, has_tparams: false };
      break $m215$match;
    }
    if (true) {
      return { params: [], ret: { tag: "None" }, effects: [], contracts: [], is_const: false, is_intrinsic: false, has_tparams: false };
      break $m215$match;
    }
    $rt.unreachable();
  }
}

export function signature_of({ ck, ctx, d }) {
  const $m224 = $std_map.find({ d: ctx.signatures, key: d.id });
  $m224$match: {
    if ($m224.tag === "Some") {
      const value = $m224.value;
      return [value, ck, ctx];
      break $m224$match;
    }
    if ($m224.tag === "None") {
      skip({  });
      break $m224$match;
    }
    $rt.unreachable();
  }
  const saved_file = ck.current_file;
  ck = { ...ck, current_file: d.file };
  const parts = sig_parts({ node: d.node });
  let tparams = [];
  if (parts.has_tparams) {
    const [$r226, ck$75, ctx$75] = type_params_of({ fuel: max_type_depth, ck: ck, ctx: ctx, d: d });
    ck = ck$75;
    ctx = ctx$75;
    tparams = $r226;
  }
  let fn_params = $std_list.builder({  });
  let param_defs = $std_list.builder({  });
  for (const p of parts.params) {
    const pd = def_of_key({ ctx: ctx, k: key({ ck: ck, tag: $defs.tag_sig, span: p.span }) });
    const [$r227, ck$76, ctx$76] = type_of({ fuel: max_type_depth, ck: ck, ctx: ctx, t: p.ty });
    ck = ck$76;
    ctx = ctx$76;
    const t = $r227;
    if (pd >= 0) {
      const [, ctx$77] = set_decl_type({ ctx: ctx, def: pd, t: t });
      ctx = ctx$77;
      const [, param_defs$78] = $std_list.push({ b: param_defs, x: pd });
      param_defs = param_defs$78;
    }
    const [, fn_params$79] = $std_list.push({ b: fn_params, x: { name: p.name.text, is_inout: p.is_inout, ty: t } });
    fn_params = fn_params$79;
  }
  let ret = $types.bool_t;
  const $m229 = parts.ret;
  $m229$match: {
    if ($m229.tag === "Some") {
      const value = $m229.value;
      const [$r230, ck$80, ctx$80] = type_of({ fuel: max_type_depth, ck: ck, ctx: ctx, t: value });
      ck = ck$80;
      ctx = ctx$80;
      ret = $r230;
      break $m229$match;
    }
    if ($m229.tag === "None") {
      ret = $types.bool_t;
      break $m229$match;
    }
    $rt.unreachable();
  }
  const sig = { def: d.id, tparams: tparams, params: $std_list.finish({ b: fn_params }), param_defs: $std_list.finish({ b: param_defs }), ret: ret, effects: effect_set_of({ ctx: ctx, file: d.file, refs: parts.effects }), contracts: parts.contracts, is_const_fn: parts.is_const, is_intrinsic: parts.is_intrinsic };
  let table = ctx.signatures;
  const [, table$81] = $std_map.set({ d: table, key: d.id, value: sig });
  table = table$81;
  ck = { ...ck, current_file: saved_file };
  return [sig, ck, ctx];
}

export function sig_of_id({ ck, ctx, id }) {
  const [$r233, ck$82, ctx$82] = signature_of({ ck: ck, ctx: ctx, d: $context.get_def({ ctx: ctx, id: id }) });
  ck = ck$82;
  ctx = ctx$82;
  return [$r233, ck, ctx];
}

export function const_decl_type({ ck, ctx, d }) {
  const $m234 = $std_map.find({ d: ctx.decl_types, key: d.id });
  $m234$match: {
    if ($m234.tag === "Some") {
      const value = $m234.value;
      return [value, ck, ctx];
      break $m234$match;
    }
    if ($m234.tag === "None") {
      skip({  });
      break $m234$match;
    }
    $rt.unreachable();
  }
  const saved_file = ck.current_file;
  ck = { ...ck, current_file: d.file };
  let t = { tag: "ErrorT" };
  const $m237 = d.node;
  $m237$match: {
    if ($m237.tag === "ConstNode") {
      const decl = $m237.decl;
      const [$r238, ck$83, ctx$83] = type_of({ fuel: max_type_depth, ck: ck, ctx: ctx, t: decl.ty });
      ck = ck$83;
      ctx = ctx$83;
      t = $r238;
      break $m237$match;
    }
    if (true) {
      t = { tag: "ErrorT" };
      break $m237$match;
    }
    $rt.unreachable();
  }
  const [, ctx$84] = set_decl_type({ ctx: ctx, def: d.id, t: t });
  ctx = ctx$84;
  ck = { ...ck, current_file: saved_file };
  return [t, ck, ctx];
}

export function item_name({ item }) {
  const $m241 = item;
  $m241$match: {
    if ($m241.tag === "FnItem") {
      const decl = $m241.decl;
      return decl.name.text;
      break $m241$match;
    }
    if ($m241.tag === "TypeAliasItem") {
      const decl = $m241.decl;
      return decl.name.text;
      break $m241$match;
    }
    if ($m241.tag === "IntrinsicTypeItem") {
      const decl = $m241.decl;
      return decl.name.text;
      break $m241$match;
    }
    if ($m241.tag === "ConstItem") {
      const decl = $m241.decl;
      return decl.name.text;
      break $m241$match;
    }
    if ($m241.tag === "RecordItem") {
      const decl = $m241.decl;
      return decl.name.text;
      break $m241$match;
    }
    if ($m241.tag === "UnionItem") {
      const decl = $m241.decl;
      return decl.name.text;
      break $m241$match;
    }
    if ($m241.tag === "InterfaceItem") {
      const decl = $m241.decl;
      return decl.name.text;
      break $m241$match;
    }
    if ($m241.tag === "ImplItem") {
      const decl = $m241.decl;
      return decl.iface.text;
      break $m241$match;
    }
    if ($m241.tag === "ClaimItem") {
      const decl = $m241.decl;
      return decl.name.text;
      break $m241$match;
    }
    if ($m241.tag === "CapabilityItem") {
      const decl = $m241.decl;
      return decl.name.text;
      break $m241$match;
    }
    if ($m241.tag === "PathItem") {
      const decl = $m241.decl;
      return decl.name.text;
      break $m241$match;
    }
    if ($m241.tag === "PolicyItem") {
      const decl = $m241.decl;
      return decl.name.text;
      break $m241$match;
    }
    if ($m241.tag === "ExampleItem") {
      const decl = $m241.decl;
      return decl.name.text;
      break $m241$match;
    }
    if ($m241.tag === "PropertyItem") {
      const decl = $m241.decl;
      return decl.name.text;
      break $m241$match;
    }
    $rt.unreachable();
  }
}

export function iface_item_name({ member }) {
  const $m242 = member;
  $m242$match: {
    if ($m242.tag === "IfaceFnItem") {
      const decl = $m242.decl;
      return decl.name.text;
      break $m242$match;
    }
    if ($m242.tag === "LawItem") {
      const decl = $m242.decl;
      return decl.name.text;
      break $m242$match;
    }
    $rt.unreachable();
  }
}

export function law_body({ member }) {
  const $m243 = member;
  $m243$match: {
    if ($m243.tag === "LawItem") {
      const decl = $m243.decl;
      return decl.body;
      break $m243$match;
    }
    if (true) {
      return { stmts: [], elided: false, span: { start: 0, end: 0 } };
      break $m243$match;
    }
    $rt.unreachable();
  }
}

export function iface_item_span({ member }) {
  const $m246 = member;
  $m246$match: {
    if ($m246.tag === "IfaceFnItem") {
      const decl = $m246.decl;
      return decl.span;
      break $m246$match;
    }
    if ($m246.tag === "LawItem") {
      const decl = $m246.decl;
      return decl.span;
      break $m246$match;
    }
    $rt.unreachable();
  }
}

export function item_def({ ck, ctx, span }) {
  return $context.get_def({ ctx: ctx, id: def_of_key({ ctx: ctx, k: key({ ck: ck, tag: $defs.tag_item, span: span }) }) });
}

export function sealed_misuse({ ck, ctx, vis, name }) {
  if (vis.is_sealed) {
    const [, ctx$85] = rep({ ck: ck, ctx: ctx, code: "E0335", at: name.span, detail: "`sealed` applies to records and unions only" });
    ctx = ctx$85;
  }
  return [undefined, ctx];
  return [undefined, ctx];
}

export function add_impl({ ck, ctx, iface, target, def }) {
  let current = [];
  const $m247 = $std_map.find({ d: ck.impls, key: iface });
  $m247$match: {
    if ($m247.tag === "Some") {
      const value = $m247.value;
      current = value;
      break $m247$match;
    }
    if ($m247.tag === "None") {
      current = [];
      break $m247$match;
    }
    $rt.unreachable();
  }
  const entry = { target: target, def: def };
  let mine = ck.impls;
  const [, mine$86] = $std_map.set({ d: mine, key: iface, value: $std_list.append({ xs: current, x: entry }) });
  mine = mine$86;
  let shared = [];
  const $m249 = $std_map.find({ d: ctx.impls, key: iface });
  $m249$match: {
    if ($m249.tag === "Some") {
      const value = $m249.value;
      shared = value;
      break $m249$match;
    }
    if ($m249.tag === "None") {
      shared = [];
      break $m249$match;
    }
    $rt.unreachable();
  }
  let table = ctx.impls;
  const [, table$87] = $std_map.set({ d: table, key: iface, value: $std_list.append({ xs: shared, x: entry }) });
  table = table$87;
  return [undefined, ck, ctx];
  return [undefined, ck, ctx];
}

export function has_impl_for({ ck, iface, target }) {
  const $m250 = $std_map.find({ d: ck.impls, key: iface });
  $m250$match: {
    if ($m250.tag === "None") {
      return false;
      break $m250$match;
    }
    if ($m250.tag === "Some") {
      const value = $m250.value;
      for (const x of value) {
        if ($types.same_base({ a: x.target, b: target })) {
          return true;
        }
      }
      return false;
      break $m250$match;
    }
    $rt.unreachable();
  }
}

export function elaborate_item({ ck, ctx, item }) {
  ck = { ...ck, current_def: { tag: "Some", value: item_name({ item: item }) } };
  const $m253 = item;
  $m253$match: {
    if ($m253.tag === "FnItem") {
      const decl = $m253.decl;
      const [, ctx$88] = sealed_misuse({ ck: ck, ctx: ctx, vis: decl.vis, name: decl.name });
      ctx = ctx$88;
      const [$r254, ck$89, ctx$89] = signature_of({ ck: ck, ctx: ctx, d: item_def({ ck: ck, ctx: ctx, span: decl.span }) });
      ck = ck$89;
      ctx = ctx$89;
      const sig = $r254;
      break $m253$match;
    }
    if ($m253.tag === "TypeAliasItem") {
      const decl = $m253.decl;
      const [, ctx$90] = sealed_misuse({ ck: ck, ctx: ctx, vis: decl.vis, name: decl.name });
      ctx = ctx$90;
      const [$r255, ck$91, ctx$91] = alias_type({ fuel: max_type_depth, ck: ck, ctx: ctx, d: item_def({ ck: ck, ctx: ctx, span: decl.span }) });
      ck = ck$91;
      ctx = ctx$91;
      const t = $r255;
      break $m253$match;
    }
    if ($m253.tag === "IntrinsicTypeItem") {
      const decl = $m253.decl;
      const [$r256, ck$92, ctx$92] = type_params_of({ fuel: max_type_depth, ck: ck, ctx: ctx, d: item_def({ ck: ck, ctx: ctx, span: decl.span }) });
      ck = ck$92;
      ctx = ctx$92;
      const ps = $r256;
      break $m253$match;
    }
    if ($m253.tag === "CapabilityItem") {
      const decl = $m253.decl;
      const [$r257, ck$93, ctx$93] = type_params_of({ fuel: max_type_depth, ck: ck, ctx: ctx, d: item_def({ ck: ck, ctx: ctx, span: decl.span }) });
      ck = ck$93;
      ctx = ctx$93;
      const ps = $r257;
      break $m253$match;
    }
    if ($m253.tag === "ConstItem") {
      const decl = $m253.decl;
      const d = item_def({ ck: ck, ctx: ctx, span: decl.span });
      const [$r258, ck$94, ctx$94] = type_of({ fuel: max_type_depth, ck: ck, ctx: ctx, t: decl.ty });
      ck = ck$94;
      ctx = ctx$94;
      const t = $r258;
      const [, ctx$95] = set_decl_type({ ctx: ctx, def: d.id, t: t });
      ctx = ctx$95;
      break $m253$match;
    }
    if ($m253.tag === "RecordItem") {
      const decl = $m253.decl;
      const d = item_def({ ck: ck, ctx: ctx, span: decl.span });
      const [$r259, ck$96, ctx$96] = type_params_of({ fuel: max_type_depth, ck: ck, ctx: ctx, d: d });
      ck = ck$96;
      ctx = ctx$96;
      const ps = $r259;
      const [$r260, ck$97, ctx$97] = fields_of({ ck: ck, ctx: ctx, d: d });
      ck = ck$97;
      ctx = ctx$97;
      const fs = $r260;
      break $m253$match;
    }
    if ($m253.tag === "UnionItem") {
      const decl = $m253.decl;
      const d = item_def({ ck: ck, ctx: ctx, span: decl.span });
      const [$r261, ck$98, ctx$98] = type_params_of({ fuel: max_type_depth, ck: ck, ctx: ctx, d: d });
      ck = ck$98;
      ctx = ctx$98;
      const ps = $r261;
      const [$r262, ctx$99] = variants_of({ ck: ck, ctx: ctx, d: d });
      ctx = ctx$99;
      for (const v of $r262) {
        const [$r263, ck$100, ctx$100] = fields_of({ ck: ck, ctx: ctx, d: $context.get_def({ ctx: ctx, id: v }) });
        ck = ck$100;
        ctx = ctx$100;
        const fs = $r263;
      }
      break $m253$match;
    }
    if ($m253.tag === "InterfaceItem") {
      const decl = $m253.decl;
      const [, ctx$101] = sealed_misuse({ ck: ck, ctx: ctx, vis: decl.vis, name: decl.name });
      ctx = ctx$101;
      for (const member of decl.items) {
        const [$r264, ck$102, ctx$102] = signature_of({ ck: ck, ctx: ctx, d: $context.get_def({ ctx: ctx, id: def_of_key({ ctx: ctx, k: key({ ck: ck, tag: $defs.tag_sig, span: iface_item_span({ member: member }) }) }) }) });
        ck = ck$102;
        ctx = ctx$102;
        const sig = $r264;
      }
      break $m253$match;
    }
    if ($m253.tag === "ImplItem") {
      const decl = $m253.decl;
      const iface = res_def({ res: ref_at({ ctx: ctx, k: key({ ck: ck, tag: $defs.tag_item, span: decl.span }) }) });
      const [$r265, ck$103, ctx$103] = type_of({ fuel: max_type_depth, ck: ck, ctx: ctx, t: decl.target });
      ck = ck$103;
      ctx = ctx$103;
      const target = $r265;
      if (iface >= 0) {
        if (has_impl_for({ ck: ck, iface: iface, target: target })) {
          const [, ctx$104] = rep({ ck: ck, ctx: ctx, code: "E0107", at: decl.iface.span, detail: "`" + decl.iface.text + "` is already implemented for " + show({ ctx: ctx, t: target }) });
          ctx = ctx$104;
        }
        const [, ck$105, ctx$105] = add_impl({ ck: ck, ctx: ctx, iface: iface, target: target, def: def_of_key({ ctx: ctx, k: key({ ck: ck, tag: $defs.tag_item, span: decl.span }) }) });
        ck = ck$105;
        ctx = ctx$105;
      }
      for (const f of decl.fns) {
        const [$r266, ck$106, ctx$106] = signature_of({ ck: ck, ctx: ctx, d: item_def({ ck: ck, ctx: ctx, span: f.span }) });
        ck = ck$106;
        ctx = ctx$106;
        const sig = $r266;
      }
      break $m253$match;
    }
    if ($m253.tag === "PropertyItem") {
      const decl = $m253.decl;
      for (const p of decl.params) {
        const [$r267, ck$107, ctx$107] = type_of({ fuel: max_type_depth, ck: ck, ctx: ctx, t: p.ty });
        ck = ck$107;
        ctx = ctx$107;
        const [, ctx$108] = set_decl_type({ ctx: ctx, def: def_of_key({ ctx: ctx, k: key({ ck: ck, tag: $defs.tag_sig, span: p.span }) }), t: $r267 });
        ctx = ctx$108;
      }
      break $m253$match;
    }
    if (true) {
      skip({  });
      break $m253$match;
    }
    $rt.unreachable();
  }
  ck = { ...ck, current_def: { tag: "None" } };
  return [undefined, ck, ctx];
  return [undefined, ck, ctx];
}

export function check_item({ ck, ctx, item }) {
  const $m270 = item;
  $m270$match: {
    if ($m270.tag === "FnItem") {
      const decl = $m270.decl;
      const [, ck$109, ctx$109] = check_fn({ ck: ck, ctx: ctx, f: decl });
      ck = ck$109;
      ctx = ctx$109;
      break $m270$match;
    }
    if ($m270.tag === "ConstItem") {
      const decl = $m270.decl;
      const t = decl_type({ ctx: ctx, def: def_of_key({ ctx: ctx, k: key({ ck: ck, tag: $defs.tag_item, span: decl.span }) }) });
      const [, ck$110] = push_fn({ ck: ck, f: plain_fn({ ret: { tag: "None" }, frame: 0 }) });
      ck = ck$110;
      const [$r273, ck$111, ctx$111] = check_expr({ ck: ck, ctx: ctx, e: decl.value, expected: { tag: "Some", value: t } });
      ck = ck$111;
      ctx = ctx$111;
      const vt = $r273;
      const [, ck$112] = pop_fn({ ck: ck });
      ck = ck$112;
      break $m270$match;
    }
    if ($m270.tag === "CapabilityItem") {
      const decl = $m270.decl;
      for (const g of decl.grant_list) {
        const $m274 = g.when_cond;
        $m274$match: {
          if ($m274.tag === "Some") {
            const value = $m274.value;
            const [, ck$113] = push_fn({ ck: ck, f: plain_fn({ ret: { tag: "None" }, frame: 0 }) });
            ck = ck$113;
            const [$r277, ck$114, ctx$114] = check_expr({ ck: ck, ctx: ctx, e: value, expected: { tag: "Some", value: $types.bool_t } });
            ck = ck$114;
            ctx = ctx$114;
            const wt = $r277;
            const [, ck$115] = pop_fn({ ck: ck });
            ck = ck$115;
            break $m274$match;
          }
          if ($m274.tag === "None") {
            skip({  });
            break $m274$match;
          }
          $rt.unreachable();
        }
      }
      break $m270$match;
    }
    if ($m270.tag === "InterfaceItem") {
      const decl = $m270.decl;
      for (const member of decl.items) {
        ck = { ...ck, current_def: { tag: "Some", value: decl.name.text + "." + iface_item_name({ member: member }) } };
        const [$r280, ck$116, ctx$116] = signature_of({ ck: ck, ctx: ctx, d: $context.get_def({ ctx: ctx, id: def_of_key({ ctx: ctx, k: key({ ck: ck, tag: $defs.tag_sig, span: iface_item_span({ member: member }) }) }) }) });
        ck = ck$116;
        ctx = ctx$116;
        const sig = $r280;
        if (member.tag === "IfaceFnItem") {
          const [, ck$117, ctx$117] = check_contracts({ ck: ck, ctx: ctx, sig: sig });
          ck = ck$117;
          ctx = ctx$117;
        } else {
          const [, ck$118, ctx$118] = check_assertion_block({ ck: ck, ctx: ctx, b: law_body({ member: member }) });
          ck = ck$118;
          ctx = ctx$118;
        }
      }
      break $m270$match;
    }
    if ($m270.tag === "ImplItem") {
      const decl = $m270.decl;
      const [, ck$119, ctx$119] = check_impl({ ck: ck, ctx: ctx, im: decl });
      ck = ck$119;
      ctx = ctx$119;
      break $m270$match;
    }
    if ($m270.tag === "ExampleItem") {
      const decl = $m270.decl;
      const [, ck$120, ctx$120] = check_assertion_block({ ck: ck, ctx: ctx, b: decl.body });
      ck = ck$120;
      ctx = ctx$120;
      break $m270$match;
    }
    if ($m270.tag === "PropertyItem") {
      const decl = $m270.decl;
      const [, ck$121, ctx$121] = check_assertion_block({ ck: ck, ctx: ctx, b: decl.body });
      ck = ck$121;
      ctx = ctx$121;
      break $m270$match;
    }
    if (true) {
      skip({  });
      break $m270$match;
    }
    $rt.unreachable();
  }
  return [undefined, ck, ctx];
  return [undefined, ck, ctx];
}

export function check_fn({ ck, ctx, f }) {
  const [$r281, ck$122, ctx$122] = signature_of({ ck: ck, ctx: ctx, d: item_def({ ck: ck, ctx: ctx, span: f.span }) });
  ck = ck$122;
  ctx = ctx$122;
  const sig = $r281;
  const [, ck$123, ctx$123] = check_contracts({ ck: ck, ctx: ctx, sig: sig });
  ck = ck$123;
  ctx = ctx$123;
  const $m282 = f.body;
  $m282$match: {
    if ($m282.tag === "None") {
      skip({  });
      break $m282$match;
    }
    if ($m282.tag === "Some") {
      const value = $m282.value;
      const [, ck$124] = push_fn({ ck: ck, f: plain_fn({ ret: { tag: "Some", value: sig.ret }, frame: 0 }) });
      ck = ck$124;
      const [, ck$125, ctx$125] = check_block({ ck: ck, ctx: ctx, b: value });
      ck = ck$125;
      ctx = ctx$125;
      if (!$types.same_base({ a: sig.ret, b: $types.unit_t }) && !block_returns({ b: value })) {
        const [, ctx$126] = rep({ ck: ck, ctx: ctx, code: "E0331", at: f.name.span, detail: "`" + f.name.text + "` returns " + show({ ctx: ctx, t: sig.ret }) + " but not every path ends in `return`" });
        ctx = ctx$126;
      }
      const [, ck$127] = pop_fn({ ck: ck });
      ck = ck$127;
      break $m282$match;
    }
    $rt.unreachable();
  }
  return [undefined, ck, ctx];
  return [undefined, ck, ctx];
}

export function check_contracts({ ck, ctx, sig }) {
  for (const c of sig.contracts) {
    const [, ck$128] = push_fn({ ck: ck, f: plain_fn({ ret: { tag: "Some", value: sig.ret }, frame: 0 }) });
    ck = ck$128;
    if (c.clause !== "decreases") {
      const [$r286, ck$129, ctx$129] = check_expr({ ck: ck, ctx: ctx, e: c.expr, expected: { tag: "Some", value: $types.bool_t } });
      ck = ck$129;
      ctx = ctx$129;
      const bt = $r286;
    } else {
      const [$r288, ck$130, ctx$130] = check_expr({ ck: ck, ctx: ctx, e: c.expr, expected: { tag: "None" } });
      ck = ck$130;
      ctx = ctx$130;
      const t = $r288;
      const s = $types.strip({ t: t });
      const pn = $types.prim_name({ t: s });
      const ok = pn === "Int" || pn === "Duration" || s.tag === "RecordT" || s.tag === "UnionT" || s.tag === "ErrorT" || s.tag === "Opaque" && $context.qualified_name({ ctx: ctx, id: $types.def_of({ t: s }) }) === "std.list.List";
      if (!ok) {
        const [, ctx$131] = rep({ ck: ck, ctx: ctx, code: "E0321", at: $parser.span_of_expr({ e: c.expr }), detail: "a `decreases` measure is an Int, or a record, union or list value; found " + show({ ctx: ctx, t: t }) });
        ctx = ctx$131;
      }
    }
    const [, ck$132] = pop_fn({ ck: ck });
    ck = ck$132;
  }
  return [undefined, ck, ctx];
  return [undefined, ck, ctx];
}

export function check_verify({ ck, ctx, v }) {
  let ps = $std_list.builder({  });
  for (const p of v.params) {
    const [$r289, ck$133, ctx$133] = type_of({ fuel: max_type_depth, ck: ck, ctx: ctx, t: p.ty });
    ck = ck$133;
    ctx = ctx$133;
    const t = $r289;
    const [, ctx$134] = set_decl_type({ ctx: ctx, def: def_of_key({ ctx: ctx, k: key({ ck: ck, tag: $defs.tag_sig, span: p.span }) }), t: t });
    ctx = ctx$134;
    const [, ps$135] = $std_list.push({ b: ps, x: { name: p.name.text, is_inout: false, ty: t } });
    ps = ps$135;
    if (!($types.strip({ t: t }).tag === "Capability")) {
      const [, ctx$136] = rep({ ck: ck, ctx: ctx, code: "E0208", at: p.span, detail: "verify parameters are capabilities supplied by the environment (§20.2); `" + p.name.text + "` is " + show({ ctx: ctx, t: t }) });
      ctx = ctx$136;
    }
  }
  let table = ctx.verifies;
  const [, table$137] = $std_map.set({ d: table, key: key({ ck: ck, tag: $defs.tag_misc, span: v.span }), value: { params: $std_list.finish({ b: ps }), effects: effect_set_of({ ctx: ctx, file: ck.current_file, refs: v.effects }) } });
  table = table$137;
  const [, ck$138] = push_fn({ ck: ck, f: { ret: { tag: "Some", value: $types.bool_t }, assertion: true, in_recover: false, frame: 0, is_verify: true } });
  ck = ck$138;
  const [, ck$139, ctx$139] = check_block({ ck: ck, ctx: ctx, b: v.body });
  ck = ck$139;
  ctx = ctx$139;
  const [, ck$140] = pop_fn({ ck: ck });
  ck = ck$140;
  return [undefined, ck, ctx];
  return [undefined, ck, ctx];
}

export function check_assertion_block({ ck, ctx, b }) {
  const [, ck$141] = push_fn({ ck: ck, f: { ret: { tag: "None" }, assertion: true, in_recover: false, frame: 0, is_verify: false } });
  ck = ck$141;
  const [, ck$142, ctx$142] = check_block({ ck: ck, ctx: ctx, b: b });
  ck = ck$142;
  ctx = ctx$142;
  const [, ck$143] = pop_fn({ ck: ck });
  ck = ck$143;
  return [undefined, ck, ctx];
  return [undefined, ck, ctx];
}

export function iface_fns({ ctx, iface }) {
  let out = $std_list.builder({  });
  const $hi296 = $context.def_count({ ctx: ctx });
  for (let i = 0; i < $hi296; i++) {
    const d = $context.get_def({ ctx: ctx, id: i });
    if ($rt.eq(d.kind, { tag: "IfaceFn" }) && $rt.eq(d.parent, { tag: "Some", value: iface })) {
      const [, out$144] = $std_list.push({ b: out, x: d });
      out = out$144;
    }
  }
  return $std_list.finish({ b: out });
}

export function find_impl_fn({ fns, name }) {
  for (const x of fns) {
    if (x.def.name === name) {
      return { tag: "Some", value: x };
    }
  }
  return { tag: "None" };
}

export function has_iface_fn({ fns, name }) {
  for (const d of fns) {
    if (d.name === name) {
      return true;
    }
  }
  return false;
}

export function check_impl({ ck, ctx, im }) {
  const iface_id = res_def({ res: ref_at({ ctx: ctx, k: key({ ck: ck, tag: $defs.tag_item, span: im.span }) }) });
  if (iface_id < 0) {
    return [undefined, ck, ctx];
  }
  const iface = $context.get_def({ ctx: ctx, id: iface_id });
  const [$r301, ck$145, ctx$145] = type_of({ fuel: max_type_depth, ck: ck, ctx: ctx, t: im.target });
  ck = ck$145;
  ctx = ctx$145;
  const target = $r301;
  const tp = iface_param({ ctx: ctx, iface: iface_id });
  let subst = $std_map.dict({  });
  const [, subst$146] = $std_map.set({ d: subst, key: tp, value: { tag: "TypeA", ty: target } });
  subst = subst$146;
  const wanted = iface_fns({ ctx: ctx, iface: iface_id });
  let impl_fns = $std_list.builder({  });
  for (const f of im.fns) {
    const [, impl_fns$147] = $std_list.push({ b: impl_fns, x: { node: f, def: item_def({ ck: ck, ctx: ctx, span: f.span }) } });
    impl_fns = impl_fns$147;
  }
  const have_fns = $std_list.finish({ b: impl_fns });
  for (const want of wanted) {
    const $m304 = find_impl_fn({ fns: have_fns, name: want.name });
    $m304$match: {
      if ($m304.tag === "None") {
        const [, ctx$148] = rep({ ck: ck, ctx: ctx, code: "E0334", at: im.iface.span, detail: "impl " + iface.name + "[" + show({ ctx: ctx, t: target }) + "] is missing `" + want.name + "`" });
        ctx = ctx$148;
        break $m304$match;
      }
      if ($m304.tag === "Some") {
        const value = $m304.value;
        const [$r305, ck$149, ctx$149] = signature_of({ ck: ck, ctx: ctx, d: want });
        ck = ck$149;
        ctx = ctx$149;
        const ws = $r305;
        const [$r306, ck$150, ctx$150] = signature_of({ ck: ck, ctx: ctx, d: value.def });
        ck = ck$150;
        ctx = ctx$150;
        const hs = $r306;
        let problems = $std_list.builder({  });
        if ($std_list.len({ xs: ws.params }) !== $std_list.len({ xs: hs.params })) {
          const [, problems$151] = $std_list.push({ b: problems, x: "expected " + $std_int.to_text({ x: $std_list.len({ xs: ws.params }) }) + " parameters, found " + $std_int.to_text({ x: $std_list.len({ xs: hs.params }) }) });
          problems = problems$151;
        } else {
          const $hi307 = $std_list.len({ xs: ws.params });
          for (let i = 0; i < $hi307; i++) {
            const wp = $std_list.get({ xs: ws.params, i: i });
            const hp = $std_list.get({ xs: hs.params, i: i });
            const wt = $types.substitute({ t: wp.ty, subst: subst });
            if (wp.name !== hp.name) {
              const [, problems$152] = $std_list.push({ b: problems, x: "parameter " + $std_int.to_text({ x: $rt.int.add(i, 1, $ob29) }) + " is named `" + wp.name + "` in the interface, `" + hp.name + "` here" });
              problems = problems$152;
            } else {
              if (wp.is_inout !== hp.is_inout) {
                const [, problems$153] = $std_list.push({ b: problems, x: "parameter `" + wp.name + "` differs in `inout`" });
                problems = problems$153;
              } else {
                if (!$types.same_base({ a: wt, b: hp.ty })) {
                  const [, problems$154] = $std_list.push({ b: problems, x: "parameter `" + wp.name + "` has type " + show({ ctx: ctx, t: wt }) + " in the interface, " + show({ ctx: ctx, t: hp.ty }) + " here" });
                  problems = problems$154;
                }
              }
            }
          }
        }
        const wr = $types.substitute({ t: ws.ret, subst: subst });
        if (!$types.same_base({ a: wr, b: hs.ret })) {
          const [, problems$155] = $std_list.push({ b: problems, x: "returns " + show({ ctx: ctx, t: wr }) + " in the interface, " + show({ ctx: ctx, t: hs.ret }) + " here" });
          problems = problems$155;
        }
        if ($std_list.len({ xs: hs.tparams }) > 0) {
          const [, problems$156] = $std_list.push({ b: problems, x: "an impl function takes no type parameters" });
          problems = problems$156;
        }
        for (const p of $std_list.finish({ b: problems })) {
          const [, ctx$157] = rep({ ck: ck, ctx: ctx, code: "E0334", at: value.node.name.span, detail: "`" + want.name + "`: " + p });
          ctx = ctx$157;
        }
        break $m304$match;
      }
      $rt.unreachable();
    }
  }
  for (const have of have_fns) {
    if (!has_iface_fn({ fns: wanted, name: have.def.name })) {
      const [, ctx$158] = rep({ ck: ck, ctx: ctx, code: "E0334", at: have.node.name.span, detail: "interface `" + iface.name + "` has no function `" + have.def.name + "`" });
      ctx = ctx$158;
    }
  }
  for (const f of im.fns) {
    ck = { ...ck, current_def: { tag: "Some", value: im.iface.text + "." + f.name.text } };
    const [, ck$159, ctx$159] = check_fn({ ck: ck, ctx: ctx, f: f });
    ck = ck$159;
    ctx = ctx$159;
  }
  return [undefined, ck, ctx];
  return [undefined, ck, ctx];
}

export function implemented({ ck, ctx, iface, target }) {
  const s = $types.strip({ t: target });
  if (s.tag === "ErrorT") {
    return true;
  }
  const $m310 = s;
  $m310$match: {
    if ($m310.tag === "ParamT") {
      const def = $m310.def;
      for (const infos of $std_map.values({ d: ctx.type_params })) {
        for (const p of infos) {
          if (p.tag === "TypeP" && $types.tparam_def({ p: p }) === def) {
            return $rt.eq(tparam_bound({ p: p }), { tag: "Some", value: iface });
          }
        }
      }
      return false;
      break $m310$match;
    }
    if (true) {
      return has_impl_for({ ck: ck, iface: iface, target: s });
      break $m310$match;
    }
    $rt.unreachable();
  }
}

export function check_block({ ck, ctx, b }) {
  let returned = false;
  for (const s of b.stmts) {
    if (returned) {
      const [, ctx$160] = rep({ ck: ck, ctx: ctx, code: "E0332", at: $parser.span_of_stmt({ st: s }), detail: "this statement follows a `return` on every path" });
      ctx = ctx$160;
      returned = false;
    }
    const [, ck$161, ctx$161] = check_stmt({ ck: ck, ctx: ctx, s: s });
    ck = ck$161;
    ctx = ctx$161;
    if (stmt_returns({ s: s })) {
      returned = true;
    }
  }
  return [undefined, ck, ctx];
  return [undefined, ck, ctx];
}

export function stmt_returns({ s }) {
  const $m312 = s;
  $m312$match: {
    if ($m312.tag === "Return") {
      return true;
      break $m312$match;
    }
    if ($m312.tag === "If") {
      const then_block = $m312.then_block;
      const else_block = $m312.else_block;
      const $m313 = else_block;
      $m313$match: {
        if ($m313.tag === "None") {
          return false;
          break $m313$match;
        }
        if ($m313.tag === "Some") {
          const value = $m313.value;
          return block_returns({ b: then_block }) && block_returns({ b: value });
          break $m313$match;
        }
        $rt.unreachable();
      }
      break $m312$match;
    }
    if ($m312.tag === "Match") {
      const arms = $m312.arms;
      if ($std_list.len({ xs: arms }) === 0) {
        return false;
      }
      for (const a of arms) {
        const $m314 = a.body;
        $m314$match: {
          if ($m314.tag === "ArmBlock") {
            const block = $m314.block;
            if (!block_returns({ b: block })) {
              return false;
            }
            break $m314$match;
          }
          if ($m314.tag === "ArmStmt") {
            const stmt = $m314.stmt;
            if (!stmt_returns({ s: stmt })) {
              return false;
            }
            break $m314$match;
          }
          $rt.unreachable();
        }
      }
      return true;
      break $m312$match;
    }
    if (true) {
      return false;
      break $m312$match;
    }
    $rt.unreachable();
  }
}

export function block_returns({ b }) {
  for (const s of b.stmts) {
    if (stmt_returns({ s: s })) {
      return true;
    }
  }
  return false;
}

export function describe_binding({ d }) {
  if ($rt.eq(d.kind, { tag: "Let" })) {
    return "a `let` binding";
  }
  if ($rt.eq(d.kind, { tag: "Param" })) {
    return "a parameter";
  }
  if ($rt.eq(d.kind, { tag: "For" })) {
    return "a loop variable";
  }
  if ($rt.eq(d.kind, { tag: "Const" })) {
    return "a constant";
  }
  return "a " + $defs.kind_text({ k: d.kind });
}

export function check_stmt({ ck, ctx, s }) {
  const $m319 = s;
  $m319$match: {
    if ($m319.tag === "Let") {
      const name = $m319.name;
      const ty = $m319.ty;
      const value = $m319.value;
      const span = $m319.span;
      const [$r320, ck$162, ctx$162] = type_of({ fuel: max_type_depth, ck: ck, ctx: ctx, t: ty });
      ck = ck$162;
      ctx = ctx$162;
      const t = $r320;
      const [, ctx$163] = set_decl_type({ ctx: ctx, def: def_of_key({ ctx: ctx, k: key({ ck: ck, tag: $defs.tag_stmt, span: span }) }), t: t });
      ctx = ctx$163;
      const [$r322, ck$164, ctx$164] = check_expr({ ck: ck, ctx: ctx, e: value, expected: { tag: "Some", value: t } });
      ck = ck$164;
      ctx = ctx$164;
      const vt = $r322;
      break $m319$match;
    }
    if ($m319.tag === "Var") {
      const name = $m319.name;
      const ty = $m319.ty;
      const value = $m319.value;
      const span = $m319.span;
      const [$r323, ck$165, ctx$165] = type_of({ fuel: max_type_depth, ck: ck, ctx: ctx, t: ty });
      ck = ck$165;
      ctx = ctx$165;
      const t = $r323;
      const [, ctx$166] = set_decl_type({ ctx: ctx, def: def_of_key({ ctx: ctx, k: key({ ck: ck, tag: $defs.tag_stmt, span: span }) }), t: t });
      ctx = ctx$166;
      const [$r325, ck$167, ctx$167] = check_expr({ ck: ck, ctx: ctx, e: value, expected: { tag: "Some", value: t } });
      ck = ck$167;
      ctx = ctx$167;
      const vt = $r325;
      break $m319$match;
    }
    if ($m319.tag === "Assign") {
      const name = $m319.name;
      const value = $m319.value;
      const span = $m319.span;
      const def = res_def({ res: ref_at({ ctx: ctx, k: key({ ck: ck, tag: $defs.tag_stmt, span: span }) }) });
      if (def >= 0) {
        const d = $context.get_def({ ctx: ctx, id: def });
        if (!$rt.eq(d.kind, { tag: "Var" }) && !($rt.eq(d.kind, { tag: "Param" }) && d.is_inout)) {
          const [, ctx$168] = rep({ ck: ck, ctx: ctx, code: "E0328", at: name.span, detail: "`" + name.text + "` is " + describe_binding({ d: d }) + "; only a `var` or an `inout` parameter can be assigned" });
          ctx = ctx$168;
        }
        const [$r329, ck$169, ctx$169] = check_expr({ ck: ck, ctx: ctx, e: value, expected: { tag: "Some", value: decl_type({ ctx: ctx, def: def }) } });
        ck = ck$169;
        ctx = ctx$169;
        const vt = $r329;
      }
      break $m319$match;
    }
    if ($m319.tag === "Return") {
      const value = $m319.value;
      const span = $m319.span;
      const rt = fn_ret({ ck: ck });
      if (rt.tag === "None") {
        let why = "`return` is not allowed here";
        if (fn_flag({ ck: ck, which: 1 })) {
          why = "`return` is not allowed inside `recover`; the block's last expression is its value";
        }
        const [, ctx$170] = rep({ ck: ck, ctx: ctx, code: "E0321", at: span, detail: why });
        ctx = ctx$170;
      }
      const [$r330, ck$171, ctx$171] = check_expr({ ck: ck, ctx: ctx, e: value, expected: rt });
      ck = ck$171;
      ctx = ctx$171;
      const vt = $r330;
      break $m319$match;
    }
    if ($m319.tag === "If") {
      const cond = $m319.cond;
      const then_block = $m319.then_block;
      const else_block = $m319.else_block;
      const span = $m319.span;
      const [$r332, ck$172, ctx$172] = check_expr({ ck: ck, ctx: ctx, e: cond, expected: { tag: "Some", value: $types.bool_t } });
      ck = ck$172;
      ctx = ctx$172;
      const ct = $r332;
      const [, ck$173, ctx$173] = check_block({ ck: ck, ctx: ctx, b: then_block });
      ck = ck$173;
      ctx = ctx$173;
      const $m333 = else_block;
      $m333$match: {
        if ($m333.tag === "Some") {
          const value = $m333.value;
          const [, ck$174, ctx$174] = check_block({ ck: ck, ctx: ctx, b: value });
          ck = ck$174;
          ctx = ctx$174;
          break $m333$match;
        }
        if ($m333.tag === "None") {
          skip({  });
          break $m333$match;
        }
        $rt.unreachable();
      }
      break $m319$match;
    }
    if ($m319.tag === "Match") {
      const scrutinee = $m319.scrutinee;
      const arms = $m319.arms;
      const span = $m319.span;
      const [, ck$175, ctx$175] = match_stmt({ ck: ck, ctx: ctx, s: s });
      ck = ck$175;
      ctx = ctx$175;
      break $m319$match;
    }
    if ($m319.tag === "Loop") {
      const cond = $m319.cond;
      const clauses = $m319.clauses;
      const body = $m319.body;
      const span = $m319.span;
      const [$r335, ck$176, ctx$176] = check_expr({ ck: ck, ctx: ctx, e: cond, expected: { tag: "Some", value: $types.bool_t } });
      ck = ck$176;
      ctx = ctx$176;
      const ct = $r335;
      for (const c of clauses) {
        let want = $types.int_t;
        if (c.clause === "invariant") {
          want = $types.bool_t;
        }
        const [$r337, ck$177, ctx$177] = check_expr({ ck: ck, ctx: ctx, e: c.expr, expected: { tag: "Some", value: want } });
        ck = ck$177;
        ctx = ctx$177;
        const lt = $r337;
      }
      const [, ck$178, ctx$178] = check_block({ ck: ck, ctx: ctx, b: body });
      ck = ck$178;
      ctx = ctx$178;
      break $m319$match;
    }
    if ($m319.tag === "For") {
      const name = $m319.name;
      const ty = $m319.ty;
      const domain = $m319.domain;
      const body = $m319.body;
      const span = $m319.span;
      const [$r338, ck$179, ctx$179] = type_of({ fuel: max_type_depth, ck: ck, ctx: ctx, t: ty });
      ck = ck$179;
      ctx = ctx$179;
      const declared = $r338;
      const [, ctx$180] = set_decl_type({ ctx: ctx, def: def_of_key({ ctx: ctx, k: key({ ck: ck, tag: $defs.tag_stmt, span: span }) }), t: declared });
      ctx = ctx$180;
      const $m339 = domain;
      $m339$match: {
        if ($m339.tag === "RangeDomain") {
          const lo = $m339.lo;
          const hi = $m339.hi;
          if (!$types.same_base({ a: declared, b: $types.int_t })) {
            const [, ctx$181] = rep({ ck: ck, ctx: ctx, code: "E0321", at: $comments.type_span({ t: ty }), detail: "a range iterates Int values, not " + show({ ctx: ctx, t: declared }) });
            ctx = ctx$181;
          }
          const [$r341, ck$182, ctx$182] = check_expr({ ck: ck, ctx: ctx, e: lo, expected: { tag: "Some", value: $types.int_t } });
          ck = ck$182;
          ctx = ctx$182;
          const lt = $r341;
          const [$r343, ck$183, ctx$183] = check_expr({ ck: ck, ctx: ctx, e: hi, expected: { tag: "Some", value: $types.int_t } });
          ck = ck$183;
          ctx = ctx$183;
          const ht = $r343;
          break $m339$match;
        }
        if ($m339.tag === "InDomain") {
          const expr = $m339.expr;
          const [$r345, ck$184, ctx$184] = check_expr({ ck: ck, ctx: ctx, e: expr, expected: { tag: "None" } });
          ck = ck$184;
          ctx = ctx$184;
          const dt = $r345;
          const [$r347, ctx$185] = element_type({ ck: ck, ctx: ctx, t: dt, at: $parser.span_of_expr({ e: expr }) });
          ctx = ctx$185;
          const $m346 = $r347;
          $m346$match: {
            if ($m346.tag === "Some") {
              const value = $m346.value;
              const [$r349, ctx$186] = coerce({ ck: ck, ctx: ctx, actual: value, expected: declared, at: $comments.type_span({ t: ty }), what: "loop variable", at_key: { tag: "None" } });
              ctx = ctx$186;
              const ct = $r349;
              break $m346$match;
            }
            if ($m346.tag === "None") {
              skip({  });
              break $m346$match;
            }
            $rt.unreachable();
          }
          break $m339$match;
        }
        $rt.unreachable();
      }
      const [, ck$187, ctx$187] = check_block({ ck: ck, ctx: ctx, b: body });
      ck = ck$187;
      ctx = ctx$187;
      break $m319$match;
    }
    if ($m319.tag === "Assume") {
      const verify_block = $m319.verify_block;
      const $m350 = verify_block;
      $m350$match: {
        if ($m350.tag === "Some") {
          const value = $m350.value;
          const [, ck$188, ctx$188] = check_verify({ ck: ck, ctx: ctx, v: value });
          ck = ck$188;
          ctx = ctx$188;
          break $m350$match;
        }
        if ($m350.tag === "None") {
          skip({  });
          break $m350$match;
        }
        $rt.unreachable();
      }
      break $m319$match;
    }
    if ($m319.tag === "ExprStmt") {
      const expr = $m319.expr;
      const span = $m319.span;
      const [$r352, ck$189, ctx$189] = check_expr({ ck: ck, ctx: ctx, e: expr, expected: { tag: "None" } });
      ck = ck$189;
      ctx = ctx$189;
      const t = $r352;
      if (fn_flag({ ck: ck, which: 0 })) {
        if (!$types.assignable({ actual: t, expected: $types.bool_t })) {
          const [, ctx$190] = rep({ ck: ck, ctx: ctx, code: "E0321", at: span, detail: "an assertion must be Bool, found " + show({ ctx: ctx, t: t }) });
          ctx = ctx$190;
        }
      } else {
        if (!fn_flag({ ck: ck, which: 1 })) {
          if (!$types.assignable({ actual: t, expected: $types.unit_t })) {
            const [, ctx$191] = rep({ ck: ck, ctx: ctx, code: "E0339", at: span, detail: "this call returns " + show({ ctx: ctx, t: t }) + "; bind it with `let` or return it" });
            ctx = ctx$191;
          }
        }
      }
      break $m319$match;
    }
    $rt.unreachable();
  }
  return [undefined, ck, ctx];
  return [undefined, ck, ctx];
}

export function element_type({ ck, ctx, t, at }) {
  const s = $types.strip({ t: t });
  if (s.tag === "ErrorT") {
    return [{ tag: "Some", value: { tag: "ErrorT" } }, ctx];
  }
  const [$r355, ctx$192] = is_list_type({ ctx: ctx, t: s });
  ctx = ctx$192;
  if ($r355) {
    const $m356 = first_type_arg({ t: s });
    $m356$match: {
      if ($m356.tag === "Some") {
        const value = $m356.value;
        return [{ tag: "Some", value: value }, ctx];
        break $m356$match;
      }
      if ($m356.tag === "None") {
        skip({  });
        break $m356$match;
      }
      $rt.unreachable();
    }
  }
  const [, ctx$193] = rep({ ck: ck, ctx: ctx, code: "E0321", at: at, detail: "`for` iterates a range or a List, not " + show({ ctx: ctx, t: t }) });
  ctx = ctx$193;
  return [{ tag: "None" }, ctx];
}

export function shape_key({ a }) {
  const $m359 = a;
  $m359$match: {
    if ($m359.tag === "VariantShape") {
      const variant = $m359.variant;
      return { tag: "Some", value: "v:" + $std_int.to_text({ x: variant }) };
      break $m359$match;
    }
    if ($m359.tag === "BoolShape") {
      const value = $m359.value;
      return { tag: "Some", value: "b:" + $std_bool.to_text({ b: value }) };
      break $m359$match;
    }
    if (true) {
      return { tag: "None" };
      break $m359$match;
    }
    $rt.unreachable();
  }
}

export function shape_guarded({ a }) {
  const $m363 = a;
  $m363$match: {
    if ($m363.tag === "AllShape") {
      const guarded = $m363.guarded;
      return guarded;
      break $m363$match;
    }
    if ($m363.tag === "VariantShape") {
      const guarded = $m363.guarded;
      return guarded;
      break $m363$match;
    }
    if ($m363.tag === "BoolShape") {
      const guarded = $m363.guarded;
      return guarded;
      break $m363$match;
    }
    if ($m363.tag === "LitShape") {
      const guarded = $m363.guarded;
      return guarded;
      break $m363$match;
    }
    $rt.unreachable();
  }
}

export function all_variants_covered({ variants, covered }) {
  for (const v of variants) {
    if (!$std_map.contains({ d: covered, key: "v:" + $std_int.to_text({ x: v }) })) {
      return false;
    }
  }
  return true;
}

export function coverage({ ctx, scrutinee, arms }) {
  let covered = $std_map.dict({  });
  let all = false;
  let unreachable = $std_list.builder({  });
  let i = 0;
  for (const arm of arms) {
    const k = shape_key({ a: arm });
    let already = all;
    const $m364 = k;
    $m364$match: {
      if ($m364.tag === "Some") {
        const value = $m364.value;
        if ($std_map.contains({ d: covered, key: value })) {
          already = true;
        }
        break $m364$match;
      }
      if ($m364.tag === "None") {
        skip({  });
        break $m364$match;
      }
      $rt.unreachable();
    }
    if (already) {
      const [, unreachable$194] = $std_list.push({ b: unreachable, x: i });
      unreachable = unreachable$194;
    } else {
      if (!shape_guarded({ a: arm })) {
        if (arm.tag === "AllShape") {
          all = true;
        } else {
          const $m365 = k;
          $m365$match: {
            if ($m365.tag === "Some") {
              const value = $m365.value;
              const [, covered$195] = $std_map.set({ d: covered, key: value, value: true });
              covered = covered$195;
              break $m365$match;
            }
            if ($m365.tag === "None") {
              skip({  });
              break $m365$match;
            }
            $rt.unreachable();
          }
        }
        const $m366 = scrutinee;
        $m366$match: {
          if ($m366.tag === "BoolS") {
            if ($std_map.contains({ d: covered, key: "b:true" }) && $std_map.contains({ d: covered, key: "b:false" })) {
              all = true;
            }
            break $m366$match;
          }
          if ($m366.tag === "UnionS") {
            const variants = $m366.variants;
            if (all_variants_covered({ variants: variants, covered: covered })) {
              all = true;
            }
            break $m366$match;
          }
          if ($m366.tag === "OtherS") {
            skip({  });
            break $m366$match;
          }
          $rt.unreachable();
        }
      }
    }
    i = $rt.int.add(i, 1, $ob30);
  }
  if (all) {
    return { missing: [], needs_catch_all: false, unreachable: $std_list.finish({ b: unreachable }) };
  }
  const $m368 = scrutinee;
  $m368$match: {
    if ($m368.tag === "UnionS") {
      const variants = $m368.variants;
      let missing = $std_list.builder({  });
      for (const v of variants) {
        if (!$std_map.contains({ d: covered, key: "v:" + $std_int.to_text({ x: v }) })) {
          const [, missing$196] = $std_list.push({ b: missing, x: def_name({ ctx: ctx, id: v }) });
          missing = missing$196;
        }
      }
      return { missing: $std_list.finish({ b: missing }), needs_catch_all: false, unreachable: $std_list.finish({ b: unreachable }) };
      break $m368$match;
    }
    if ($m368.tag === "BoolS") {
      let missing = $std_list.builder({  });
      if (!$std_map.contains({ d: covered, key: "b:true" })) {
        const [, missing$197] = $std_list.push({ b: missing, x: "true" });
        missing = missing$197;
      }
      if (!$std_map.contains({ d: covered, key: "b:false" })) {
        const [, missing$198] = $std_list.push({ b: missing, x: "false" });
        missing = missing$198;
      }
      return { missing: $std_list.finish({ b: missing }), needs_catch_all: false, unreachable: $std_list.finish({ b: unreachable }) };
      break $m368$match;
    }
    if ($m368.tag === "OtherS") {
      return { missing: [], needs_catch_all: true, unreachable: $std_list.finish({ b: unreachable }) };
      break $m368$match;
    }
    $rt.unreachable();
  }
}

export function pattern_span({ p }) {
  const $m372 = p;
  $m372$match: {
    if ($m372.tag === "WildcardPat") {
      const span = $m372.span;
      return span;
      break $m372$match;
    }
    if ($m372.tag === "BindPat") {
      const span = $m372.span;
      return span;
      break $m372$match;
    }
    if ($m372.tag === "LitPat") {
      const span = $m372.span;
      return span;
      break $m372$match;
    }
    if ($m372.tag === "VariantPat") {
      const span = $m372.span;
      return span;
      break $m372$match;
    }
    $rt.unreachable();
  }
}

export function match_stmt({ ck, ctx, s }) {
  const $m373 = s;
  $m373$match: {
    if ($m373.tag === "Match") {
      const scrutinee = $m373.scrutinee;
      const arms = $m373.arms;
      const span = $m373.span;
      const [$r375, ck$199, ctx$199] = check_expr({ ck: ck, ctx: ctx, e: scrutinee, expected: { tag: "None" } });
      ck = ck$199;
      ctx = ctx$199;
      const st_full = $r375;
      const st = $types.strip({ t: st_full });
      let shape = { tag: "OtherS" };
      const pn = $types.prim_name({ t: st });
      if (st.tag === "UnionT") {
        const [$r377, ctx$200] = variants_of({ ck: ck, ctx: ctx, d: $context.get_def({ ctx: ctx, id: $types.def_of({ t: st }) }) });
        ctx = ctx$200;
        shape = { tag: "UnionS", variants: $r377 };
      } else {
        if (pn === "Bool") {
          shape = { tag: "BoolS" };
        } else {
          if (st.tag === "ErrorT" || pn === "Int" || pn === "Text" || pn === "Float" || pn === "Duration") {
            shape = { tag: "OtherS" };
          } else {
            const [, ctx$201] = rep({ ck: ck, ctx: ctx, code: "E0321", at: $parser.span_of_expr({ e: scrutinee }), detail: "`match` needs a union, Bool, Int, Text, Float or Duration, not " + show({ ctx: ctx, t: st_full }) });
            ctx = ctx$201;
            shape = { tag: "OtherS" };
          }
        }
      }
      let shapes = $std_list.builder({  });
      for (const a of arms) {
        const [$r382, ck$202, ctx$202] = check_pattern({ ck: ck, ctx: ctx, p: a.pattern, scrutinee: st_full, guarded: a.guard.tag === "Some" });
        ck = ck$202;
        ctx = ctx$202;
        const [, shapes$203] = $std_list.push({ b: shapes, x: $r382 });
        shapes = shapes$203;
        const $m383 = a.guard;
        $m383$match: {
          if ($m383.tag === "Some") {
            const value = $m383.value;
            const [$r385, ck$204, ctx$204] = check_expr({ ck: ck, ctx: ctx, e: value, expected: { tag: "Some", value: $types.bool_t } });
            ck = ck$204;
            ctx = ctx$204;
            const gt = $r385;
            break $m383$match;
          }
          if ($m383.tag === "None") {
            skip({  });
            break $m383$match;
          }
          $rt.unreachable();
        }
        const $m386 = a.body;
        $m386$match: {
          if ($m386.tag === "ArmBlock") {
            const block = $m386.block;
            const [, ck$205, ctx$205] = check_block({ ck: ck, ctx: ctx, b: block });
            ck = ck$205;
            ctx = ctx$205;
            break $m386$match;
          }
          if ($m386.tag === "ArmStmt") {
            const stmt = $m386.stmt;
            const [, ck$206, ctx$206] = check_stmt({ ck: ck, ctx: ctx, s: stmt });
            ck = ck$206;
            ctx = ctx$206;
            break $m386$match;
          }
          $rt.unreachable();
        }
      }
      if (st.tag === "ErrorT") {
        return [undefined, ck, ctx];
      }
      const cov = coverage({ ctx: ctx, scrutinee: shape, arms: $std_list.finish({ b: shapes }) });
      for (const i of cov.unreachable) {
        if (i >= 0 && i < $std_list.len({ xs: arms })) {
          const [, ctx$207] = rep({ ck: ck, ctx: ctx, code: "E0327", at: pattern_span({ p: $std_list.get({ xs: arms, i: i }).pattern }), detail: "every value this arm could match is handled by an earlier arm" });
          ctx = ctx$207;
        }
      }
      if (cov.needs_catch_all) {
        const [, ctx$208] = rep({ ck: ck, ctx: ctx, code: "E0326", at: $parser.span_of_expr({ e: scrutinee }), detail: "a `match` on " + show({ ctx: ctx, t: st_full }) + " needs a `_` arm" });
        ctx = ctx$208;
      } else {
        if ($std_list.len({ xs: cov.missing }) > 0) {
          let quoted = $std_list.builder({  });
          for (const m of cov.missing) {
            const [, quoted$209] = $std_list.push({ b: quoted, x: "`" + m + "`" });
            quoted = quoted$209;
          }
          const [, ctx$210] = rep({ ck: ck, ctx: ctx, code: "E0326", at: $parser.span_of_expr({ e: scrutinee }), detail: "missing " + $std_text.join({ parts: $std_list.finish({ b: quoted }), sep: ", " }) });
          ctx = ctx$210;
        }
      }
      break $m373$match;
    }
    if (true) {
      skip({  });
      break $m373$match;
    }
    $rt.unreachable();
  }
  return [undefined, ck, ctx];
  return [undefined, ck, ctx];
}

export function pat_field_tag({ pf }) {
  const $m387 = pf;
  $m387$match: {
    if ($m387.tag === "PatFieldName") {
      return 0;
      break $m387$match;
    }
    if ($m387.tag === "PatFieldSkip") {
      return 1;
      break $m387$match;
    }
    if ($m387.tag === "PatFieldRest") {
      return 2;
      break $m387$match;
    }
    $rt.unreachable();
  }
}

export function pat_field_span({ pf }) {
  const $m388 = pf;
  $m388$match: {
    if ($m388.tag === "PatFieldName") {
      const span = $m388.span;
      return span;
      break $m388$match;
    }
    if ($m388.tag === "PatFieldSkip") {
      const span = $m388.span;
      return span;
      break $m388$match;
    }
    if ($m388.tag === "PatFieldRest") {
      const span = $m388.span;
      return span;
      break $m388$match;
    }
    $rt.unreachable();
  }
}

export function pat_field_ident({ pf }) {
  const $m389 = pf;
  $m389$match: {
    if ($m389.tag === "PatFieldName") {
      const name = $m389.name;
      return name;
      break $m389$match;
    }
    if (true) {
      return { text: "", span: { start: 0, end: 0 } };
      break $m389$match;
    }
    $rt.unreachable();
  }
}

export function subst_of({ ck, ctx, d, args }) {
  let subst = $std_map.dict({  });
  const [$r392, ck$211, ctx$211] = type_params_of({ fuel: max_type_depth, ck: ck, ctx: ctx, d: d });
  ck = ck$211;
  ctx = ctx$211;
  const ps = $r392;
  const $hi393 = $std_list.len({ xs: ps });
  for (let i = 0; i < $hi393; i++) {
    if (i < $std_list.len({ xs: args })) {
      const [, subst$212] = $std_map.set({ d: subst, key: $types.tparam_def({ p: $std_list.get({ xs: ps, i: i }) }), value: $std_list.get({ xs: args, i: i }) });
      subst = subst$212;
    }
  }
  return [subst, ck, ctx];
}

export function plural_fields({ n }) {
  if (n === 1) {
    return "1 field";
  }
  return $std_int.to_text({ x: n }) + " fields";
}

export function check_pattern({ ck, ctx, p, scrutinee, guarded }) {
  const st = $types.strip({ t: scrutinee });
  const $m394 = p;
  $m394$match: {
    if ($m394.tag === "WildcardPat") {
      return [{ tag: "AllShape", guarded: guarded }, ck, ctx];
      break $m394$match;
    }
    if ($m394.tag === "BindPat") {
      const span = $m394.span;
      const [, ctx$213] = set_decl_type({ ctx: ctx, def: def_of_key({ ctx: ctx, k: key({ ck: ck, tag: $defs.tag_pattern, span: span }) }), t: scrutinee });
      ctx = ctx$213;
      return [{ tag: "AllShape", guarded: guarded }, ck, ctx];
      break $m394$match;
    }
    if ($m394.tag === "LitPat") {
      const literal = $m394.literal;
      const span = $m394.span;
      const [$r398, ck$214, ctx$214] = check_expr({ ck: ck, ctx: ctx, e: literal, expected: { tag: "None" } });
      ck = ck$214;
      ctx = ctx$214;
      const lt = $r398;
      if (!$types.assignable({ actual: lt, expected: scrutinee })) {
        const [, ctx$215] = rep({ ck: ck, ctx: ctx, code: "E0341", at: span, detail: "a " + show({ ctx: ctx, t: lt }) + " literal cannot match " + show({ ctx: ctx, t: scrutinee }) });
        ctx = ctx$215;
      }
      const $m399 = literal;
      $m399$match: {
        if ($m399.tag === "BoolLit") {
          const value = $m399.value;
          return [{ tag: "BoolShape", value: value, guarded: guarded }, ck, ctx];
          break $m399$match;
        }
        if (true) {
          return [{ tag: "LitShape", guarded: guarded }, ck, ctx];
          break $m399$match;
        }
        $rt.unreachable();
      }
      break $m394$match;
    }
    if ($m394.tag === "VariantPat") {
      const name = $m394.name;
      const fields = $m394.fields;
      const span = $m394.span;
      const vdef = res_def({ res: ref_at({ ctx: ctx, k: key({ ck: ck, tag: $defs.tag_pattern, span: span }) }) });
      if (vdef < 0) {
        return [{ tag: "AllShape", guarded: true }, ck, ctx];
      }
      const variant = $context.get_def({ ctx: ctx, id: vdef });
      const union_id = or_neg({ o: variant.parent });
      if (st.tag === "ErrorT") {
        return [{ tag: "VariantShape", variant: vdef, guarded: guarded }, ck, ctx];
      }
      if (union_id < 0 || !(st.tag === "UnionT") || $types.def_of({ t: st }) !== union_id) {
        let union_name = "?";
        if (union_id >= 0) {
          union_name = def_name({ ctx: ctx, id: union_id });
        }
        const [, ctx$216] = rep({ ck: ck, ctx: ctx, code: "E0341", at: name.span, detail: "`" + variant.name + "` is a variant of " + union_name + ", but the scrutinee has type " + show({ ctx: ctx, t: scrutinee }) });
        ctx = ctx$216;
        return [{ tag: "VariantShape", variant: vdef, guarded: true }, ck, ctx];
      }
      const [$r405, ck$217, ctx$217] = subst_of({ ck: ck, ctx: ctx, d: $context.get_def({ ctx: ctx, id: union_id }), args: $types.args_of({ t: st }) });
      ck = ck$217;
      ctx = ctx$217;
      const subst = $r405;
      const [$r406, ck$218, ctx$218] = fields_of({ ck: ck, ctx: ctx, d: variant });
      ck = ck$218;
      ctx = ctx$218;
      const fs = $r406;
      const nf = $std_list.len({ xs: fs });
      const $m407 = fields;
      $m407$match: {
        if ($m407.tag === "None") {
          return [{ tag: "VariantShape", variant: vdef, guarded: guarded }, ck, ctx];
          break $m407$match;
        }
        if ($m407.tag === "Some") {
          const value = $m407.value;
          let i = 0;
          let rest = false;
          let stop = false;
          for (const pf of value) {
            if (!stop) {
              if (rest) {
                const [, ctx$219] = rep({ ck: ck, ctx: ctx, code: "E0310", at: pat_field_span({ pf: pf }), detail: "`..` must be the last pattern field" });
                ctx = ctx$219;
                stop = true;
              } else {
                if (pat_field_tag({ pf: pf }) === 2) {
                  rest = true;
                } else {
                  if (i >= nf) {
                    const [, ctx$220] = rep({ ck: ck, ctx: ctx, code: "E0310", at: pat_field_span({ pf: pf }), detail: "`" + variant.name + "` has only " + plural_fields({ n: nf }) });
                    ctx = ctx$220;
                    stop = true;
                  } else {
                    const f = $std_list.get({ xs: fs, i: i });
                    if (pat_field_tag({ pf: pf }) === 0) {
                      const ident = pat_field_ident({ pf: pf });
                      if (ident.text !== f.name) {
                        const [, ctx$221] = rep({ ck: ck, ctx: ctx, code: "E0310", at: ident.span, detail: "field " + $std_int.to_text({ x: $rt.int.add(i, 1, $ob31) }) + " of `" + variant.name + "` is `" + f.name + "`; patterns cannot rename" });
                        ctx = ctx$221;
                      }
                      const [, ctx$222] = set_decl_type({ ctx: ctx, def: def_of_key({ ctx: ctx, k: key({ ck: ck, tag: $defs.tag_pattern, span: pat_field_span({ pf: pf }) }) }), t: $types.substitute({ t: f.ty, subst: subst }) });
                      ctx = ctx$222;
                    }
                    i = $rt.int.add(i, 1, $ob32);
                  }
                }
              }
            }
          }
          if (!stop && !rest && i < nf) {
            const [, ctx$223] = rep({ ck: ck, ctx: ctx, code: "E0310", at: span, detail: "`" + variant.name + "` has " + plural_fields({ n: nf }) + "; list them all or end with `..`" });
            ctx = ctx$223;
          }
          return [{ tag: "VariantShape", variant: vdef, guarded: guarded }, ck, ctx];
          break $m407$match;
        }
        $rt.unreachable();
      }
      break $m394$match;
    }
    $rt.unreachable();
  }
}

export function coerce({ ck, ctx, actual, expected, at, what, at_key }) {
  if (actual.tag === "ErrorT") {
    return [{ tag: "ErrorT" }, ctx];
  }
  if (expected.tag === "ErrorT") {
    return [actual, ctx];
  }
  if (!$types.assignable({ actual: actual, expected: expected })) {
    let suffix = "";
    if (what !== "expression") {
      suffix = " (" + what + ")";
    }
    const [, ctx$224] = rep({ ck: ck, ctx: ctx, code: "E0321", at: at, detail: "expected " + show({ ctx: ctx, t: expected }) + ", found " + show({ ctx: ctx, t: actual }) + suffix });
    ctx = ctx$224;
    return [{ tag: "ErrorT" }, ctx];
  }
  const $m412 = at_key;
  $m412$match: {
    if ($m412.tag === "Some") {
      const value = $m412.value;
      if ($types.is_refined({ t: expected }) && !$types.same_refinement({ a: actual, b: expected })) {
        let flows = ctx.refinement_flows;
        const [, flows$225] = $std_list.push({ b: flows, x: { at: value, from: actual, to: expected } });
        flows = flows$225;
      }
      break $m412$match;
    }
    if ($m412.tag === "None") {
      skip({  });
      break $m412$match;
    }
    $rt.unreachable();
  }
  return [actual, ctx];
}

export function infer({ ck, ctx, e }) {
  const [$r415, ck$226, ctx$226] = check_expr({ ck: ck, ctx: ctx, e: e, expected: { tag: "None" } });
  ck = ck$226;
  ctx = ctx$226;
  return [$r415, ck, ctx];
}

export function check_expr({ ck, ctx, e, expected }) {
  const [$r416, ck$227, ctx$227] = expr_inner({ ck: ck, ctx: ctx, e: e, expected: expected });
  ck = ck$227;
  ctx = ctx$227;
  const t = $r416;
  const k = key({ ck: ck, tag: $defs.tag_expr, span: $parser.span_of_expr({ e: e }) });
  let res_t = t;
  const $m417 = expected;
  $m417$match: {
    if ($m417.tag === "None") {
      res_t = t;
      break $m417$match;
    }
    if ($m417.tag === "Some") {
      const value = $m417.value;
      const [$r419, ctx$228] = coerce({ ck: ck, ctx: ctx, actual: t, expected: value, at: $parser.span_of_expr({ e: e }), what: "expression", at_key: { tag: "Some", value: k } });
      ctx = ctx$228;
      res_t = $r419;
      break $m417$match;
    }
    $rt.unreachable();
  }
  let table = ctx.expr_types;
  const [, table$229] = $std_map.set({ d: table, key: k, value: res_t });
  table = table$229;
  return [res_t, ck, ctx];
}

export function set_expr_type({ ck, ctx, e, t }) {
  let table = ctx.expr_types;
  const [, table$230] = $std_map.set({ d: table, key: key({ ck: ck, tag: $defs.tag_expr, span: $parser.span_of_expr({ e: e }) }), value: t });
  table = table$230;
  return [undefined, ctx];
  return [undefined, ctx];
}

export function it_top({ ck }) {
  const n = $std_list.len({ xs: ck.it_stack });
  if (n === 0) {
    return { tag: "ErrorT" };
  }
  return $std_list.get({ xs: ck.it_stack, i: $rt.int.sub(n, 1, $ob33) });
}

export function expr_inner({ ck, ctx, e, expected }) {
  const $m421 = e;
  $m421$match: {
    if ($m421.tag === "IntLit") {
      return [$types.int_t, ck, ctx];
      break $m421$match;
    }
    if ($m421.tag === "FloatLit") {
      return [$types.float_t, ck, ctx];
      break $m421$match;
    }
    if ($m421.tag === "TextLit") {
      return [$types.text_t, ck, ctx];
      break $m421$match;
    }
    if ($m421.tag === "BoolLit") {
      return [$types.bool_t, ck, ctx];
      break $m421$match;
    }
    if ($m421.tag === "DurationLit") {
      return [$types.duration_t, ck, ctx];
      break $m421$match;
    }
    if ($m421.tag === "Name") {
      const [$r422, ck$231, ctx$231] = name_type({ ck: ck, ctx: ctx, e: e });
      ck = ck$231;
      ctx = ctx$231;
      return [$r422, ck, ctx];
      break $m421$match;
    }
    if ($m421.tag === "It") {
      return [it_top({ ck: ck }), ck, ctx];
      break $m421$match;
    }
    if ($m421.tag === "ResultRef") {
      return [or_error({ o: fn_ret({ ck: ck }) }), ck, ctx];
      break $m421$match;
    }
    if ($m421.tag === "Old") {
      const span = $m421.span;
      const def = res_def({ res: ref_at({ ctx: ctx, k: key({ ck: ck, tag: $defs.tag_expr, span: span }) }) });
      if (def < 0) {
        return [{ tag: "ErrorT" }, ck, ctx];
      }
      return [decl_type({ ctx: ctx, def: def }), ck, ctx];
      break $m421$match;
    }
    if ($m421.tag === "Ctor") {
      const [$r424, ck$232, ctx$232] = ctor({ ck: ck, ctx: ctx, e: e, expected: expected });
      ck = ck$232;
      ctx = ctx$232;
      return [$r424, ck, ctx];
      break $m421$match;
    }
    if ($m421.tag === "RecordUpdate") {
      const [$r425, ck$233, ctx$233] = record_update({ ck: ck, ctx: ctx, e: e });
      ck = ck$233;
      ctx = ctx$233;
      return [$r425, ck, ctx];
      break $m421$match;
    }
    if ($m421.tag === "ListLit") {
      const [$r426, ck$234, ctx$234] = list_lit({ ck: ck, ctx: ctx, e: e, expected: expected });
      ck = ck$234;
      ctx = ctx$234;
      return [$r426, ck, ctx];
      break $m421$match;
    }
    if ($m421.tag === "Try") {
      const [$r427, ck$235, ctx$235] = try_expr({ ck: ck, ctx: ctx, e: e });
      ck = ck$235;
      ctx = ctx$235;
      return [$r427, ck, ctx];
      break $m421$match;
    }
    if ($m421.tag === "Recover") {
      const [$r428, ck$236, ctx$236] = recover_expr({ ck: ck, ctx: ctx, e: e });
      ck = ck$236;
      ctx = ctx$236;
      return [$r428, ck, ctx];
      break $m421$match;
    }
    if ($m421.tag === "Quantifier") {
      const [$r429, ck$237, ctx$237] = quantifier({ ck: ck, ctx: ctx, e: e });
      ck = ck$237;
      ctx = ctx$237;
      return [$r429, ck, ctx];
      break $m421$match;
    }
    if ($m421.tag === "Closure") {
      const [$r430, ck$238, ctx$238] = closure({ ck: ck, ctx: ctx, e: e });
      ck = ck$238;
      ctx = ctx$238;
      return [$r430, ck, ctx];
      break $m421$match;
    }
    if ($m421.tag === "Fake") {
      const [$r431, ck$239, ctx$239] = fake_expr({ ck: ck, ctx: ctx, e: e, expected: expected });
      ck = ck$239;
      ctx = ctx$239;
      return [$r431, ck, ctx];
      break $m421$match;
    }
    if ($m421.tag === "Hole") {
      const span = $m421.span;
      let table = ctx.type_holes;
      const [, table$240] = $std_map.set({ d: table, key: key({ ck: ck, tag: $defs.tag_expr, span: span }), value: expected });
      table = table$240;
      return [{ tag: "ErrorT" }, ck, ctx];
      break $m421$match;
    }
    if ($m421.tag === "FieldAccess") {
      const [$r433, ck$241, ctx$241] = field_access({ ck: ck, ctx: ctx, e: e });
      ck = ck$241;
      ctx = ctx$241;
      return [$r433, ck, ctx];
      break $m421$match;
    }
    if ($m421.tag === "Call") {
      const [$r434, ck$242, ctx$242] = call({ ck: ck, ctx: ctx, e: e, expected: expected });
      ck = ck$242;
      ctx = ctx$242;
      return [$r434, ck, ctx];
      break $m421$match;
    }
    if ($m421.tag === "Unary") {
      const op = $m421.op;
      const operand = $m421.operand;
      const span = $m421.span;
      if (op === "not") {
        const [$r436, ck$243, ctx$243] = check_expr({ ck: ck, ctx: ctx, e: operand, expected: { tag: "Some", value: $types.bool_t } });
        ck = ck$243;
        ctx = ctx$243;
        const bt = $r436;
        return [$types.bool_t, ck, ctx];
      }
      const [$r437, ck$244, ctx$244] = infer({ ck: ck, ctx: ctx, e: operand });
      ck = ck$244;
      ctx = ctx$244;
      const t = $r437;
      if (!$types.is_numeric({ t: t })) {
        if (!(t.tag === "ErrorT")) {
          const [, ctx$245] = rep({ ck: ck, ctx: ctx, code: "E0340", at: span, detail: "unary `-` needs Int, Float or Duration, not " + show({ ctx: ctx, t: t }) });
          ctx = ctx$245;
        }
        return [{ tag: "ErrorT" }, ck, ctx];
      }
      return [$types.strip({ t: t }), ck, ctx];
      break $m421$match;
    }
    if ($m421.tag === "Binary") {
      const [$r439, ck$246, ctx$246] = binary({ ck: ck, ctx: ctx, e: e });
      ck = ck$246;
      ctx = ctx$246;
      return [$r439, ck, ctx];
      break $m421$match;
    }
    if ($m421.tag === "And") {
      const operands = $m421.operands;
      for (const o of operands) {
        const [$r441, ck$247, ctx$247] = check_expr({ ck: ck, ctx: ctx, e: o, expected: { tag: "Some", value: $types.bool_t } });
        ck = ck$247;
        ctx = ctx$247;
        const bt = $r441;
      }
      return [$types.bool_t, ck, ctx];
      break $m421$match;
    }
    if ($m421.tag === "Or") {
      const operands = $m421.operands;
      for (const o of operands) {
        const [$r443, ck$248, ctx$248] = check_expr({ ck: ck, ctx: ctx, e: o, expected: { tag: "Some", value: $types.bool_t } });
        ck = ck$248;
        ctx = ctx$248;
        const bt = $r443;
      }
      return [$types.bool_t, ck, ctx];
      break $m421$match;
    }
    if ($m421.tag === "Is") {
      const expr = $m421.expr;
      const pattern = $m421.pattern;
      const [$r444, ck$249, ctx$249] = infer({ ck: ck, ctx: ctx, e: expr });
      ck = ck$249;
      ctx = ctx$249;
      const t = $r444;
      const [$r445, ck$250, ctx$250] = check_pattern({ ck: ck, ctx: ctx, p: pattern, scrutinee: t, guarded: false });
      ck = ck$250;
      ctx = ctx$250;
      const shape = $r445;
      return [$types.bool_t, ck, ctx];
      break $m421$match;
    }
    $rt.unreachable();
  }
}

export function fn_value_type({ ck, ctx, d, at }) {
  const [$r446, ck$251, ctx$251] = signature_of({ ck: ck, ctx: ctx, d: d });
  ck = ck$251;
  ctx = ctx$251;
  const sig = $r446;
  if ($std_list.len({ xs: sig.tparams }) > 0) {
    const [, ctx$252] = rep({ ck: ck, ctx: ctx, code: "E0324", at: at, detail: "`" + d.name + "` is generic; a generic function cannot be used as a value" });
    ctx = ctx$252;
    return [{ tag: "ErrorT" }, ck, ctx];
  }
  return [{ tag: "FnT", params: sig.params, ret: sig.ret, effects: sig.effects }, ck, ctx];
}

export function name_type({ ck, ctx, e }) {
  const $m449 = e;
  $m449$match: {
    if ($m449.tag === "Name") {
      const name = $m449.name;
      const span = $m449.span;
      const def = res_def({ res: ref_at({ ctx: ctx, k: key({ ck: ck, tag: $defs.tag_expr, span: span }) }) });
      if (def < 0) {
        return [{ tag: "ErrorT" }, ck, ctx];
      }
      const d = $context.get_def({ ctx: ctx, id: def });
      if ($rt.eq(d.kind, { tag: "Fn" })) {
        const [$r452, ck$253, ctx$253] = fn_value_type({ ck: ck, ctx: ctx, d: d, at: span });
        ck = ck$253;
        ctx = ctx$253;
        return [$r452, ck, ctx];
      }
      if ($rt.eq(d.kind, { tag: "Const" })) {
        const [$r454, ck$254, ctx$254] = const_decl_type({ ck: ck, ctx: ctx, d: d });
        ck = ck$254;
        ctx = ctx$254;
        return [$r454, ck, ctx];
      }
      const t = decl_type({ ctx: ctx, def: def });
      if (fn_top({ ck: ck }).tag === "Some" && d.frame >= 0 && d.frame < fn_frame({ ck: ck }) && $types.strip({ t: t }).tag === "Capability") {
        const [, ctx$255] = rep({ ck: ck, ctx: ctx, code: "E0330", at: span, detail: "`" + d.name + "` is a capability; closures may not capture capabilities (§8)" });
        ctx = ctx$255;
      }
      return [t, ck, ctx];
      break $m449$match;
    }
    if (true) {
      return [{ tag: "ErrorT" }, ck, ctx];
      break $m449$match;
    }
    $rt.unreachable();
  }
}

export function is_arith({ op }) {
  return op === "+" || op === "-" || op === "*" || op === "/" || op === "%";
}

export function is_order({ op }) {
  return op === "<" || op === "<=" || op === ">" || op === ">=";
}

export function binary({ ck, ctx, e }) {
  const $m456 = e;
  $m456$match: {
    if ($m456.tag === "Binary") {
      const op = $m456.op;
      const left = $m456.left;
      const right = $m456.right;
      const span = $m456.span;
      if (is_arith({ op: op })) {
        const [$r457, ck$256, ctx$256] = infer({ ck: ck, ctx: ctx, e: left });
        ck = ck$256;
        ctx = ctx$256;
        const l = $r457;
        if (l.tag === "ErrorT") {
          const [$r458, ck$257, ctx$257] = infer({ ck: ck, ctx: ctx, e: right });
          ck = ck$257;
          ctx = ctx$257;
          const rt = $r458;
          return [{ tag: "ErrorT" }, ck, ctx];
        }
        if (!$types.is_numeric({ t: l })) {
          const [, ctx$258] = rep({ ck: ck, ctx: ctx, code: "E0340", at: $parser.span_of_expr({ e: left }), detail: "`" + op + "` needs Int, Float or Duration operands, not " + show({ ctx: ctx, t: l }) });
          ctx = ctx$258;
          const [$r460, ck$259, ctx$259] = infer({ ck: ck, ctx: ctx, e: right });
          ck = ck$259;
          ctx = ctx$259;
          const rt = $r460;
          return [{ tag: "ErrorT" }, ck, ctx];
        }
        const base = $types.strip({ t: l });
        const [$r463, ck$260, ctx$260] = check_expr({ ck: ck, ctx: ctx, e: right, expected: { tag: "Some", value: base } });
        ck = ck$260;
        ctx = ctx$260;
        const rt = $r463;
        return [base, ck, ctx];
      }
      if (op === "++") {
        const [$r464, ck$261, ctx$261] = infer({ ck: ck, ctx: ctx, e: left });
        ck = ck$261;
        ctx = ctx$261;
        const l = $r464;
        if (l.tag === "ErrorT") {
          const [$r465, ck$262, ctx$262] = infer({ ck: ck, ctx: ctx, e: right });
          ck = ck$262;
          ctx = ctx$262;
          const rt = $r465;
          return [{ tag: "ErrorT" }, ck, ctx];
        }
        const base = $types.strip({ t: l });
        const [$r467, ctx$263] = is_list_type({ ctx: ctx, t: base });
        ctx = ctx$263;
        if (!$types.same_base({ a: base, b: $types.text_t }) && !$r467) {
          const [, ctx$264] = rep({ ck: ck, ctx: ctx, code: "E0340", at: $parser.span_of_expr({ e: left }), detail: "`++` joins Text or List values, not " + show({ ctx: ctx, t: l }) });
          ctx = ctx$264;
          const [$r468, ck$265, ctx$265] = infer({ ck: ck, ctx: ctx, e: right });
          ck = ck$265;
          ctx = ctx$265;
          const rt = $r468;
          return [{ tag: "ErrorT" }, ck, ctx];
        }
        const [$r471, ck$266, ctx$266] = check_expr({ ck: ck, ctx: ctx, e: right, expected: { tag: "Some", value: base } });
        ck = ck$266;
        ctx = ctx$266;
        const rt = $r471;
        return [base, ck, ctx];
      }
      if (op === "==" || op === "!=") {
        const [$r472, ck$267, ctx$267] = infer({ ck: ck, ctx: ctx, e: left });
        ck = ck$267;
        ctx = ctx$267;
        const l = $r472;
        if ($types.strip({ t: l }).tag === "FnT") {
          const [, ctx$268] = rep({ ck: ck, ctx: ctx, code: "E0340", at: span, detail: "function values cannot be compared (§3.7)" });
          ctx = ctx$268;
        }
        let want = $types.strip({ t: l });
        if (l.tag === "ErrorT") {
          want = l;
        }
        const [$r474, ck$269, ctx$269] = check_expr({ ck: ck, ctx: ctx, e: right, expected: { tag: "Some", value: want } });
        ck = ck$269;
        ctx = ctx$269;
        const rt = $r474;
        return [$types.bool_t, ck, ctx];
      }
      if (is_order({ op: op })) {
        const [$r475, ck$270, ctx$270] = infer({ ck: ck, ctx: ctx, e: left });
        ck = ck$270;
        ctx = ctx$270;
        const l = $r475;
        if (!(l.tag === "ErrorT") && !$types.is_numeric({ t: l })) {
          const [, ctx$271] = rep({ ck: ck, ctx: ctx, code: "E0340", at: $parser.span_of_expr({ e: left }), detail: "`" + op + "` compares Int, Float or Duration values, not " + show({ ctx: ctx, t: l }) });
          ctx = ctx$271;
          const [$r476, ck$272, ctx$272] = infer({ ck: ck, ctx: ctx, e: right });
          ck = ck$272;
          ctx = ctx$272;
          const rt = $r476;
          return [$types.bool_t, ck, ctx];
        }
        let want = $types.strip({ t: l });
        if (l.tag === "ErrorT") {
          want = l;
        }
        const [$r478, ck$273, ctx$273] = check_expr({ ck: ck, ctx: ctx, e: right, expected: { tag: "Some", value: want } });
        ck = ck$273;
        ctx = ctx$273;
        const rt = $r478;
        return [$types.bool_t, ck, ctx];
      }
      const [$r480, ck$274, ctx$274] = check_expr({ ck: ck, ctx: ctx, e: left, expected: { tag: "Some", value: $types.bool_t } });
      ck = ck$274;
      ctx = ctx$274;
      const lt = $r480;
      const [$r482, ck$275, ctx$275] = check_expr({ ck: ck, ctx: ctx, e: right, expected: { tag: "Some", value: $types.bool_t } });
      ck = ck$275;
      ctx = ctx$275;
      const rt = $r482;
      return [$types.bool_t, ck, ctx];
      break $m456$match;
    }
    if (true) {
      return [{ tag: "ErrorT" }, ck, ctx];
      break $m456$match;
    }
    $rt.unreachable();
  }
}

export function field_params({ fs }) {
  let out = $std_list.builder({  });
  for (const f of fs) {
    const [, out$276] = $std_list.push({ b: out, x: { name: f.name, is_inout: false, ty: f.ty } });
    out = out$276;
  }
  return $std_list.finish({ b: out });
}

export function ctor({ ck, ctx, e, expected }) {
  const $m485 = e;
  $m485$match: {
    if ($m485.tag === "Ctor") {
      const args = $m485.args;
      const fields = $m485.fields;
      const span = $m485.span;
      const res = ref_at({ ctx: ctx, k: key({ ck: ck, tag: $defs.tag_expr, span: span }) });
      const tag = res_tag({ res: res });
      if (tag === 2) {
        return [$types.unit_t, ck, ctx];
      }
      if (tag === 4) {
        return [{ tag: "TypeInfoT" }, ck, ctx];
      }
      if (tag !== 0) {
        return [{ tag: "ErrorT" }, ck, ctx];
      }
      const d = $context.get_def({ ctx: ctx, id: res_def({ res: res }) });
      if ($rt.eq(d.kind, { tag: "Variant" })) {
        const union_id = or_neg({ o: d.parent });
        if (union_id < 0) {
          return [{ tag: "ErrorT" }, ck, ctx];
        }
        if (fields.tag === "Some") {
          const [, ctx$277] = rep({ ck: ck, ctx: ctx, code: "E0322", at: span, detail: "`" + d.name + "` is a variant; give its fields as named arguments in parentheses" });
          ctx = ctx$277;
          return [{ tag: "ErrorT" }, ck, ctx];
        }
        const union_def = $context.get_def({ ctx: ctx, id: union_id });
        const [$r491, ck$278, ctx$278] = bind_from_expected({ ck: ck, ctx: ctx, d: union_def, expected: expected });
        ck = ck$278;
        ctx = ctx$278;
        const subst = $r491;
        const [$r492, ck$279, ctx$279] = fields_of({ ck: ck, ctx: ctx, d: d });
        ck = ck$279;
        ctx = ctx$279;
        const fs = $r492;
        const vparams = field_params({ fs: fs });
        const [$r493, ck$280, ctx$280] = type_params_of({ fuel: max_type_depth, ck: ck, ctx: ctx, d: union_def });
        ck = ck$280;
        ctx = ctx$280;
        const vtparams = $r493;
        const $m494 = args;
        $m494$match: {
          if ($m494.tag === "Some") {
            const value = $m494.value;
            const [, ck$281, ctx$281] = match_named({ ck: ck, ctx: ctx, args: value, params: vparams, subst: subst, tparams: vtparams, at: span, what: "variant `" + d.name + "`" });
            ck = ck$281;
            ctx = ctx$281;
            break $m494$match;
          }
          if ($m494.tag === "None") {
            const [, ctx$282] = report_missing({ ck: ck, ctx: ctx, params: vparams, seen: $std_map.dict({  }), at: span, what: "variant `" + d.name + "`" });
            ctx = ctx$282;
            break $m494$match;
          }
          $rt.unreachable();
        }
        const [$r495, ck$283, ctx$283] = finish_args({ ck: ck, ctx: ctx, d: union_def, subst: subst, at: span });
        ck = ck$283;
        ctx = ctx$283;
        return [{ tag: "UnionT", def: union_id, args: $r495 }, ck, ctx];
      }
      if ($rt.eq(d.kind, { tag: "Record" })) {
        if (args.tag === "Some") {
          const [, ctx$284] = rep({ ck: ck, ctx: ctx, code: "E0322", at: span, detail: "`" + d.name + "` is a record; give its fields in braces" });
          ctx = ctx$284;
          return [{ tag: "ErrorT" }, ck, ctx];
        }
        const [$r499, ck$285, ctx$285] = bind_from_expected({ ck: ck, ctx: ctx, d: d, expected: expected });
        ck = ck$285;
        ctx = ctx$285;
        const subst = $r499;
        const [$r500, ck$286, ctx$286] = fields_of({ ck: ck, ctx: ctx, d: d });
        ck = ck$286;
        ctx = ctx$286;
        const fs = $r500;
        const rparams = field_params({ fs: fs });
        const [$r501, ck$287, ctx$287] = type_params_of({ fuel: max_type_depth, ck: ck, ctx: ctx, d: d });
        ck = ck$287;
        ctx = ctx$287;
        const rtparams = $r501;
        const $m502 = fields;
        $m502$match: {
          if ($m502.tag === "Some") {
            const value = $m502.value;
            const [, ck$288, ctx$288] = match_named_fields({ ck: ck, ctx: ctx, inits: value, params: rparams, subst: subst, tparams: rtparams, at: span, what: "record `" + d.name + "`" });
            ck = ck$288;
            ctx = ctx$288;
            break $m502$match;
          }
          if ($m502.tag === "None") {
            const [, ctx$289] = report_missing({ ck: ck, ctx: ctx, params: rparams, seen: $std_map.dict({  }), at: span, what: "record `" + d.name + "`" });
            ctx = ctx$289;
            break $m502$match;
          }
          $rt.unreachable();
        }
        const [$r503, ck$290, ctx$290] = finish_args({ ck: ck, ctx: ctx, d: d, subst: subst, at: span });
        ck = ck$290;
        ctx = ctx$290;
        return [{ tag: "RecordT", def: d.id, args: $r503 }, ck, ctx];
      }
      return [{ tag: "ErrorT" }, ck, ctx];
      break $m485$match;
    }
    if (true) {
      return [{ tag: "ErrorT" }, ck, ctx];
      break $m485$match;
    }
    $rt.unreachable();
  }
}

export function bind_from_expected({ ck, ctx, d, expected }) {
  let subst = $std_map.dict({  });
  const $m507 = expected;
  $m507$match: {
    if ($m507.tag === "None") {
      return [subst, ck, ctx];
      break $m507$match;
    }
    if ($m507.tag === "Some") {
      const value = $m507.value;
      const s = $types.strip({ t: value });
      if ((s.tag === "RecordT" || s.tag === "UnionT" || s.tag === "Opaque") && $types.def_of({ t: s }) === d.id) {
        const [$r508, ck$291, ctx$291] = type_params_of({ fuel: max_type_depth, ck: ck, ctx: ctx, d: d });
        ck = ck$291;
        ctx = ctx$291;
        const ps = $r508;
        const args = $types.args_of({ t: s });
        const $hi509 = $std_list.len({ xs: ps });
        for (let i = 0; i < $hi509; i++) {
          if (i < $std_list.len({ xs: args })) {
            const [, subst$292] = $std_map.set({ d: subst, key: $types.tparam_def({ p: $std_list.get({ xs: ps, i: i }) }), value: $std_list.get({ xs: args, i: i }) });
            subst = subst$292;
          }
        }
      }
      return [subst, ck, ctx];
      break $m507$match;
    }
    $rt.unreachable();
  }
}

export function finish_args({ ck, ctx, d, subst, at }) {
  let out = $std_list.builder({  });
  const [$r510, ck$293, ctx$293] = type_params_of({ fuel: max_type_depth, ck: ck, ctx: ctx, d: d });
  ck = ck$293;
  ctx = ctx$293;
  for (const p of $r510) {
    const $m511 = $std_map.find({ d: subst, key: $types.tparam_def({ p: p }) });
    $m511$match: {
      if ($m511.tag === "Some") {
        const value = $m511.value;
        const [, out$294] = $std_list.push({ b: out, x: value });
        out = out$294;
        break $m511$match;
      }
      if ($m511.tag === "None") {
        const [, ctx$295] = rep({ ck: ck, ctx: ctx, code: "E0324", at: at, detail: "cannot determine `" + def_name({ ctx: ctx, id: $types.tparam_def({ p: p }) }) + "` of `" + d.name + "`; annotate the binding or pass the type argument" });
        ctx = ctx$295;
        if (p.tag === "TypeP") {
          const [, out$296] = $std_list.push({ b: out, x: { tag: "TypeA", ty: { tag: "ErrorT" } } });
          out = out$296;
        } else {
          const [, out$297] = $std_list.push({ b: out, x: { tag: "ConstA", value: { tag: "UnitV" } } });
          out = out$297;
        }
        break $m511$match;
      }
      $rt.unreachable();
    }
  }
  return [$std_list.finish({ b: out }), ck, ctx];
}

export function find_param({ params, name }) {
  for (const p of params) {
    if (p.name === name) {
      return { tag: "Some", value: p };
    }
  }
  return { tag: "None" };
}

export function tvars_of({ tparams }) {
  let tvars = $std_map.dict({  });
  for (const p of tparams) {
    const [, tvars$298] = $std_map.set({ d: tvars, key: $types.tparam_def({ p: p }), value: true });
    tvars = tvars$298;
  }
  return tvars;
}

export function missing_params({ params, seen }) {
  let out = $std_list.builder({  });
  for (const p of params) {
    if (!$std_map.contains({ d: seen, key: p.name })) {
      const [, out$299] = $std_list.push({ b: out, x: "`" + p.name + "`" });
      out = out$299;
    }
  }
  return $std_list.finish({ b: out });
}

export function report_missing({ ck, ctx, params, seen, at, what }) {
  const missing = missing_params({ params: params, seen: seen });
  if ($std_list.len({ xs: missing }) > 0) {
    const [, ctx$300] = rep({ ck: ck, ctx: ctx, code: "E0322", at: at, detail: what + " is missing " + $std_text.join({ parts: missing, sep: ", " }) });
    ctx = ctx$300;
  }
  return [undefined, ctx];
  return [undefined, ctx];
}

export function expr_text({ e }) {
  const $m518 = e;
  $m518$match: {
    if ($m518.tag === "Name") {
      const name = $m518.name;
      return name.text;
      break $m518$match;
    }
    if (true) {
      return "…";
      break $m518$match;
    }
    $rt.unreachable();
  }
}

export function check_arg_value({ ck, ctx, value, p, subst, tvars }) {
  const pt = $types.substitute({ t: p.ty, subst: subst });
  if ($types.has_unbound({ t: pt, tvars: tvars, subst: subst })) {
    const [$r519, ck$301, ctx$301] = infer({ ck: ck, ctx: ctx, e: value });
    ck = ck$301;
    ctx = ctx$301;
    const actual = $r519;
    if (!unify({ pattern: pt, actual: actual, subst: subst, tvars: tvars })) {
      const [, ctx$302] = rep({ ck: ck, ctx: ctx, code: "E0321", at: $parser.span_of_expr({ e: value }), detail: "expected " + show({ ctx: ctx, t: $types.substitute({ t: pt, subst: subst }) }) + ", found " + show({ ctx: ctx, t: actual }) });
      ctx = ctx$302;
    }
  } else {
    const [$r521, ck$303, ctx$303] = check_expr({ ck: ck, ctx: ctx, e: value, expected: { tag: "Some", value: pt } });
    ck = ck$303;
    ctx = ctx$303;
    const at = $r521;
  }
  return [undefined, ck, ctx];
  return [undefined, ck, ctx];
}

export function match_named({ ck, ctx, args, params, subst, tparams, at, what }) {
  const tvars = tvars_of({ tparams: tparams });
  let seen = $std_map.dict({  });
  let inout_vars = $std_map.dict({  });
  for (const a of args) {
    const $m522 = find_param({ params: params, name: a.name.text });
    $m522$match: {
      if ($m522.tag === "None") {
        const [, ctx$304] = rep({ ck: ck, ctx: ctx, code: "E0322", at: a.name.span, detail: what + " has no parameter `" + a.name.text + "`" });
        ctx = ctx$304;
        const [$r523, ck$305, ctx$305] = infer({ ck: ck, ctx: ctx, e: a.value });
        ck = ck$305;
        ctx = ctx$305;
        const it_ = $r523;
        break $m522$match;
      }
      if ($m522.tag === "Some") {
        const value = $m522.value;
        const [, seen$306] = $std_map.set({ d: seen, key: value.name, value: true });
        seen = seen$306;
        if (value.is_inout !== a.is_inout) {
          if (value.is_inout) {
            const [, ctx$307] = rep({ ck: ck, ctx: ctx, code: "E0329", at: a.span, detail: "`" + value.name + "` is an `inout` parameter; write `" + value.name + ": inout " + expr_text({ e: a.value }) + "`" });
            ctx = ctx$307;
          } else {
            const [, ctx$308] = rep({ ck: ck, ctx: ctx, code: "E0329", at: a.span, detail: "`" + value.name + "` is not an `inout` parameter" });
            ctx = ctx$308;
          }
        }
        if (a.is_inout) {
          const [, ctx$309] = check_inout_arg({ ck: ck, ctx: ctx, a: a, inout_vars: inout_vars });
          ctx = ctx$309;
        }
        const [, ck$310, ctx$310] = check_arg_value({ ck: ck, ctx: ctx, value: a.value, p: value, subst: subst, tvars: tvars });
        ck = ck$310;
        ctx = ctx$310;
        break $m522$match;
      }
      $rt.unreachable();
    }
  }
  const missing = missing_params({ params: params, seen: seen });
  if ($std_list.len({ xs: missing }) > 0) {
    const [, ctx$311] = rep({ ck: ck, ctx: ctx, code: "E0322", at: at, detail: what + " is missing " + $std_text.join({ parts: missing, sep: ", " }) });
    ctx = ctx$311;
  }
  return [undefined, ck, ctx];
  return [undefined, ck, ctx];
}

export function match_named_fields({ ck, ctx, inits, params, subst, tparams, at, what }) {
  const tvars = tvars_of({ tparams: tparams });
  let seen = $std_map.dict({  });
  for (const f of inits) {
    const $m524 = find_param({ params: params, name: f.name.text });
    $m524$match: {
      if ($m524.tag === "None") {
        const [, ctx$312] = rep({ ck: ck, ctx: ctx, code: "E0322", at: f.name.span, detail: what + " has no parameter `" + f.name.text + "`" });
        ctx = ctx$312;
        const [$r525, ck$313, ctx$313] = infer({ ck: ck, ctx: ctx, e: f.value });
        ck = ck$313;
        ctx = ctx$313;
        const it_ = $r525;
        break $m524$match;
      }
      if ($m524.tag === "Some") {
        const value = $m524.value;
        const [, seen$314] = $std_map.set({ d: seen, key: value.name, value: true });
        seen = seen$314;
        const [, ck$315, ctx$315] = check_arg_value({ ck: ck, ctx: ctx, value: f.value, p: value, subst: subst, tvars: tvars });
        ck = ck$315;
        ctx = ctx$315;
        break $m524$match;
      }
      $rt.unreachable();
    }
  }
  const missing = missing_params({ params: params, seen: seen });
  if ($std_list.len({ xs: missing }) > 0) {
    const [, ctx$316] = rep({ ck: ck, ctx: ctx, code: "E0322", at: at, detail: what + " is missing " + $std_text.join({ parts: missing, sep: ", " }) });
    ctx = ctx$316;
  }
  return [undefined, ck, ctx];
  return [undefined, ck, ctx];
}

export function check_inout_arg({ ck, ctx, a, inout_vars }) {
  let def = $rt.int.neg(1, $ob34);
  if (a.value.tag === "Name") {
    def = res_def({ res: ref_at({ ctx: ctx, k: key({ ck: ck, tag: $defs.tag_expr, span: $parser.span_of_expr({ e: a.value }) }) }) });
  }
  let ok = def >= 0;
  if (ok) {
    const d = $context.get_def({ ctx: ctx, id: def });
    ok = $rt.eq(d.kind, { tag: "Var" }) || $rt.eq(d.kind, { tag: "Param" }) && d.is_inout;
  }
  if (!ok) {
    const [, ctx$317] = rep({ ck: ck, ctx: ctx, code: "E0329", at: $parser.span_of_expr({ e: a.value }), detail: "an `inout` argument must be a `var` or an `inout` parameter" });
    ctx = ctx$317;
    return [undefined, ctx];
  }
  if ($std_map.contains({ d: inout_vars, key: def })) {
    const [, ctx$318] = rep({ ck: ck, ctx: ctx, code: "E0329", at: $parser.span_of_expr({ e: a.value }), detail: "`" + def_name({ ctx: ctx, id: def }) + "` is passed `inout` twice in one call" });
    ctx = ctx$318;
  }
  let vars = inout_vars;
  const [, vars$319] = $std_map.set({ d: vars, key: def, value: true });
  vars = vars$319;
  return [undefined, ctx];
  return [undefined, ctx];
}

export function unify({ pattern, actual, subst, tvars }) {
  let st = subst;
  const $m528 = pattern;
  $m528$match: {
    if ($m528.tag === "Refined") {
      const base = $m528.base;
      return unify({ pattern: base, actual: actual, subst: subst, tvars: tvars });
      break $m528$match;
    }
    if (true) {
      skip({  });
      break $m528$match;
    }
    $rt.unreachable();
  }
  const a = $types.strip({ t: actual });
  if (a.tag === "ErrorT" || pattern.tag === "ErrorT") {
    return true;
  }
  const $m529 = pattern;
  $m529$match: {
    if ($m529.tag === "ParamT") {
      const def = $m529.def;
      if ($std_map.contains({ d: tvars, key: def })) {
        const $m530 = $std_map.find({ d: subst, key: def });
        $m530$match: {
          if ($m530.tag === "None") {
            const [, st$320] = $std_map.set({ d: st, key: def, value: { tag: "TypeA", ty: actual } });
            st = st$320;
            return true;
            break $m530$match;
          }
          if ($m530.tag === "Some") {
            const value = $m530.value;
            return value.tag === "TypeA" && $types.assignable({ actual: actual, expected: $types.arg_type({ a: value }) });
            break $m530$match;
          }
          $rt.unreachable();
        }
      }
      return a.tag === "ParamT" && $types.def_of({ t: a }) === def;
      break $m529$match;
    }
    if (true) {
      skip({  });
      break $m529$match;
    }
    $rt.unreachable();
  }
  if ($types.kind_tag({ t: pattern }) !== $types.kind_tag({ t: a })) {
    return false;
  }
  const $m532 = pattern;
  $m532$match: {
    if ($m532.tag === "Prim") {
      const name = $m532.name;
      return name === $types.prim_name({ t: a });
      break $m532$match;
    }
    if ($m532.tag === "RecordT") {
      const def = $m532.def;
      const args = $m532.args;
      return unify_args({ pargs: args, aargs: $types.args_of({ t: a }), same_def: def === $types.def_of({ t: a }), subst: subst, tvars: tvars });
      break $m532$match;
    }
    if ($m532.tag === "UnionT") {
      const def = $m532.def;
      const args = $m532.args;
      return unify_args({ pargs: args, aargs: $types.args_of({ t: a }), same_def: def === $types.def_of({ t: a }), subst: subst, tvars: tvars });
      break $m532$match;
    }
    if ($m532.tag === "Opaque") {
      const def = $m532.def;
      const args = $m532.args;
      return unify_args({ pargs: args, aargs: $types.args_of({ t: a }), same_def: def === $types.def_of({ t: a }), subst: subst, tvars: tvars });
      break $m532$match;
    }
    if ($m532.tag === "Capability") {
      const def = $m532.def;
      const args = $m532.args;
      return unify_args({ pargs: args, aargs: $types.args_of({ t: a }), same_def: def === $types.def_of({ t: a }), subst: subst, tvars: tvars });
      break $m532$match;
    }
    if ($m532.tag === "FnT") {
      const params = $m532.params;
      const ret = $m532.ret;
      const effects = $m532.effects;
      const aps = $types.params_of({ t: a });
      if ($std_list.len({ xs: aps }) !== $std_list.len({ xs: params })) {
        return false;
      }
      const $hi533 = $std_list.len({ xs: params });
      for (let i = 0; i < $hi533; i++) {
        const pp = $std_list.get({ xs: params, i: i });
        const ap = $std_list.get({ xs: aps, i: i });
        if (pp.is_inout !== ap.is_inout || !unify({ pattern: pp.ty, actual: ap.ty, subst: subst, tvars: tvars })) {
          return false;
        }
      }
      if (!unify({ pattern: ret, actual: $types.ret_of({ t: a }), subst: subst, tvars: tvars })) {
        return false;
      }
      let concrete = $std_list.builder({  });
      for (const x of $effectset.values({ s: effects })) {
        let is_var = false;
        const $m534 = x;
        $m534$match: {
          if ($m534.tag === "ParamEffect") {
            const def = $m534.def;
            is_var = $std_map.contains({ d: tvars, key: def });
            break $m534$match;
          }
          if (true) {
            is_var = false;
            break $m534$match;
          }
          $rt.unreachable();
        }
        if (!is_var) {
          const [, concrete$321] = $std_list.push({ b: concrete, x: x });
          concrete = concrete$321;
        }
      }
      const concrete_set = $effectset.of_list({ effects: $std_list.finish({ b: concrete }) });
      for (const x of $effectset.values({ s: effects })) {
        const $m535 = x;
        $m535$match: {
          if ($m535.tag === "ParamEffect") {
            const def = $m535.def;
            if ($std_map.contains({ d: tvars, key: def }) && !$std_map.contains({ d: subst, key: def })) {
              const [, st$322] = $std_map.set({ d: st, key: def, value: { tag: "EffectsA", effects: $effectset.of_list({ effects: $effectset.minus({ a: $types.effects_of({ t: a }), b: concrete_set }) }) } });
              st = st$322;
            }
            break $m535$match;
          }
          if (true) {
            skip({  });
            break $m535$match;
          }
          $rt.unreachable();
        }
      }
      return true;
      break $m532$match;
    }
    if (true) {
      return $types.same_base({ a: pattern, b: a });
      break $m532$match;
    }
    $rt.unreachable();
  }
}

export function unify_args({ pargs, aargs, same_def, subst, tvars }) {
  let st = subst;
  if (!same_def || $std_list.len({ xs: pargs }) !== $std_list.len({ xs: aargs })) {
    return false;
  }
  const $hi537 = $std_list.len({ xs: pargs });
  for (let i = 0; i < $hi537; i++) {
    const pa = $std_list.get({ xs: pargs, i: i });
    const aa = $std_list.get({ xs: aargs, i: i });
    if ($types.arg_tag({ a: pa }) !== $types.arg_tag({ a: aa })) {
      return false;
    }
    const $m538 = pa;
    $m538$match: {
      if ($m538.tag === "TypeA") {
        const ty = $m538.ty;
        if (!unify({ pattern: ty, actual: $types.arg_type({ a: aa }), subst: subst, tvars: tvars })) {
          return false;
        }
        break $m538$match;
      }
      if ($m538.tag === "ConstA") {
        const value = $m538.value;
        const av = $types.arg_const({ a: aa });
        let handled = false;
        const $m539 = value;
        $m539$match: {
          if ($m539.tag === "SymV") {
            const def = $m539.def;
            if ($std_map.contains({ d: tvars, key: def })) {
              handled = true;
              const bound = $std_map.find({ d: subst, key: def });
              if (bound.tag === "None") {
                const [, st$323] = $std_map.set({ d: st, key: def, value: aa });
                st = st$323;
              } else {
                const ba = arg_or({ o: bound });
                if (!(ba.tag === "ConstA") || !$types.const_equals({ a: $types.arg_const({ a: ba }), b: av })) {
                  return false;
                }
              }
            }
            break $m539$match;
          }
          if (true) {
            skip({  });
            break $m539$match;
          }
          $rt.unreachable();
        }
        if (!handled && !$types.const_equals({ a: value, b: av })) {
          return false;
        }
        break $m538$match;
      }
      if ($m538.tag === "EffectsA") {
        return false;
        break $m538$match;
      }
      $rt.unreachable();
    }
  }
  return true;
}

export function record_update({ ck, ctx, e }) {
  const $m540 = e;
  $m540$match: {
    if ($m540.tag === "RecordUpdate") {
      const base = $m540.base;
      const fields = $m540.fields;
      const span = $m540.span;
      const [$r541, ck$324, ctx$324] = infer({ ck: ck, ctx: ctx, e: base });
      ck = ck$324;
      ctx = ctx$324;
      const bt = $r541;
      const s = $types.strip({ t: bt });
      if (s.tag === "ErrorT") {
        for (const f of fields) {
          const [$r542, ck$325, ctx$325] = infer({ ck: ck, ctx: ctx, e: f.value });
          ck = ck$325;
          ctx = ctx$325;
          const ft = $r542;
        }
        return [{ tag: "ErrorT" }, ck, ctx];
      }
      if (!(s.tag === "RecordT")) {
        const [, ctx$326] = rep({ ck: ck, ctx: ctx, code: "E0321", at: $parser.span_of_expr({ e: base }), detail: "`with` updates a record, not " + show({ ctx: ctx, t: bt }) });
        ctx = ctx$326;
        for (const f of fields) {
          const [$r544, ck$327, ctx$327] = infer({ ck: ck, ctx: ctx, e: f.value });
          ck = ck$327;
          ctx = ctx$327;
          const ft = $r544;
        }
        return [{ tag: "ErrorT" }, ck, ctx];
      }
      const d = $context.get_def({ ctx: ctx, id: $types.def_of({ t: s }) });
      const $m546 = ck.current_module;
      $m546$match: {
        if ($m546.tag === "Some") {
          const value = $m546.value;
          if (d.is_sealed && d.mod !== value.id && !value.tree.is_test) {
            const [, ctx$328] = rep({ ck: ck, ctx: ctx, code: "E0111", at: span, detail: "`" + $context.qualified_name({ ctx: ctx, id: d.id }) + "` is sealed; `with` is allowed only in module `" + $context.module_name({ ctx: ctx, id: d.mod }) + "`" });
            ctx = ctx$328;
          }
          break $m546$match;
        }
        if ($m546.tag === "None") {
          skip({  });
          break $m546$match;
        }
        $rt.unreachable();
      }
      const [$r547, ck$329, ctx$329] = subst_of({ ck: ck, ctx: ctx, d: d, args: $types.args_of({ t: s }) });
      ck = ck$329;
      ctx = ctx$329;
      const subst = $r547;
      const [$r548, ck$330, ctx$330] = fields_of({ ck: ck, ctx: ctx, d: d });
      ck = ck$330;
      ctx = ctx$330;
      const fs = $r548;
      for (const f of fields) {
        const $m549 = find_param({ params: field_params({ fs: fs }), name: f.name.text });
        $m549$match: {
          if ($m549.tag === "None") {
            const [, ctx$331] = rep({ ck: ck, ctx: ctx, code: "E0325", at: f.name.span, detail: "`" + d.name + "` has no field `" + f.name.text + "`" });
            ctx = ctx$331;
            const [$r550, ck$332, ctx$332] = infer({ ck: ck, ctx: ctx, e: f.value });
            ck = ck$332;
            ctx = ctx$332;
            const ft = $r550;
            break $m549$match;
          }
          if ($m549.tag === "Some") {
            const value = $m549.value;
            const [$r552, ck$333, ctx$333] = check_expr({ ck: ck, ctx: ctx, e: f.value, expected: { tag: "Some", value: $types.substitute({ t: value.ty, subst: subst }) } });
            ck = ck$333;
            ctx = ctx$333;
            const ft = $r552;
            break $m549$match;
          }
          $rt.unreachable();
        }
      }
      return [s, ck, ctx];
      break $m540$match;
    }
    if (true) {
      return [{ tag: "ErrorT" }, ck, ctx];
      break $m540$match;
    }
    $rt.unreachable();
  }
}

export function list_lit({ ck, ctx, e, expected }) {
  const $m554 = e;
  $m554$match: {
    if ($m554.tag === "ListLit") {
      const elems = $m554.elems;
      const span = $m554.span;
      let exp_elem = { tag: "None" };
      let has_expected = false;
      const $m556 = expected;
      $m556$match: {
        if ($m556.tag === "Some") {
          const value = $m556.value;
          has_expected = true;
          const exp = $types.strip({ t: value });
          const [$r557, ctx$334] = is_list_type({ ctx: ctx, t: exp });
          ctx = ctx$334;
          if ($r557) {
            exp_elem = first_type_arg({ t: exp });
          }
          break $m556$match;
        }
        if ($m556.tag === "None") {
          skip({  });
          break $m556$match;
        }
        $rt.unreachable();
      }
      const $m558 = exp_elem;
      $m558$match: {
        if ($m558.tag === "Some") {
          const value = $m558.value;
          for (const x of elems) {
            const [$r560, ck$335, ctx$335] = check_expr({ ck: ck, ctx: ctx, e: x, expected: { tag: "Some", value: value } });
            ck = ck$335;
            ctx = ctx$335;
            const xt = $r560;
          }
          const [$r561, ctx$336] = list_of({ ctx: ctx, elem: value });
          ctx = ctx$336;
          return [$r561, ck, ctx];
          break $m558$match;
        }
        if ($m558.tag === "None") {
          skip({  });
          break $m558$match;
        }
        $rt.unreachable();
      }
      if ($std_list.len({ xs: elems }) === 0) {
        if (!has_expected) {
          const [, ctx$337] = rep({ ck: ck, ctx: ctx, code: "E0324", at: span, detail: "cannot determine the element type of an empty list; annotate the binding" });
          ctx = ctx$337;
          return [{ tag: "ErrorT" }, ck, ctx];
        }
        const [$r564, ctx$338] = list_of({ ctx: ctx, elem: { tag: "ErrorT" } });
        ctx = ctx$338;
        return [$r564, ck, ctx];
      }
      let elem = { tag: "ErrorT" };
      let first = true;
      for (const x of elems) {
        if (first) {
          const [$r566, ck$339, ctx$339] = infer({ ck: ck, ctx: ctx, e: x });
          ck = ck$339;
          ctx = ctx$339;
          elem = $types.strip({ t: $r566 });
          first = false;
        } else {
          const [$r568, ck$340, ctx$340] = check_expr({ ck: ck, ctx: ctx, e: x, expected: { tag: "Some", value: elem } });
          ck = ck$340;
          ctx = ctx$340;
          const xt = $r568;
        }
      }
      const [$r569, ctx$341] = list_of({ ctx: ctx, elem: elem });
      ctx = ctx$341;
      return [$r569, ck, ctx];
      break $m554$match;
    }
    if (true) {
      return [{ tag: "ErrorT" }, ck, ctx];
      break $m554$match;
    }
    $rt.unreachable();
  }
}

export function try_expr({ ck, ctx, e }) {
  const $m571 = e;
  $m571$match: {
    if ($m571.tag === "Try") {
      const expr = $m571.expr;
      const else_ = $m571.else_;
      const span = $m571.span;
      const [$r572, ck$342, ctx$342] = infer({ ck: ck, ctx: ctx, e: expr });
      ck = ck$342;
      ctx = ctx$342;
      const inner = $r572;
      const [$r574, ctx$343] = unwrap_fallible({ ctx: ctx, t: inner });
      ctx = ctx$343;
      const $m573 = $r574;
      $m573$match: {
        if ($m573.tag === "None") {
          if (!(inner.tag === "ErrorT")) {
            const [, ctx$344] = rep({ ck: ck, ctx: ctx, code: "E0321", at: $parser.span_of_expr({ e: expr }), detail: "`try` unwraps a Result or Option, not " + show({ ctx: ctx, t: inner }) });
            ctx = ctx$344;
          }
          const $m575 = else_;
          $m575$match: {
            if ($m575.tag === "Some") {
              const value = $m575.value;
              const [$r576, ck$345, ctx$345] = infer({ ck: ck, ctx: ctx, e: value.expr });
              ck = ck$345;
              ctx = ctx$345;
              const et = $r576;
              break $m575$match;
            }
            if ($m575.tag === "None") {
              skip({  });
              break $m575$match;
            }
            $rt.unreachable();
          }
          return [{ tag: "ErrorT" }, ck, ctx];
          break $m573$match;
        }
        if ($m573.tag === "Some") {
          const value = $m573.value;
          const [$r578, ck$346, ctx$346] = try_tail({ ck: ck, ctx: ctx, else_: else_, fallible: value, at: span });
          ck = ck$346;
          ctx = ctx$346;
          return [$r578, ck, ctx];
          break $m573$match;
        }
        $rt.unreachable();
      }
      break $m571$match;
    }
    if (true) {
      return [{ tag: "ErrorT" }, ck, ctx];
      break $m571$match;
    }
    $rt.unreachable();
  }
}

export function fallible_of_ret({ ck, ctx }) {
  const $m580 = fn_ret({ ck: ck });
  $m580$match: {
    if ($m580.tag === "Some") {
      const value = $m580.value;
      const [$r581, ctx$347] = unwrap_fallible({ ctx: ctx, t: value });
      ctx = ctx$347;
      return [$r581, ctx];
      break $m580$match;
    }
    if ($m580.tag === "None") {
      return [{ tag: "None" }, ctx];
      break $m580$match;
    }
    $rt.unreachable();
  }
}

export function fallible_or({ o }) {
  const $m583 = o;
  $m583$match: {
    if ($m583.tag === "Some") {
      const value = $m583.value;
      return value;
      break $m583$match;
    }
    if ($m583.tag === "None") {
      return { value: { tag: "ErrorT" }, error: { tag: "ErrorT" }, is_option: false };
      break $m583$match;
    }
    $rt.unreachable();
  }
}

export function report_no_fallible_ret({ ck, ctx, at }) {
  const $m587 = fn_ret({ ck: ck });
  $m587$match: {
    if ($m587.tag === "None") {
      const [, ctx$348] = rep({ ck: ck, ctx: ctx, code: "E0321", at: at, detail: "`try` is allowed only in a function returning Result or Option" });
      ctx = ctx$348;
      break $m587$match;
    }
    if ($m587.tag === "Some") {
      const value = $m587.value;
      const [, ctx$349] = rep({ ck: ck, ctx: ctx, code: "E0321", at: at, detail: "`try` needs the enclosing function to return Result or Option, not " + show({ ctx: ctx, t: value }) });
      ctx = ctx$349;
      break $m587$match;
    }
    $rt.unreachable();
  }
  return [undefined, ctx];
  return [undefined, ctx];
}

export function try_tail({ ck, ctx, else_, fallible, at }) {
  if (fn_flag({ ck: ck, which: 2 })) {
    const $m588 = else_;
    $m588$match: {
      if ($m588.tag === "None") {
        const [, ctx$350] = rep({ ck: ck, ctx: ctx, code: "E0321", at: at, detail: "`try` in a verify block needs an `else` yielding the Bool result" });
        ctx = ctx$350;
        return [fallible.value, ck, ctx];
        break $m588$match;
      }
      if ($m588.tag === "Some") {
        const value = $m588.value;
        if (value.name.text !== "_") {
          const [, ctx$351] = set_decl_type({ ctx: ctx, def: def_of_key({ ctx: ctx, k: key({ ck: ck, tag: $defs.tag_misc, span: value.span }) }), t: fallible.error });
          ctx = ctx$351;
        }
        const [$r590, ck$352, ctx$352] = check_expr({ ck: ck, ctx: ctx, e: value.expr, expected: { tag: "Some", value: $types.bool_t } });
        ck = ck$352;
        ctx = ctx$352;
        const et = $r590;
        return [fallible.value, ck, ctx];
        break $m588$match;
      }
      $rt.unreachable();
    }
  }
  const [$r591, ctx$353] = fallible_of_ret({ ck: ck, ctx: ctx });
  ctx = ctx$353;
  const outer = $r591;
  const $m592 = else_;
  $m592$match: {
    if ($m592.tag === "None") {
      if (outer.tag === "None") {
        const [, ctx$354] = report_no_fallible_ret({ ck: ck, ctx: ctx, at: at });
        ctx = ctx$354;
        return [fallible.value, ck, ctx];
      }
      const o = fallible_or({ o: outer });
      if (fallible.is_option !== o.is_option) {
        let inner_kind = "a Result";
        if (fallible.is_option) {
          inner_kind = "an Option";
        }
        let outer_kind = "Result";
        if (o.is_option) {
          outer_kind = "Option";
        }
        const [, ctx$355] = rep({ ck: ck, ctx: ctx, code: "E0321", at: at, detail: "`try` on " + inner_kind + " inside a function returning " + outer_kind + " needs an `else`" });
        ctx = ctx$355;
      } else {
        if (!fallible.is_option && !$types.same_base({ a: fallible.error, b: o.error })) {
          const [, ctx$356] = rep({ ck: ck, ctx: ctx, code: "E0321", at: at, detail: "the error type " + show({ ctx: ctx, t: fallible.error }) + " differs from the function's " + show({ ctx: ctx, t: o.error }) + "; add `else e: ...` to convert it" });
          ctx = ctx$356;
        }
      }
      return [fallible.value, ck, ctx];
      break $m592$match;
    }
    if ($m592.tag === "Some") {
      const value = $m592.value;
      if (outer.tag === "None") {
        const [, ctx$357] = report_no_fallible_ret({ ck: ck, ctx: ctx, at: at });
        ctx = ctx$357;
        const [$r593, ck$358, ctx$358] = infer({ ck: ck, ctx: ctx, e: value.expr });
        ck = ck$358;
        ctx = ctx$358;
        const et = $r593;
        return [fallible.value, ck, ctx];
      }
      const o = fallible_or({ o: outer });
      if (value.name.text !== "_") {
        const [, ctx$359] = set_decl_type({ ctx: ctx, def: def_of_key({ ctx: ctx, k: key({ ck: ck, tag: $defs.tag_misc, span: value.span }) }), t: fallible.error });
        ctx = ctx$359;
      }
      const [$r595, ck$360, ctx$360] = check_expr({ ck: ck, ctx: ctx, e: value.expr, expected: { tag: "Some", value: o.error } });
      ck = ck$360;
      ctx = ctx$360;
      const et = $r595;
      return [fallible.value, ck, ctx];
      break $m592$match;
    }
    $rt.unreachable();
  }
}

export function recover_expr({ ck, ctx, e }) {
  const $m596 = e;
  $m596$match: {
    if ($m596.tag === "Recover") {
      const body = $m596.body;
      const span = $m596.span;
      const n = $std_list.len({ xs: body.stmts });
      let value = { tag: "ErrorT" };
      let ends_in_expr = false;
      const [, ck$361] = push_fn({ ck: ck, f: { ret: { tag: "None" }, assertion: false, in_recover: true, frame: fn_frame({ ck: ck }), is_verify: false } });
      ck = ck$361;
      let i = 0;
      for (const s of body.stmts) {
        let handled = false;
        if (i === $rt.int.sub(n, 1, $ob35)) {
          const $m600 = s;
          $m600$match: {
            if ($m600.tag === "ExprStmt") {
              const expr = $m600.expr;
              const [$r601, ck$362, ctx$362] = infer({ ck: ck, ctx: ctx, e: expr });
              ck = ck$362;
              ctx = ctx$362;
              value = $r601;
              ends_in_expr = true;
              handled = true;
              break $m600$match;
            }
            if (true) {
              skip({  });
              break $m600$match;
            }
            $rt.unreachable();
          }
        }
        if (!handled) {
          const [, ck$363, ctx$363] = check_stmt({ ck: ck, ctx: ctx, s: s });
          ck = ck$363;
          ctx = ctx$363;
        }
        i = $rt.int.add(i, 1, $ob36);
      }
      const [, ck$364] = pop_fn({ ck: ck });
      ck = ck$364;
      if (!ends_in_expr) {
        const [, ctx$365] = rep({ ck: ck, ctx: ctx, code: "E0321", at: span, detail: "a `recover` block ends in the expression whose value it yields" });
        ctx = ctx$365;
      }
      const [$r602, ctx$366] = panicked_type({ ctx: ctx });
      ctx = ctx$366;
      const [$r603, ctx$367] = result_of({ ctx: ctx, value: value, error: $r602 });
      ctx = ctx$367;
      return [$r603, ck, ctx];
      break $m596$match;
    }
    if (true) {
      return [{ tag: "ErrorT" }, ck, ctx];
      break $m596$match;
    }
    $rt.unreachable();
  }
}

export function finite_type({ ck, ctx, base }) {
  if ($types.prim_name({ t: base }) === "Bool") {
    return [true, ck, ctx];
  }
  if (base.tag === "UnionT") {
    const [$r605, ctx$368] = variants_of({ ck: ck, ctx: ctx, d: $context.get_def({ ctx: ctx, id: $types.def_of({ t: base }) }) });
    ctx = ctx$368;
    for (const v of $r605) {
      const [$r606, ck$369, ctx$369] = fields_of({ ck: ck, ctx: ctx, d: $context.get_def({ ctx: ctx, id: v }) });
      ck = ck$369;
      ctx = ctx$369;
      if ($std_list.len({ xs: $r606 }) > 0) {
        return [false, ck, ctx];
      }
    }
    return [true, ck, ctx];
  }
  return [false, ck, ctx];
}

export function quantifier({ ck, ctx, e }) {
  const $m607 = e;
  $m607$match: {
    if ($m607.tag === "Quantifier") {
      const ty = $m607.ty;
      const domain = $m607.domain;
      const where_ = $m607.where_;
      const body = $m607.body;
      const span = $m607.span;
      const [$r608, ck$370, ctx$370] = type_of({ fuel: max_type_depth, ck: ck, ctx: ctx, t: ty });
      ck = ck$370;
      ctx = ctx$370;
      const binder = $r608;
      const [, ctx$371] = set_decl_type({ ctx: ctx, def: def_of_key({ ctx: ctx, k: key({ ck: ck, tag: $defs.tag_expr, span: span }) }), t: binder });
      ctx = ctx$371;
      const base = $types.strip({ t: binder });
      const $m609 = domain;
      $m609$match: {
        if ($m609.tag === "None") {
          const [$r610, ck$372, ctx$372] = finite_type({ ck: ck, ctx: ctx, base: base });
          ck = ck$372;
          ctx = ctx$372;
          if (!$r610 && !(base.tag === "ErrorT")) {
            const [, ctx$373] = rep({ ck: ck, ctx: ctx, code: "E0338", at: span, detail: "quantify over a range or a list: " + show({ ctx: ctx, t: binder }) + " is not a finite type (§5.3)" });
            ctx = ctx$373;
          }
          break $m609$match;
        }
        if ($m609.tag === "Some") {
          const value = $m609.value;
          const $m611 = value;
          $m611$match: {
            if ($m611.tag === "RangeDomain") {
              const lo = $m611.lo;
              const hi = $m611.hi;
              if (!$types.same_base({ a: base, b: $types.int_t })) {
                const [, ctx$374] = rep({ ck: ck, ctx: ctx, code: "E0321", at: $comments.type_span({ t: ty }), detail: "a range binds an Int, not " + show({ ctx: ctx, t: binder }) });
                ctx = ctx$374;
              }
              const [$r613, ck$375, ctx$375] = check_expr({ ck: ck, ctx: ctx, e: lo, expected: { tag: "Some", value: $types.int_t } });
              ck = ck$375;
              ctx = ctx$375;
              const lt = $r613;
              const [$r615, ck$376, ctx$376] = check_expr({ ck: ck, ctx: ctx, e: hi, expected: { tag: "Some", value: $types.int_t } });
              ck = ck$376;
              ctx = ctx$376;
              const ht = $r615;
              break $m611$match;
            }
            if ($m611.tag === "InDomain") {
              const expr = $m611.expr;
              const [$r616, ck$377, ctx$377] = infer({ ck: ck, ctx: ctx, e: expr });
              ck = ck$377;
              ctx = ctx$377;
              const dom_type = $r616;
              const [$r617, ctx$378] = unwrap_fallible({ ctx: ctx, t: dom_type });
              ctx = ctx$378;
              const inner_type = fallible_value_or({ o: $r617, dflt: dom_type });
              const [$r618, ctx$379] = element_type({ ck: ck, ctx: ctx, t: inner_type, at: $parser.span_of_expr({ e: expr }) });
              ctx = ctx$379;
              const elem = $r618;
              if (elem.tag === "Some") {
                const [$r620, ctx$380] = coerce({ ck: ck, ctx: ctx, actual: or_error({ o: elem }), expected: binder, at: $comments.type_span({ t: ty }), what: "binder", at_key: { tag: "None" } });
                ctx = ctx$380;
                const ct = $r620;
              }
              break $m611$match;
            }
            $rt.unreachable();
          }
          break $m609$match;
        }
        $rt.unreachable();
      }
      const $m621 = where_;
      $m621$match: {
        if ($m621.tag === "Some") {
          const value = $m621.value;
          const [$r623, ck$381, ctx$381] = check_expr({ ck: ck, ctx: ctx, e: value, expected: { tag: "Some", value: $types.bool_t } });
          ck = ck$381;
          ctx = ctx$381;
          const wt = $r623;
          break $m621$match;
        }
        if ($m621.tag === "None") {
          skip({  });
          break $m621$match;
        }
        $rt.unreachable();
      }
      const [$r625, ck$382, ctx$382] = check_expr({ ck: ck, ctx: ctx, e: body, expected: { tag: "Some", value: $types.bool_t } });
      ck = ck$382;
      ctx = ctx$382;
      const bt = $r625;
      return [$types.bool_t, ck, ctx];
      break $m607$match;
    }
    if (true) {
      return [{ tag: "ErrorT" }, ck, ctx];
      break $m607$match;
    }
    $rt.unreachable();
  }
}

export function closure({ ck, ctx, e }) {
  const $m627 = e;
  $m627$match: {
    if ($m627.tag === "Closure") {
      const params = $m627.params;
      const ret = $m627.ret;
      const effects = $m627.effects;
      const body = $m627.body;
      const span = $m627.span;
      let ps = $std_list.builder({  });
      for (const p of params) {
        const [$r628, ck$383, ctx$383] = type_of({ fuel: max_type_depth, ck: ck, ctx: ctx, t: p.ty });
        ck = ck$383;
        ctx = ctx$383;
        const t = $r628;
        const [, ctx$384] = set_decl_type({ ctx: ctx, def: def_of_key({ ctx: ctx, k: key({ ck: ck, tag: $defs.tag_sig, span: p.span }) }), t: t });
        ctx = ctx$384;
        const [, ps$385] = $std_list.push({ b: ps, x: { name: p.name.text, is_inout: p.is_inout, ty: t } });
        ps = ps$385;
      }
      const [$r630, ck$386, ctx$386] = type_of({ fuel: max_type_depth, ck: ck, ctx: ctx, t: ret });
      ck = ck$386;
      ctx = ctx$386;
      const rt = $r630;
      const frame = $rt.int.add(fn_frame({ ck: ck }), 1, $ob37);
      const [, ck$387] = push_fn({ ck: ck, f: plain_fn({ ret: { tag: "Some", value: rt }, frame: frame }) });
      ck = ck$387;
      const [, ck$388, ctx$388] = check_block({ ck: ck, ctx: ctx, b: body });
      ck = ck$388;
      ctx = ctx$388;
      if (!$types.same_base({ a: rt, b: $types.unit_t }) && !block_returns({ b: body })) {
        const [, ctx$389] = rep({ ck: ck, ctx: ctx, code: "E0331", at: span, detail: "this closure returns " + show({ ctx: ctx, t: rt }) + " but not every path ends in `return`" });
        ctx = ctx$389;
      }
      const [, ck$390] = pop_fn({ ck: ck });
      ck = ck$390;
      return [{ tag: "FnT", params: $std_list.finish({ b: ps }), ret: rt, effects: effect_set_of({ ctx: ctx, file: ck.current_file, refs: effects }) }, ck, ctx];
      break $m627$match;
    }
    if (true) {
      return [{ tag: "ErrorT" }, ck, ctx];
      break $m627$match;
    }
    $rt.unreachable();
  }
}

export function fake_expr({ ck, ctx, e, expected }) {
  const $m634 = e;
  $m634$match: {
    if ($m634.tag === "Fake") {
      const capability = $m634.capability;
      const fields = $m634.fields;
      const span = $m634.span;
      for (const f of fields) {
        const [$r635, ck$391, ctx$391] = infer({ ck: ck, ctx: ctx, e: f.value });
        ck = ck$391;
        ctx = ctx$391;
        const ft = $r635;
      }
      const def = res_def({ res: ref_at({ ctx: ctx, k: key({ ck: ck, tag: $defs.tag_expr, span: span }) }) });
      if (def < 0) {
        return [{ tag: "ErrorT" }, ck, ctx];
      }
      const d = $context.get_def({ ctx: ctx, id: def });
      if (!$rt.eq(d.kind, { tag: "Capability" })) {
        const [, ctx$392] = rep({ ck: ck, ctx: ctx, code: "E0321", at: capability.span, detail: "`fake` constructs a capability, not " + $defs.kind_text({ k: d.kind }) + " `" + d.name + "`" });
        ctx = ctx$392;
        return [{ tag: "ErrorT" }, ck, ctx];
      }
      const $m639 = expected;
      $m639$match: {
        if ($m639.tag === "Some") {
          const value = $m639.value;
          const exp = $types.strip({ t: value });
          if (exp.tag === "Capability" && $types.def_of({ t: exp }) === def) {
            return [exp, ck, ctx];
          }
          break $m639$match;
        }
        if ($m639.tag === "None") {
          skip({  });
          break $m639$match;
        }
        $rt.unreachable();
      }
      const [$r640, ck$393, ctx$393] = instantiate({ fuel: max_type_depth, ck: ck, ctx: ctx, d: d, args: [], at: span });
      ck = ck$393;
      ctx = ctx$393;
      return [$r640, ck, ctx];
      break $m634$match;
    }
    if (true) {
      return [{ tag: "ErrorT" }, ck, ctx];
      break $m634$match;
    }
    $rt.unreachable();
  }
}

export function field_access({ ck, ctx, e }) {
  const $m642 = e;
  $m642$match: {
    if ($m642.tag === "FieldAccess") {
      const object = $m642.object;
      const name = $m642.name;
      const span = $m642.span;
      const $m643 = ref_at({ ctx: ctx, k: key({ ck: ck, tag: $defs.tag_expr, span: span }) });
      $m643$match: {
        if ($m643.tag === "Some") {
          const value = $m643.value;
          const [$r644, ck$394, ctx$394] = resolved_value_type({ ck: ck, ctx: ctx, res: value, at: span });
          ck = ck$394;
          ctx = ctx$394;
          return [$r644, ck, ctx];
          break $m643$match;
        }
        if ($m643.tag === "None") {
          skip({  });
          break $m643$match;
        }
        $rt.unreachable();
      }
      const [$r645, ck$395, ctx$395] = infer({ ck: ck, ctx: ctx, e: object });
      ck = ck$395;
      ctx = ctx$395;
      const obj = $r645;
      const s = $types.strip({ t: obj });
      if (s.tag === "ErrorT") {
        return [{ tag: "ErrorT" }, ck, ctx];
      }
      if (!(s.tag === "RecordT")) {
        let hint = "";
        if (s.tag === "UnionT") {
          hint = "; match on it to reach a variant's fields";
        }
        const [, ctx$396] = rep({ ck: ck, ctx: ctx, code: "E0325", at: name.span, detail: show({ ctx: ctx, t: obj }) + " has no fields" + hint });
        ctx = ctx$396;
        return [{ tag: "ErrorT" }, ck, ctx];
      }
      const d = $context.get_def({ ctx: ctx, id: $types.def_of({ t: s }) });
      const [$r648, ck$397, ctx$397] = fields_of({ ck: ck, ctx: ctx, d: d });
      ck = ck$397;
      ctx = ctx$397;
      const fs = $r648;
      const $m649 = find_param({ params: field_params({ fs: fs }), name: name.text });
      $m649$match: {
        if ($m649.tag === "None") {
          const [, ctx$398] = rep({ ck: ck, ctx: ctx, code: "E0325", at: name.span, detail: "`" + d.name + "` has no field `" + name.text + "`" });
          ctx = ctx$398;
          return [{ tag: "ErrorT" }, ck, ctx];
          break $m649$match;
        }
        if ($m649.tag === "Some") {
          const value = $m649.value;
          const [$r651, ck$399, ctx$399] = subst_of({ ck: ck, ctx: ctx, d: d, args: $types.args_of({ t: s }) });
          ck = ck$399;
          ctx = ctx$399;
          return [$types.substitute({ t: value.ty, subst: $r651 }), ck, ctx];
          break $m649$match;
        }
        $rt.unreachable();
      }
      break $m642$match;
    }
    if (true) {
      return [{ tag: "ErrorT" }, ck, ctx];
      break $m642$match;
    }
    $rt.unreachable();
  }
}

export function resolved_value_type({ ck, ctx, res, at }) {
  let target_def = $rt.int.neg(1, $ob38);
  const $m653 = res;
  $m653$match: {
    if ($m653.tag === "DefRes") {
      const def = $m653.def;
      target_def = def;
      break $m653$match;
    }
    if ($m653.tag === "CompanionRes") {
      const owner = $m653.owner;
      const fn_def = $m653.fn_def;
      target_def = fn_def;
      break $m653$match;
    }
    if ($m653.tag === "IfaceFnRes") {
      const iface = $m653.iface;
      const fn_def = $m653.fn_def;
      const [, ctx$400] = rep({ ck: ck, ctx: ctx, code: "E0323", at: at, detail: "an interface function must be called" });
      ctx = ctx$400;
      return [{ tag: "ErrorT" }, ck, ctx];
      break $m653$match;
    }
    if ($m653.tag === "UnitRes") {
      return [$types.unit_t, ck, ctx];
      break $m653$match;
    }
    if ($m653.tag === "TypeValueRes") {
      const owner = $m653.owner;
      return [{ tag: "TypeInfoT" }, ck, ctx];
      break $m653$match;
    }
    if (true) {
      return [{ tag: "ErrorT" }, ck, ctx];
      break $m653$match;
    }
    $rt.unreachable();
  }
  if (target_def < 0) {
    return [{ tag: "ErrorT" }, ck, ctx];
  }
  const d = $context.get_def({ ctx: ctx, id: target_def });
  if ($rt.eq(d.kind, { tag: "Fn" })) {
    const [$r659, ck$401, ctx$401] = fn_value_type({ ck: ck, ctx: ctx, d: d, at: at });
    ck = ck$401;
    ctx = ctx$401;
    return [$r659, ck, ctx];
  }
  if ($rt.eq(d.kind, { tag: "Const" })) {
    const [$r661, ck$402, ctx$402] = const_decl_type({ ck: ck, ctx: ctx, d: d });
    ck = ck$402;
    ctx = ctx$402;
    return [$r661, ck, ctx];
  }
  return [decl_type({ ctx: ctx, def: target_def }), ck, ctx];
}

export function call({ ck, ctx, e, expected }) {
  const $m662 = e;
  $m662$match: {
    if ($m662.tag === "Call") {
      const callee = $m662.callee;
      const targs = $m662.targs;
      const args = $m662.args;
      const span = $m662.span;
      const res = ref_at({ ctx: ctx, k: key({ ck: ck, tag: $defs.tag_expr, span: $parser.span_of_expr({ e: callee }) }) });
      const tag = res_tag({ res: res });
      if (tag === 0 || tag === 5 || tag === 6) {
        const fn_def = $context.get_def({ ctx: ctx, id: res_def({ res: res }) });
        if ($rt.eq(fn_def.kind, { tag: "Fn" }) || $rt.eq(fn_def.kind, { tag: "IfaceFn" })) {
          const [, ctx$403] = set_expr_type({ ck: ck, ctx: ctx, e: callee, t: { tag: "ErrorT" } });
          ctx = ctx$403;
          let iface = { tag: "None" };
          if (tag === 6) {
            iface = { tag: "Some", value: res_iface({ res: res }) };
          }
          const [$r668, ck$404, ctx$404] = call_signature({ ck: ck, ctx: ctx, e: e, fn_def: fn_def, iface: iface, expected: expected });
          ck = ck$404;
          ctx = ctx$404;
          return [$r668, ck, ctx];
        }
      }
      const [$r669, ck$405, ctx$405] = infer({ ck: ck, ctx: ctx, e: callee });
      ck = ck$405;
      ctx = ctx$405;
      const callee_type = $r669;
      const s = $types.strip({ t: callee_type });
      if (s.tag === "ErrorT") {
        for (const a of args) {
          const [$r670, ck$406, ctx$406] = infer({ ck: ck, ctx: ctx, e: a.value });
          ck = ck$406;
          ctx = ctx$406;
          const at = $r670;
        }
        return [{ tag: "ErrorT" }, ck, ctx];
      }
      if (!(s.tag === "FnT")) {
        const [, ctx$407] = rep({ ck: ck, ctx: ctx, code: "E0323", at: $parser.span_of_expr({ e: callee }), detail: show({ ctx: ctx, t: callee_type }) + " is not a function" });
        ctx = ctx$407;
        for (const a of args) {
          const [$r672, ck$408, ctx$408] = infer({ ck: ck, ctx: ctx, e: a.value });
          ck = ck$408;
          ctx = ctx$408;
          const at = $r672;
        }
        return [{ tag: "ErrorT" }, ck, ctx];
      }
      if (targs.tag === "Some") {
        const [, ctx$409] = rep({ ck: ck, ctx: ctx, code: "E0324", at: span, detail: "a function value takes no type arguments" });
        ctx = ctx$409;
      }
      const [, ck$410, ctx$410] = match_named({ ck: ck, ctx: ctx, args: args, params: $types.params_of({ t: s }), subst: $std_map.dict({  }), tparams: [], at: span, what: "this function" });
      ck = ck$410;
      ctx = ctx$410;
      return [$types.ret_of({ t: s }), ck, ctx];
      break $m662$match;
    }
    if (true) {
      return [{ tag: "ErrorT" }, ck, ctx];
      break $m662$match;
    }
    $rt.unreachable();
  }
}

export function find_tparam({ ctx, tparams, name }) {
  for (const p of tparams) {
    if (def_name({ ctx: ctx, id: $types.tparam_def({ p: p }) }) === name) {
      return { tag: "Some", value: p };
    }
  }
  return { tag: "None" };
}

export function call_signature({ ck, ctx, e, fn_def, iface, expected }) {
  const $m677 = e;
  $m677$match: {
    if ($m677.tag === "Call") {
      const callee = $m677.callee;
      const targs = $m677.targs;
      const args = $m677.args;
      const span = $m677.span;
      const [$r678, ck$411, ctx$411] = signature_of({ ck: ck, ctx: ctx, d: fn_def });
      ck = ck$411;
      ctx = ctx$411;
      const sig = $r678;
      let tparams = sig.tparams;
      const $m679 = iface;
      $m679$match: {
        if ($m679.tag === "Some") {
          const value = $m679.value;
          tparams = $std_list.append({ xs: tparams, x: { tag: "TypeP", def: iface_param({ ctx: ctx, iface: value }), bound: { tag: "Some", value: value } } });
          break $m679$match;
        }
        if ($m679.tag === "None") {
          skip({  });
          break $m679$match;
        }
        $rt.unreachable();
      }
      let subst = $std_map.dict({  });
      const tvars = tvars_of({ tparams: tparams });
      const $m682 = targs;
      $m682$match: {
        if ($m682.tag === "Some") {
          const value = $m682.value;
          let positional = 0;
          for (const a of value) {
            let p = { tag: "None" };
            const label = type_arg_label({ a: a });
            if (label.tag === "Some") {
              const lb = ident_or({ o: label });
              p = find_tparam({ ctx: ctx, tparams: tparams, name: lb.text });
              if (p.tag === "None") {
                const [, ctx$412] = rep({ ck: ck, ctx: ctx, code: "E0324", at: type_arg_span({ a: a }), detail: "`" + fn_def.name + "` has no type parameter `" + lb.text + "`" });
                ctx = ctx$412;
              }
            } else {
              if (positional < $std_list.len({ xs: tparams })) {
                p = { tag: "Some", value: $std_list.get({ xs: tparams, i: positional }) };
              } else {
                const [, ctx$413] = rep({ ck: ck, ctx: ctx, code: "E0324", at: type_arg_span({ a: a }), detail: "`" + fn_def.name + "` takes " + plural({ n: $std_list.len({ xs: tparams }) }) });
                ctx = ctx$413;
              }
              positional = $rt.int.add(positional, 1, $ob39);
            }
            if (p.tag === "Some") {
              const [$r685, ck$414, ctx$414] = type_arg_of({ fuel: max_type_depth, ck: ck, ctx: ctx, a: a, param: p });
              ck = ck$414;
              ctx = ctx$414;
              const v = $r685;
              if (v.tag === "Some") {
                const [, subst$415] = $std_map.set({ d: subst, key: $types.tparam_def({ p: tparam_or({ o: p }) }), value: arg_or({ o: v }) });
                subst = subst$415;
              }
            }
          }
          break $m682$match;
        }
        if ($m682.tag === "None") {
          skip({  });
          break $m682$match;
        }
        $rt.unreachable();
      }
      const $m686 = expected;
      $m686$match: {
        if ($m686.tag === "Some") {
          const value = $m686.value;
          const u = unify({ pattern: sig.ret, actual: value, subst: subst, tvars: tvars });
          break $m686$match;
        }
        if ($m686.tag === "None") {
          skip({  });
          break $m686$match;
        }
        $rt.unreachable();
      }
      const [, ck$416, ctx$416] = match_named({ ck: ck, ctx: ctx, args: args, params: sig.params, subst: subst, tparams: tparams, at: span, what: "`" + fn_def.name + "`" });
      ck = ck$416;
      ctx = ctx$416;
      for (const p of tparams) {
        if (!$std_map.contains({ d: subst, key: $types.tparam_def({ p: p }) })) {
          if (p.tag === "EffectP") {
            const [, subst$417] = $std_map.set({ d: subst, key: $types.tparam_def({ p: p }), value: { tag: "EffectsA", effects: $effectset.empty({  }) } });
            subst = subst$417;
          } else {
            const [, ctx$418] = rep({ ck: ck, ctx: ctx, code: "E0324", at: span, detail: "cannot determine type argument `" + def_name({ ctx: ctx, id: $types.tparam_def({ p: p }) }) + "` of `" + fn_def.name + "`; pass it explicitly" });
            ctx = ctx$418;
            if (p.tag === "TypeP") {
              const [, subst$419] = $std_map.set({ d: subst, key: $types.tparam_def({ p: p }), value: { tag: "TypeA", ty: { tag: "ErrorT" } } });
              subst = subst$419;
            } else {
              const [, subst$420] = $std_map.set({ d: subst, key: $types.tparam_def({ p: p }), value: { tag: "ConstA", value: { tag: "UnitV" } } });
              subst = subst$420;
            }
          }
        }
      }
      for (const p of tparams) {
        const bound = tparam_bound({ p: p });
        if (bound.tag === "Some") {
          const b = or_neg({ o: bound });
          const pdef = $types.tparam_def({ p: p });
          const bv = $std_map.find({ d: subst, key: pdef });
          if (bv.tag === "Some") {
            const ba = arg_or({ o: bv });
            if (ba.tag === "TypeA" && !implemented({ ck: ck, ctx: ctx, iface: b, target: $types.arg_type({ a: ba }) })) {
              const [, ctx$421] = rep({ ck: ck, ctx: ctx, code: "E0333", at: span, detail: show({ ctx: ctx, t: $types.arg_type({ a: ba }) }) + " does not implement `" + def_name({ ctx: ctx, id: b }) + "` (required by `" + def_name({ ctx: ctx, id: pdef }) + "` of `" + fn_def.name + "`)" });
              ctx = ctx$421;
            }
          }
        }
      }
      let index_subst = $std_map.dict({  });
      const $hi692 = $std_list.len({ xs: sig.param_defs });
      for (let i = 0; i < $hi692; i++) {
        const pd = $std_list.get({ xs: sig.param_defs, i: i });
        let pname = "";
        if (i < $std_list.len({ xs: sig.params })) {
          pname = $std_list.get({ xs: sig.params, i: i }).name;
        }
        for (const a of args) {
          if (a.name.text === pname) {
            const v = eval_const({ ck: ck, ctx: ctx, e: a.value });
            if (v.tag === "Some") {
              const [, index_subst$422] = $std_map.set({ d: index_subst, key: pd, value: { tag: "ConstA", value: const_or({ o: v }) } });
              index_subst = index_subst$422;
            } else {
              if ($types.mentions_sym({ t: sig.ret, def: pd })) {
                const [, ctx$423] = rep({ ck: ck, ctx: ctx, code: "E0337", at: $parser.span_of_expr({ e: a.value }), detail: "`" + pname + "` is used as a type index in the result; pass a literal, a `const` or a parameter" });
                ctx = ctx$423;
                const [, index_subst$424] = $std_map.set({ d: index_subst, key: pd, value: { tag: "ConstA", value: { tag: "ErrorV" } } });
                index_subst = index_subst$424;
              }
            }
          }
        }
      }
      const ret = $types.substitute({ t: $types.substitute({ t: sig.ret, subst: subst }), subst: index_subst });
      const call_key = key({ ck: ck, tag: $defs.tag_expr, span: span });
      let inst = $std_list.builder({  });
      let bindings = $std_map.dict({  });
      for (const p of tparams) {
        const $m696 = $std_map.find({ d: subst, key: $types.tparam_def({ p: p }) });
        $m696$match: {
          if ($m696.tag === "Some") {
            const value = $m696.value;
            const [, inst$425] = $std_list.push({ b: inst, x: value });
            inst = inst$425;
            if (p.tag === "EffectP" && value.tag === "EffectsA") {
              const [, bindings$426] = $std_map.set({ d: bindings, key: $types.tparam_def({ p: p }), value: $types.arg_effects({ a: value }) });
              bindings = bindings$426;
            }
            break $m696$match;
          }
          if ($m696.tag === "None") {
            const [, inst$427] = $std_list.push({ b: inst, x: { tag: "TypeA", ty: { tag: "ErrorT" } } });
            inst = inst$427;
            break $m696$match;
          }
          $rt.unreachable();
        }
      }
      let insts = ctx.instantiations;
      const [, insts$428] = $std_map.set({ d: insts, key: call_key, value: $std_list.finish({ b: inst }) });
      insts = insts$428;
      let idx = ctx.index_bindings;
      const [, idx$429] = $std_map.set({ d: idx, key: call_key, value: index_subst });
      idx = idx$429;
      let effs = ctx.effect_bindings;
      const [, effs$430] = $std_map.set({ d: effs, key: call_key, value: bindings });
      effs = effs$430;
      return [ret, ck, ctx];
      break $m677$match;
    }
    if (true) {
      return [{ tag: "ErrorT" }, ck, ctx];
      break $m677$match;
    }
    $rt.unreachable();
  }
}

export const max_pending = 10000000;

export function process_pending({ ck, ctx }) {
  let i = ck.processed;
  while (i < $std_list.built({ b: ck.pending }) && i < max_pending) {
    const p = $std_list.at({ b: ck.pending, i: i });
    i = i + 1;
    const saved_file = ck.current_file;
    const saved_def = ck.current_def;
    ck = { ...ck, current_file: p.file, current_def: p.def_name, it_stack: $std_list.append({ xs: ck.it_stack, x: p.base }) };
    let pushed = false;
    const $m702 = p.fn_ctx;
    $m702$match: {
      if ($m702.tag === "Some") {
        const value = $m702.value;
        const [, ck$431] = push_fn({ ck: ck, f: value });
        ck = ck$431;
        pushed = true;
        break $m702$match;
      }
      if ($m702.tag === "None") {
        skip({  });
        break $m702$match;
      }
      $rt.unreachable();
    }
    const [$r704, ck$432, ctx$432] = check_expr({ ck: ck, ctx: ctx, e: p.pred, expected: { tag: "Some", value: $types.bool_t } });
    ck = ck$432;
    ctx = ctx$432;
    const t = $r704;
    if (pushed) {
      const [, ck$433] = pop_fn({ ck: ck });
      ck = ck$433;
    }
    const n = $std_list.len({ xs: ck.it_stack });
    if (n > 0) {
      ck = { ...ck, it_stack: $std_list.slice({ xs: ck.it_stack, from: 0, to: $rt.int.sub(n, 1, $ob40) }) };
    }
    ck = { ...ck, current_file: saved_file, current_def: saved_def };
  }
  ck = { ...ck, processed: i };
  return [undefined, ck, ctx];
  return [undefined, ck, ctx];
}

export function polymorphic_recursion({ ctx }) {
  let adjacency = $std_map.dict({  });
  let edges = $std_list.builder({  });
  const $hi708 = $context.def_count({ ctx: ctx });
  for (let id = 0; id < $hi708; id++) {
    const f = $context.get_def({ ctx: ctx, id: id });
    if (f.kind.tag === "Fn") {
      const params = type_param_defs({ ctx: ctx, owner: f.id });
      if ($std_list.len({ xs: params }) > 0) {
        const $m709 = f.node;
        $m709$match: {
          if ($m709.tag === "FnNode") {
            const decl = $m709.decl;
            const $m710 = decl.body;
            $m710$match: {
              if ($m710.tag === "Some") {
                const value = $m710.value;
                const [, adjacency$434, edges$434] = collect_edges({ ctx: ctx, f: f, params: params, body: value, adjacency: adjacency, edges: edges });
                adjacency = adjacency$434;
                edges = edges$434;
                break $m710$match;
              }
              if ($m710.tag === "None") {
                skip({  });
                break $m710$match;
              }
              $rt.unreachable();
            }
            break $m709$match;
          }
          if (true) {
            skip({  });
            break $m709$match;
          }
          $rt.unreachable();
        }
      }
    }
  }
  let reported = $std_map.dict({  });
  for (const e of $std_list.finish({ b: edges })) {
    if (!$std_map.contains({ d: reported, key: e.call_key }) && reaches({ adjacency: adjacency, start: e.to, target: e.from })) {
      const [, reported$435] = $std_map.set({ d: reported, key: e.call_key, value: true });
      reported = reported$435;
      const caller = $context.get_def({ ctx: ctx, id: e.caller }).name;
      const [, ctx$436] = $context.report({ ctx: ctx, d: $report.diagnostic({ code: "E0345", file: e.file, span: e.span, def_name: { tag: "Some", value: caller }, detail: "`" + caller + "` calls `" + $context.get_def({ ctx: ctx, id: e.callee }).name + "` at `" + show({ ctx: ctx, t: e.arg }) + "`, which wraps its type parameter `" + $context.get_def({ ctx: ctx, id: e.param }).name + "`, on a cycle back to itself: polymorphic recursion needs an unbounded set of instantiations (§3.6)" }) });
      ctx = ctx$436;
    }
  }
  return [undefined, ctx];
  return [undefined, ctx];
}

export function type_param_defs({ ctx, owner }) {
  let out = $std_list.builder({  });
  const $m712 = $std_map.find({ d: ctx.type_params, key: owner });
  $m712$match: {
    if ($m712.tag === "Some") {
      const value = $m712.value;
      for (const p of value) {
        const $m713 = p;
        $m713$match: {
          if ($m713.tag === "TypeP") {
            const def = $m713.def;
            const [, out$437] = $std_list.push({ b: out, x: def });
            out = out$437;
            break $m713$match;
          }
          if (true) {
            skip({  });
            break $m713$match;
          }
          $rt.unreachable();
        }
      }
      break $m712$match;
    }
    if ($m712.tag === "None") {
      skip({  });
      break $m712$match;
    }
    $rt.unreachable();
  }
  return $std_list.finish({ b: out });
}

export function callee_def({ ctx, file, callee }) {
  const $m714 = $std_map.find({ d: ctx.refs, key: $defs.node_key({ file: file, tag: $defs.tag_expr, span: $parser.span_of_expr({ e: callee }) }) });
  $m714$match: {
    if ($m714.tag === "Some") {
      const value = $m714.value;
      const $m715 = value;
      $m715$match: {
        if ($m715.tag === "DefRes") {
          const def = $m715.def;
          return { tag: "Some", value: def };
          break $m715$match;
        }
        if (true) {
          return { tag: "None" };
          break $m715$match;
        }
        $rt.unreachable();
      }
      break $m714$match;
    }
    if ($m714.tag === "None") {
      return { tag: "None" };
      break $m714$match;
    }
    $rt.unreachable();
  }
}

export function instantiation_at({ ctx, file, span }) {
  const $m719 = $std_map.find({ d: ctx.instantiations, key: $defs.node_key({ file: file, tag: $defs.tag_expr, span: span }) });
  $m719$match: {
    if ($m719.tag === "Some") {
      const value = $m719.value;
      return value;
      break $m719$match;
    }
    if ($m719.tag === "None") {
      return [];
      break $m719$match;
    }
    $rt.unreachable();
  }
}

export function add_edge({ adjacency, from, to }) {
  let list = [];
  const $m720 = $std_map.find({ d: adjacency, key: from });
  $m720$match: {
    if ($m720.tag === "Some") {
      const value = $m720.value;
      list = value;
      break $m720$match;
    }
    if ($m720.tag === "None") {
      skip({  });
      break $m720$match;
    }
    $rt.unreachable();
  }
  const [, adjacency$438] = $std_map.set({ d: adjacency, key: from, value: [...list, ...[to]] });
  adjacency = adjacency$438;
  return [undefined, adjacency];
  return [undefined, adjacency];
}

export function collect_edges({ ctx, f, params, body, adjacency, edges }) {
  for (const n of $walk.nodes_in_block({ b: body, into_verify: true })) {
    const $m721 = n;
    $m721$match: {
      if ($m721.tag === "NExpr") {
        const e = $m721.e;
        const $m722 = e;
        $m722$match: {
          if ($m722.tag === "Call") {
            const callee = $m722.callee;
            const span = $m722.span;
            const $m723 = callee_def({ ctx: ctx, file: f.file, callee: callee });
            $m723$match: {
              if ($m723.tag === "Some") {
                const value = $m723.value;
                const g_params = type_param_defs({ ctx: ctx, owner: value });
                const inst = instantiation_at({ ctx: ctx, file: f.file, span: span });
                const $hi724 = $std_list.len({ xs: g_params });
                for (let i = 0; i < $hi724; i++) {
                  if (i < $std_list.len({ xs: inst })) {
                    const $m725 = $std_list.get({ xs: inst, i: i });
                    $m725$match: {
                      if ($m725.tag === "TypeA") {
                        const ty = $m725.ty;
                        for (const p of params) {
                          if (occurs({ t: ty, p: p })) {
                            const from = $std_int.to_text({ x: f.id }) + ":" + $std_int.to_text({ x: p });
                            const to = $std_int.to_text({ x: value }) + ":" + $std_int.to_text({ x: $std_list.get({ xs: g_params, i: i }) });
                            const [, adjacency$439] = add_edge({ adjacency: adjacency, from: from, to: to });
                            adjacency = adjacency$439;
                            if (!is_param({ t: ty, p: p })) {
                              const [, edges$440] = $std_list.push({ b: edges, x: { from: from, to: to, call_key: $defs.node_key({ file: f.file, tag: $defs.tag_expr, span: span }), file: f.file, span: span, caller: f.id, callee: value, arg: ty, param: p } });
                              edges = edges$440;
                            }
                          }
                        }
                        break $m725$match;
                      }
                      if (true) {
                        skip({  });
                        break $m725$match;
                      }
                      $rt.unreachable();
                    }
                  }
                }
                break $m723$match;
              }
              if ($m723.tag === "None") {
                skip({  });
                break $m723$match;
              }
              $rt.unreachable();
            }
            break $m722$match;
          }
          if (true) {
            skip({  });
            break $m722$match;
          }
          $rt.unreachable();
        }
        break $m721$match;
      }
      if (true) {
        skip({  });
        break $m721$match;
      }
      $rt.unreachable();
    }
  }
  return [undefined, adjacency, edges];
  return [undefined, adjacency, edges];
}

export function is_param({ t, p }) {
  const $m727 = t;
  $m727$match: {
    if ($m727.tag === "ParamT") {
      const def = $m727.def;
      return def === p;
      break $m727$match;
    }
    if (true) {
      return false;
      break $m727$match;
    }
    $rt.unreachable();
  }
}

export function occurs({ t, p }) {
  const $m728 = t;
  $m728$match: {
    if ($m728.tag === "ParamT") {
      const def = $m728.def;
      return def === p;
      break $m728$match;
    }
    if ($m728.tag === "Refined") {
      const base = $m728.base;
      return occurs({ t: base, p: p });
      break $m728$match;
    }
    if ($m728.tag === "RecordT") {
      const args = $m728.args;
      return occurs_args({ args: args, p: p });
      break $m728$match;
    }
    if ($m728.tag === "UnionT") {
      const args = $m728.args;
      return occurs_args({ args: args, p: p });
      break $m728$match;
    }
    if ($m728.tag === "Opaque") {
      const args = $m728.args;
      return occurs_args({ args: args, p: p });
      break $m728$match;
    }
    if ($m728.tag === "Capability") {
      const args = $m728.args;
      return occurs_args({ args: args, p: p });
      break $m728$match;
    }
    if ($m728.tag === "FnT") {
      const params = $m728.params;
      const ret = $m728.ret;
      return occurs_params({ params: params, p: p }) || occurs({ t: ret, p: p });
      break $m728$match;
    }
    if (true) {
      return false;
      break $m728$match;
    }
    $rt.unreachable();
  }
}

export function occurs_args({ args, p }) {
  for (const a of args) {
    const $m729 = a;
    $m729$match: {
      if ($m729.tag === "TypeA") {
        const ty = $m729.ty;
        if (occurs({ t: ty, p: p })) {
          return true;
        }
        break $m729$match;
      }
      if (true) {
        skip({  });
        break $m729$match;
      }
      $rt.unreachable();
    }
  }
  return false;
}

export function occurs_params({ params, p }) {
  for (const x of params) {
    if (occurs({ t: x.ty, p: p })) {
      return true;
    }
  }
  return false;
}

export function reaches({ adjacency, start, target }) {
  if (start === target) {
    return true;
  }
  let seen = $std_map.dict({  });
  const [, seen$441] = $std_map.set({ d: seen, key: start, value: true });
  seen = seen$441;
  let queue = [start];
  let steps = 100000;
  while ($std_list.len({ xs: queue }) > 0 && steps > 0) {
    steps = $rt.int.sub(steps, 1, $ob41);
    const cur = $std_list.get({ xs: queue, i: $rt.int.sub($std_list.len({ xs: queue }), 1, $ob42) });
    queue = $std_list.slice({ xs: queue, from: 0, to: $rt.int.sub($std_list.len({ xs: queue }), 1, $ob43) });
    const $m731 = $std_map.find({ d: adjacency, key: cur });
    $m731$match: {
      if ($m731.tag === "Some") {
        const value = $m731.value;
        for (const next of value) {
          if (next === target) {
            return true;
          }
          if (!$std_map.contains({ d: seen, key: next })) {
            const [, seen$442] = $std_map.set({ d: seen, key: next, value: true });
            seen = seen$442;
            queue = [...queue, ...[next]];
          }
        }
        break $m731$match;
      }
      if ($m731.tag === "None") {
        skip({  });
        break $m731$match;
      }
      $rt.unreachable();
    }
  }
  return false;
}

export function types_pass({ ctx }) {
  let ck = new_checker({  });
  const $hi732 = ctx.module_count;
  for (let id = 0; id < $hi732; id++) {
    const $m733 = $std_map.find({ d: ctx.modules, key: id });
    $m733$match: {
      if ($m733.tag === "Some") {
        const value = $m733.value;
        ck = { ...ck, current_module: { tag: "Some", value: value }, current_file: value.file };
        for (const item of value.tree.items) {
          const [, ck$443, ctx$443] = elaborate_item({ ck: ck, ctx: ctx, item: item });
          ck = ck$443;
          ctx = ctx$443;
        }
        const [, ck$444, ctx$444] = process_pending({ ck: ck, ctx: ctx });
        ck = ck$444;
        ctx = ctx$444;
        break $m733$match;
      }
      if ($m733.tag === "None") {
        skip({  });
        break $m733$match;
      }
      $rt.unreachable();
    }
  }
  const $hi736 = ctx.module_count;
  for (let id = 0; id < $hi736; id++) {
    const $m737 = $std_map.find({ d: ctx.modules, key: id });
    $m737$match: {
      if ($m737.tag === "Some") {
        const value = $m737.value;
        ck = { ...ck, current_module: { tag: "Some", value: value }, current_file: value.file };
        for (const item of value.tree.items) {
          ck = { ...ck, current_def: { tag: "Some", value: item_name({ item: item }) } };
          const [, ck$445, ctx$445] = check_item({ ck: ck, ctx: ctx, item: item });
          ck = ck$445;
          ctx = ctx$445;
          ck = { ...ck, current_def: { tag: "None" } };
          const [, ck$446, ctx$446] = process_pending({ ck: ck, ctx: ctx });
          ck = ck$446;
          ctx = ctx$446;
        }
        break $m737$match;
      }
      if ($m737.tag === "None") {
        skip({  });
        break $m737$match;
      }
      $rt.unreachable();
    }
  }
  const [, ck$447, ctx$447] = process_pending({ ck: ck, ctx: ctx });
  ck = ck$447;
  ctx = ctx$447;
  const [, ctx$448] = polymorphic_recursion({ ctx: ctx });
  ctx = ctx$448;
  return [undefined, ctx];
  return [undefined, ctx];
}

