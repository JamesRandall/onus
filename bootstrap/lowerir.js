// Generated by onus from module lowerir. Do not edit.
import * as $rt from "./onus-runtime/index.js";
import * as $defs from "./defs.js";
import * as $parser from "./parser.js";
import * as $std_map from "./std/map.js";
import * as $std_int from "./std/int.js";
import * as $loc from "./loc.js";
import * as $obligations from "./obligations.js";
import * as $context from "./context.js";
import * as $std_list from "./std/list.js";
import * as $lower from "./lower.js";
import * as $types from "./types.js";
import * as $walk from "./walk.js";
import * as $std_text from "./std/text.js";
import * as $printer from "./printer.js";
import * as $capabilities from "./capabilities.js";
import * as $json from "./json.js";
import * as $claimcheck from "./claimcheck.js";

const $ob1 = { kind: "overflow", text: "-1 within Int", at: "self/lowerir.onus:106:20", def: "or_neg" };
const $ob2 = { kind: "overflow", text: "-1 within Int", at: "self/lowerir.onus:169:20", def: "ref_def" };
const $ob3 = { kind: "overflow", text: "-1 within Int", at: "self/lowerir.onus:173:19", def: "ref_def" };
const $ob4 = { kind: "overflow", text: "l.temp + 1 within Int", at: "self/lowerir.onus:178:22", def: "tmp" };
const $ob5 = { kind: "overflow", text: "guard - 1 within Int", at: "self/lowerir.onus:259:13", def: "refinement_preds" };
const $ob6 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/lowerir.onus:308:11", def: "type_slug" };
const $ob7 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/lowerir.onus:313:11", def: "type_slug" };
const $ob8 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/lowerir.onus:318:11", def: "type_slug" };
const $ob9 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/lowerir.onus:323:11", def: "type_slug" };
const $ob10 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/lowerir.onus:344:62", def: "args_slug" };
const $ob11 = { kind: "overflow", text: "i + 1 within Int", at: "self/lowerir.onus:409:9", def: "subst_of" };
const $ob12 = { kind: "overflow", text: "-1 within Int", at: "self/lowerir.onus:634:10", def: "iface_fn_named" };
const $ob13 = { kind: "overflow", text: "i + 1 within Int", at: "self/lowerir.onus:795:11", def: "fn_decl_sig" };
const $ob14 = { kind: "overflow", text: "-1 within Int", at: "self/lowerir.onus:838:26", def: "fn_decl_sig" };
const $ob15 = { kind: "overflow", text: "flows + 1 within Int", at: "self/lowerir.onus:1023:21", def: "entry_checks" };
const $ob16 = { kind: "overflow", text: "i + 1 within Int", at: "self/lowerir.onus:1059:9", def: "entry_checks" };
const $ob17 = { kind: "overflow", text: "n + 1 within Int", at: "self/lowerir.onus:1068:15", def: "entry_checks" };
const $ob18 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/lowerir.onus:1112:33", def: "with_it" };
const $ob19 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/lowerir.onus:1170:29", def: "block" };
const $ob20 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/lowerir.onus:1187:45", def: "stmt" };
const $ob21 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/lowerir.onus:1204:11", def: "stmt_inner" };
const $ob22 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/lowerir.onus:1213:11", def: "stmt_inner" };
const $ob23 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/lowerir.onus:1233:35", def: "stmt_inner" };
const $ob24 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/lowerir.onus:1238:11", def: "stmt_inner" };
const $ob25 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/lowerir.onus:1245:33", def: "stmt_inner" };
const $ob26 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/lowerir.onus:1246:46", def: "stmt_inner" };
const $ob27 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/lowerir.onus:1249:56", def: "stmt_inner" };
const $ob28 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/lowerir.onus:1254:11", def: "stmt_inner" };
const $ob29 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/lowerir.onus:1261:11", def: "stmt_inner" };
const $ob30 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/lowerir.onus:1270:11", def: "stmt_inner" };
const $ob31 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/lowerir.onus:1281:36", def: "stmt_inner" };
const $ob32 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/lowerir.onus:1323:33", def: "let_stmt" };
const $ob33 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/lowerir.onus:1346:35", def: "checked_flow" };
const $ob34 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/lowerir.onus:1365:29", def: "checked_flow" };
const $ob35 = { kind: "overflow", text: "-1 within Int", at: "self/lowerir.onus:1377:9", def: "ob_or" };
const $ob36 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/lowerir.onus:1408:33", def: "return_stmt" };
const $ob37 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/lowerir.onus:1411:41", def: "return_stmt" };
const $ob38 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/lowerir.onus:1435:32", def: "return_stmt" };
const $ob39 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/lowerir.onus:1461:35", def: "match_stmt" };
const $ob40 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/lowerir.onus:1466:13", def: "match_stmt" };
const $ob41 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/lowerir.onus:1483:25", def: "match_stmt" };
const $ob42 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/lowerir.onus:1488:52", def: "match_stmt" };
const $ob43 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/lowerir.onus:1489:49", def: "match_stmt" };
const $ob44 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/lowerir.onus:1509:22", def: "block_ir" };
const $ob45 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/lowerir.onus:1523:21", def: "stmt_ir" };
const $ob46 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/lowerir.onus:1545:25", def: "pattern_test_of" };
const $ob47 = { kind: "overflow", text: "i + 1 within Int", at: "self/lowerir.onus:1606:17", def: "pattern_bindings" };
const $ob48 = { kind: "overflow", text: "i + 1 within Int", at: "self/lowerir.onus:1608:40", def: "pattern_bindings" };
const $ob49 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/lowerir.onus:1671:30", def: "loop_stmt" };
const $ob50 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/lowerir.onus:1682:31", def: "loop_stmt" };
const $ob51 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/lowerir.onus:1702:13", def: "loop_stmt" };
const $ob52 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/lowerir.onus:1721:36", def: "loop_stmt" };
const $ob53 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/lowerir.onus:1732:17", def: "loop_stmt" };
const $ob54 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/lowerir.onus:1750:13", def: "loop_stmt" };
const $ob55 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/lowerir.onus:1775:35", def: "loop_stmt" };
const $ob56 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/lowerir.onus:1810:37", def: "for_stmt" };
const $ob57 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/lowerir.onus:1811:37", def: "for_stmt" };
const $ob58 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/lowerir.onus:1825:27", def: "for_stmt" };
const $ob59 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/lowerir.onus:1830:39", def: "for_stmt" };
const $ob60 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/lowerir.onus:1843:45", def: "for_stmt" };
const $ob61 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/lowerir.onus:1853:38", def: "for_stmt" };
const $ob62 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/lowerir.onus:1872:21", def: "expr_of" };
const $ob63 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/lowerir.onus:1914:47", def: "expr" };
const $ob64 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/lowerir.onus:1915:61", def: "expr" };
const $ob65 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/lowerir.onus:1919:45", def: "expr" };
const $ob66 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/lowerir.onus:1923:47", def: "expr" };
const $ob67 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/lowerir.onus:1924:52", def: "expr" };
const $ob68 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/lowerir.onus:1926:11", def: "expr" };
const $ob69 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/lowerir.onus:1931:56", def: "expr" };
const $ob70 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/lowerir.onus:1932:49", def: "expr" };
const $ob71 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/lowerir.onus:1934:59", def: "expr" };
const $ob72 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/lowerir.onus:1936:11", def: "expr" };
const $ob73 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/lowerir.onus:1942:46", def: "expr" };
const $ob74 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/lowerir.onus:1943:51", def: "expr" };
const $ob75 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/lowerir.onus:1945:11", def: "expr" };
const $ob76 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/lowerir.onus:1952:11", def: "expr" };
const $ob77 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/lowerir.onus:1959:42", def: "expr" };
const $ob78 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/lowerir.onus:1961:13", def: "expr" };
const $ob79 = { kind: "refinement", text: "it >= 0", at: "self/lowerir.onus:1974:3", def: "exprs" };
const $ob80 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/lowerir.onus:1986:43", def: "exprs" };
const $ob81 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/lowerir.onus:2089:33", def: "ctor" };
const $ob82 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/lowerir.onus:2098:17", def: "ctor" };
const $ob83 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/lowerir.onus:2139:39", def: "record_update" };
const $ob84 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/lowerir.onus:2152:29", def: "record_update" };
const $ob85 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/lowerir.onus:2165:13", def: "record_update" };
const $ob86 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/lowerir.onus:2216:37", def: "field_checks" };
const $ob87 = { kind: "overflow", text: "-1 within Int", at: "self/lowerir.onus:2235:43", def: "field_or" };
const $ob88 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/lowerir.onus:2251:42", def: "try_expr" };
const $ob89 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/lowerir.onus:2280:15", def: "try_expr" };
const $ob90 = { kind: "overflow", text: "n - 1 within Int", at: "self/lowerir.onus:2326:15", def: "recover_expr" };
const $ob91 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/lowerir.onus:2334:31", def: "recover_expr" };
const $ob92 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/lowerir.onus:2337:41", def: "recover_expr" };
const $ob93 = { kind: "overflow", text: "i + 1 within Int", at: "self/lowerir.onus:2338:11", def: "recover_expr" };
const $ob94 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/lowerir.onus:2370:24", def: "quantifier" };
const $ob95 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/lowerir.onus:2371:24", def: "quantifier" };
const $ob96 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/lowerir.onus:2375:43", def: "quantifier" };
const $ob97 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/lowerir.onus:2386:25", def: "quantifier" };
const $ob98 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/lowerir.onus:2395:24", def: "quantifier" };
const $ob99 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/lowerir.onus:2469:19", def: "closure" };
const $ob100 = { kind: "overflow", text: "i + 1 within Int", at: "self/lowerir.onus:2495:11", def: "closure" };
const $ob101 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/lowerir.onus:2497:46", def: "closure" };
const $ob102 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/lowerir.onus:2537:29", def: "fake_expr" };
const $ob103 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/lowerir.onus:2585:26", def: "field_access" };
const $ob104 = { kind: "overflow", text: "-1 within Int", at: "self/lowerir.onus:2620:26", def: "call" };
const $ob105 = { kind: "overflow", text: "-1 within Int", at: "self/lowerir.onus:2621:26", def: "call" };
const $ob106 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/lowerir.onus:2640:17", def: "call" };
const $ob107 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/lowerir.onus:2654:41", def: "call" };
const $ob108 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/lowerir.onus:2666:23", def: "call" };
const $ob109 = { kind: "overflow", text: "i + 1 within Int", at: "self/lowerir.onus:2777:9", def: "named_call" };
const $ob110 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/lowerir.onus:2786:21", def: "named_call" };
const $ob111 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/lowerir.onus:2794:11", def: "named_call" };
const $ob112 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/lowerir.onus:2803:36", def: "named_call" };
const $ob113 = { kind: "overflow", text: "i + 1 within Int", at: "self/lowerir.onus:2898:9", def: "decreases_check" };
const $ob114 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/lowerir.onus:2904:11", def: "decreases_check" };
const $ob115 = { kind: "overflow", text: "-1 within Int", at: "self/lowerir.onus:2951:20", def: "select_decoder" };
const $ob116 = { kind: "overflow", text: "i + 1 within Int", at: "self/lowerir.onus:2957:9", def: "select_decoder" };
const $ob117 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/lowerir.onus:2998:41", def: "select_decoder" };
const $ob118 = { kind: "overflow", text: "n - 1 within Int", at: "self/lowerir.onus:3122:34", def: "dispatch" };
const $ob119 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/lowerir.onus:3167:33", def: "unary" };
const $ob120 = { kind: "overflow", text: "-1 within Int", at: "self/lowerir.onus:3205:9", def: "def_or" };
const $ob121 = { kind: "overflow", text: "-1 within Int", at: "self/lowerir.onus:3209:10", def: "def_or" };
const $ob122 = { kind: "overflow", text: "-1 within Int", at: "self/lowerir.onus:3210:11", def: "def_or" };
const $ob123 = { kind: "overflow", text: "-1 within Int", at: "self/lowerir.onus:3211:10", def: "def_or" };
const $ob124 = { kind: "overflow", text: "-1 within Int", at: "self/lowerir.onus:3218:12", def: "def_or" };
const $ob125 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/lowerir.onus:3249:11", def: "short_circuit" };
const $ob126 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/lowerir.onus:3263:35", def: "short_circuit" };
const $ob127 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/lowerir.onus:3317:37", def: "implies_expr" };
const $ob128 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/lowerir.onus:3319:38", def: "implies_expr" };
const $ob129 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/lowerir.onus:3356:33", def: "binary" };
const $ob130 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/lowerir.onus:3360:39", def: "binary" };
const $ob131 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/lowerir.onus:3361:40", def: "binary" };
const $ob132 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/lowerir.onus:3599:32", def: "generator" };
const $ob133 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/lowerir.onus:3641:31", def: "base_generator" };
const $ob134 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/lowerir.onus:3648:30", def: "base_generator" };
const $ob135 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/lowerir.onus:3657:36", def: "base_generator" };
const $ob136 = { kind: "overflow", text: "fuel - 1 within Int", at: "self/lowerir.onus:3683:30", def: "gen_fields" };
const $ob137 = { kind: "overflow", text: "-1 within Int", at: "self/lowerir.onus:3816:13", def: "site_or" };
const $ob138 = { kind: "overflow", text: "-1 within Int", at: "self/lowerir.onus:3817:16", def: "site_or" };
const $ob139 = { kind: "overflow", text: "-1 within Int", at: "self/lowerir.onus:3819:11", def: "site_or" };
const $ob140 = { kind: "overflow", text: "-1 within Int", at: "self/lowerir.onus:3820:14", def: "site_or" };
export const max_depth = 100000;

export function skip($args) {
  return undefined;
}

export function or_neg({ o }) {
  const $m1 = o;
  $m1$match: {
    if ($m1.tag === "Some") {
      const value = $m1.value;
      return value;
      break $m1$match;
    }
    if ($m1.tag === "None") {
      return $rt.int.neg(1, $ob1);
      break $m1$match;
    }
    $rt.unreachable();
  }
}

export function type_or({ o }) {
  const $m2 = o;
  $m2$match: {
    if ($m2.tag === "Some") {
      const value = $m2.value;
      return value;
      break $m2$match;
    }
    if ($m2.tag === "None") {
      return { tag: "ErrorT" };
      break $m2$match;
    }
    $rt.unreachable();
  }
}

export function fields_or({ o }) {
  const $m4 = o;
  $m4$match: {
    if ($m4.tag === "Some") {
      const value = $m4.value;
      return value;
      break $m4$match;
    }
    if ($m4.tag === "None") {
      return [];
      break $m4$match;
    }
    $rt.unreachable();
  }
}

export function ids_or({ o }) {
  const $m5 = o;
  $m5$match: {
    if ($m5.tag === "Some") {
      const value = $m5.value;
      return value;
      break $m5$match;
    }
    if ($m5.tag === "None") {
      return [];
      break $m5$match;
    }
    $rt.unreachable();
  }
}

export function args_or({ o }) {
  const $m6 = o;
  $m6$match: {
    if ($m6.tag === "Some") {
      const value = $m6.value;
      return value;
      break $m6$match;
    }
    if ($m6.tag === "None") {
      return [];
      break $m6$match;
    }
    $rt.unreachable();
  }
}

export function tparams_or({ o }) {
  const $m7 = o;
  $m7$match: {
    if ($m7.tag === "Some") {
      const value = $m7.value;
      return value;
      break $m7$match;
    }
    if ($m7.tag === "None") {
      return [];
      break $m7$match;
    }
    $rt.unreachable();
  }
}

export function expr_or_unit({ o }) {
  const $m8 = o;
  $m8$match: {
    if ($m8.tag === "Some") {
      const value = $m8.value;
      return value;
      break $m8$match;
    }
    if ($m8.tag === "None") {
      return { tag: "UnitE" };
      break $m8$match;
    }
    $rt.unreachable();
  }
}

export function text_or({ o, dflt }) {
  const $m10 = o;
  $m10$match: {
    if ($m10.tag === "Some") {
      const value = $m10.value;
      return value;
      break $m10$match;
    }
    if ($m10.tag === "None") {
      return dflt;
      break $m10$match;
    }
    $rt.unreachable();
  }
}

export function key({ l, tag, span }) {
  return $defs.node_key({ file: l.file, tag: tag, span: span });
}

export function expr_key({ l, e }) {
  return key({ l: l, tag: $defs.tag_expr, span: $parser.span_of_expr({ e: e }) });
}

export function type_of_expr({ l, ctx, e }) {
  return type_or({ o: $std_map.find({ d: ctx.expr_types, key: expr_key({ l: l, e: e }) }) });
}

export function def_at({ l, ctx, tag, span }) {
  return or_neg({ o: $std_map.find({ d: ctx.def_of, key: key({ l: l, tag: tag, span: span }) }) });
}

export function ref_def({ ctx, k }) {
  const $m11 = $std_map.find({ d: ctx.refs, key: k });
  $m11$match: {
    if ($m11.tag === "None") {
      return $rt.int.neg(1, $ob2);
      break $m11$match;
    }
    if ($m11.tag === "Some") {
      const value = $m11.value;
      const $m12 = value;
      $m12$match: {
        if ($m12.tag === "DefRes") {
          const def = $m12.def;
          return def;
          break $m12$match;
        }
        if (true) {
          return $rt.int.neg(1, $ob3);
          break $m12$match;
        }
        $rt.unreachable();
      }
      break $m11$match;
    }
    $rt.unreachable();
  }
}

export function tmp({ l, prefix }) {
  l = { ...l, temp: $rt.int.add(l.temp, 1, $ob4) };
  return ["$" + prefix + $std_int.to_text({ x: l.temp }), l];
}

export function where_text({ l, ctx, file, span }) {
  return $loc.location_text({ ctx: ctx, tables: l.tables, file: file, span: span });
}

export function ob_ref({ l, ctx, kind, text, file, span, def }) {
  return { kind: kind, text: text, at: where_text({ l: l, ctx: ctx, file: file, span: span }), def: def };
}

export function ob_ref_of({ l, ctx, o }) {
  return ob_ref({ l: l, ctx: ctx, kind: $obligations.kind_text({ k: o.kind }), text: o.text, file: o.at_file, span: o.at_span, def: $context.get_def({ ctx: ctx, id: o.def }).name });
}

export function obligations_at({ ctx, k }) {
  let out = $std_list.builder({  });
  for (const id of ids_or({ o: $std_map.find({ d: ctx.obligations_at, key: k }) })) {
    const [, out$1] = $std_list.push({ b: out, x: $context.get_obligation({ ctx: ctx, id: id }) });
    out = out$1;
  }
  return $std_list.finish({ b: out });
}

export function same_kind({ a, b }) {
  return $obligations.kind_text({ k: a }) === $obligations.kind_text({ k: b });
}

export function find_ob({ ctx, k, kind }) {
  for (const o of obligations_at({ ctx: ctx, k: k })) {
    if (same_kind({ a: o.kind, b: kind })) {
      return { tag: "Some", value: o };
    }
  }
  return { tag: "None" };
}

export function is_proved({ o }) {
  return o.status.tag === "Proved";
}

export function is_checked({ o }) {
  return o.status.tag === "Checked";
}

export function refinement_preds({ ctx, t }) {
  let out = $std_list.builder({  });
  let cur = t;
  let guard = 1000;
  while (cur.tag === "Refined" && guard > 0) {
    guard = $rt.int.sub(guard, 1, $ob5);
    const $m18 = cur;
    $m18$match: {
      if ($m18.tag === "Refined") {
        const base = $m18.base;
        const pred = $m18.pred;
        const $m19 = $std_map.find({ d: ctx.where_exprs, key: pred });
        $m19$match: {
          if ($m19.tag === "Some") {
            const value = $m19.value;
            const [, out$2] = $std_list.push({ b: out, x: { e: value, file: $lower.pred_file({ k: pred }) } });
            out = out$2;
            break $m19$match;
          }
          if ($m19.tag === "None") {
            skip({  });
            break $m19$match;
          }
          $rt.unreachable();
        }
        cur = base;
        break $m18$match;
      }
      if (true) {
        guard = 0;
        break $m18$match;
      }
      $rt.unreachable();
    }
  }
  return $std_list.finish({ b: out });
}

export function is_option({ ctx, t }) {
  const s = $types.strip({ t: t });
  if (s.tag === "UnionT") {
    return $context.qualified_name({ ctx: ctx, id: $types.def_of({ t: s }) }) === "std.option.Option";
  }
  return false;
}

export function is_unit({ t }) {
  const s = $types.strip({ t: t });
  return s.tag === "Prim" && $types.prim_name({ t: s }) === "Unit";
}

export function is_int_like({ t }) {
  const s = $types.strip({ t: t });
  if (!(s.tag === "Prim")) {
    return false;
  }
  const n = $types.prim_name({ t: s });
  return n === "Int" || n === "Duration";
}

export function type_slug({ fuel, ctx, t }) {
  if (fuel === 0) {
    return "X";
  }
  const s = $types.strip({ t: t });
  const $m21 = s;
  $m21$match: {
    if ($m21.tag === "Prim") {
      const name = $m21.name;
      return name;
      break $m21$match;
    }
    if ($m21.tag === "RecordT") {
      const def = $m21.def;
      const args = $m21.args;
      return $context.get_def({ ctx: ctx, id: def }).name + args_slug({ fuel: $rt.int.sub(fuel, 1, $ob6), ctx: ctx, args: args });
      break $m21$match;
    }
    if ($m21.tag === "UnionT") {
      const def = $m21.def;
      const args = $m21.args;
      return $context.get_def({ ctx: ctx, id: def }).name + args_slug({ fuel: $rt.int.sub(fuel, 1, $ob7), ctx: ctx, args: args });
      break $m21$match;
    }
    if ($m21.tag === "Opaque") {
      const def = $m21.def;
      const args = $m21.args;
      return $context.get_def({ ctx: ctx, id: def }).name + args_slug({ fuel: $rt.int.sub(fuel, 1, $ob8), ctx: ctx, args: args });
      break $m21$match;
    }
    if ($m21.tag === "Capability") {
      const def = $m21.def;
      const args = $m21.args;
      return $context.get_def({ ctx: ctx, id: def }).name + args_slug({ fuel: $rt.int.sub(fuel, 1, $ob9), ctx: ctx, args: args });
      break $m21$match;
    }
    if ($m21.tag === "ParamT") {
      const def = $m21.def;
      return $context.get_def({ ctx: ctx, id: def }).name;
      break $m21$match;
    }
    if (true) {
      return "X";
      break $m21$match;
    }
    $rt.unreachable();
  }
}

export function args_slug({ fuel, ctx, args }) {
  if (fuel === 0) {
    return "";
  }
  let out = "";
  for (const a of args) {
    const $m22 = a;
    $m22$match: {
      if ($m22.tag === "TypeA") {
        const ty = $m22.ty;
        out = out + "_" + type_slug({ fuel: $rt.int.sub(fuel, 1, $ob10), ctx: ctx, t: ty });
        break $m22$match;
      }
      if (true) {
        skip({  });
        break $m22$match;
      }
      $rt.unreachable();
    }
  }
  return out;
}

export function impl_target({ ctx, impl_def }) {
  for (const iface of $std_map.keys({ d: ctx.impls })) {
    const $m23 = $std_map.find({ d: ctx.impls, key: iface });
    $m23$match: {
      if ($m23.tag === "Some") {
        const value = $m23.value;
        for (const im of value) {
          if (im.def === impl_def) {
            return im.target;
          }
        }
        break $m23$match;
      }
      if ($m23.tag === "None") {
        skip({  });
        break $m23$match;
      }
      $rt.unreachable();
    }
  }
  return { tag: "ErrorT" };
}

export function impl_fn_name({ ctx, def }) {
  const parent = or_neg({ o: def.parent });
  if (parent < 0) {
    return def.name;
  }
  const im = $context.get_def({ ctx: ctx, id: parent });
  return im.name + "$" + type_slug({ fuel: 1000, ctx: ctx, t: impl_target({ ctx: ctx, impl_def: parent }) }) + "$" + def.name;
}

export function emitted_name({ ctx, def }) {
  const parent = or_neg({ o: def.parent });
  if (parent >= 0 && $rt.eq($context.get_def({ ctx: ctx, id: parent }).kind, { tag: "Impl" })) {
    return impl_fn_name({ ctx: ctx, def: def });
  }
  return def.name;
}

export function impl_dict_name({ ctx, iface, target }) {
  return iface.name + "$" + type_slug({ fuel: 1000, ctx: ctx, t: target });
}

export function subst_of({ ctx, ty }) {
  const s = $types.strip({ t: ty });
  let subst = $std_map.dict({  });
  if (!(s.tag === "RecordT" || s.tag === "UnionT" || s.tag === "Opaque")) {
    return subst;
  }
  const args = $types.args_of({ t: s });
  let i = 0;
  for (const p of tparams_or({ o: $std_map.find({ d: ctx.type_params, key: $types.def_of({ t: s }) }) })) {
    if (i < $std_list.len({ xs: args })) {
      const [, subst$3] = $std_map.set({ d: subst, key: $types.tparam_def({ p: p }), value: $std_list.get({ xs: args, i: i }) });
      subst = subst$3;
    }
    i = $rt.int.add(i, 1, $ob11);
  }
  return subst;
}

export function type_param_defs({ ctx, owner }) {
  let out = $std_list.builder({  });
  for (const p of tparams_or({ o: $std_map.find({ d: ctx.type_params, key: owner }) })) {
    const $m26 = p;
    $m26$match: {
      if ($m26.tag === "TypeP") {
        const def = $m26.def;
        const [, out$4] = $std_list.push({ b: out, x: def });
        out = out$4;
        break $m26$match;
      }
      if (true) {
        skip({  });
        break $m26$match;
      }
      $rt.unreachable();
    }
  }
  return $std_list.finish({ b: out });
}

export function field_list({ ctx, owner }) {
  let out = $std_list.builder({  });
  for (const f of fields_or({ o: $std_map.find({ d: ctx.fields, key: owner }) })) {
    const [, out$5] = $std_list.push({ b: out, x: { name: f.name, ty: f.ty } });
    out = out$5;
  }
  return $std_list.finish({ b: out });
}

export function find_field({ fs, name }) {
  for (const f of fs) {
    if (f.name === name) {
      return { tag: "Some", value: f };
    }
  }
  return { tag: "None" };
}

export function begin_collect({ l }) {
  const saved = l.pre;
  l = { ...l, pre: [] };
  return [saved, l];
}

export function end_collect({ l, saved, out }) {
  const result_stmts = [...l.pre, ...out];
  l = { ...l, pre: saved };
  return [result_stmts, l];
}

export function push_pre({ l, s }) {
  l = { ...l, pre: $std_list.append({ xs: l.pre, x: s }) };
  return [undefined, l];
  return [undefined, l];
}

export function lower_module({ ctx, m, tables, opts }) {
  let l = { file: m.file, m: m, tables: tables, fn_ctx: { tag: "None" }, subst: $std_map.dict({  }), it_expr: { tag: "None" }, field_object: { tag: "None" }, temp: 0, pre: [], impl_fns: $std_map.dict({  }), verifying: false, opts: opts };
  let items = $std_list.builder({  });
  let main = { tag: "None" };
  for (const item of m.tree.items) {
    const [$r39, l$6] = item_ir({ l: l, ctx: ctx, item: item });
    l = l$6;
    const $m38 = $r39;
    $m38$match: {
      if ($m38.tag === "Some") {
        const value = $m38.value;
        const [, items$7] = $std_list.push({ b: items, x: value });
        items = items$7;
        break $m38$match;
      }
      if ($m38.tag === "None") {
        skip({  });
        break $m38$match;
      }
      $rt.unreachable();
    }
    const $m40 = item;
    $m40$match: {
      if ($m40.tag === "FnItem") {
        const decl = $m40.decl;
        if (decl.name.text === "main" && decl.vis.is_pub) {
          main = main_spec({ l: l, ctx: ctx, f: decl });
        }
        break $m40$match;
      }
      if (true) {
        skip({  });
        break $m40$match;
      }
      $rt.unreachable();
    }
  }
  const [$r41, l$8] = tests_of({ l: l, ctx: ctx });
  l = l$8;
  const tests = $r41;
  let verifies = [];
  if (opts.verify_all) {
    const [$r42, l$9] = verifies_of({ l: l, ctx: ctx });
    l = l$9;
    verifies = $r42;
  }
  return { mod: m, items: $std_list.finish({ b: items }), tests: tests, verifies: verifies, main: main };
}

export function item_ir({ l, ctx, item }) {
  const $m44 = item;
  $m44$match: {
    if ($m44.tag === "FnItem") {
      const decl = $m44.decl;
      const [$r47, l$10] = fn_decl({ l: l, ctx: ctx, f: decl, impl_name: { tag: "None" } });
      l = l$10;
      const $m45 = $r47;
      $m45$match: {
        if ($m45.tag === "Some") {
          const value = $m45.value;
          return [{ tag: "Some", value: { tag: "FnItem", f: value } }, l];
          break $m45$match;
        }
        if ($m45.tag === "None") {
          return [{ tag: "None" }, l];
          break $m45$match;
        }
        $rt.unreachable();
      }
      break $m44$match;
    }
    if ($m44.tag === "ConstItem") {
      const decl = $m44.decl;
      const def = $context.get_def({ ctx: ctx, id: def_at({ l: l, ctx: ctx, tag: $defs.tag_item, span: decl.span }) });
      const ty = type_or({ o: $std_map.find({ d: ctx.decl_types, key: def.id }) });
      const $m51 = $std_map.find({ d: ctx.const_values, key: def.id });
      $m51$match: {
        if ($m51.tag === "Some") {
          const value = $m51.value;
          return [{ tag: "Some", value: { tag: "ConstItem", def: def, ty: ty, value: { tag: "ValueE", value: value } } }, l];
          break $m51$match;
        }
        if ($m51.tag === "None") {
          const [$r55, l$11] = expr({ fuel: max_depth, l: l, ctx: ctx, e: decl.value, discard: false });
          l = l$11;
          return [{ tag: "Some", value: { tag: "ConstItem", def: def, ty: ty, value: $r55 } }, l];
          break $m51$match;
        }
        $rt.unreachable();
      }
      break $m44$match;
    }
    if ($m44.tag === "TypeAliasItem") {
      const decl = $m44.decl;
      const def = $context.get_def({ ctx: ctx, id: def_at({ l: l, ctx: ctx, tag: $defs.tag_item, span: decl.span }) });
      return [{ tag: "Some", value: { tag: "AliasItem", def: def, ty: type_or({ o: $std_map.find({ d: ctx.type_aliases, key: def.id }) }) } }, l];
      break $m44$match;
    }
    if ($m44.tag === "IntrinsicTypeItem") {
      const decl = $m44.decl;
      const def = $context.get_def({ ctx: ctx, id: def_at({ l: l, ctx: ctx, tag: $defs.tag_item, span: decl.span }) });
      return [{ tag: "Some", value: { tag: "IntrinsicTypeItem", def: def, params: type_param_defs({ ctx: ctx, owner: def.id }) } }, l];
      break $m44$match;
    }
    if ($m44.tag === "RecordItem") {
      const decl = $m44.decl;
      const def = $context.get_def({ ctx: ctx, id: def_at({ l: l, ctx: ctx, tag: $defs.tag_item, span: decl.span }) });
      return [{ tag: "Some", value: { tag: "RecordItem", def: def, params: type_param_defs({ ctx: ctx, owner: def.id }), fields: field_list({ ctx: ctx, owner: def.id }) } }, l];
      break $m44$match;
    }
    if ($m44.tag === "UnionItem") {
      const decl = $m44.decl;
      const def = $context.get_def({ ctx: ctx, id: def_at({ l: l, ctx: ctx, tag: $defs.tag_item, span: decl.span }) });
      let variants = $std_list.builder({  });
      for (const v of ids_or({ o: $std_map.find({ d: ctx.variants, key: def.id }) })) {
        const [, variants$12] = $std_list.push({ b: variants, x: { def: $context.get_def({ ctx: ctx, id: v }), fields: field_list({ ctx: ctx, owner: v }) } });
        variants = variants$12;
      }
      return [{ tag: "Some", value: { tag: "UnionItem", def: def, params: type_param_defs({ ctx: ctx, owner: def.id }), variants: $std_list.finish({ b: variants }) } }, l];
      break $m44$match;
    }
    if ($m44.tag === "InterfaceItem") {
      const decl = $m44.decl;
      const def = $context.get_def({ ctx: ctx, id: def_at({ l: l, ctx: ctx, tag: $defs.tag_item, span: decl.span }) });
      let tparam = { tag: "None" };
      let fns = $std_list.builder({  });
      for (const d of $std_list.finish({ b: ctx.defs })) {
        if (or_neg({ o: d.parent }) === def.id) {
          if ($rt.eq(d.kind, { tag: "TypeParam" }) && tparam.tag === "None") {
            tparam = { tag: "Some", value: d.name };
          }
          if ($rt.eq(d.kind, { tag: "IfaceFn" })) {
            const $m71 = $std_map.find({ d: ctx.signatures, key: d.id });
            $m71$match: {
              if ($m71.tag === "Some") {
                const value = $m71.value;
                const [, fns$13] = $std_list.push({ b: fns, x: { def: d, sig: value } });
                fns = fns$13;
                break $m71$match;
              }
              if ($m71.tag === "None") {
                skip({  });
                break $m71$match;
              }
              $rt.unreachable();
            }
          }
        }
      }
      return [{ tag: "Some", value: { tag: "InterfaceItem", def: def, tparam: tparam, fns: $std_list.finish({ b: fns }) } }, l];
      break $m44$match;
    }
    if ($m44.tag === "ImplItem") {
      const decl = $m44.decl;
      const [$r75, l$14] = impl_item({ l: l, ctx: ctx, item: decl });
      l = l$14;
      return [$r75, l];
      break $m44$match;
    }
    if ($m44.tag === "CapabilityItem") {
      const decl = $m44.decl;
      return [{ tag: "Some", value: { tag: "CapabilityItem", def: $context.get_def({ ctx: ctx, id: def_at({ l: l, ctx: ctx, tag: $defs.tag_item, span: decl.span }) }) } }, l];
      break $m44$match;
    }
    if (true) {
      return [{ tag: "None" }, l];
      break $m44$match;
    }
    $rt.unreachable();
  }
}

export function iface_fn_named({ ctx, iface, name }) {
  for (const d of $std_list.finish({ b: ctx.defs })) {
    if (or_neg({ o: d.parent }) === iface && $rt.eq(d.kind, { tag: "IfaceFn" }) && d.name === name) {
      return d.id;
    }
  }
  return $rt.int.neg(1, $ob12);
}

export function impl_map({ l, ctx, iface, fns }) {
  let map = $std_map.dict({  });
  for (const f of fns) {
    const fd = $context.get_def({ ctx: ctx, id: def_at({ l: l, ctx: ctx, tag: $defs.tag_item, span: f.span }) });
    const want = iface_fn_named({ ctx: ctx, iface: iface, name: f.name.text });
    if (want >= 0) {
      const [, map$15] = $std_map.set({ d: map, key: want, value: { def: fd, name: impl_fn_name({ ctx: ctx, def: fd }) } });
      map = map$15;
    }
  }
  return map;
}

export function impl_item({ l, ctx, item }) {
  const impl_key = key({ l: l, tag: $defs.tag_item, span: item.span });
  const iface_id = ref_def({ ctx: ctx, k: impl_key });
  if (iface_id < 0) {
    return [{ tag: "None" }, l];
  }
  const iface = $context.get_def({ ctx: ctx, id: iface_id });
  const def = $context.get_def({ ctx: ctx, id: or_neg({ o: $std_map.find({ d: ctx.def_of, key: impl_key }) }) });
  const target = impl_target({ ctx: ctx, impl_def: def.id });
  l = { ...l, impl_fns: impl_map({ l: l, ctx: ctx, iface: iface_id, fns: item.fns }) };
  let fns = $std_list.builder({  });
  for (const f of item.fns) {
    const fd = $context.get_def({ ctx: ctx, id: def_at({ l: l, ctx: ctx, tag: $defs.tag_item, span: f.span }) });
    const [$r85, l$16] = fn_decl({ l: l, ctx: ctx, f: f, impl_name: { tag: "Some", value: impl_fn_name({ ctx: ctx, def: fd }) } });
    l = l$16;
    const $m83 = $r85;
    $m83$match: {
      if ($m83.tag === "Some") {
        const value = $m83.value;
        const [, fns$17] = $std_list.push({ b: fns, x: value });
        fns = fns$17;
        break $m83$match;
      }
      if ($m83.tag === "None") {
        skip({  });
        break $m83$match;
      }
      $rt.unreachable();
    }
  }
  l = { ...l, impl_fns: $std_map.dict({  }) };
  const fn_list = $std_list.finish({ b: fns });
  let entries = $std_list.builder({  });
  for (const f of fn_list) {
    const [, entries$18] = $std_list.push({ b: entries, x: { name: f.def.name, fn_ir: f } });
    entries = entries$18;
  }
  return [{ tag: "Some", value: { tag: "ImplItem", def: def, iface: iface, target: target, dict_name: impl_dict_name({ ctx: ctx, iface: iface, target: target }), fns: fn_list, entries: $std_list.finish({ b: entries }) } }, l];
}

export function checked_somewhere({ ctx, source }) {
  const $hi90 = ctx.obligation_count;
  for (let i = 0; i < $hi90; i++) {
    const o = $context.get_obligation({ ctx: ctx, id: i });
    if (or_neg({ o: o.source }) === source && !is_proved({ o: o })) {
      return true;
    }
  }
  return false;
}

export function old_names({ root }) {
  let out = $std_list.builder({  });
  for (const n of $walk.nodes_in_expr({ e: root, into_verify: true })) {
    const $m91 = n;
    $m91$match: {
      if ($m91.tag === "NExpr") {
        const e = $m91.e;
        const $m92 = e;
        $m92$match: {
          if ($m92.tag === "Old") {
            const name = $m92.name;
            const [, out$19] = $std_list.push({ b: out, x: name.text });
            out = out$19;
            break $m92$match;
          }
          if (true) {
            skip({  });
            break $m92$match;
          }
          $rt.unreachable();
        }
        break $m91$match;
      }
      if (true) {
        skip({  });
        break $m91$match;
      }
      $rt.unreachable();
    }
  }
  return $std_list.finish({ b: out });
}

export function contains_text({ xs, x }) {
  for (const y of xs) {
    if (y === x) {
      return true;
    }
  }
  return false;
}

export function fn_decl({ l, ctx, f, impl_name }) {
  const def = $context.get_def({ ctx: ctx, id: def_at({ l: l, ctx: ctx, tag: $defs.tag_item, span: f.span }) });
  const $m93 = $std_map.find({ d: ctx.signatures, key: def.id });
  $m93$match: {
    if ($m93.tag === "None") {
      return [{ tag: "None" }, l];
      break $m93$match;
    }
    if ($m93.tag === "Some") {
      const value = $m93.value;
      const [$r95, l$20] = fn_decl_sig({ l: l, ctx: ctx, f: f, def: def, sig: value, impl_name: impl_name });
      l = l$20;
      return [$r95, l];
      break $m93$match;
    }
    $rt.unreachable();
  }
}

export function fn_decl_sig({ l, ctx, f, def, sig, impl_name }) {
  const tp = tparam_lists({ ctx: ctx, sig: sig });
  const dicts = tp.dicts;
  let params = $std_list.builder({  });
  let inout_names = $std_list.builder({  });
  for (const p of sig.params) {
    const [, params$21] = $std_list.push({ b: params, x: { name: p.name, ty: p.ty, is_inout: p.is_inout } });
    params = params$21;
    if (p.is_inout) {
      const [, inout_names$22] = $std_list.push({ b: inout_names, x: p.name });
      inout_names = inout_names$22;
    }
  }
  const param_list = $std_list.finish({ b: params });
  const name = text_or({ o: impl_name, dflt: def.name });
  if (f.is_intrinsic) {
    const q = $context.qualified_name({ ctx: ctx, id: def.id });
    let ns = "";
    let i = 0;
    for (const seg of $std_text.split({ t: q, sep: "." })) {
      if (i === 1) {
        ns = seg;
      }
      i = $rt.int.add(i, 1, $ob13);
    }
    return [{ tag: "Some", value: { def: def, sig: sig, name: name, dict_params: tp.dict_params, const_params: tp.const_params, params: param_list, ret: sig.ret, intrinsic: { tag: "Some", value: { ns: ns, name: def.name } }, entry_checks: [], body: { tag: "None" }, early_return: false } }, l];
  }
  if (f.body.tag === "None") {
    return [{ tag: "None" }, l];
  }
  let olds = [];
  let post = $std_list.builder({  });
  let measure_clause = { tag: "None" };
  for (const c of f.contracts) {
    if (c.clause === "ensures") {
      const [, post$23] = $std_list.push({ b: post, x: c });
      post = post$23;
    }
    if (c.clause === "decreases" && measure_clause.tag === "None") {
      measure_clause = { tag: "Some", value: c };
    }
    if (checked_somewhere({ ctx: ctx, source: key({ l: l, tag: $defs.tag_sig, span: c.span }) })) {
      for (const n of old_names({ root: c.expr })) {
        if (!contains_text({ xs: olds, x: n })) {
          olds = $std_list.append({ xs: olds, x: n });
        }
      }
    }
  }
  let measured = false;
  let measure_key = $rt.int.neg(1, $ob14);
  const $m105 = measure_clause;
  $m105$match: {
    if ($m105.tag === "Some") {
      const value = $m105.value;
      measure_key = key({ l: l, tag: $defs.tag_sig, span: value.span });
      const numeric = is_int_like({ t: type_of_expr({ l: l, ctx: ctx, e: value.expr }) });
      const $hi106 = ctx.obligation_count;
      for (let i = 0; i < $hi106; i++) {
        const o = $context.get_obligation({ ctx: ctx, id: i });
        if (numeric && o.def === def.id && o.kind.tag === "Decreases" && (o.callee.tag === "Some" || o.at === measure_key) && !is_proved({ o: o })) {
          measured = true;
        }
      }
      break $m105$match;
    }
    if ($m105.tag === "None") {
      skip({  });
      break $m105$match;
    }
    $rt.unreachable();
  }
  let measure_name = { tag: "None" };
  if (measured) {
    measure_name = { tag: "Some", value: "$measure" };
  }
  const fctx = { inout_names: $std_list.finish({ b: inout_names }), ret: sig.ret, post: $std_list.finish({ b: post }), def: { tag: "Some", value: def }, dicts: dicts, result_expr: { tag: "None" }, measure: measure_name };
  l = { ...l, fn_ctx: { tag: "Some", value: fctx } };
  const [$r114, l$24] = begin_collect({ l: l });
  l = l$24;
  const saved = $r114;
  const [$r115, l$25] = entry_checks({ l: l, ctx: ctx, sig: sig, contracts: f.contracts, def_name: def.name });
  l = l$25;
  const checks = $r115;
  const [$r116, l$26] = end_collect({ l: l, saved: saved, out: checks });
  l = l$26;
  let entry = $r116;
  const $m117 = measure_clause;
  $m117$match: {
    if ($m117.tag === "Some") {
      const value = $m117.value;
      if (measured) {
        const [$r118, l$27] = begin_collect({ l: l });
        l = l$27;
        const saved2 = $r118;
        const [$r119, l$28] = expr({ fuel: max_depth, l: l, ctx: ctx, e: value.expr, discard: false });
        l = l$28;
        const m = $r119;
        const [$r122, l$29] = end_collect({ l: l, saved: saved2, out: [{ tag: "Let", name: "$measure", ty: { tag: "Prim", name: "Int" }, mutable: false, value: m }] });
        l = l$29;
        entry = [...entry, ...$r122];
        const $hi123 = ctx.obligation_count;
        for (let i = 0; i < $hi123; i++) {
          const o = $context.get_obligation({ ctx: ctx, id: i });
          if (o.def === def.id && o.kind.tag === "Decreases" && o.at === measure_key) {
            if (!is_proved({ o: o })) {
              entry = $std_list.append({ xs: entry, x: { tag: "Check", cond: { tag: "Cmp", op: ">=", left: { tag: "Local", name: "$measure", ty: { tag: "Prim", name: "Int" } }, right: { tag: "IntE", v: 0 }, is_float: false }, ob: ob_ref_of({ l: l, ctx: ctx, o: o }) } });
            }
          }
        }
      }
      break $m117$match;
    }
    if ($m117.tag === "None") {
      skip({  });
      break $m117$match;
    }
    $rt.unreachable();
  }
  for (const o of olds) {
    let ty = { tag: "ErrorT" };
    for (const p of param_list) {
      if (p.name === o) {
        ty = p.ty;
      }
    }
    entry = $std_list.append({ xs: entry, x: { tag: "Let", name: "$old_" + o, ty: ty, mutable: false, value: { tag: "Snapshot", value: { tag: "Local", name: o, ty: ty }, ty: ty } } });
  }
  let body = [];
  let early = false;
  const $m133 = f.body;
  $m133$match: {
    if ($m133.tag === "Some") {
      const value = $m133.value;
      const [$r134, l$30] = block({ fuel: max_depth, l: l, ctx: ctx, b: value });
      l = l$30;
      body = $r134;
      early = block_has_try({ b: value });
      break $m133$match;
    }
    if ($m133.tag === "None") {
      skip({  });
      break $m133$match;
    }
    $rt.unreachable();
  }
  if ($std_list.len({ xs: inout_names_of({ l: l }) }) > 0 && is_unit({ t: sig.ret })) {
    body = $std_list.append({ xs: body, x: { tag: "Return", value: { tag: "UnitE" } } });
  }
  l = { ...l, fn_ctx: { tag: "None" } };
  return [{ tag: "Some", value: { def: def, sig: sig, name: name, dict_params: tp.dict_params, const_params: tp.const_params, params: param_list, ret: sig.ret, intrinsic: { tag: "None" }, entry_checks: entry, body: { tag: "Some", value: body }, early_return: early } }, l];
}

export function tparam_lists({ ctx, sig }) {
  let dict_params = $std_list.builder({  });
  let const_params = $std_list.builder({  });
  let dicts = $std_map.dict({  });
  for (const p of sig.tparams) {
    const $m143 = p;
    $m143$match: {
      if ($m143.tag === "TypeP") {
        const def = $m143.def;
        const bound = $m143.bound;
        const $m144 = bound;
        $m144$match: {
          if ($m144.tag === "Some") {
            const value = $m144.value;
            const name = "$dict_" + $context.get_def({ ctx: ctx, id: def }).name;
            const [, dict_params$31] = $std_list.push({ b: dict_params, x: { def: def, iface: value, name: name } });
            dict_params = dict_params$31;
            const [, dicts$32] = $std_map.set({ d: dicts, key: def, value: name });
            dicts = dicts$32;
            break $m144$match;
          }
          if ($m144.tag === "None") {
            skip({  });
            break $m144$match;
          }
          $rt.unreachable();
        }
        break $m143$match;
      }
      if ($m143.tag === "ConstP") {
        const def = $m143.def;
        const ty = $m143.ty;
        const [, const_params$33] = $std_list.push({ b: const_params, x: { def: def, name: $context.get_def({ ctx: ctx, id: def }).name, ty: ty } });
        const_params = const_params$33;
        break $m143$match;
      }
      if (true) {
        skip({  });
        break $m143$match;
      }
      $rt.unreachable();
    }
  }
  return { dict_params: $std_list.finish({ b: dict_params }), const_params: $std_list.finish({ b: const_params }), dicts: dicts };
}

export function inout_names_of({ l }) {
  const $m148 = l.fn_ctx;
  $m148$match: {
    if ($m148.tag === "Some") {
      const value = $m148.value;
      return value.inout_names;
      break $m148$match;
    }
    if ($m148.tag === "None") {
      return [];
      break $m148$match;
    }
    $rt.unreachable();
  }
}

export function entry_checks({ l, ctx, sig, contracts, def_name }) {
  let out = $std_list.builder({  });
  let call_sites = $std_list.builder({  });
  const $hi149 = ctx.obligation_count;
  for (let i = 0; i < $hi149; i++) {
    const o = $context.get_obligation({ ctx: ctx, id: i });
    if (or_neg({ o: o.callee }) === sig.def) {
      const [, call_sites$34] = $std_list.push({ b: call_sites, x: o });
      call_sites = call_sites$34;
    }
  }
  const sites = $std_list.finish({ b: call_sites });
  let i = 0;
  for (const p of sig.params) {
    if (i < $std_list.len({ xs: sig.param_defs })) {
      const pd = $std_list.get({ xs: sig.param_defs, i: i });
      const $m150 = $std_map.find({ d: ctx.decl_types, key: pd });
      $m150$match: {
        if ($m150.tag === "None") {
          skip({  });
          break $m150$match;
        }
        if ($m150.tag === "Some") {
          const value = $m150.value;
          let flows = 0;
          let all_proved = true;
          for (const o of sites) {
            if (o.kind.tag === "Refinement" && text_or({ o: o.param, dflt: "" }) === p.name) {
              flows = $rt.int.add(flows, 1, $ob15);
              if (!is_proved({ o: o })) {
                all_proved = false;
              }
            }
          }
          if (!(flows > 0 && all_proved)) {
            const pdef = $context.get_def({ ctx: ctx, id: pd });
            const local = { tag: "Local", name: p.name, ty: value };
            for (const pred of refinement_preds({ ctx: ctx, t: value })) {
              const [$r152, l$35] = with_it({ fuel: max_depth, l: l, ctx: ctx, it_expr: local, pred: pred });
              l = l$35;
              const cond = $r152;
              const [, out$36] = $std_list.push({ b: out, x: { tag: "Check", cond: cond, ob: ob_ref({ l: l, ctx: ctx, kind: "refinement", text: $printer.print_expr({ e: pred.e }), file: pdef.file, span: pdef.span, def: def_name }) } });
              out = out$36;
            }
          }
          break $m150$match;
        }
        $rt.unreachable();
      }
    }
    i = $rt.int.add(i, 1, $ob16);
  }
  for (const c of contracts) {
    if (c.clause === "requires" && !c.is_proved) {
      const ck = key({ l: l, tag: $defs.tag_sig, span: c.span });
      let n = 0;
      let all_proved = true;
      for (const o of sites) {
        if (o.kind.tag === "Requires" && or_neg({ o: o.source }) === ck) {
          n = $rt.int.add(n, 1, $ob17);
          if (!is_proved({ o: o })) {
            all_proved = false;
          }
        }
      }
      if (!(n > 0 && all_proved)) {
        const [$r154, l$37] = expr({ fuel: max_depth, l: l, ctx: ctx, e: c.expr, discard: false });
        l = l$37;
        const [, out$38] = $std_list.push({ b: out, x: { tag: "Check", cond: $r154, ob: ob_ref({ l: l, ctx: ctx, kind: "requires", text: $printer.print_expr({ e: c.expr }), file: l.file, span: c.span, def: def_name }) } });
        out = out$38;
      }
    }
  }
  return [$std_list.finish({ b: out }), l];
}

export function with_it({ fuel, l, ctx, it_expr, pred }) {
  if (fuel === 0) {
    return [{ tag: "UnitE" }, l];
  }
  const saved_it = l.it_expr;
  const saved_file = l.file;
  l = { ...l, it_expr: { tag: "Some", value: it_expr }, file: pred.file };
  const [$r159, l$39] = expr({ fuel: $rt.int.sub(fuel, 1, $ob18), l: l, ctx: ctx, e: pred.e, discard: false });
  l = l$39;
  const out = $r159;
  l = { ...l, it_expr: saved_it, file: saved_file };
  return [out, l];
}

export function main_spec({ l, ctx, f }) {
  const fn_id = def_at({ l: l, ctx: ctx, tag: $defs.tag_item, span: f.span });
  const $m161 = $std_map.find({ d: ctx.signatures, key: fn_id });
  $m161$match: {
    if ($m161.tag === "None") {
      return { tag: "None" };
      break $m161$match;
    }
    if ($m161.tag === "Some") {
      const value = $m161.value;
      let roots = $std_list.builder({  });
      let args = "args";
      for (const p of value.params) {
        const s = $types.strip({ t: p.ty });
        const $m163 = s;
        $m163$match: {
          if ($m163.tag === "Capability") {
            const def = $m163.def;
            if ($std_text.starts_with({ t: $context.qualified_name({ ctx: ctx, id: def }), prefix: "std.io." })) {
              const [, roots$40] = $std_list.push({ b: roots, x: { name: p.name, kind: $context.get_def({ ctx: ctx, id: def }).name } });
              roots = roots$40;
            }
            break $m163$match;
          }
          if ($m163.tag === "Opaque") {
            args = p.name;
            break $m163$match;
          }
          if (true) {
            skip({  });
            break $m163$match;
          }
          $rt.unreachable();
        }
      }
      return { tag: "Some", value: { roots: $std_list.finish({ b: roots }), args: args, status: main_reports_status({ ctx: ctx, ret: value.ret }) } };
      break $m161$match;
    }
    $rt.unreachable();
  }
}

export function main_reports_status({ ctx, ret }) {
  let probe = ctx;
  const [$r168, probe$41] = $capabilities.main_status({ ctx: probe, ret: ret });
  probe = probe$41;
  const $m167 = $r168;
  $m167$match: {
    if ($m167.tag === "Some") {
      const value = $m167.value;
      return value;
      break $m167$match;
    }
    if ($m167.tag === "None") {
      return false;
      break $m167$match;
    }
    $rt.unreachable();
  }
}

export function block({ fuel, l, ctx, b }) {
  if (fuel === 0) {
    return [[], l];
  }
  let out = [];
  for (const s of b.stmts) {
    const [$r169, l$42] = stmt({ fuel: $rt.int.sub(fuel, 1, $ob19), l: l, ctx: ctx, s: s });
    l = l$42;
    out = [...out, ...$r169];
  }
  return [out, l];
}

export function stmt({ fuel, l, ctx, s }) {
  if (fuel === 0) {
    return [[], l];
  }
  const [$r170, l$43] = begin_collect({ l: l });
  l = l$43;
  const saved = $r170;
  const [$r171, l$44] = stmt_inner({ fuel: $rt.int.sub(fuel, 1, $ob20), l: l, ctx: ctx, s: s });
  l = l$44;
  const out = $r171;
  const [$r172, l$45] = end_collect({ l: l, saved: saved, out: out });
  l = l$45;
  return [$r172, l];
}

export function stmt_inner({ fuel, l, ctx, s }) {
  if (fuel === 0) {
    return [[], l];
  }
  const $m173 = s;
  $m173$match: {
    if ($m173.tag === "Let") {
      const name = $m173.name;
      const value = $m173.value;
      const span = $m173.span;
      const [$r174, l$46] = let_stmt({ fuel: $rt.int.sub(fuel, 1, $ob21), l: l, ctx: ctx, name: name.text, e: value, at: span, mutable: false });
      l = l$46;
      return [$r174, l];
      break $m173$match;
    }
    if ($m173.tag === "Var") {
      const name = $m173.name;
      const value = $m173.value;
      const span = $m173.span;
      const [$r175, l$47] = let_stmt({ fuel: $rt.int.sub(fuel, 1, $ob22), l: l, ctx: ctx, name: name.text, e: value, at: span, mutable: true });
      l = l$47;
      return [$r175, l];
      break $m173$match;
    }
    if ($m173.tag === "Assign") {
      const name = $m173.name;
      const value = $m173.value;
      const span = $m173.span;
      const def = ref_def({ ctx: ctx, k: key({ l: l, tag: $defs.tag_stmt, span: span }) });
      let declared = { tag: "ErrorT" };
      let target = name.text;
      if (def >= 0) {
        declared = type_or({ o: $std_map.find({ d: ctx.decl_types, key: def }) });
        target = $context.get_def({ ctx: ctx, id: def }).name;
      }
      const [$r177, l$48] = checked_flow({ fuel: $rt.int.sub(fuel, 1, $ob23), l: l, ctx: ctx, e: value, declared: declared });
      l = l$48;
      return [[{ tag: "Assign", name: target, ty: declared, value: $r177 }], l];
      break $m173$match;
    }
    if ($m173.tag === "Return") {
      const value = $m173.value;
      const span = $m173.span;
      const [$r179, l$49] = return_stmt({ fuel: $rt.int.sub(fuel, 1, $ob24), l: l, ctx: ctx, e: value, at: span });
      l = l$49;
      return [$r179, l];
      break $m173$match;
    }
    if ($m173.tag === "If") {
      const cond = $m173.cond;
      const then_block = $m173.then_block;
      const else_block = $m173.else_block;
      const [$r180, l$50] = expr({ fuel: $rt.int.sub(fuel, 1, $ob25), l: l, ctx: ctx, e: cond, discard: false });
      l = l$50;
      const c = $r180;
      const [$r181, l$51] = block({ fuel: $rt.int.sub(fuel, 1, $ob26), l: l, ctx: ctx, b: then_block });
      l = l$51;
      const then_ir = $r181;
      let else_ir = { tag: "None" };
      const $m183 = else_block;
      $m183$match: {
        if ($m183.tag === "Some") {
          const value = $m183.value;
          const [$r184, l$52] = block({ fuel: $rt.int.sub(fuel, 1, $ob27), l: l, ctx: ctx, b: value });
          l = l$52;
          else_ir = { tag: "Some", value: $r184 };
          break $m183$match;
        }
        if ($m183.tag === "None") {
          skip({  });
          break $m183$match;
        }
        $rt.unreachable();
      }
      return [[{ tag: "If", cond: c, then_block: then_ir, else_block: else_ir }], l];
      break $m173$match;
    }
    if ($m173.tag === "Match") {
      const scrutinee = $m173.scrutinee;
      const arms = $m173.arms;
      const [$r187, l$53] = match_stmt({ fuel: $rt.int.sub(fuel, 1, $ob28), l: l, ctx: ctx, scrutinee: scrutinee, arms: arms });
      l = l$53;
      return [$r187, l];
      break $m173$match;
    }
    if ($m173.tag === "Loop") {
      const cond = $m173.cond;
      const clauses = $m173.clauses;
      const body = $m173.body;
      const span = $m173.span;
      const [$r188, l$54] = loop_stmt({ fuel: $rt.int.sub(fuel, 1, $ob29), l: l, ctx: ctx, cond: cond, clauses: clauses, body: body, at: span });
      l = l$54;
      return [$r188, l];
      break $m173$match;
    }
    if ($m173.tag === "For") {
      const name = $m173.name;
      const domain = $m173.domain;
      const body = $m173.body;
      const span = $m173.span;
      const [$r189, l$55] = for_stmt({ fuel: $rt.int.sub(fuel, 1, $ob30), l: l, ctx: ctx, domain: domain, body: body, at: span });
      l = l$55;
      return [$r189, l];
      break $m173$match;
    }
    if ($m173.tag === "Assume") {
      const claim = $m173.claim;
      const justification = $m173.justification;
      return [[{ tag: "Comment", text: "assume " + qname_text({ q: claim }) + " " + $json.quote({ t: justification }) }], l];
      break $m173$match;
    }
    if ($m173.tag === "ExprStmt") {
      const expr = $m173.expr;
      const [$r191, l$56] = expr_of({ fuel: $rt.int.sub(fuel, 1, $ob31), l: l, ctx: ctx, e: expr, discard: true });
      l = l$56;
      const e = $r191;
      if (e.tag === "UnitE") {
        return [[], l];
      }
      return [[{ tag: "ExprStmt", expr: e }], l];
      break $m173$match;
    }
    $rt.unreachable();
  }
}

export function qname_text({ q }) {
  let parts = $std_list.builder({  });
  for (const s of q.segments) {
    const [, parts$57] = $std_list.push({ b: parts, x: s.text });
    parts = parts$57;
  }
  return $std_text.join({ parts: $std_list.finish({ b: parts }), sep: "." });
}

export function let_stmt({ fuel, l, ctx, name, e, at, mutable }) {
  if (fuel === 0) {
    return [[], l];
  }
  const def = def_at({ l: l, ctx: ctx, tag: $defs.tag_stmt, span: at });
  let declared = { tag: "ErrorT" };
  let target = name;
  if (def >= 0) {
    declared = type_or({ o: $std_map.find({ d: ctx.decl_types, key: def }) });
    target = $context.get_def({ ctx: ctx, id: def }).name;
  }
  const [$r194, l$58] = checked_flow({ fuel: $rt.int.sub(fuel, 1, $ob32), l: l, ctx: ctx, e: e, declared: declared });
  l = l$58;
  return [[{ tag: "Let", name: target, ty: declared, mutable: mutable, value: $r194 }], l];
}

export function checked_flow({ fuel, l, ctx, e, declared }) {
  if (fuel === 0) {
    return [{ tag: "UnitE" }, l];
  }
  const ob = find_ob({ ctx: ctx, k: expr_key({ l: l, e: e }), kind: { tag: "Refinement" } });
  const [$r198, l$59] = expr({ fuel: $rt.int.sub(fuel, 1, $ob33), l: l, ctx: ctx, e: e, discard: false });
  l = l$59;
  const value = $r198;
  if (ob.tag === "None") {
    return [value, l];
  }
  const o = ob_or({ o: ob });
  if (!is_checked({ o: o })) {
    return [value, l];
  }
  const preds = refinement_preds({ ctx: ctx, t: declared });
  if ($std_list.len({ xs: preds }) === 0) {
    return [value, l];
  }
  const [$r199, l$60] = tmp({ l: l, prefix: "it" });
  l = l$60;
  const it_name = $r199;
  const it_expr = { tag: "Local", name: it_name, ty: declared };
  let checks = $std_list.builder({  });
  for (const p of preds) {
    const [$r201, l$61] = with_it({ fuel: $rt.int.sub(fuel, 1, $ob34), l: l, ctx: ctx, it_expr: it_expr, pred: p });
    l = l$61;
    const [, checks$62] = $std_list.push({ b: checks, x: { tag: "Check", cond: $r201, ob: ob_ref_of({ l: l, ctx: ctx, o: o }) } });
    checks = checks$62;
  }
  return [{ tag: "Checked", value: value, it_name: it_name, ty: declared, checks: $std_list.finish({ b: checks }) }, l];
}

export function ob_or({ o }) {
  const $m204 = o;
  $m204$match: {
    if ($m204.tag === "Some") {
      const value = $m204.value;
      return value;
      break $m204$match;
    }
    if ($m204.tag === "None") {
      return { id: $rt.int.neg(1, $ob35), kind: { tag: "Assertion" }, at: 0, at_file: 0, at_span: { start: 0, end: 0 }, def: 0, text: "", source: { tag: "None" }, site: { tag: "OtherSite" }, pinned: false, callee: { tag: "None" }, param: { tag: "None" }, status: { tag: "Pending" }, by: { tag: "None" } };
      break $m204$match;
    }
    $rt.unreachable();
  }
}

export function return_stmt({ fuel, l, ctx, e, at }) {
  if (fuel === 0) {
    return [[], l];
  }
  const $m214 = l.fn_ctx;
  $m214$match: {
    if ($m214.tag === "None") {
      const [$r215, l$63] = expr({ fuel: $rt.int.sub(fuel, 1, $ob36), l: l, ctx: ctx, e: e, discard: false });
      l = l$63;
      return [[{ tag: "Return", value: $r215 }], l];
      break $m214$match;
    }
    if ($m214.tag === "Some") {
      const value = $m214.value;
      const [$r217, l$64] = checked_flow({ fuel: $rt.int.sub(fuel, 1, $ob37), l: l, ctx: ctx, e: e, declared: value.ret });
      l = l$64;
      const v = $r217;
      let post_obs = $std_list.builder({  });
      for (const o of obligations_at({ ctx: ctx, k: key({ l: l, tag: $defs.tag_stmt, span: at }) })) {
        if (o.kind.tag === "Ensures" && is_checked({ o: o })) {
          const [, post_obs$65] = $std_list.push({ b: post_obs, x: o });
          post_obs = post_obs$65;
        }
      }
      if ($std_list.built({ b: post_obs }) === 0) {
        return [[{ tag: "Return", value: v }], l];
      }
      const [$r219, l$66] = tmp({ l: l, prefix: "r" });
      l = l$66;
      const r = $r219;
      const result_expr = { tag: "Local", name: r, ty: value.ret };
      let out = [{ tag: "Let", name: r, ty: value.ret, mutable: false, value: v }];
      l = { ...l, fn_ctx: { tag: "Some", value: { ...value, result_expr: { tag: "Some", value: result_expr } } } };
      for (const o of $std_list.finish({ b: post_obs })) {
        const src = or_neg({ o: o.source });
        for (const c of value.post) {
          if (key({ l: l, tag: $defs.tag_sig, span: c.span }) === src) {
            const [$r226, l$67] = expr({ fuel: $rt.int.sub(fuel, 1, $ob38), l: l, ctx: ctx, e: c.expr, discard: false });
            l = l$67;
            out = $std_list.append({ xs: out, x: { tag: "Check", cond: $r226, ob: ob_ref_of({ l: l, ctx: ctx, o: o }) } });
          }
        }
      }
      l = { ...l, fn_ctx: { tag: "Some", value: { ...value, result_expr: { tag: "None" } } } };
      return [$std_list.append({ xs: out, x: { tag: "Return", value: result_expr } }), l];
      break $m214$match;
    }
    $rt.unreachable();
  }
}

export function match_stmt({ fuel, l, ctx, scrutinee, arms }) {
  if (fuel === 0) {
    return [[], l];
  }
  const [$r233, l$68] = tmp({ l: l, prefix: "m" });
  l = l$68;
  const t = $r233;
  const ty = type_of_expr({ l: l, ctx: ctx, e: scrutinee });
  const [$r234, l$69] = expr({ fuel: $rt.int.sub(fuel, 1, $ob39), l: l, ctx: ctx, e: scrutinee, discard: false });
  l = l$69;
  const scrut = $r234;
  const subject = { tag: "Local", name: t, ty: ty };
  let out = $std_list.builder({  });
  for (const arm of arms) {
    const [$r236, l$70] = pattern_test_of({ fuel: $rt.int.sub(fuel, 1, $ob40), l: l, ctx: ctx, p: arm.pattern, subject: subject, ty: ty });
    l = l$70;
    const pattern_test = $r236;
    const bindings = pattern_bindings({ l: l, ctx: ctx, p: arm.pattern, subject: subject, ty: ty });
    let guard = { tag: "None" };
    const $m238 = arm.guard;
    $m238$match: {
      if ($m238.tag === "Some") {
        const value = $m238.value;
        const [$r239, l$71] = expr({ fuel: $rt.int.sub(fuel, 1, $ob41), l: l, ctx: ctx, e: value, discard: false });
        l = l$71;
        guard = { tag: "Some", value: $r239 };
        break $m238$match;
      }
      if ($m238.tag === "None") {
        skip({  });
        break $m238$match;
      }
      $rt.unreachable();
    }
    let body = [];
    const $m241 = arm.body;
    $m241$match: {
      if ($m241.tag === "ArmBlock") {
        const block = $m241.block;
        const [$r242, l$72] = block_ir({ fuel: $rt.int.sub(fuel, 1, $ob42), l: l, ctx: ctx, b: block });
        l = l$72;
        body = $r242;
        break $m241$match;
      }
      if ($m241.tag === "ArmStmt") {
        const stmt = $m241.stmt;
        const [$r243, l$73] = stmt_ir({ fuel: $rt.int.sub(fuel, 1, $ob43), l: l, ctx: ctx, s: stmt });
        l = l$73;
        body = $r243;
        break $m241$match;
      }
      $rt.unreachable();
    }
    const [, out$74] = $std_list.push({ b: out, x: { pattern_test: pattern_test, bindings: bindings, guard: guard, body: body } });
    out = out$74;
  }
  return [[{ tag: "Match", tmp: t, ty: ty, scrutinee: scrut, arms: $std_list.finish({ b: out }) }], l];
}

export function block_ir({ fuel, l, ctx, b }) {
  if (fuel === 0) {
    return [[], l];
  }
  const [$r246, l$75] = block({ fuel: $rt.int.sub(fuel, 1, $ob44), l: l, ctx: ctx, b: b });
  l = l$75;
  return [$r246, l];
}

export function stmt_ir({ fuel, l, ctx, s }) {
  if (fuel === 0) {
    return [[], l];
  }
  const [$r247, l$76] = stmt({ fuel: $rt.int.sub(fuel, 1, $ob45), l: l, ctx: ctx, s: s });
  l = l$76;
  return [$r247, l];
}

export function pattern_test_of({ fuel, l, ctx, p, subject, ty }) {
  if (fuel === 0) {
    return [{ tag: "None" }, l];
  }
  const $m249 = p;
  $m249$match: {
    if ($m249.tag === "WildcardPat") {
      return [{ tag: "None" }, l];
      break $m249$match;
    }
    if ($m249.tag === "BindPat") {
      return [{ tag: "None" }, l];
      break $m249$match;
    }
    if ($m249.tag === "LitPat") {
      const literal = $m249.literal;
      const [$r252, l$77] = expr({ fuel: $rt.int.sub(fuel, 1, $ob46), l: l, ctx: ctx, e: literal, discard: false });
      l = l$77;
      return [{ tag: "Some", value: { tag: "Eq", left: subject, right: $r252, ty: ty, prim: false, negate: false } }, l];
      break $m249$match;
    }
    if ($m249.tag === "VariantPat") {
      const span = $m249.span;
      const def = ref_def({ ctx: ctx, k: key({ l: l, tag: $defs.tag_pattern, span: span }) });
      if (def < 0) {
        return [{ tag: "Some", value: { tag: "BoolE", v: false } }, l];
      }
      return [{ tag: "Some", value: { tag: "IsVariant", subject: subject, variant: $context.get_def({ ctx: ctx, id: def }), ty: ty } }, l];
      break $m249$match;
    }
    $rt.unreachable();
  }
}

export function pattern_bindings({ l, ctx, p, subject, ty }) {
  const $m259 = p;
  $m259$match: {
    if ($m259.tag === "BindPat") {
      const name = $m259.name;
      return [{ name: name.text, ty: ty, value: subject }];
      break $m259$match;
    }
    if ($m259.tag === "VariantPat") {
      const fields = $m259.fields;
      const span = $m259.span;
      const $m261 = fields;
      $m261$match: {
        if ($m261.tag === "None") {
          return [];
          break $m261$match;
        }
        if ($m261.tag === "Some") {
          const value = $m261.value;
          const def = ref_def({ ctx: ctx, k: key({ l: l, tag: $defs.tag_pattern, span: span }) });
          if (def < 0) {
            return [];
          }
          const fs = fields_or({ o: $std_map.find({ d: ctx.fields, key: def }) });
          const subst = subst_of({ ctx: ctx, ty: ty });
          let out = $std_list.builder({  });
          let i = 0;
          let stop = false;
          for (const pf of value) {
            if (!stop) {
              const $m262 = pf;
              $m262$match: {
                if ($m262.tag === "PatFieldRest") {
                  stop = true;
                  break $m262$match;
                }
                if ($m262.tag === "PatFieldName") {
                  const name = $m262.name;
                  if (i < $std_list.len({ xs: fs })) {
                    const f = $std_list.get({ xs: fs, i: i });
                    const ft = $types.substitute({ t: f.ty, subst: subst });
                    const [, out$78] = $std_list.push({ b: out, x: { name: name.text, ty: ft, value: { tag: "FieldE", object: subject, name: f.name, ty: ft, owner: { tag: "Some", value: $context.get_def({ ctx: ctx, id: def }) } } } });
                    out = out$78;
                  }
                  i = $rt.int.add(i, 1, $ob47);
                  break $m262$match;
                }
                if ($m262.tag === "PatFieldSkip") {
                  i = $rt.int.add(i, 1, $ob48);
                  break $m262$match;
                }
                $rt.unreachable();
              }
            }
          }
          return $std_list.finish({ b: out });
          break $m261$match;
        }
        $rt.unreachable();
      }
      break $m259$match;
    }
    if (true) {
      return [];
      break $m259$match;
    }
    $rt.unreachable();
  }
}

export function find_source({ obs, kind, src }) {
  for (const o of obs) {
    if (same_kind({ a: o.kind, b: kind }) && or_neg({ o: o.source }) === src) {
      return { tag: "Some", value: o };
    }
  }
  return { tag: "None" };
}

export function loop_stmt({ fuel, l, ctx, cond, clauses, body, at }) {
  if (fuel === 0) {
    return [[], l];
  }
  const obs = obligations_at({ ctx: ctx, k: key({ l: l, tag: $defs.tag_stmt, span: at }) });
  let invariants = $std_list.builder({  });
  let dec_clause = { tag: "None" };
  for (const c of clauses) {
    if (c.clause === "invariant") {
      const [, invariants$79] = $std_list.push({ b: invariants, x: c });
      invariants = invariants$79;
    }
    if (c.clause === "decreases" && dec_clause.tag === "None") {
      dec_clause = { tag: "Some", value: c };
    }
  }
  const invs = $std_list.finish({ b: invariants });
  let out = [];
  for (const inv of invs) {
    const $m270 = find_source({ obs: obs, kind: { tag: "InvariantEntry" }, src: key({ l: l, tag: $defs.tag_sig, span: inv.span }) });
    $m270$match: {
      if ($m270.tag === "Some") {
        const value = $m270.value;
        if (is_checked({ o: value })) {
          const [$r272, l$80] = expr({ fuel: $rt.int.sub(fuel, 1, $ob49), l: l, ctx: ctx, e: inv.expr, discard: false });
          l = l$80;
          out = $std_list.append({ xs: out, x: { tag: "Check", cond: $r272, ob: ob_ref_of({ l: l, ctx: ctx, o: value }) } });
        }
        break $m270$match;
      }
      if ($m270.tag === "None") {
        skip({  });
        break $m270$match;
      }
      $rt.unreachable();
    }
  }
  const [$r274, l$81] = begin_collect({ l: l });
  l = l$81;
  const saved_cond = $r274;
  const [$r275, l$82] = expr({ fuel: $rt.int.sub(fuel, 1, $ob50), l: l, ctx: ctx, e: cond, discard: false });
  l = l$82;
  const c = $r275;
  const [$r276, l$83] = end_collect({ l: l, saved: saved_cond, out: [] });
  l = l$83;
  const cond_pre = $r276;
  let body_ir = [];
  let dec = { tag: "None" };
  const $m278 = dec_clause;
  $m278$match: {
    if ($m278.tag === "Some") {
      const value = $m278.value;
      dec = find_source({ obs: obs, kind: { tag: "Decreases" }, src: key({ l: l, tag: $defs.tag_sig, span: value.span }) });
      break $m278$match;
    }
    if ($m278.tag === "None") {
      skip({  });
      break $m278$match;
    }
    $rt.unreachable();
  }
  const [$r280, l$84] = tmp({ l: l, prefix: "measure" });
  l = l$84;
  const measure = $r280;
  const measure_expr = { tag: "Local", name: measure, ty: { tag: "Prim", name: "Int" } };
  let checked_decreases = false;
  const $m283 = dec;
  $m283$match: {
    if ($m283.tag === "Some") {
      const value = $m283.value;
      checked_decreases = is_checked({ o: value });
      break $m283$match;
    }
    if ($m283.tag === "None") {
      skip({  });
      break $m283$match;
    }
    $rt.unreachable();
  }
  if (checked_decreases) {
    const [$r284, l$85] = begin_collect({ l: l });
    l = l$85;
    const saved = $r284;
    const [$r285, l$86] = expr({ fuel: $rt.int.sub(fuel, 1, $ob51), l: l, ctx: ctx, e: clause_expr({ o: dec_clause }), discard: false });
    l = l$86;
    const m = $r285;
    const [$r288, l$87] = end_collect({ l: l, saved: saved, out: [{ tag: "Let", name: measure, ty: { tag: "Prim", name: "Int" }, mutable: false, value: m }] });
    l = l$87;
    body_ir = [...body_ir, ...$r288];
    body_ir = $std_list.append({ xs: body_ir, x: { tag: "Check", cond: { tag: "Cmp", op: ">=", left: measure_expr, right: { tag: "IntE", v: 0 }, is_float: false }, ob: ob_ref_of({ l: l, ctx: ctx, o: ob_or({ o: dec }) }) } });
  }
  const [$r292, l$88] = block({ fuel: $rt.int.sub(fuel, 1, $ob52), l: l, ctx: ctx, b: body });
  l = l$88;
  body_ir = [...body_ir, ...$r292];
  for (const inv of invs) {
    const $m293 = find_source({ obs: obs, kind: { tag: "InvariantStep" }, src: key({ l: l, tag: $defs.tag_sig, span: inv.span }) });
    $m293$match: {
      if ($m293.tag === "Some") {
        const value = $m293.value;
        if (is_checked({ o: value })) {
          const [$r295, l$89] = begin_collect({ l: l });
          l = l$89;
          const saved = $r295;
          const [$r296, l$90] = expr({ fuel: $rt.int.sub(fuel, 1, $ob53), l: l, ctx: ctx, e: inv.expr, discard: false });
          l = l$90;
          const cond_ir = $r296;
          const [$r298, l$91] = end_collect({ l: l, saved: saved, out: [{ tag: "Check", cond: cond_ir, ob: ob_ref_of({ l: l, ctx: ctx, o: value }) }] });
          l = l$91;
          body_ir = [...body_ir, ...$r298];
        }
        break $m293$match;
      }
      if ($m293.tag === "None") {
        skip({  });
        break $m293$match;
      }
      $rt.unreachable();
    }
  }
  if (checked_decreases) {
    const [$r299, l$92] = begin_collect({ l: l });
    l = l$92;
    const saved = $r299;
    const [$r300, l$93] = expr({ fuel: $rt.int.sub(fuel, 1, $ob54), l: l, ctx: ctx, e: clause_expr({ o: dec_clause }), discard: false });
    l = l$93;
    const again = $r300;
    const [$r303, l$94] = end_collect({ l: l, saved: saved, out: [{ tag: "Check", cond: { tag: "Cmp", op: "<", left: again, right: measure_expr, is_float: false }, ob: ob_ref_of({ l: l, ctx: ctx, o: ob_or({ o: dec }) }) }] });
    l = l$94;
    body_ir = [...body_ir, ...$r303];
  }
  if ($std_list.len({ xs: cond_pre }) === 0) {
    return [$std_list.append({ xs: out, x: { tag: "Loop", cond: c, body: body_ir } }), l];
  }
  const [$r305, l$95] = tmp({ l: l, prefix: "go" });
  l = l$95;
  const go = $r305;
  const bool_t = { tag: "Prim", name: "Bool" };
  out = [...out, ...cond_pre];
  out = $std_list.append({ xs: out, x: { tag: "Let", name: go, ty: bool_t, mutable: true, value: c } });
  const [$r308, l$96] = begin_collect({ l: l });
  l = l$96;
  const saved_again = $r308;
  const [$r309, l$97] = expr({ fuel: $rt.int.sub(fuel, 1, $ob55), l: l, ctx: ctx, e: cond, discard: false });
  l = l$97;
  const again = $r309;
  const [$r311, l$98] = end_collect({ l: l, saved: saved_again, out: [{ tag: "Assign", name: go, ty: bool_t, value: again }] });
  l = l$98;
  body_ir = [...body_ir, ...$r311];
  return [$std_list.append({ xs: out, x: { tag: "Loop", cond: { tag: "Local", name: go, ty: bool_t }, body: body_ir } }), l];
}

export function clause_expr({ o }) {
  const $m314 = o;
  $m314$match: {
    if ($m314.tag === "Some") {
      const value = $m314.value;
      return value.expr;
      break $m314$match;
    }
    if ($m314.tag === "None") {
      return { tag: "Hole", span: { start: 0, end: 0 } };
      break $m314$match;
    }
    $rt.unreachable();
  }
}

export function for_stmt({ fuel, l, ctx, domain, body, at }) {
  if (fuel === 0) {
    return [[], l];
  }
  const def = $context.get_def({ ctx: ctx, id: def_at({ l: l, ctx: ctx, tag: $defs.tag_stmt, span: at }) });
  const declared = type_or({ o: $std_map.find({ d: ctx.decl_types, key: def.id }) });
  const $m317 = domain;
  $m317$match: {
    if ($m317.tag === "RangeDomain") {
      const lo = $m317.lo;
      const hi = $m317.hi;
      const [$r318, l$99] = expr({ fuel: $rt.int.sub(fuel, 1, $ob56), l: l, ctx: ctx, e: lo, discard: false });
      l = l$99;
      const lo_ir = $r318;
      const [$r319, l$100] = expr({ fuel: $rt.int.sub(fuel, 1, $ob57), l: l, ctx: ctx, e: hi, discard: false });
      l = l$100;
      let hi_ir = $r319;
      if (!(hi_ir.tag === "IntE" || hi_ir.tag === "Local")) {
        const [$r320, l$101] = tmp({ l: l, prefix: "hi" });
        l = l$101;
        const name = $r320;
        const [, l$102] = push_pre({ l: l, s: { tag: "Let", name: name, ty: { tag: "Prim", name: "Int" }, mutable: false, value: hi_ir } });
        l = l$102;
        hi_ir = { tag: "Local", name: name, ty: { tag: "Prim", name: "Int" } };
      }
      const [$r325, l$103] = block({ fuel: $rt.int.sub(fuel, 1, $ob58), l: l, ctx: ctx, b: body });
      l = l$103;
      return [[{ tag: "ForRange", name: def.name, lo: lo_ir, hi: hi_ir, body: $r325 }], l];
      break $m317$match;
    }
    if ($m317.tag === "InDomain") {
      const expr = $m317.expr;
      const [$r327, l$104] = expr_of({ fuel: $rt.int.sub(fuel, 1, $ob59), l: l, ctx: ctx, e: expr, discard: false });
      l = l$104;
      const list = $r327;
      const ob = find_ob({ ctx: ctx, k: expr_key({ l: l, e: expr }), kind: { tag: "Refinement" } });
      let body_ir = [];
      const $m329 = ob;
      $m329$match: {
        if ($m329.tag === "Some") {
          const value = $m329.value;
          if (is_checked({ o: value })) {
            const local = { tag: "Local", name: def.name, ty: declared };
            for (const p of refinement_preds({ ctx: ctx, t: declared })) {
              const [$r331, l$105] = begin_collect({ l: l });
              l = l$105;
              const saved = $r331;
              const [$r332, l$106] = with_it({ fuel: $rt.int.sub(fuel, 1, $ob60), l: l, ctx: ctx, it_expr: local, pred: p });
              l = l$106;
              const cond = $r332;
              const [$r334, l$107] = end_collect({ l: l, saved: saved, out: [{ tag: "Check", cond: cond, ob: ob_ref_of({ l: l, ctx: ctx, o: value }) }] });
              l = l$107;
              body_ir = [...body_ir, ...$r334];
            }
          }
          break $m329$match;
        }
        if ($m329.tag === "None") {
          skip({  });
          break $m329$match;
        }
        $rt.unreachable();
      }
      const [$r335, l$108] = block({ fuel: $rt.int.sub(fuel, 1, $ob61), l: l, ctx: ctx, b: body });
      l = l$108;
      body_ir = [...body_ir, ...$r335];
      return [[{ tag: "ForEach", name: def.name, ty: declared, list: list, body: body_ir }], l];
      break $m317$match;
    }
    $rt.unreachable();
  }
}

export function expr_of({ fuel, l, ctx, e, discard }) {
  if (fuel === 0) {
    return [{ tag: "UnitE" }, l];
  }
  const [$r338, l$109] = expr({ fuel: $rt.int.sub(fuel, 1, $ob62), l: l, ctx: ctx, e: e, discard: discard });
  l = l$109;
  return [$r338, l];
}

export function expr({ fuel, l, ctx, e, discard }) {
  if (fuel === 0) {
    return [{ tag: "UnitE" }, l];
  }
  const $m340 = e;
  $m340$match: {
    if ($m340.tag === "IntLit") {
      const value = $m340.value;
      return [{ tag: "IntE", v: value }, l];
      break $m340$match;
    }
    if ($m340.tag === "FloatLit") {
      const value = $m340.value;
      return [{ tag: "FloatE", v: value }, l];
      break $m340$match;
    }
    if ($m340.tag === "TextLit") {
      const value = $m340.value;
      return [{ tag: "TextE", v: value }, l];
      break $m340$match;
    }
    if ($m340.tag === "BoolLit") {
      const value = $m340.value;
      return [{ tag: "BoolE", v: value }, l];
      break $m340$match;
    }
    if ($m340.tag === "DurationLit") {
      const nanos = $m340.nanos;
      return [{ tag: "IntE", v: nanos }, l];
      break $m340$match;
    }
    if ($m340.tag === "Name") {
      return [name_expr({ l: l, ctx: ctx, e: e }), l];
      break $m340$match;
    }
    if ($m340.tag === "It") {
      const $m346 = l.it_expr;
      $m346$match: {
        if ($m346.tag === "Some") {
          const value = $m346.value;
          return [value, l];
          break $m346$match;
        }
        if ($m346.tag === "None") {
          return [{ tag: "Local", name: "$it", ty: type_of_expr({ l: l, ctx: ctx, e: e }) }, l];
          break $m346$match;
        }
        $rt.unreachable();
      }
      break $m340$match;
    }
    if ($m340.tag === "ResultRef") {
      const $m348 = l.fn_ctx;
      $m348$match: {
        if ($m348.tag === "Some") {
          const value = $m348.value;
          if (value.result_expr.tag === "Some") {
            return [expr_or_unit({ o: value.result_expr }), l];
          }
          break $m348$match;
        }
        if ($m348.tag === "None") {
          skip({  });
          break $m348$match;
        }
        $rt.unreachable();
      }
      return [{ tag: "Local", name: "$r", ty: type_of_expr({ l: l, ctx: ctx, e: e }) }, l];
      break $m340$match;
    }
    if ($m340.tag === "Old") {
      const name = $m340.name;
      return [{ tag: "Local", name: "$old_" + name.text, ty: type_of_expr({ l: l, ctx: ctx, e: e }) }, l];
      break $m340$match;
    }
    if ($m340.tag === "Ctor") {
      const [$r351, l$110] = ctor({ fuel: $rt.int.sub(fuel, 1, $ob63), l: l, ctx: ctx, e: e });
      l = l$110;
      return [$r351, l];
      break $m340$match;
    }
    if ($m340.tag === "RecordUpdate") {
      const [$r352, l$111] = record_update({ fuel: $rt.int.sub(fuel, 1, $ob64), l: l, ctx: ctx, e: e });
      l = l$111;
      return [$r352, l];
      break $m340$match;
    }
    if ($m340.tag === "ListLit") {
      const elems = $m340.elems;
      let out = $std_list.builder({  });
      for (const x of elems) {
        const [$r353, l$112] = expr({ fuel: $rt.int.sub(fuel, 1, $ob65), l: l, ctx: ctx, e: x, discard: false });
        l = l$112;
        const [, out$113] = $std_list.push({ b: out, x: $r353 });
        out = out$113;
      }
      return [{ tag: "ListE", elems: $std_list.finish({ b: out }), ty: type_of_expr({ l: l, ctx: ctx, e: e }) }, l];
      break $m340$match;
    }
    if ($m340.tag === "Try") {
      const [$r355, l$114] = try_expr({ fuel: $rt.int.sub(fuel, 1, $ob66), l: l, ctx: ctx, e: e });
      l = l$114;
      return [$r355, l];
      break $m340$match;
    }
    if ($m340.tag === "Recover") {
      const [$r356, l$115] = recover_expr({ fuel: $rt.int.sub(fuel, 1, $ob67), l: l, ctx: ctx, e: e });
      l = l$115;
      return [$r356, l];
      break $m340$match;
    }
    if ($m340.tag === "Quantifier") {
      const [$r357, l$116] = quantifier({ fuel: $rt.int.sub(fuel, 1, $ob68), l: l, ctx: ctx, e: e });
      l = l$116;
      return [$r357, l];
      break $m340$match;
    }
    if ($m340.tag === "Closure") {
      const [$r358, l$117] = closure({ fuel: $rt.int.sub(fuel, 1, $ob69), l: l, ctx: ctx, e: e });
      l = l$117;
      return [$r358, l];
      break $m340$match;
    }
    if ($m340.tag === "Fake") {
      const [$r359, l$118] = fake_expr({ fuel: $rt.int.sub(fuel, 1, $ob70), l: l, ctx: ctx, e: e });
      l = l$118;
      return [$r359, l];
      break $m340$match;
    }
    if ($m340.tag === "Hole") {
      return [{ tag: "UnitE" }, l];
      break $m340$match;
    }
    if ($m340.tag === "FieldAccess") {
      const [$r361, l$119] = field_access({ fuel: $rt.int.sub(fuel, 1, $ob71), l: l, ctx: ctx, e: e });
      l = l$119;
      return [$r361, l];
      break $m340$match;
    }
    if ($m340.tag === "Call") {
      const [$r362, l$120] = call({ fuel: $rt.int.sub(fuel, 1, $ob72), l: l, ctx: ctx, e: e, discard: discard });
      l = l$120;
      return [$r362, l];
      break $m340$match;
    }
    if ($m340.tag === "Unary") {
      const [$r363, l$121] = unary({ fuel: $rt.int.sub(fuel, 1, $ob73), l: l, ctx: ctx, e: e });
      l = l$121;
      return [$r363, l];
      break $m340$match;
    }
    if ($m340.tag === "Binary") {
      const [$r364, l$122] = binary({ fuel: $rt.int.sub(fuel, 1, $ob74), l: l, ctx: ctx, e: e });
      l = l$122;
      return [$r364, l];
      break $m340$match;
    }
    if ($m340.tag === "And") {
      const operands = $m340.operands;
      const [$r365, l$123] = short_circuit({ fuel: $rt.int.sub(fuel, 1, $ob75), l: l, ctx: ctx, is_and: true, operands: operands });
      l = l$123;
      return [$r365, l];
      break $m340$match;
    }
    if ($m340.tag === "Or") {
      const operands = $m340.operands;
      const [$r366, l$124] = short_circuit({ fuel: $rt.int.sub(fuel, 1, $ob76), l: l, ctx: ctx, is_and: false, operands: operands });
      l = l$124;
      return [$r366, l];
      break $m340$match;
    }
    if ($m340.tag === "Is") {
      const expr = $m340.expr;
      const pattern = $m340.pattern;
      const [$r367, l$125] = expr_of({ fuel: $rt.int.sub(fuel, 1, $ob77), l: l, ctx: ctx, e: expr, discard: false });
      l = l$125;
      const subject = $r367;
      const [$r369, l$126] = pattern_test_of({ fuel: $rt.int.sub(fuel, 1, $ob78), l: l, ctx: ctx, p: pattern, subject: subject, ty: type_of_expr({ l: l, ctx: ctx, e: expr }) });
      l = l$126;
      const $m368 = $r369;
      $m368$match: {
        if ($m368.tag === "Some") {
          const value = $m368.value;
          return [value, l];
          break $m368$match;
        }
        if ($m368.tag === "None") {
          return [{ tag: "BoolE", v: true }, l];
          break $m368$match;
        }
        $rt.unreachable();
      }
      break $m340$match;
    }
    $rt.unreachable();
  }
}

export function exprs({ fuel, l, ctx, es }) {
  $rt.check(fuel >= 0, $ob79);
  if (fuel === 0) {
    return [[], l];
  }
  let out = $std_list.builder({  });
  for (const x of es) {
    const [$r371, l$127] = expr({ fuel: $rt.int.sub(fuel, 1, $ob80), l: l, ctx: ctx, e: x, discard: false });
    l = l$127;
    const [, out$128] = $std_list.push({ b: out, x: $r371 });
    out = out$128;
  }
  return [$std_list.finish({ b: out }), l];
}

export function fn_value({ ctx, def }) {
  const $m372 = $std_map.find({ d: ctx.signatures, key: def.id });
  $m372$match: {
    if ($m372.tag === "None") {
      return { tag: "Local", name: def.name, ty: { tag: "ErrorT" } };
      break $m372$match;
    }
    if ($m372.tag === "Some") {
      const value = $m372.value;
      return { tag: "FnRef", def: def, name: emitted_name({ ctx: ctx, def: def }), sig: value };
      break $m372$match;
    }
    $rt.unreachable();
  }
}

export function name_expr({ l, ctx, e }) {
  const ty = type_of_expr({ l: l, ctx: ctx, e: e });
  let source = "";
  const $m376 = e;
  $m376$match: {
    if ($m376.tag === "Name") {
      const name = $m376.name;
      source = name.text;
      break $m376$match;
    }
    if (true) {
      skip({  });
      break $m376$match;
    }
    $rt.unreachable();
  }
  const def = ref_def({ ctx: ctx, k: expr_key({ l: l, e: e }) });
  if (def < 0) {
    return { tag: "Local", name: source, ty: ty };
  }
  const $m378 = $std_map.find({ d: l.subst, key: def });
  $m378$match: {
    if ($m378.tag === "Some") {
      const value = $m378.value;
      return value;
      break $m378$match;
    }
    if ($m378.tag === "None") {
      skip({  });
      break $m378$match;
    }
    $rt.unreachable();
  }
  const d = $context.get_def({ ctx: ctx, id: def });
  if ($rt.eq(d.kind, { tag: "Fn" })) {
    return fn_value({ ctx: ctx, def: d });
  }
  if ($rt.eq(d.kind, { tag: "Const" })) {
    return { tag: "Global", def: d, ty: ty };
  }
  if ($rt.eq(d.kind, { tag: "Field" })) {
    const $m383 = l.field_object;
    $m383$match: {
      if ($m383.tag === "Some") {
        const value = $m383.value;
        if (value.record_def === or_neg({ o: d.parent })) {
          return { tag: "FieldE", object: { tag: "Local", name: value.name, ty: value.ty }, name: d.name, ty: ty, owner: { tag: "Some", value: $context.get_def({ ctx: ctx, id: value.record_def }) } };
        }
        break $m383$match;
      }
      if ($m383.tag === "None") {
        skip({  });
        break $m383$match;
      }
      $rt.unreachable();
    }
  }
  return { tag: "Local", name: d.name, ty: ty };
}

export function type_info_of({ ctx, owner }) {
  const $m388 = owner;
  $m388$match: {
    if ($m388.tag === "PrimOwner") {
      const name = $m388.name;
      return { tag: "TypeInfoE", name: name, fields: [] };
      break $m388$match;
    }
    if ($m388.tag === "DefOwner") {
      const def = $m388.def;
      let fields = $std_list.builder({  });
      for (const f of fields_or({ o: $std_map.find({ d: ctx.fields, key: def }) })) {
        const [, fields$129] = $std_list.push({ b: fields, x: { name: f.name, type_name: type_slug({ fuel: 1000, ctx: ctx, t: f.ty }) } });
        fields = fields$129;
      }
      return { tag: "TypeInfoE", name: $context.get_def({ ctx: ctx, id: def }).name, fields: $std_list.finish({ b: fields }) };
      break $m388$match;
    }
    $rt.unreachable();
  }
}

export function ctor({ fuel, l, ctx, e }) {
  if (fuel === 0) {
    return [{ tag: "UnitE" }, l];
  }
  const $m393 = e;
  $m393$match: {
    if ($m393.tag === "Ctor") {
      const args = $m393.args;
      const fields = $m393.fields;
      const span = $m393.span;
      const k = key({ l: l, tag: $defs.tag_expr, span: span });
      const $m394 = $std_map.find({ d: ctx.refs, key: k });
      $m394$match: {
        if ($m394.tag === "None") {
          return [{ tag: "UnitE" }, l];
          break $m394$match;
        }
        if ($m394.tag === "Some") {
          const value = $m394.value;
          const $m396 = value;
          $m396$match: {
            if ($m396.tag === "UnitRes") {
              return [{ tag: "UnitE" }, l];
              break $m396$match;
            }
            if ($m396.tag === "TypeValueRes") {
              const owner = $m396.owner;
              return [type_info_of({ ctx: ctx, owner: owner }), l];
              break $m396$match;
            }
            if ($m396.tag === "DefRes") {
              const def = $m396.def;
              const d = $context.get_def({ ctx: ctx, id: def });
              const ty = type_of_expr({ l: l, ctx: ctx, e: e });
              let inits = $std_list.builder({  });
              for (const a of args_list({ o: args })) {
                const [, inits$130] = $std_list.push({ b: inits, x: { name: a.name.text, value: a.value } });
                inits = inits$130;
              }
              for (const f of field_inits({ o: fields })) {
                const [, inits$131] = $std_list.push({ b: inits, x: { name: f.name.text, value: f.value } });
                inits = inits$131;
              }
              const init_list = $std_list.finish({ b: inits });
              let lowered = $std_list.builder({  });
              for (const i of init_list) {
                const [$r400, l$132] = expr({ fuel: $rt.int.sub(fuel, 1, $ob81), l: l, ctx: ctx, e: i.value, discard: false });
                l = l$132;
                const [, lowered$133] = $std_list.push({ b: lowered, x: { name: i.name, value: $r400 } });
                lowered = lowered$133;
              }
              let literal = { tag: "RecordE", def: d, ty: ty, fields: $std_list.finish({ b: lowered }) };
              if ($rt.eq(d.kind, { tag: "Variant" })) {
                literal = { tag: "VariantE", def: d, ty: ty, fields: $std_list.finish({ b: lowered }) };
              }
              const [$r405, l$134] = field_checks({ fuel: $rt.int.sub(fuel, 1, $ob82), l: l, ctx: ctx, def: d, literal: literal, ty: ty, inits: init_list });
              l = l$134;
              return [$r405, l];
              break $m396$match;
            }
            if (true) {
              return [{ tag: "UnitE" }, l];
              break $m396$match;
            }
            $rt.unreachable();
          }
          break $m394$match;
        }
        $rt.unreachable();
      }
      break $m393$match;
    }
    if (true) {
      return [{ tag: "UnitE" }, l];
      break $m393$match;
    }
    $rt.unreachable();
  }
}

export function args_list({ o }) {
  const $m408 = o;
  $m408$match: {
    if ($m408.tag === "Some") {
      const value = $m408.value;
      return value;
      break $m408$match;
    }
    if ($m408.tag === "None") {
      return [];
      break $m408$match;
    }
    $rt.unreachable();
  }
}

export function field_inits({ o }) {
  const $m409 = o;
  $m409$match: {
    if ($m409.tag === "Some") {
      const value = $m409.value;
      return value;
      break $m409$match;
    }
    if ($m409.tag === "None") {
      return [];
      break $m409$match;
    }
    $rt.unreachable();
  }
}

export function record_update({ fuel, l, ctx, e }) {
  if (fuel === 0) {
    return [{ tag: "UnitE" }, l];
  }
  const $m411 = e;
  $m411$match: {
    if ($m411.tag === "RecordUpdate") {
      const base = $m411.base;
      const fields = $m411.fields;
      const ty = type_of_expr({ l: l, ctx: ctx, e: e });
      const [$r412, l$135] = expr({ fuel: $rt.int.sub(fuel, 1, $ob83), l: l, ctx: ctx, e: base, discard: false });
      l = l$135;
      const base_ir = $r412;
      const s = $types.strip({ t: type_of_expr({ l: l, ctx: ctx, e: base }) });
      let inits = $std_list.builder({  });
      for (const f of fields) {
        const [, inits$136] = $std_list.push({ b: inits, x: { name: f.name.text, value: f.value } });
        inits = inits$136;
      }
      const init_list = $std_list.finish({ b: inits });
      let lowered = $std_list.builder({  });
      for (const i of init_list) {
        const [$r414, l$137] = expr({ fuel: $rt.int.sub(fuel, 1, $ob84), l: l, ctx: ctx, e: i.value, discard: false });
        l = l$137;
        const [, lowered$138] = $std_list.push({ b: lowered, x: { name: i.name, value: $r414 } });
        lowered = lowered$138;
      }
      if (!(s.tag === "RecordT")) {
        const module_def = $context.get_def({ ctx: ctx, id: def_at({ l: l, ctx: ctx, tag: $defs.tag_item, span: l.m.tree.span }) });
        return [{ tag: "Update", base: base_ir, def: module_def, ty: ty, fields: $std_list.finish({ b: lowered }) }, l];
      }
      const d = $context.get_def({ ctx: ctx, id: $types.def_of({ t: s }) });
      const [$r418, l$139] = field_checks({ fuel: $rt.int.sub(fuel, 1, $ob85), l: l, ctx: ctx, def: d, literal: { tag: "Update", base: base_ir, def: d, ty: ty, fields: $std_list.finish({ b: lowered }) }, ty: ty, inits: init_list });
      l = l$139;
      return [$r418, l];
      break $m411$match;
    }
    if (true) {
      return [{ tag: "UnitE" }, l];
      break $m411$match;
    }
    $rt.unreachable();
  }
}

export function field_checks({ fuel, l, ctx, def, literal, ty, inits }) {
  if (fuel === 0) {
    return [{ tag: "UnitE" }, l];
  }
  const fs = fields_or({ o: $std_map.find({ d: ctx.fields, key: def.id }) });
  let checks = $std_list.builder({  });
  const [$r421, l$140] = tmp({ l: l, prefix: "v" });
  l = l$140;
  const v = $r421;
  const object = { tag: "Local", name: v, ty: ty };
  const saved = l.field_object;
  l = { ...l, field_object: { tag: "Some", value: { record_def: def.id, name: v, ty: ty } } };
  for (const init of inits) {
    const $m426 = find_ob({ ctx: ctx, k: expr_key({ l: l, e: init.value }), kind: { tag: "Refinement" } });
    $m426$match: {
      if ($m426.tag === "None") {
        skip({  });
        break $m426$match;
      }
      if ($m426.tag === "Some") {
        const value = $m426.value;
        if (is_checked({ o: value })) {
          const fo = find_field({ fs: fs, name: init.name });
          if (fo.tag === "Some") {
            const field = field_or({ o: fo });
            const it_expr = { tag: "FieldE", object: object, name: field.name, ty: field.ty, owner: { tag: "Some", value: def } };
            for (const p of refinement_preds({ ctx: ctx, t: field.ty })) {
              const [$r430, l$141] = with_it({ fuel: $rt.int.sub(fuel, 1, $ob86), l: l, ctx: ctx, it_expr: it_expr, pred: p });
              l = l$141;
              const [, checks$142] = $std_list.push({ b: checks, x: { tag: "Check", cond: $r430, ob: ob_ref_of({ l: l, ctx: ctx, o: value }) } });
              checks = checks$142;
            }
          }
        }
        break $m426$match;
      }
      $rt.unreachable();
    }
  }
  l = { ...l, field_object: saved };
  if ($std_list.built({ b: checks }) === 0) {
    return [literal, l];
  }
  return [{ tag: "Checked", value: literal, it_name: v, ty: ty, checks: $std_list.finish({ b: checks }) }, l];
}

export function field_or({ o }) {
  const $m434 = o;
  $m434$match: {
    if ($m434.tag === "Some") {
      const value = $m434.value;
      return value;
      break $m434$match;
    }
    if ($m434.tag === "None") {
      return { def: $rt.int.neg(1, $ob87), name: "", ty: { tag: "ErrorT" } };
      break $m434$match;
    }
    $rt.unreachable();
  }
}

export function try_expr({ fuel, l, ctx, e }) {
  if (fuel === 0) {
    return [{ tag: "UnitE" }, l];
  }
  const $m438 = e;
  $m438$match: {
    if ($m438.tag === "Try") {
      const expr = $m438.expr;
      const else_ = $m438.else_;
      const [$r439, l$143] = expr_of({ fuel: $rt.int.sub(fuel, 1, $ob88), l: l, ctx: ctx, e: expr, discard: false });
      l = l$143;
      const operand = $r439;
      const option = is_option({ ctx: ctx, t: type_of_expr({ l: l, ctx: ctx, e: expr }) });
      let outer_option = false;
      const $m440 = l.fn_ctx;
      $m440$match: {
        if ($m440.tag === "Some") {
          const value = $m440.value;
          outer_option = is_option({ ctx: ctx, t: value.ret });
          break $m440$match;
        }
        if ($m440.tag === "None") {
          skip({  });
          break $m440$match;
        }
        $rt.unreachable();
      }
      const ty = type_of_expr({ l: l, ctx: ctx, e: e });
      const $m441 = else_;
      $m441$match: {
        if ($m441.tag === "None") {
          return [{ tag: "Try", operand: operand, option: option, outer_option: outer_option, else_clause: { tag: "None" }, raw: false, ty: ty }, l];
          break $m441$match;
        }
        if ($m441.tag === "Some") {
          const value = $m441.value;
          let name = { tag: "None" };
          let error_type = { tag: "ErrorT" };
          if (value.name.text !== "_") {
            name = { tag: "Some", value: value.name.text };
            error_type = type_or({ o: $std_map.find({ d: ctx.decl_types, key: def_at({ l: l, ctx: ctx, tag: $defs.tag_misc, span: value.span }) }) });
          }
          const [$r447, l$144] = expr_of({ fuel: $rt.int.sub(fuel, 1, $ob89), l: l, ctx: ctx, e: value.expr, discard: false });
          l = l$144;
          const else_value = $r447;
          return [{ tag: "Try", operand: operand, option: option, outer_option: outer_option, else_clause: { tag: "Some", value: { name: name, error_type: error_type, value: else_value } }, raw: l.verifying, ty: ty }, l];
          break $m441$match;
        }
        $rt.unreachable();
      }
      break $m438$match;
    }
    if (true) {
      return [{ tag: "UnitE" }, l];
      break $m438$match;
    }
    $rt.unreachable();
  }
}

export function recover_expr({ fuel, l, ctx, e }) {
  if (fuel === 0) {
    return [{ tag: "UnitE" }, l];
  }
  const $m453 = e;
  $m453$match: {
    if ($m453.tag === "Recover") {
      const body = $m453.body;
      const saved = l.fn_ctx;
      const $m454 = saved;
      $m454$match: {
        if ($m454.tag === "Some") {
          const value = $m454.value;
          l = { ...l, fn_ctx: { tag: "Some", value: { ...value, ret: { tag: "ErrorT" }, post: [], inout_names: [] } } };
          break $m454$match;
        }
        if ($m454.tag === "None") {
          skip({  });
          break $m454$match;
        }
        $rt.unreachable();
      }
      const n = $std_list.len({ xs: body.stmts });
      let out = [];
      let value_ir = { tag: "UnitE" };
      let i = 0;
      for (const s of body.stmts) {
        let last_expr = { tag: "None" };
        if (i === $rt.int.sub(n, 1, $ob90)) {
          const $m461 = s;
          $m461$match: {
            if ($m461.tag === "ExprStmt") {
              const expr = $m461.expr;
              last_expr = { tag: "Some", value: expr };
              break $m461$match;
            }
            if (true) {
              skip({  });
              break $m461$match;
            }
            $rt.unreachable();
          }
        }
        const $m463 = last_expr;
        $m463$match: {
          if ($m463.tag === "Some") {
            const value = $m463.value;
            const [$r464, l$145] = begin_collect({ l: l });
            l = l$145;
            const saved_pre = $r464;
            const [$r465, l$146] = expr({ fuel: $rt.int.sub(fuel, 1, $ob91), l: l, ctx: ctx, e: value, discard: false });
            l = l$146;
            value_ir = $r465;
            const [$r466, l$147] = end_collect({ l: l, saved: saved_pre, out: [] });
            l = l$147;
            out = [...out, ...$r466];
            break $m463$match;
          }
          if ($m463.tag === "None") {
            const [$r467, l$148] = stmt({ fuel: $rt.int.sub(fuel, 1, $ob92), l: l, ctx: ctx, s: s });
            l = l$148;
            out = [...out, ...$r467];
            break $m463$match;
          }
          $rt.unreachable();
        }
        i = $rt.int.add(i, 1, $ob93);
      }
      l = { ...l, fn_ctx: saved };
      return [{ tag: "Recover", body: out, value: value_ir, ty: type_of_expr({ l: l, ctx: ctx, e: e }) }, l];
      break $m453$match;
    }
    if (true) {
      return [{ tag: "UnitE" }, l];
      break $m453$match;
    }
    $rt.unreachable();
  }
}

export function quantifier({ fuel, l, ctx, e }) {
  if (fuel === 0) {
    return [{ tag: "UnitE" }, l];
  }
  const $m472 = e;
  $m472$match: {
    if ($m472.tag === "Quantifier") {
      const quant = $m472.quant;
      const domain = $m472.domain;
      const where_ = $m472.where_;
      const body = $m472.body;
      const span = $m472.span;
      const def = $context.get_def({ ctx: ctx, id: def_at({ l: l, ctx: ctx, tag: $defs.tag_expr, span: span }) });
      const binder = type_or({ o: $std_map.find({ d: ctx.decl_types, key: def.id }) });
      let dom = { tag: "BoolsD" };
      const $m474 = domain;
      $m474$match: {
        if ($m474.tag === "None") {
          dom = { tag: "BoolsD" };
          break $m474$match;
        }
        if ($m474.tag === "Some") {
          const value = $m474.value;
          const $m476 = value;
          $m476$match: {
            if ($m476.tag === "RangeDomain") {
              const lo = $m476.lo;
              const hi = $m476.hi;
              const [$r477, l$149] = expr({ fuel: $rt.int.sub(fuel, 1, $ob94), l: l, ctx: ctx, e: lo, discard: false });
              l = l$149;
              const [$r478, l$150] = expr({ fuel: $rt.int.sub(fuel, 1, $ob95), l: l, ctx: ctx, e: hi, discard: false });
              l = l$150;
              dom = { tag: "RangeD", lo: $r477, hi: $r478 };
              break $m476$match;
            }
            if ($m476.tag === "InDomain") {
              const expr = $m476.expr;
              const s = $types.strip({ t: type_of_expr({ l: l, ctx: ctx, e: expr }) });
              const [$r480, l$151] = expr_of({ fuel: $rt.int.sub(fuel, 1, $ob96), l: l, ctx: ctx, e: expr, discard: false });
              l = l$151;
              const list = $r480;
              if (s.tag === "UnionT") {
                dom = { tag: "OkListD", expr: list };
              } else {
                dom = { tag: "ListD", expr: list };
              }
              break $m476$match;
            }
            $rt.unreachable();
          }
          break $m474$match;
        }
        $rt.unreachable();
      }
      let where_ir = { tag: "None" };
      const $m484 = where_;
      $m484$match: {
        if ($m484.tag === "Some") {
          const value = $m484.value;
          const [$r485, l$152] = expr({ fuel: $rt.int.sub(fuel, 1, $ob97), l: l, ctx: ctx, e: value, discard: false });
          l = l$152;
          where_ir = { tag: "Some", value: $r485 };
          break $m484$match;
        }
        if ($m484.tag === "None") {
          skip({  });
          break $m484$match;
        }
        $rt.unreachable();
      }
      const [$r487, l$153] = expr({ fuel: $rt.int.sub(fuel, 1, $ob98), l: l, ctx: ctx, e: body, discard: false });
      l = l$153;
      return [{ tag: "Quantifier", quant: quant, name: def.name, binder: binder, domain: dom, where_: where_ir, body: $r487 }, l];
      break $m472$match;
    }
    if (true) {
      return [{ tag: "UnitE" }, l];
      break $m472$match;
    }
    $rt.unreachable();
  }
}

export function closure({ fuel, l, ctx, e }) {
  if (fuel === 0) {
    return [{ tag: "UnitE" }, l];
  }
  const $m491 = e;
  $m491$match: {
    if ($m491.tag === "Closure") {
      const params = $m491.params;
      const body = $m491.body;
      const ty = type_of_expr({ l: l, ctx: ctx, e: e });
      let fn_type = { tag: "None" };
      let ret = { tag: "ErrorT" };
      if (ty.tag === "FnT") {
        fn_type = { tag: "Some", value: ty };
        ret = $types.ret_of({ t: ty });
      }
      let ps = $std_list.builder({  });
      let inout_names = $std_list.builder({  });
      for (const p of params) {
        const pd = def_at({ l: l, ctx: ctx, tag: $defs.tag_sig, span: p.span });
        let pt = { tag: "ErrorT" };
        if (pd >= 0) {
          pt = type_or({ o: $std_map.find({ d: ctx.decl_types, key: pd }) });
        }
        const [, ps$154] = $std_list.push({ b: ps, x: { name: p.name.text, ty: pt, is_inout: p.is_inout } });
        ps = ps$154;
        if (p.is_inout) {
          const [, inout_names$155] = $std_list.push({ b: inout_names, x: p.name.text });
          inout_names = inout_names$155;
        }
      }
      const param_list = $std_list.finish({ b: ps });
      const saved = l.fn_ctx;
      let outer_def = { tag: "None" };
      let outer_dicts = $std_map.dict({  });
      let outer_measure = { tag: "None" };
      const $m499 = saved;
      $m499$match: {
        if ($m499.tag === "Some") {
          const value = $m499.value;
          outer_def = value.def;
          outer_dicts = value.dicts;
          outer_measure = value.measure;
          break $m499$match;
        }
        if ($m499.tag === "None") {
          skip({  });
          break $m499$match;
        }
        $rt.unreachable();
      }
      const cctx = { inout_names: $std_list.finish({ b: inout_names }), ret: ret, post: [], def: outer_def, dicts: outer_dicts, result_expr: { tag: "None" }, measure: outer_measure };
      l = { ...l, fn_ctx: { tag: "Some", value: cctx } };
      let entry = [];
      let def_name = "closure";
      const $m504 = outer_def;
      $m504$match: {
        if ($m504.tag === "Some") {
          const value = $m504.value;
          def_name = value.name;
          break $m504$match;
        }
        if ($m504.tag === "None") {
          skip({  });
          break $m504$match;
        }
        $rt.unreachable();
      }
      let i = 0;
      for (const p of params) {
        if (i < $std_list.len({ xs: param_list })) {
          const param = $std_list.get({ xs: param_list, i: i });
          const local = { tag: "Local", name: param.name, ty: param.ty };
          for (const pred of refinement_preds({ ctx: ctx, t: param.ty })) {
            const [$r506, l$156] = begin_collect({ l: l });
            l = l$156;
            const saved_pre = $r506;
            const [$r507, l$157] = with_it({ fuel: $rt.int.sub(fuel, 1, $ob99), l: l, ctx: ctx, it_expr: local, pred: pred });
            l = l$157;
            const cond = $r507;
            const [$r509, l$158] = end_collect({ l: l, saved: saved_pre, out: [{ tag: "Check", cond: cond, ob: ob_ref({ l: l, ctx: ctx, kind: "refinement", text: $printer.print_expr({ e: pred.e }), file: l.file, span: p.span, def: def_name }) }] });
            l = l$158;
            entry = [...entry, ...$r509];
          }
        }
        i = $rt.int.add(i, 1, $ob100);
      }
      const [$r510, l$159] = block({ fuel: $rt.int.sub(fuel, 1, $ob101), l: l, ctx: ctx, b: body });
      l = l$159;
      let body_ir = $r510;
      if ($std_list.built({ b: inout_names }) > 0 && fn_type.tag === "Some" && is_unit({ t: ret })) {
        body_ir = $std_list.append({ xs: body_ir, x: { tag: "Return", value: { tag: "UnitE" } } });
      }
      l = { ...l, fn_ctx: saved };
      return [{ tag: "Closure", params: param_list, fn_type: fn_type, entry_checks: entry, body: body_ir, early_return: block_has_try({ b: body }) }, l];
      break $m491$match;
    }
    if (true) {
      return [{ tag: "UnitE" }, l];
      break $m491$match;
    }
    $rt.unreachable();
  }
}

export function fake_expr({ fuel, l, ctx, e }) {
  if (fuel === 0) {
    return [{ tag: "UnitE" }, l];
  }
  const $m517 = e;
  $m517$match: {
    if ($m517.tag === "Fake") {
      const fields = $m517.fields;
      const span = $m517.span;
      const def = ref_def({ ctx: ctx, k: key({ l: l, tag: $defs.tag_expr, span: span }) });
      let kind = "capability";
      if (def >= 0) {
        kind = $context.get_def({ ctx: ctx, id: def }).name;
      }
      let out = $std_list.builder({  });
      for (const f of fields) {
        const [$r518, l$160] = expr({ fuel: $rt.int.sub(fuel, 1, $ob102), l: l, ctx: ctx, e: f.value, discard: false });
        l = l$160;
        const [, out$161] = $std_list.push({ b: out, x: { name: f.name.text, value: $r518 } });
        out = out$161;
      }
      return [{ tag: "Fake", kind: kind, fields: $std_list.finish({ b: out }), ty: type_of_expr({ l: l, ctx: ctx, e: e }) }, l];
      break $m517$match;
    }
    if (true) {
      return [{ tag: "UnitE" }, l];
      break $m517$match;
    }
    $rt.unreachable();
  }
}

export function field_access({ fuel, l, ctx, e }) {
  if (fuel === 0) {
    return [{ tag: "UnitE" }, l];
  }
  const $m523 = e;
  $m523$match: {
    if ($m523.tag === "FieldAccess") {
      const object = $m523.object;
      const name = $m523.name;
      const span = $m523.span;
      const ty = type_of_expr({ l: l, ctx: ctx, e: e });
      const $m524 = $std_map.find({ d: ctx.refs, key: key({ l: l, tag: $defs.tag_expr, span: span }) });
      $m524$match: {
        if ($m524.tag === "Some") {
          const value = $m524.value;
          const $m525 = value;
          $m525$match: {
            if ($m525.tag === "DefRes") {
              const def = $m525.def;
              const d = $context.get_def({ ctx: ctx, id: def });
              if ($rt.eq(d.kind, { tag: "Fn" })) {
                return [fn_value({ ctx: ctx, def: d }), l];
              }
              return [{ tag: "Global", def: d, ty: ty }, l];
              break $m525$match;
            }
            if ($m525.tag === "CompanionRes") {
              const fn_def = $m525.fn_def;
              return [fn_value({ ctx: ctx, def: $context.get_def({ ctx: ctx, id: fn_def }) }), l];
              break $m525$match;
            }
            if ($m525.tag === "UnitRes") {
              return [{ tag: "UnitE" }, l];
              break $m525$match;
            }
            if ($m525.tag === "TypeValueRes") {
              const owner = $m525.owner;
              return [type_info_of({ ctx: ctx, owner: owner }), l];
              break $m525$match;
            }
            if (true) {
              return [{ tag: "UnitE" }, l];
              break $m525$match;
            }
            $rt.unreachable();
          }
          break $m524$match;
        }
        if ($m524.tag === "None") {
          skip({  });
          break $m524$match;
        }
        $rt.unreachable();
      }
      const s = $types.strip({ t: type_of_expr({ l: l, ctx: ctx, e: object }) });
      let owner = { tag: "None" };
      if (s.tag === "RecordT") {
        owner = { tag: "Some", value: $context.get_def({ ctx: ctx, id: $types.def_of({ t: s }) }) };
      }
      const [$r532, l$162] = expr({ fuel: $rt.int.sub(fuel, 1, $ob103), l: l, ctx: ctx, e: object, discard: false });
      l = l$162;
      return [{ tag: "FieldE", object: $r532, name: name.text, ty: ty, owner: owner }, l];
      break $m523$match;
    }
    if (true) {
      return [{ tag: "UnitE" }, l];
      break $m523$match;
    }
    $rt.unreachable();
  }
}

export function find_arg({ args, name }) {
  for (const a of args) {
    if (a.name.text === name) {
      return { tag: "Some", value: a };
    }
  }
  return { tag: "None" };
}

export function call({ fuel, l, ctx, e, discard }) {
  if (fuel === 0) {
    return [{ tag: "UnitE" }, l];
  }
  const $m538 = e;
  $m538$match: {
    if ($m538.tag === "Call") {
      const callee = $m538.callee;
      const args = $m538.args;
      const span = $m538.span;
      const callee_key = key({ l: l, tag: $defs.tag_expr, span: $parser.span_of_expr({ e: callee }) });
      const call_key = key({ l: l, tag: $defs.tag_expr, span: span });
      const ty = type_of_expr({ l: l, ctx: ctx, e: e });
      let target_id = $rt.int.neg(1, $ob104);
      let via_iface = $rt.int.neg(1, $ob105);
      const $m539 = $std_map.find({ d: ctx.refs, key: callee_key });
      $m539$match: {
        if ($m539.tag === "Some") {
          const value = $m539.value;
          const $m540 = value;
          $m540$match: {
            if ($m540.tag === "DefRes") {
              const def = $m540.def;
              target_id = def;
              break $m540$match;
            }
            if ($m540.tag === "CompanionRes") {
              const fn_def = $m540.fn_def;
              target_id = fn_def;
              break $m540$match;
            }
            if ($m540.tag === "IfaceFnRes") {
              const iface = $m540.iface;
              const fn_def = $m540.fn_def;
              target_id = fn_def;
              via_iface = iface;
              break $m540$match;
            }
            if (true) {
              skip({  });
              break $m540$match;
            }
            $rt.unreachable();
          }
          break $m539$match;
        }
        if ($m539.tag === "None") {
          skip({  });
          break $m539$match;
        }
        $rt.unreachable();
      }
      if (target_id >= 0) {
        const target = $context.get_def({ ctx: ctx, id: target_id });
        if ($rt.eq(target.kind, { tag: "Fn" }) || $rt.eq(target.kind, { tag: "IfaceFn" })) {
          const $m543 = $std_map.find({ d: ctx.signatures, key: target.id });
          $m543$match: {
            if ($m543.tag === "None") {
              return [{ tag: "UnitE" }, l];
              break $m543$match;
            }
            if ($m543.tag === "Some") {
              const value = $m543.value;
              const [$r545, l$163] = named_call({ fuel: $rt.int.sub(fuel, 1, $ob106), l: l, ctx: ctx, target: target, via_iface: via_iface, sig: value, args: args, call_key: call_key, call_ty: ty, discard: discard, e: e });
              l = l$163;
              return [$r545, l];
              break $m543$match;
            }
            $rt.unreachable();
          }
        }
      }
      const [$r546, l$164] = expr({ fuel: $rt.int.sub(fuel, 1, $ob107), l: l, ctx: ctx, e: callee, discard: false });
      l = l$164;
      const callee_ir = $r546;
      const ct = $types.strip({ t: type_of_expr({ l: l, ctx: ctx, e: callee }) });
      if (!(ct.tag === "FnT")) {
        return [{ tag: "UnitE" }, l];
      }
      let ordered = $std_list.builder({  });
      let inout_args = $std_list.builder({  });
      for (const p of $types.params_of({ t: ct })) {
        const a = find_arg({ args: args, name: p.name });
        const $m548 = a;
        $m548$match: {
          if ($m548.tag === "Some") {
            const value = $m548.value;
            const [$r549, l$165] = expr({ fuel: $rt.int.sub(fuel, 1, $ob108), l: l, ctx: ctx, e: value.value, discard: false });
            l = l$165;
            const [, ordered$166] = $std_list.push({ b: ordered, x: $r549 });
            ordered = ordered$166;
            break $m548$match;
          }
          if ($m548.tag === "None") {
            const [, ordered$167] = $std_list.push({ b: ordered, x: { tag: "UnitE" } });
            ordered = ordered$167;
            break $m548$match;
          }
          $rt.unreachable();
        }
        if (p.is_inout) {
          const [, inout_args$168] = $std_list.push({ b: inout_args, x: a });
          inout_args = inout_args$168;
        }
      }
      const call_ir = { tag: "CallValue", callee: callee_ir, fn_type: ct, args: $std_list.finish({ b: ordered }), ty: ty };
      if ($std_list.built({ b: inout_args }) === 0) {
        return [call_ir, l];
      }
      const [$r552, l$169] = inout_call({ l: l, ctx: ctx, call_ir: call_ir, ty: ty, args: $std_list.finish({ b: inout_args }), discard: discard });
      l = l$169;
      return [$r552, l];
      break $m538$match;
    }
    if (true) {
      return [{ tag: "UnitE" }, l];
      break $m538$match;
    }
    $rt.unreachable();
  }
}

export function named_call({ fuel, l, ctx, target, via_iface, sig, args, call_key, call_ty, discard, e }) {
  if (fuel === 0) {
    return [{ tag: "UnitE" }, l];
  }
  let call_target = { tag: "FnTarget", def: target, name: emitted_name({ ctx: ctx, def: target }) };
  if (via_iface >= 0) {
    call_target = dispatch({ l: l, ctx: ctx, iface: via_iface, fn_def: target, call_key: call_key, e: e });
  } else {
    if ($rt.eq(target.kind, { tag: "IfaceFn" })) {
      const $m557 = $std_map.find({ d: l.impl_fns, key: target.id });
      $m557$match: {
        if ($m557.tag === "Some") {
          const value = $m557.value;
          call_target = { tag: "FnTarget", def: value.def, name: value.name };
          break $m557$match;
        }
        if ($m557.tag === "None") {
          call_target = { tag: "FnTarget", def: target, name: target.name };
          break $m557$match;
        }
        $rt.unreachable();
      }
    }
  }
  const targs = args_or({ o: $std_map.find({ d: ctx.instantiations, key: call_key }) });
  let dicts = $std_list.builder({  });
  let consts = $std_list.builder({  });
  let i = 0;
  for (const p of sig.tparams) {
    let a = { tag: "None" };
    if (i < $std_list.len({ xs: targs })) {
      a = { tag: "Some", value: $std_list.get({ xs: targs, i: i }) };
    }
    const $m562 = p;
    $m562$match: {
      if ($m562.tag === "TypeP") {
        const bound = $m562.bound;
        const $m563 = bound;
        $m563$match: {
          if ($m563.tag === "Some") {
            const value = $m563.value;
            let bound_type = { tag: "None" };
            if (a.tag === "Some") {
              const $m565 = arg_or({ o: a });
              $m565$match: {
                if ($m565.tag === "TypeA") {
                  const ty = $m565.ty;
                  bound_type = { tag: "Some", value: ty };
                  break $m565$match;
                }
                if (true) {
                  skip({  });
                  break $m565$match;
                }
                $rt.unreachable();
              }
            }
            if (bound_type.tag === "Some") {
              const [, dicts$170] = $std_list.push({ b: dicts, x: dict_for({ l: l, ctx: ctx, iface: value, target: type_or({ o: bound_type }) }) });
              dicts = dicts$170;
            } else {
              const [, dicts$171] = $std_list.push({ b: dicts, x: { tag: "UnitE" } });
              dicts = dicts$171;
            }
            break $m563$match;
          }
          if ($m563.tag === "None") {
            skip({  });
            break $m563$match;
          }
          $rt.unreachable();
        }
        break $m562$match;
      }
      if ($m562.tag === "ConstP") {
        let pushed = false;
        if (a.tag === "Some") {
          const $m568 = arg_or({ o: a });
          $m568$match: {
            if ($m568.tag === "ConstA") {
              const value = $m568.value;
              const [, consts$172] = $std_list.push({ b: consts, x: const_literal({ ctx: ctx, v: value }) });
              consts = consts$172;
              pushed = true;
              break $m568$match;
            }
            if (true) {
              skip({  });
              break $m568$match;
            }
            $rt.unreachable();
          }
        }
        if (!pushed) {
          const [, consts$173] = $std_list.push({ b: consts, x: { tag: "UnitE" } });
          consts = consts$173;
        }
        break $m562$match;
      }
      if (true) {
        skip({  });
        break $m562$match;
      }
      $rt.unreachable();
    }
    i = $rt.int.add(i, 1, $ob109);
  }
  let arg_exprs = $std_list.builder({  });
  let inout_args = $std_list.builder({  });
  for (const p of sig.params) {
    const a = find_arg({ args: args, name: p.name });
    const $m570 = a;
    $m570$match: {
      if ($m570.tag === "Some") {
        const value = $m570.value;
        const [$r571, l$174] = expr({ fuel: $rt.int.sub(fuel, 1, $ob110), l: l, ctx: ctx, e: value.value, discard: false });
        l = l$174;
        const [, arg_exprs$175] = $std_list.push({ b: arg_exprs, x: $r571 });
        arg_exprs = arg_exprs$175;
        break $m570$match;
      }
      if ($m570.tag === "None") {
        const [, arg_exprs$176] = $std_list.push({ b: arg_exprs, x: { tag: "UnitE" } });
        arg_exprs = arg_exprs$176;
        break $m570$match;
      }
      $rt.unreachable();
    }
    if (p.is_inout) {
      const [, inout_args$177] = $std_list.push({ b: inout_args, x: a });
      inout_args = inout_args$177;
    }
  }
  const [$r573, l$178] = decreases_check({ fuel: $rt.int.sub(fuel, 1, $ob111), l: l, ctx: ctx, call_key: call_key, sig: sig, args: $std_list.finish({ b: arg_exprs }) });
  l = l$178;
  const arg_list = $r573;
  let decoder = { tag: "None" };
  if ($context.qualified_name({ ctx: ctx, id: target.id }) === "std.sql.select") {
    const [$r575, l$179] = select_decoder({ fuel: $rt.int.sub(fuel, 1, $ob112), l: l, ctx: ctx, call_key: call_key, sig: sig });
    l = l$179;
    decoder = $r575;
  }
  let inst = [];
  const $m576 = $std_map.find({ d: ctx.instantiations, key: call_key });
  $m576$match: {
    if ($m576.tag === "Some") {
      const value = $m576.value;
      inst = value;
      break $m576$match;
    }
    if ($m576.tag === "None") {
      skip({  });
      break $m576$match;
    }
    $rt.unreachable();
  }
  const call_ir = { tag: "Call", target: call_target, sig: sig, dicts: $std_list.finish({ b: dicts }), consts: $std_list.finish({ b: consts }), args: arg_list, ty: call_ty, decoder: decoder, targs: inst };
  if ($std_list.built({ b: inout_args }) === 0) {
    return [call_ir, l];
  }
  const [$r578, l$180] = inout_call({ l: l, ctx: ctx, call_ir: call_ir, ty: call_ty, args: $std_list.finish({ b: inout_args }), discard: discard });
  l = l$180;
  return [$r578, l];
}

export function arg_or({ o }) {
  const $m579 = o;
  $m579$match: {
    if ($m579.tag === "Some") {
      const value = $m579.value;
      return value;
      break $m579$match;
    }
    if ($m579.tag === "None") {
      return { tag: "TypeA", ty: { tag: "ErrorT" } };
      break $m579$match;
    }
    $rt.unreachable();
  }
}

export function decreases_check({ fuel, l, ctx, call_key, sig, args }) {
  if (fuel === 0) {
    return [[], l];
  }
  let ob = { tag: "None" };
  for (const o of obligations_at({ ctx: ctx, k: call_key })) {
    if (ob.tag === "None" && o.kind.tag === "Decreases" && !is_proved({ o: o })) {
      ob = { tag: "Some", value: o };
    }
  }
  let measure = { tag: "None" };
  const $m585 = l.fn_ctx;
  $m585$match: {
    if ($m585.tag === "Some") {
      const value = $m585.value;
      measure = value.measure;
      break $m585$match;
    }
    if ($m585.tag === "None") {
      skip({  });
      break $m585$match;
    }
    $rt.unreachable();
  }
  if (ob.tag === "None" || measure.tag === "None") {
    return [args, l];
  }
  const o = ob_or({ o: ob });
  const src = or_neg({ o: o.source });
  const clause = $std_map.find({ d: ctx.contract_nodes, key: src });
  if (clause.tag === "None") {
    return [args, l];
  }
  let clause_numeric = false;
  const $m586 = clause;
  $m586$match: {
    if ($m586.tag === "Some") {
      const value = $m586.value;
      clause_numeric = is_int_like({ t: type_of_expr({ l: l, ctx: ctx, e: value.expr }) });
      break $m586$match;
    }
    if ($m586.tag === "None") {
      skip({  });
      break $m586$match;
    }
    $rt.unreachable();
  }
  if (!clause_numeric) {
    return [args, l];
  }
  let bound = $std_list.builder({  });
  let subst = $std_map.dict({  });
  let i = 0;
  for (const p of sig.params) {
    let value_ir = { tag: "UnitE" };
    if (i < $std_list.len({ xs: args })) {
      value_ir = $std_list.get({ xs: args, i: i });
    }
    let local = value_ir;
    if (!p.is_inout && !(value_ir.tag === "Local" || value_ir.tag === "IntE" || value_ir.tag === "BoolE" || value_ir.tag === "TextE")) {
      const [$r588, l$181] = tmp({ l: l, prefix: "arg" });
      l = l$181;
      const name = $r588;
      const [, l$182] = push_pre({ l: l, s: { tag: "Let", name: name, ty: p.ty, mutable: false, value: value_ir } });
      l = l$182;
      local = { tag: "Local", name: name, ty: p.ty };
    }
    const [, bound$183] = $std_list.push({ b: bound, x: local });
    bound = bound$183;
    if (i < $std_list.len({ xs: sig.param_defs })) {
      const [, subst$184] = $std_map.set({ d: subst, key: $std_list.get({ xs: sig.param_defs, i: i }), value: local });
      subst = subst$184;
    }
    i = $rt.int.add(i, 1, $ob113);
  }
  const saved_subst = l.subst;
  const saved_file = l.file;
  l = { ...l, subst: subst, file: $lower.pred_file({ k: src }) };
  const [$r592, l$185] = expr({ fuel: $rt.int.sub(fuel, 1, $ob114), l: l, ctx: ctx, e: contract_or({ o: clause }).expr, discard: false });
  l = l$185;
  const callee_measure = $r592;
  l = { ...l, subst: saved_subst, file: saved_file };
  const [, l$186] = push_pre({ l: l, s: { tag: "Check", cond: { tag: "Cmp", op: "<", left: callee_measure, right: { tag: "Local", name: text_or({ o: measure, dflt: "$measure" }), ty: { tag: "Prim", name: "Int" } }, is_float: false }, ob: ob_ref_of({ l: l, ctx: ctx, o: o }) } });
  l = l$186;
  return [$std_list.finish({ b: bound }), l];
}

export function contract_or({ o }) {
  const $m598 = o;
  $m598$match: {
    if ($m598.tag === "Some") {
      const value = $m598.value;
      return value;
      break $m598$match;
    }
    if ($m598.tag === "None") {
      return { clause: "", is_proved: false, expr: { tag: "Hole", span: { start: 0, end: 0 } }, span: { start: 0, end: 0 } };
      break $m598$match;
    }
    $rt.unreachable();
  }
}

export function select_decoder({ fuel, l, ctx, call_key, sig }) {
  if (fuel === 0) {
    return [{ tag: "None" }, l];
  }
  const targs = args_or({ o: $std_map.find({ d: ctx.instantiations, key: call_key }) });
  let index = $rt.int.neg(1, $ob115);
  let i = 0;
  for (const p of sig.tparams) {
    if (index < 0 && p.tag === "TypeP") {
      index = i;
    }
    i = $rt.int.add(i, 1, $ob116);
  }
  if (index < 0 || index >= $std_list.len({ xs: targs })) {
    return [{ tag: "None" }, l];
  }
  let row_type = { tag: "None" };
  const $m606 = $std_list.get({ xs: targs, i: index });
  $m606$match: {
    if ($m606.tag === "TypeA") {
      const ty = $m606.ty;
      row_type = { tag: "Some", value: ty };
      break $m606$match;
    }
    if (true) {
      skip({  });
      break $m606$match;
    }
    $rt.unreachable();
  }
  if (row_type.tag === "None") {
    return [{ tag: "None" }, l];
  }
  const ty = type_or({ o: row_type });
  const s = $types.strip({ t: ty });
  if (!(s.tag === "RecordT")) {
    return [{ tag: "None" }, l];
  }
  const def = $context.get_def({ ctx: ctx, id: $types.def_of({ t: s }) });
  const [$r610, l$187] = tmp({ l: l, prefix: "row" });
  l = l$187;
  const it_name = $r610;
  const object = { tag: "Local", name: it_name, ty: ty };
  let fields = $std_list.builder({  });
  let checks = [];
  const saved = l.field_object;
  l = { ...l, field_object: { tag: "Some", value: { record_def: def.id, name: it_name, ty: ty } } };
  for (const f of fields_or({ o: $std_map.find({ d: ctx.fields, key: def.id }) })) {
    const fs = $types.strip({ t: f.ty });
    let kind = "other";
    if (fs.tag === "Prim") {
      kind = $types.prim_name({ t: fs });
    }
    const [, fields$188] = $std_list.push({ b: fields, x: { name: f.name, kind: kind } });
    fields = fields$188;
    const value_ir = { tag: "FieldE", object: object, name: f.name, ty: f.ty, owner: { tag: "Some", value: def } };
    for (const p of refinement_preds({ ctx: ctx, t: f.ty })) {
      const [$r618, l$189] = begin_collect({ l: l });
      l = l$189;
      const saved_pre = $r618;
      const [$r619, l$190] = with_it({ fuel: $rt.int.sub(fuel, 1, $ob117), l: l, ctx: ctx, it_expr: value_ir, pred: p });
      l = l$190;
      const cond = $r619;
      const [$r621, l$191] = end_collect({ l: l, saved: saved_pre, out: [{ tag: "Reject", cond: cond, column: f.name }] });
      l = l$191;
      checks = [...checks, ...$r621];
    }
  }
  l = { ...l, field_object: saved };
  return [{ tag: "Some", value: { it_name: it_name, ty: ty, fields: $std_list.finish({ b: fields }), checks: checks } }, l];
}

export function inout_call({ l, ctx, call_ir, ty, args, discard }) {
  let targets = $std_list.builder({  });
  for (const a of args) {
    let target = { tag: "None" };
    const $m626 = a;
    $m626$match: {
      if ($m626.tag === "Some") {
        const value = $m626.value;
        const $m627 = value.value;
        $m627$match: {
          if ($m627.tag === "Name") {
            const name = $m627.name;
            target = { tag: "Some", value: { name: name.text, ty: type_of_expr({ l: l, ctx: ctx, e: value.value }) } };
            break $m627$match;
          }
          if (true) {
            skip({  });
            break $m627$match;
          }
          $rt.unreachable();
        }
        break $m626$match;
      }
      if ($m626.tag === "None") {
        skip({  });
        break $m626$match;
      }
      $rt.unreachable();
    }
    const $m630 = target;
    $m630$match: {
      if ($m630.tag === "Some") {
        const value = $m630.value;
        const [, targets$192] = $std_list.push({ b: targets, x: value });
        targets = targets$192;
        break $m630$match;
      }
      if ($m630.tag === "None") {
        const [$r631, l$193] = tmp({ l: l, prefix: "x" });
        l = l$193;
        const [, targets$194] = $std_list.push({ b: targets, x: { name: $r631, ty: { tag: "ErrorT" } } });
        targets = targets$194;
        break $m630$match;
      }
      $rt.unreachable();
    }
  }
  let result_target = { tag: "None" };
  if (!discard) {
    const [$r635, l$195] = tmp({ l: l, prefix: "r" });
    l = l$195;
    result_target = { tag: "Some", value: { name: $r635, ty: ty } };
  }
  const [, l$196] = push_pre({ l: l, s: { tag: "CallInout", result_target: result_target, call: call_ir, targets: $std_list.finish({ b: targets }) } });
  l = l$196;
  const $m639 = result_target;
  $m639$match: {
    if ($m639.tag === "None") {
      return [{ tag: "UnitE" }, l];
      break $m639$match;
    }
    if ($m639.tag === "Some") {
      const value = $m639.value;
      return [{ tag: "Local", name: value.name, ty: ty }, l];
      break $m639$match;
    }
    $rt.unreachable();
  }
}

export function const_literal({ ctx, v }) {
  const $m642 = v;
  $m642$match: {
    if ($m642.tag === "SymV") {
      const def = $m642.def;
      return { tag: "Local", name: $context.get_def({ ctx: ctx, id: def }).name, ty: { tag: "ErrorT" } };
      break $m642$match;
    }
    if (true) {
      return { tag: "ConstE", value: v };
      break $m642$match;
    }
    $rt.unreachable();
  }
}

export function dict_for({ l, ctx, iface, target }) {
  const s = $types.strip({ t: target });
  if (s.tag === "ParamT") {
    const $m646 = l.fn_ctx;
    $m646$match: {
      if ($m646.tag === "Some") {
        const value = $m646.value;
        const dn = $std_map.find({ d: value.dicts, key: $types.def_of({ t: s }) });
        if (dn.tag === "Some") {
          return { tag: "DictParamE", name: text_or({ o: dn, dflt: "" }) };
        }
        break $m646$match;
      }
      if ($m646.tag === "None") {
        skip({  });
        break $m646$match;
      }
      $rt.unreachable();
    }
  }
  const iface_def = $context.get_def({ ctx: ctx, id: iface });
  const want = type_slug({ fuel: 1000, ctx: ctx, t: s });
  const $hi648 = ctx.module_count;
  for (let mid = 0; mid < $hi648; mid++) {
    const $m649 = $std_map.find({ d: ctx.modules, key: mid });
    $m649$match: {
      if ($m649.tag === "None") {
        skip({  });
        break $m649$match;
      }
      if ($m649.tag === "Some") {
        const value = $m649.value;
        for (const item of value.tree.items) {
          const $m650 = item;
          $m650$match: {
            if ($m650.tag === "ImplItem") {
              const decl = $m650.decl;
              const k = $defs.node_key({ file: value.file, tag: $defs.tag_item, span: decl.span });
              if (ref_def({ ctx: ctx, k: k }) === iface) {
                const impl_def = or_neg({ o: $std_map.find({ d: ctx.def_of, key: k }) });
                const target_type = impl_target({ ctx: ctx, impl_def: impl_def });
                if (type_slug({ fuel: 1000, ctx: ctx, t: target_type }) === want) {
                  return { tag: "DictE", iface: iface_def, target: target_type, mod: mid, name: impl_dict_name({ ctx: ctx, iface: iface_def, target: target_type }) };
                }
              }
              break $m650$match;
            }
            if (true) {
              skip({  });
              break $m650$match;
            }
            $rt.unreachable();
          }
        }
        break $m649$match;
      }
      $rt.unreachable();
    }
  }
  return { tag: "UnitE" };
}

export function dispatch({ l, ctx, iface, fn_def, call_key, e }) {
  const targs = args_or({ o: $std_map.find({ d: ctx.instantiations, key: call_key }) });
  let target = { tag: "None" };
  const n = $std_list.len({ xs: targs });
  if (n > 0) {
    const $m654 = $std_list.get({ xs: targs, i: $rt.int.sub(n, 1, $ob118) });
    $m654$match: {
      if ($m654.tag === "TypeA") {
        const ty = $m654.ty;
        target = { tag: "Some", value: ty };
        break $m654$match;
      }
      if (true) {
        skip({  });
        break $m654$match;
      }
      $rt.unreachable();
    }
  }
  let target_is_param = false;
  const $m656 = target;
  $m656$match: {
    if ($m656.tag === "Some") {
      const value = $m656.value;
      target_is_param = $types.strip({ t: value }).tag === "ParamT";
      break $m656$match;
    }
    if ($m656.tag === "None") {
      skip({  });
      break $m656$match;
    }
    $rt.unreachable();
  }
  const $m657 = $std_map.find({ d: l.impl_fns, key: fn_def.id });
  $m657$match: {
    if ($m657.tag === "Some") {
      const value = $m657.value;
      if (target.tag === "Some" && target_is_param) {
        return { tag: "FnTarget", def: value.def, name: value.name };
      }
      break $m657$match;
    }
    if ($m657.tag === "None") {
      skip({  });
      break $m657$match;
    }
    $rt.unreachable();
  }
  if (target.tag === "None") {
    return { tag: "DictTarget", dict: { tag: "UnitE" }, name: fn_def.name };
  }
  if (!target_is_param) {
    const $m661 = $claimcheck.callee_of({ ctx: ctx, file: l.file, e: e });
    $m661$match: {
      if ($m661.tag === "ImplCallee") {
        const def = $m661.def;
        const d = $context.get_def({ ctx: ctx, id: def });
        return { tag: "FnTarget", def: d, name: emitted_name({ ctx: ctx, def: d }) };
        break $m661$match;
      }
      if (true) {
        skip({  });
        break $m661$match;
      }
      $rt.unreachable();
    }
  }
  return { tag: "DictTarget", dict: dict_for({ l: l, ctx: ctx, iface: iface, target: type_or({ o: target }) }), name: fn_def.name };
}

export function unary({ fuel, l, ctx, e }) {
  if (fuel === 0) {
    return [{ tag: "UnitE" }, l];
  }
  const $m665 = e;
  $m665$match: {
    if ($m665.tag === "Unary") {
      const op = $m665.op;
      const operand = $m665.operand;
      const span = $m665.span;
      const [$r666, l$197] = expr({ fuel: $rt.int.sub(fuel, 1, $ob119), l: l, ctx: ctx, e: operand, discard: false });
      l = l$197;
      const v = $r666;
      if (op === "not") {
        return [{ tag: "Not", operand: v }, l];
      }
      if (is_int_like({ t: type_of_expr({ l: l, ctx: ctx, e: operand }) })) {
        let def_name = "?";
        const $m668 = l.fn_ctx;
        $m668$match: {
          if ($m668.tag === "Some") {
            const value = $m668.value;
            if (value.def.tag === "Some") {
              def_name = def_or({ o: value.def }).name;
            }
            break $m668$match;
          }
          if ($m668.tag === "None") {
            skip({  });
            break $m668$match;
          }
          $rt.unreachable();
        }
        return [{ tag: "Neg", operand: v, is_float: false, ob: { tag: "Some", value: ob_ref({ l: l, ctx: ctx, kind: "overflow", text: "-" + $printer.print_expr({ e: operand }) + " within Int", file: l.file, span: span, def: def_name }) } }, l];
      }
      return [{ tag: "Neg", operand: v, is_float: true, ob: { tag: "None" } }, l];
      break $m665$match;
    }
    if (true) {
      return [{ tag: "UnitE" }, l];
      break $m665$match;
    }
    $rt.unreachable();
  }
}

export function def_or({ o }) {
  const $m674 = o;
  $m674$match: {
    if ($m674.tag === "Some") {
      const value = $m674.value;
      return value;
      break $m674$match;
    }
    if ($m674.tag === "None") {
      return { id: $rt.int.neg(1, $ob120), kind: { tag: "Fn" }, name: "?", node: { tag: "NoNode" }, mod: $rt.int.neg(1, $ob121), file: $rt.int.neg(1, $ob122), key: $rt.int.neg(1, $ob123), span: { start: 0, end: 0 }, is_pub: false, is_hardened: false, is_sealed: false, is_intrinsic: false, parent: { tag: "None" }, frame: $rt.int.neg(1, $ob124), is_inout: false };
      break $m674$match;
    }
    $rt.unreachable();
  }
}

export function short_circuit({ fuel, l, ctx, is_and, operands }) {
  if (fuel === 0) {
    return [{ tag: "UnitE" }, l];
  }
  if ($std_list.len({ xs: operands }) === 0) {
    return [{ tag: "BoolE", v: is_and }, l];
  }
  const [$r682, l$198] = expr({ fuel: $rt.int.sub(fuel, 1, $ob125), l: l, ctx: ctx, e: $std_list.get({ xs: operands, i: 0 }), discard: false });
  l = l$198;
  const head = $r682;
  let laters = $std_list.builder({  });
  let any_pre = false;
  let first = true;
  for (const o of operands) {
    if (first) {
      first = false;
    } else {
      const [$r683, l$199] = begin_collect({ l: l });
      l = l$199;
      const saved = $r683;
      const [$r684, l$200] = expr({ fuel: $rt.int.sub(fuel, 1, $ob126), l: l, ctx: ctx, e: o, discard: false });
      l = l$200;
      const v = $r684;
      const [$r685, l$201] = end_collect({ l: l, saved: saved, out: [] });
      l = l$201;
      const pre = $r685;
      if ($std_list.len({ xs: pre }) > 0) {
        any_pre = true;
      }
      const [, laters$202] = $std_list.push({ b: laters, x: { pre: pre, value: v } });
      laters = laters$202;
    }
  }
  const rest = $std_list.finish({ b: laters });
  if (!any_pre) {
    let all = $std_list.builder({  });
    const [, all$203] = $std_list.push({ b: all, x: head });
    all = all$203;
    for (const lt of rest) {
      const [, all$204] = $std_list.push({ b: all, x: lt.value });
      all = all$204;
    }
    if (is_and) {
      return [{ tag: "And", operands: $std_list.finish({ b: all }) }, l];
    }
    return [{ tag: "Or", operands: $std_list.finish({ b: all }) }, l];
  }
  const [$r689, l$205] = tmp({ l: l, prefix: "sc" });
  l = l$205;
  const name = $r689;
  const bool_t = { tag: "Prim", name: "Bool" };
  const local = { tag: "Local", name: name, ty: bool_t };
  const [, l$206] = push_pre({ l: l, s: { tag: "Let", name: name, ty: bool_t, mutable: true, value: head } });
  l = l$206;
  for (const lt of rest) {
    let cond = local;
    if (!is_and) {
      cond = { tag: "Not", operand: local };
    }
    const [, l$207] = push_pre({ l: l, s: { tag: "If", cond: cond, then_block: [...lt.pre, ...[{ tag: "Assign", name: name, ty: bool_t, value: lt.value }]], else_block: { tag: "None" } } });
    l = l$207;
  }
  return [local, l];
}

export function implies_expr({ fuel, l, ctx, left, right }) {
  if (fuel === 0) {
    return [{ tag: "UnitE" }, l];
  }
  const [$r698, l$208] = expr({ fuel: $rt.int.sub(fuel, 1, $ob127), l: l, ctx: ctx, e: left, discard: false });
  l = l$208;
  const left_ir = $r698;
  const [$r699, l$209] = begin_collect({ l: l });
  l = l$209;
  const saved = $r699;
  const [$r700, l$210] = expr({ fuel: $rt.int.sub(fuel, 1, $ob128), l: l, ctx: ctx, e: right, discard: false });
  l = l$210;
  const right_ir = $r700;
  const [$r701, l$211] = end_collect({ l: l, saved: saved, out: [] });
  l = l$211;
  const pre = $r701;
  if ($std_list.len({ xs: pre }) === 0) {
    return [{ tag: "Implies", left: left_ir, right: right_ir }, l];
  }
  const [$r703, l$212] = tmp({ l: l, prefix: "sc" });
  l = l$212;
  const name = $r703;
  const bool_t = { tag: "Prim", name: "Bool" };
  const local = { tag: "Local", name: name, ty: bool_t };
  const [, l$213] = push_pre({ l: l, s: { tag: "Let", name: name, ty: bool_t, mutable: true, value: { tag: "Not", operand: left_ir } } });
  l = l$213;
  const [, l$214] = push_pre({ l: l, s: { tag: "If", cond: { tag: "Not", operand: local }, then_block: [...pre, ...[{ tag: "Assign", name: name, ty: bool_t, value: right_ir }]], else_block: { tag: "None" } } });
  l = l$214;
  return [local, l];
}

export function binary({ fuel, l, ctx, e }) {
  if (fuel === 0) {
    return [{ tag: "UnitE" }, l];
  }
  const $m713 = e;
  $m713$match: {
    if ($m713.tag === "Binary") {
      const op = $m713.op;
      const left = $m713.left;
      const right = $m713.right;
      const span = $m713.span;
      if (op === "implies") {
        const [$r714, l$215] = implies_expr({ fuel: $rt.int.sub(fuel, 1, $ob129), l: l, ctx: ctx, left: left, right: right });
        l = l$215;
        return [$r714, l];
      }
      const lt = type_of_expr({ l: l, ctx: ctx, e: left });
      const s = $types.strip({ t: lt });
      const [$r715, l$216] = expr({ fuel: $rt.int.sub(fuel, 1, $ob130), l: l, ctx: ctx, e: left, discard: false });
      l = l$216;
      const left_ir = $r715;
      const [$r716, l$217] = expr({ fuel: $rt.int.sub(fuel, 1, $ob131), l: l, ctx: ctx, e: right, discard: false });
      l = l$217;
      const right_ir = $r716;
      if (op === "+" || op === "-" || op === "*" || op === "/" || op === "%") {
        if (is_int_like({ t: lt })) {
          let ob = { tag: "None" };
          const $m718 = find_ob({ ctx: ctx, k: key({ l: l, tag: $defs.tag_expr, span: span }), kind: { tag: "Overflow" } });
          $m718$match: {
            if ($m718.tag === "Some") {
              const value = $m718.value;
              if (is_checked({ o: value })) {
                ob = { tag: "Some", value: ob_ref_of({ l: l, ctx: ctx, o: value }) };
              }
              break $m718$match;
            }
            if ($m718.tag === "None") {
              skip({  });
              break $m718$match;
            }
            $rt.unreachable();
          }
          return [{ tag: "IntOp", op: op, left: left_ir, right: right_ir, ob: ob }, l];
        }
        return [{ tag: "FloatOp", op: op, left: left_ir, right: right_ir }, l];
      }
      if (op === "++") {
        return [{ tag: "Concat", left: left_ir, right: right_ir, is_text: s.tag === "Prim" && $types.prim_name({ t: s }) === "Text", ty: type_of_expr({ l: l, ctx: ctx, e: e }) }, l];
      }
      if (op === "==" || op === "!=") {
        return [{ tag: "Eq", left: left_ir, right: right_ir, ty: lt, prim: s.tag === "Prim", negate: op === "!=" }, l];
      }
      if (op === "<" || op === "<=" || op === ">" || op === ">=") {
        return [{ tag: "Cmp", op: op, left: left_ir, right: right_ir, is_float: s.tag === "Prim" && $types.prim_name({ t: s }) === "Float" }, l];
      }
      return [{ tag: "Implies", left: left_ir, right: right_ir }, l];
      break $m713$match;
    }
    if (true) {
      return [{ tag: "UnitE" }, l];
      break $m713$match;
    }
    $rt.unreachable();
  }
}

export function assertion_block({ l, ctx, b }) {
  let out = [];
  for (const s of b.stmts) {
    const $m728 = s;
    $m728$match: {
      if ($m728.tag === "ExprStmt") {
        const expr = $m728.expr;
        const [$r729, l$218] = begin_collect({ l: l });
        l = l$218;
        const saved = $r729;
        const [$r730, l$219] = expr_of({ fuel: max_depth, l: l, ctx: ctx, e: expr, discard: false });
        l = l$219;
        const cond = $r730;
        const [$r732, l$220] = end_collect({ l: l, saved: saved, out: [{ tag: "Assert", cond: cond }] });
        l = l$220;
        out = [...out, ...$r732];
        break $m728$match;
      }
      if (true) {
        const [$r733, l$221] = stmt({ fuel: max_depth, l: l, ctx: ctx, s: s });
        l = l$221;
        out = [...out, ...$r733];
        break $m728$match;
      }
      $rt.unreachable();
    }
  }
  return [out, l];
}

export function tests_of({ l, ctx }) {
  let examples = $std_list.builder({  });
  let properties = $std_list.builder({  });
  for (const item of l.m.tree.items) {
    const $m734 = item;
    $m734$match: {
      if ($m734.tag === "ExampleItem") {
        const decl = $m734.decl;
        l = { ...l, fn_ctx: { tag: "None" } };
        const [$r737, l$222] = assertion_block({ l: l, ctx: ctx, b: decl.body });
        l = l$222;
        const [, examples$223] = $std_list.push({ b: examples, x: { name: decl.name.text, body: $r737 } });
        examples = examples$223;
        break $m734$match;
      }
      if ($m734.tag === "PropertyItem") {
        const decl = $m734.decl;
        l = { ...l, fn_ctx: { tag: "None" } };
        const negate = def_at({ l: l, ctx: ctx, tag: $defs.tag_item, span: decl.span }) === or_neg({ o: l.opts.negate_guard });
        const [$r741, l$224] = gen_params({ l: l, ctx: ctx, params: decl.params, subst: $std_map.dict({  }), negate: negate });
        l = l$224;
        const params = $r741;
        const [$r742, l$225] = assertion_block({ l: l, ctx: ctx, b: decl.body });
        l = l$225;
        const [, properties$226] = $std_list.push({ b: properties, x: { label: "property " + decl.name.text, params: params, body: $r742 } });
        properties = properties$226;
        break $m734$match;
      }
      if ($m734.tag === "ImplItem") {
        const decl = $m734.decl;
        const impl_key = key({ l: l, tag: $defs.tag_item, span: decl.span });
        const iface_id = ref_def({ ctx: ctx, k: impl_key });
        if (iface_id >= 0) {
          const iface = $context.get_def({ ctx: ctx, id: iface_id });
          const impl_def = or_neg({ o: $std_map.find({ d: ctx.def_of, key: impl_key }) });
          const target = impl_target({ ctx: ctx, impl_def: impl_def });
          let subst = $std_map.dict({  });
          for (const d of $std_list.finish({ b: ctx.defs })) {
            if (or_neg({ o: d.parent }) === iface_id && $rt.eq(d.kind, { tag: "TypeParam" })) {
              const [, subst$227] = $std_map.set({ d: subst, key: d.id, value: { tag: "TypeA", ty: target } });
              subst = subst$227;
            }
          }
          const map = impl_map({ l: l, ctx: ctx, iface: iface_id, fns: decl.fns });
          const [, l$228, properties$228] = law_properties({ l: l, ctx: ctx, iface: iface, iface_id: iface_id, target: target, subst: subst, map: map, properties: properties });
          l = l$228;
          properties = properties$228;
        }
        break $m734$match;
      }
      if (true) {
        skip({  });
        break $m734$match;
      }
      $rt.unreachable();
    }
  }
  if ($std_list.built({ b: examples }) === 0 && $std_list.built({ b: properties }) === 0) {
    return [{ tag: "None" }, l];
  }
  return [{ tag: "Some", value: { examples: $std_list.finish({ b: examples }), properties: $std_list.finish({ b: properties }) } }, l];
}

export function law_properties({ l, ctx, iface, iface_id, target, subst, map, properties }) {
  for (const d of $std_list.finish({ b: ctx.defs })) {
    if (or_neg({ o: d.parent }) === iface_id && $rt.eq(d.kind, { tag: "Law" })) {
      const $m750 = d.node;
      $m750$match: {
        if ($m750.tag === "LawNode") {
          const decl = $m750.decl;
          l = { ...l, impl_fns: map, fn_ctx: { tag: "None" } };
          const [$r753, l$229] = gen_params({ l: l, ctx: ctx, params: decl.params, subst: subst, negate: false });
          l = l$229;
          const params = $r753;
          const [$r754, l$230] = assertion_block({ l: l, ctx: ctx, b: decl.body });
          l = l$230;
          const [, properties$231] = $std_list.push({ b: properties, x: { label: "law " + iface.name + "[" + type_slug({ fuel: 1000, ctx: ctx, t: target }) + "]." + d.name, params: params, body: $r754 } });
          properties = properties$231;
          l = { ...l, impl_fns: $std_map.dict({  }) };
          break $m750$match;
        }
        if (true) {
          skip({  });
          break $m750$match;
        }
        $rt.unreachable();
      }
    }
  }
  return [undefined, l, properties];
  return [undefined, l, properties];
}

export function gen_params({ l, ctx, params, subst, negate }) {
  let out = $std_list.builder({  });
  for (const p of params) {
    const pd = def_at({ l: l, ctx: ctx, tag: $defs.tag_sig, span: p.span });
    let ty = { tag: "ErrorT" };
    if (pd >= 0) {
      ty = type_or({ o: $std_map.find({ d: ctx.decl_types, key: pd }) });
    }
    const [$r758, l$232] = generator({ fuel: max_depth, l: l, ctx: ctx, t: $types.substitute({ t: ty, subst: subst }), negate: negate });
    l = l$232;
    const [, out$233] = $std_list.push({ b: out, x: { name: p.name.text, gen: $r758 } });
    out = out$233;
  }
  return [$std_list.finish({ b: out }), l];
}

export function generator({ fuel, l, ctx, t, negate }) {
  if (fuel === 0) {
    return [{ base: { tag: "UnknownG" }, filters: [] }, l];
  }
  const [$r762, l$234] = tmp({ l: l, prefix: "it" });
  l = l$234;
  const it_name = $r762;
  const local = { tag: "Local", name: it_name, ty: t };
  let filters = $std_list.builder({  });
  for (const p of refinement_preds({ ctx: ctx, t: t })) {
    const [$r764, l$235] = with_it({ fuel: fuel, l: l, ctx: ctx, it_expr: local, pred: p });
    l = l$235;
    let cond = $r764;
    if (negate) {
      cond = { tag: "Not", operand: cond };
    }
    const [, filters$236] = $std_list.push({ b: filters, x: { it_name: it_name, cond: cond } });
    filters = filters$236;
  }
  const [$r767, l$237] = base_generator({ fuel: $rt.int.sub(fuel, 1, $ob132), l: l, ctx: ctx, s: $types.strip({ t: t }) });
  l = l$237;
  return [{ base: $r767, filters: $std_list.finish({ b: filters }) }, l];
}

export function base_generator({ fuel, l, ctx, s }) {
  if (fuel === 0) {
    return [{ tag: "UnknownG" }, l];
  }
  const $m770 = s;
  $m770$match: {
    if ($m770.tag === "Prim") {
      const name = $m770.name;
      if (name === "Int") {
        return [{ tag: "IntG" }, l];
      }
      if (name === "Float") {
        return [{ tag: "FloatG" }, l];
      }
      if (name === "Duration") {
        return [{ tag: "DurationG" }, l];
      }
      if (name === "Bool") {
        return [{ tag: "BoolG" }, l];
      }
      if (name === "Text") {
        return [{ tag: "TextG" }, l];
      }
      if (name === "Unit") {
        return [{ tag: "UnitG" }, l];
      }
      return [{ tag: "UnknownG" }, l];
      break $m770$match;
    }
    if ($m770.tag === "Opaque") {
      const def = $m770.def;
      const args = $m770.args;
      if ($context.qualified_name({ ctx: ctx, id: def }) === "std.list.List" && $std_list.len({ xs: args }) > 0) {
        const $m778 = $std_list.get({ xs: args, i: 0 });
        $m778$match: {
          if ($m778.tag === "TypeA") {
            const ty = $m778.ty;
            const [$r779, l$238] = generator({ fuel: $rt.int.sub(fuel, 1, $ob133), l: l, ctx: ctx, t: ty, negate: false });
            l = l$238;
            return [{ tag: "ListG", elem: $r779 }, l];
            break $m778$match;
          }
          if (true) {
            return [{ tag: "UnknownG" }, l];
            break $m778$match;
          }
          $rt.unreachable();
        }
      }
      return [{ tag: "UnknownG" }, l];
      break $m770$match;
    }
    if ($m770.tag === "RecordT") {
      const def = $m770.def;
      const [$r783, l$239] = gen_fields({ fuel: $rt.int.sub(fuel, 1, $ob134), l: l, ctx: ctx, owner: def });
      l = l$239;
      return [{ tag: "RecordG", fields: $r783 }, l];
      break $m770$match;
    }
    if ($m770.tag === "UnionT") {
      const def = $m770.def;
      let variants = $std_list.builder({  });
      for (const v of ids_or({ o: $std_map.find({ d: ctx.variants, key: def }) })) {
        const [$r785, l$240] = gen_fields({ fuel: $rt.int.sub(fuel, 1, $ob135), l: l, ctx: ctx, owner: v });
        l = l$240;
        const [, variants$241] = $std_list.push({ b: variants, x: { name: $context.get_def({ ctx: ctx, id: v }).name, fields: $r785 } });
        variants = variants$241;
      }
      return [{ tag: "UnionG", variants: $std_list.finish({ b: variants }) }, l];
      break $m770$match;
    }
    if (true) {
      return [{ tag: "UnknownG" }, l];
      break $m770$match;
    }
    $rt.unreachable();
  }
}

export function gen_fields({ fuel, l, ctx, owner }) {
  if (fuel === 0) {
    return [[], l];
  }
  let out = $std_list.builder({  });
  for (const f of fields_or({ o: $std_map.find({ d: ctx.fields, key: owner }) })) {
    const [$r789, l$242] = generator({ fuel: $rt.int.sub(fuel, 1, $ob136), l: l, ctx: ctx, t: f.ty, negate: false });
    l = l$242;
    const [, out$243] = $std_list.push({ b: out, x: { name: f.name, gen: $r789 } });
    out = out$243;
  }
  return [$std_list.finish({ b: out }), l];
}

export function verifies_of({ l, ctx }) {
  let out = $std_list.builder({  });
  let fns = $std_list.builder({  });
  for (const item of l.m.tree.items) {
    const $m791 = item;
    $m791$match: {
      if ($m791.tag === "FnItem") {
        const decl = $m791.decl;
        const [, fns$244] = $std_list.push({ b: fns, x: decl });
        fns = fns$244;
        break $m791$match;
      }
      if ($m791.tag === "ImplItem") {
        const decl = $m791.decl;
        for (const f of decl.fns) {
          const [, fns$245] = $std_list.push({ b: fns, x: f });
          fns = fns$245;
        }
        break $m791$match;
      }
      if (true) {
        skip({  });
        break $m791$match;
      }
      $rt.unreachable();
    }
  }
  for (const f of $std_list.finish({ b: fns })) {
    const $m792 = f.body;
    $m792$match: {
      if ($m792.tag === "None") {
        skip({  });
        break $m792$match;
      }
      if ($m792.tag === "Some") {
        const value = $m792.value;
        const fn_id = def_at({ l: l, ctx: ctx, tag: $defs.tag_item, span: f.span });
        for (const n of $walk.nodes_in_block({ b: value, into_verify: false })) {
          const $m793 = n;
          $m793$match: {
            if ($m793.tag === "NStmt") {
              const s = $m793.s;
              const $m794 = s;
              $m794$match: {
                if ($m794.tag === "Assume") {
                  const verify_block = $m794.verify_block;
                  const span = $m794.span;
                  if (verify_block.tag === "Some") {
                    const [, l$246, out$246] = verify_of({ l: l, ctx: ctx, fn_id: fn_id, v: verify_or({ o: verify_block }), at: span, out: out });
                    l = l$246;
                    out = out$246;
                  }
                  break $m794$match;
                }
                if (true) {
                  skip({  });
                  break $m794$match;
                }
                $rt.unreachable();
              }
              break $m793$match;
            }
            if (true) {
              skip({  });
              break $m793$match;
            }
            $rt.unreachable();
          }
        }
        break $m792$match;
      }
      $rt.unreachable();
    }
  }
  return [$std_list.finish({ b: out }), l];
}

export function verify_or({ o }) {
  const $m795 = o;
  $m795$match: {
    if ($m795.tag === "Some") {
      const value = $m795.value;
      return value;
      break $m795$match;
    }
    if ($m795.tag === "None") {
      return { params: [], effects: [], body: { stmts: [], elided: false, span: { start: 0, end: 0 } }, span: { start: 0, end: 0 } };
      break $m795$match;
    }
    $rt.unreachable();
  }
}

export function verify_of({ l, ctx, fn_id, v, at, out }) {
  const node_key = key({ l: l, tag: $defs.tag_stmt, span: at });
  let site = { tag: "None" };
  for (const a of $claimcheck.assumes_of({ ctx: ctx, fn_def: fn_id })) {
    if (a.node === node_key && site.tag === "None") {
      site = { tag: "Some", value: a };
    }
  }
  const vd = def_at({ l: l, ctx: ctx, tag: $defs.tag_misc, span: v.span });
  if (site.tag === "None" || vd < 0) {
    return [undefined, l, out];
  }
  const a = site_or({ o: site });
  let params = $std_list.builder({  });
  for (const p of v.params) {
    const pd = def_at({ l: l, ctx: ctx, tag: $defs.tag_sig, span: p.span });
    if (pd >= 0) {
      const $m802 = $types.strip({ t: type_or({ o: $std_map.find({ d: ctx.decl_types, key: pd }) }) });
      $m802$match: {
        if ($m802.tag === "Capability") {
          const def = $m802.def;
          const [, params$247] = $std_list.push({ b: params, x: { name: p.name.text, capability: def, node: key({ l: l, tag: $defs.tag_sig, span: p.span }), at_span: p.span } });
          params = params$247;
          break $m802$match;
        }
        if (true) {
          skip({  });
          break $m802$match;
        }
        $rt.unreachable();
      }
    }
  }
  const pos = $loc.line_col({ tables: l.tables, file: l.file, offset: at.start });
  const vctx = { inout_names: [], ret: { tag: "Prim", name: "Bool" }, post: [], def: { tag: "Some", value: $context.get_def({ ctx: ctx, id: vd }) }, dicts: $std_map.dict({  }), result_expr: { tag: "None" }, measure: { tag: "None" } };
  l = { ...l, fn_ctx: { tag: "Some", value: vctx }, verifying: true };
  const [$r811, l$248] = assertion_block({ l: l, ctx: ctx, b: v.body });
  l = l$248;
  const body = $r811;
  l = { ...l, verifying: false, fn_ctx: { tag: "None" } };
  const owner = $context.qualified_name({ ctx: ctx, id: a.fn_def });
  const [, out$249] = $std_list.push({ b: out, x: { name: "verify$" + $std_int.to_text({ x: $std_list.built({ b: out }) }), key: a.key, claim: $context.qualified_name({ ctx: ctx, id: a.claim_def }), def: owner, at: owner + ":" + $std_int.to_text({ x: pos.line }) + ":" + $std_int.to_text({ x: pos.col }), params: $std_list.finish({ b: params }), body: body, early_return: block_has_try({ b: v.body }) } });
  out = out$249;
  return [undefined, l, out];
  return [undefined, l, out];
}

export function site_or({ o }) {
  const $m815 = o;
  $m815$match: {
    if ($m815.tag === "Some") {
      const value = $m815.value;
      return value;
      break $m815$match;
    }
    if ($m815.tag === "None") {
      return { fn_def: $rt.int.neg(1, $ob137), claim_def: $rt.int.neg(1, $ob138), justification: "", node: $rt.int.neg(1, $ob139), at_file: $rt.int.neg(1, $ob140), at_span: { start: 0, end: 0 }, verify_key: { tag: "None" }, verify_block: { tag: "None" }, key: "" };
      break $m815$match;
    }
    $rt.unreachable();
  }
}

export function block_has_try({ b }) {
  for (const s of b.stmts) {
    if (stmt_has_try({ s: s })) {
      return true;
    }
  }
  return false;
}

export function stmt_has_try({ s }) {
  const $m820 = s;
  $m820$match: {
    if ($m820.tag === "Let") {
      const value = $m820.value;
      return expr_has_try({ e: value });
      break $m820$match;
    }
    if ($m820.tag === "Var") {
      const value = $m820.value;
      return expr_has_try({ e: value });
      break $m820$match;
    }
    if ($m820.tag === "Assign") {
      const value = $m820.value;
      return expr_has_try({ e: value });
      break $m820$match;
    }
    if ($m820.tag === "Return") {
      const value = $m820.value;
      return expr_has_try({ e: value });
      break $m820$match;
    }
    if ($m820.tag === "If") {
      const cond = $m820.cond;
      const then_block = $m820.then_block;
      const else_block = $m820.else_block;
      if (expr_has_try({ e: cond }) || block_has_try({ b: then_block })) {
        return true;
      }
      const $m821 = else_block;
      $m821$match: {
        if ($m821.tag === "Some") {
          const value = $m821.value;
          return block_has_try({ b: value });
          break $m821$match;
        }
        if ($m821.tag === "None") {
          return false;
          break $m821$match;
        }
        $rt.unreachable();
      }
      break $m820$match;
    }
    if ($m820.tag === "Match") {
      const scrutinee = $m820.scrutinee;
      const arms = $m820.arms;
      if (expr_has_try({ e: scrutinee })) {
        return true;
      }
      for (const a of arms) {
        const $m822 = a.guard;
        $m822$match: {
          if ($m822.tag === "Some") {
            const value = $m822.value;
            if (expr_has_try({ e: value })) {
              return true;
            }
            break $m822$match;
          }
          if ($m822.tag === "None") {
            skip({  });
            break $m822$match;
          }
          $rt.unreachable();
        }
        const $m823 = a.body;
        $m823$match: {
          if ($m823.tag === "ArmBlock") {
            const block = $m823.block;
            if (block_has_try({ b: block })) {
              return true;
            }
            break $m823$match;
          }
          if ($m823.tag === "ArmStmt") {
            const stmt = $m823.stmt;
            if (stmt_has_try({ s: stmt })) {
              return true;
            }
            break $m823$match;
          }
          $rt.unreachable();
        }
      }
      return false;
      break $m820$match;
    }
    if ($m820.tag === "Loop") {
      const cond = $m820.cond;
      const clauses = $m820.clauses;
      const body = $m820.body;
      if (expr_has_try({ e: cond })) {
        return true;
      }
      for (const c of clauses) {
        if (expr_has_try({ e: c.expr })) {
          return true;
        }
      }
      return block_has_try({ b: body });
      break $m820$match;
    }
    if ($m820.tag === "For") {
      const domain = $m820.domain;
      const body = $m820.body;
      const $m824 = domain;
      $m824$match: {
        if ($m824.tag === "RangeDomain") {
          const lo = $m824.lo;
          const hi = $m824.hi;
          if (expr_has_try({ e: lo }) || expr_has_try({ e: hi })) {
            return true;
          }
          break $m824$match;
        }
        if ($m824.tag === "InDomain") {
          const expr = $m824.expr;
          if (expr_has_try({ e: expr })) {
            return true;
          }
          break $m824$match;
        }
        $rt.unreachable();
      }
      return block_has_try({ b: body });
      break $m820$match;
    }
    if ($m820.tag === "Assume") {
      return false;
      break $m820$match;
    }
    if ($m820.tag === "ExprStmt") {
      const expr = $m820.expr;
      return expr_has_try({ e: expr });
      break $m820$match;
    }
    $rt.unreachable();
  }
}

export function exprs_have_try({ es }) {
  for (const e of es) {
    if (expr_has_try({ e: e })) {
      return true;
    }
  }
  return false;
}

export function inits_have_try({ fs }) {
  for (const f of fs) {
    if (expr_has_try({ e: f.value })) {
      return true;
    }
  }
  return false;
}

export function args_have_try({ args }) {
  for (const a of args) {
    if (expr_has_try({ e: a.value })) {
      return true;
    }
  }
  return false;
}

export function expr_has_try({ e }) {
  const $m825 = e;
  $m825$match: {
    if ($m825.tag === "Try") {
      return true;
      break $m825$match;
    }
    if ($m825.tag === "Ctor") {
      const args = $m825.args;
      const fields = $m825.fields;
      const $m826 = args;
      $m826$match: {
        if ($m826.tag === "Some") {
          const value = $m826.value;
          if (args_have_try({ args: value })) {
            return true;
          }
          break $m826$match;
        }
        if ($m826.tag === "None") {
          skip({  });
          break $m826$match;
        }
        $rt.unreachable();
      }
      const $m827 = fields;
      $m827$match: {
        if ($m827.tag === "Some") {
          const value = $m827.value;
          return inits_have_try({ fs: value });
          break $m827$match;
        }
        if ($m827.tag === "None") {
          return false;
          break $m827$match;
        }
        $rt.unreachable();
      }
      break $m825$match;
    }
    if ($m825.tag === "RecordUpdate") {
      const base = $m825.base;
      const fields = $m825.fields;
      return expr_has_try({ e: base }) || inits_have_try({ fs: fields });
      break $m825$match;
    }
    if ($m825.tag === "ListLit") {
      const elems = $m825.elems;
      return exprs_have_try({ es: elems });
      break $m825$match;
    }
    if ($m825.tag === "Recover") {
      const body = $m825.body;
      return block_has_try({ b: body });
      break $m825$match;
    }
    if ($m825.tag === "Quantifier") {
      const domain = $m825.domain;
      const where_ = $m825.where_;
      const body = $m825.body;
      const $m828 = domain;
      $m828$match: {
        if ($m828.tag === "Some") {
          const value = $m828.value;
          const $m829 = value;
          $m829$match: {
            if ($m829.tag === "RangeDomain") {
              const lo = $m829.lo;
              const hi = $m829.hi;
              if (expr_has_try({ e: lo }) || expr_has_try({ e: hi })) {
                return true;
              }
              break $m829$match;
            }
            if ($m829.tag === "InDomain") {
              const expr = $m829.expr;
              if (expr_has_try({ e: expr })) {
                return true;
              }
              break $m829$match;
            }
            $rt.unreachable();
          }
          break $m828$match;
        }
        if ($m828.tag === "None") {
          skip({  });
          break $m828$match;
        }
        $rt.unreachable();
      }
      const $m830 = where_;
      $m830$match: {
        if ($m830.tag === "Some") {
          const value = $m830.value;
          if (expr_has_try({ e: value })) {
            return true;
          }
          break $m830$match;
        }
        if ($m830.tag === "None") {
          skip({  });
          break $m830$match;
        }
        $rt.unreachable();
      }
      return expr_has_try({ e: body });
      break $m825$match;
    }
    if ($m825.tag === "Closure") {
      return false;
      break $m825$match;
    }
    if ($m825.tag === "Fake") {
      const fields = $m825.fields;
      return inits_have_try({ fs: fields });
      break $m825$match;
    }
    if ($m825.tag === "FieldAccess") {
      const object = $m825.object;
      return expr_has_try({ e: object });
      break $m825$match;
    }
    if ($m825.tag === "Call") {
      const callee = $m825.callee;
      const args = $m825.args;
      return expr_has_try({ e: callee }) || args_have_try({ args: args });
      break $m825$match;
    }
    if ($m825.tag === "Unary") {
      const operand = $m825.operand;
      return expr_has_try({ e: operand });
      break $m825$match;
    }
    if ($m825.tag === "Binary") {
      const left = $m825.left;
      const right = $m825.right;
      return expr_has_try({ e: left }) || expr_has_try({ e: right });
      break $m825$match;
    }
    if ($m825.tag === "And") {
      const operands = $m825.operands;
      return exprs_have_try({ es: operands });
      break $m825$match;
    }
    if ($m825.tag === "Or") {
      const operands = $m825.operands;
      return exprs_have_try({ es: operands });
      break $m825$match;
    }
    if ($m825.tag === "Is") {
      const expr = $m825.expr;
      return expr_has_try({ e: expr });
      break $m825$match;
    }
    if (true) {
      return false;
      break $m825$match;
    }
    $rt.unreachable();
  }
}

