// Generated by onus from module lowerir. Do not edit.
import * as $rt from "/Users/jamesrandall/code/onus/packages/runtime/dist/index.js";
import * as $defs from "./defs.js";
import * as $parser from "./parser.js";
import * as $std_map from "./std/map.js";
import * as $std_int from "./std/int.js";
import * as $loc from "./loc.js";
import * as $obligations from "./obligations.js";
import * as $context from "./context.js";
import * as $std_list from "./std/list.js";
import * as $lower from "./lower.js";
import * as $types from "./types.js";
import * as $walk from "./walk.js";
import * as $std_text from "./std/text.js";
import * as $printer from "./printer.js";
import * as $json from "./json.js";
import * as $claimcheck from "./claimcheck.js";

const $ob1 = { kind: "overflow", text: "-1 within Int", at: "/Users/jamesrandall/code/onus/self/lowerir.onus:105:20", def: "or_neg" };
const $ob2 = { kind: "overflow", text: "-1 within Int", at: "/Users/jamesrandall/code/onus/self/lowerir.onus:168:20", def: "ref_def" };
const $ob3 = { kind: "overflow", text: "-1 within Int", at: "/Users/jamesrandall/code/onus/self/lowerir.onus:172:19", def: "ref_def" };
const $ob4 = { kind: "overflow", text: "l.temp + 1 within Int", at: "/Users/jamesrandall/code/onus/self/lowerir.onus:177:22", def: "tmp" };
const $ob5 = { kind: "overflow", text: "guard - 1 within Int", at: "/Users/jamesrandall/code/onus/self/lowerir.onus:258:13", def: "refinement_preds" };
const $ob6 = { kind: "overflow", text: "fuel - 1 within Int", at: "/Users/jamesrandall/code/onus/self/lowerir.onus:307:11", def: "type_slug" };
const $ob7 = { kind: "overflow", text: "fuel - 1 within Int", at: "/Users/jamesrandall/code/onus/self/lowerir.onus:312:11", def: "type_slug" };
const $ob8 = { kind: "overflow", text: "fuel - 1 within Int", at: "/Users/jamesrandall/code/onus/self/lowerir.onus:317:11", def: "type_slug" };
const $ob9 = { kind: "overflow", text: "fuel - 1 within Int", at: "/Users/jamesrandall/code/onus/self/lowerir.onus:322:11", def: "type_slug" };
const $ob10 = { kind: "overflow", text: "fuel - 1 within Int", at: "/Users/jamesrandall/code/onus/self/lowerir.onus:343:62", def: "args_slug" };
const $ob11 = { kind: "overflow", text: "i + 1 within Int", at: "/Users/jamesrandall/code/onus/self/lowerir.onus:408:9", def: "subst_of" };
const $ob12 = { kind: "overflow", text: "-1 within Int", at: "/Users/jamesrandall/code/onus/self/lowerir.onus:633:10", def: "iface_fn_named" };
const $ob13 = { kind: "overflow", text: "i + 1 within Int", at: "/Users/jamesrandall/code/onus/self/lowerir.onus:794:11", def: "fn_decl_sig" };
const $ob14 = { kind: "overflow", text: "-1 within Int", at: "/Users/jamesrandall/code/onus/self/lowerir.onus:837:26", def: "fn_decl_sig" };
const $ob15 = { kind: "overflow", text: "flows + 1 within Int", at: "/Users/jamesrandall/code/onus/self/lowerir.onus:1022:21", def: "entry_checks" };
const $ob16 = { kind: "overflow", text: "i + 1 within Int", at: "/Users/jamesrandall/code/onus/self/lowerir.onus:1058:9", def: "entry_checks" };
const $ob17 = { kind: "overflow", text: "n + 1 within Int", at: "/Users/jamesrandall/code/onus/self/lowerir.onus:1067:15", def: "entry_checks" };
const $ob18 = { kind: "overflow", text: "fuel - 1 within Int", at: "/Users/jamesrandall/code/onus/self/lowerir.onus:1111:33", def: "with_it" };
const $ob19 = { kind: "overflow", text: "fuel - 1 within Int", at: "/Users/jamesrandall/code/onus/self/lowerir.onus:1155:29", def: "block" };
const $ob20 = { kind: "overflow", text: "fuel - 1 within Int", at: "/Users/jamesrandall/code/onus/self/lowerir.onus:1172:45", def: "stmt" };
const $ob21 = { kind: "overflow", text: "fuel - 1 within Int", at: "/Users/jamesrandall/code/onus/self/lowerir.onus:1189:11", def: "stmt_inner" };
const $ob22 = { kind: "overflow", text: "fuel - 1 within Int", at: "/Users/jamesrandall/code/onus/self/lowerir.onus:1198:11", def: "stmt_inner" };
const $ob23 = { kind: "overflow", text: "fuel - 1 within Int", at: "/Users/jamesrandall/code/onus/self/lowerir.onus:1218:35", def: "stmt_inner" };
const $ob24 = { kind: "overflow", text: "fuel - 1 within Int", at: "/Users/jamesrandall/code/onus/self/lowerir.onus:1223:11", def: "stmt_inner" };
const $ob25 = { kind: "overflow", text: "fuel - 1 within Int", at: "/Users/jamesrandall/code/onus/self/lowerir.onus:1230:33", def: "stmt_inner" };
const $ob26 = { kind: "overflow", text: "fuel - 1 within Int", at: "/Users/jamesrandall/code/onus/self/lowerir.onus:1231:46", def: "stmt_inner" };
const $ob27 = { kind: "overflow", text: "fuel - 1 within Int", at: "/Users/jamesrandall/code/onus/self/lowerir.onus:1234:56", def: "stmt_inner" };
const $ob28 = { kind: "overflow", text: "fuel - 1 within Int", at: "/Users/jamesrandall/code/onus/self/lowerir.onus:1239:11", def: "stmt_inner" };
const $ob29 = { kind: "overflow", text: "fuel - 1 within Int", at: "/Users/jamesrandall/code/onus/self/lowerir.onus:1246:11", def: "stmt_inner" };
const $ob30 = { kind: "overflow", text: "fuel - 1 within Int", at: "/Users/jamesrandall/code/onus/self/lowerir.onus:1255:11", def: "stmt_inner" };
const $ob31 = { kind: "overflow", text: "fuel - 1 within Int", at: "/Users/jamesrandall/code/onus/self/lowerir.onus:1266:36", def: "stmt_inner" };
const $ob32 = { kind: "overflow", text: "fuel - 1 within Int", at: "/Users/jamesrandall/code/onus/self/lowerir.onus:1308:33", def: "let_stmt" };
const $ob33 = { kind: "overflow", text: "fuel - 1 within Int", at: "/Users/jamesrandall/code/onus/self/lowerir.onus:1331:35", def: "checked_flow" };
const $ob34 = { kind: "overflow", text: "fuel - 1 within Int", at: "/Users/jamesrandall/code/onus/self/lowerir.onus:1350:29", def: "checked_flow" };
const $ob35 = { kind: "overflow", text: "-1 within Int", at: "/Users/jamesrandall/code/onus/self/lowerir.onus:1362:9", def: "ob_or" };
const $ob36 = { kind: "overflow", text: "fuel - 1 within Int", at: "/Users/jamesrandall/code/onus/self/lowerir.onus:1393:33", def: "return_stmt" };
const $ob37 = { kind: "overflow", text: "fuel - 1 within Int", at: "/Users/jamesrandall/code/onus/self/lowerir.onus:1396:41", def: "return_stmt" };
const $ob38 = { kind: "overflow", text: "fuel - 1 within Int", at: "/Users/jamesrandall/code/onus/self/lowerir.onus:1420:32", def: "return_stmt" };
const $ob39 = { kind: "overflow", text: "fuel - 1 within Int", at: "/Users/jamesrandall/code/onus/self/lowerir.onus:1446:35", def: "match_stmt" };
const $ob40 = { kind: "overflow", text: "fuel - 1 within Int", at: "/Users/jamesrandall/code/onus/self/lowerir.onus:1451:13", def: "match_stmt" };
const $ob41 = { kind: "overflow", text: "fuel - 1 within Int", at: "/Users/jamesrandall/code/onus/self/lowerir.onus:1468:25", def: "match_stmt" };
const $ob42 = { kind: "overflow", text: "fuel - 1 within Int", at: "/Users/jamesrandall/code/onus/self/lowerir.onus:1473:52", def: "match_stmt" };
const $ob43 = { kind: "overflow", text: "fuel - 1 within Int", at: "/Users/jamesrandall/code/onus/self/lowerir.onus:1474:49", def: "match_stmt" };
const $ob44 = { kind: "overflow", text: "fuel - 1 within Int", at: "/Users/jamesrandall/code/onus/self/lowerir.onus:1494:22", def: "block_ir" };
const $ob45 = { kind: "overflow", text: "fuel - 1 within Int", at: "/Users/jamesrandall/code/onus/self/lowerir.onus:1508:21", def: "stmt_ir" };
const $ob46 = { kind: "overflow", text: "fuel - 1 within Int", at: "/Users/jamesrandall/code/onus/self/lowerir.onus:1530:25", def: "pattern_test_of" };
const $ob47 = { kind: "overflow", text: "i + 1 within Int", at: "/Users/jamesrandall/code/onus/self/lowerir.onus:1591:17", def: "pattern_bindings" };
const $ob48 = { kind: "overflow", text: "i + 1 within Int", at: "/Users/jamesrandall/code/onus/self/lowerir.onus:1593:40", def: "pattern_bindings" };
const $ob49 = { kind: "overflow", text: "fuel - 1 within Int", at: "/Users/jamesrandall/code/onus/self/lowerir.onus:1656:30", def: "loop_stmt" };
const $ob50 = { kind: "overflow", text: "fuel - 1 within Int", at: "/Users/jamesrandall/code/onus/self/lowerir.onus:1664:31", def: "loop_stmt" };
const $ob51 = { kind: "overflow", text: "fuel - 1 within Int", at: "/Users/jamesrandall/code/onus/self/lowerir.onus:1683:13", def: "loop_stmt" };
const $ob52 = { kind: "overflow", text: "fuel - 1 within Int", at: "/Users/jamesrandall/code/onus/self/lowerir.onus:1702:36", def: "loop_stmt" };
const $ob53 = { kind: "overflow", text: "fuel - 1 within Int", at: "/Users/jamesrandall/code/onus/self/lowerir.onus:1713:17", def: "loop_stmt" };
const $ob54 = { kind: "overflow", text: "fuel - 1 within Int", at: "/Users/jamesrandall/code/onus/self/lowerir.onus:1731:13", def: "loop_stmt" };
const $ob55 = { kind: "overflow", text: "fuel - 1 within Int", at: "/Users/jamesrandall/code/onus/self/lowerir.onus:1777:37", def: "for_stmt" };
const $ob56 = { kind: "overflow", text: "fuel - 1 within Int", at: "/Users/jamesrandall/code/onus/self/lowerir.onus:1778:37", def: "for_stmt" };
const $ob57 = { kind: "overflow", text: "fuel - 1 within Int", at: "/Users/jamesrandall/code/onus/self/lowerir.onus:1792:27", def: "for_stmt" };
const $ob58 = { kind: "overflow", text: "fuel - 1 within Int", at: "/Users/jamesrandall/code/onus/self/lowerir.onus:1797:39", def: "for_stmt" };
const $ob59 = { kind: "overflow", text: "fuel - 1 within Int", at: "/Users/jamesrandall/code/onus/self/lowerir.onus:1810:45", def: "for_stmt" };
const $ob60 = { kind: "overflow", text: "fuel - 1 within Int", at: "/Users/jamesrandall/code/onus/self/lowerir.onus:1820:38", def: "for_stmt" };
const $ob61 = { kind: "overflow", text: "fuel - 1 within Int", at: "/Users/jamesrandall/code/onus/self/lowerir.onus:1839:21", def: "expr_of" };
const $ob62 = { kind: "overflow", text: "fuel - 1 within Int", at: "/Users/jamesrandall/code/onus/self/lowerir.onus:1881:47", def: "expr" };
const $ob63 = { kind: "overflow", text: "fuel - 1 within Int", at: "/Users/jamesrandall/code/onus/self/lowerir.onus:1882:61", def: "expr" };
const $ob64 = { kind: "overflow", text: "fuel - 1 within Int", at: "/Users/jamesrandall/code/onus/self/lowerir.onus:1886:45", def: "expr" };
const $ob65 = { kind: "overflow", text: "fuel - 1 within Int", at: "/Users/jamesrandall/code/onus/self/lowerir.onus:1890:47", def: "expr" };
const $ob66 = { kind: "overflow", text: "fuel - 1 within Int", at: "/Users/jamesrandall/code/onus/self/lowerir.onus:1891:52", def: "expr" };
const $ob67 = { kind: "overflow", text: "fuel - 1 within Int", at: "/Users/jamesrandall/code/onus/self/lowerir.onus:1893:11", def: "expr" };
const $ob68 = { kind: "overflow", text: "fuel - 1 within Int", at: "/Users/jamesrandall/code/onus/self/lowerir.onus:1898:56", def: "expr" };
const $ob69 = { kind: "overflow", text: "fuel - 1 within Int", at: "/Users/jamesrandall/code/onus/self/lowerir.onus:1899:49", def: "expr" };
const $ob70 = { kind: "overflow", text: "fuel - 1 within Int", at: "/Users/jamesrandall/code/onus/self/lowerir.onus:1901:59", def: "expr" };
const $ob71 = { kind: "overflow", text: "fuel - 1 within Int", at: "/Users/jamesrandall/code/onus/self/lowerir.onus:1903:11", def: "expr" };
const $ob72 = { kind: "overflow", text: "fuel - 1 within Int", at: "/Users/jamesrandall/code/onus/self/lowerir.onus:1909:46", def: "expr" };
const $ob73 = { kind: "overflow", text: "fuel - 1 within Int", at: "/Users/jamesrandall/code/onus/self/lowerir.onus:1910:51", def: "expr" };
const $ob74 = { kind: "overflow", text: "fuel - 1 within Int", at: "/Users/jamesrandall/code/onus/self/lowerir.onus:1912:27", def: "expr" };
const $ob75 = { kind: "overflow", text: "fuel - 1 within Int", at: "/Users/jamesrandall/code/onus/self/lowerir.onus:1915:27", def: "expr" };
const $ob76 = { kind: "overflow", text: "fuel - 1 within Int", at: "/Users/jamesrandall/code/onus/self/lowerir.onus:1918:42", def: "expr" };
const $ob77 = { kind: "overflow", text: "fuel - 1 within Int", at: "/Users/jamesrandall/code/onus/self/lowerir.onus:1920:13", def: "expr" };
const $ob78 = { kind: "overflow", text: "fuel - 1 within Int", at: "/Users/jamesrandall/code/onus/self/lowerir.onus:1945:43", def: "exprs" };
const $ob79 = { kind: "overflow", text: "fuel - 1 within Int", at: "/Users/jamesrandall/code/onus/self/lowerir.onus:2048:33", def: "ctor" };
const $ob80 = { kind: "overflow", text: "fuel - 1 within Int", at: "/Users/jamesrandall/code/onus/self/lowerir.onus:2057:17", def: "ctor" };
const $ob81 = { kind: "overflow", text: "fuel - 1 within Int", at: "/Users/jamesrandall/code/onus/self/lowerir.onus:2098:39", def: "record_update" };
const $ob82 = { kind: "overflow", text: "fuel - 1 within Int", at: "/Users/jamesrandall/code/onus/self/lowerir.onus:2111:29", def: "record_update" };
const $ob83 = { kind: "overflow", text: "fuel - 1 within Int", at: "/Users/jamesrandall/code/onus/self/lowerir.onus:2124:13", def: "record_update" };
const $ob84 = { kind: "overflow", text: "fuel - 1 within Int", at: "/Users/jamesrandall/code/onus/self/lowerir.onus:2175:37", def: "field_checks" };
const $ob85 = { kind: "overflow", text: "-1 within Int", at: "/Users/jamesrandall/code/onus/self/lowerir.onus:2194:43", def: "field_or" };
const $ob86 = { kind: "overflow", text: "fuel - 1 within Int", at: "/Users/jamesrandall/code/onus/self/lowerir.onus:2210:42", def: "try_expr" };
const $ob87 = { kind: "overflow", text: "fuel - 1 within Int", at: "/Users/jamesrandall/code/onus/self/lowerir.onus:2239:15", def: "try_expr" };
const $ob88 = { kind: "overflow", text: "n - 1 within Int", at: "/Users/jamesrandall/code/onus/self/lowerir.onus:2285:15", def: "recover_expr" };
const $ob89 = { kind: "overflow", text: "fuel - 1 within Int", at: "/Users/jamesrandall/code/onus/self/lowerir.onus:2293:31", def: "recover_expr" };
const $ob90 = { kind: "overflow", text: "fuel - 1 within Int", at: "/Users/jamesrandall/code/onus/self/lowerir.onus:2296:41", def: "recover_expr" };
const $ob91 = { kind: "overflow", text: "i + 1 within Int", at: "/Users/jamesrandall/code/onus/self/lowerir.onus:2297:11", def: "recover_expr" };
const $ob92 = { kind: "overflow", text: "fuel - 1 within Int", at: "/Users/jamesrandall/code/onus/self/lowerir.onus:2329:24", def: "quantifier" };
const $ob93 = { kind: "overflow", text: "fuel - 1 within Int", at: "/Users/jamesrandall/code/onus/self/lowerir.onus:2330:24", def: "quantifier" };
const $ob94 = { kind: "overflow", text: "fuel - 1 within Int", at: "/Users/jamesrandall/code/onus/self/lowerir.onus:2334:43", def: "quantifier" };
const $ob95 = { kind: "overflow", text: "fuel - 1 within Int", at: "/Users/jamesrandall/code/onus/self/lowerir.onus:2345:25", def: "quantifier" };
const $ob96 = { kind: "overflow", text: "fuel - 1 within Int", at: "/Users/jamesrandall/code/onus/self/lowerir.onus:2354:24", def: "quantifier" };
const $ob97 = { kind: "overflow", text: "fuel - 1 within Int", at: "/Users/jamesrandall/code/onus/self/lowerir.onus:2428:19", def: "closure" };
const $ob98 = { kind: "overflow", text: "i + 1 within Int", at: "/Users/jamesrandall/code/onus/self/lowerir.onus:2454:11", def: "closure" };
const $ob99 = { kind: "overflow", text: "fuel - 1 within Int", at: "/Users/jamesrandall/code/onus/self/lowerir.onus:2456:46", def: "closure" };
const $ob100 = { kind: "overflow", text: "fuel - 1 within Int", at: "/Users/jamesrandall/code/onus/self/lowerir.onus:2496:29", def: "fake_expr" };
const $ob101 = { kind: "overflow", text: "fuel - 1 within Int", at: "/Users/jamesrandall/code/onus/self/lowerir.onus:2544:26", def: "field_access" };
const $ob102 = { kind: "overflow", text: "-1 within Int", at: "/Users/jamesrandall/code/onus/self/lowerir.onus:2579:26", def: "call" };
const $ob103 = { kind: "overflow", text: "-1 within Int", at: "/Users/jamesrandall/code/onus/self/lowerir.onus:2580:26", def: "call" };
const $ob104 = { kind: "overflow", text: "fuel - 1 within Int", at: "/Users/jamesrandall/code/onus/self/lowerir.onus:2599:17", def: "call" };
const $ob105 = { kind: "overflow", text: "fuel - 1 within Int", at: "/Users/jamesrandall/code/onus/self/lowerir.onus:2613:41", def: "call" };
const $ob106 = { kind: "overflow", text: "fuel - 1 within Int", at: "/Users/jamesrandall/code/onus/self/lowerir.onus:2625:23", def: "call" };
const $ob107 = { kind: "overflow", text: "i + 1 within Int", at: "/Users/jamesrandall/code/onus/self/lowerir.onus:2736:9", def: "named_call" };
const $ob108 = { kind: "overflow", text: "fuel - 1 within Int", at: "/Users/jamesrandall/code/onus/self/lowerir.onus:2745:21", def: "named_call" };
const $ob109 = { kind: "overflow", text: "fuel - 1 within Int", at: "/Users/jamesrandall/code/onus/self/lowerir.onus:2753:11", def: "named_call" };
const $ob110 = { kind: "overflow", text: "fuel - 1 within Int", at: "/Users/jamesrandall/code/onus/self/lowerir.onus:2762:36", def: "named_call" };
const $ob111 = { kind: "overflow", text: "i + 1 within Int", at: "/Users/jamesrandall/code/onus/self/lowerir.onus:2852:9", def: "decreases_check" };
const $ob112 = { kind: "overflow", text: "fuel - 1 within Int", at: "/Users/jamesrandall/code/onus/self/lowerir.onus:2858:11", def: "decreases_check" };
const $ob113 = { kind: "overflow", text: "-1 within Int", at: "/Users/jamesrandall/code/onus/self/lowerir.onus:2905:20", def: "select_decoder" };
const $ob114 = { kind: "overflow", text: "i + 1 within Int", at: "/Users/jamesrandall/code/onus/self/lowerir.onus:2911:9", def: "select_decoder" };
const $ob115 = { kind: "overflow", text: "fuel - 1 within Int", at: "/Users/jamesrandall/code/onus/self/lowerir.onus:2952:41", def: "select_decoder" };
const $ob116 = { kind: "overflow", text: "n - 1 within Int", at: "/Users/jamesrandall/code/onus/self/lowerir.onus:3076:34", def: "dispatch" };
const $ob117 = { kind: "overflow", text: "fuel - 1 within Int", at: "/Users/jamesrandall/code/onus/self/lowerir.onus:3121:33", def: "unary" };
const $ob118 = { kind: "overflow", text: "-1 within Int", at: "/Users/jamesrandall/code/onus/self/lowerir.onus:3159:9", def: "def_or" };
const $ob119 = { kind: "overflow", text: "-1 within Int", at: "/Users/jamesrandall/code/onus/self/lowerir.onus:3163:10", def: "def_or" };
const $ob120 = { kind: "overflow", text: "-1 within Int", at: "/Users/jamesrandall/code/onus/self/lowerir.onus:3164:11", def: "def_or" };
const $ob121 = { kind: "overflow", text: "-1 within Int", at: "/Users/jamesrandall/code/onus/self/lowerir.onus:3165:10", def: "def_or" };
const $ob122 = { kind: "overflow", text: "-1 within Int", at: "/Users/jamesrandall/code/onus/self/lowerir.onus:3171:12", def: "def_or" };
const $ob123 = { kind: "overflow", text: "fuel - 1 within Int", at: "/Users/jamesrandall/code/onus/self/lowerir.onus:3191:39", def: "binary" };
const $ob124 = { kind: "overflow", text: "fuel - 1 within Int", at: "/Users/jamesrandall/code/onus/self/lowerir.onus:3192:40", def: "binary" };
const $ob125 = { kind: "overflow", text: "fuel - 1 within Int", at: "/Users/jamesrandall/code/onus/self/lowerir.onus:3430:32", def: "generator" };
const $ob126 = { kind: "overflow", text: "fuel - 1 within Int", at: "/Users/jamesrandall/code/onus/self/lowerir.onus:3472:31", def: "base_generator" };
const $ob127 = { kind: "overflow", text: "fuel - 1 within Int", at: "/Users/jamesrandall/code/onus/self/lowerir.onus:3479:30", def: "base_generator" };
const $ob128 = { kind: "overflow", text: "fuel - 1 within Int", at: "/Users/jamesrandall/code/onus/self/lowerir.onus:3488:36", def: "base_generator" };
const $ob129 = { kind: "overflow", text: "fuel - 1 within Int", at: "/Users/jamesrandall/code/onus/self/lowerir.onus:3514:30", def: "gen_fields" };
const $ob130 = { kind: "overflow", text: "-1 within Int", at: "/Users/jamesrandall/code/onus/self/lowerir.onus:3647:13", def: "site_or" };
const $ob131 = { kind: "overflow", text: "-1 within Int", at: "/Users/jamesrandall/code/onus/self/lowerir.onus:3648:16", def: "site_or" };
const $ob132 = { kind: "overflow", text: "-1 within Int", at: "/Users/jamesrandall/code/onus/self/lowerir.onus:3650:11", def: "site_or" };
const $ob133 = { kind: "overflow", text: "-1 within Int", at: "/Users/jamesrandall/code/onus/self/lowerir.onus:3651:14", def: "site_or" };
export const max_depth = 100000;

export function skip($args) {
  return undefined;
}

export function or_neg({ o }) {
  const $m1 = o;
  $m1$match: {
    if ($m1.tag === "Some") {
      const value = $m1.value;
      return value;
      break $m1$match;
    }
    if ($m1.tag === "None") {
      return $rt.int.neg(1, $ob1);
      break $m1$match;
    }
    $rt.unreachable();
  }
}

export function type_or({ o }) {
  const $m2 = o;
  $m2$match: {
    if ($m2.tag === "Some") {
      const value = $m2.value;
      return value;
      break $m2$match;
    }
    if ($m2.tag === "None") {
      return { tag: "ErrorT" };
      break $m2$match;
    }
    $rt.unreachable();
  }
}

export function fields_or({ o }) {
  const $m4 = o;
  $m4$match: {
    if ($m4.tag === "Some") {
      const value = $m4.value;
      return value;
      break $m4$match;
    }
    if ($m4.tag === "None") {
      return [];
      break $m4$match;
    }
    $rt.unreachable();
  }
}

export function ids_or({ o }) {
  const $m5 = o;
  $m5$match: {
    if ($m5.tag === "Some") {
      const value = $m5.value;
      return value;
      break $m5$match;
    }
    if ($m5.tag === "None") {
      return [];
      break $m5$match;
    }
    $rt.unreachable();
  }
}

export function args_or({ o }) {
  const $m6 = o;
  $m6$match: {
    if ($m6.tag === "Some") {
      const value = $m6.value;
      return value;
      break $m6$match;
    }
    if ($m6.tag === "None") {
      return [];
      break $m6$match;
    }
    $rt.unreachable();
  }
}

export function tparams_or({ o }) {
  const $m7 = o;
  $m7$match: {
    if ($m7.tag === "Some") {
      const value = $m7.value;
      return value;
      break $m7$match;
    }
    if ($m7.tag === "None") {
      return [];
      break $m7$match;
    }
    $rt.unreachable();
  }
}

export function expr_or_unit({ o }) {
  const $m8 = o;
  $m8$match: {
    if ($m8.tag === "Some") {
      const value = $m8.value;
      return value;
      break $m8$match;
    }
    if ($m8.tag === "None") {
      return { tag: "UnitE" };
      break $m8$match;
    }
    $rt.unreachable();
  }
}

export function text_or({ o, dflt }) {
  const $m10 = o;
  $m10$match: {
    if ($m10.tag === "Some") {
      const value = $m10.value;
      return value;
      break $m10$match;
    }
    if ($m10.tag === "None") {
      return dflt;
      break $m10$match;
    }
    $rt.unreachable();
  }
}

export function key({ l, tag, span }) {
  return $defs.node_key({ file: l.file, tag: tag, span: span });
}

export function expr_key({ l, e }) {
  return key({ l: l, tag: $defs.tag_expr, span: $parser.span_of_expr({ e: e }) });
}

export function type_of_expr({ l, ctx, e }) {
  return type_or({ o: $std_map.find({ d: ctx.expr_types, key: expr_key({ l: l, e: e }) }) });
}

export function def_at({ l, ctx, tag, span }) {
  return or_neg({ o: $std_map.find({ d: ctx.def_of, key: key({ l: l, tag: tag, span: span }) }) });
}

export function ref_def({ ctx, k }) {
  const $m11 = $std_map.find({ d: ctx.refs, key: k });
  $m11$match: {
    if ($m11.tag === "None") {
      return $rt.int.neg(1, $ob2);
      break $m11$match;
    }
    if ($m11.tag === "Some") {
      const value = $m11.value;
      const $m12 = value;
      $m12$match: {
        if ($m12.tag === "DefRes") {
          const def = $m12.def;
          return def;
          break $m12$match;
        }
        if (true) {
          return $rt.int.neg(1, $ob3);
          break $m12$match;
        }
        $rt.unreachable();
      }
      break $m11$match;
    }
    $rt.unreachable();
  }
}

export function tmp({ l, prefix }) {
  l = { ...l, temp: $rt.int.add(l.temp, 1, $ob4) };
  return ["$" + prefix + $std_int.to_text({ x: l.temp }), l];
}

export function where_text({ l, ctx, file, span }) {
  return $loc.location_text({ ctx: ctx, tables: l.tables, file: file, span: span });
}

export function ob_ref({ l, ctx, kind, text, file, span, def }) {
  return { kind: kind, text: text, at: where_text({ l: l, ctx: ctx, file: file, span: span }), def: def };
}

export function ob_ref_of({ l, ctx, o }) {
  return ob_ref({ l: l, ctx: ctx, kind: $obligations.kind_text({ k: o.kind }), text: o.text, file: o.at_file, span: o.at_span, def: $context.get_def({ ctx: ctx, id: o.def }).name });
}

export function obligations_at({ ctx, k }) {
  let out = $std_list.builder({  });
  for (const id of ids_or({ o: $std_map.find({ d: ctx.obligations_at, key: k }) })) {
    const [, out$1] = $std_list.push({ b: out, x: $context.get_obligation({ ctx: ctx, id: id }) });
    out = out$1;
  }
  return $std_list.finish({ b: out });
}

export function same_kind({ a, b }) {
  return $obligations.kind_text({ k: a }) === $obligations.kind_text({ k: b });
}

export function find_ob({ ctx, k, kind }) {
  for (const o of obligations_at({ ctx: ctx, k: k })) {
    if (same_kind({ a: o.kind, b: kind })) {
      return { tag: "Some", value: o };
    }
  }
  return { tag: "None" };
}

export function is_proved({ o }) {
  return o.status.tag === "Proved";
}

export function is_checked({ o }) {
  return o.status.tag === "Checked";
}

export function refinement_preds({ ctx, t }) {
  let out = $std_list.builder({  });
  let cur = t;
  let guard = 1000;
  while (cur.tag === "Refined" && guard > 0) {
    guard = $rt.int.sub(guard, 1, $ob5);
    const $m18 = cur;
    $m18$match: {
      if ($m18.tag === "Refined") {
        const base = $m18.base;
        const pred = $m18.pred;
        const $m19 = $std_map.find({ d: ctx.where_exprs, key: pred });
        $m19$match: {
          if ($m19.tag === "Some") {
            const value = $m19.value;
            const [, out$2] = $std_list.push({ b: out, x: { e: value, file: $lower.pred_file({ k: pred }) } });
            out = out$2;
            break $m19$match;
          }
          if ($m19.tag === "None") {
            skip({  });
            break $m19$match;
          }
          $rt.unreachable();
        }
        cur = base;
        break $m18$match;
      }
      if (true) {
        guard = 0;
        break $m18$match;
      }
      $rt.unreachable();
    }
  }
  return $std_list.finish({ b: out });
}

export function is_option({ ctx, t }) {
  const s = $types.strip({ t: t });
  if (s.tag === "UnionT") {
    return $context.qualified_name({ ctx: ctx, id: $types.def_of({ t: s }) }) === "std.option.Option";
  }
  return false;
}

export function is_unit({ t }) {
  const s = $types.strip({ t: t });
  return s.tag === "Prim" && $types.prim_name({ t: s }) === "Unit";
}

export function is_int_like({ t }) {
  const s = $types.strip({ t: t });
  if (!(s.tag === "Prim")) {
    return false;
  }
  const n = $types.prim_name({ t: s });
  return n === "Int" || n === "Duration";
}

export function type_slug({ fuel, ctx, t }) {
  if (fuel === 0) {
    return "X";
  }
  const s = $types.strip({ t: t });
  const $m21 = s;
  $m21$match: {
    if ($m21.tag === "Prim") {
      const name = $m21.name;
      return name;
      break $m21$match;
    }
    if ($m21.tag === "RecordT") {
      const def = $m21.def;
      const args = $m21.args;
      return $context.get_def({ ctx: ctx, id: def }).name + args_slug({ fuel: $rt.int.sub(fuel, 1, $ob6), ctx: ctx, args: args });
      break $m21$match;
    }
    if ($m21.tag === "UnionT") {
      const def = $m21.def;
      const args = $m21.args;
      return $context.get_def({ ctx: ctx, id: def }).name + args_slug({ fuel: $rt.int.sub(fuel, 1, $ob7), ctx: ctx, args: args });
      break $m21$match;
    }
    if ($m21.tag === "Opaque") {
      const def = $m21.def;
      const args = $m21.args;
      return $context.get_def({ ctx: ctx, id: def }).name + args_slug({ fuel: $rt.int.sub(fuel, 1, $ob8), ctx: ctx, args: args });
      break $m21$match;
    }
    if ($m21.tag === "Capability") {
      const def = $m21.def;
      const args = $m21.args;
      return $context.get_def({ ctx: ctx, id: def }).name + args_slug({ fuel: $rt.int.sub(fuel, 1, $ob9), ctx: ctx, args: args });
      break $m21$match;
    }
    if ($m21.tag === "ParamT") {
      const def = $m21.def;
      return $context.get_def({ ctx: ctx, id: def }).name;
      break $m21$match;
    }
    if (true) {
      return "X";
      break $m21$match;
    }
    $rt.unreachable();
  }
}

export function args_slug({ fuel, ctx, args }) {
  if (fuel === 0) {
    return "";
  }
  let out = "";
  for (const a of args) {
    const $m22 = a;
    $m22$match: {
      if ($m22.tag === "TypeA") {
        const ty = $m22.ty;
        out = out + "_" + type_slug({ fuel: $rt.int.sub(fuel, 1, $ob10), ctx: ctx, t: ty });
        break $m22$match;
      }
      if (true) {
        skip({  });
        break $m22$match;
      }
      $rt.unreachable();
    }
  }
  return out;
}

export function impl_target({ ctx, impl_def }) {
  for (const iface of $std_map.keys({ d: ctx.impls })) {
    const $m23 = $std_map.find({ d: ctx.impls, key: iface });
    $m23$match: {
      if ($m23.tag === "Some") {
        const value = $m23.value;
        for (const im of value) {
          if (im.def === impl_def) {
            return im.target;
          }
        }
        break $m23$match;
      }
      if ($m23.tag === "None") {
        skip({  });
        break $m23$match;
      }
      $rt.unreachable();
    }
  }
  return { tag: "ErrorT" };
}

export function impl_fn_name({ ctx, def }) {
  const parent = or_neg({ o: def.parent });
  if (parent < 0) {
    return def.name;
  }
  const im = $context.get_def({ ctx: ctx, id: parent });
  return im.name + "$" + type_slug({ fuel: 1000, ctx: ctx, t: impl_target({ ctx: ctx, impl_def: parent }) }) + "$" + def.name;
}

export function emitted_name({ ctx, def }) {
  const parent = or_neg({ o: def.parent });
  if (parent >= 0 && $rt.eq($context.get_def({ ctx: ctx, id: parent }).kind, { tag: "Impl" })) {
    return impl_fn_name({ ctx: ctx, def: def });
  }
  return def.name;
}

export function impl_dict_name({ ctx, iface, target }) {
  return iface.name + "$" + type_slug({ fuel: 1000, ctx: ctx, t: target });
}

export function subst_of({ ctx, ty }) {
  const s = $types.strip({ t: ty });
  let subst = $std_map.dict({  });
  if (!(s.tag === "RecordT" || s.tag === "UnionT" || s.tag === "Opaque")) {
    return subst;
  }
  const args = $types.args_of({ t: s });
  let i = 0;
  for (const p of tparams_or({ o: $std_map.find({ d: ctx.type_params, key: $types.def_of({ t: s }) }) })) {
    if (i < $std_list.len({ xs: args })) {
      const [, subst$3] = $std_map.set({ d: subst, key: $types.tparam_def({ p: p }), value: $std_list.get({ xs: args, i: i }) });
      subst = subst$3;
    }
    i = $rt.int.add(i, 1, $ob11);
  }
  return subst;
}

export function type_param_defs({ ctx, owner }) {
  let out = $std_list.builder({  });
  for (const p of tparams_or({ o: $std_map.find({ d: ctx.type_params, key: owner }) })) {
    const $m26 = p;
    $m26$match: {
      if ($m26.tag === "TypeP") {
        const def = $m26.def;
        const [, out$4] = $std_list.push({ b: out, x: def });
        out = out$4;
        break $m26$match;
      }
      if (true) {
        skip({  });
        break $m26$match;
      }
      $rt.unreachable();
    }
  }
  return $std_list.finish({ b: out });
}

export function field_list({ ctx, owner }) {
  let out = $std_list.builder({  });
  for (const f of fields_or({ o: $std_map.find({ d: ctx.fields, key: owner }) })) {
    const [, out$5] = $std_list.push({ b: out, x: { name: f.name, ty: f.ty } });
    out = out$5;
  }
  return $std_list.finish({ b: out });
}

export function find_field({ fs, name }) {
  for (const f of fs) {
    if (f.name === name) {
      return { tag: "Some", value: f };
    }
  }
  return { tag: "None" };
}

export function begin_collect({ l }) {
  const saved = l.pre;
  l = { ...l, pre: [] };
  return [saved, l];
}

export function end_collect({ l, saved, out }) {
  const result_stmts = [...l.pre, ...out];
  l = { ...l, pre: saved };
  return [result_stmts, l];
}

export function push_pre({ l, s }) {
  l = { ...l, pre: $std_list.append({ xs: l.pre, x: s }) };
  return [undefined, l];
  return [undefined, l];
}

export function lower_module({ ctx, m, tables, opts }) {
  let l = { file: m.file, m: m, tables: tables, fn_ctx: { tag: "None" }, subst: $std_map.dict({  }), it_expr: { tag: "None" }, field_object: { tag: "None" }, temp: 0, pre: [], impl_fns: $std_map.dict({  }), verifying: false, opts: opts };
  let items = $std_list.builder({  });
  let main = { tag: "None" };
  for (const item of m.tree.items) {
    const [$r39, l$6] = item_ir({ l: l, ctx: ctx, item: item });
    l = l$6;
    const $m38 = $r39;
    $m38$match: {
      if ($m38.tag === "Some") {
        const value = $m38.value;
        const [, items$7] = $std_list.push({ b: items, x: value });
        items = items$7;
        break $m38$match;
      }
      if ($m38.tag === "None") {
        skip({  });
        break $m38$match;
      }
      $rt.unreachable();
    }
    const $m40 = item;
    $m40$match: {
      if ($m40.tag === "FnItem") {
        const decl = $m40.decl;
        if (decl.name.text === "main" && decl.vis.is_pub) {
          main = main_spec({ l: l, ctx: ctx, f: decl });
        }
        break $m40$match;
      }
      if (true) {
        skip({  });
        break $m40$match;
      }
      $rt.unreachable();
    }
  }
  const [$r41, l$8] = tests_of({ l: l, ctx: ctx });
  l = l$8;
  const tests = $r41;
  let verifies = [];
  if (opts.verify_all) {
    const [$r42, l$9] = verifies_of({ l: l, ctx: ctx });
    l = l$9;
    verifies = $r42;
  }
  return { mod: m, items: $std_list.finish({ b: items }), tests: tests, verifies: verifies, main: main };
}

export function item_ir({ l, ctx, item }) {
  const $m44 = item;
  $m44$match: {
    if ($m44.tag === "FnItem") {
      const decl = $m44.decl;
      const [$r47, l$10] = fn_decl({ l: l, ctx: ctx, f: decl, impl_name: { tag: "None" } });
      l = l$10;
      const $m45 = $r47;
      $m45$match: {
        if ($m45.tag === "Some") {
          const value = $m45.value;
          return [{ tag: "Some", value: { tag: "FnItem", f: value } }, l];
          break $m45$match;
        }
        if ($m45.tag === "None") {
          return [{ tag: "None" }, l];
          break $m45$match;
        }
        $rt.unreachable();
      }
      break $m44$match;
    }
    if ($m44.tag === "ConstItem") {
      const decl = $m44.decl;
      const def = $context.get_def({ ctx: ctx, id: def_at({ l: l, ctx: ctx, tag: $defs.tag_item, span: decl.span }) });
      const ty = type_or({ o: $std_map.find({ d: ctx.decl_types, key: def.id }) });
      const $m51 = $std_map.find({ d: ctx.const_values, key: def.id });
      $m51$match: {
        if ($m51.tag === "Some") {
          const value = $m51.value;
          return [{ tag: "Some", value: { tag: "ConstItem", def: def, ty: ty, value: { tag: "ValueE", value: value } } }, l];
          break $m51$match;
        }
        if ($m51.tag === "None") {
          const [$r55, l$11] = expr({ fuel: max_depth, l: l, ctx: ctx, e: decl.value, discard: false });
          l = l$11;
          return [{ tag: "Some", value: { tag: "ConstItem", def: def, ty: ty, value: $r55 } }, l];
          break $m51$match;
        }
        $rt.unreachable();
      }
      break $m44$match;
    }
    if ($m44.tag === "TypeAliasItem") {
      const decl = $m44.decl;
      const def = $context.get_def({ ctx: ctx, id: def_at({ l: l, ctx: ctx, tag: $defs.tag_item, span: decl.span }) });
      return [{ tag: "Some", value: { tag: "AliasItem", def: def, ty: type_or({ o: $std_map.find({ d: ctx.type_aliases, key: def.id }) }) } }, l];
      break $m44$match;
    }
    if ($m44.tag === "IntrinsicTypeItem") {
      const decl = $m44.decl;
      const def = $context.get_def({ ctx: ctx, id: def_at({ l: l, ctx: ctx, tag: $defs.tag_item, span: decl.span }) });
      return [{ tag: "Some", value: { tag: "IntrinsicTypeItem", def: def, params: type_param_defs({ ctx: ctx, owner: def.id }) } }, l];
      break $m44$match;
    }
    if ($m44.tag === "RecordItem") {
      const decl = $m44.decl;
      const def = $context.get_def({ ctx: ctx, id: def_at({ l: l, ctx: ctx, tag: $defs.tag_item, span: decl.span }) });
      return [{ tag: "Some", value: { tag: "RecordItem", def: def, params: type_param_defs({ ctx: ctx, owner: def.id }), fields: field_list({ ctx: ctx, owner: def.id }) } }, l];
      break $m44$match;
    }
    if ($m44.tag === "UnionItem") {
      const decl = $m44.decl;
      const def = $context.get_def({ ctx: ctx, id: def_at({ l: l, ctx: ctx, tag: $defs.tag_item, span: decl.span }) });
      let variants = $std_list.builder({  });
      for (const v of ids_or({ o: $std_map.find({ d: ctx.variants, key: def.id }) })) {
        const [, variants$12] = $std_list.push({ b: variants, x: { def: $context.get_def({ ctx: ctx, id: v }), fields: field_list({ ctx: ctx, owner: v }) } });
        variants = variants$12;
      }
      return [{ tag: "Some", value: { tag: "UnionItem", def: def, params: type_param_defs({ ctx: ctx, owner: def.id }), variants: $std_list.finish({ b: variants }) } }, l];
      break $m44$match;
    }
    if ($m44.tag === "InterfaceItem") {
      const decl = $m44.decl;
      const def = $context.get_def({ ctx: ctx, id: def_at({ l: l, ctx: ctx, tag: $defs.tag_item, span: decl.span }) });
      let tparam = { tag: "None" };
      let fns = $std_list.builder({  });
      for (const d of $std_list.finish({ b: ctx.defs })) {
        if (or_neg({ o: d.parent }) === def.id) {
          if ($rt.eq(d.kind, { tag: "TypeParam" }) && tparam.tag === "None") {
            tparam = { tag: "Some", value: d.name };
          }
          if ($rt.eq(d.kind, { tag: "IfaceFn" })) {
            const $m71 = $std_map.find({ d: ctx.signatures, key: d.id });
            $m71$match: {
              if ($m71.tag === "Some") {
                const value = $m71.value;
                const [, fns$13] = $std_list.push({ b: fns, x: { def: d, sig: value } });
                fns = fns$13;
                break $m71$match;
              }
              if ($m71.tag === "None") {
                skip({  });
                break $m71$match;
              }
              $rt.unreachable();
            }
          }
        }
      }
      return [{ tag: "Some", value: { tag: "InterfaceItem", def: def, tparam: tparam, fns: $std_list.finish({ b: fns }) } }, l];
      break $m44$match;
    }
    if ($m44.tag === "ImplItem") {
      const decl = $m44.decl;
      const [$r75, l$14] = impl_item({ l: l, ctx: ctx, item: decl });
      l = l$14;
      return [$r75, l];
      break $m44$match;
    }
    if ($m44.tag === "CapabilityItem") {
      const decl = $m44.decl;
      return [{ tag: "Some", value: { tag: "CapabilityItem", def: $context.get_def({ ctx: ctx, id: def_at({ l: l, ctx: ctx, tag: $defs.tag_item, span: decl.span }) }) } }, l];
      break $m44$match;
    }
    if (true) {
      return [{ tag: "None" }, l];
      break $m44$match;
    }
    $rt.unreachable();
  }
}

export function iface_fn_named({ ctx, iface, name }) {
  for (const d of $std_list.finish({ b: ctx.defs })) {
    if (or_neg({ o: d.parent }) === iface && $rt.eq(d.kind, { tag: "IfaceFn" }) && d.name === name) {
      return d.id;
    }
  }
  return $rt.int.neg(1, $ob12);
}

export function impl_map({ l, ctx, iface, fns }) {
  let map = $std_map.dict({  });
  for (const f of fns) {
    const fd = $context.get_def({ ctx: ctx, id: def_at({ l: l, ctx: ctx, tag: $defs.tag_item, span: f.span }) });
    const want = iface_fn_named({ ctx: ctx, iface: iface, name: f.name.text });
    if (want >= 0) {
      const [, map$15] = $std_map.set({ d: map, key: want, value: { def: fd, name: impl_fn_name({ ctx: ctx, def: fd }) } });
      map = map$15;
    }
  }
  return map;
}

export function impl_item({ l, ctx, item }) {
  const impl_key = key({ l: l, tag: $defs.tag_item, span: item.span });
  const iface_id = ref_def({ ctx: ctx, k: impl_key });
  if (iface_id < 0) {
    return [{ tag: "None" }, l];
  }
  const iface = $context.get_def({ ctx: ctx, id: iface_id });
  const def = $context.get_def({ ctx: ctx, id: or_neg({ o: $std_map.find({ d: ctx.def_of, key: impl_key }) }) });
  const target = impl_target({ ctx: ctx, impl_def: def.id });
  l = { ...l, impl_fns: impl_map({ l: l, ctx: ctx, iface: iface_id, fns: item.fns }) };
  let fns = $std_list.builder({  });
  for (const f of item.fns) {
    const fd = $context.get_def({ ctx: ctx, id: def_at({ l: l, ctx: ctx, tag: $defs.tag_item, span: f.span }) });
    const [$r85, l$16] = fn_decl({ l: l, ctx: ctx, f: f, impl_name: { tag: "Some", value: impl_fn_name({ ctx: ctx, def: fd }) } });
    l = l$16;
    const $m83 = $r85;
    $m83$match: {
      if ($m83.tag === "Some") {
        const value = $m83.value;
        const [, fns$17] = $std_list.push({ b: fns, x: value });
        fns = fns$17;
        break $m83$match;
      }
      if ($m83.tag === "None") {
        skip({  });
        break $m83$match;
      }
      $rt.unreachable();
    }
  }
  l = { ...l, impl_fns: $std_map.dict({  }) };
  const fn_list = $std_list.finish({ b: fns });
  let entries = $std_list.builder({  });
  for (const f of fn_list) {
    const [, entries$18] = $std_list.push({ b: entries, x: { name: f.def.name, fn_ir: f } });
    entries = entries$18;
  }
  return [{ tag: "Some", value: { tag: "ImplItem", def: def, iface: iface, target: target, dict_name: impl_dict_name({ ctx: ctx, iface: iface, target: target }), fns: fn_list, entries: $std_list.finish({ b: entries }) } }, l];
}

export function checked_somewhere({ ctx, source }) {
  const $hi90 = ctx.obligation_count;
  for (let i = 0; i < $hi90; i++) {
    const o = $context.get_obligation({ ctx: ctx, id: i });
    if (or_neg({ o: o.source }) === source && !is_proved({ o: o })) {
      return true;
    }
  }
  return false;
}

export function old_names({ root }) {
  let out = $std_list.builder({  });
  for (const n of $walk.nodes_in_expr({ e: root, into_verify: true })) {
    const $m91 = n;
    $m91$match: {
      if ($m91.tag === "NExpr") {
        const e = $m91.e;
        const $m92 = e;
        $m92$match: {
          if ($m92.tag === "Old") {
            const name = $m92.name;
            const [, out$19] = $std_list.push({ b: out, x: name.text });
            out = out$19;
            break $m92$match;
          }
          if (true) {
            skip({  });
            break $m92$match;
          }
          $rt.unreachable();
        }
        break $m91$match;
      }
      if (true) {
        skip({  });
        break $m91$match;
      }
      $rt.unreachable();
    }
  }
  return $std_list.finish({ b: out });
}

export function contains_text({ xs, x }) {
  for (const y of xs) {
    if (y === x) {
      return true;
    }
  }
  return false;
}

export function fn_decl({ l, ctx, f, impl_name }) {
  const def = $context.get_def({ ctx: ctx, id: def_at({ l: l, ctx: ctx, tag: $defs.tag_item, span: f.span }) });
  const $m93 = $std_map.find({ d: ctx.signatures, key: def.id });
  $m93$match: {
    if ($m93.tag === "None") {
      return [{ tag: "None" }, l];
      break $m93$match;
    }
    if ($m93.tag === "Some") {
      const value = $m93.value;
      const [$r95, l$20] = fn_decl_sig({ l: l, ctx: ctx, f: f, def: def, sig: value, impl_name: impl_name });
      l = l$20;
      return [$r95, l];
      break $m93$match;
    }
    $rt.unreachable();
  }
}

export function fn_decl_sig({ l, ctx, f, def, sig, impl_name }) {
  const tp = tparam_lists({ ctx: ctx, sig: sig });
  const dicts = tp.dicts;
  let params = $std_list.builder({  });
  let inout_names = $std_list.builder({  });
  for (const p of sig.params) {
    const [, params$21] = $std_list.push({ b: params, x: { name: p.name, ty: p.ty, is_inout: p.is_inout } });
    params = params$21;
    if (p.is_inout) {
      const [, inout_names$22] = $std_list.push({ b: inout_names, x: p.name });
      inout_names = inout_names$22;
    }
  }
  const param_list = $std_list.finish({ b: params });
  const name = text_or({ o: impl_name, dflt: def.name });
  if (f.is_intrinsic) {
    const q = $context.qualified_name({ ctx: ctx, id: def.id });
    let ns = "";
    let i = 0;
    for (const seg of $std_text.split({ t: q, sep: "." })) {
      if (i === 1) {
        ns = seg;
      }
      i = $rt.int.add(i, 1, $ob13);
    }
    return [{ tag: "Some", value: { def: def, sig: sig, name: name, dict_params: tp.dict_params, const_params: tp.const_params, params: param_list, ret: sig.ret, intrinsic: { tag: "Some", value: { ns: ns, name: def.name } }, entry_checks: [], body: { tag: "None" }, early_return: false } }, l];
  }
  if (f.body.tag === "None") {
    return [{ tag: "None" }, l];
  }
  let olds = [];
  let post = $std_list.builder({  });
  let measure_clause = { tag: "None" };
  for (const c of f.contracts) {
    if (c.clause === "ensures") {
      const [, post$23] = $std_list.push({ b: post, x: c });
      post = post$23;
    }
    if (c.clause === "decreases" && measure_clause.tag === "None") {
      measure_clause = { tag: "Some", value: c };
    }
    if (checked_somewhere({ ctx: ctx, source: key({ l: l, tag: $defs.tag_sig, span: c.span }) })) {
      for (const n of old_names({ root: c.expr })) {
        if (!contains_text({ xs: olds, x: n })) {
          olds = $std_list.append({ xs: olds, x: n });
        }
      }
    }
  }
  let measured = false;
  let measure_key = $rt.int.neg(1, $ob14);
  const $m105 = measure_clause;
  $m105$match: {
    if ($m105.tag === "Some") {
      const value = $m105.value;
      measure_key = key({ l: l, tag: $defs.tag_sig, span: value.span });
      const numeric = is_int_like({ t: type_of_expr({ l: l, ctx: ctx, e: value.expr }) });
      const $hi106 = ctx.obligation_count;
      for (let i = 0; i < $hi106; i++) {
        const o = $context.get_obligation({ ctx: ctx, id: i });
        if (numeric && o.def === def.id && o.kind.tag === "Decreases" && (o.callee.tag === "Some" || o.at === measure_key) && !is_proved({ o: o })) {
          measured = true;
        }
      }
      break $m105$match;
    }
    if ($m105.tag === "None") {
      skip({  });
      break $m105$match;
    }
    $rt.unreachable();
  }
  let measure_name = { tag: "None" };
  if (measured) {
    measure_name = { tag: "Some", value: "$measure" };
  }
  const fctx = { inout_names: $std_list.finish({ b: inout_names }), ret: sig.ret, post: $std_list.finish({ b: post }), def: { tag: "Some", value: def }, dicts: dicts, result_expr: { tag: "None" }, measure: measure_name };
  l = { ...l, fn_ctx: { tag: "Some", value: fctx } };
  const [$r114, l$24] = begin_collect({ l: l });
  l = l$24;
  const saved = $r114;
  const [$r115, l$25] = entry_checks({ l: l, ctx: ctx, sig: sig, contracts: f.contracts, def_name: def.name });
  l = l$25;
  const checks = $r115;
  const [$r116, l$26] = end_collect({ l: l, saved: saved, out: checks });
  l = l$26;
  let entry = $r116;
  const $m117 = measure_clause;
  $m117$match: {
    if ($m117.tag === "Some") {
      const value = $m117.value;
      if (measured) {
        const [$r118, l$27] = begin_collect({ l: l });
        l = l$27;
        const saved2 = $r118;
        const [$r119, l$28] = expr({ fuel: max_depth, l: l, ctx: ctx, e: value.expr, discard: false });
        l = l$28;
        const m = $r119;
        const [$r122, l$29] = end_collect({ l: l, saved: saved2, out: [{ tag: "Let", name: "$measure", ty: { tag: "Prim", name: "Int" }, mutable: false, value: m }] });
        l = l$29;
        entry = [...entry, ...$r122];
        const $hi123 = ctx.obligation_count;
        for (let i = 0; i < $hi123; i++) {
          const o = $context.get_obligation({ ctx: ctx, id: i });
          if (o.def === def.id && o.kind.tag === "Decreases" && o.at === measure_key) {
            if (!is_proved({ o: o })) {
              entry = $std_list.append({ xs: entry, x: { tag: "Check", cond: { tag: "Cmp", op: ">=", left: { tag: "Local", name: "$measure", ty: { tag: "Prim", name: "Int" } }, right: { tag: "IntE", v: 0 }, is_float: false }, ob: ob_ref_of({ l: l, ctx: ctx, o: o }) } });
            }
          }
        }
      }
      break $m117$match;
    }
    if ($m117.tag === "None") {
      skip({  });
      break $m117$match;
    }
    $rt.unreachable();
  }
  for (const o of olds) {
    let ty = { tag: "ErrorT" };
    for (const p of param_list) {
      if (p.name === o) {
        ty = p.ty;
      }
    }
    entry = $std_list.append({ xs: entry, x: { tag: "Let", name: "$old_" + o, ty: ty, mutable: false, value: { tag: "Snapshot", value: { tag: "Local", name: o, ty: ty }, ty: ty } } });
  }
  let body = [];
  let early = false;
  const $m133 = f.body;
  $m133$match: {
    if ($m133.tag === "Some") {
      const value = $m133.value;
      const [$r134, l$30] = block({ fuel: max_depth, l: l, ctx: ctx, b: value });
      l = l$30;
      body = $r134;
      early = block_has_try({ b: value });
      break $m133$match;
    }
    if ($m133.tag === "None") {
      skip({  });
      break $m133$match;
    }
    $rt.unreachable();
  }
  if ($std_list.len({ xs: inout_names_of({ l: l }) }) > 0 && is_unit({ t: sig.ret })) {
    body = $std_list.append({ xs: body, x: { tag: "Return", value: { tag: "UnitE" } } });
  }
  l = { ...l, fn_ctx: { tag: "None" } };
  return [{ tag: "Some", value: { def: def, sig: sig, name: name, dict_params: tp.dict_params, const_params: tp.const_params, params: param_list, ret: sig.ret, intrinsic: { tag: "None" }, entry_checks: entry, body: { tag: "Some", value: body }, early_return: early } }, l];
}

export function tparam_lists({ ctx, sig }) {
  let dict_params = $std_list.builder({  });
  let const_params = $std_list.builder({  });
  let dicts = $std_map.dict({  });
  for (const p of sig.tparams) {
    const $m143 = p;
    $m143$match: {
      if ($m143.tag === "TypeP") {
        const def = $m143.def;
        const bound = $m143.bound;
        const $m144 = bound;
        $m144$match: {
          if ($m144.tag === "Some") {
            const value = $m144.value;
            const name = "$dict_" + $context.get_def({ ctx: ctx, id: def }).name;
            const [, dict_params$31] = $std_list.push({ b: dict_params, x: { def: def, iface: value, name: name } });
            dict_params = dict_params$31;
            const [, dicts$32] = $std_map.set({ d: dicts, key: def, value: name });
            dicts = dicts$32;
            break $m144$match;
          }
          if ($m144.tag === "None") {
            skip({  });
            break $m144$match;
          }
          $rt.unreachable();
        }
        break $m143$match;
      }
      if ($m143.tag === "ConstP") {
        const def = $m143.def;
        const ty = $m143.ty;
        const [, const_params$33] = $std_list.push({ b: const_params, x: { def: def, name: $context.get_def({ ctx: ctx, id: def }).name, ty: ty } });
        const_params = const_params$33;
        break $m143$match;
      }
      if (true) {
        skip({  });
        break $m143$match;
      }
      $rt.unreachable();
    }
  }
  return { dict_params: $std_list.finish({ b: dict_params }), const_params: $std_list.finish({ b: const_params }), dicts: dicts };
}

export function inout_names_of({ l }) {
  const $m148 = l.fn_ctx;
  $m148$match: {
    if ($m148.tag === "Some") {
      const value = $m148.value;
      return value.inout_names;
      break $m148$match;
    }
    if ($m148.tag === "None") {
      return [];
      break $m148$match;
    }
    $rt.unreachable();
  }
}

export function entry_checks({ l, ctx, sig, contracts, def_name }) {
  let out = $std_list.builder({  });
  let call_sites = $std_list.builder({  });
  const $hi149 = ctx.obligation_count;
  for (let i = 0; i < $hi149; i++) {
    const o = $context.get_obligation({ ctx: ctx, id: i });
    if (or_neg({ o: o.callee }) === sig.def) {
      const [, call_sites$34] = $std_list.push({ b: call_sites, x: o });
      call_sites = call_sites$34;
    }
  }
  const sites = $std_list.finish({ b: call_sites });
  let i = 0;
  for (const p of sig.params) {
    if (i < $std_list.len({ xs: sig.param_defs })) {
      const pd = $std_list.get({ xs: sig.param_defs, i: i });
      const $m150 = $std_map.find({ d: ctx.decl_types, key: pd });
      $m150$match: {
        if ($m150.tag === "None") {
          skip({  });
          break $m150$match;
        }
        if ($m150.tag === "Some") {
          const value = $m150.value;
          let flows = 0;
          let all_proved = true;
          for (const o of sites) {
            if (o.kind.tag === "Refinement" && text_or({ o: o.param, dflt: "" }) === p.name) {
              flows = $rt.int.add(flows, 1, $ob15);
              if (!is_proved({ o: o })) {
                all_proved = false;
              }
            }
          }
          if (!(flows > 0 && all_proved)) {
            const pdef = $context.get_def({ ctx: ctx, id: pd });
            const local = { tag: "Local", name: p.name, ty: value };
            for (const pred of refinement_preds({ ctx: ctx, t: value })) {
              const [$r152, l$35] = with_it({ fuel: max_depth, l: l, ctx: ctx, it_expr: local, pred: pred });
              l = l$35;
              const cond = $r152;
              const [, out$36] = $std_list.push({ b: out, x: { tag: "Check", cond: cond, ob: ob_ref({ l: l, ctx: ctx, kind: "refinement", text: $printer.print_expr({ e: pred.e }), file: pdef.file, span: pdef.span, def: def_name }) } });
              out = out$36;
            }
          }
          break $m150$match;
        }
        $rt.unreachable();
      }
    }
    i = $rt.int.add(i, 1, $ob16);
  }
  for (const c of contracts) {
    if (c.clause === "requires" && !c.is_proved) {
      const ck = key({ l: l, tag: $defs.tag_sig, span: c.span });
      let n = 0;
      let all_proved = true;
      for (const o of sites) {
        if (o.kind.tag === "Requires" && or_neg({ o: o.source }) === ck) {
          n = $rt.int.add(n, 1, $ob17);
          if (!is_proved({ o: o })) {
            all_proved = false;
          }
        }
      }
      if (!(n > 0 && all_proved)) {
        const [$r154, l$37] = expr({ fuel: max_depth, l: l, ctx: ctx, e: c.expr, discard: false });
        l = l$37;
        const [, out$38] = $std_list.push({ b: out, x: { tag: "Check", cond: $r154, ob: ob_ref({ l: l, ctx: ctx, kind: "requires", text: $printer.print_expr({ e: c.expr }), file: l.file, span: c.span, def: def_name }) } });
        out = out$38;
      }
    }
  }
  return [$std_list.finish({ b: out }), l];
}

export function with_it({ fuel, l, ctx, it_expr, pred }) {
  if (fuel === 0) {
    return [{ tag: "UnitE" }, l];
  }
  const saved_it = l.it_expr;
  const saved_file = l.file;
  l = { ...l, it_expr: { tag: "Some", value: it_expr }, file: pred.file };
  const [$r159, l$39] = expr({ fuel: $rt.int.sub(fuel, 1, $ob18), l: l, ctx: ctx, e: pred.e, discard: false });
  l = l$39;
  const out = $r159;
  l = { ...l, it_expr: saved_it, file: saved_file };
  return [out, l];
}

export function main_spec({ l, ctx, f }) {
  const fn_id = def_at({ l: l, ctx: ctx, tag: $defs.tag_item, span: f.span });
  const $m161 = $std_map.find({ d: ctx.signatures, key: fn_id });
  $m161$match: {
    if ($m161.tag === "None") {
      return { tag: "None" };
      break $m161$match;
    }
    if ($m161.tag === "Some") {
      const value = $m161.value;
      let roots = $std_list.builder({  });
      let args = "args";
      for (const p of value.params) {
        const s = $types.strip({ t: p.ty });
        const $m163 = s;
        $m163$match: {
          if ($m163.tag === "Capability") {
            const def = $m163.def;
            if ($std_text.starts_with({ t: $context.qualified_name({ ctx: ctx, id: def }), prefix: "std.io." })) {
              const [, roots$40] = $std_list.push({ b: roots, x: { name: p.name, kind: $context.get_def({ ctx: ctx, id: def }).name } });
              roots = roots$40;
            }
            break $m163$match;
          }
          if ($m163.tag === "Opaque") {
            args = p.name;
            break $m163$match;
          }
          if (true) {
            skip({  });
            break $m163$match;
          }
          $rt.unreachable();
        }
      }
      return { tag: "Some", value: { roots: $std_list.finish({ b: roots }), args: args } };
      break $m161$match;
    }
    $rt.unreachable();
  }
}

export function block({ fuel, l, ctx, b }) {
  if (fuel === 0) {
    return [[], l];
  }
  let out = [];
  for (const s of b.stmts) {
    const [$r167, l$41] = stmt({ fuel: $rt.int.sub(fuel, 1, $ob19), l: l, ctx: ctx, s: s });
    l = l$41;
    out = [...out, ...$r167];
  }
  return [out, l];
}

export function stmt({ fuel, l, ctx, s }) {
  if (fuel === 0) {
    return [[], l];
  }
  const [$r168, l$42] = begin_collect({ l: l });
  l = l$42;
  const saved = $r168;
  const [$r169, l$43] = stmt_inner({ fuel: $rt.int.sub(fuel, 1, $ob20), l: l, ctx: ctx, s: s });
  l = l$43;
  const out = $r169;
  const [$r170, l$44] = end_collect({ l: l, saved: saved, out: out });
  l = l$44;
  return [$r170, l];
}

export function stmt_inner({ fuel, l, ctx, s }) {
  if (fuel === 0) {
    return [[], l];
  }
  const $m171 = s;
  $m171$match: {
    if ($m171.tag === "Let") {
      const name = $m171.name;
      const value = $m171.value;
      const span = $m171.span;
      const [$r172, l$45] = let_stmt({ fuel: $rt.int.sub(fuel, 1, $ob21), l: l, ctx: ctx, name: name.text, e: value, at: span, mutable: false });
      l = l$45;
      return [$r172, l];
      break $m171$match;
    }
    if ($m171.tag === "Var") {
      const name = $m171.name;
      const value = $m171.value;
      const span = $m171.span;
      const [$r173, l$46] = let_stmt({ fuel: $rt.int.sub(fuel, 1, $ob22), l: l, ctx: ctx, name: name.text, e: value, at: span, mutable: true });
      l = l$46;
      return [$r173, l];
      break $m171$match;
    }
    if ($m171.tag === "Assign") {
      const name = $m171.name;
      const value = $m171.value;
      const span = $m171.span;
      const def = ref_def({ ctx: ctx, k: key({ l: l, tag: $defs.tag_stmt, span: span }) });
      let declared = { tag: "ErrorT" };
      let target = name.text;
      if (def >= 0) {
        declared = type_or({ o: $std_map.find({ d: ctx.decl_types, key: def }) });
        target = $context.get_def({ ctx: ctx, id: def }).name;
      }
      const [$r175, l$47] = checked_flow({ fuel: $rt.int.sub(fuel, 1, $ob23), l: l, ctx: ctx, e: value, declared: declared });
      l = l$47;
      return [[{ tag: "Assign", name: target, ty: declared, value: $r175 }], l];
      break $m171$match;
    }
    if ($m171.tag === "Return") {
      const value = $m171.value;
      const span = $m171.span;
      const [$r177, l$48] = return_stmt({ fuel: $rt.int.sub(fuel, 1, $ob24), l: l, ctx: ctx, e: value, at: span });
      l = l$48;
      return [$r177, l];
      break $m171$match;
    }
    if ($m171.tag === "If") {
      const cond = $m171.cond;
      const then_block = $m171.then_block;
      const else_block = $m171.else_block;
      const [$r178, l$49] = expr({ fuel: $rt.int.sub(fuel, 1, $ob25), l: l, ctx: ctx, e: cond, discard: false });
      l = l$49;
      const c = $r178;
      const [$r179, l$50] = block({ fuel: $rt.int.sub(fuel, 1, $ob26), l: l, ctx: ctx, b: then_block });
      l = l$50;
      const then_ir = $r179;
      let else_ir = { tag: "None" };
      const $m181 = else_block;
      $m181$match: {
        if ($m181.tag === "Some") {
          const value = $m181.value;
          const [$r182, l$51] = block({ fuel: $rt.int.sub(fuel, 1, $ob27), l: l, ctx: ctx, b: value });
          l = l$51;
          else_ir = { tag: "Some", value: $r182 };
          break $m181$match;
        }
        if ($m181.tag === "None") {
          skip({  });
          break $m181$match;
        }
        $rt.unreachable();
      }
      return [[{ tag: "If", cond: c, then_block: then_ir, else_block: else_ir }], l];
      break $m171$match;
    }
    if ($m171.tag === "Match") {
      const scrutinee = $m171.scrutinee;
      const arms = $m171.arms;
      const [$r185, l$52] = match_stmt({ fuel: $rt.int.sub(fuel, 1, $ob28), l: l, ctx: ctx, scrutinee: scrutinee, arms: arms });
      l = l$52;
      return [$r185, l];
      break $m171$match;
    }
    if ($m171.tag === "Loop") {
      const cond = $m171.cond;
      const clauses = $m171.clauses;
      const body = $m171.body;
      const span = $m171.span;
      const [$r186, l$53] = loop_stmt({ fuel: $rt.int.sub(fuel, 1, $ob29), l: l, ctx: ctx, cond: cond, clauses: clauses, body: body, at: span });
      l = l$53;
      return [$r186, l];
      break $m171$match;
    }
    if ($m171.tag === "For") {
      const name = $m171.name;
      const domain = $m171.domain;
      const body = $m171.body;
      const span = $m171.span;
      const [$r187, l$54] = for_stmt({ fuel: $rt.int.sub(fuel, 1, $ob30), l: l, ctx: ctx, domain: domain, body: body, at: span });
      l = l$54;
      return [$r187, l];
      break $m171$match;
    }
    if ($m171.tag === "Assume") {
      const claim = $m171.claim;
      const justification = $m171.justification;
      return [[{ tag: "Comment", text: "assume " + qname_text({ q: claim }) + " " + $json.quote({ t: justification }) }], l];
      break $m171$match;
    }
    if ($m171.tag === "ExprStmt") {
      const expr = $m171.expr;
      const [$r189, l$55] = expr_of({ fuel: $rt.int.sub(fuel, 1, $ob31), l: l, ctx: ctx, e: expr, discard: true });
      l = l$55;
      const e = $r189;
      if (e.tag === "UnitE") {
        return [[], l];
      }
      return [[{ tag: "ExprStmt", expr: e }], l];
      break $m171$match;
    }
    $rt.unreachable();
  }
}

export function qname_text({ q }) {
  let parts = $std_list.builder({  });
  for (const s of q.segments) {
    const [, parts$56] = $std_list.push({ b: parts, x: s.text });
    parts = parts$56;
  }
  return $std_text.join({ parts: $std_list.finish({ b: parts }), sep: "." });
}

export function let_stmt({ fuel, l, ctx, name, e, at, mutable }) {
  if (fuel === 0) {
    return [[], l];
  }
  const def = def_at({ l: l, ctx: ctx, tag: $defs.tag_stmt, span: at });
  let declared = { tag: "ErrorT" };
  let target = name;
  if (def >= 0) {
    declared = type_or({ o: $std_map.find({ d: ctx.decl_types, key: def }) });
    target = $context.get_def({ ctx: ctx, id: def }).name;
  }
  const [$r192, l$57] = checked_flow({ fuel: $rt.int.sub(fuel, 1, $ob32), l: l, ctx: ctx, e: e, declared: declared });
  l = l$57;
  return [[{ tag: "Let", name: target, ty: declared, mutable: mutable, value: $r192 }], l];
}

export function checked_flow({ fuel, l, ctx, e, declared }) {
  if (fuel === 0) {
    return [{ tag: "UnitE" }, l];
  }
  const ob = find_ob({ ctx: ctx, k: expr_key({ l: l, e: e }), kind: { tag: "Refinement" } });
  const [$r196, l$58] = expr({ fuel: $rt.int.sub(fuel, 1, $ob33), l: l, ctx: ctx, e: e, discard: false });
  l = l$58;
  const value = $r196;
  if (ob.tag === "None") {
    return [value, l];
  }
  const o = ob_or({ o: ob });
  if (!is_checked({ o: o })) {
    return [value, l];
  }
  const preds = refinement_preds({ ctx: ctx, t: declared });
  if ($std_list.len({ xs: preds }) === 0) {
    return [value, l];
  }
  const [$r197, l$59] = tmp({ l: l, prefix: "it" });
  l = l$59;
  const it_name = $r197;
  const it_expr = { tag: "Local", name: it_name, ty: declared };
  let checks = $std_list.builder({  });
  for (const p of preds) {
    const [$r199, l$60] = with_it({ fuel: $rt.int.sub(fuel, 1, $ob34), l: l, ctx: ctx, it_expr: it_expr, pred: p });
    l = l$60;
    const [, checks$61] = $std_list.push({ b: checks, x: { tag: "Check", cond: $r199, ob: ob_ref_of({ l: l, ctx: ctx, o: o }) } });
    checks = checks$61;
  }
  return [{ tag: "Checked", value: value, it_name: it_name, ty: declared, checks: $std_list.finish({ b: checks }) }, l];
}

export function ob_or({ o }) {
  const $m202 = o;
  $m202$match: {
    if ($m202.tag === "Some") {
      const value = $m202.value;
      return value;
      break $m202$match;
    }
    if ($m202.tag === "None") {
      return { id: $rt.int.neg(1, $ob35), kind: { tag: "Assertion" }, at: 0, at_file: 0, at_span: { start: 0, end: 0 }, def: 0, text: "", source: { tag: "None" }, site: { tag: "OtherSite" }, pinned: false, callee: { tag: "None" }, param: { tag: "None" }, status: { tag: "Pending" }, by: { tag: "None" } };
      break $m202$match;
    }
    $rt.unreachable();
  }
}

export function return_stmt({ fuel, l, ctx, e, at }) {
  if (fuel === 0) {
    return [[], l];
  }
  const $m212 = l.fn_ctx;
  $m212$match: {
    if ($m212.tag === "None") {
      const [$r213, l$62] = expr({ fuel: $rt.int.sub(fuel, 1, $ob36), l: l, ctx: ctx, e: e, discard: false });
      l = l$62;
      return [[{ tag: "Return", value: $r213 }], l];
      break $m212$match;
    }
    if ($m212.tag === "Some") {
      const value = $m212.value;
      const [$r215, l$63] = checked_flow({ fuel: $rt.int.sub(fuel, 1, $ob37), l: l, ctx: ctx, e: e, declared: value.ret });
      l = l$63;
      const v = $r215;
      let post_obs = $std_list.builder({  });
      for (const o of obligations_at({ ctx: ctx, k: key({ l: l, tag: $defs.tag_stmt, span: at }) })) {
        if (o.kind.tag === "Ensures" && is_checked({ o: o })) {
          const [, post_obs$64] = $std_list.push({ b: post_obs, x: o });
          post_obs = post_obs$64;
        }
      }
      if ($std_list.built({ b: post_obs }) === 0) {
        return [[{ tag: "Return", value: v }], l];
      }
      const [$r217, l$65] = tmp({ l: l, prefix: "r" });
      l = l$65;
      const r = $r217;
      const result_expr = { tag: "Local", name: r, ty: value.ret };
      let out = [{ tag: "Let", name: r, ty: value.ret, mutable: false, value: v }];
      l = { ...l, fn_ctx: { tag: "Some", value: { ...value, result_expr: { tag: "Some", value: result_expr } } } };
      for (const o of $std_list.finish({ b: post_obs })) {
        const src = or_neg({ o: o.source });
        for (const c of value.post) {
          if (key({ l: l, tag: $defs.tag_sig, span: c.span }) === src) {
            const [$r224, l$66] = expr({ fuel: $rt.int.sub(fuel, 1, $ob38), l: l, ctx: ctx, e: c.expr, discard: false });
            l = l$66;
            out = $std_list.append({ xs: out, x: { tag: "Check", cond: $r224, ob: ob_ref_of({ l: l, ctx: ctx, o: o }) } });
          }
        }
      }
      l = { ...l, fn_ctx: { tag: "Some", value: { ...value, result_expr: { tag: "None" } } } };
      return [$std_list.append({ xs: out, x: { tag: "Return", value: result_expr } }), l];
      break $m212$match;
    }
    $rt.unreachable();
  }
}

export function match_stmt({ fuel, l, ctx, scrutinee, arms }) {
  if (fuel === 0) {
    return [[], l];
  }
  const [$r231, l$67] = tmp({ l: l, prefix: "m" });
  l = l$67;
  const t = $r231;
  const ty = type_of_expr({ l: l, ctx: ctx, e: scrutinee });
  const [$r232, l$68] = expr({ fuel: $rt.int.sub(fuel, 1, $ob39), l: l, ctx: ctx, e: scrutinee, discard: false });
  l = l$68;
  const scrut = $r232;
  const subject = { tag: "Local", name: t, ty: ty };
  let out = $std_list.builder({  });
  for (const arm of arms) {
    const [$r234, l$69] = pattern_test_of({ fuel: $rt.int.sub(fuel, 1, $ob40), l: l, ctx: ctx, p: arm.pattern, subject: subject, ty: ty });
    l = l$69;
    const pattern_test = $r234;
    const bindings = pattern_bindings({ l: l, ctx: ctx, p: arm.pattern, subject: subject, ty: ty });
    let guard = { tag: "None" };
    const $m236 = arm.guard;
    $m236$match: {
      if ($m236.tag === "Some") {
        const value = $m236.value;
        const [$r237, l$70] = expr({ fuel: $rt.int.sub(fuel, 1, $ob41), l: l, ctx: ctx, e: value, discard: false });
        l = l$70;
        guard = { tag: "Some", value: $r237 };
        break $m236$match;
      }
      if ($m236.tag === "None") {
        skip({  });
        break $m236$match;
      }
      $rt.unreachable();
    }
    let body = [];
    const $m239 = arm.body;
    $m239$match: {
      if ($m239.tag === "ArmBlock") {
        const block = $m239.block;
        const [$r240, l$71] = block_ir({ fuel: $rt.int.sub(fuel, 1, $ob42), l: l, ctx: ctx, b: block });
        l = l$71;
        body = $r240;
        break $m239$match;
      }
      if ($m239.tag === "ArmStmt") {
        const stmt = $m239.stmt;
        const [$r241, l$72] = stmt_ir({ fuel: $rt.int.sub(fuel, 1, $ob43), l: l, ctx: ctx, s: stmt });
        l = l$72;
        body = $r241;
        break $m239$match;
      }
      $rt.unreachable();
    }
    const [, out$73] = $std_list.push({ b: out, x: { pattern_test: pattern_test, bindings: bindings, guard: guard, body: body } });
    out = out$73;
  }
  return [[{ tag: "Match", tmp: t, ty: ty, scrutinee: scrut, arms: $std_list.finish({ b: out }) }], l];
}

export function block_ir({ fuel, l, ctx, b }) {
  if (fuel === 0) {
    return [[], l];
  }
  const [$r244, l$74] = block({ fuel: $rt.int.sub(fuel, 1, $ob44), l: l, ctx: ctx, b: b });
  l = l$74;
  return [$r244, l];
}

export function stmt_ir({ fuel, l, ctx, s }) {
  if (fuel === 0) {
    return [[], l];
  }
  const [$r245, l$75] = stmt({ fuel: $rt.int.sub(fuel, 1, $ob45), l: l, ctx: ctx, s: s });
  l = l$75;
  return [$r245, l];
}

export function pattern_test_of({ fuel, l, ctx, p, subject, ty }) {
  if (fuel === 0) {
    return [{ tag: "None" }, l];
  }
  const $m247 = p;
  $m247$match: {
    if ($m247.tag === "WildcardPat") {
      return [{ tag: "None" }, l];
      break $m247$match;
    }
    if ($m247.tag === "BindPat") {
      return [{ tag: "None" }, l];
      break $m247$match;
    }
    if ($m247.tag === "LitPat") {
      const literal = $m247.literal;
      const [$r250, l$76] = expr({ fuel: $rt.int.sub(fuel, 1, $ob46), l: l, ctx: ctx, e: literal, discard: false });
      l = l$76;
      return [{ tag: "Some", value: { tag: "Eq", left: subject, right: $r250, ty: ty, prim: false, negate: false } }, l];
      break $m247$match;
    }
    if ($m247.tag === "VariantPat") {
      const span = $m247.span;
      const def = ref_def({ ctx: ctx, k: key({ l: l, tag: $defs.tag_pattern, span: span }) });
      if (def < 0) {
        return [{ tag: "Some", value: { tag: "BoolE", v: false } }, l];
      }
      return [{ tag: "Some", value: { tag: "IsVariant", subject: subject, variant: $context.get_def({ ctx: ctx, id: def }), ty: ty } }, l];
      break $m247$match;
    }
    $rt.unreachable();
  }
}

export function pattern_bindings({ l, ctx, p, subject, ty }) {
  const $m257 = p;
  $m257$match: {
    if ($m257.tag === "BindPat") {
      const name = $m257.name;
      return [{ name: name.text, ty: ty, value: subject }];
      break $m257$match;
    }
    if ($m257.tag === "VariantPat") {
      const fields = $m257.fields;
      const span = $m257.span;
      const $m259 = fields;
      $m259$match: {
        if ($m259.tag === "None") {
          return [];
          break $m259$match;
        }
        if ($m259.tag === "Some") {
          const value = $m259.value;
          const def = ref_def({ ctx: ctx, k: key({ l: l, tag: $defs.tag_pattern, span: span }) });
          if (def < 0) {
            return [];
          }
          const fs = fields_or({ o: $std_map.find({ d: ctx.fields, key: def }) });
          const subst = subst_of({ ctx: ctx, ty: ty });
          let out = $std_list.builder({  });
          let i = 0;
          let stop = false;
          for (const pf of value) {
            if (!stop) {
              const $m260 = pf;
              $m260$match: {
                if ($m260.tag === "PatFieldRest") {
                  stop = true;
                  break $m260$match;
                }
                if ($m260.tag === "PatFieldName") {
                  const name = $m260.name;
                  if (i < $std_list.len({ xs: fs })) {
                    const f = $std_list.get({ xs: fs, i: i });
                    const ft = $types.substitute({ t: f.ty, subst: subst });
                    const [, out$77] = $std_list.push({ b: out, x: { name: name.text, ty: ft, value: { tag: "FieldE", object: subject, name: f.name, ty: ft, owner: { tag: "Some", value: $context.get_def({ ctx: ctx, id: def }) } } } });
                    out = out$77;
                  }
                  i = $rt.int.add(i, 1, $ob47);
                  break $m260$match;
                }
                if ($m260.tag === "PatFieldSkip") {
                  i = $rt.int.add(i, 1, $ob48);
                  break $m260$match;
                }
                $rt.unreachable();
              }
            }
          }
          return $std_list.finish({ b: out });
          break $m259$match;
        }
        $rt.unreachable();
      }
      break $m257$match;
    }
    if (true) {
      return [];
      break $m257$match;
    }
    $rt.unreachable();
  }
}

export function find_source({ obs, kind, src }) {
  for (const o of obs) {
    if (same_kind({ a: o.kind, b: kind }) && or_neg({ o: o.source }) === src) {
      return { tag: "Some", value: o };
    }
  }
  return { tag: "None" };
}

export function loop_stmt({ fuel, l, ctx, cond, clauses, body, at }) {
  if (fuel === 0) {
    return [[], l];
  }
  const obs = obligations_at({ ctx: ctx, k: key({ l: l, tag: $defs.tag_stmt, span: at }) });
  let invariants = $std_list.builder({  });
  let dec_clause = { tag: "None" };
  for (const c of clauses) {
    if (c.clause === "invariant") {
      const [, invariants$78] = $std_list.push({ b: invariants, x: c });
      invariants = invariants$78;
    }
    if (c.clause === "decreases" && dec_clause.tag === "None") {
      dec_clause = { tag: "Some", value: c };
    }
  }
  const invs = $std_list.finish({ b: invariants });
  let out = [];
  for (const inv of invs) {
    const $m268 = find_source({ obs: obs, kind: { tag: "InvariantEntry" }, src: key({ l: l, tag: $defs.tag_sig, span: inv.span }) });
    $m268$match: {
      if ($m268.tag === "Some") {
        const value = $m268.value;
        if (is_checked({ o: value })) {
          const [$r270, l$79] = expr({ fuel: $rt.int.sub(fuel, 1, $ob49), l: l, ctx: ctx, e: inv.expr, discard: false });
          l = l$79;
          out = $std_list.append({ xs: out, x: { tag: "Check", cond: $r270, ob: ob_ref_of({ l: l, ctx: ctx, o: value }) } });
        }
        break $m268$match;
      }
      if ($m268.tag === "None") {
        skip({  });
        break $m268$match;
      }
      $rt.unreachable();
    }
  }
  const [$r272, l$80] = expr({ fuel: $rt.int.sub(fuel, 1, $ob50), l: l, ctx: ctx, e: cond, discard: false });
  l = l$80;
  const c = $r272;
  let body_ir = [];
  let dec = { tag: "None" };
  const $m274 = dec_clause;
  $m274$match: {
    if ($m274.tag === "Some") {
      const value = $m274.value;
      dec = find_source({ obs: obs, kind: { tag: "Decreases" }, src: key({ l: l, tag: $defs.tag_sig, span: value.span }) });
      break $m274$match;
    }
    if ($m274.tag === "None") {
      skip({  });
      break $m274$match;
    }
    $rt.unreachable();
  }
  const [$r276, l$81] = tmp({ l: l, prefix: "measure" });
  l = l$81;
  const measure = $r276;
  const measure_expr = { tag: "Local", name: measure, ty: { tag: "Prim", name: "Int" } };
  let checked_decreases = false;
  const $m279 = dec;
  $m279$match: {
    if ($m279.tag === "Some") {
      const value = $m279.value;
      checked_decreases = is_checked({ o: value });
      break $m279$match;
    }
    if ($m279.tag === "None") {
      skip({  });
      break $m279$match;
    }
    $rt.unreachable();
  }
  if (checked_decreases) {
    const [$r280, l$82] = begin_collect({ l: l });
    l = l$82;
    const saved = $r280;
    const [$r281, l$83] = expr({ fuel: $rt.int.sub(fuel, 1, $ob51), l: l, ctx: ctx, e: clause_expr({ o: dec_clause }), discard: false });
    l = l$83;
    const m = $r281;
    const [$r284, l$84] = end_collect({ l: l, saved: saved, out: [{ tag: "Let", name: measure, ty: { tag: "Prim", name: "Int" }, mutable: false, value: m }] });
    l = l$84;
    body_ir = [...body_ir, ...$r284];
    body_ir = $std_list.append({ xs: body_ir, x: { tag: "Check", cond: { tag: "Cmp", op: ">=", left: measure_expr, right: { tag: "IntE", v: 0 }, is_float: false }, ob: ob_ref_of({ l: l, ctx: ctx, o: ob_or({ o: dec }) }) } });
  }
  const [$r288, l$85] = block({ fuel: $rt.int.sub(fuel, 1, $ob52), l: l, ctx: ctx, b: body });
  l = l$85;
  body_ir = [...body_ir, ...$r288];
  for (const inv of invs) {
    const $m289 = find_source({ obs: obs, kind: { tag: "InvariantStep" }, src: key({ l: l, tag: $defs.tag_sig, span: inv.span }) });
    $m289$match: {
      if ($m289.tag === "Some") {
        const value = $m289.value;
        if (is_checked({ o: value })) {
          const [$r291, l$86] = begin_collect({ l: l });
          l = l$86;
          const saved = $r291;
          const [$r292, l$87] = expr({ fuel: $rt.int.sub(fuel, 1, $ob53), l: l, ctx: ctx, e: inv.expr, discard: false });
          l = l$87;
          const cond_ir = $r292;
          const [$r294, l$88] = end_collect({ l: l, saved: saved, out: [{ tag: "Check", cond: cond_ir, ob: ob_ref_of({ l: l, ctx: ctx, o: value }) }] });
          l = l$88;
          body_ir = [...body_ir, ...$r294];
        }
        break $m289$match;
      }
      if ($m289.tag === "None") {
        skip({  });
        break $m289$match;
      }
      $rt.unreachable();
    }
  }
  if (checked_decreases) {
    const [$r295, l$89] = begin_collect({ l: l });
    l = l$89;
    const saved = $r295;
    const [$r296, l$90] = expr({ fuel: $rt.int.sub(fuel, 1, $ob54), l: l, ctx: ctx, e: clause_expr({ o: dec_clause }), discard: false });
    l = l$90;
    const again = $r296;
    const [$r299, l$91] = end_collect({ l: l, saved: saved, out: [{ tag: "Check", cond: { tag: "Cmp", op: "<", left: again, right: measure_expr, is_float: false }, ob: ob_ref_of({ l: l, ctx: ctx, o: ob_or({ o: dec }) }) }] });
    l = l$91;
    body_ir = [...body_ir, ...$r299];
  }
  return [$std_list.append({ xs: out, x: { tag: "Loop", cond: c, body: body_ir } }), l];
}

export function clause_expr({ o }) {
  const $m301 = o;
  $m301$match: {
    if ($m301.tag === "Some") {
      const value = $m301.value;
      return value.expr;
      break $m301$match;
    }
    if ($m301.tag === "None") {
      return { tag: "Hole", span: { start: 0, end: 0 } };
      break $m301$match;
    }
    $rt.unreachable();
  }
}

export function for_stmt({ fuel, l, ctx, domain, body, at }) {
  if (fuel === 0) {
    return [[], l];
  }
  const def = $context.get_def({ ctx: ctx, id: def_at({ l: l, ctx: ctx, tag: $defs.tag_stmt, span: at }) });
  const declared = type_or({ o: $std_map.find({ d: ctx.decl_types, key: def.id }) });
  const $m304 = domain;
  $m304$match: {
    if ($m304.tag === "RangeDomain") {
      const lo = $m304.lo;
      const hi = $m304.hi;
      const [$r305, l$92] = expr({ fuel: $rt.int.sub(fuel, 1, $ob55), l: l, ctx: ctx, e: lo, discard: false });
      l = l$92;
      const lo_ir = $r305;
      const [$r306, l$93] = expr({ fuel: $rt.int.sub(fuel, 1, $ob56), l: l, ctx: ctx, e: hi, discard: false });
      l = l$93;
      let hi_ir = $r306;
      if (!(hi_ir.tag === "IntE" || hi_ir.tag === "Local")) {
        const [$r307, l$94] = tmp({ l: l, prefix: "hi" });
        l = l$94;
        const name = $r307;
        const [, l$95] = push_pre({ l: l, s: { tag: "Let", name: name, ty: { tag: "Prim", name: "Int" }, mutable: false, value: hi_ir } });
        l = l$95;
        hi_ir = { tag: "Local", name: name, ty: { tag: "Prim", name: "Int" } };
      }
      const [$r312, l$96] = block({ fuel: $rt.int.sub(fuel, 1, $ob57), l: l, ctx: ctx, b: body });
      l = l$96;
      return [[{ tag: "ForRange", name: def.name, lo: lo_ir, hi: hi_ir, body: $r312 }], l];
      break $m304$match;
    }
    if ($m304.tag === "InDomain") {
      const expr = $m304.expr;
      const [$r314, l$97] = expr_of({ fuel: $rt.int.sub(fuel, 1, $ob58), l: l, ctx: ctx, e: expr, discard: false });
      l = l$97;
      const list = $r314;
      const ob = find_ob({ ctx: ctx, k: expr_key({ l: l, e: expr }), kind: { tag: "Refinement" } });
      let body_ir = [];
      const $m316 = ob;
      $m316$match: {
        if ($m316.tag === "Some") {
          const value = $m316.value;
          if (is_checked({ o: value })) {
            const local = { tag: "Local", name: def.name, ty: declared };
            for (const p of refinement_preds({ ctx: ctx, t: declared })) {
              const [$r318, l$98] = begin_collect({ l: l });
              l = l$98;
              const saved = $r318;
              const [$r319, l$99] = with_it({ fuel: $rt.int.sub(fuel, 1, $ob59), l: l, ctx: ctx, it_expr: local, pred: p });
              l = l$99;
              const cond = $r319;
              const [$r321, l$100] = end_collect({ l: l, saved: saved, out: [{ tag: "Check", cond: cond, ob: ob_ref_of({ l: l, ctx: ctx, o: value }) }] });
              l = l$100;
              body_ir = [...body_ir, ...$r321];
            }
          }
          break $m316$match;
        }
        if ($m316.tag === "None") {
          skip({  });
          break $m316$match;
        }
        $rt.unreachable();
      }
      const [$r322, l$101] = block({ fuel: $rt.int.sub(fuel, 1, $ob60), l: l, ctx: ctx, b: body });
      l = l$101;
      body_ir = [...body_ir, ...$r322];
      return [[{ tag: "ForEach", name: def.name, ty: declared, list: list, body: body_ir }], l];
      break $m304$match;
    }
    $rt.unreachable();
  }
}

export function expr_of({ fuel, l, ctx, e, discard }) {
  if (fuel === 0) {
    return [{ tag: "UnitE" }, l];
  }
  const [$r325, l$102] = expr({ fuel: $rt.int.sub(fuel, 1, $ob61), l: l, ctx: ctx, e: e, discard: discard });
  l = l$102;
  return [$r325, l];
}

export function expr({ fuel, l, ctx, e, discard }) {
  if (fuel === 0) {
    return [{ tag: "UnitE" }, l];
  }
  const $m327 = e;
  $m327$match: {
    if ($m327.tag === "IntLit") {
      const value = $m327.value;
      return [{ tag: "IntE", v: value }, l];
      break $m327$match;
    }
    if ($m327.tag === "FloatLit") {
      const value = $m327.value;
      return [{ tag: "FloatE", v: value }, l];
      break $m327$match;
    }
    if ($m327.tag === "TextLit") {
      const value = $m327.value;
      return [{ tag: "TextE", v: value }, l];
      break $m327$match;
    }
    if ($m327.tag === "BoolLit") {
      const value = $m327.value;
      return [{ tag: "BoolE", v: value }, l];
      break $m327$match;
    }
    if ($m327.tag === "DurationLit") {
      const nanos = $m327.nanos;
      return [{ tag: "IntE", v: nanos }, l];
      break $m327$match;
    }
    if ($m327.tag === "Name") {
      return [name_expr({ l: l, ctx: ctx, e: e }), l];
      break $m327$match;
    }
    if ($m327.tag === "It") {
      const $m333 = l.it_expr;
      $m333$match: {
        if ($m333.tag === "Some") {
          const value = $m333.value;
          return [value, l];
          break $m333$match;
        }
        if ($m333.tag === "None") {
          return [{ tag: "Local", name: "$it", ty: type_of_expr({ l: l, ctx: ctx, e: e }) }, l];
          break $m333$match;
        }
        $rt.unreachable();
      }
      break $m327$match;
    }
    if ($m327.tag === "ResultRef") {
      const $m335 = l.fn_ctx;
      $m335$match: {
        if ($m335.tag === "Some") {
          const value = $m335.value;
          if (value.result_expr.tag === "Some") {
            return [expr_or_unit({ o: value.result_expr }), l];
          }
          break $m335$match;
        }
        if ($m335.tag === "None") {
          skip({  });
          break $m335$match;
        }
        $rt.unreachable();
      }
      return [{ tag: "Local", name: "$r", ty: type_of_expr({ l: l, ctx: ctx, e: e }) }, l];
      break $m327$match;
    }
    if ($m327.tag === "Old") {
      const name = $m327.name;
      return [{ tag: "Local", name: "$old_" + name.text, ty: type_of_expr({ l: l, ctx: ctx, e: e }) }, l];
      break $m327$match;
    }
    if ($m327.tag === "Ctor") {
      const [$r338, l$103] = ctor({ fuel: $rt.int.sub(fuel, 1, $ob62), l: l, ctx: ctx, e: e });
      l = l$103;
      return [$r338, l];
      break $m327$match;
    }
    if ($m327.tag === "RecordUpdate") {
      const [$r339, l$104] = record_update({ fuel: $rt.int.sub(fuel, 1, $ob63), l: l, ctx: ctx, e: e });
      l = l$104;
      return [$r339, l];
      break $m327$match;
    }
    if ($m327.tag === "ListLit") {
      const elems = $m327.elems;
      let out = $std_list.builder({  });
      for (const x of elems) {
        const [$r340, l$105] = expr({ fuel: $rt.int.sub(fuel, 1, $ob64), l: l, ctx: ctx, e: x, discard: false });
        l = l$105;
        const [, out$106] = $std_list.push({ b: out, x: $r340 });
        out = out$106;
      }
      return [{ tag: "ListE", elems: $std_list.finish({ b: out }), ty: type_of_expr({ l: l, ctx: ctx, e: e }) }, l];
      break $m327$match;
    }
    if ($m327.tag === "Try") {
      const [$r342, l$107] = try_expr({ fuel: $rt.int.sub(fuel, 1, $ob65), l: l, ctx: ctx, e: e });
      l = l$107;
      return [$r342, l];
      break $m327$match;
    }
    if ($m327.tag === "Recover") {
      const [$r343, l$108] = recover_expr({ fuel: $rt.int.sub(fuel, 1, $ob66), l: l, ctx: ctx, e: e });
      l = l$108;
      return [$r343, l];
      break $m327$match;
    }
    if ($m327.tag === "Quantifier") {
      const [$r344, l$109] = quantifier({ fuel: $rt.int.sub(fuel, 1, $ob67), l: l, ctx: ctx, e: e });
      l = l$109;
      return [$r344, l];
      break $m327$match;
    }
    if ($m327.tag === "Closure") {
      const [$r345, l$110] = closure({ fuel: $rt.int.sub(fuel, 1, $ob68), l: l, ctx: ctx, e: e });
      l = l$110;
      return [$r345, l];
      break $m327$match;
    }
    if ($m327.tag === "Fake") {
      const [$r346, l$111] = fake_expr({ fuel: $rt.int.sub(fuel, 1, $ob69), l: l, ctx: ctx, e: e });
      l = l$111;
      return [$r346, l];
      break $m327$match;
    }
    if ($m327.tag === "Hole") {
      return [{ tag: "UnitE" }, l];
      break $m327$match;
    }
    if ($m327.tag === "FieldAccess") {
      const [$r348, l$112] = field_access({ fuel: $rt.int.sub(fuel, 1, $ob70), l: l, ctx: ctx, e: e });
      l = l$112;
      return [$r348, l];
      break $m327$match;
    }
    if ($m327.tag === "Call") {
      const [$r349, l$113] = call({ fuel: $rt.int.sub(fuel, 1, $ob71), l: l, ctx: ctx, e: e, discard: discard });
      l = l$113;
      return [$r349, l];
      break $m327$match;
    }
    if ($m327.tag === "Unary") {
      const [$r350, l$114] = unary({ fuel: $rt.int.sub(fuel, 1, $ob72), l: l, ctx: ctx, e: e });
      l = l$114;
      return [$r350, l];
      break $m327$match;
    }
    if ($m327.tag === "Binary") {
      const [$r351, l$115] = binary({ fuel: $rt.int.sub(fuel, 1, $ob73), l: l, ctx: ctx, e: e });
      l = l$115;
      return [$r351, l];
      break $m327$match;
    }
    if ($m327.tag === "And") {
      const operands = $m327.operands;
      const [$r352, l$116] = exprs({ fuel: $rt.int.sub(fuel, 1, $ob74), l: l, ctx: ctx, es: operands });
      l = l$116;
      return [{ tag: "And", operands: $r352 }, l];
      break $m327$match;
    }
    if ($m327.tag === "Or") {
      const operands = $m327.operands;
      const [$r354, l$117] = exprs({ fuel: $rt.int.sub(fuel, 1, $ob75), l: l, ctx: ctx, es: operands });
      l = l$117;
      return [{ tag: "Or", operands: $r354 }, l];
      break $m327$match;
    }
    if ($m327.tag === "Is") {
      const expr = $m327.expr;
      const pattern = $m327.pattern;
      const [$r356, l$118] = expr_of({ fuel: $rt.int.sub(fuel, 1, $ob76), l: l, ctx: ctx, e: expr, discard: false });
      l = l$118;
      const subject = $r356;
      const [$r358, l$119] = pattern_test_of({ fuel: $rt.int.sub(fuel, 1, $ob77), l: l, ctx: ctx, p: pattern, subject: subject, ty: type_of_expr({ l: l, ctx: ctx, e: expr }) });
      l = l$119;
      const $m357 = $r358;
      $m357$match: {
        if ($m357.tag === "Some") {
          const value = $m357.value;
          return [value, l];
          break $m357$match;
        }
        if ($m357.tag === "None") {
          return [{ tag: "BoolE", v: true }, l];
          break $m357$match;
        }
        $rt.unreachable();
      }
      break $m327$match;
    }
    $rt.unreachable();
  }
}

export function exprs({ fuel, l, ctx, es }) {
  if (fuel === 0) {
    return [[], l];
  }
  let out = $std_list.builder({  });
  for (const x of es) {
    const [$r360, l$120] = expr({ fuel: $rt.int.sub(fuel, 1, $ob78), l: l, ctx: ctx, e: x, discard: false });
    l = l$120;
    const [, out$121] = $std_list.push({ b: out, x: $r360 });
    out = out$121;
  }
  return [$std_list.finish({ b: out }), l];
}

export function fn_value({ ctx, def }) {
  const $m361 = $std_map.find({ d: ctx.signatures, key: def.id });
  $m361$match: {
    if ($m361.tag === "None") {
      return { tag: "Local", name: def.name, ty: { tag: "ErrorT" } };
      break $m361$match;
    }
    if ($m361.tag === "Some") {
      const value = $m361.value;
      return { tag: "FnRef", def: def, name: emitted_name({ ctx: ctx, def: def }), sig: value };
      break $m361$match;
    }
    $rt.unreachable();
  }
}

export function name_expr({ l, ctx, e }) {
  const ty = type_of_expr({ l: l, ctx: ctx, e: e });
  let source = "";
  const $m365 = e;
  $m365$match: {
    if ($m365.tag === "Name") {
      const name = $m365.name;
      source = name.text;
      break $m365$match;
    }
    if (true) {
      skip({  });
      break $m365$match;
    }
    $rt.unreachable();
  }
  const def = ref_def({ ctx: ctx, k: expr_key({ l: l, e: e }) });
  if (def < 0) {
    return { tag: "Local", name: source, ty: ty };
  }
  const $m367 = $std_map.find({ d: l.subst, key: def });
  $m367$match: {
    if ($m367.tag === "Some") {
      const value = $m367.value;
      return value;
      break $m367$match;
    }
    if ($m367.tag === "None") {
      skip({  });
      break $m367$match;
    }
    $rt.unreachable();
  }
  const d = $context.get_def({ ctx: ctx, id: def });
  if ($rt.eq(d.kind, { tag: "Fn" })) {
    return fn_value({ ctx: ctx, def: d });
  }
  if ($rt.eq(d.kind, { tag: "Const" })) {
    return { tag: "Global", def: d, ty: ty };
  }
  if ($rt.eq(d.kind, { tag: "Field" })) {
    const $m372 = l.field_object;
    $m372$match: {
      if ($m372.tag === "Some") {
        const value = $m372.value;
        if (value.record_def === or_neg({ o: d.parent })) {
          return { tag: "FieldE", object: { tag: "Local", name: value.name, ty: value.ty }, name: d.name, ty: ty, owner: { tag: "Some", value: $context.get_def({ ctx: ctx, id: value.record_def }) } };
        }
        break $m372$match;
      }
      if ($m372.tag === "None") {
        skip({  });
        break $m372$match;
      }
      $rt.unreachable();
    }
  }
  return { tag: "Local", name: d.name, ty: ty };
}

export function type_info_of({ ctx, owner }) {
  const $m377 = owner;
  $m377$match: {
    if ($m377.tag === "PrimOwner") {
      const name = $m377.name;
      return { tag: "TypeInfoE", name: name, fields: [] };
      break $m377$match;
    }
    if ($m377.tag === "DefOwner") {
      const def = $m377.def;
      let fields = $std_list.builder({  });
      for (const f of fields_or({ o: $std_map.find({ d: ctx.fields, key: def }) })) {
        const [, fields$122] = $std_list.push({ b: fields, x: { name: f.name, type_name: type_slug({ fuel: 1000, ctx: ctx, t: f.ty }) } });
        fields = fields$122;
      }
      return { tag: "TypeInfoE", name: $context.get_def({ ctx: ctx, id: def }).name, fields: $std_list.finish({ b: fields }) };
      break $m377$match;
    }
    $rt.unreachable();
  }
}

export function ctor({ fuel, l, ctx, e }) {
  if (fuel === 0) {
    return [{ tag: "UnitE" }, l];
  }
  const $m382 = e;
  $m382$match: {
    if ($m382.tag === "Ctor") {
      const args = $m382.args;
      const fields = $m382.fields;
      const span = $m382.span;
      const k = key({ l: l, tag: $defs.tag_expr, span: span });
      const $m383 = $std_map.find({ d: ctx.refs, key: k });
      $m383$match: {
        if ($m383.tag === "None") {
          return [{ tag: "UnitE" }, l];
          break $m383$match;
        }
        if ($m383.tag === "Some") {
          const value = $m383.value;
          const $m385 = value;
          $m385$match: {
            if ($m385.tag === "UnitRes") {
              return [{ tag: "UnitE" }, l];
              break $m385$match;
            }
            if ($m385.tag === "TypeValueRes") {
              const owner = $m385.owner;
              return [type_info_of({ ctx: ctx, owner: owner }), l];
              break $m385$match;
            }
            if ($m385.tag === "DefRes") {
              const def = $m385.def;
              const d = $context.get_def({ ctx: ctx, id: def });
              const ty = type_of_expr({ l: l, ctx: ctx, e: e });
              let inits = $std_list.builder({  });
              for (const a of args_list({ o: args })) {
                const [, inits$123] = $std_list.push({ b: inits, x: { name: a.name.text, value: a.value } });
                inits = inits$123;
              }
              for (const f of field_inits({ o: fields })) {
                const [, inits$124] = $std_list.push({ b: inits, x: { name: f.name.text, value: f.value } });
                inits = inits$124;
              }
              const init_list = $std_list.finish({ b: inits });
              let lowered = $std_list.builder({  });
              for (const i of init_list) {
                const [$r389, l$125] = expr({ fuel: $rt.int.sub(fuel, 1, $ob79), l: l, ctx: ctx, e: i.value, discard: false });
                l = l$125;
                const [, lowered$126] = $std_list.push({ b: lowered, x: { name: i.name, value: $r389 } });
                lowered = lowered$126;
              }
              let literal = { tag: "RecordE", def: d, ty: ty, fields: $std_list.finish({ b: lowered }) };
              if ($rt.eq(d.kind, { tag: "Variant" })) {
                literal = { tag: "VariantE", def: d, ty: ty, fields: $std_list.finish({ b: lowered }) };
              }
              const [$r394, l$127] = field_checks({ fuel: $rt.int.sub(fuel, 1, $ob80), l: l, ctx: ctx, def: d, literal: literal, ty: ty, inits: init_list });
              l = l$127;
              return [$r394, l];
              break $m385$match;
            }
            if (true) {
              return [{ tag: "UnitE" }, l];
              break $m385$match;
            }
            $rt.unreachable();
          }
          break $m383$match;
        }
        $rt.unreachable();
      }
      break $m382$match;
    }
    if (true) {
      return [{ tag: "UnitE" }, l];
      break $m382$match;
    }
    $rt.unreachable();
  }
}

export function args_list({ o }) {
  const $m397 = o;
  $m397$match: {
    if ($m397.tag === "Some") {
      const value = $m397.value;
      return value;
      break $m397$match;
    }
    if ($m397.tag === "None") {
      return [];
      break $m397$match;
    }
    $rt.unreachable();
  }
}

export function field_inits({ o }) {
  const $m398 = o;
  $m398$match: {
    if ($m398.tag === "Some") {
      const value = $m398.value;
      return value;
      break $m398$match;
    }
    if ($m398.tag === "None") {
      return [];
      break $m398$match;
    }
    $rt.unreachable();
  }
}

export function record_update({ fuel, l, ctx, e }) {
  if (fuel === 0) {
    return [{ tag: "UnitE" }, l];
  }
  const $m400 = e;
  $m400$match: {
    if ($m400.tag === "RecordUpdate") {
      const base = $m400.base;
      const fields = $m400.fields;
      const ty = type_of_expr({ l: l, ctx: ctx, e: e });
      const [$r401, l$128] = expr({ fuel: $rt.int.sub(fuel, 1, $ob81), l: l, ctx: ctx, e: base, discard: false });
      l = l$128;
      const base_ir = $r401;
      const s = $types.strip({ t: type_of_expr({ l: l, ctx: ctx, e: base }) });
      let inits = $std_list.builder({  });
      for (const f of fields) {
        const [, inits$129] = $std_list.push({ b: inits, x: { name: f.name.text, value: f.value } });
        inits = inits$129;
      }
      const init_list = $std_list.finish({ b: inits });
      let lowered = $std_list.builder({  });
      for (const i of init_list) {
        const [$r403, l$130] = expr({ fuel: $rt.int.sub(fuel, 1, $ob82), l: l, ctx: ctx, e: i.value, discard: false });
        l = l$130;
        const [, lowered$131] = $std_list.push({ b: lowered, x: { name: i.name, value: $r403 } });
        lowered = lowered$131;
      }
      if (!(s.tag === "RecordT")) {
        const module_def = $context.get_def({ ctx: ctx, id: def_at({ l: l, ctx: ctx, tag: $defs.tag_item, span: l.m.tree.span }) });
        return [{ tag: "Update", base: base_ir, def: module_def, ty: ty, fields: $std_list.finish({ b: lowered }) }, l];
      }
      const d = $context.get_def({ ctx: ctx, id: $types.def_of({ t: s }) });
      const [$r407, l$132] = field_checks({ fuel: $rt.int.sub(fuel, 1, $ob83), l: l, ctx: ctx, def: d, literal: { tag: "Update", base: base_ir, def: d, ty: ty, fields: $std_list.finish({ b: lowered }) }, ty: ty, inits: init_list });
      l = l$132;
      return [$r407, l];
      break $m400$match;
    }
    if (true) {
      return [{ tag: "UnitE" }, l];
      break $m400$match;
    }
    $rt.unreachable();
  }
}

export function field_checks({ fuel, l, ctx, def, literal, ty, inits }) {
  if (fuel === 0) {
    return [{ tag: "UnitE" }, l];
  }
  const fs = fields_or({ o: $std_map.find({ d: ctx.fields, key: def.id }) });
  let checks = $std_list.builder({  });
  const [$r410, l$133] = tmp({ l: l, prefix: "v" });
  l = l$133;
  const v = $r410;
  const object = { tag: "Local", name: v, ty: ty };
  const saved = l.field_object;
  l = { ...l, field_object: { tag: "Some", value: { record_def: def.id, name: v, ty: ty } } };
  for (const init of inits) {
    const $m415 = find_ob({ ctx: ctx, k: expr_key({ l: l, e: init.value }), kind: { tag: "Refinement" } });
    $m415$match: {
      if ($m415.tag === "None") {
        skip({  });
        break $m415$match;
      }
      if ($m415.tag === "Some") {
        const value = $m415.value;
        if (is_checked({ o: value })) {
          const fo = find_field({ fs: fs, name: init.name });
          if (fo.tag === "Some") {
            const field = field_or({ o: fo });
            const it_expr = { tag: "FieldE", object: object, name: field.name, ty: field.ty, owner: { tag: "Some", value: def } };
            for (const p of refinement_preds({ ctx: ctx, t: field.ty })) {
              const [$r419, l$134] = with_it({ fuel: $rt.int.sub(fuel, 1, $ob84), l: l, ctx: ctx, it_expr: it_expr, pred: p });
              l = l$134;
              const [, checks$135] = $std_list.push({ b: checks, x: { tag: "Check", cond: $r419, ob: ob_ref_of({ l: l, ctx: ctx, o: value }) } });
              checks = checks$135;
            }
          }
        }
        break $m415$match;
      }
      $rt.unreachable();
    }
  }
  l = { ...l, field_object: saved };
  if ($std_list.built({ b: checks }) === 0) {
    return [literal, l];
  }
  return [{ tag: "Checked", value: literal, it_name: v, ty: ty, checks: $std_list.finish({ b: checks }) }, l];
}

export function field_or({ o }) {
  const $m423 = o;
  $m423$match: {
    if ($m423.tag === "Some") {
      const value = $m423.value;
      return value;
      break $m423$match;
    }
    if ($m423.tag === "None") {
      return { def: $rt.int.neg(1, $ob85), name: "", ty: { tag: "ErrorT" } };
      break $m423$match;
    }
    $rt.unreachable();
  }
}

export function try_expr({ fuel, l, ctx, e }) {
  if (fuel === 0) {
    return [{ tag: "UnitE" }, l];
  }
  const $m427 = e;
  $m427$match: {
    if ($m427.tag === "Try") {
      const expr = $m427.expr;
      const else_ = $m427.else_;
      const [$r428, l$136] = expr_of({ fuel: $rt.int.sub(fuel, 1, $ob86), l: l, ctx: ctx, e: expr, discard: false });
      l = l$136;
      const operand = $r428;
      const option = is_option({ ctx: ctx, t: type_of_expr({ l: l, ctx: ctx, e: expr }) });
      let outer_option = false;
      const $m429 = l.fn_ctx;
      $m429$match: {
        if ($m429.tag === "Some") {
          const value = $m429.value;
          outer_option = is_option({ ctx: ctx, t: value.ret });
          break $m429$match;
        }
        if ($m429.tag === "None") {
          skip({  });
          break $m429$match;
        }
        $rt.unreachable();
      }
      const ty = type_of_expr({ l: l, ctx: ctx, e: e });
      const $m430 = else_;
      $m430$match: {
        if ($m430.tag === "None") {
          return [{ tag: "Try", operand: operand, option: option, outer_option: outer_option, else_clause: { tag: "None" }, raw: false, ty: ty }, l];
          break $m430$match;
        }
        if ($m430.tag === "Some") {
          const value = $m430.value;
          let name = { tag: "None" };
          let error_type = { tag: "ErrorT" };
          if (value.name.text !== "_") {
            name = { tag: "Some", value: value.name.text };
            error_type = type_or({ o: $std_map.find({ d: ctx.decl_types, key: def_at({ l: l, ctx: ctx, tag: $defs.tag_misc, span: value.span }) }) });
          }
          const [$r436, l$137] = expr_of({ fuel: $rt.int.sub(fuel, 1, $ob87), l: l, ctx: ctx, e: value.expr, discard: false });
          l = l$137;
          const else_value = $r436;
          return [{ tag: "Try", operand: operand, option: option, outer_option: outer_option, else_clause: { tag: "Some", value: { name: name, error_type: error_type, value: else_value } }, raw: l.verifying, ty: ty }, l];
          break $m430$match;
        }
        $rt.unreachable();
      }
      break $m427$match;
    }
    if (true) {
      return [{ tag: "UnitE" }, l];
      break $m427$match;
    }
    $rt.unreachable();
  }
}

export function recover_expr({ fuel, l, ctx, e }) {
  if (fuel === 0) {
    return [{ tag: "UnitE" }, l];
  }
  const $m442 = e;
  $m442$match: {
    if ($m442.tag === "Recover") {
      const body = $m442.body;
      const saved = l.fn_ctx;
      const $m443 = saved;
      $m443$match: {
        if ($m443.tag === "Some") {
          const value = $m443.value;
          l = { ...l, fn_ctx: { tag: "Some", value: { ...value, ret: { tag: "ErrorT" }, post: [], inout_names: [] } } };
          break $m443$match;
        }
        if ($m443.tag === "None") {
          skip({  });
          break $m443$match;
        }
        $rt.unreachable();
      }
      const n = $std_list.len({ xs: body.stmts });
      let out = [];
      let value_ir = { tag: "UnitE" };
      let i = 0;
      for (const s of body.stmts) {
        let last_expr = { tag: "None" };
        if (i === $rt.int.sub(n, 1, $ob88)) {
          const $m450 = s;
          $m450$match: {
            if ($m450.tag === "ExprStmt") {
              const expr = $m450.expr;
              last_expr = { tag: "Some", value: expr };
              break $m450$match;
            }
            if (true) {
              skip({  });
              break $m450$match;
            }
            $rt.unreachable();
          }
        }
        const $m452 = last_expr;
        $m452$match: {
          if ($m452.tag === "Some") {
            const value = $m452.value;
            const [$r453, l$138] = begin_collect({ l: l });
            l = l$138;
            const saved_pre = $r453;
            const [$r454, l$139] = expr({ fuel: $rt.int.sub(fuel, 1, $ob89), l: l, ctx: ctx, e: value, discard: false });
            l = l$139;
            value_ir = $r454;
            const [$r455, l$140] = end_collect({ l: l, saved: saved_pre, out: [] });
            l = l$140;
            out = [...out, ...$r455];
            break $m452$match;
          }
          if ($m452.tag === "None") {
            const [$r456, l$141] = stmt({ fuel: $rt.int.sub(fuel, 1, $ob90), l: l, ctx: ctx, s: s });
            l = l$141;
            out = [...out, ...$r456];
            break $m452$match;
          }
          $rt.unreachable();
        }
        i = $rt.int.add(i, 1, $ob91);
      }
      l = { ...l, fn_ctx: saved };
      return [{ tag: "Recover", body: out, value: value_ir, ty: type_of_expr({ l: l, ctx: ctx, e: e }) }, l];
      break $m442$match;
    }
    if (true) {
      return [{ tag: "UnitE" }, l];
      break $m442$match;
    }
    $rt.unreachable();
  }
}

export function quantifier({ fuel, l, ctx, e }) {
  if (fuel === 0) {
    return [{ tag: "UnitE" }, l];
  }
  const $m461 = e;
  $m461$match: {
    if ($m461.tag === "Quantifier") {
      const quant = $m461.quant;
      const domain = $m461.domain;
      const where_ = $m461.where_;
      const body = $m461.body;
      const span = $m461.span;
      const def = $context.get_def({ ctx: ctx, id: def_at({ l: l, ctx: ctx, tag: $defs.tag_expr, span: span }) });
      const binder = type_or({ o: $std_map.find({ d: ctx.decl_types, key: def.id }) });
      let dom = { tag: "BoolsD" };
      const $m463 = domain;
      $m463$match: {
        if ($m463.tag === "None") {
          dom = { tag: "BoolsD" };
          break $m463$match;
        }
        if ($m463.tag === "Some") {
          const value = $m463.value;
          const $m465 = value;
          $m465$match: {
            if ($m465.tag === "RangeDomain") {
              const lo = $m465.lo;
              const hi = $m465.hi;
              const [$r466, l$142] = expr({ fuel: $rt.int.sub(fuel, 1, $ob92), l: l, ctx: ctx, e: lo, discard: false });
              l = l$142;
              const [$r467, l$143] = expr({ fuel: $rt.int.sub(fuel, 1, $ob93), l: l, ctx: ctx, e: hi, discard: false });
              l = l$143;
              dom = { tag: "RangeD", lo: $r466, hi: $r467 };
              break $m465$match;
            }
            if ($m465.tag === "InDomain") {
              const expr = $m465.expr;
              const s = $types.strip({ t: type_of_expr({ l: l, ctx: ctx, e: expr }) });
              const [$r469, l$144] = expr_of({ fuel: $rt.int.sub(fuel, 1, $ob94), l: l, ctx: ctx, e: expr, discard: false });
              l = l$144;
              const list = $r469;
              if (s.tag === "UnionT") {
                dom = { tag: "OkListD", expr: list };
              } else {
                dom = { tag: "ListD", expr: list };
              }
              break $m465$match;
            }
            $rt.unreachable();
          }
          break $m463$match;
        }
        $rt.unreachable();
      }
      let where_ir = { tag: "None" };
      const $m473 = where_;
      $m473$match: {
        if ($m473.tag === "Some") {
          const value = $m473.value;
          const [$r474, l$145] = expr({ fuel: $rt.int.sub(fuel, 1, $ob95), l: l, ctx: ctx, e: value, discard: false });
          l = l$145;
          where_ir = { tag: "Some", value: $r474 };
          break $m473$match;
        }
        if ($m473.tag === "None") {
          skip({  });
          break $m473$match;
        }
        $rt.unreachable();
      }
      const [$r476, l$146] = expr({ fuel: $rt.int.sub(fuel, 1, $ob96), l: l, ctx: ctx, e: body, discard: false });
      l = l$146;
      return [{ tag: "Quantifier", quant: quant, name: def.name, binder: binder, domain: dom, where_: where_ir, body: $r476 }, l];
      break $m461$match;
    }
    if (true) {
      return [{ tag: "UnitE" }, l];
      break $m461$match;
    }
    $rt.unreachable();
  }
}

export function closure({ fuel, l, ctx, e }) {
  if (fuel === 0) {
    return [{ tag: "UnitE" }, l];
  }
  const $m480 = e;
  $m480$match: {
    if ($m480.tag === "Closure") {
      const params = $m480.params;
      const body = $m480.body;
      const ty = type_of_expr({ l: l, ctx: ctx, e: e });
      let fn_type = { tag: "None" };
      let ret = { tag: "ErrorT" };
      if (ty.tag === "FnT") {
        fn_type = { tag: "Some", value: ty };
        ret = $types.ret_of({ t: ty });
      }
      let ps = $std_list.builder({  });
      let inout_names = $std_list.builder({  });
      for (const p of params) {
        const pd = def_at({ l: l, ctx: ctx, tag: $defs.tag_sig, span: p.span });
        let pt = { tag: "ErrorT" };
        if (pd >= 0) {
          pt = type_or({ o: $std_map.find({ d: ctx.decl_types, key: pd }) });
        }
        const [, ps$147] = $std_list.push({ b: ps, x: { name: p.name.text, ty: pt, is_inout: p.is_inout } });
        ps = ps$147;
        if (p.is_inout) {
          const [, inout_names$148] = $std_list.push({ b: inout_names, x: p.name.text });
          inout_names = inout_names$148;
        }
      }
      const param_list = $std_list.finish({ b: ps });
      const saved = l.fn_ctx;
      let outer_def = { tag: "None" };
      let outer_dicts = $std_map.dict({  });
      let outer_measure = { tag: "None" };
      const $m488 = saved;
      $m488$match: {
        if ($m488.tag === "Some") {
          const value = $m488.value;
          outer_def = value.def;
          outer_dicts = value.dicts;
          outer_measure = value.measure;
          break $m488$match;
        }
        if ($m488.tag === "None") {
          skip({  });
          break $m488$match;
        }
        $rt.unreachable();
      }
      const cctx = { inout_names: $std_list.finish({ b: inout_names }), ret: ret, post: [], def: outer_def, dicts: outer_dicts, result_expr: { tag: "None" }, measure: outer_measure };
      l = { ...l, fn_ctx: { tag: "Some", value: cctx } };
      let entry = [];
      let def_name = "closure";
      const $m493 = outer_def;
      $m493$match: {
        if ($m493.tag === "Some") {
          const value = $m493.value;
          def_name = value.name;
          break $m493$match;
        }
        if ($m493.tag === "None") {
          skip({  });
          break $m493$match;
        }
        $rt.unreachable();
      }
      let i = 0;
      for (const p of params) {
        if (i < $std_list.len({ xs: param_list })) {
          const param = $std_list.get({ xs: param_list, i: i });
          const local = { tag: "Local", name: param.name, ty: param.ty };
          for (const pred of refinement_preds({ ctx: ctx, t: param.ty })) {
            const [$r495, l$149] = begin_collect({ l: l });
            l = l$149;
            const saved_pre = $r495;
            const [$r496, l$150] = with_it({ fuel: $rt.int.sub(fuel, 1, $ob97), l: l, ctx: ctx, it_expr: local, pred: pred });
            l = l$150;
            const cond = $r496;
            const [$r498, l$151] = end_collect({ l: l, saved: saved_pre, out: [{ tag: "Check", cond: cond, ob: ob_ref({ l: l, ctx: ctx, kind: "refinement", text: $printer.print_expr({ e: pred.e }), file: l.file, span: p.span, def: def_name }) }] });
            l = l$151;
            entry = [...entry, ...$r498];
          }
        }
        i = $rt.int.add(i, 1, $ob98);
      }
      const [$r499, l$152] = block({ fuel: $rt.int.sub(fuel, 1, $ob99), l: l, ctx: ctx, b: body });
      l = l$152;
      let body_ir = $r499;
      if ($std_list.built({ b: inout_names }) > 0 && fn_type.tag === "Some" && is_unit({ t: ret })) {
        body_ir = $std_list.append({ xs: body_ir, x: { tag: "Return", value: { tag: "UnitE" } } });
      }
      l = { ...l, fn_ctx: saved };
      return [{ tag: "Closure", params: param_list, fn_type: fn_type, entry_checks: entry, body: body_ir, early_return: block_has_try({ b: body }) }, l];
      break $m480$match;
    }
    if (true) {
      return [{ tag: "UnitE" }, l];
      break $m480$match;
    }
    $rt.unreachable();
  }
}

export function fake_expr({ fuel, l, ctx, e }) {
  if (fuel === 0) {
    return [{ tag: "UnitE" }, l];
  }
  const $m506 = e;
  $m506$match: {
    if ($m506.tag === "Fake") {
      const fields = $m506.fields;
      const span = $m506.span;
      const def = ref_def({ ctx: ctx, k: key({ l: l, tag: $defs.tag_expr, span: span }) });
      let kind = "capability";
      if (def >= 0) {
        kind = $context.get_def({ ctx: ctx, id: def }).name;
      }
      let out = $std_list.builder({  });
      for (const f of fields) {
        const [$r507, l$153] = expr({ fuel: $rt.int.sub(fuel, 1, $ob100), l: l, ctx: ctx, e: f.value, discard: false });
        l = l$153;
        const [, out$154] = $std_list.push({ b: out, x: { name: f.name.text, value: $r507 } });
        out = out$154;
      }
      return [{ tag: "Fake", kind: kind, fields: $std_list.finish({ b: out }), ty: type_of_expr({ l: l, ctx: ctx, e: e }) }, l];
      break $m506$match;
    }
    if (true) {
      return [{ tag: "UnitE" }, l];
      break $m506$match;
    }
    $rt.unreachable();
  }
}

export function field_access({ fuel, l, ctx, e }) {
  if (fuel === 0) {
    return [{ tag: "UnitE" }, l];
  }
  const $m512 = e;
  $m512$match: {
    if ($m512.tag === "FieldAccess") {
      const object = $m512.object;
      const name = $m512.name;
      const span = $m512.span;
      const ty = type_of_expr({ l: l, ctx: ctx, e: e });
      const $m513 = $std_map.find({ d: ctx.refs, key: key({ l: l, tag: $defs.tag_expr, span: span }) });
      $m513$match: {
        if ($m513.tag === "Some") {
          const value = $m513.value;
          const $m514 = value;
          $m514$match: {
            if ($m514.tag === "DefRes") {
              const def = $m514.def;
              const d = $context.get_def({ ctx: ctx, id: def });
              if ($rt.eq(d.kind, { tag: "Fn" })) {
                return [fn_value({ ctx: ctx, def: d }), l];
              }
              return [{ tag: "Global", def: d, ty: ty }, l];
              break $m514$match;
            }
            if ($m514.tag === "CompanionRes") {
              const fn_def = $m514.fn_def;
              return [fn_value({ ctx: ctx, def: $context.get_def({ ctx: ctx, id: fn_def }) }), l];
              break $m514$match;
            }
            if ($m514.tag === "UnitRes") {
              return [{ tag: "UnitE" }, l];
              break $m514$match;
            }
            if ($m514.tag === "TypeValueRes") {
              const owner = $m514.owner;
              return [type_info_of({ ctx: ctx, owner: owner }), l];
              break $m514$match;
            }
            if (true) {
              return [{ tag: "UnitE" }, l];
              break $m514$match;
            }
            $rt.unreachable();
          }
          break $m513$match;
        }
        if ($m513.tag === "None") {
          skip({  });
          break $m513$match;
        }
        $rt.unreachable();
      }
      const s = $types.strip({ t: type_of_expr({ l: l, ctx: ctx, e: object }) });
      let owner = { tag: "None" };
      if (s.tag === "RecordT") {
        owner = { tag: "Some", value: $context.get_def({ ctx: ctx, id: $types.def_of({ t: s }) }) };
      }
      const [$r521, l$155] = expr({ fuel: $rt.int.sub(fuel, 1, $ob101), l: l, ctx: ctx, e: object, discard: false });
      l = l$155;
      return [{ tag: "FieldE", object: $r521, name: name.text, ty: ty, owner: owner }, l];
      break $m512$match;
    }
    if (true) {
      return [{ tag: "UnitE" }, l];
      break $m512$match;
    }
    $rt.unreachable();
  }
}

export function find_arg({ args, name }) {
  for (const a of args) {
    if (a.name.text === name) {
      return { tag: "Some", value: a };
    }
  }
  return { tag: "None" };
}

export function call({ fuel, l, ctx, e, discard }) {
  if (fuel === 0) {
    return [{ tag: "UnitE" }, l];
  }
  const $m527 = e;
  $m527$match: {
    if ($m527.tag === "Call") {
      const callee = $m527.callee;
      const args = $m527.args;
      const span = $m527.span;
      const callee_key = key({ l: l, tag: $defs.tag_expr, span: $parser.span_of_expr({ e: callee }) });
      const call_key = key({ l: l, tag: $defs.tag_expr, span: span });
      const ty = type_of_expr({ l: l, ctx: ctx, e: e });
      let target_id = $rt.int.neg(1, $ob102);
      let via_iface = $rt.int.neg(1, $ob103);
      const $m528 = $std_map.find({ d: ctx.refs, key: callee_key });
      $m528$match: {
        if ($m528.tag === "Some") {
          const value = $m528.value;
          const $m529 = value;
          $m529$match: {
            if ($m529.tag === "DefRes") {
              const def = $m529.def;
              target_id = def;
              break $m529$match;
            }
            if ($m529.tag === "CompanionRes") {
              const fn_def = $m529.fn_def;
              target_id = fn_def;
              break $m529$match;
            }
            if ($m529.tag === "IfaceFnRes") {
              const iface = $m529.iface;
              const fn_def = $m529.fn_def;
              target_id = fn_def;
              via_iface = iface;
              break $m529$match;
            }
            if (true) {
              skip({  });
              break $m529$match;
            }
            $rt.unreachable();
          }
          break $m528$match;
        }
        if ($m528.tag === "None") {
          skip({  });
          break $m528$match;
        }
        $rt.unreachable();
      }
      if (target_id >= 0) {
        const target = $context.get_def({ ctx: ctx, id: target_id });
        if ($rt.eq(target.kind, { tag: "Fn" }) || $rt.eq(target.kind, { tag: "IfaceFn" })) {
          const $m532 = $std_map.find({ d: ctx.signatures, key: target.id });
          $m532$match: {
            if ($m532.tag === "None") {
              return [{ tag: "UnitE" }, l];
              break $m532$match;
            }
            if ($m532.tag === "Some") {
              const value = $m532.value;
              const [$r534, l$156] = named_call({ fuel: $rt.int.sub(fuel, 1, $ob104), l: l, ctx: ctx, target: target, via_iface: via_iface, sig: value, args: args, call_key: call_key, call_ty: ty, discard: discard, e: e });
              l = l$156;
              return [$r534, l];
              break $m532$match;
            }
            $rt.unreachable();
          }
        }
      }
      const [$r535, l$157] = expr({ fuel: $rt.int.sub(fuel, 1, $ob105), l: l, ctx: ctx, e: callee, discard: false });
      l = l$157;
      const callee_ir = $r535;
      const ct = $types.strip({ t: type_of_expr({ l: l, ctx: ctx, e: callee }) });
      if (!(ct.tag === "FnT")) {
        return [{ tag: "UnitE" }, l];
      }
      let ordered = $std_list.builder({  });
      let inout_args = $std_list.builder({  });
      for (const p of $types.params_of({ t: ct })) {
        const a = find_arg({ args: args, name: p.name });
        const $m537 = a;
        $m537$match: {
          if ($m537.tag === "Some") {
            const value = $m537.value;
            const [$r538, l$158] = expr({ fuel: $rt.int.sub(fuel, 1, $ob106), l: l, ctx: ctx, e: value.value, discard: false });
            l = l$158;
            const [, ordered$159] = $std_list.push({ b: ordered, x: $r538 });
            ordered = ordered$159;
            break $m537$match;
          }
          if ($m537.tag === "None") {
            const [, ordered$160] = $std_list.push({ b: ordered, x: { tag: "UnitE" } });
            ordered = ordered$160;
            break $m537$match;
          }
          $rt.unreachable();
        }
        if (p.is_inout) {
          const [, inout_args$161] = $std_list.push({ b: inout_args, x: a });
          inout_args = inout_args$161;
        }
      }
      const call_ir = { tag: "CallValue", callee: callee_ir, fn_type: ct, args: $std_list.finish({ b: ordered }), ty: ty };
      if ($std_list.built({ b: inout_args }) === 0) {
        return [call_ir, l];
      }
      const [$r541, l$162] = inout_call({ l: l, ctx: ctx, call_ir: call_ir, ty: ty, args: $std_list.finish({ b: inout_args }), discard: discard });
      l = l$162;
      return [$r541, l];
      break $m527$match;
    }
    if (true) {
      return [{ tag: "UnitE" }, l];
      break $m527$match;
    }
    $rt.unreachable();
  }
}

export function named_call({ fuel, l, ctx, target, via_iface, sig, args, call_key, call_ty, discard, e }) {
  if (fuel === 0) {
    return [{ tag: "UnitE" }, l];
  }
  let call_target = { tag: "FnTarget", def: target, name: emitted_name({ ctx: ctx, def: target }) };
  if (via_iface >= 0) {
    call_target = dispatch({ l: l, ctx: ctx, iface: via_iface, fn_def: target, call_key: call_key, e: e });
  } else {
    if ($rt.eq(target.kind, { tag: "IfaceFn" })) {
      const $m546 = $std_map.find({ d: l.impl_fns, key: target.id });
      $m546$match: {
        if ($m546.tag === "Some") {
          const value = $m546.value;
          call_target = { tag: "FnTarget", def: value.def, name: value.name };
          break $m546$match;
        }
        if ($m546.tag === "None") {
          call_target = { tag: "FnTarget", def: target, name: target.name };
          break $m546$match;
        }
        $rt.unreachable();
      }
    }
  }
  const targs = args_or({ o: $std_map.find({ d: ctx.instantiations, key: call_key }) });
  let dicts = $std_list.builder({  });
  let consts = $std_list.builder({  });
  let i = 0;
  for (const p of sig.tparams) {
    let a = { tag: "None" };
    if (i < $std_list.len({ xs: targs })) {
      a = { tag: "Some", value: $std_list.get({ xs: targs, i: i }) };
    }
    const $m551 = p;
    $m551$match: {
      if ($m551.tag === "TypeP") {
        const bound = $m551.bound;
        const $m552 = bound;
        $m552$match: {
          if ($m552.tag === "Some") {
            const value = $m552.value;
            let bound_type = { tag: "None" };
            if (a.tag === "Some") {
              const $m554 = arg_or({ o: a });
              $m554$match: {
                if ($m554.tag === "TypeA") {
                  const ty = $m554.ty;
                  bound_type = { tag: "Some", value: ty };
                  break $m554$match;
                }
                if (true) {
                  skip({  });
                  break $m554$match;
                }
                $rt.unreachable();
              }
            }
            if (bound_type.tag === "Some") {
              const [, dicts$163] = $std_list.push({ b: dicts, x: dict_for({ l: l, ctx: ctx, iface: value, target: type_or({ o: bound_type }) }) });
              dicts = dicts$163;
            } else {
              const [, dicts$164] = $std_list.push({ b: dicts, x: { tag: "UnitE" } });
              dicts = dicts$164;
            }
            break $m552$match;
          }
          if ($m552.tag === "None") {
            skip({  });
            break $m552$match;
          }
          $rt.unreachable();
        }
        break $m551$match;
      }
      if ($m551.tag === "ConstP") {
        let pushed = false;
        if (a.tag === "Some") {
          const $m557 = arg_or({ o: a });
          $m557$match: {
            if ($m557.tag === "ConstA") {
              const value = $m557.value;
              const [, consts$165] = $std_list.push({ b: consts, x: const_literal({ ctx: ctx, v: value }) });
              consts = consts$165;
              pushed = true;
              break $m557$match;
            }
            if (true) {
              skip({  });
              break $m557$match;
            }
            $rt.unreachable();
          }
        }
        if (!pushed) {
          const [, consts$166] = $std_list.push({ b: consts, x: { tag: "UnitE" } });
          consts = consts$166;
        }
        break $m551$match;
      }
      if (true) {
        skip({  });
        break $m551$match;
      }
      $rt.unreachable();
    }
    i = $rt.int.add(i, 1, $ob107);
  }
  let arg_exprs = $std_list.builder({  });
  let inout_args = $std_list.builder({  });
  for (const p of sig.params) {
    const a = find_arg({ args: args, name: p.name });
    const $m559 = a;
    $m559$match: {
      if ($m559.tag === "Some") {
        const value = $m559.value;
        const [$r560, l$167] = expr({ fuel: $rt.int.sub(fuel, 1, $ob108), l: l, ctx: ctx, e: value.value, discard: false });
        l = l$167;
        const [, arg_exprs$168] = $std_list.push({ b: arg_exprs, x: $r560 });
        arg_exprs = arg_exprs$168;
        break $m559$match;
      }
      if ($m559.tag === "None") {
        const [, arg_exprs$169] = $std_list.push({ b: arg_exprs, x: { tag: "UnitE" } });
        arg_exprs = arg_exprs$169;
        break $m559$match;
      }
      $rt.unreachable();
    }
    if (p.is_inout) {
      const [, inout_args$170] = $std_list.push({ b: inout_args, x: a });
      inout_args = inout_args$170;
    }
  }
  const [$r562, l$171] = decreases_check({ fuel: $rt.int.sub(fuel, 1, $ob109), l: l, ctx: ctx, call_key: call_key, sig: sig, args: $std_list.finish({ b: arg_exprs }) });
  l = l$171;
  const arg_list = $r562;
  let decoder = { tag: "None" };
  if ($context.qualified_name({ ctx: ctx, id: target.id }) === "std.sql.select") {
    const [$r564, l$172] = select_decoder({ fuel: $rt.int.sub(fuel, 1, $ob110), l: l, ctx: ctx, call_key: call_key, sig: sig });
    l = l$172;
    decoder = $r564;
  }
  const call_ir = { tag: "Call", target: call_target, sig: sig, dicts: $std_list.finish({ b: dicts }), consts: $std_list.finish({ b: consts }), args: arg_list, ty: call_ty, decoder: decoder };
  if ($std_list.built({ b: inout_args }) === 0) {
    return [call_ir, l];
  }
  const [$r566, l$173] = inout_call({ l: l, ctx: ctx, call_ir: call_ir, ty: call_ty, args: $std_list.finish({ b: inout_args }), discard: discard });
  l = l$173;
  return [$r566, l];
}

export function arg_or({ o }) {
  const $m567 = o;
  $m567$match: {
    if ($m567.tag === "Some") {
      const value = $m567.value;
      return value;
      break $m567$match;
    }
    if ($m567.tag === "None") {
      return { tag: "TypeA", ty: { tag: "ErrorT" } };
      break $m567$match;
    }
    $rt.unreachable();
  }
}

export function decreases_check({ fuel, l, ctx, call_key, sig, args }) {
  if (fuel === 0) {
    return [[], l];
  }
  let ob = { tag: "None" };
  for (const o of obligations_at({ ctx: ctx, k: call_key })) {
    if (ob.tag === "None" && o.kind.tag === "Decreases" && !is_proved({ o: o })) {
      ob = { tag: "Some", value: o };
    }
  }
  let measure = { tag: "None" };
  const $m573 = l.fn_ctx;
  $m573$match: {
    if ($m573.tag === "Some") {
      const value = $m573.value;
      measure = value.measure;
      break $m573$match;
    }
    if ($m573.tag === "None") {
      skip({  });
      break $m573$match;
    }
    $rt.unreachable();
  }
  if (ob.tag === "None" || measure.tag === "None") {
    return [args, l];
  }
  const o = ob_or({ o: ob });
  const src = or_neg({ o: o.source });
  const clause = $std_map.find({ d: ctx.contract_nodes, key: src });
  if (clause.tag === "None") {
    return [args, l];
  }
  let clause_numeric = false;
  const $m574 = clause;
  $m574$match: {
    if ($m574.tag === "Some") {
      const value = $m574.value;
      clause_numeric = is_int_like({ t: type_of_expr({ l: l, ctx: ctx, e: value.expr }) });
      break $m574$match;
    }
    if ($m574.tag === "None") {
      skip({  });
      break $m574$match;
    }
    $rt.unreachable();
  }
  if (!clause_numeric) {
    return [args, l];
  }
  let bound = $std_list.builder({  });
  let subst = $std_map.dict({  });
  let i = 0;
  for (const p of sig.params) {
    let value_ir = { tag: "UnitE" };
    if (i < $std_list.len({ xs: args })) {
      value_ir = $std_list.get({ xs: args, i: i });
    }
    let local = value_ir;
    if (!p.is_inout && !(value_ir.tag === "Local" || value_ir.tag === "IntE" || value_ir.tag === "BoolE" || value_ir.tag === "TextE")) {
      const [$r576, l$174] = tmp({ l: l, prefix: "arg" });
      l = l$174;
      const name = $r576;
      const [, l$175] = push_pre({ l: l, s: { tag: "Let", name: name, ty: p.ty, mutable: false, value: value_ir } });
      l = l$175;
      local = { tag: "Local", name: name, ty: p.ty };
    }
    const [, bound$176] = $std_list.push({ b: bound, x: local });
    bound = bound$176;
    if (i < $std_list.len({ xs: sig.param_defs })) {
      const [, subst$177] = $std_map.set({ d: subst, key: $std_list.get({ xs: sig.param_defs, i: i }), value: local });
      subst = subst$177;
    }
    i = $rt.int.add(i, 1, $ob111);
  }
  const saved_subst = l.subst;
  const saved_file = l.file;
  l = { ...l, subst: subst, file: $lower.pred_file({ k: src }) };
  const [$r580, l$178] = expr({ fuel: $rt.int.sub(fuel, 1, $ob112), l: l, ctx: ctx, e: contract_or({ o: clause }).expr, discard: false });
  l = l$178;
  const callee_measure = $r580;
  l = { ...l, subst: saved_subst, file: saved_file };
  const [, l$179] = push_pre({ l: l, s: { tag: "Check", cond: { tag: "Cmp", op: "<", left: callee_measure, right: { tag: "Local", name: text_or({ o: measure, dflt: "$measure" }), ty: { tag: "Prim", name: "Int" } }, is_float: false }, ob: ob_ref_of({ l: l, ctx: ctx, o: o }) } });
  l = l$179;
  return [$std_list.finish({ b: bound }), l];
}

export function contract_or({ o }) {
  const $m586 = o;
  $m586$match: {
    if ($m586.tag === "Some") {
      const value = $m586.value;
      return value;
      break $m586$match;
    }
    if ($m586.tag === "None") {
      return { clause: "", is_proved: false, expr: { tag: "Hole", span: { start: 0, end: 0 } }, span: { start: 0, end: 0 } };
      break $m586$match;
    }
    $rt.unreachable();
  }
}

export function select_decoder({ fuel, l, ctx, call_key, sig }) {
  if (fuel === 0) {
    return [{ tag: "None" }, l];
  }
  const targs = args_or({ o: $std_map.find({ d: ctx.instantiations, key: call_key }) });
  let index = $rt.int.neg(1, $ob113);
  let i = 0;
  for (const p of sig.tparams) {
    if (index < 0 && p.tag === "TypeP") {
      index = i;
    }
    i = $rt.int.add(i, 1, $ob114);
  }
  if (index < 0 || index >= $std_list.len({ xs: targs })) {
    return [{ tag: "None" }, l];
  }
  let row_type = { tag: "None" };
  const $m594 = $std_list.get({ xs: targs, i: index });
  $m594$match: {
    if ($m594.tag === "TypeA") {
      const ty = $m594.ty;
      row_type = { tag: "Some", value: ty };
      break $m594$match;
    }
    if (true) {
      skip({  });
      break $m594$match;
    }
    $rt.unreachable();
  }
  if (row_type.tag === "None") {
    return [{ tag: "None" }, l];
  }
  const ty = type_or({ o: row_type });
  const s = $types.strip({ t: ty });
  if (!(s.tag === "RecordT")) {
    return [{ tag: "None" }, l];
  }
  const def = $context.get_def({ ctx: ctx, id: $types.def_of({ t: s }) });
  const [$r598, l$180] = tmp({ l: l, prefix: "row" });
  l = l$180;
  const it_name = $r598;
  const object = { tag: "Local", name: it_name, ty: ty };
  let fields = $std_list.builder({  });
  let checks = [];
  const saved = l.field_object;
  l = { ...l, field_object: { tag: "Some", value: { record_def: def.id, name: it_name, ty: ty } } };
  for (const f of fields_or({ o: $std_map.find({ d: ctx.fields, key: def.id }) })) {
    const fs = $types.strip({ t: f.ty });
    let kind = "other";
    if (fs.tag === "Prim") {
      kind = $types.prim_name({ t: fs });
    }
    const [, fields$181] = $std_list.push({ b: fields, x: { name: f.name, kind: kind } });
    fields = fields$181;
    const value_ir = { tag: "FieldE", object: object, name: f.name, ty: f.ty, owner: { tag: "Some", value: def } };
    for (const p of refinement_preds({ ctx: ctx, t: f.ty })) {
      const [$r606, l$182] = begin_collect({ l: l });
      l = l$182;
      const saved_pre = $r606;
      const [$r607, l$183] = with_it({ fuel: $rt.int.sub(fuel, 1, $ob115), l: l, ctx: ctx, it_expr: value_ir, pred: p });
      l = l$183;
      const cond = $r607;
      const [$r609, l$184] = end_collect({ l: l, saved: saved_pre, out: [{ tag: "Reject", cond: cond, column: f.name }] });
      l = l$184;
      checks = [...checks, ...$r609];
    }
  }
  l = { ...l, field_object: saved };
  return [{ tag: "Some", value: { it_name: it_name, ty: ty, fields: $std_list.finish({ b: fields }), checks: checks } }, l];
}

export function inout_call({ l, ctx, call_ir, ty, args, discard }) {
  let targets = $std_list.builder({  });
  for (const a of args) {
    let target = { tag: "None" };
    const $m614 = a;
    $m614$match: {
      if ($m614.tag === "Some") {
        const value = $m614.value;
        const $m615 = value.value;
        $m615$match: {
          if ($m615.tag === "Name") {
            const name = $m615.name;
            target = { tag: "Some", value: { name: name.text, ty: type_of_expr({ l: l, ctx: ctx, e: value.value }) } };
            break $m615$match;
          }
          if (true) {
            skip({  });
            break $m615$match;
          }
          $rt.unreachable();
        }
        break $m614$match;
      }
      if ($m614.tag === "None") {
        skip({  });
        break $m614$match;
      }
      $rt.unreachable();
    }
    const $m618 = target;
    $m618$match: {
      if ($m618.tag === "Some") {
        const value = $m618.value;
        const [, targets$185] = $std_list.push({ b: targets, x: value });
        targets = targets$185;
        break $m618$match;
      }
      if ($m618.tag === "None") {
        const [$r619, l$186] = tmp({ l: l, prefix: "x" });
        l = l$186;
        const [, targets$187] = $std_list.push({ b: targets, x: { name: $r619, ty: { tag: "ErrorT" } } });
        targets = targets$187;
        break $m618$match;
      }
      $rt.unreachable();
    }
  }
  let result_target = { tag: "None" };
  if (!discard) {
    const [$r623, l$188] = tmp({ l: l, prefix: "r" });
    l = l$188;
    result_target = { tag: "Some", value: { name: $r623, ty: ty } };
  }
  const [, l$189] = push_pre({ l: l, s: { tag: "CallInout", result_target: result_target, call: call_ir, targets: $std_list.finish({ b: targets }) } });
  l = l$189;
  const $m627 = result_target;
  $m627$match: {
    if ($m627.tag === "None") {
      return [{ tag: "UnitE" }, l];
      break $m627$match;
    }
    if ($m627.tag === "Some") {
      const value = $m627.value;
      return [{ tag: "Local", name: value.name, ty: ty }, l];
      break $m627$match;
    }
    $rt.unreachable();
  }
}

export function const_literal({ ctx, v }) {
  const $m630 = v;
  $m630$match: {
    if ($m630.tag === "SymV") {
      const def = $m630.def;
      return { tag: "Local", name: $context.get_def({ ctx: ctx, id: def }).name, ty: { tag: "ErrorT" } };
      break $m630$match;
    }
    if (true) {
      return { tag: "ConstE", value: v };
      break $m630$match;
    }
    $rt.unreachable();
  }
}

export function dict_for({ l, ctx, iface, target }) {
  const s = $types.strip({ t: target });
  if (s.tag === "ParamT") {
    const $m634 = l.fn_ctx;
    $m634$match: {
      if ($m634.tag === "Some") {
        const value = $m634.value;
        const dn = $std_map.find({ d: value.dicts, key: $types.def_of({ t: s }) });
        if (dn.tag === "Some") {
          return { tag: "DictParamE", name: text_or({ o: dn, dflt: "" }) };
        }
        break $m634$match;
      }
      if ($m634.tag === "None") {
        skip({  });
        break $m634$match;
      }
      $rt.unreachable();
    }
  }
  const iface_def = $context.get_def({ ctx: ctx, id: iface });
  const want = type_slug({ fuel: 1000, ctx: ctx, t: s });
  const $hi636 = ctx.module_count;
  for (let mid = 0; mid < $hi636; mid++) {
    const $m637 = $std_map.find({ d: ctx.modules, key: mid });
    $m637$match: {
      if ($m637.tag === "None") {
        skip({  });
        break $m637$match;
      }
      if ($m637.tag === "Some") {
        const value = $m637.value;
        for (const item of value.tree.items) {
          const $m638 = item;
          $m638$match: {
            if ($m638.tag === "ImplItem") {
              const decl = $m638.decl;
              const k = $defs.node_key({ file: value.file, tag: $defs.tag_item, span: decl.span });
              if (ref_def({ ctx: ctx, k: k }) === iface) {
                const impl_def = or_neg({ o: $std_map.find({ d: ctx.def_of, key: k }) });
                const target_type = impl_target({ ctx: ctx, impl_def: impl_def });
                if (type_slug({ fuel: 1000, ctx: ctx, t: target_type }) === want) {
                  return { tag: "DictE", iface: iface_def, target: target_type, mod: mid, name: impl_dict_name({ ctx: ctx, iface: iface_def, target: target_type }) };
                }
              }
              break $m638$match;
            }
            if (true) {
              skip({  });
              break $m638$match;
            }
            $rt.unreachable();
          }
        }
        break $m637$match;
      }
      $rt.unreachable();
    }
  }
  return { tag: "UnitE" };
}

export function dispatch({ l, ctx, iface, fn_def, call_key, e }) {
  const targs = args_or({ o: $std_map.find({ d: ctx.instantiations, key: call_key }) });
  let target = { tag: "None" };
  const n = $std_list.len({ xs: targs });
  if (n > 0) {
    const $m642 = $std_list.get({ xs: targs, i: $rt.int.sub(n, 1, $ob116) });
    $m642$match: {
      if ($m642.tag === "TypeA") {
        const ty = $m642.ty;
        target = { tag: "Some", value: ty };
        break $m642$match;
      }
      if (true) {
        skip({  });
        break $m642$match;
      }
      $rt.unreachable();
    }
  }
  let target_is_param = false;
  const $m644 = target;
  $m644$match: {
    if ($m644.tag === "Some") {
      const value = $m644.value;
      target_is_param = $types.strip({ t: value }).tag === "ParamT";
      break $m644$match;
    }
    if ($m644.tag === "None") {
      skip({  });
      break $m644$match;
    }
    $rt.unreachable();
  }
  const $m645 = $std_map.find({ d: l.impl_fns, key: fn_def.id });
  $m645$match: {
    if ($m645.tag === "Some") {
      const value = $m645.value;
      if (target.tag === "Some" && target_is_param) {
        return { tag: "FnTarget", def: value.def, name: value.name };
      }
      break $m645$match;
    }
    if ($m645.tag === "None") {
      skip({  });
      break $m645$match;
    }
    $rt.unreachable();
  }
  if (target.tag === "None") {
    return { tag: "DictTarget", dict: { tag: "UnitE" }, name: fn_def.name };
  }
  if (!target_is_param) {
    const $m649 = $claimcheck.callee_of({ ctx: ctx, file: l.file, e: e });
    $m649$match: {
      if ($m649.tag === "ImplCallee") {
        const def = $m649.def;
        const d = $context.get_def({ ctx: ctx, id: def });
        return { tag: "FnTarget", def: d, name: emitted_name({ ctx: ctx, def: d }) };
        break $m649$match;
      }
      if (true) {
        skip({  });
        break $m649$match;
      }
      $rt.unreachable();
    }
  }
  return { tag: "DictTarget", dict: dict_for({ l: l, ctx: ctx, iface: iface, target: type_or({ o: target }) }), name: fn_def.name };
}

export function unary({ fuel, l, ctx, e }) {
  if (fuel === 0) {
    return [{ tag: "UnitE" }, l];
  }
  const $m653 = e;
  $m653$match: {
    if ($m653.tag === "Unary") {
      const op = $m653.op;
      const operand = $m653.operand;
      const span = $m653.span;
      const [$r654, l$190] = expr({ fuel: $rt.int.sub(fuel, 1, $ob117), l: l, ctx: ctx, e: operand, discard: false });
      l = l$190;
      const v = $r654;
      if (op === "not") {
        return [{ tag: "Not", operand: v }, l];
      }
      if (is_int_like({ t: type_of_expr({ l: l, ctx: ctx, e: operand }) })) {
        let def_name = "?";
        const $m656 = l.fn_ctx;
        $m656$match: {
          if ($m656.tag === "Some") {
            const value = $m656.value;
            if (value.def.tag === "Some") {
              def_name = def_or({ o: value.def }).name;
            }
            break $m656$match;
          }
          if ($m656.tag === "None") {
            skip({  });
            break $m656$match;
          }
          $rt.unreachable();
        }
        return [{ tag: "Neg", operand: v, is_float: false, ob: { tag: "Some", value: ob_ref({ l: l, ctx: ctx, kind: "overflow", text: "-" + $printer.print_expr({ e: operand }) + " within Int", file: l.file, span: span, def: def_name }) } }, l];
      }
      return [{ tag: "Neg", operand: v, is_float: true, ob: { tag: "None" } }, l];
      break $m653$match;
    }
    if (true) {
      return [{ tag: "UnitE" }, l];
      break $m653$match;
    }
    $rt.unreachable();
  }
}

export function def_or({ o }) {
  const $m662 = o;
  $m662$match: {
    if ($m662.tag === "Some") {
      const value = $m662.value;
      return value;
      break $m662$match;
    }
    if ($m662.tag === "None") {
      return { id: $rt.int.neg(1, $ob118), kind: { tag: "Fn" }, name: "?", node: { tag: "NoNode" }, mod: $rt.int.neg(1, $ob119), file: $rt.int.neg(1, $ob120), key: $rt.int.neg(1, $ob121), span: { start: 0, end: 0 }, is_pub: false, is_sealed: false, is_intrinsic: false, parent: { tag: "None" }, frame: $rt.int.neg(1, $ob122), is_inout: false };
      break $m662$match;
    }
    $rt.unreachable();
  }
}

export function binary({ fuel, l, ctx, e }) {
  if (fuel === 0) {
    return [{ tag: "UnitE" }, l];
  }
  const $m669 = e;
  $m669$match: {
    if ($m669.tag === "Binary") {
      const op = $m669.op;
      const left = $m669.left;
      const right = $m669.right;
      const span = $m669.span;
      const lt = type_of_expr({ l: l, ctx: ctx, e: left });
      const s = $types.strip({ t: lt });
      const [$r670, l$191] = expr({ fuel: $rt.int.sub(fuel, 1, $ob123), l: l, ctx: ctx, e: left, discard: false });
      l = l$191;
      const left_ir = $r670;
      const [$r671, l$192] = expr({ fuel: $rt.int.sub(fuel, 1, $ob124), l: l, ctx: ctx, e: right, discard: false });
      l = l$192;
      const right_ir = $r671;
      if (op === "+" || op === "-" || op === "*" || op === "/" || op === "%") {
        if (is_int_like({ t: lt })) {
          let ob = { tag: "None" };
          const $m673 = find_ob({ ctx: ctx, k: key({ l: l, tag: $defs.tag_expr, span: span }), kind: { tag: "Overflow" } });
          $m673$match: {
            if ($m673.tag === "Some") {
              const value = $m673.value;
              if (is_checked({ o: value })) {
                ob = { tag: "Some", value: ob_ref_of({ l: l, ctx: ctx, o: value }) };
              }
              break $m673$match;
            }
            if ($m673.tag === "None") {
              skip({  });
              break $m673$match;
            }
            $rt.unreachable();
          }
          return [{ tag: "IntOp", op: op, left: left_ir, right: right_ir, ob: ob }, l];
        }
        return [{ tag: "FloatOp", op: op, left: left_ir, right: right_ir }, l];
      }
      if (op === "++") {
        return [{ tag: "Concat", left: left_ir, right: right_ir, is_text: s.tag === "Prim" && $types.prim_name({ t: s }) === "Text", ty: type_of_expr({ l: l, ctx: ctx, e: e }) }, l];
      }
      if (op === "==" || op === "!=") {
        return [{ tag: "Eq", left: left_ir, right: right_ir, ty: lt, prim: s.tag === "Prim", negate: op === "!=" }, l];
      }
      if (op === "<" || op === "<=" || op === ">" || op === ">=") {
        return [{ tag: "Cmp", op: op, left: left_ir, right: right_ir, is_float: s.tag === "Prim" && $types.prim_name({ t: s }) === "Float" }, l];
      }
      return [{ tag: "Implies", left: left_ir, right: right_ir }, l];
      break $m669$match;
    }
    if (true) {
      return [{ tag: "UnitE" }, l];
      break $m669$match;
    }
    $rt.unreachable();
  }
}

export function assertion_block({ l, ctx, b }) {
  let out = [];
  for (const s of b.stmts) {
    const $m683 = s;
    $m683$match: {
      if ($m683.tag === "ExprStmt") {
        const expr = $m683.expr;
        const [$r684, l$193] = begin_collect({ l: l });
        l = l$193;
        const saved = $r684;
        const [$r685, l$194] = expr_of({ fuel: max_depth, l: l, ctx: ctx, e: expr, discard: false });
        l = l$194;
        const cond = $r685;
        const [$r687, l$195] = end_collect({ l: l, saved: saved, out: [{ tag: "Assert", cond: cond }] });
        l = l$195;
        out = [...out, ...$r687];
        break $m683$match;
      }
      if (true) {
        const [$r688, l$196] = stmt({ fuel: max_depth, l: l, ctx: ctx, s: s });
        l = l$196;
        out = [...out, ...$r688];
        break $m683$match;
      }
      $rt.unreachable();
    }
  }
  return [out, l];
}

export function tests_of({ l, ctx }) {
  let examples = $std_list.builder({  });
  let properties = $std_list.builder({  });
  for (const item of l.m.tree.items) {
    const $m689 = item;
    $m689$match: {
      if ($m689.tag === "ExampleItem") {
        const decl = $m689.decl;
        l = { ...l, fn_ctx: { tag: "None" } };
        const [$r692, l$197] = assertion_block({ l: l, ctx: ctx, b: decl.body });
        l = l$197;
        const [, examples$198] = $std_list.push({ b: examples, x: { name: decl.name.text, body: $r692 } });
        examples = examples$198;
        break $m689$match;
      }
      if ($m689.tag === "PropertyItem") {
        const decl = $m689.decl;
        l = { ...l, fn_ctx: { tag: "None" } };
        const negate = def_at({ l: l, ctx: ctx, tag: $defs.tag_item, span: decl.span }) === or_neg({ o: l.opts.negate_guard });
        const [$r696, l$199] = gen_params({ l: l, ctx: ctx, params: decl.params, subst: $std_map.dict({  }), negate: negate });
        l = l$199;
        const params = $r696;
        const [$r697, l$200] = assertion_block({ l: l, ctx: ctx, b: decl.body });
        l = l$200;
        const [, properties$201] = $std_list.push({ b: properties, x: { label: "property " + decl.name.text, params: params, body: $r697 } });
        properties = properties$201;
        break $m689$match;
      }
      if ($m689.tag === "ImplItem") {
        const decl = $m689.decl;
        const impl_key = key({ l: l, tag: $defs.tag_item, span: decl.span });
        const iface_id = ref_def({ ctx: ctx, k: impl_key });
        if (iface_id >= 0) {
          const iface = $context.get_def({ ctx: ctx, id: iface_id });
          const impl_def = or_neg({ o: $std_map.find({ d: ctx.def_of, key: impl_key }) });
          const target = impl_target({ ctx: ctx, impl_def: impl_def });
          let subst = $std_map.dict({  });
          for (const d of $std_list.finish({ b: ctx.defs })) {
            if (or_neg({ o: d.parent }) === iface_id && $rt.eq(d.kind, { tag: "TypeParam" })) {
              const [, subst$202] = $std_map.set({ d: subst, key: d.id, value: { tag: "TypeA", ty: target } });
              subst = subst$202;
            }
          }
          const map = impl_map({ l: l, ctx: ctx, iface: iface_id, fns: decl.fns });
          const [, l$203, properties$203] = law_properties({ l: l, ctx: ctx, iface: iface, iface_id: iface_id, target: target, subst: subst, map: map, properties: properties });
          l = l$203;
          properties = properties$203;
        }
        break $m689$match;
      }
      if (true) {
        skip({  });
        break $m689$match;
      }
      $rt.unreachable();
    }
  }
  if ($std_list.built({ b: examples }) === 0 && $std_list.built({ b: properties }) === 0) {
    return [{ tag: "None" }, l];
  }
  return [{ tag: "Some", value: { examples: $std_list.finish({ b: examples }), properties: $std_list.finish({ b: properties }) } }, l];
}

export function law_properties({ l, ctx, iface, iface_id, target, subst, map, properties }) {
  for (const d of $std_list.finish({ b: ctx.defs })) {
    if (or_neg({ o: d.parent }) === iface_id && $rt.eq(d.kind, { tag: "Law" })) {
      const $m705 = d.node;
      $m705$match: {
        if ($m705.tag === "LawNode") {
          const decl = $m705.decl;
          l = { ...l, impl_fns: map, fn_ctx: { tag: "None" } };
          const [$r708, l$204] = gen_params({ l: l, ctx: ctx, params: decl.params, subst: subst, negate: false });
          l = l$204;
          const params = $r708;
          const [$r709, l$205] = assertion_block({ l: l, ctx: ctx, b: decl.body });
          l = l$205;
          const [, properties$206] = $std_list.push({ b: properties, x: { label: "law " + iface.name + "[" + type_slug({ fuel: 1000, ctx: ctx, t: target }) + "]." + d.name, params: params, body: $r709 } });
          properties = properties$206;
          l = { ...l, impl_fns: $std_map.dict({  }) };
          break $m705$match;
        }
        if (true) {
          skip({  });
          break $m705$match;
        }
        $rt.unreachable();
      }
    }
  }
  return [undefined, l, properties];
  return [undefined, l, properties];
}

export function gen_params({ l, ctx, params, subst, negate }) {
  let out = $std_list.builder({  });
  for (const p of params) {
    const pd = def_at({ l: l, ctx: ctx, tag: $defs.tag_sig, span: p.span });
    let ty = { tag: "ErrorT" };
    if (pd >= 0) {
      ty = type_or({ o: $std_map.find({ d: ctx.decl_types, key: pd }) });
    }
    const [$r713, l$207] = generator({ fuel: max_depth, l: l, ctx: ctx, t: $types.substitute({ t: ty, subst: subst }), negate: negate });
    l = l$207;
    const [, out$208] = $std_list.push({ b: out, x: { name: p.name.text, gen: $r713 } });
    out = out$208;
  }
  return [$std_list.finish({ b: out }), l];
}

export function generator({ fuel, l, ctx, t, negate }) {
  if (fuel === 0) {
    return [{ base: { tag: "UnknownG" }, filters: [] }, l];
  }
  const [$r717, l$209] = tmp({ l: l, prefix: "it" });
  l = l$209;
  const it_name = $r717;
  const local = { tag: "Local", name: it_name, ty: t };
  let filters = $std_list.builder({  });
  for (const p of refinement_preds({ ctx: ctx, t: t })) {
    const [$r719, l$210] = with_it({ fuel: fuel, l: l, ctx: ctx, it_expr: local, pred: p });
    l = l$210;
    let cond = $r719;
    if (negate) {
      cond = { tag: "Not", operand: cond };
    }
    const [, filters$211] = $std_list.push({ b: filters, x: { it_name: it_name, cond: cond } });
    filters = filters$211;
  }
  const [$r722, l$212] = base_generator({ fuel: $rt.int.sub(fuel, 1, $ob125), l: l, ctx: ctx, s: $types.strip({ t: t }) });
  l = l$212;
  return [{ base: $r722, filters: $std_list.finish({ b: filters }) }, l];
}

export function base_generator({ fuel, l, ctx, s }) {
  if (fuel === 0) {
    return [{ tag: "UnknownG" }, l];
  }
  const $m725 = s;
  $m725$match: {
    if ($m725.tag === "Prim") {
      const name = $m725.name;
      if (name === "Int") {
        return [{ tag: "IntG" }, l];
      }
      if (name === "Float") {
        return [{ tag: "FloatG" }, l];
      }
      if (name === "Duration") {
        return [{ tag: "DurationG" }, l];
      }
      if (name === "Bool") {
        return [{ tag: "BoolG" }, l];
      }
      if (name === "Text") {
        return [{ tag: "TextG" }, l];
      }
      if (name === "Unit") {
        return [{ tag: "UnitG" }, l];
      }
      return [{ tag: "UnknownG" }, l];
      break $m725$match;
    }
    if ($m725.tag === "Opaque") {
      const def = $m725.def;
      const args = $m725.args;
      if ($context.qualified_name({ ctx: ctx, id: def }) === "std.list.List" && $std_list.len({ xs: args }) > 0) {
        const $m733 = $std_list.get({ xs: args, i: 0 });
        $m733$match: {
          if ($m733.tag === "TypeA") {
            const ty = $m733.ty;
            const [$r734, l$213] = generator({ fuel: $rt.int.sub(fuel, 1, $ob126), l: l, ctx: ctx, t: ty, negate: false });
            l = l$213;
            return [{ tag: "ListG", elem: $r734 }, l];
            break $m733$match;
          }
          if (true) {
            return [{ tag: "UnknownG" }, l];
            break $m733$match;
          }
          $rt.unreachable();
        }
      }
      return [{ tag: "UnknownG" }, l];
      break $m725$match;
    }
    if ($m725.tag === "RecordT") {
      const def = $m725.def;
      const [$r738, l$214] = gen_fields({ fuel: $rt.int.sub(fuel, 1, $ob127), l: l, ctx: ctx, owner: def });
      l = l$214;
      return [{ tag: "RecordG", fields: $r738 }, l];
      break $m725$match;
    }
    if ($m725.tag === "UnionT") {
      const def = $m725.def;
      let variants = $std_list.builder({  });
      for (const v of ids_or({ o: $std_map.find({ d: ctx.variants, key: def }) })) {
        const [$r740, l$215] = gen_fields({ fuel: $rt.int.sub(fuel, 1, $ob128), l: l, ctx: ctx, owner: v });
        l = l$215;
        const [, variants$216] = $std_list.push({ b: variants, x: { name: $context.get_def({ ctx: ctx, id: v }).name, fields: $r740 } });
        variants = variants$216;
      }
      return [{ tag: "UnionG", variants: $std_list.finish({ b: variants }) }, l];
      break $m725$match;
    }
    if (true) {
      return [{ tag: "UnknownG" }, l];
      break $m725$match;
    }
    $rt.unreachable();
  }
}

export function gen_fields({ fuel, l, ctx, owner }) {
  if (fuel === 0) {
    return [[], l];
  }
  let out = $std_list.builder({  });
  for (const f of fields_or({ o: $std_map.find({ d: ctx.fields, key: owner }) })) {
    const [$r744, l$217] = generator({ fuel: $rt.int.sub(fuel, 1, $ob129), l: l, ctx: ctx, t: f.ty, negate: false });
    l = l$217;
    const [, out$218] = $std_list.push({ b: out, x: { name: f.name, gen: $r744 } });
    out = out$218;
  }
  return [$std_list.finish({ b: out }), l];
}

export function verifies_of({ l, ctx }) {
  let out = $std_list.builder({  });
  let fns = $std_list.builder({  });
  for (const item of l.m.tree.items) {
    const $m746 = item;
    $m746$match: {
      if ($m746.tag === "FnItem") {
        const decl = $m746.decl;
        const [, fns$219] = $std_list.push({ b: fns, x: decl });
        fns = fns$219;
        break $m746$match;
      }
      if ($m746.tag === "ImplItem") {
        const decl = $m746.decl;
        for (const f of decl.fns) {
          const [, fns$220] = $std_list.push({ b: fns, x: f });
          fns = fns$220;
        }
        break $m746$match;
      }
      if (true) {
        skip({  });
        break $m746$match;
      }
      $rt.unreachable();
    }
  }
  for (const f of $std_list.finish({ b: fns })) {
    const $m747 = f.body;
    $m747$match: {
      if ($m747.tag === "None") {
        skip({  });
        break $m747$match;
      }
      if ($m747.tag === "Some") {
        const value = $m747.value;
        const fn_id = def_at({ l: l, ctx: ctx, tag: $defs.tag_item, span: f.span });
        for (const n of $walk.nodes_in_block({ b: value, into_verify: false })) {
          const $m748 = n;
          $m748$match: {
            if ($m748.tag === "NStmt") {
              const s = $m748.s;
              const $m749 = s;
              $m749$match: {
                if ($m749.tag === "Assume") {
                  const verify_block = $m749.verify_block;
                  const span = $m749.span;
                  if (verify_block.tag === "Some") {
                    const [, l$221, out$221] = verify_of({ l: l, ctx: ctx, fn_id: fn_id, v: verify_or({ o: verify_block }), at: span, out: out });
                    l = l$221;
                    out = out$221;
                  }
                  break $m749$match;
                }
                if (true) {
                  skip({  });
                  break $m749$match;
                }
                $rt.unreachable();
              }
              break $m748$match;
            }
            if (true) {
              skip({  });
              break $m748$match;
            }
            $rt.unreachable();
          }
        }
        break $m747$match;
      }
      $rt.unreachable();
    }
  }
  return [$std_list.finish({ b: out }), l];
}

export function verify_or({ o }) {
  const $m750 = o;
  $m750$match: {
    if ($m750.tag === "Some") {
      const value = $m750.value;
      return value;
      break $m750$match;
    }
    if ($m750.tag === "None") {
      return { params: [], effects: [], body: { stmts: [], elided: false, span: { start: 0, end: 0 } }, span: { start: 0, end: 0 } };
      break $m750$match;
    }
    $rt.unreachable();
  }
}

export function verify_of({ l, ctx, fn_id, v, at, out }) {
  const node_key = key({ l: l, tag: $defs.tag_stmt, span: at });
  let site = { tag: "None" };
  for (const a of $claimcheck.assumes_of({ ctx: ctx, fn_def: fn_id })) {
    if (a.node === node_key && site.tag === "None") {
      site = { tag: "Some", value: a };
    }
  }
  const vd = def_at({ l: l, ctx: ctx, tag: $defs.tag_misc, span: v.span });
  if (site.tag === "None" || vd < 0) {
    return [undefined, l, out];
  }
  const a = site_or({ o: site });
  let params = $std_list.builder({  });
  for (const p of v.params) {
    const pd = def_at({ l: l, ctx: ctx, tag: $defs.tag_sig, span: p.span });
    if (pd >= 0) {
      const $m757 = $types.strip({ t: type_or({ o: $std_map.find({ d: ctx.decl_types, key: pd }) }) });
      $m757$match: {
        if ($m757.tag === "Capability") {
          const def = $m757.def;
          const [, params$222] = $std_list.push({ b: params, x: { name: p.name.text, capability: def, node: key({ l: l, tag: $defs.tag_sig, span: p.span }), at_span: p.span } });
          params = params$222;
          break $m757$match;
        }
        if (true) {
          skip({  });
          break $m757$match;
        }
        $rt.unreachable();
      }
    }
  }
  const pos = $loc.line_col({ tables: l.tables, file: l.file, offset: at.start });
  const vctx = { inout_names: [], ret: { tag: "Prim", name: "Bool" }, post: [], def: { tag: "Some", value: $context.get_def({ ctx: ctx, id: vd }) }, dicts: $std_map.dict({  }), result_expr: { tag: "None" }, measure: { tag: "None" } };
  l = { ...l, fn_ctx: { tag: "Some", value: vctx }, verifying: true };
  const [$r766, l$223] = assertion_block({ l: l, ctx: ctx, b: v.body });
  l = l$223;
  const body = $r766;
  l = { ...l, verifying: false, fn_ctx: { tag: "None" } };
  const owner = $context.qualified_name({ ctx: ctx, id: a.fn_def });
  const [, out$224] = $std_list.push({ b: out, x: { name: "verify$" + $std_int.to_text({ x: $std_list.built({ b: out }) }), key: a.key, claim: $context.qualified_name({ ctx: ctx, id: a.claim_def }), def: owner, at: owner + ":" + $std_int.to_text({ x: pos.line }) + ":" + $std_int.to_text({ x: pos.col }), params: $std_list.finish({ b: params }), body: body, early_return: block_has_try({ b: v.body }) } });
  out = out$224;
  return [undefined, l, out];
  return [undefined, l, out];
}

export function site_or({ o }) {
  const $m770 = o;
  $m770$match: {
    if ($m770.tag === "Some") {
      const value = $m770.value;
      return value;
      break $m770$match;
    }
    if ($m770.tag === "None") {
      return { fn_def: $rt.int.neg(1, $ob130), claim_def: $rt.int.neg(1, $ob131), justification: "", node: $rt.int.neg(1, $ob132), at_file: $rt.int.neg(1, $ob133), at_span: { start: 0, end: 0 }, verify_key: { tag: "None" }, verify_block: { tag: "None" }, key: "" };
      break $m770$match;
    }
    $rt.unreachable();
  }
}

export function block_has_try({ b }) {
  for (const s of b.stmts) {
    if (stmt_has_try({ s: s })) {
      return true;
    }
  }
  return false;
}

export function stmt_has_try({ s }) {
  const $m775 = s;
  $m775$match: {
    if ($m775.tag === "Let") {
      const value = $m775.value;
      return expr_has_try({ e: value });
      break $m775$match;
    }
    if ($m775.tag === "Var") {
      const value = $m775.value;
      return expr_has_try({ e: value });
      break $m775$match;
    }
    if ($m775.tag === "Assign") {
      const value = $m775.value;
      return expr_has_try({ e: value });
      break $m775$match;
    }
    if ($m775.tag === "Return") {
      const value = $m775.value;
      return expr_has_try({ e: value });
      break $m775$match;
    }
    if ($m775.tag === "If") {
      const cond = $m775.cond;
      const then_block = $m775.then_block;
      const else_block = $m775.else_block;
      if (expr_has_try({ e: cond }) || block_has_try({ b: then_block })) {
        return true;
      }
      const $m776 = else_block;
      $m776$match: {
        if ($m776.tag === "Some") {
          const value = $m776.value;
          return block_has_try({ b: value });
          break $m776$match;
        }
        if ($m776.tag === "None") {
          return false;
          break $m776$match;
        }
        $rt.unreachable();
      }
      break $m775$match;
    }
    if ($m775.tag === "Match") {
      const scrutinee = $m775.scrutinee;
      const arms = $m775.arms;
      if (expr_has_try({ e: scrutinee })) {
        return true;
      }
      for (const a of arms) {
        const $m777 = a.guard;
        $m777$match: {
          if ($m777.tag === "Some") {
            const value = $m777.value;
            if (expr_has_try({ e: value })) {
              return true;
            }
            break $m777$match;
          }
          if ($m777.tag === "None") {
            skip({  });
            break $m777$match;
          }
          $rt.unreachable();
        }
        const $m778 = a.body;
        $m778$match: {
          if ($m778.tag === "ArmBlock") {
            const block = $m778.block;
            if (block_has_try({ b: block })) {
              return true;
            }
            break $m778$match;
          }
          if ($m778.tag === "ArmStmt") {
            const stmt = $m778.stmt;
            if (stmt_has_try({ s: stmt })) {
              return true;
            }
            break $m778$match;
          }
          $rt.unreachable();
        }
      }
      return false;
      break $m775$match;
    }
    if ($m775.tag === "Loop") {
      const cond = $m775.cond;
      const clauses = $m775.clauses;
      const body = $m775.body;
      if (expr_has_try({ e: cond })) {
        return true;
      }
      for (const c of clauses) {
        if (expr_has_try({ e: c.expr })) {
          return true;
        }
      }
      return block_has_try({ b: body });
      break $m775$match;
    }
    if ($m775.tag === "For") {
      const domain = $m775.domain;
      const body = $m775.body;
      const $m779 = domain;
      $m779$match: {
        if ($m779.tag === "RangeDomain") {
          const lo = $m779.lo;
          const hi = $m779.hi;
          if (expr_has_try({ e: lo }) || expr_has_try({ e: hi })) {
            return true;
          }
          break $m779$match;
        }
        if ($m779.tag === "InDomain") {
          const expr = $m779.expr;
          if (expr_has_try({ e: expr })) {
            return true;
          }
          break $m779$match;
        }
        $rt.unreachable();
      }
      return block_has_try({ b: body });
      break $m775$match;
    }
    if ($m775.tag === "Assume") {
      return false;
      break $m775$match;
    }
    if ($m775.tag === "ExprStmt") {
      const expr = $m775.expr;
      return expr_has_try({ e: expr });
      break $m775$match;
    }
    $rt.unreachable();
  }
}

export function exprs_have_try({ es }) {
  for (const e of es) {
    if (expr_has_try({ e: e })) {
      return true;
    }
  }
  return false;
}

export function inits_have_try({ fs }) {
  for (const f of fs) {
    if (expr_has_try({ e: f.value })) {
      return true;
    }
  }
  return false;
}

export function args_have_try({ args }) {
  for (const a of args) {
    if (expr_has_try({ e: a.value })) {
      return true;
    }
  }
  return false;
}

export function expr_has_try({ e }) {
  const $m780 = e;
  $m780$match: {
    if ($m780.tag === "Try") {
      return true;
      break $m780$match;
    }
    if ($m780.tag === "Ctor") {
      const args = $m780.args;
      const fields = $m780.fields;
      const $m781 = args;
      $m781$match: {
        if ($m781.tag === "Some") {
          const value = $m781.value;
          if (args_have_try({ args: value })) {
            return true;
          }
          break $m781$match;
        }
        if ($m781.tag === "None") {
          skip({  });
          break $m781$match;
        }
        $rt.unreachable();
      }
      const $m782 = fields;
      $m782$match: {
        if ($m782.tag === "Some") {
          const value = $m782.value;
          return inits_have_try({ fs: value });
          break $m782$match;
        }
        if ($m782.tag === "None") {
          return false;
          break $m782$match;
        }
        $rt.unreachable();
      }
      break $m780$match;
    }
    if ($m780.tag === "RecordUpdate") {
      const base = $m780.base;
      const fields = $m780.fields;
      return expr_has_try({ e: base }) || inits_have_try({ fs: fields });
      break $m780$match;
    }
    if ($m780.tag === "ListLit") {
      const elems = $m780.elems;
      return exprs_have_try({ es: elems });
      break $m780$match;
    }
    if ($m780.tag === "Recover") {
      const body = $m780.body;
      return block_has_try({ b: body });
      break $m780$match;
    }
    if ($m780.tag === "Quantifier") {
      const domain = $m780.domain;
      const where_ = $m780.where_;
      const body = $m780.body;
      const $m783 = domain;
      $m783$match: {
        if ($m783.tag === "Some") {
          const value = $m783.value;
          const $m784 = value;
          $m784$match: {
            if ($m784.tag === "RangeDomain") {
              const lo = $m784.lo;
              const hi = $m784.hi;
              if (expr_has_try({ e: lo }) || expr_has_try({ e: hi })) {
                return true;
              }
              break $m784$match;
            }
            if ($m784.tag === "InDomain") {
              const expr = $m784.expr;
              if (expr_has_try({ e: expr })) {
                return true;
              }
              break $m784$match;
            }
            $rt.unreachable();
          }
          break $m783$match;
        }
        if ($m783.tag === "None") {
          skip({  });
          break $m783$match;
        }
        $rt.unreachable();
      }
      const $m785 = where_;
      $m785$match: {
        if ($m785.tag === "Some") {
          const value = $m785.value;
          if (expr_has_try({ e: value })) {
            return true;
          }
          break $m785$match;
        }
        if ($m785.tag === "None") {
          skip({  });
          break $m785$match;
        }
        $rt.unreachable();
      }
      return expr_has_try({ e: body });
      break $m780$match;
    }
    if ($m780.tag === "Closure") {
      return false;
      break $m780$match;
    }
    if ($m780.tag === "Fake") {
      const fields = $m780.fields;
      return inits_have_try({ fs: fields });
      break $m780$match;
    }
    if ($m780.tag === "FieldAccess") {
      const object = $m780.object;
      return expr_has_try({ e: object });
      break $m780$match;
    }
    if ($m780.tag === "Call") {
      const callee = $m780.callee;
      const args = $m780.args;
      return expr_has_try({ e: callee }) || args_have_try({ args: args });
      break $m780$match;
    }
    if ($m780.tag === "Unary") {
      const operand = $m780.operand;
      return expr_has_try({ e: operand });
      break $m780$match;
    }
    if ($m780.tag === "Binary") {
      const left = $m780.left;
      const right = $m780.right;
      return expr_has_try({ e: left }) || expr_has_try({ e: right });
      break $m780$match;
    }
    if ($m780.tag === "And") {
      const operands = $m780.operands;
      return exprs_have_try({ es: operands });
      break $m780$match;
    }
    if ($m780.tag === "Or") {
      const operands = $m780.operands;
      return exprs_have_try({ es: operands });
      break $m780$match;
    }
    if ($m780.tag === "Is") {
      const expr = $m780.expr;
      return expr_has_try({ e: expr });
      break $m780$match;
    }
    if (true) {
      return false;
      break $m780$match;
    }
    $rt.unreachable();
  }
}

